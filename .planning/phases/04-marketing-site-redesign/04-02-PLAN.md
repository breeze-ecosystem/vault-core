---
phase: 04-marketing-site-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/marketing/components/shared/glass-panel.tsx
  - apps/marketing/components/shared/gradient-border.tsx
  - apps/marketing/components/shared/eyebrow-tag.tsx
  - apps/marketing/components/shared/noise-overlay.tsx
  - apps/marketing/components/shared/stats-counter.tsx
  - apps/marketing/components/ui/button.tsx
  - apps/marketing/components/ui/animated-section.tsx
autonomous: true
requirements: [MKT-01]
user_setup: []

must_haves:
  truths:
    - "GlassPanel component renders with backdrop-blur-xl and semi-transparent background"
    - "Button component has a `glass` variant for dark-on-dark CTA usage"
    - "AnimatedSection uses motion whileInView with premium cubic-bezier easing"
    - "StatsCounter animates from 0 to final value on scroll into view"
    - "GradientBorder wraps children with animated border gradient effect"
    - "NoiseOverlay renders as a fixed position grain texture layer"
  artifacts:
    - path: "apps/marketing/components/shared/glass-panel.tsx"
      provides: "Reusable glassmorphism card component"
      min_lines: 30
    - path: "apps/marketing/components/shared/stats-counter.tsx"
      provides: "Animated number counter with useInView + motion"
      min_lines: 40
    - path: "apps/marketing/components/ui/button.tsx"
      provides: "Button with added glass variant"
      contains: "glass"
    - path: "apps/marketing/components/ui/animated-section.tsx"
      provides: "Scroll-triggered fade-up using motion whileInView"
      uses: "motion/react"
  key_links:
    - from: "stats-counter.tsx"
      to: "motion/react"
      via: "useInView + animate with spring/bounce easing"
    - from: "button.tsx"
      to: "glass-panel.tsx"
      via: "shared glass aesthetic pattern (backdrop-blur, border-white/5)"
---

<objective>
Create the shared design component library that embodies the glassmorphism premium aesthetic — reusable building blocks consumed by all subsequent plans.

Purpose: GlassPanel, GradientBorder, EyebrowTag, NoiseOverlay, and StatsCounter are used by landing sections, product pages, solutions pages, case studies, and the demo tour. Building them once prevents duplicate glass styles across 9+ later plans.
Output: 5 new shared components, Button updated with glass variant, AnimatedSection rewritten to use motion whileInView.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-marketing-site-redesign/04-CONTEXT.md
@.planning/phases/04-marketing-site-redesign/04-RESEARCH.md
@.planning/phases/04-marketing-site-redesign/04-UI-SPEC.md

## Key References
- **UI-SPEC Colors section**: Glass value `rgba(12,16,32,0.7)`, border white/6 at 0.06 opacity
- **UI-SPEC Interaction & Animation Contract**: Glass panel styling (`bg-white/[0.03] backdrop-blur-xl border border-white/[0.06] rounded-2xl`), hover scale `1.02`, stats counter animation from 0 to value
- **RESEARCH.md Patterns 2-4**: AnimatedSection pattern (motion whileInView), GlassPanel pattern, Staggered Children pattern
- **Existing ui/button.tsx**: Current cva structure with primary/secondary/ghost variants — add glass variant, do not remove existing variants
</context>

<interfaces>
<!-- Existing patterns the executor must follow -->

From AnimatedSection (current - apps/marketing/components/ui/animated-section.tsx):
```
Uses: motion `useInView` ref + CSS transitions (not motion components)
Interface: { children, className?, as?: 'section' | 'div' }
Needs to change: replace CSS transition with <motion.div> whileInView
```

From Button (current - apps/marketing/components/ui/button.tsx):
```
cva base classes + variants: { primary, secondary, ghost }
size variants: { sm, md, lg, xl }
Interface: ButtonProps extends ButtonHTMLAttributes, VariantProps<typeof buttonVariants>
Added isLoading spinner pattern
```

From UI-SPEC Glass Panel pattern (RESEARCH.md):
```
rounded-2xl border border-white/[0.06] bg-white/[0.03] backdrop-blur-xl
hover: transition-all duration-300 hover:bg-white/[0.06] hover:border-white/[0.10] hover:scale-[1.02]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create GlassPanel, GradientBorder, EyebrowTag, NoiseOverlay</name>
  <files>
    apps/marketing/components/shared/glass-panel.tsx
    apps/marketing/components/shared/gradient-border.tsx
    apps/marketing/components/shared/eyebrow-tag.tsx
    apps/marketing/components/shared/noise-overlay.tsx
  </files>
  <read_first>
    - .planning/phases/04-marketing-site-redesign/04-RESEARCH.md (Pattern 3: GlassPanel — exact styling, Pattern 4: Staggered — for reference)
    - .planning/phases/04-marketing-site-redesign/04-UI-SPEC.md (Interaction & Animation Contract for glassmorphism values, Colors for glass/token values)
  </read_first>
  <action>
    Create all four components in `apps/marketing/components/shared/`. Ensure the `shared/` directory exists first.

    **A. Create `components/shared/glass-panel.tsx`:**
    - `'use client'` component
    - Props: `{ children: React.ReactNode; className?: string; hover?: boolean; as?: 'div' | 'section' | 'article' }`  
    - Default: `hover={false}`, `as="div"`
    - Render using the specified `as` tag with `cn()` merging classes:
      ```
      'rounded-2xl border border-white/[0.06] bg-white/[0.03] backdrop-blur-xl',
      hover && 'transition-all duration-300 hover:bg-white/[0.06] hover:border-white/[0.10] hover:scale-[1.02]',
      className
      ```
    - Named export: `GlassPanel`

    **B. Create `components/shared/gradient-border.tsx`:**
    - `'use client'` optional (no interactivity needed)
    - Props: `{ children: React.ReactNode; className?: string; as?: 'div' | 'section' }`
    - Wraps children in a container with:
      - `relative rounded-2xl bg-gradient-to-b from-cyan-500/20 to-transparent p-px`
      - Inner div with `rounded-2xl bg-[#070912] h-full w-full` to create border-only gradient effect
    - Accept `className` on the outer wrapper
    - Named export: `GradientBorder`

    **C. Create `components/shared/eyebrow-tag.tsx`:**
    - Server component (no client directives unless needed)
    - Props: `{ children: React.ReactNode; className?: string; variant?: 'default' | 'accent' }`
    - Styling: `inline-flex items-center gap-1.5 rounded-full px-3 py-1 text-xs font-semibold uppercase tracking-wider`
    - Variant `default`: `bg-white/[0.05] text-muted`
    - Variant `accent`: `bg-cyan-500/10 text-cyan-400`
    - Named export: `EyebrowTag`

    **D. Create `components/shared/noise-overlay.tsx`:**
    - Server component — renders a fixed `div` with `pointer-events-none`
    - Uses the `noise-overlay` class from globals.css (which should exist after Plan 04-01)
    - Props: `{ className?: string }` for optional density/opacity override
    - If class `noise-overlay` exists in globals.css (set in Plan 04-01), use `<div className={cn("fixed inset-0 z-40 pointer-events-none noise-overlay", className)} />`
    - Named export: `NoiseOverlay`
  </action>
  <verify>
    <automated>
      # Verify all 4 files exist and export components
      for f in glass-panel gradient-border eyebrow-tag noise-overlay; do
        [ -f "apps/marketing/components/shared/$f.tsx" ] && echo "✅ $f.tsx exists" || echo "❌ $f.tsx missing"
      done
      # Check each has proper export (named, not default)
      for f in glass-panel gradient-border eyebrow-tag noise-overlay; do
        grep -q "export function" "apps/marketing/components/shared/$f.tsx" && echo "✅ $f has named export" || echo "❌ $f missing named export"
      done
    </automated>
  </verify>
  <done>4 shared components created with proper exports. GlassPanel renders with backdrop-blur-xl. GradientBorder wraps children with gradient border. EyebrowTag has default/accent variants. NoiseOverlay uses fixed positioning with pointer-events:none.</done>
</task>

<task type="auto">
  <name>Task 2: Create StatsCounter + update Button + rewrite AnimatedSection</name>
  <files>
    apps/marketing/components/shared/stats-counter.tsx
    apps/marketing/components/ui/button.tsx
    apps/marketing/components/ui/animated-section.tsx
  </files>
  <read_first>
    - apps/marketing/components/ui/button.tsx (existing — add glass variant)
    - apps/marketing/components/ui/animated-section.tsx (existing — rewrite to use motion whileInView)
    - .planning/phases/04-marketing-site-redesign/04-RESEARCH.md (Pattern 2: AnimatedSection — motion whileInView pattern, Pattern: StatsCounter — useInView + animate)
    - .planning/phases/04-marketing-site-redesign/04-UI-SPEC.md (Interaction & Animation Contract section for scroll-triggered fade-up values: 700ms, cubic-bezier(0.16, 1, 0.3, 1))
  </read_first>
  <action>
    **A. Create `components/shared/stats-counter.tsx`:**
    - `'use client'` component
    - Props: `{ value: number; suffix?: string; prefix?: string; duration?: number; decimals?: number; className?: string; }`
    - Uses `motion` `useInView` ref + `animate` with the target value
    - Import `motion` from `'motion/react'`
    - Animate from 0 to `value` when in view, using `ease: [0.16, 1, 0.3, 1]` and default `duration: 2`
    - Render a `<span>` or `<motion.span>` with the animated value formatted to the specified decimals
    - Named export: `StatsCounter`
    - Note: For static rendering safety, use `useMemo` and `useState` with `useEffect` pattern

    **B. Update `components/ui/button.tsx`:**
    - Read the existing file to see its current structure (cva variants, interfaces). Do NOT rewrite the whole file.
    - Add a `glass` variant to the `buttonVariants.cva.variants.variant` object:
      ```ts
      glass: 'bg-white/[0.05] backdrop-blur-xl border border-white/[0.10] text-white hover:bg-white/[0.10] hover:border-white/[0.20] shadow-sm'
      ```
    - Keep all existing variants (primary, secondary, ghost) unchanged
    - Keep all existing sizes (sm, md, lg, xl) unchanged
    - Keep the existing `hover:scale-[1.03] active:scale-[0.97]` that's appended to the cva() call
    - Export `GlassButtonProps` is not needed — the existing `ButtonProps` type already covers variants

    **C. Rewrite `components/ui/animated-section.tsx`:**
    - Keep `'use client'` directive
    - Keep the same interface: `{ children: ReactNode; className?: string; as?: 'section' | 'div'; delay?: number }`
    - Remove CSS transition approach
    - Rewrite to use `<motion.div>` (or the appropriate `as` tag):
      ```tsx
      <motion.div
        initial={{ opacity: 0, y: 24 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true, margin: '-80px' }}
        transition={{
          duration: 0.7,
          delay,
          ease: [0.16, 1, 0.3, 1],
        }}
        className={className}
      >
        {children}
      </motion.div>
      ```
    - Handle the `as` prop by using `motion[as]` or always using `motion.div` (simpler approach)
    - Named export: `AnimatedSection`
  </action>
  <verify>
    <automated>
      # StatsCounter exists and exports
      grep -c "export function StatsCounter" apps/marketing/components/shared/stats-counter.tsx
      grep -c "from 'motion/react'" apps/marketing/components/shared/stats-counter.tsx
      # Button has glass variant
      grep -c "glass:" apps/marketing/components/ui/button.tsx
      # AnimatedSection uses motion whileInView
      grep -c "whileInView" apps/marketing/components/ui/animated-section.tsx
      grep -c "from 'motion/react'" apps/marketing/components/ui/animated-section.tsx
    </automated>
  </verify>
  <done>StatsCounter created with useInView + motion animate. Button has glass variant (backdrop-blur, semi-transparent white). AnimatedSection uses motion whileInView with premium cubic-bezier easing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | All shared components are presentational. No user input, no data fetching. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Denial of Service | StatsCounter animation loop | mitigate | Only animates once (viewport: { once: true }). Uses transform/opacity — no layout thrashing. |
| T-04-05 | Denial of Service | GlassPanel backdrop-blur on mobile | mitigate | Per UI-SPEC: backdrop-blur only on sticky/fixed elements. GlassPanel used for cards with semi-transparent bg, not heavy blur. |
</threat_model>

<verification>
- [ ] `pnpm --filter @repo/marketing check-types` passes
- [ ] All 4 shared components export properly
- [ ] StatsCounter animates from 0 to value on scroll
- [ ] Button retains all 3 existing variants + glass variant
- [ ] AnimatedSection uses motion whileInView, not CSS transitions
</verification>

<success_criteria>
All 5 shared components created and 2 existing components updated. TypeScript compilation passes. Components follow patterns established in RESEARCH.md.
</success_criteria>

<output>
Create `.planning/phases/04-marketing-site-redesign/04-02-SUMMARY.md` when done
</output>
