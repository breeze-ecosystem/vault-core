---
phase: 04-marketing-site-redesign
plan: 09
type: execute
wave: 4
depends_on: [04-03, 04-06]
files_modified:
  - apps/marketing/app/[locale]/etudes-de-cas/page.tsx
  - apps/marketing/app/[locale]/etudes-de-cas/[slug]/page.tsx
  - apps/marketing/components/case-studies/case-study-card.tsx
  - apps/marketing/components/case-studies/case-study-grid.tsx
  - apps/marketing/components/case-studies/case-study-layout.tsx
autonomous: true
requirements: [MKT-02]
user_setup: []

must_haves:
  truths:
    - "Case studies listing at /etudes-de-cas shows grid of case study cards with excerpt"
    - "Case study detail page at /etudes-de-cas/[slug] renders MDX content with proper prose styling"
    - "CaseStudyCard shows title, industry tag, excerpt, and client name"
    - "Case study data comes from velite CaseStudy collection"
    - "Empty state renders when no case studies are available"
  artifacts:
    - path: "apps/marketing/app/[locale]/etudes-de-cas/page.tsx"
      provides: "Case studies listing page"
      contains: "etudes-de-cas/"
    - path: "apps/marketing/app/[locale]/etudes-de-cas/[slug]/page.tsx"
      provides: "Case study detail page using velite MDX"
      contains: "generateStaticParams"
    - path: "apps/marketing/components/case-studies/case-study-layout.tsx"
      provides: "MDX content layout with prose styling"
      min_lines: 35
  key_links:
    - from: "etudes-de-cas/page.tsx"
      to: "velite caseStudies collection"
      via: "imports CaseStudy from velite content"
    - from: "etudes-de-cas/[slug]/page.tsx"
      to: "velite"
      via: "generateStaticParams queries velite for all case study slugs"
---

<objective>
Create the Case Studies section — listing page with grid of case study cards and detail page that renders MDX content from the velite pipeline.

Purpose: Fulfills the "case studies" content requirement (MKT-02). Case studies build trust with prospects by showing real-world results. Uses the velite content pipeline established in Plan 04-06.
Output: 1 case studies listing page, 1 case study detail page, 3 shared components (CaseStudyCard, CaseStudyGrid, CaseStudyLayout).
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-marketing-site-redesign/04-CONTEXT.md
@.planning/phases/04-marketing-site-redesign/04-RESEARCH.md
@.planning/phases/04-marketing-site-redesign/04-UI-SPEC.md

## Key References
- **D-12**: Separate route segment — Case studies at /etudes-de-cas
- **D-13**: Full scope — Case studies listing + 2 sample case studies
- **UI-SPEC Page Architecture**: Case studies listing uses Hero + CaseStudyGrid + CTASection. Detail uses CaseStudyLayout + StatsQuote + RelatedContent + CTASection.
- **UI-SPEC Copywriting Contract**: Case study empty state "Aucune étude de cas pour le moment. Revenez bientôt."
- **Existing blog pattern**: `app/[locale]/blog/` and `app/[locale]/blog/[slug]/` — case studies follow same velite-driven pattern
- **Plan 04-06**: Created velite caseStudies collection + sample MDX content
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CaseStudyCard, CaseStudyGrid, CaseStudyLayout components</name>
  <files>
    apps/marketing/components/case-studies/case-study-card.tsx
    apps/marketing/components/case-studies/case-study-grid.tsx
    apps/marketing/components/case-studies/case-study-layout.tsx
  </files>
  <read_first>
    - apps/marketing/components/blog/blog-card.tsx (reference — similar pattern)
    - apps/marketing/components/shared/glass-panel.tsx (reference)
    - apps/marketing/components/shared/gradient-border.tsx (reference)
    - .planning/phases/04-marketing-site-redesign/04-RESEARCH.md (velite CaseStudy schema — fields available in the data)
  </read_first>
  <action>
    Create `components/case-studies/` directory, then create 3 components:

    **A. Create `case-study-card.tsx`:**
    - `'use client'` component
    - Import: `GlassPanel`, `Link`, `motion`
    - Props: `{ title: string; excerpt: string; slug: string; industry: string; client: string; index?: number }`
    - Renders:
      - `<Link href={href}>` wrapping `<GlassPanel hover={true} className="p-6 h-full flex flex-col">`
      - Industry tag at top: `<span className="text-xs font-semibold text-cyan-400 uppercase tracking-wider">{industry}</span>`
      - Title: `text-lg font-display text-white mt-2`
      - Client: `text-sm text-[#64748b] mt-1` — "Client: {client}"
      - Excerpt: `text-sm text-[#94a3b8] mt-3 flex-1`
      - "Lire l'étude →" link at bottom in text-cyan-400
    - Named export: `CaseStudyCard`
    - Import `useTranslations('caseStudies')` for the "Lire l'étude" label (use `readMore` key, with fallback to "Lire l'étude complète")

    **B. Create `case-study-grid.tsx`:**
    - Server component
    - Import: `CaseStudyCard`, `AnimatedSection`, `useTranslations`
    - Props: `{ caseStudies: Array<{ title, excerpt, slug, industry, client }> }`
    - Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
    - If `caseStudies.length === 0`, render empty state: centered glass panel with muted heading and body text from i18n `caseStudies.empty`
    - Wrap cards in AnimatedSection with staggered delays

    **C. Create `case-study-layout.tsx`:**
    - Server component
    - Import: `Section`, `Container`, `GlassPanel`
    - Props: `{ 
        children: React.ReactNode; 
        title: string; 
        client: string; 
        industry: string;
        results?: Array<{ metric: string; value: string }> 
      }`
    - Structure:
      - Hero section (same gradient pattern as product-detail): title + client + industry tag
      - Content section: `<Section variant="default">` → `<Container>`
        - 2-column layout: Left (8 cols) for children (MDX content rendered as prose), Right (4 cols) for sidebar
        - Sidebar:
          - Client info: `<GlassPanel className="p-6 mb-6">` with detail label + value
          - Results panel (if results provided): `<GlassPanel className="p-6">`
            - `<h3 className="text-sm font-semibold text-green-400 uppercase tracking-wider mb-4">Résultats clés</h3>`
            - Each result: metric name + value in large font-display cyan text
      - Children rendered in left column with `prose prose-invert prose-lg max-w-none` for MDX content styling
    - Named export: `CaseStudyLayout`
  </action>
  <verify>
    <automated>
      for f in case-study-card case-study-grid case-study-layout; do
        [ -f "apps/marketing/components/case-studies/$f.tsx" ] && echo "✅ $f.tsx exists" || echo "❌ $f.tsx missing"
      done
      grep -c "GlassPanel" apps/marketing/components/case-studies/case-study-card.tsx
      grep -c "empty" apps/marketing/components/case-studies/case-study-grid.tsx
      grep -c "prose-invert" apps/marketing/components/case-studies/case-study-layout.tsx
    </automated>
  </verify>
  <done>CaseStudyCard uses GlassPanel with hover. CaseStudyGrid shows cards in responsive grid with empty state. CaseStudyLayout has hero, sidebar with results, and prose-styled MDX content.</done>
</task>

<task type="auto">
  <name>Task 2: Create case studies listing + detail pages</name>
  <files>
    apps/marketing/app/[locale]/etudes-de-cas/page.tsx
    apps/marketing/app/[locale]/etudes-de-cas/[slug]/page.tsx
  </files>
  <read_first>
    - apps/marketing/app/[locale]/blog/page.tsx (reference — same pattern for velite-powered listing)
    - apps/marketing/app/[locale]/blog/[slug]/page.tsx (reference — same pattern for MDX detail page)
    - apps/marketing/src/lib/velite.ts (reference — the velite helper that exports getAllSlugsByLocale and other utilities)
    - .planning/phases/04-marketing-site-redesign/04-RESEARCH.md (Pattern 1: Server Component Page Template)
  </read_first>
  <action>
    **A. Create `app/[locale]/etudes-de-cas/page.tsx` (Listing page):**
    - Pattern 1: generateStaticParams + generateMetadata + async Server Component
    - Metadata: French "Études de cas - OVERSIGHT AI", description from i18n caseStudies.heading
    - Import: `Header`, `Footer`, `Section`, `Container`, `CaseStudyGrid`, `CTASection`, `useTranslations`
    - Import velite CaseStudy collection and locale filtering
    - Component:
      - `<Header />`
      - `<Section variant="dark" className="pt-32 pb-16">`
        - `<Container>` with heading (font-display text-[32px]) from i18n and subheading from i18n
      - `<Section variant="alt">`
        - `<Container>` → `<CaseStudyGrid caseStudies={filteredCaseStudies} />`
      - `<CTASection />`
      - `<Footer />`
    
    - Filter case studies by current locale. Use the same velite pattern as blog listing:
      ```typescript
      import { CaseStudy } from 'velite/generated';
      // Filter by locale
      const studies = CaseStudy.filter(s => s.locale === locale);
      ```
    - Map velite CaseStudy objects to the CaseStudyGrid props format

    **B. Create `app/[locale]/etudes-de-cas/[slug]/page.tsx` (Detail page):**
    - Pattern 1: generateStaticParams + generateMetadata + async Server Component
    - generateStaticParams: Query velite for all case studies, return locale + slug pairs
    - generateMetadata: Find case study by slug + locale, return title, description, canonical URL
    - Component:
      - `<Header />`
      - `<CaseStudyLayout title={study.title} client={study.client} industry={study.industry} results={study.results}>`
        - Render `study.content` (the MDX body — use `<div className="prose prose-invert" dangerouslySetInnerHTML={{ __html: study.content }} />` or use the velite rendering approach used in blog)
      - `<CTASection />`
      - `<Footer />`
    
    - Follow the exact same pattern used by the blog `[slug]/page.tsx` for rendering velite MDX content
  </action>
  <verify>
    <automated>
      [ -f "apps/marketing/app/\[locale\]/etudes-de-cas/page.tsx" ] && echo "✅ listing page exists" || echo "❌ listing page missing"
      [ -f "apps/marketing/app/\[locale\]/etudes-de-cas/\[slug\]/page.tsx" ] && echo "✅ detail page exists" || echo "❌ detail page missing"
      grep -c "CaseStudy" apps/marketing/app/\[locale\]/etudes-de-cas/page.tsx
      grep -c "CaseStudy" apps/marketing/app/\[locale\]/etudes-de-cas/\[slug\]/page.tsx
    </automated>
  </verify>
  <done>Case studies listing renders velite-powered grid with locale filtering. Detail page renders MDX content in CaseStudyLayout with prose styling. Empty state handled. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time content pipeline | velite processes MDX at build time. No runtime risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Tampering | MDX content rendering | accept | MDX compiled at build time by velite. Only developer-authored content. |
</threat_model>

<verification>
- [ ] `pnpm --filter @repo/marketing build` succeeds (velite generates case studies)
- [ ] Case studies listing shows cards with correct locale filtering
- [ ] Case study detail renders MDX content with prose-invert
- [ ] Empty state renders when no case studies available
- [ ] Results panel shows in sidebar when data present
</verification>

<success_criteria>
Case studies listing and detail pages render with velite-powered content. Components use shared library patterns. Empty state gracefully handled. TypeScript build passes.
</success_criteria>

<output>
Create `.planning/phases/04-marketing-site-redesign/04-09-SUMMARY.md` when done
</output>
