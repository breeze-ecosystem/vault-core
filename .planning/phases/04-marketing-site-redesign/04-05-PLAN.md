---
phase: 04-marketing-site-redesign
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-04]
files_modified:
  - apps/marketing/app/[locale]/page.tsx
  - apps/marketing/app/[locale]/blog/page.tsx
  - apps/marketing/app/[locale]/blog/[slug]/page.tsx
  - apps/marketing/app/[locale]/pricing/page.tsx
  - apps/marketing/app/[locale]/contact/page.tsx
  - apps/marketing/app/[locale]/not-found.tsx
  - apps/marketing/components/blog/blog-card.tsx
  - apps/marketing/components/blog/blog-grid.tsx
  - apps/marketing/components/blog/blog-post-layout.tsx
  - apps/marketing/components/pricing/pricing-card.tsx
  - apps/marketing/components/pricing/faq-section.tsx
  - apps/marketing/components/contact/contact-form.tsx
autonomous: true
requirements: [MKT-01, MKT-03]
user_setup: []

must_haves:
  truths:
    - "Homepage composes Header + all landing sections + Footer with new design"
    - "Homepage metadata uses French title/description when locale=fr"
    - "Blog listing page has premium dark card design with proper typography"
    - "Pricing page has glassmorphism tier cards with GradientBorder on featured tier"
    - "Contact form renders on dark background with glass card styling"
    - "404 page shows consistent dark theme with navigation back to home"
    - "All preserved pages maintain their existing content and functionality"
  artifacts:
    - path: "apps/marketing/app/[locale]/page.tsx"
      provides: "Premium homepage composing all landing sections"
      min_lines: 50
    - path: "apps/marketing/app/[locale]/blog/page.tsx"
      provides: "Blog listing with new card design"
      min_lines: 40
    - path: "apps/marketing/app/[locale]/pricing/page.tsx"
      provides: "Pricing page with glass cards"
      min_lines: 40
  key_links:
    - from: "app/[locale]/page.tsx"
      to: "components/landing/*"
      via: "imports all 11 section components and Header/Footer"
    - from: "app/[locale]/blog/page.tsx"
      to: "components/blog/blog-card.tsx"
      via: "renders BlogCard within BlogGrid"
---

<objective>
Compose the homepage using the new landing section components, and restyle all preserved pages (blog, pricing, contact, 404) with the new dark theme.

Purpose: The homepage is the highest-visibility page of the marketing site. Blog, pricing, and contact are existing pages that need visual consistency with the new design language. This plan ensures all public-facing pages share the same premium aesthetic.
Output: Homepage rewritten with new sections, blog/pricing/contact/404 restyled with dark theme.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-marketing-site-redesign/04-CONTEXT.md
@.planning/phases/04-marketing-site-redesign/04-RESEARCH.md
@.planning/phases/04-marketing-site-redesign/04-UI-SPEC.md

## Key References
- **D-14**: Existing pages preserved: Blog, Pricing, Contact (styling refresh only — content and routes unchanged)
- **D-17**: In-place refactor — update existing files
- **UI-SPEC Page Architecture**: Homepage: Full redesign. Blog: styling refresh (cards, typography, spacing). Pricing: styling refresh (card treatments, glassmorphism, FAQ). Contact: styling refresh (dark background, glass form card). 404: consistent with new design language.
- **Existing page structure**: Homepage page.tsx composes Header + HeroSection + TrustBar + FeatureShowcase + AIHighlightSection + TestimonialCarousel + StatsSection + CTASection + Footer
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Homepage page.tsx</name>
  <files>apps/marketing/app/[locale]/page.tsx</files>
  <read_first>
    - apps/marketing/app/[locale]/page.tsx (existing — rewrite with new sections)
    - apps/marketing/components/landing/hero-section.tsx (new — composed here)
    - apps/marketing/components/landing/feature-showcase.tsx (new)
    - .planning/phases/04-marketing-site-redesign/04-UI-SPEC.md (Copywriting contract for hero, CTA, nav)
  </read_first>
  <action>
    Rewrite the homepage page.tsx:

    - **Keep as Server Component** (no 'use client')
    - Keep `generateStaticParams` and `generateMetadata` pattern
    - Update `generateMetadata`:
      - When locale is 'fr': title: "OVERSIGHT AI - Intelligence de sécurité physique pilotée par l'IA", description French
      - Use `alternates.canonical = \`https://oversighthub.com/${locale}\``
      - Include `openGraph` with locale-appropriate title/description
      - Import `routing` from i18n
    
    - **Page component structure** (same composition pattern, updated imports):
      ```
      <Header />
      <main>
        <HeroSection />
        <TrustBar />
        <FeatureShowcase />
        <AIHighlightSection />
        <TestimonialCarousel />
        <StatsSection />
        <CTASection />
      </main>
      <Footer />
      ```
    - Import paths update: All landing components are at `@/components/landing/*`
    - Note: The layout.tsx already provides `<html>` wrapper, next-intl provider, and JSON-LD. The page only renders `<Header>`, `<main>`, `<Footer>`.
  </action>
  <verify>
    <automated>
      # All 7 section components imported
      for section in Header HeroSection TrustBar FeatureShowcase AIHighlightSection TestimonialCarousel StatsSection CTASection Footer; do
        grep -q "$section" apps/marketing/app/\[locale\]/page.tsx && echo "✅ $section imported" || echo "❌ $section missing"
      done
      # French metadata
      grep -c "Intelligence de sécurité" apps/marketing/app/\[locale\]/page.tsx
    </automated>
  </verify>
  <done>Homepage composes all new landing sections and Header/Footer. Metadata is locale-aware with French primary. TypeScript checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Blog listing + detail pages and components</name>
  <files>
    apps/marketing/app/[locale]/blog/page.tsx
    apps/marketing/app/[locale]/blog/[slug]/page.tsx
    apps/marketing/components/blog/blog-card.tsx
    apps/marketing/components/blog/blog-grid.tsx
    apps/marketing/components/blog/blog-post-layout.tsx
  </files>
  <read_first>
    - apps/marketing/app/[locale]/blog/page.tsx (existing — update styling)
    - apps/marketing/app/[locale]/blog/[slug]/page.tsx (existing — update styling)
    - apps/marketing/components/blog/blog-card.tsx (existing — restyle)
    - apps/marketing/components/blog/blog-grid.tsx (existing — restyle)
    - apps/marketing/components/blog/blog-post-layout.tsx (existing — restyle)
  </read_first>
  <action>
    Read each file first, then update styling for the dark theme:
    
    **Do NOT change content logic, data fetching, or component structure** — only CSS/classes and typography.

    **Styling changes to apply across all blog files:**
    - Replace any light-mode colors (white bg, dark text) with dark equivalents:
      - `bg-white` → `bg-[#0c1020]`
      - `text-foreground` / dark text → `text-white`
      - `text-muted` or gray text — already maps via CSS variables
      - `border-border` — already maps via CSS variables now pointing to dark values
    - Update font families:
      - Headings: Add `font-display` class
      - Body: stays as default (Inter via sans)
    - BlogCard: Use `GlassPanel hover={true}` wrapper instead of existing card styling
      - Title: `text-white font-display`
      - Excerpt: `text-sm text-[#94a3b8]`
      - Meta (date/category): `text-xs text-[#64748b]`
    - BlogGrid: Update gap/spacing if needed — `gap-6`
    - BlogPostLayout: 
      - Use prose styles with dark theme (`prose-invert` class if using @tailwindcss/typography)
      - Article container: `max-w-3xl mx-auto`
    - Blog listing page: Update PageHeader or section heading to use font-display, dark colors
  </action>
  <verify>
    <automated>
      grep -c "GlassPanel" apps/marketing/components/blog/blog-card.tsx || echo "Note: blog-card may not use GlassPanel directly if restructured"
      # Verify dark theme applied
      grep -c "prose-invert" apps/marketing/components/blog/blog-post-layout.tsx 2>/dev/null || true
      # Files still work
      [ -f "apps/marketing/app/\[locale\]/blog/page.tsx" ] && echo "✅ blog listing exists"
      [ -f "apps/marketing/app/\[locale\]/blog/\[slug\]/page.tsx" ] && echo "✅ blog detail exists"
    </automated>
  </verify>
  <done>Blog listing cards use GlassPanel styling. Blog detail uses prose-invert for dark readability. All pages maintain existing content and data fetching but with dark theme.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle Pricing + Contact + 404 pages</name>
  <files>
    apps/marketing/app/[locale]/pricing/page.tsx
    apps/marketing/app/[locale]/contact/page.tsx
    apps/marketing/app/[locale]/not-found.tsx
    apps/marketing/components/pricing/pricing-card.tsx
    apps/marketing/components/pricing/faq-section.tsx
    apps/marketing/components/contact/contact-form.tsx
  </files>
  <read_first>
    - apps/marketing/app/[locale]/pricing/page.tsx (existing — restyle)
    - apps/marketing/app/[locale]/contact/page.tsx (existing — restyle)
    - apps/marketing/app/[locale]/not-found.tsx (existing — restyle)
    - apps/marketing/components/pricing/pricing-card.tsx (existing — restyle)
    - apps/marketing/components/pricing/faq-section.tsx (existing — restyle)
    - apps/marketing/components/contact/contact-form.tsx (existing — restyle)
  </read_first>
  <action>
    Read each file first, then update styling for dark theme:

    **Pricing page styling:**
    - Update pricing-card.tsx:
      - Use `GlassPanel hover={true}` as card wrapper
      - For the "Professional" tier (most popular), wrap in `GradientBorder` for visual emphasis
      - Tier name: `text-lg font-display text-white`
      - Price: `text-4xl font-display text-white`
      - Description: `text-sm text-[#94a3b8]`
      - Feature list items: `text-sm text-[#94a3b8]` with cyan check icons
      - CTA button on each card: use Button component
    - Update faq-section.tsx:
      - Dark background (`bg-[#0c1020]` or Section variant "alt")
      - FAQ items: glass-style accordion with `border border-white/5`
      - Question: `text-white font-semibold`
      - Answer: `text-[#94a3b8]`
    - Pricing page: Update heading to use font-display, ensure dark background

    **Contact page styling:**
    - Update contact-form.tsx:
      - Wrap form in `<GlassPanel className="max-w-xl mx-auto p-8">`
      - Form fields: Input styling with `bg-white/[0.05] border-white/10 text-white` 
      - Labels: `text-sm text-[#94a3b8]`
      - Submit button: Use Button component with primary variant
    - Contact page: Dark background, heading with font-display

    **404 page styling:**
    - Update not-found.tsx:
      - Dark background matching theme
      - Large "404" or "Page introuvable" in font-display with muted color
      - Descriptive text in text-muted
      - "Retour à l'accueil" link with Button component
    - Import Header and Footer for consistent layout

    For all pages: Do NOT change content logic, form handlers, or data fetching. Only update CSS classes.
  </action>
  <verify>
    <automated>
      grep -c "GlassPanel" apps/marketing/components/pricing/pricing-card.tsx
      grep -c "GradientBorder" apps/marketing/components/pricing/pricing-card.tsx
      grep -c "GlassPanel" apps/marketing/components/contact/contact-form.tsx
      grep -c "Header" apps/marketing/app/\[locale\]/not-found.tsx
    </automated>
  </verify>
  <done>PricingCard uses GlassPanel (with GradientBorder on featured tier). Contact form wrapped in glass card. 404 page has consistent dark theme with home navigation. All existing content/functionality preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contact form → API | Contact form submits to NestJS API. Already uses Cloudflare Turnstile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Spoofing | Contact form Turnstile | mitigate | Already configured — verify Turnstile script still loads after restyle. Form element ID/classes should remain compatible. |
</threat_model>

<verification>
- [ ] `pnpm --filter @repo/marketing build` succeeds
- [ ] Homepage renders with all new landing sections
- [ ] Blog listing shows posts with new card styling
- [ ] Pricing cards have glassmorphism styling with GradientBorder on featured tier
- [ ] Contact form is in glass card with dark input styling
- [ ] 404 page has consistent dark theme
- [ ] All existing page content and functionality preserved
</verification>

<success_criteria>
Homepage, blog (listing + detail), pricing, contact, and 404 pages all render with dark theme. No content or functionality regressions. Build passes.
</success_criteria>

<output>
Create `.planning/phases/04-marketing-site-redesign/04-05-SUMMARY.md` when done
</output>
