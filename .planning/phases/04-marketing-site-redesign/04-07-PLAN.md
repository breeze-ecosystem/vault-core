---
phase: 04-marketing-site-redesign
plan: 07
type: execute
wave: 4
depends_on: [04-03, 04-06]
files_modified:
  - apps/marketing/app/[locale]/produits/page.tsx
  - apps/marketing/app/[locale]/produits/video/page.tsx
  - apps/marketing/app/[locale]/produits/access-control/page.tsx
  - apps/marketing/app/[locale]/produits/ai-analytics/page.tsx
  - apps/marketing/app/[locale]/produits/analytics/page.tsx
  - apps/marketing/components/products/product-card.tsx
  - apps/marketing/components/products/product-grid.tsx
  - apps/marketing/components/products/product-detail-layout.tsx
  - apps/marketing/components/products/products-hero.tsx
autonomous: true
requirements: [MKT-02]
user_setup: []

must_haves:
  truths:
    - "Products overview page at /produits shows Hero + ProductGrid with 4 capability cards + CTA"
    - "Each product sub-page (video, access-control, ai-analytics, analytics) has detail layout with features list"
    - "ProductCard renders in GlassPanel with icon, title, description, hover scale effect"
    - "All pages have locale-aware metadata and canonical URLs"
    - "Product detail pages at /produits/video, /produits/access-control, /produits/ai-analytics, /produits/analytics"
  artifacts:
    - path: "apps/marketing/app/[locale]/produits/page.tsx"
      provides: "Products overview page"
      contains: "/produits/video"
    - path: "apps/marketing/components/products/product-card.tsx"
      provides: "Product capability card for overview grid"
      uses: "GlassPanel"
    - path: "apps/marketing/components/products/product-detail-layout.tsx"
      provides: "Shared layout for product sub-pages"
      min_lines: 30
  key_links:
    - from: "produits/page.tsx"
      to: "messages/*.json"
      via: "useTranslations('produits') for headings, card content"
    - from: "product-detail-layout.tsx"
      to: "components/shared/glass-panel.tsx"
      via: "uses GlassPanel for content sections"
---

<objective>
Create the Products section — overview page with 4 capability cards and 4 detailed sub-pages (Video Intelligence, Access Control, AI Analytics, Reporting & Analytics), with shared product components.

Purpose: Fulfills the "complete product content" requirement (MKT-02). Prospects need to understand each product capability in detail with features, use cases, and CTAs.
Output: 1 products overview page, 4 product sub-pages, 3 shared product components (ProductCard, ProductGrid, ProductDetailLayout), ProductsHero component.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-marketing-site-redesign/04-CONTEXT.md
@.planning/phases/04-marketing-site-redesign/04-RESEARCH.md
@.planning/phases/04-marketing-site-redesign/04-UI-SPEC.md

## Key References
- **D-12**: Separate route segment — Products at /produits
- **D-13**: Full scope — Products overview + 4 sub-pages (Video, Access Control, AI, Analytics)
- **UI-SPEC Page Architecture**: Products overview uses Hero + ProductGrid (4 cards) + CTASection. Sub-pages use ProductDetailLayout + FeatureShowcase + StatsSection + CTASection.
- **RESEARCH.md Pattern 1**: Server Component Page Template (generateStaticParams + generateMetadata + Server Component)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared product components + ProductsHero</name>
  <files>
    apps/marketing/components/products/product-card.tsx
    apps/marketing/components/products/product-grid.tsx
    apps/marketing/components/products/product-detail-layout.tsx
    apps/marketing/components/products/products-hero.tsx
  </files>
  <read_first>
    - apps/marketing/components/shared/glass-panel.tsx (reference)
    - apps/marketing/components/shared/animated-section.tsx (reference)
    - apps/marketing/components/landing/hero-section.tsx (reference for hero pattern)
    - .planning/phases/04-marketing-site-redesign/04-RESEARCH.md (Pattern 1: Server Component Page Template)
    - .planning/phases/04-marketing-site-redesign/04-UI-SPEC.md (Interaction Contract for animation values)
  </read_first>
  <action>
    Create `components/products/` directory, then create 4 components:

    **A. Create `product-card.tsx`:**
    - `'use client'` component
    - Import: `GlassPanel`, `motion`, `Link`
    - Props: `{ title: string; description: string; href: string; icon: React.ReactNode; index?: number }`
    - Renders:
      - `<Link href={href}>` wrapping a `<GlassPanel hover={true} className="p-6 h-full">`
      - Icon: `w-12 h-12 rounded-xl bg-cyan-500/10 flex items-center justify-center text-cyan-400 mb-4`
      - Title: `text-lg font-display text-white`
      - Description: `text-sm text-[#94a3b8] mt-2`
      - Bottom: "En savoir plus →" link in cyan-400
    - Named export: `ProductCard`

    **B. Create `product-grid.tsx`:**
    - Server component
    - Import: `ProductCard`, `AnimatedSection`
    - Props: `{ products: Array<{ title, description, href, icon }> }`
    - Renders: `grid grid-cols-1 md:grid-cols-2 gap-6`
    - Wraps cards in AnimatedSection with staggered delays

    **C. Create `product-detail-layout.tsx`:**
    - Server component
    - Props: `{ children: React.ReactNode; title: string; description: string; className?: string }`
    - Renders a full-width hero banner section with:
      - `pt-32 pb-16 bg-gradient-to-b from-[#070912] via-[#0c1020] to-[#070912]`
      - Heading: `text-[56px] font-display text-white` (title prop)
      - Subheading: `text-lg text-[#94a3b8] max-w-2xl` (description prop)
    - Then renders `{children}` (the detailed content passed as children)

    **D. Create `products-hero.tsx`:**
    - `'use client'` component
    - Uses `useTranslations('produits')` for heading/subheading/cta
    - Similar structure to landing HeroSection but simpler — no grid background
    - Heading + subheading + CTA to first product sub-page
  </action>
  <verify>
    <automated>
      for f in product-card product-grid product-detail-layout products-hero; do
        [ -f "apps/marketing/components/products/$f.tsx" ] && echo "✅ $f.tsx exists" || echo "❌ $f.tsx missing"
      done
      grep -c "GlassPanel" apps/marketing/components/products/product-card.tsx
      grep -c "export function" apps/marketing/components/products/product-card.tsx
    </automated>
  </verify>
  <done>ProductCard uses GlassPanel with hover. ProductGrid renders 2-column responsive grid. ProductDetailLayout has hero banner + children slot. ProductsHero has dark gradient heading section.</done>
</task>

<task type="auto">
  <name>Task 2: Create Products overview page + 4 sub-pages</name>
  <files>
    apps/marketing/app/[locale]/produits/page.tsx
    apps/marketing/app/[locale]/produits/video/page.tsx
    apps/marketing/app/[locale]/produits/access-control/page.tsx
    apps/marketing/app/[locale]/produits/ai-analytics/page.tsx
    apps/marketing/app/[locale]/produits/analytics/page.tsx
  </files>
  <read_first>
    - .planning/phases/04-marketing-site-redesign/04-RESEARCH.md (Pattern 1: Server Component Page Template — exact pattern for generateStaticParams, generateMetadata, page component)
    - apps/marketing/app/[locale]/page.tsx (reference for existing page pattern)
  </read_first>
  <action>
    Create `app/[locale]/produits/` directory and all 5 pages.

    **A. Create `app/[locale]/produits/page.tsx` (Products overview):**
    - Follow Pattern 1: `generateStaticParams` → `generateMetadata` → async Server Component
    - Metadata: French title "Produits - OVERSIGHT AI", description from i18n
    - Component: `<Header /> + <main> + <ProductsHero /> + <ProductGrid> + <CTASection /> + <Footer />`
    - ProductGrid data: 4 products with icons (lucide-react icons: Video, Key, Brain, BarChart3), hrefs to sub-pages, titles/descriptions from i18n `produits.*.name` and `produits.*.description`
    - Import `Header`, `Footer` from layout, `ProductsHero` from products, `ProductGrid` from products, `CTASection` from landing

    **B. Create 4 sub-pages (video, access-control, ai-analytics, analytics):**
    Each sub-page follows the same pattern:
    - Pattern 1: generateStaticParams + generateMetadata + async Server Component
    - Metadata: locale-aware title/description
    - Component: `<Header /> + <main>`
    - Main content: `<ProductDetailLayout title={t('produits.video.name')} description={t('produits.video.description')}>`
      - Inside: Feature list from i18n `produits.video.features` array
      - Render each feature as a `<GlassPanel className="p-4">` with icon + feature text
      - Optionally include a `<StatsSection />` or `<CTASection />` for engagement
    - `</main> + <Footer />`
    - Note: For the Analytics page, use the key `produits.analytics.*` (not `produits.analytics.name` but `produits.analytics.name`)
  </action>
  <verify>
    <automated>
      # All 5 pages exist
      for d in produits produits/video produits/access-control produits/ai-analytics produits/analytics; do
        [ -f "apps/marketing/app/\[locale\]/$d/page.tsx" ] && echo "✅ $d/page.tsx exists" || echo "❌ $d/page.tsx missing"
      done
      # Each has generateStaticParams
      for d in produits produits/video produits/access-control produits/ai-analytics produits/analytics; do
        grep -q "generateStaticParams" "apps/marketing/app/\[locale\]/$d/page.tsx" && echo "✅ $d has generateStaticParams" || echo "❌ $d missing generateStaticParams"
      done
    </automated>
  </verify>
  <done>Products overview page renders with grid of 4 capability cards. All 4 sub-pages have detail layout with feature lists from i18n. All use locale-aware metadata and canonical URLs. TypeScript checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Static content pages. No user input, no API calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | Route exposure | accept | Public product pages — intended to be discoverable and indexed. |
</threat_model>

<verification>
- [ ] `pnpm --filter @repo/marketing build` succeeds
- [ ] Products overview at /produits shows 4 product cards
- [ ] Each sub-page navigable and renders with dark theme
- [ ] All feature list content comes from i18n messages
- [ ] Metadata includes canonical URLs and alternates
</verification>

<success_criteria>
All 5 product pages created and rendering. Components import from established shared library. i18n messages drive all text content. Build passes without errors.
</success_criteria>

<output>
Create `.planning/phases/04-marketing-site-redesign/04-07-SUMMARY.md` when done
</output>
