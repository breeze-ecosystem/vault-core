---
phase: 04-marketing-site-redesign
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/marketing/components/layout/header.tsx
  - apps/marketing/components/layout/footer.tsx
  - apps/marketing/components/layout/mobile-menu.tsx
  - apps/marketing/components/navigation/nav-link.tsx
  - apps/marketing/components/navigation/language-switcher.tsx
autonomous: true
requirements: [MKT-01]
user_setup: []

must_haves:
  truths:
    - "Header is a glass sticky nav with backdrop-blur-2xl on scroll"
    - "Nav links include Produits, Solutions, Études de cas, Démo (from i18n messages)"
    - "Mobile menu is a full-screen overlay with staggered animated nav links"
    - "Footer has dark background with glass divider and new link columns"
    - "NavLink shows active indicator (underline) for current page"
    - "LanguageSwitcher shows locale labels and uses restyled dropdown"
  artifacts:
    - path: "apps/marketing/components/layout/header.tsx"
      provides: "Glass sticky navigation with scroll detection and mobile toggle"
      min_lines: 80
      contains: "Produits"
    - path: "apps/marketing/components/layout/footer.tsx"
      provides: "Dark footer with product/resource/company/legal columns"
      min_lines: 60
    - path: "apps/marketing/components/navigation/nav-link.tsx"
      provides: "Active-aware link component with underline indicator"
      min_lines: 30
  key_links:
    - from: "header.tsx"
      to: "messages/*.json"
      via: "useTranslations() for nav link labels"
    - from: "mobile-menu.tsx"
      to: "header.tsx"
      via: "toggle state passed from Header"
---

<objective>
Rewrite the navigation and layout chrome — header, footer, mobile menu, nav link, and language switcher — with the new dark glassmorphism aesthetic and new route structure.

Purpose: Every page (home, products, solutions, case studies, demo, blog, pricing, contact) shares the header and footer. Getting these right once allows all page plans to compose them without further modification.
Output: Header with glass sticky effect, Footer with dark bg and new columns, MobileMenu with full-screen overlay, NavLink with active indicator, LanguageSwitcher restyled.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-marketing-site-redesign/04-CONTEXT.md
@.planning/phases/04-marketing-site-redesign/04-RESEARCH.md
@.planning/phases/04-marketing-site-redesign/04-UI-SPEC.md

## Key References
- **D-12**: New route segments — Produits, Solutions, Études de cas
- **D-10**: Demo page accessible from nav
- **D-14**: Existing pages preserved (Blog, Pricing, Contact)
- **UI-SPEC Copywriting Contract**: Nav labels in French: Produits, Solutions, Études de cas, Démo, Blog, Tarifs, Contact
- **UI-SPEC Interaction Contract**: Sticky nav transition (transparent → bg-black/60 backdrop-blur-2xl), mobile menu reveal (full-screen backdrop-blur-3xl)
- **RESEARCH.md Pattern**: Sticky nav glass effect exact styles
- **UI-SPEC Typography**: Nav links use 14px weight 600 body (Inter)
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From existing header.tsx:
```
const NAV_LINKS = [{ href: '/', label: 'Home' }, ...]; // hardcoded strings
// Uses: useLocale(), useState scrolled, useEffect scroll listener
// Renders: sticky header, Container, Logo, NavLink[], LanguageSwitcher, CTA button, hamburger
// MobileMenu receives: isOpen, onClose
```

From existing nav-link.tsx:
```
Props: { href: string; children: ReactNode; className?: string }
// Uses: usePathname() from @/src/i18n/navigation
// Active: pathname === href || pathname.startsWith(href + '/')
// Style: relative, after pseudo-element underline with cyan-500 scale
```

From existing footer.tsx:
```
const footerColumns = [{ title: 'Product', links: [...] }, ...]; // hardcoded columns
// Renders: border-t, white bg, 4-column grid, copyright
```

UI-SPEC Copy:
| Nav | French | English |
|-----|--------|---------|
| Products | Produits | Products |
| Solutions | Solutions | Solutions |
| Case Studies | Études de cas | Case Studies |
| Blog | Blog | Blog |
| Pricing | Tarifs | Pricing |
| Contact | Contact | Contact |
| Demo | Démo | Demo |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Header component</name>
  <files>apps/marketing/components/layout/header.tsx</files>
  <read_first>
    - apps/marketing/components/layout/header.tsx (existing — full rewrite)
    - apps/marketing/components/layout/mobile-menu.tsx (existing — will be rewritten in Task 3)
    - apps/marketing/components/navigation/nav-link.tsx (existing — updated in Task 3)
    - .planning/phases/04-marketing-site-redesign/04-CONTEXT.md (D-10, D-12, D-14 for nav link decisions)
    - .planning/phases/04-marketing-site-redesign/04-UI-SPEC.md (Interaction Contract for sticky nav glass effect, Copywriting for nav labels)
  </read_first>
  <action>
    Rewrite `apps/marketing/components/layout/header.tsx` completely:

    - Keep `'use client'` directive
    - Import `useState, useEffect` from React, `Link` from `next/link`, `useTranslations` from `next-intl`, `cn` from utils
    - Import `Container`, `NavLink`, `LanguageSwitcher`, `MobileMenu`, and `Button` from their paths

    - **Remove** the hardcoded `NAV_LINKS` array — use `useTranslations('nav')` instead
    - Nav structure (using message keys — these will be added in Plan 04-06 but define the key names):
      - `produits` → `/${locale}/produits`
      - `solutions` → `/${locale}/solutions`
      - `etudesDeCas` → `/${locale}/etudes-de-cas`
      - `demo` → `/${locale}/demo`
      - `blog` → `/${locale}/blog`
      - `tarifs` → `/${locale}/pricing`
    
    - **Scroll detection**: Use `window.addEventListener('scroll', handler, { passive: true })` with 100px threshold
    - **Header wrapper**:
      - `fixed top-0 left-0 right-0 z-50 transition-all duration-300`
      - When scrolled: `bg-black/60 backdrop-blur-2xl border-b border-white/5`
      - When not scrolled: `bg-transparent`
    
    - **Desktop nav**: Center section with NavLink components for each nav item
    - **Right section**: LanguageSwitcher + "Réserver une démo" CTA button using Button component with primary variant (links to `/${locale}/contact`)
    - **Logo**: Keep same structure but use Plus Jakarta Sans (font-display) for the brand name. Use cyan diamond ◆ with text-white
    
    - **Mobile hamburger**: Keep 3-line animation but update colors to white (was foreground which was dark in light mode)
    - **Mobile Menu integration**: Pass `isOpen` and `onClose` to MobileMenu component

    - Navigation links must use locale-prefixed paths via `useLocale()` hook
    - Use `me-*` / `ms-*` logical properties for RTL compatibility (D-19)
  </action>
  <verify>
    <automated>
      # Check for key imports and features
      grep -c "useTranslations" apps/marketing/components/layout/header.tsx
      grep -c "backdrop-blur-2xl" apps/marketing/components/layout/header.tsx
      grep -c "MobileMenu" apps/marketing/components/layout/header.tsx
      grep -c "LanguageSwitcher" apps/marketing/components/layout/header.tsx
      # Verify no hardcoded nav array
      ! grep -c "const NAV_LINKS" apps/marketing/components/layout/header.tsx
    </automated>
  </verify>
  <done>Header renders as fixed glass sticky nav. Nav links driven by i18n messages. Mobile menu toggle works. CTA button links to /contact.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Footer component</name>
  <files>apps/marketing/components/layout/footer.tsx</files>
  <read_first>
    - apps/marketing/components/layout/footer.tsx (existing — full rewrite)
    - .planning/phases/04-marketing-site-redesign/04-UI-SPEC.md (Copywriting for footer labels)
  </read_first>
  <action>
    Rewrite `apps/marketing/components/layout/footer.tsx` completely:

    - **Mode**: Server component (no `'use client'` unless needed for interactivity)
    - Import `Link` from `next/link`, `Container`, `LanguageSwitcher`, `cn` from utils
    
    - **Footer wrapper**: `bg-[#070912] border-t border-white/5 pt-16 pb-8`
      - The border-t acts as a subtle glass divider
    
    - **Column structure**: 4-column grid (responsive: 1→2→4 columns):
      1. **Produit** (Product): Vue d'ensemble → /produits, Vidéo → /produits/video, Contrôle d'accès → /produits/access-control, IA → /produits/ai-analytics
      2. **Ressources** (Resources): Blog → /blog, Documentation → #, API → #
      3. **Entreprise** (Company): Contact → /contact, Tarifs → /pricing
      4. **Légal** (Legal): Confidentialité → #, Conditions → #, Cookies → #
    
    - **Column heading**: `text-sm font-semibold text-white mb-4` (use font-display class)
    - **Link style**: `text-sm text-muted transition-colors hover:text-cyan-400`
    
    - **Bottom bar**: Above the copyright, a subtle glass divider: `<div className="my-8 h-px bg-gradient-to-r from-transparent via-white/10 to-transparent" />`
    - **Bottom row**: LanguageSwitcher on left, copyright centered/right
    - **Copyright**: `text-sm text-muted` — "© 2026 Oversight AI. Tous droits réservés."
    
    - Use logical properties (`ms-*`, `me-*`, `text-start`, `text-end`) for RTL support
  </action>
  <verify>
    <automated>
      grep -c "from-transparent via-white/10 to-transparent" apps/marketing/components/layout/footer.tsx
      grep -c "LanguageSwitcher" apps/marketing/components/layout/footer.tsx
      grep -c "/produits" apps/marketing/components/layout/footer.tsx
      grep -c "/blog" apps/marketing/components/layout/footer.tsx
    </automated>
  </verify>
  <done>Footer has dark background, glass gradient divider, 4 columns with product/resource/company/legal links, LanguageSwitcher, and copyright.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite MobileMenu + update NavLink + restyle LanguageSwitcher</name>
  <files>
    apps/marketing/components/layout/mobile-menu.tsx
    apps/marketing/components/navigation/nav-link.tsx
    apps/marketing/components/navigation/language-switcher.tsx
  </files>
  <read_first>
    - apps/marketing/components/layout/mobile-menu.tsx (existing — full rewrite)
    - apps/marketing/components/navigation/nav-link.tsx (existing — update styling)
    - apps/marketing/components/navigation/language-switcher.tsx (existing — read to understand current structure)
    - .planning/phases/04-marketing-site-redesign/04-UI-SPEC.md (Interaction Contract for mobile menu)
  </read_first>
  <action>
    **A. Rewrite `components/layout/mobile-menu.tsx`:**
    - Keep `'use client'`
    - Use `motion` from `motion/react` for AnimatePresence + staggered animations
    - Use `useLocale()` for locale prefix, `useTranslations('nav')` for nav labels
    - Props: `{ isOpen: boolean; onClose: () => void }`
    - Render a full-screen overlay using AnimatePresence:
      ```tsx
      <AnimatePresence>
        {isOpen && (
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            className="fixed inset-0 z-40 bg-black/80 backdrop-blur-3xl lg:hidden"
          >
            {/* Nav links with staggered entrance */}
            <nav className="flex flex-col gap-6 p-8 pt-24">
              {navItems.map((item, i) => (
                <motion.div
                  key={item.href}
                  initial={{ opacity: 0, y: 20 }}
                  animate={{ opacity: 1, y: 0 }}
                  transition={{ delay: 0.1 + i * 0.05, duration: 0.4 }}
                >
                  <Link ... onClick={onClose}>{item.label}</Link>
                </motion.div>
              ))}
            </nav>
            {/* CTA button and LanguageSwitcher */}
          </motion.div>
        )}
      </AnimatePresence>
      ```
    - Nav items: Produits (/produits), Solutions (/solutions), Études de cas (/etudes-de-cas), Démo (/demo), Blog (/blog), Tarifs (/pricing), Contact (/contact)
    - Include LanguageSwitcher and "Réserver une démo" CTA button at bottom
    - Use `text-2xl font-display text-white` for nav link text

    **B. Update `components/navigation/nav-link.tsx`:**
    - Read the existing file. It already has a solid active indicator pattern (underlines with after pseudo-element).
    - Update styling for dark theme:
      - Active text color: `text-cyan-400` (was `text-cyan-500`)
      - Inactive text: `text-white/70 hover:text-white`
      - After pseudo-element underline: `bg-cyan-400` (when active)
      - Keep the `after:` pseudo-element animation approach
    - Add `isExternal` prop option for external links
    - Use logical properties for RTL support

    **C. Restyle `components/navigation/language-switcher.tsx`:**
    - Read the existing file first
    - Update styling for dark theme compatibility
    - Apply text-white/70 hover:text-white, glass-appropriate background
  </action>
  <verify>
    <automated>
      # MobileMenu uses motion AnimatePresence
      grep -c "AnimatePresence" apps/marketing/components/layout/mobile-menu.tsx
      # NavLink has cyan-400 active color
      grep -c "cyan-400" apps/marketing/components/navigation/nav-link.tsx
      grep -c "text-white/70" apps/marketing/components/navigation/nav-link.tsx
    </automated>
  </verify>
  <done>MobileMenu is full-screen overlay with staggered motion animations. NavLink has updated dark-theme active indicator. LanguageSwitcher restyled for dark theme.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Presentational components only. LanguageSwitcher changes locale — no user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Denial of Service | Header backdrop-blur-2xl | accept | Sticky/fixed element — no scrolling container. GPU-accelerated. Per UI-SPEC performance constraints. |
| T-04-07 | Tampering | Nav link href injection | accept | Links are static routes defined in component, not user-provided. |
</threat_model>

<verification>
- [ ] `pnpm --filter @repo/marketing check-types` passes
- [ ] Header shows new nav links: Produits, Solutions, Études de cas, Démo
- [ ] Header scrolls from transparent to glass black/60 with backdrop-blur
- [ ] Footer has 4 columns with glass gradient divider
- [ ] Mobile menu is full-screen overlay with staggered animations
- [ ] NavLink shows underline active indicator
</verification>

<success_criteria>
All 5 navigation/layout files modified. TypeScript compilation passes. Components use i18n messages for nav labels. Glass aesthetic applied consistently across all components. RTL logical properties used.
</success_criteria>

<output>
Create `.planning/phases/04-marketing-site-redesign/04-03-SUMMARY.md` when done
</output>
