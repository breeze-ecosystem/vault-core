---
phase: 04-marketing-site-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/marketing/app/globals.css
  - apps/marketing/tailwind.config.ts
  - packages/design/src/marketing.ts
  - apps/marketing/app/[locale]/layout.tsx
  - apps/marketing/components/layout/section.tsx
  - apps/marketing/components/layout/container.tsx
  - apps/marketing/components/ui/page-header.tsx
autonomous: true
requirements: [MKT-01]
user_setup: []

must_haves:
  truths:
    - "Marketing site renders in dark-only theme with deep navy background (#070912)"
    - "Page uses Inter (body) with Plus Jakarta Sans (headings) — IBM Plex Sans replaced"
    - "CSS variables use dark-only HSL values — no light mode variables exist"
    - "Noise texture overlay visible as subtle grain pattern on all pages"
    - "Section component defaults to dark background with py-24 vertical spacing"
    - "Design tokens in packages/design/ reflect the new dark premium palette"
  artifacts:
    - path: "apps/marketing/app/globals.css"
      provides: "Dark-only theme variables, noise texture, grid background, scrollbar"
      min_lines: 80
    - path: "apps/marketing/tailwind.config.ts"
      provides: "Theme config with Inter/PJS/JB Mono fonts, new colors, animations"
      min_lines: 80
    - path: "packages/design/src/marketing.ts"
      provides: "Updated marketing design tokens"
      exports: ["marketingTheme"]
    - path: "apps/marketing/app/[locale]/layout.tsx"
      provides: "Font loading via next/font/google with CSS variables"
      contains: "Plus_Jakarta_Sans"
  key_links:
    - from: "tailwind.config.ts"
      to: "globals.css"
      via: "CSS variable references in colors (hsl(var(--marketing-*)))"
    - from: "layout.tsx"
      to: "tailwind.config.ts"
      via: "fontFamily references CSS variable --font-*"
---

<objective>
Establish the visual foundation for the marketing site redesign — dark-only CSS variables, new font stack, updated Tailwind config, design tokens, and base layout components.

Purpose: Every subsequent plan depends on these CSS variables, fonts, and design tokens being correct. Building foundation first prevents breaking existing pages when components are restyled.
Output: globals.css rewritten, tailwind.config.ts updated, design tokens migrated, layout.tsx fonts loaded, Section/Container/PageHeader components updated.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-marketing-site-redesign/04-CONTEXT.md
@.planning/phases/04-marketing-site-redesign/04-RESEARCH.md
@.planning/phases/04-marketing-site-redesign/04-UI-SPEC.md

## Key References
- **UI-SPEC Colors section**: Exact hex values for dominant, secondary, elevated, accent, text, border colors (D-02)
- **UI-SPEC Typography section**: Inter for body, Plus Jakarta Sans for headings, JetBrains Mono for code (D-07)
- **UI-SPEC Spacing section**: 8-point scale, py-24/py-32 for section spacing
- **RESEARCH.md Code Examples**: Font loading, Tailwind config, noise texture, sticky nav CSS patterns
</context>

<interfaces>
<!-- Existing types the executor needs before modifying -->

From apps/marketing/tailwind.config.ts (current):
```
fontFamily: { sans: ['"IBM Plex Sans"', ...], mono: ['"JetBrains Mono"', ...] }
colors: { background, foreground, muted, border, primary, secondary, ring, ... }  // all hsl(var(--marketing-*))
darkMode: 'media'
```

From packages/design/src/marketing.ts (current):
```
colors: { hero, surface, text, accent }
spacing: { section, container }
typography: { display, heading, body, small }
```

From apps/marketing/app/[locale]/layout.tsx (current):
```
const rtlLocales = new Set(['ar']);
const direction = rtlLocales.has(locale) ? 'rtl' : 'ltr';
<html lang={locale} dir={direction} className="dark" suppressHydrationWarning>
```
No font loading — fonts come from globals.css @import.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite globals.css + tailwind.config.ts + design tokens</name>
  <files>
    apps/marketing/app/globals.css
    apps/marketing/tailwind.config.ts
    packages/design/src/marketing.ts
  </files>
  <read_first>
    - apps/marketing/app/globals.css (existing — rewrite entirely)
    - apps/marketing/tailwind.config.ts (existing — update)
    - packages/design/src/marketing.ts (existing — update)
    - .planning/phases/04-marketing-site-redesign/04-UI-SPEC.md (read Colors, Typography, Spacing sections for exact values)
  </read_first>
  <action>
    **A. Rewrite apps/marketing/app/globals.css:**
    - Remove the `@import url(...)` for Google Fonts (fonts will load via next/font in layout.tsx)
    - Add `@tailwind base/components/utilities` directives
    - Replace `:root` block with dark-only CSS variables using these exact hex-to-HSL values:
      - `--marketing-bg: 228 63% 4%;` (#070912 approx)
      - `--marketing-bg-secondary: 228 45% 9%;` (#0c1020 approx)
      - `--marketing-bg-card: 228 40% 15%;` (#1a2332 approx)
      - `--marketing-foreground: 210 40% 96%;` (#f1f5f9 approx)
      - `--marketing-muted: 215 16% 47%;` (#64748b)
      - `--marketing-muted-light: 215 20% 65%;` (#94a3b8)
      - `--marketing-border: 215 25% 27%;` (#1e293b)
      - `--marketing-border-strong: 215 20% 46%;` (#334155)
      - `--marketing-primary: 190 90% 50%;` (#06b6d4)
      - `--marketing-primary-hover: 190 90% 48%;` (#0891b2)
      - `--marketing-primary-foreground: 0 0% 100%;`
      - `--marketing-ring: 190 90% 50%;`
      - `--marketing-success: 160 84% 39%;` (#10b981)
      - `--marketing-destructive: 0 84% 60%;` (#ef4444)
      - `--marketing-warning: 35 92% 50%;` (#f59e0b)
      - `--marketing-radius-sm: 0.375rem;` / `--marketing-radius: 0.5rem;` / `--marketing-radius-lg: 0.75rem;` / `--marketing-radius-xl: 1rem;`
      - `--marketing-container: 1280px;`
    - Keep existing `html { scroll-behavior: smooth; }` and `body` antialiasing
    - Replace `::selection` background to use new primary
    - Keep `.bg-grid` but change the grid line color to cyan at 0.04 opacity (hsl(var(--marketing-primary) / 0.04))
    - Keep scrollbar styling, muted colors unchanged
    - Keep `.scrollbar-hide` utility
    - Keep `@media (prefers-reduced-motion: reduce)` block
    - **Add a noise texture layer**: Create `body::after` with `position: fixed; inset: 0; z-index: 40; pointer-events: none; opacity: 0.03;` and the SVG noise filter background-image from RESEARCH.md. Use `background-repeat: repeat; background-size: 256px 256px;`
    - Remove `font-family` on body element (now handled by Tailwind fontFamily)

    **B. Update apps/marketing/tailwind.config.ts:**
    - Remove `darkMode: 'media'` entirely (dark-only per D-04)
    - Change `fontFamily.sans` to `['var(--font-inter)', 'system-ui', '-apple-system', 'sans-serif']`
    - Add `fontFamily.display` as `['var(--font-plus-jakarta)', 'var(--font-inter)', 'system-ui', 'sans-serif']`
    - Update `fontFamily.mono` to `['var(--font-jetbrains-mono)', 'monospace']`
    - Update `fontSize.display` to `["56px", { lineHeight: "1.1", fontWeight: "600" }]` (per UI-SPEC)
    - Add `fontSize.heading` as `["32px", { lineHeight: "1.2", fontWeight: "600" }]`
    - Colors stay the same (use hsl(var(--marketing-*))) — values changed in CSS
    - Keep all existing animations and keyframes
    - Keep plugins: tailwindcss-animate, @tailwindcss/typography

    **C. Update packages/design/src/marketing.ts:**
    - Keep the `marketingTheme` object shape but update values for dark premium theme:
    - `colors.hero.bg` → `"from-[#070912] via-[#0c1020] to-[#070912]"`
    - `colors.surface.card` → `"#1a2332"`
    - `colors.surface.cardHover` → `"#243044"`
    - `colors.surface.muted` → `"#0c1020"`
    - `colors.text.primary` → `"#f1f5f9"`
    - `colors.text.secondary` → `"#94a3b8"`
    - `colors.text.muted` → `"#64748b"`
    - `colors.text.inverted` → `"#070912"`
    - Keep accent colors unchanged (#06b6d4, #0891b2, #10b981, #f59e0b, #ef4444)
    - `spacing.section` → `"py-24 md:py-32"`
    - `spacing.container` → `"max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"`
    - Update `typography.display` to `{ fontSize: 56, fontWeight: 600, lineHeight: 1.1 }`
    - Update `typography.heading` to `{ fontSize: 32, fontWeight: 600, lineHeight: 1.2 }`
  </action>
  <verify>
    <automated>
      # Verify CSS parses and Tailwind config loads
      node -e "
        const css = require('fs').readFileSync('apps/marketing/app/globals.css', 'utf8');
        if (!css.includes('--marketing-bg:')) throw new Error('Missing bg variable');
        if (!css.includes('--marketing-primary: 190 90% 50%')) throw new Error('Wrong primary');
        if (css.includes('@import url')) throw new Error('Font @import still present');
        console.log('CSS OK');
      "
      # Verify design tokens export
      node -e "
        const m = require('../../packages/design/src/marketing.ts');
        // Won't execute directly — check types via build
      "
      # Verify Tailwind config has new fonts
      node -e "
        const cfg = require('../../apps/marketing/tailwind.config.ts');
        // Module parse check will happen via build
      "
    </automated>
  </verify>
  <done>globals.css has no font @import, has all dark-only CSS variables, has noise overlay. Tailwind config has Inter/Plus Jakarta Sans/JetBrains Mono fonts. marketing.ts tokens updated to dark theme values.</done>
</task>

<task type="auto">
  <name>Task 2: Font loading in layout.tsx + Section/Container/PageHeader updates</name>
  <files>
    apps/marketing/app/[locale]/layout.tsx
    apps/marketing/components/layout/section.tsx
    apps/marketing/components/layout/container.tsx
    apps/marketing/components/ui/page-header.tsx
  </files>
  <read_first>
    - apps/marketing/app/[locale]/layout.tsx (existing — add font loading)
    - apps/marketing/components/layout/section.tsx (existing — update variant styles)
    - apps/marketing/components/layout/container.tsx (existing — minor update)
    - apps/marketing/components/ui/page-header.tsx (existing — update typography)
  </read_first>
  <action>
    **A. Update apps/marketing/app/[locale]/layout.tsx:**
    - Add imports for `Inter`, `Plus_Jakarta_Sans`, `JetBrains_Mono` from `next/font/google`
    - Configure Inter: `subsets: ['latin']`, `variable: '--font-inter'`, `display: 'swap'`, weight `['400', '600']`
    - Configure Plus_Jakarta_Sans: `subsets: ['latin']`, `variable: '--font-plus-jakarta'`, `display: 'swap'`, weight `['400', '600']`
    - Configure JetBrains_Mono: `subsets: ['latin']`, `variable: '--font-jetbrains-mono'`, `display: 'swap'`, weight `['400', '500', '600']`
    - Add the `.variable` and `.className` to the `<html>` element's `className` prop alongside `dark`:
      ```tsx
      className={`${inter.variable} ${plusJakartaSans.variable} ${jetbrainsMono.variable} dark`}
      ```
    - Keep all existing structure (rtlLocales, direction, providers, JSON-LD)

    **B. Update apps/marketing/components/layout/section.tsx:**
    - Replace `variantStyles` to use dark-only backgrounds:
      - `default` → `'bg-[#070912]'` (dominant color from UI-SPEC)
      - `alt` → `'bg-[#0c1020]'` (secondary color from UI-SPEC)
      - `dark` → `'bg-[#070912]'` (same as default, dominant)
    - Keep `py-24 md:py-32` spacing
    - Keep the Section, SectionProps exports unchanged

    **C. Update apps/marketing/components/layout/container.tsx:**
    - If max-w-7xl is 1280px, keep as-is. The `--marketing-container` CSS variable is 1280px.
    - No changes needed unless container width needs adjusting.
    - Ensure `mx-auto` and padding classes are present.

    **D. Update apps/marketing/components/ui/page-header.tsx:**
    - Read the existing file first to understand its current structure.
    - Update any hardcoded font-family references to use Tailwind `font-display` for headings.
    - Update any IBM Plex Sans references.
    - Ensure page title uses `font-display` class (Plus Jakarta Sans via CSS variable).
    - Update colors to use dark theme tokens (`text-foreground`, `text-muted`, etc.)
  </action>
  <verify>
    <automated>
      # Verify fonts are loaded in layout
      grep -c "Plus_Jakarta_Sans" apps/marketing/app/\[locale\]/layout.tsx
      grep -c "Inter" apps/marketing/app/\[locale\]/layout.tsx
      grep -c "\-\-font-inter" apps/marketing/app/\[locale\]/layout.tsx
      # Verify font variables on html element
      grep -c "inter.variable" apps/marketing/app/\[locale\]/layout.tsx
      grep -c "plusJakartaSans.variable" apps/marketing/app/\[locale\]/layout.tsx
      # Verify section variant updated
      grep -c "bg-\[#070912\]" apps/marketing/components/layout/section.tsx
    </automated>
  </verify>
  <done>layout.tsx loads Inter + Plus Jakarta Sans + JetBrains Mono via next/font/google and applies CSS variables. Section component uses dark backgrounds. Container unchanged. PageHeader uses font-display for headings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Marketing site is public-facing frontend with no user input on modified pages. Contact form already secured with Turnstile (unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | CSS variables globals.css | accept | CSS-only changes — no user input, no injection vector |
| T-04-02 | Information Disclosure | Google Fonts @import removal | mitigate | Using next/font/google (self-hosted) — no CDN requests from client, better privacy |
| T-04-03 | Denial of Service | Noise texture SVG | accept | Static SVG data URI, 256x256 repeat, minimal performance impact — runs on GPU composite layer |
</threat_model>

<verification>
- [ ] `pnpm --filter @repo/marketing check-types` passes
- [ ] `pnpm --filter @repo/marketing build` succeeds
- [ ] globals.css has no @import font directive
- [ ] tailwind.config.ts references --font-inter, --font-plus-jakarta, --font-jetbrains-mono
- [ ] marketing.ts design tokens reflect dark premium theme
- [ ] layout.tsx loads all 3 fonts via next/font/google
- [ ] Section component uses bg-[#070912] as default variant
</verification>

<success_criteria>
All 7 files modified as specified. TypeScript check and Next.js build both pass. Fonts load correctly with CSS variables applied to html element.
</success_criteria>

<output>
Create `.planning/phases/04-marketing-site-redesign/04-01-SUMMARY.md` when done
</output>
