---
phase: 04-marketing-site-redesign
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - apps/marketing/components/landing/hero-section.tsx
  - apps/marketing/components/landing/ai-grid-background.tsx
  - apps/marketing/components/landing/scroll-indicator.tsx
  - apps/marketing/components/landing/feature-showcase.tsx
  - apps/marketing/components/landing/feature-card.tsx
  - apps/marketing/components/landing/stats-section.tsx
  - apps/marketing/components/landing/ai-highlight-section.tsx
  - apps/marketing/components/landing/testimonial-carousel.tsx
  - apps/marketing/components/landing/testimonial-card.tsx
  - apps/marketing/components/landing/cta-section.tsx
  - apps/marketing/components/landing/trust-bar.tsx
autonomous: true
requirements: [MKT-01]
user_setup: []

must_haves:
  truths:
    - "Hero section has animated grid background, staggered entrance, and mixed WOW focal point (D-06)"
    - "FeatureShowcase shows 6 feature cards in a responsive grid with hover scale animations"
    - "FeatureCard is a GlassPanel with icon, title, description — hover scales 1.02"
    - "StatsSection uses StatsCounter components with animated numbers on scroll"
    - "TestimonialCarousel shows quote cards with glass styling"
    - "CTASection has gradient accent border and dual-action buttons per UI-SPEC copy"
    - "Every section has at least one subtle micro-animation (D-15)"
    - "All sections wrapped in AnimatedSection for scroll-triggered fade-up"
  artifacts:
    - path: "apps/marketing/components/landing/hero-section.tsx"
      provides: "Premium hero with staggered entrance, grid bg, mixed focal point"
      min_lines: 60
    - path: "apps/marketing/components/landing/feature-showcase.tsx"
      provides: "Feature cards grid layout"
      uses: "FeatureCard, AnimatedSection, Section, Container"
    - path: "apps/marketing/components/landing/cta-section.tsx"
      provides: "Final CTA with glass styling and dual buttons"
      uses: "Button"
  key_links:
    - from: "hero-section.tsx"
      to: "ai-grid-background.tsx"
      via: "imports and renders AIGridBackground"
    - from: "stats-section.tsx"
      to: "components/shared/stats-counter.tsx"
      via: "imports StatsCounter for each stat item"
    - from: "feature-card.tsx"
      to: "components/shared/glass-panel.tsx"
      via: "uses GlassPanel as wrapper with hover prop"
---

<objective>
Rewrite all 11 landing section components with the new premium dark aesthetic — hero with staggered entrance and WOW focal point, feature cards in glass panels, animated stats counter, testimonial carousel, and CTA with glass styling.

Purpose: These are the core content sections of the homepage and are also reused as section building blocks across product/solution pages. Delivering them in Wave 2 allows Plan 04-05 (homepage rewrite) to compose them immediately.
Output: All 11 landing section files rewritten with motion scroll animations, glassmorphism styling, and dark theme consistency.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-marketing-site-redesign/04-CONTEXT.md
@.planning/phases/04-marketing-site-redesign/04-RESEARCH.md
@.planning/phases/04-marketing-site-redesign/04-UI-SPEC.md

## Key References
- **D-06**: Mixed hero approach — WOW animated/interactive focal point + 1-2 strategic WOW moments  
- **D-15**: Subtle micro-animations — fade-up on scroll, hover scale on cards, smooth transitions
- **D-16**: Animated elements: major section pieces only — hero, section titles, feature cards, stats counters, CTA
- **UI-SPEC Interaction & Animation Contract**: All animation values (duration, easing, stagger delays, hover scale)
- **UI-SPEC Copywriting Contract**: Hero CTAs, CTA section text, empty states
- **UI-SPEC Colors**: Text Primary (#f1f5f9), Text Secondary (#94a3b8), Accent (#06b6d4)
</context>

<interfaces>
<!-- Key types and contracts from already-built components -->

From Plan 04-02 — components/shared/*:
```
GlassPanel: { children, className?, hover?, as? }
StatsCounter: { value, suffix?, prefix?, duration?, decimals?, className? }
AnimatedSection: { children, className?, as?, delay? }
Button: { variant: 'primary'|'secondary'|'ghost'|'glass', size: 'sm'|'md'|'lg'|'xl', ... }
```

From Plan 04-01 — components/layout/*:
```
Section: { id?, variant?: 'default'|'alt'|'dark', children, className? }
Container: { children, className? }
```

Message keys (from fr.json existing — will be extended in Plan 04-06):
```
hero: { headline, subtitle, trustBar, scrollHint }
features: { heading, subheading }
featureCards: [{ title, description }]  // array of 6
ai: { heading, subheading }
testimonials: { heading }
stats: { uptime, monitoring, teams, selfHosted }
finalCta: { heading, body, action }
cta: { seeInAction, talkToSales }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite HeroSection + AIGridBackground + ScrollIndicator</name>
  <files>
    apps/marketing/components/landing/hero-section.tsx
    apps/marketing/components/landing/ai-grid-background.tsx
    apps/marketing/components/landing/scroll-indicator.tsx
  </files>
  <read_first>
    - apps/marketing/components/landing/hero-section.tsx (existing — full rewrite)
    - apps/marketing/components/landing/ai-grid-background.tsx (existing — full rewrite)
    - apps/marketing/components/landing/scroll-indicator.tsx (existing — full rewrite)
    - apps/marketing/components/shared/glass-panel.tsx (reference — pattern to use)
    - .planning/phases/04-marketing-site-redesign/04-UI-SPEC.md (Interaction Contract for hero entrance stagger values, Copywriting for hero CTAs)
  </read_first>
  <action>
    **A. Rewrite `components/landing/hero-section.tsx`:**
    - `'use client'` component
    - Import: `motion` from `motion/react`, `useTranslations`, `Container`, `Button`, `AIGridBackground`, `ScrollIndicator`
    - Uses `useTranslations('hero')` for headline/subtitle and `useTranslations('cta')` for CTA buttons
    - Structure:
      - Wrapper: `relative min-h-[85vh] flex items-center overflow-hidden pt-24`
      - Render `<AIGridBackground />` as absolute-positioned background
      - Content container with staggered motion children:
        - **EyebrowTag** (accent variant): "Plateforme de sécurité IA" (hardcoded French — or from messages if key exists)
        - **H1 heading**: `text-[56px] font-display font-semibold leading-[1.1] text-white max-w-3xl`
          - Staggered: `initial={{ opacity: 0, y: 24 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0, duration: 0.7, ease: [0.16,1,0.3,1] }}`
        - **Subtitle**: `text-lg text-[#94a3b8] max-w-2xl mt-6`
          - Staggered with delay 0.15s
        - **CTA buttons row**: Two buttons side by side:
          - Primary CTA "Voir Oversight Hub en action" → `/${locale}/demo` (Button variant: primary, size: xl)
          - Secondary CTA "Parler aux ventes" → `/${locale}/contact` (Button variant: glass, size: xl)
          - Staggered with delay 0.3s
        - **TrustBar text**: Below buttons, `text-sm text-[#64748b]`, staggered with delay 0.5s
      - Bottom: `<ScrollIndicator />` centered below the fold
    - Use motion staggered children or manual delay staggering per UI-SPEC (0s, 0.15s, 0.3s, 0.5s)
    - The hero itself is the WOW moment — no separate "animated product mockup" needed if the grid + stagger create enough impact. Agent discretion per D-06.

    **B. Rewrite `components/landing/ai-grid-background.tsx`:**
    - Server component
    - Renders a fixed/animated grid pattern background:
      - `absolute inset-0 overflow-hidden pointer-events-none`
      - Animated grid: Create a div with `bg-grid` class (from globals.css)
      - Add a subtle radial gradient overlay: `bg-gradient-to-b from-transparent via-transparent to-[#070912]`
      - Add optional floating gradient orbs (cyber accents):
        - One at top-right: `absolute -top-40 -right-40 w-96 h-96 bg-cyan-500/10 rounded-full blur-3xl`
        - One at bottom-left: `absolute -bottom-40 -left-40 w-80 h-80 bg-blue-500/10 rounded-full blur-3xl`
    
    **C. Rewrite `components/landing/scroll-indicator.tsx`:**
    - `'use client'` component (for motion animation)
    - Simple animated chevron/arrow bouncing:
      - `flex flex-col items-center gap-2 text-[#64748b] pb-8`
      - Text: "Faites défiler" or from i18n `hero.scrollHint`
      - Animated chevron with `motion.div` that bounces Y on a loop using `animate={{ y: [0, 8, 0] }} transition={{ repeat: Infinity, duration: 2 }}`
  </action>
  <verify>
    <automated>
      grep -c "whileInView\|initial.*opacity.*y" apps/marketing/components/landing/hero-section.tsx
      grep -c "ScrollIndicator" apps/marketing/components/landing/hero-section.tsx
      grep -c "bg-grid" apps/marketing/components/landing/ai-grid-background.tsx
      grep -c "animate.*repeat.*Infinity" apps/marketing/components/landing/scroll-indicator.tsx || grep -c "repeat: Infinity" apps/marketing/components/landing/scroll-indicator.tsx
    </automated>
  </verify>
  <done>HeroSection has staggered entrance with motion, animated grid background, floating gradient orbs, dual CTA buttons, scroll indicator. AIGridBackground renders grid pattern with gradient overlays. ScrollIndicator has bouncing chevron animation.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite FeatureShowcase + FeatureCard + StatsSection</name>
  <files>
    apps/marketing/components/landing/feature-showcase.tsx
    apps/marketing/components/landing/feature-card.tsx
    apps/marketing/components/landing/stats-section.tsx
  </files>
  <read_first>
    - apps/marketing/components/landing/feature-showcase.tsx (existing — full rewrite)
    - apps/marketing/components/landing/feature-card.tsx (existing — full rewrite)
    - apps/marketing/components/landing/stats-section.tsx (existing — full rewrite)
    - apps/marketing/components/shared/glass-panel.tsx (reference)
    - apps/marketing/components/shared/stats-counter.tsx (reference)
  </read_first>
  <action>
    **A. Rewrite `components/landing/feature-showcase.tsx`:**
    - `'use client'` component
    - Import: `useTranslations`, `Section`, `Container`, `AnimatedSection`, `FeatureCard`
    - Structure:
      - `<Section variant="alt">` → `<Container>` → heading + subheading + grid
      - Section heading: `font-display text-[32px] text-white` — wrapped in AnimatedSection
      - Subheading: `text-muted max-w-2xl` — wrapped in AnimatedSection with delay
      - Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-16`
      - FeatureCard loop — use the 6 featureCards from `useTranslations('featureCards')` array
      - Each card wrapped in AnimatedSection with staggered delay (index * 0.1)

    **B. Rewrite `components/landing/feature-card.tsx`:**
    - `'use client'` component (for motion transitions)
    - Import: `GlassPanel`, `motion`
    - Props: `{ icon: React.ReactNode; title: string; description: string; index?: number }`
    - Wraps content in `<GlassPanel hover={true} className="p-6">`:
      - Icon container: `w-12 h-12 rounded-xl bg-cyan-500/10 flex items-center justify-center text-cyan-400 mb-4`
      - Title: `text-lg font-semibold text-white font-display`
      - Description: `text-sm text-[#94a3b8] mt-2 leading-relaxed`

    **C. Rewrite `components/landing/stats-section.tsx`:**
    - `'use client'` component
    - Import: `Section`, `Container`, `AnimatedSection`, `StatsCounter`
    - Use `useTranslations('stats')` for labels
    - Stats data (hardcoded values + i18n labels):
      - `{ value: 99.9, suffix: '%', labelKey: 'uptime' }`
      - `{ value: 247, suffix: '', labelKey: 'monitoring' }` — display as "24/7" (special case — render as text, not counter)
      - `{ value: 150, suffix: '+', labelKey: 'teams' }`
      - `{ value: 100, suffix: '%', labelKey: 'selfHosted' }`
    - Section variant="dark" with `bg-gradient-to-b from-[#070912] via-[#0c1020] to-[#070912]` for visual interest
    - Grid: 4 columns, center-aligned
    - Each stat cell: AnimatedSection wrapping StatsCounter (value) + label text
    - Label: `text-sm text-[#94a3b8] mt-2`
    - For "24/7" monitoring, render a static span instead of StatsCounter
  </action>
  <verify>
    <automated>
      grep -c "GlassPanel" apps/marketing/components/landing/feature-card.tsx
      grep -c "StatsCounter" apps/marketing/components/landing/stats-section.tsx
      grep -c "md:grid-cols-2 lg:grid-cols-3" apps/marketing/components/landing/feature-showcase.tsx
    </automated>
  </verify>
  <done>FeatureShowcase renders 3-column grid of FeatureCards. FeatureCard uses GlassPanel with hover effect. StatsSection uses StatsCounter for animated numbers.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite AIHighlight + TestimonialCarousel + TestimonialCard + CTASection + TrustBar</name>
  <files>
    apps/marketing/components/landing/ai-highlight-section.tsx
    apps/marketing/components/landing/testimonial-carousel.tsx
    apps/marketing/components/landing/testimonial-card.tsx
    apps/marketing/components/landing/cta-section.tsx
    apps/marketing/components/landing/trust-bar.tsx
  </files>
  <read_first>
    - apps/marketing/components/landing/ai-highlight-section.tsx (existing — full rewrite)
    - apps/marketing/components/landing/testimonial-carousel.tsx (existing — full rewrite)
    - apps/marketing/components/landing/testimonial-card.tsx (existing — full rewrite)
    - apps/marketing/components/landing/cta-section.tsx (existing — full rewrite)
    - apps/marketing/components/landing/trust-bar.tsx (existing — full rewrite)
    - .planning/phases/04-marketing-site-redesign/04-UI-SPEC.md (Copywriting for CTA section, Interaction Contract for hover/scroll animations)
  </read_first>
  <action>
    **A. Rewrite `components/landing/ai-highlight-section.tsx`:**
    - `'use client'` component
    - Use `useTranslations('ai')` for heading/subheading
    - Structure:
      - `<Section variant="alt">` → `<Container>` → 2-column layout
      - Left column (60%): heading (font-display text-white), subheading (text-muted), feature list (3-4 bullet points with cyan check icons)
      - Right column (40%): A "platform-in-action" WOW moment — a decorative glass panel with embedded mockup label, or a `GradientBorder` wrapping a styled mockup placeholder
      - Use `AnimatedSection` with stagger on the left content
      - Keep the narrative: "Built on AI. Powered by you." (D-02)
    
    **B. Rewrite `components/landing/testimonial-carousel.tsx` and `testimonial-card.tsx`:**
    - **testimonial-card.tsx**: 
      - Props: `{ quote: string; author: string; role: string; company: string; avatar?: string }`
      - Renders a `<GlassPanel hover={true} className="p-6">` with:
        - Quote text: `text-sm text-[#94a3b8] italic leading-relaxed`
        - Quote mark icon: `text-cyan-400/30 text-3xl font-display` at top — `"`
        - Author info at bottom: name (text-white font-semibold), role+company (text-muted text-xs)
    
    - **testimonial-carousel.tsx**:
      - `'use client'` component
      - Keep simple — not a swipeable carousel, just a grid of TestimonialCards
      - Use `useTranslations('testimonials')` for heading
      - For now, use hardcoded testimonial data (2-3 testimonials in French) or from messages
      - Grid: `grid grid-cols-1 md:grid-cols-3 gap-6`
      - Each card wrapped in AnimatedSection with staggered delay
    
    **C. Rewrite `components/landing/cta-section.tsx`:**
    - `'use client'` component
    - Use `useTranslations('finalCta')` for CTA content, `useTranslations('cta')` for button labels
    - Structure:
      - `<Section variant="default">` → `<Container>` → centered content
      - Wrap content in a `GradientBorder` for the premium CTA card feel
      - Inner card: `py-16 px-8 text-center bg-[#070912] rounded-2xl`
      - Heading: `text-[32px] font-display text-white` — wrapped in AnimatedSection
      - Body: `text-muted max-w-xl mx-auto mt-4` — wrapped in AnimatedSection with delay
      - CTA row: Two buttons side by side (similar to hero but different copy):
        - Primary: "Réserver votre démo" → /contact (Button primary, size: lg)
        - Secondary: "Explorer les tarifs" → /pricing (Button glass, size: lg)
      - AnimatedSection wrapping buttons with delay

    **D. Rewrite `components/landing/trust-bar.tsx`:**
    - Server component
    - Simple: Section with brand logos or trust text
    - Renders below hero — `py-12` only (reduced from section-level spacing)
    - Text: "Approuvé par les équipes de sécurité du monde entier" from i18n
    - Simple: `text-center text-sm text-[#64748b] tracking-wide uppercase`
    - If brand logos exist, render them as muted SVG placeholders; otherwise just text
  </action>
  <verify>
    <automated>
      grep -c "GradientBorder\|GlassPanel" apps/marketing/components/landing/ai-highlight-section.tsx
      grep -c "GlassPanel" apps/marketing/components/landing/testimonial-card.tsx
      grep -c "GradientBorder" apps/marketing/components/landing/cta-section.tsx
      # All files exist and compile
      for f in ai-highlight-section testimonial-carousel testimonial-card cta-section trust-bar; do
        [ -f "apps/marketing/components/landing/$f.tsx" ] && echo "✅ $f.tsx exists" || echo "❌ $f.tsx missing"
      done
    </automated>
  </verify>
  <done>AIHighlight has 2-column layout with platform mockup highlight. Testimonial cards use GlassPanel in 3-column grid. CTASection has GradientBorder wrapper with dual CTA buttons. TrustBar renders muted trust text or logos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | All landing sections are presentational. No user input, no API calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Denial of Service | Multiple motion whileInView observers | mitigate | Each AnimatedSection uses viewport: { once: true } — observers disconnect after trigger. Max ~6 animated sections per page — well within limit. |
| T-04-09 | Denial of Service | Grid gradient orbs blur-3xl | accept | CSS blur on positioned elements — GPU composited. 2 blur orbs max. No impact on scrolling performance. |
</threat_model>

<verification>
- [ ] `pnpm --filter @repo/marketing check-types` passes
- [ ] All 11 landing section files rewritten
- [ ] Each section uses AnimatedSection for scroll-triggered fade-up
- [ ] FeatureCard wraps content in GlassPanel with hover effect
- [ ] StatsSection uses StatsCounter for animated numbers
- [ ] CTASection uses GradientBorder wrapper
- [ ] Hero has staggered entrance with motion animations
</verification>

<success_criteria>
All 11 landing section files rewritten with premium dark aesthetic. Every section has at least one subtle micro-animation. Glassmorphism used consistently via GlassPanel and GradientBorder. TypeScript compilation passes.
</success_criteria>

<output>
Create `.planning/phases/04-marketing-site-redesign/04-04-SUMMARY.md` when done
</output>
