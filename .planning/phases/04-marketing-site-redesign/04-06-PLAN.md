---
phase: 04-marketing-site-redesign
plan: 06
type: execute
wave: 3
depends_on: [04-01]
files_modified:
  - apps/marketing/messages/fr.json
  - apps/marketing/messages/en.json
  - apps/marketing/messages/es.json
  - apps/marketing/messages/de.json
  - apps/marketing/messages/ja.json
  - apps/marketing/messages/ar.json
  - apps/marketing/velite.config.ts
  - apps/marketing/content/case-studies/fr/campus-entreprise.mdx
  - apps/marketing/content/case-studies/fr/infrastructure-critique.mdx
  - apps/marketing/content/case-studies/en/enterprise-campus.mdx
  - apps/marketing/content/case-studies/en/critical-infrastructure.mdx
  - apps/marketing/app/sitemap.ts
autonomous: true
requirements: [MKT-03]
user_setup: []

must_haves:
  truths:
    - "All 6 locale JSON files contain new message keys: nav.produits, nav.solutions, nav.etudesDeCas, nav.demo, produits.*, solutions.*, caseStudies.*, demo.*"
    - "All locale files parse as valid JSON"
    - "velite.config.ts has a caseStudies collection with schema including title, slug, date, locale, industry, client, excerpt, results, content"
    - "2 case study MDX files in fr/ and 2 in en/ directories"
    - "Sitemap includes produits/, solutions/, etudes-de-cas/, demo/ routes for all locales"
  artifacts:
    - path: "apps/marketing/messages/fr.json"
      provides: "French translations with all new route keys"
      contains: '"produits"'
    - path: "apps/marketing/velite.config.ts"
      provides: "MDX content pipeline with caseStudies collection"
      contains: "caseStudies"
    - path: "apps/marketing/app/sitemap.ts"
      provides: "Updated sitemap with all new routes"
      contains: "/produits"
  key_links:
    - from: "messages/*.json"
      to: "Header component"
      via: "useTranslations('nav') reads nav.produits, nav.solutions, etc."
    - from: "content/case-studies/*.mdx"
      to: "velite.config.ts"
      via: "caseStudies collection pattern matches content/case-studies/**/*.mdx"
    - from: "sitemap.ts"
      to: "velite"
      via: "imports getAllSlugsByLocale for case study slugs"
---

<objective>
Add all new i18n message keys across 6 locale files, extend the velite content pipeline for case studies, create sample case study MDX content, and update the sitemap with all new routes.

Purpose: All new pages (products, solutions, case studies, demo) depend on message keys being available. The velite caseStudies collection and sample MDX content are needed before case study pages can render. Sitemap must include all new routes for SEO.
Output: 6 locale files updated with complete key sets, velite.config.ts extended with caseStudies, 2 case study MDX content pairs (fr/en), sitemap updated.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-marketing-site-redesign/04-CONTEXT.md
@.planning/phases/04-marketing-site-redesign/04-RESEARCH.md
@.planning/phases/04-marketing-site-redesign/04-UI-SPEC.md

## Key References
- **D-18**: French-first — all content written in French first, translated to EN/ES/DE/JA/AR
- **D-19**: Full RTL for Arabic (ar) — mirrored layout direction (handled in layout.tsx, but message keys needed)
- **UI-SPEC Copywriting Contract**: Exact nav labels, CTA text, heading copy for all new routes
- **UI-SPEC Page Architecture**: Route paths: produits, solutions, etudes-de-cas, demo
- **RESEARCH.md Patterns**: velite config pattern, case study collection schema, message key structure
- **Existing fr.json**: Current message structure (nav, cta, hero, features, featureCards, ai, testimonials, stats, finalCta, pricing, blog, contact, errors, notFound, footer)
- **Existing sitemap.ts**: Per-locale entries for /, /pricing, /blog, blog post slugs
</context>

<interfaces>
<!-- Existing patterns from codebase -->

Current message structure (fr.json):
```
{
  "nav": { "home": "...", "pricing": "...", "blog": "...", "contact": "..." },
  "cta": { "bookDemo": "...", "talkToSales": "...", ... },
  "hero": { "headline": "...", "subtitle": "...", ... },
  "features": { "heading": "...", "subheading": "..." },
  "featureCards": [{ "title": "...", "description": "..." }, ...],
  "ai": { "heading": "...", "subheading": "..." },
  "testimonials": { "heading": "...", "subheading": "..." },
  "stats": { "uptime": "...", "monitoring": "...", "teams": "...", "selfHosted": "..." },
  "finalCta": { "heading": "...", "body": "...", "action": "..." },
  "pricing": { "heading": "...", "subheading": "...", "tiers": {...}, ... },
  "blog": { "heading": "...", "subheading": "...", "categories": {...}, "empty": {...}, "readMore": "...", ... },
  "contact": { "heading": "...", ... },
  "errors": { ... },
  "notFound": { ... },
  "footer": { "product": "...", "resources": "...", "company": "...", "legal": "...", "copyright": "..." }
}
```

Existing velite posts schema:
```
s.object({
  title: s.string().max(100), slug: s.slug('posts'), date: s.isodate(),
  locale: s.string(), category: s.string(), excerpt: s.excerpt(),
  cover: s.image().optional(), tags: s.array(s.string()).optional(),
  content: s.mdx(), metadata: s.metadata(),
})
```

Existing sitemap pattern:
- For each locale, add entries for /, /pricing, /blog
- Then try/catch import velite getAllSlugsByLocale for blog posts
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update all 6 locale JSON files with new message keys</name>
  <files>
    apps/marketing/messages/fr.json
    apps/marketing/messages/en.json
    apps/marketing/messages/es.json
    apps/marketing/messages/de.json
    apps/marketing/messages/ja.json
    apps/marketing/messages/ar.json
  </files>
  <read_first>
    - apps/marketing/messages/fr.json (existing — add new keys, French-first)
    - apps/marketing/messages/en.json (existing — translate from French)
    - .planning/phases/04-marketing-site-redesign/04-RESEARCH.md (New i18n Message Keys section — exact key structure to add)
    - .planning/phases/04-marketing-site-redesign/04-UI-SPEC.md (Copywriting Contract — exact copy for nav, CTAs, headings)
  </read_first>
  <action>
    Read each existing locale file and add new keys. **Do NOT remove or change existing keys.**

    **Keys to add to EVERY locale file:**

    1. **Extend the existing `"nav"` section** with the new nav link labels (these are consumed by `useTranslations('nav')` in Header and MobileMenu — per Plan 03):
    ```json
    // Inside the existing "nav" object, add:
    "produits": "Produits",
    "solutions": "Solutions",
    "etudesDeCas": "Études de cas",
    "demo": "Démo"
    ```

    2. **Add these new top-level sections:**
    ```json
    "produits": {
      "heading": "Nos solutions de sécurité physique",
      "subheading": "Une plateforme unifiée pour la vidéo, le contrôle d'accès et l'analyse IA — conçue pour les équipes de sécurité modernes.",
      "cta": "En savoir plus",
      "video": {
        "name": "Video Intelligence",
        "description": "Surveillance vidéo en temps réel avec analyse IA, détection d'anomalies et corrélation d'événements.",
        "features": ["Analyse vidéo en temps réel", "Détection d'anomalies par IA", "Corrélation multi-caméras", "Export et partage d'extraits"]
      },
      "accessControl": {
        "name": "Contrôle d'accès",
        "description": "Gérez les badges, les portes et les zones d'accès depuis un tableau de bord centralisé avec des règles dynamiques.",
        "features": ["Gestion des badges et identités", "Contrôle de portes en temps réel", "Règles d'accès dynamiques", "Historique complet des accès"]
      },
      "aiAnalytics": {
        "name": "Analytique IA",
        "description": "Analyse prédictive, détection de comportements suspects et rapports automatisés alimentés par l'apprentissage automatique.",
        "features": ["Détection de comportements", "Analyse prédictive", "Rapports automatisés", "Tableaux de bord personnalisables"]
      },
      "analytics": {
        "name": "Rapports & Analytics",
        "description": "Visualisez les tendances, générez des rapports de conformité et suivez les indicateurs clés de performance de votre sécurité.",
        "features": ["Tendances et statistiques", "Rapports de conformité", "Indicateurs personnalisés", "Export PDF et CSV"]
      }
    },
    "solutions": {
      "heading": "Solutions par secteur",
      "subheading": "Des solutions adaptées aux défis uniques de chaque secteur d'activité.",
      "cta": "Explorer",
      "enterprise": {
        "name": "Campus d'entreprise",
        "description": "Sécurisez vos campus multi-bâtiments avec une solution unifiée qui intègre vidéo, accès et IA.",
        "challenges": ["Centaines de portes et badges à gérer", "Sites multiples répartis géographiquement", "Conformité réglementaire complexe"],
        "outcomes": ["Réduction de 40% des incidents de sécurité", "Gestion centralisée de 50+ bâtiments", "Économies de 60% sur les opérations"]
      },
      "critical": {
        "name": "Infrastructure critique",
        "description": "Protégez vos sites sensibles avec une sécurité physique renforcée et une surveillance IA continue.",
        "challenges": ["Menaces de sécurité élevées", "Exigences strictes de conformité", "Besoin de disponibilité 24/7"],
        "outcomes": ["Détection des menaces en temps réel", "Conformité réglementaire automatisée", "Temps de réponse réduit de 70%"]
      }
    },
    "caseStudies": {
      "heading": "Études de cas",
      "subheading": "Découvrez comment les organisations utilisent Oversight Hub pour transformer leur sécurité physique.",
      "readMore": "Lire l'étude complète",
      "results": "Résultats clés",
      "related": "Études de cas similaires",
      "empty": "Aucune étude de cas pour le moment. Revenez bientôt."
    },
    "demo": {
      "heading": "Voir Oversight Hub en action",
      "subheading": "Suivez un scénario de sécurité complet, étape par étape, sans créer de compte.",
      "startTour": "Lancer la visite",
      "next": "Suivant",
      "previous": "Précédent",
      "step": "Étape {current} sur {total}",
      "startOver": "Recommencer"
    }
    ```

    **Translation workflow per D-18:**
    1. Start with `fr.json` — write French copy first
    2. Translate to `en.json` — English (professional translation quality)
    3. For `es.json`, `de.json`, `ja.json`, `ar.json`: Use reasonable translations. For ES/DE use professional quality. For JA/AR, note these need professional review.
    
    **For each file**, add the keys in the correct JSON structure, maintaining valid JSON.

    **Arabic special handling (D-19):**
    - `ar.json` uses Arabic text — write the keys in Arabic
    - Example (inside existing `"nav"`): `"produits": "المنتجات", "solutions": "الحلول", ...`
    
    Verify each file is valid JSON after editing: `node -e "JSON.parse(require('fs').readFileSync('apps/marketing/messages/{locale}.json','utf8'))"`
  </action>
  <verify>
    <automated>
      # All 6 files are valid JSON
      for f in apps/marketing/messages/*.json; do
        node -e "JSON.parse(require('fs').readFileSync('$f','utf8'))" && echo "✅ $f valid"
      done
      # Key presence checks (on fr.json as reference)
      node -e "
        const m = JSON.parse(require('fs').readFileSync('apps/marketing/messages/fr.json','utf8'));
        const checks = ['nav.produits', 'nav.solutions', 'nav.etudesDeCas', 'nav.demo', 'produits', 'produits.heading', 'produits.video.name',
          'solutions', 'solutions.heading', 'solutions.enterprise.name', 'caseStudies', 'caseStudies.heading',
          'demo', 'demo.heading', 'demo.step'];
        const keys = new Set();
        function flatten(obj, prefix) {
          for (const k of Object.keys(obj)) {
            const p = prefix ? prefix + '.' + k : k;
            keys.add(p);
            if (typeof obj[k] === 'object' && !Array.isArray(obj[k])) flatten(obj[k], p);
          }
        }
        flatten(m, '');
        const missing = checks.filter(c => !keys.has(c));
        if (missing.length) throw new Error('Missing keys: ' + missing.join(', '));
        console.log('✅ All required keys present');
      "
    </automated>
  </verify>
  <done>All 6 locale files have new nav keys (produits, solutions, etudesDeCas, demo) inside the existing `"nav"` section, plus new top-level sections (produits, solutions, caseStudies, demo). Files parse as valid JSON. Arabic file has RTL-appropriate Arabic text.</done>
</task>

<task type="auto">
  <name>Task 2: Extend velite config + create case study MDX content</name>
  <files>
    apps/marketing/velite.config.ts
    apps/marketing/content/case-studies/fr/campus-entreprise.mdx
    apps/marketing/content/case-studies/fr/infrastructure-critique.mdx
    apps/marketing/content/case-studies/en/enterprise-campus.mdx
    apps/marketing/content/case-studies/en/critical-infrastructure.mdx
  </files>
  <read_first>
    - apps/marketing/velite.config.ts (existing — extend with caseStudies collection)
    - apps/marketing/content/blog/fr/ (existing blog post MDX — reference for MDX format and frontmatter)
    - .planning/phases/04-marketing-site-redesign/04-RESEARCH.md (velite Case Study Collection pattern)
  </read_first>
  <action>
    **A. Extend apps/marketing/velite.config.ts:**
    - Keep existing `posts` collection unchanged
    - Add a `caseStudies` collection:
      ```typescript
      caseStudies: {
        name: 'CaseStudy',
        pattern: 'content/case-studies/**/*.mdx',
        schema: s.object({
          title: s.string().max(100),
          slug: s.slug('case-studies'),
          date: s.isodate(),
          locale: s.string(),
          industry: s.string(),
          client: s.string(),
          excerpt: s.excerpt(),
          cover: s.image().optional(),
          results: s.array(s.object({
            metric: s.string(),
            value: s.string(),
          })).optional(),
          content: s.mdx(),
          metadata: s.metadata(),
        }),
      },
      ```

    **B. Create content/case-studies/fr/ directory and first MDX file:**
    - Create `content/case-studies/fr/campus-entreprise.mdx`:
      ```mdx
      ---
      title: "Sécurisation d'un campus multi-bâtiments avec Oversight Hub"
      slug: "campus-entreprise"
      date: 2026-06-15
      locale: "fr"
      industry: "enterprise"
      client: "Groupe Industrial Tech"
      excerpt: "Comment un groupe industriel de 5 000 employés a unifié sa sécurité physique sur 12 bâtiments avec Oversight Hub, réduisant les incidents de 40%."
      results:
        - metric: "Réduction des incidents"
          value: "40%"
        - metric: "Bâtiments connectés"
          value: "12"
        - metric: "Économies opérationnelles"
          value: "60%"
      ---

      ## Le défi

      Le Groupe Industrial Tech gérait 12 bâtiments répartis sur 3 sites avec des systèmes de sécurité disparates...

      ## La solution

      Oversight Hub a déployé une plateforme unifiée...

      ## Les résultats

      En 6 mois, le groupe a constaté...
      ```
    
    - Create `content/case-studies/fr/infrastructure-critique.mdx` with similar structure but for critical infrastructure use case

    **C. Create content/case-studies/en/ directory with English translations:**
    - Create `content/case-studies/en/enterprise-campus.mdx` (English version of campus-entreprise)
    - Create `content/case-studies/en/critical-infrastructure.mdx` (English version of infrastructure-critique)

    Both English files should be professional translations of the French originals.
    
    For both case studies, use realistic placeholder content (2-3 paragraphs per section) that tells a compelling security story. The content should be representative but editable later.
  </action>
  <verify>
    <automated>
      # velite config has caseStudies collection
      grep -c "caseStudies" apps/marketing/velite.config.ts
      # MDX files exist
      for f in apps/marketing/content/case-studies/fr/campus-entreprise.mdx \
               apps/marketing/content/case-studies/fr/infrastructure-critique.mdx \
               apps/marketing/content/case-studies/en/enterprise-campus.mdx \
               apps/marketing/content/case-studies/en/critical-infrastructure.mdx; do
        [ -f "$f" ] && echo "✅ $f exists" || echo "❌ $f missing"
      done
      # MDX files have frontmatter
      grep -c "title:" apps/marketing/content/case-studies/fr/campus-entreprise.mdx
      grep -c "slug:" apps/marketing/content/case-studies/fr/campus-entreprise.mdx
    </automated>
  </verify>
  <done>velite.config.ts has caseStudies collection with full schema. 4 MDX case study files created (2 fr, 2 en) with proper frontmatter and content.</done>
</task>

<task type="auto">
  <name>Task 3: Update sitemap with new routes</name>
  <files>apps/marketing/app/sitemap.ts</files>
  <read_first>
    - apps/marketing/app/sitemap.ts (existing — add new route entries)
  </read_first>
  <action>
    Update `apps/marketing/app/sitemap.ts`:

    - Keep existing entries for root (/), /pricing, /blog, and blog post slugs
    - Add entries for each new static route per locale:
      - `/produits` — priority: 0.9, changeFrequency: 'weekly'
      - `/produits/video` — priority: 0.7
      - `/produits/access-control` — priority: 0.7
      - `/produits/ai-analytics` — priority: 0.7
      - `/produits/analytics` — priority: 0.7
      - `/solutions` — priority: 0.9
      - `/solutions/enterprise-campuses` — priority: 0.7
      - `/solutions/critical-infrastructure` — priority: 0.7
      - `/etudes-de-cas` — priority: 0.8
      - `/demo` — priority: 0.7

    - Follow the same pattern as existing entries: per-locale loop, with alternates per locale
    - Each entry should have `alternates.languages` with all 6 locale variants
    - Include x-default pointing to English
    
    - For case study detail pages (`/etudes-de-cas/[slug]`): These come from velite. Add a try/catch block similar to the blog posts pattern that imports getAllSlugsByLocale and adds entries. For now, the sitemap generator can skip case study slugs gracefully if velite content isn't built yet (same try/catch pattern as blog posts).
  </action>
  <verify>
    <automated>
      grep -c "/produits" apps/marketing/app/sitemap.ts
      grep -c "/solutions" apps/marketing/app/sitemap.ts
      grep -c "/etudes-de-cas" apps/marketing/app/sitemap.ts
      grep -c "/demo" apps/marketing/app/sitemap.ts
      grep -c "case-studies" apps/marketing/app/sitemap.ts
    </automated>
  </verify>
  <done>Sitemap includes entries for /produits (all 5 routes), /solutions (all 3 routes), /etudes-de-cas, /demo, and case study detail slugs (velite-driven).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time content pipeline | velite processes MDX at build time. No runtime risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | JSON message files | accept | Static JSON — no user input. Validated by JSON.parse at build. |
| T-04-12 | Tampering | velite MDX content | accept | Built at compile time. Content authored by developers. |
</threat_model>

<verification>
- [ ] All 6 locale files parse as valid JSON
- [ ] All required message keys present in reference (fr.json)
- [ ] velite.config.ts type-checks with new caseStudies collection
- [ ] 4 MDX case study files exist with proper frontmatter
- [ ] Sitemap includes all new routes with alternates
- [ ] `pnpm --filter @repo/marketing check-types` passes
</verification>

<success_criteria>
All 6 locale files updated with complete new key sets. velite pipeline extended with caseStudies collection. 4 sample case study MDX files created. Sitemap covers all new routes with per-locale hreflang entries.
</success_criteria>

<output>
Create `.planning/phases/04-marketing-site-redesign/04-06-SUMMARY.md` when done
</output>
