---
phase: 04-marketing-site-redesign
plan: 08
type: execute
wave: 4
depends_on: [04-03, 04-06]
files_modified:
  - apps/marketing/app/[locale]/solutions/page.tsx
  - apps/marketing/app/[locale]/solutions/enterprise-campuses/page.tsx
  - apps/marketing/app/[locale]/solutions/critical-infrastructure/page.tsx
  - apps/marketing/components/solutions/solution-card.tsx
  - apps/marketing/components/solutions/solution-grid.tsx
  - apps/marketing/components/solutions/solution-detail-layout.tsx
  - apps/marketing/components/solutions/solution-hero.tsx
autonomous: true
requirements: [MKT-02]
user_setup: []

must_haves:
  truths:
    - "Solutions overview at /solutions shows Hero + SolutionGrid with 2 industry cards + CTA"
    - "Enterprise Campuses page at /solutions/enterprise-campuses has detail layout with challenges and outcomes"
    - "Critical Infrastructure page at /solutions/critical-infrastructure has detail layout with challenges and outcomes"
    - "SolutionDetailLayout renders challenges list and outcomes list with appropriate visual styling"
    - "All pages have locale-aware metadata"
  artifacts:
    - path: "apps/marketing/app/[locale]/solutions/page.tsx"
      provides: "Solutions overview page"
      contains: "solutions/enterprise-campuses"
    - path: "apps/marketing/components/solutions/solution-card.tsx"
      provides: "Industry solution card"
      uses: "GradientBorder"
    - path: "apps/marketing/components/solutions/solution-detail-layout.tsx"
      provides: "Shared layout for industry solution detail pages"
      min_lines: 35
  key_links:
    - from: "solutions/page.tsx"
      to: "i18n messages"
      via: "useTranslations('solutions') for headings and card content"
    - from: "solution-card.tsx"
      to: "GradientBorder"
      via: "wraps card in GradientBorder for premium feel"
---

<objective>
Create the Solutions section — overview page with 2 industry solution cards, and 2 detailed industry pages (Enterprise Campuses, Critical Infrastructure) with challenge/outcome content.

Purpose: Fulfills the "industry solutions" content requirement (MKT-02). Enterprise prospects in specific verticals need tailored messaging that speaks to their unique challenges.
Output: 1 solutions overview page, 2 industry detail pages, 3 shared solution components (SolutionCard, SolutionGrid, SolutionDetailLayout), SolutionHero.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-marketing-site-redesign/04-CONTEXT.md
@.planning/phases/04-marketing-site-redesign/04-RESEARCH.md
@.planning/phases/04-marketing-site-redesign/04-UI-SPEC.md

## Key References
- **D-12**: Separate route segment — Solutions at /solutions
- **D-13**: Full scope — Solutions overview + 2 industry pages (Enterprise Campuses, Critical Infrastructure)
- **UI-SPEC Page Architecture**: Solutions overview uses Hero + SolutionGrid + CTASection. Industry pages use SolutionDetailLayout + FeatureShowcase + CaseStudyTeaser + CTASection.
- **RESEARCH.md Pattern 1**: Server Component Page Template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared solution components + SolutionHero</name>
  <files>
    apps/marketing/components/solutions/solution-card.tsx
    apps/marketing/components/solutions/solution-grid.tsx
    apps/marketing/components/solutions/solution-detail-layout.tsx
    apps/marketing/components/solutions/solution-hero.tsx
  </files>
  <read_first>
    - apps/marketing/components/products/product-card.tsx (reference pattern — similar structure)
    - apps/marketing/components/shared/gradient-border.tsx (reference)
    - apps/marketing/components/shared/glass-panel.tsx (reference)
    - .planning/phases/04-marketing-site-redesign/04-UI-SPEC.md (Interaction Contract for glass/hover values)
  </read_first>
  <action>
    Create `components/solutions/` directory, then create 4 components:

    **A. Create `solution-card.tsx`:**
    - `'use client'` component
    - Import: `GradientBorder`, `GlassPanel`, `Link`, `motion`
    - Props: `{ title: string; description: string; href: string; industry: string; image?: string; index?: number }`
    - Wraps content in `<Link href={href}>`:
      - Outer: `<GradientBorder className="h-full">`
        - Inner: `<GlassPanel className="p-6 h-full bg-[#070912]">`
          - Industry badge: `<span className="text-xs font-semibold text-cyan-400 uppercase tracking-wider">{industry}</span>`
          - Title: `text-xl font-display text-white mt-2`
          - Description: `text-sm text-[#94a3b8] mt-2`
          - Explore link: "Explorer →" in text-cyan-400 at bottom
    - Named export: `SolutionCard`

    **B. Create `solution-grid.tsx`:**
    - Server component
    - Import: `SolutionCard`, `AnimatedSection`
    - Props: `{ solutions: Array<{ title, description, href, industry }> }`
    - Grid: `grid grid-cols-1 md:grid-cols-2 gap-8` (two large cards, since we only have 2 industry pages)
    - Wrap each in AnimatedSection

    **C. Create `solution-detail-layout.tsx`:**
    - Server component
    - Props: `{ children: React.ReactNode; title: string; description: string; challenges: string[]; outcomes: string[] }`
    - Structure:
      - Hero section: Same gradient bg pattern as product-detail-layout
      - Content section: `<Section variant="alt">` → `<Container>` 
        - 2-column grid: Left for children (detailed content), Right for sidebar
        - Sidebar: 
          - Challenges section: `<GlassPanel className="p-6 mb-6">`
            - `<h3 className="text-sm font-semibold text-cyan-400 uppercase tracking-wider mb-4">Défis</h3>`
            - List of challenges with red/x icon
          - Outcomes section: `<GlassPanel className="p-6">`
            - `<h3 className="text-sm font-semibold text-green-400 uppercase tracking-wider mb-4">Résultats</h3>`
            - List of outcomes with green/check icon
      - Renders `{children}` in the left column

    **D. Create `solution-hero.tsx`:**
    - `'use client'` component
    - Uses `useTranslations('solutions')` for heading/subheading/cta
    - Similar to ProductsHero but with industry-specific context
    - Dark gradient background, heading + subheading + CTA
  </action>
  <verify>
    <automated>
      for f in solution-card solution-grid solution-detail-layout solution-hero; do
        [ -f "apps/marketing/components/solutions/$f.tsx" ] && echo "✅ $f.tsx exists" || echo "❌ $f.tsx missing"
      done
      grep -c "GradientBorder" apps/marketing/components/solutions/solution-card.tsx
      grep -c "GlassPanel" apps/marketing/components/solutions/solution-detail-layout.tsx
    </automated>
  </verify>
  <done>SolutionCard uses GradientBorder + GlassPanel. SolutionGrid uses 2-column layout. SolutionDetailLayout has challenges and outcomes sidebars.</done>
</task>

<task type="auto">
  <name>Task 2: Create Solutions overview + 2 industry pages</name>
  <files>
    apps/marketing/app/[locale]/solutions/page.tsx
    apps/marketing/app/[locale]/solutions/enterprise-campuses/page.tsx
    apps/marketing/app/[locale]/solutions/critical-infrastructure/page.tsx
  </files>
  <read_first>
    - .planning/phases/04-marketing-site-redesign/04-RESEARCH.md (Pattern 1: Server Component Page Template)
    - apps/marketing/app/[locale]/produits/page.tsx (reference for overview page pattern)
    - .planning/phases/04-marketing-site-redesign/04-RESEARCH.md (Step pattern — for animations in solution detail)
  </read_first>
  <action>
    **A. Create `app/[locale]/solutions/page.tsx` (Solutions overview):**
    - Pattern 1: generateStaticParams + generateMetadata + async Server Component
    - Metadata: French title "Solutions - OVERSIGHT AI"
    - Component: `<Header /> + <main>`
    - `<SolutionsHero />` — heading from i18n `solutions.heading`, subheading from `solutions.subheading`
    - `<SolutionGrid solutions={[...]} />` — 2 solutions with data from i18n `solutions.enterprise` and `solutions.critical`
    - `<CTASection />`
    - `</main> + <Footer />`

    **B. Create `app/[locale]/solutions/enterprise-campuses/page.tsx`:**
    - Pattern 1 page
    - Metadata: French "Campus d'entreprise - Solutions - OVERSIGHT AI"
    - Component: `<Header /> + <main>`
    - `<SolutionDetailLayout 
        title={t('solutions.enterprise.name')}
        description={t('solutions.enterprise.description')}
        challenges={t.raw('solutions.enterprise.challenges')}
        outcomes={t.raw('solutions.enterprise.outcomes')}
      >`
      - Inside: Detailed content section with:
        - "Pourquoi Oversight Hub" section with 2-3 GlassPanel cards explaining the fit
        - CaseStudyTeaser linking to relevant case study
    - `</main> + <Footer />`

    **C. Create `app/[locale]/solutions/critical-infrastructure/page.tsx`:**
    - Same pattern as enterprise-campuses
    - Metadata: French "Infrastructure critique - Solutions - OVERSIGHT AI"
    - Uses i18n from `solutions.critical.*`
    - Content sections specific to critical infrastructure use case

    For both industry pages, use `t.raw()` to get the arrays from i18n messages (challenges, outcomes arrays).
  </action>
  <verify>
    <automated>
      for d in solutions solutions/enterprise-campuses solutions/critical-infrastructure; do
        [ -f "apps/marketing/app/\[locale\]/$d/page.tsx" ] && echo "✅ $d/page.tsx exists" || echo "❌ $d/page.tsx missing"
      done
      for d in solutions solutions/enterprise-campuses solutions/critical-infrastructure; do
        grep -q "generateStaticParams" "apps/marketing/app/\[locale\]/$d/page.tsx" && echo "✅ $d has generateStaticParams" || echo "❌ $d missing generateStaticParams"
      done
    </automated>
  </verify>
  <done>Solutions overview renders with SolutionGrid. Both industry pages have SolutionDetailLayout with challenges and outcomes from i18n. All pages have metadata and canonical URLs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Static content pages. No user input, no API calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Information Disclosure | Route exposure | accept | Public solution pages — intended to be discoverable. |
</threat_model>

<verification>
- [ ] `pnpm --filter @repo/marketing build` succeeds
- [ ] Solutions overview shows 2 industry cards
- [ ] Each industry page has challenges/outcomes sidebar
- [ ] All content from i18n messages
- [ ] GradientBorder used on SolutionCard
</verification>

<success_criteria>
All 3 solution pages created and rendering. Components use shared library patterns. i18n drives all text content. TypeScript build passes.
</success_criteria>

<output>
Create `.planning/phases/04-marketing-site-redesign/04-08-SUMMARY.md` when done
</output>
