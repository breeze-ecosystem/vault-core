---
phase: 07-public-presence
plan: 05
type: execute
wave: 3
depends_on: [07-04]
files_modified:
  - apps/marketing/app/[locale]/page.tsx
  - apps/marketing/components/ui/button.tsx
  - apps/marketing/components/ui/badge.tsx
  - apps/marketing/components/ui/skeleton.tsx
  - apps/marketing/components/ui/logo.tsx
  - apps/marketing/components/ui/animated-section.tsx
  - apps/marketing/components/landing/hero-section.tsx
  - apps/marketing/components/landing/ai-grid-background.tsx
  - apps/marketing/components/landing/trust-bar.tsx
  - apps/marketing/components/landing/feature-showcase.tsx
  - apps/marketing/components/landing/feature-card.tsx
  - apps/marketing/components/landing/ai-highlight-section.tsx
  - apps/marketing/components/landing/testimonial-carousel.tsx
  - apps/marketing/components/landing/testimonial-card.tsx
  - apps/marketing/components/landing/stats-section.tsx
  - apps/marketing/components/landing/cta-section.tsx
  - apps/marketing/components/landing/scroll-indicator.tsx
autonomous: true
requirements: [WEB-01, WEB-08]

must_haves:
  truths:
    - "Visitor sees a compelling hero section with animated grid background, headline, subtitle, and two CTAs"
    - "Feature showcase displays 6 feature cards in a responsive grid with icons"
    - "Trust bar shows customer logos in grayscale that colorize on hover"
    - "Testimonial carousel is user-controlled (dots/arrows) with motion slide transitions"
    - "Stats section shows 4 metrics with count-up animation"
    - "Final CTA section with heading, body, and two buttons"
    - "Footer has 4-column layout with product links, resources, company, legal"
    - "Header is sticky with transparent→solid background on scroll, includes nav, language switcher, CTA"
    - "Mobile navigation uses hamburger menu with slide-down panel"
    - "All animations respect prefers-reduced-motion"
  artifacts:
    - path: "apps/marketing/components/ui/button.tsx"
      provides: "Marketing-specific Button with primary/secondary/ghost variants"
      min_lines: 25
    - path: "apps/marketing/components/layout/header.tsx"
      provides: "Sticky header with transparent→solid scroll behavior"
      contains: "useEffect"
    - path: "apps/marketing/components/landing/hero-section.tsx"
      provides: "Full-viewport hero section with motion animations"
      contains: "motion"
    - path: "apps/marketing/app/[locale]/page.tsx"
      provides: "Landing page composing all sections"
      contains: "HeroSection"
  key_links:
    - from: "app/[locale]/page.tsx"
      to: "components/landing/hero-section.tsx"
      via: "component import"
      pattern: "HeroSection"

<objective>
Build the shared UI component library and landing page

Purpose: Create all marketing-specific reusable UI components (Button, Badge, Header, Footer, etc.) and compose them into the landing page with hero, features, testimonials, stats, trust bar, and CTA sections.
Output: ~25 component files + the landing page at app/[locale]/page.tsx
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-public-presence/07-CONTEXT.md
@.planning/phases/07-public-presence/07-RESEARCH.md
@.planning/phases/07-public-presence/07-PATTERNS.md
@.planning/phases/07-public-presence/07-UI-SPEC.md

<interfaces>
From apps/dashboard/components/ui/button.tsx — React.forwardRef pattern with cva (class-variance-authority) for variants or simpler className-based approach.

However, per D-10: marketing uses LOCAL components, NOT @repo/ui. Create Button with simple className prop interface using cn(). Use cva if installed (it's in monorepo), otherwise use inline variant maps.

Button variants from UI-SPEC.md lines 685-686:
- Variants: primary (cyan filled), secondary (outline), ghost (text only)
- Sizes: sm, md, lg, xl (hero CTA)
- States: loading spinner, hover scale 1.03, active scale 0.97

Container component: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 (per UI-SPEC spacing)
Section component: Optional background variant (white/alt/dark), section padding
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared UI primitives</name>
  <files>
    apps/marketing/components/ui/button.tsx,
    apps/marketing/components/ui/badge.tsx,
    apps/marketing/components/ui/skeleton.tsx,
    apps/marketing/components/ui/logo.tsx,
    apps/marketing/components/ui/animated-section.tsx,
    apps/marketing/components/ui/page-header.tsx
  </files>
  <read_first>
    apps/dashboard/components/ui/button.tsx — Button pattern reference (cva variants, forwardRef)
    apps/dashboard/lib/utils.ts — cn() helper import path
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 685-694 — UI component inventory
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 406-427 — Animation specs
  </read_first>
  <action>
    Create 6 files in `apps/marketing/components/ui/`:

    `button.tsx`: 'use client'. Use `cva` from `class-variance-authority` for variants: `primary` (bg-cyan text-white hover:brightness-110), `secondary` (border-2 border-muted text-foreground), `ghost` (text-muted hover:text-foreground). Sizes: `sm` (px-3 py-1.5 text-sm), `md` (px-4 py-2), `lg` (px-6 py-3), `xl` (px-8 py-4 text-lg). Loading state with spinner SVG. Use React.ButtonHTMLAttributes + cn() for className merging. Accept `isLoading` prop that shows spinner and disables button.

    `badge.tsx`: Small inline label. Variants: default (bg-muted text-muted-foreground), accent (bg-primary/10 text-primary), success (bg-green/10 text-green). Use cn() + forwardRef.

    `skeleton.tsx`: Shimmer animation div. Accept className for dimensions. Use animate-pulse from Tailwind.

    `logo.tsx`: SVG logo component. Props: variant ('light' | 'dark'). Light variant for dark backgrounds, dark variant for light. Render "OVERSIGHT" text with cyan accent dot. Simple inline SVG.

    `animated-section.tsx`: 'use client'. Intersection Observer wrapper. Triggers CSS fade-up animation when section enters viewport. Uses useInView or IntersectionObserver. Children wrapped in div with opacity-0 translate-y-6 transitioning to opacity-100 translate-y-0 on inView (600ms, cubic-bezier(0.16,1,0.3,1) per UI-SPEC).

    `page-header.tsx`: Reusable page heading section. Props: heading (string), subheading (optional string), className. Renders centered heading + subheading with responsive typography.
  </action>
  <verify>
    <automated>grep -c 'variant' apps/marketing/components/ui/button.tsx || echo "FAIL: button variants missing"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 UI primitive files created in `components/ui/`
    - `button.tsx` exports primary/secondary/ghost variants with sm/md/lg/xl sizes and loading state
    - `animated-section.tsx` uses Intersection Observer for scroll-triggered entrance
    - All components use `cn()` from `@/lib/utils` for className merging
  </acceptance_criteria>
  <done>6 shared UI primitive components created with marketing-specific styling.</done>
</task>

<task type="auto">
  <name>Task 2: Create landing page sections</name>
  <files>
    apps/marketing/components/landing/hero-section.tsx,
    apps/marketing/components/landing/ai-grid-background.tsx,
    apps/marketing/components/landing/trust-bar.tsx,
    apps/marketing/components/landing/feature-showcase.tsx,
    apps/marketing/components/landing/feature-card.tsx,
    apps/marketing/components/landing/ai-highlight-section.tsx,
    apps/marketing/components/landing/testimonial-carousel.tsx,
    apps/marketing/components/landing/testimonial-card.tsx,
    apps/marketing/components/landing/stats-section.tsx,
    apps/marketing/components/landing/cta-section.tsx,
    apps/marketing/components/landing/scroll-indicator.tsx
  </files>
  <read_first>
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 209-263 — Landing page hierarchy
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 473-511 — Copywriting: hero headlines, feature cards, etc.
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 632-646 — Landing component inventory
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 432-437 — motion animation patterns for hero
  </read_first>
  <action>
    Create 11 files in `components/landing/`:

    `hero-section.tsx`: 'use client'. Full-viewport (min-h-screen) dark navy bg. Contains AIGridBackground component as child. Layout: centered content with H1 (40px), subtitle (24px), 2 CTA buttons ("Book a Demo" primary, "Talk to Sales" secondary), trust stat text. Uses motion for staggered entrance: headline → subtitle (delay 150ms) → CTAs (delay 300ms) each fading up 600ms. Import motion from 'motion/react'.

    `ai-grid-background.tsx`: CSS-only animated grid pattern. Uses .bg-grid utility class. Subtle slow pan animation (20s linear loop). Semi-transparent gradient overlay. No JS needed.

    `trust-bar.tsx`: Section with heading "Trusted by industry leaders". Logo cloud showing 6 placeholder logos in grayscale. CSS hover: saturate to full color. On mobile: auto-scroll horizontal carousel (30s loop). Logos can be placeholder SVG circles with company initials for now.

    `feature-showcase.tsx`: Section (alt bg) with heading "Everything you need to secure your facility" + subheading. Renders 6 FeatureCard components in 3-column responsive grid.

    `feature-card.tsx`: Card with icon (cyan), title (semibold), description (regular). Hover: shadow elevation + translateY(-4px) 250ms ease-out. Icon container: rounded-xl bg-primary/10 p-3. Use lucide-react icons: Shield, Brain, Server, Bell, Smartphone, CreditCard for features 1-6 respectively.

    `ai-highlight-section.tsx`: Dark band section with AI-themed gradient background. Heading "Built on AI. Powered by you." Subheading about on-premise AI. Feature rows showcasing AI capabilities.

    `testimonial-carousel.tsx`: 'use client'. Uses motion AnimatePresence for slide transitions on nav click. User-controlled dots navigation (no autoplay per UI-SPEC). Maps over testimonial data array, renders TestimonialCard.

    `testimonial-card.tsx`: Card with quote, avatar placeholder, name, role, company. Centered layout with max-w-2xl. Quote marks decoration.

    `stats-section.tsx`: 4-column grid with stat values (99.9%, 24/7, 150+, 100%) and labels. CSS count-up entrance on scroll via AnimatedSection wrapper.

    `cta-section.tsx`: Dark band with gradient bg. Heading "Ready to transform your security operations?" Body text. Two CTAs: "Book Your Demo" (primary), "Contact Sales" (secondary/outline).

    `scroll-indicator.tsx`: Animated chevron-down icon at bottom of hero. Pulse animation loop. Scrolls to next section on click.
  </action>
  <verify>
    <automated>grep -c 'motion' apps/marketing/components/landing/hero-section.tsx || echo "FAIL: hero missing motion"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 landing section component files created
    - `hero-section.tsx` uses motion for staggered entrance animations
    - `feature-showcase.tsx` renders 6 FeatureCards in 3-column grid
    - `testimonial-carousel.tsx` uses user-controlled navigation with AnimatePresence
    - All components use copy from UI-SPEC Copywriting section
  </acceptance_criteria>
  <done>11 landing page section components created with animations and marketing copy.</done>
</task>

<task type="auto">
  <name>Task 3: Compose the landing page</name>
  <files>apps/marketing/app/[locale]/page.tsx</files>
  <read_first>
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 207-263 — Full landing page hierarchy
    .planning/phases/07-public-presence/07-RESEARCH.md lines 328-336 — generateStaticParams for SSG
  </read_first>
  <action>
    Create `app/[locale]/page.tsx`:
    - Import all landing section components
    - Import setRequestLocale from next-intl/server
    - Import routing from @/i18n/routing
    - Export generateStaticParams returning routing.locales.map
    - Export default async function HomePage that:
      1. Await params and call setRequestLocale(locale)
      2. Renders: Header → HeroSection → TrustBar → FeatureShowcase → AIHighlightSection → TestimonialCarousel → StatsSection → CTASection → Footer
    - Export metadata (generateMetadata or static metadata) with title "OVERSIGHT AI - AI-Powered Physical Security Intelligence" and description
  </action>
  <verify>
    <automated>grep -c 'HeroSection\|Header' apps/marketing/app/\[locale\]/page.tsx || echo "FAIL: page missing key components"