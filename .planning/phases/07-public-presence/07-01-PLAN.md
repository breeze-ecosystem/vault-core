---
phase: 07-public-presence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/design/src/marketing.ts
  - packages/design/src/index.ts
  - packages/design/src/spacing.ts
  - turbo.json
  - .env.example
autonomous: true
requirements: [WEB-07, WEB-04]
user_setup: []

must_haves:
  truths:
    - "Marketing site uses the same brand DNA (colors, typography, spacing) as the Dashboard via @repo/design"
    - "Marketing-specific design tokens (lighter surfaces, larger spacing) exist and are exported"
    - "Turborepo pipeline includes marketing#build, marketing#dev, marketing#lint, marketing#check-types"
    - ".env.example documents all new environment variables for marketing site and contact form"
    - "Design spacing scale includes 4xl (96px) for marketing whitespace"
  artifacts:
    - path: "packages/design/src/marketing.ts"
      provides: "Marketing-specific design token constants"
      contains: "export const marketingTheme"
    - path: "packages/design/src/index.ts"
      provides: "Barrel export including marketing tokens"
      contains: "export * from \"./marketing\""
    - path: "turbo.json"
      provides: "Marketing build pipeline tasks"
      contains: "marketing#build"
    - path: ".env.example"
      provides: "Environment variable documentation for new features"
      contains: "TURNSTILE_SITE_KEY"
  key_links:
    - from: "packages/design/src/index.ts"
      to: "packages/design/src/marketing.ts"
      via: "barrel export"
      pattern: "export.*marketing"
    - from: "turbo.json"
      to: "apps/marketing/package.json"
      via: "filter @repo/marketing"
      pattern: "marketing#build"

<objective>
Create the shared design tokens for marketing and configure the monorepo pipeline

Purpose: Establish the brand-consistent design token foundation (@repo/design marketing extension) and monorepo build pipeline for the marketing site before any code is written.
Output: packages/design/src/marketing.ts, updated packages/design/src/index.ts, updated packages/design/src/spacing.ts, updated turbo.json, updated .env.example
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-public-presence/07-RESEARCH.md
@.planning/phases/07-public-presence/07-PATTERNS.md
@.planning/phases/07-public-presence/07-CONTEXT.md
@.planning/phases/07-public-presence/07-UI-SPEC.md

<interfaces>
From packages/design/src/colors.ts:
```typescript
export const colors = {
  dark: { bg, surface, elevated, border, borderLight, text, textSecondary, textMuted, glassBg },
  light: { bg, surface, elevated, border, borderLight, text, textSecondary, textMuted, glassBg },
  shared: { primary: "#06b6d4", primaryDark: "#0891b2", success: "#10b981", warning: "#f59e0b", destructive: "#ef4444", info: "#06b6d4" },
} as const;
```

From packages/design/src/spacing.ts:
```typescript
export const spacing = { xs: 4, sm: 8, md: 12, base: 16, lg: 24, xl: 32, xxl: 48, xxxl: 64 } as const;
```

From packages/design/src/typography.ts:
```typescript
export const typography = { display: { fontSize: 28, fontWeight: 600 }, heading: { fontSize: 20, fontWeight: 600 }, body: { fontSize: 14 }, label: { fontSize: 12, fontWeight: 600 }, mono, monoSmall } as const;
```

From current turbo.json:
```json
{
  "tasks": {
    "build": { "dependsOn": ["^build"], "inputs": ["$TURBO_DEFAULT$", ".env*"], "outputs": [".next/**", "!.next/cache/**", "dist/**"] },
    "lint": { "dependsOn": ["^lint"] },
    "check-types": { "dependsOn": ["^check-types"] },
    "dev": { "cache": false, "persistent": true },
    "prisma:generate": { "cache": false },
    "prisma:migrate": { "cache": false }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketing design tokens + update spacing scale</name>
  <files>packages/design/src/marketing.ts, packages/design/src/index.ts, packages/design/src/spacing.ts</files>
  <read_first>
    packages/design/src/colors.ts — Existing color token pattern
    packages/design/src/spacing.ts — Existing spacing scale (add 4xl)
    packages/design/src/index.ts — Barrel export (add marketing export)
    packages/design/src/typography.ts — Existing typography scale
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 74-79 (spacing), 140-200 (colors)
    .planning/phases/07-public-presence/07-PATTERNS.md lines 747-818 (marketing.ts pattern)
  </read_first>
  <action>
    Create `packages/design/src/marketing.ts` exporting `marketingTheme` as a const object. Match color values from UI-SPEC.md Color section (hsl values as JS hex equivalents: primary #06b6d4, success #10b981, warning #f59e0b, destructive #ef4444). Include `colors` (hero, surface, text, accent groups), `spacing` (section: "py-24 md:py-32", container: "max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"), and `typography` (display: 40px, heading: 24px, body: 16px, small: 14px). Follow the exact object shape from PATTERNS.md lines 771-801.

    Add `xxxxl: 96` to `packages/design/src/spacing.ts` as the 4xl marketing whitespace token (per UI-SPEC.md spacing exceptions: 4xl = 96px).

    Add `export * from "./marketing"` to `packages/design/src/index.ts`.

    Verify: grep for "marketingTheme" in new file, grep for "xxxxl" in spacing.ts, grep for "marketing" in index.ts.
  </action>
  <verify>
    <automated>grep -c 'export const marketingTheme' packages/design/src/marketing.ts || echo "FAIL: marketing theme export missing"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/design/src/marketing.ts` contains `export const marketingTheme` with colors, spacing, typography sections
    - `packages/design/src/spacing.ts` contains `xxxxl: 96`
    - `packages/design/src/index.ts` contains `export * from "./marketing"`
  </acceptance_criteria>
  <done>Marketing design tokens created, spacing scale extended, barrel export updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add marketing build pipeline to turbo.json</name>
  <files>turbo.json</files>
  <read_first>
    turbo.json — Current task pipeline
    .planning/phases/07-public-presence/07-PATTERNS.md lines 517-543 (turbo.json pattern)
    .planning/phases/07-public-presence/07-RESEARCH.md lines 837-848 (turbo.json pipeline addition)
  </read_first>
  <action>
    Add four new tasks to the `tasks` object in turbo.json after the existing `prisma:migrate` entry (before the closing brace of tasks):
    ```json
    "marketing#build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", ".velite/**"]
    },
    "marketing#dev": {
      "cache": false,
      "persistent": true
    },
    "marketing#lint": {
      "dependsOn": ["^lint"]
    },
    "marketing#check-types": {
      "dependsOn": ["^check-types"]
    }
    ```
    Ensure valid JSON (no trailing comma). The `marketing#build` outputs include `.velite/**` for velite-generated content.
  </action>
  <verify>
    <automated>grep -c '"marketing#build"' turbo.json || echo "FAIL: marketing build task missing"</automated>
  </verify>
  <acceptance_criteria>
    - `turbo.json` contains `marketing#build` with dependsOn, inputs, and outputs fields
    - `turbo.json` contains `marketing#dev`, `marketing#lint`, `marketing#check-types`
    - `marketing#build` outputs includes `.velite/**`
    - File is valid JSON
  </acceptance_criteria>
  <done>Turborepo pipeline updated with 4 marketing site tasks.</done>
</task>

<task type="auto">
  <name>Task 3: Add marketing site and contact form env vars to .env.example</name>
  <files>.env.example</files>
  <read_first>
    .env.example — Current file structure and formatting conventions
    .planning/phases/07-public-presence/07-CONTEXT.md lines 137-138 (env var names)
    .planning/phases/07-public-presence/07-RESEARCH.md lines 504-506 (NEXT_PUBLIC_TURNSTILE_SITE_KEY example)
  </read_first>
  <action>
    Add a new section in .env.example after the LICENSE section titled "MARKETING SITE" with these variables using the same comment/formatting pattern:
    - `NEXT_PUBLIC_API_URL` — already exists, no change needed
    - `NEXT_PUBLIC_TURNSTILE_SITE_KEY` — Turnstile widget site key, optional, defaults to test key `1x00000000000000000000AA`
    - `TURNSTILE_SECRET_KEY` — Turnstile server-side verification key, optional, defaults to test key `1x0000000000000000000000000000000AA`
    - `CONTACT_NOTIFICATION_EMAIL` — Email address for contact form notifications, required
    - `NEXT_PUBLIC_APP_NAME` — Already exists, no change needed

    Use the same comment style (box-drawing header, [REQUIS]/[OPTIONNEL] markers). Follow existing formatting: em-dash separators, aligned comment columns.
  </action>
  <verify>
    <automated>grep -c 'TURNSTILE_SITE_KEY' .env.example || echo "FAIL: TURNSTILE_SITE_KEY missing"