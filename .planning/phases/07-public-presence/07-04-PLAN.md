---
phase: 07-public-presence
plan: 04
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - apps/marketing/package.json
  - apps/marketing/tsconfig.json
  - apps/marketing/next.config.ts
  - apps/marketing/proxy.ts
  - apps/marketing/src/i18n/routing.ts
  - apps/marketing/src/i18n/request.ts
  - apps/marketing/src/i18n/navigation.ts
  - apps/marketing/src/lib/utils.ts
  - apps/marketing/app/layout.tsx
  - apps/marketing/app/[locale]/layout.tsx
  - apps/marketing/app/[locale]/not-found.tsx
  - apps/marketing/app/globals.css
  - apps/marketing/app/robots.ts
  - apps/marketing/tailwind.config.ts
  - apps/marketing/postcss.config.js
autonomous: true
requirements: [WEB-04, WEB-08]

must_haves:
  truths:
    - "Marketing site is a valid Next.js 14 app with App Router"
    - "Site supports 6 locales (en, fr, es, de, ja, ar) with prefix-based routing (/en/pricing, /fr/pricing)"
    - "Arabic locale renders with dir='rtl' on the html element"
    - "Landing page at /{locale}/ renders static HTML (SSG) at build time"
    - "CSS custom properties define the marketing light-first theme"
    - "Tailwind is configured with market-specific tokens, animations, and typography plugins"
    - "IBM Plex Sans is loaded via Google Fonts as the primary font"
  artifacts:
    - path: "apps/marketing/package.json"
      provides: "Package manifest for @repo/marketing"
      contains: "next": "^14.2.15"
    - path: "apps/marketing/proxy.ts"
      provides: "next-intl middleware for locale routing"
      contains: "createMiddleware"
    - path: "apps/marketing/src/i18n/routing.ts"
      provides: "Locale routing configuration"
      contains: "defineRouting"
    - path: "apps/marketing/app/[locale]/layout.tsx"
      provides: "Locale-aware root layout with RTL support"
      contains: "dir={direction}"
    - path: "apps/marketing/app/globals.css"
      provides: "Marketing theme CSS variables and Tailwind directives"
      contains: "--marketing-primary"
  key_links:
    - from: "proxy.ts"
      to: "src/i18n/routing.ts"
      via: "import createMiddleware(routing)"
      pattern: "routing"
    - from: "app/[locale]/layout.tsx"
      to: "src/i18n/routing.ts"
      via: "hasLocale(routing.locales)"
      pattern: "routing"

<objective>
Scaffold the Next.js marketing application with internationalization routing and theme

Purpose: Create the foundational Next.js 14 app structure for the marketing site including package manifest, next-intl i18n setup (6 locales with prefix routing and RTL), Tailwind configuration with marketing theme variables, and global CSS.
Output: Complete apps/marketing/ scaffold with i18n routing, layouts, globals.css, and Tailwind config
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-public-presence/07-RESEARCH.md
@.planning/phases/07-public-presence/07-PATTERNS.md
@.planning/phases/07-public-presence/07-CONTEXT.md
@.planning/phases/07-public-presence/07-UI-SPEC.md

<interfaces>
From apps/dashboard/package.json:
```json
{
  "name": "@repo/dashboard",
  "version": "0.0.0", "private": true,
  "scripts": { "dev": "next dev --port 3100", "build": "next build", "start": "next start --port 3100", "lint": "eslint .", "check-types": "tsc --noEmit" },
  "dependencies": { "next": "^14.2.15", "react": "^18.3.1", "react-dom": "^18.3.1", "clsx": "^2.1.1", "tailwind-merge": "^3.5.0", "tailwindcss": "3", "tailwindcss-animate": "^1.0.7", "lucide-react": "^1.11.0", "motion": "^12.42.2", "@repo/design": "workspace:*", "autoprefixer": "^10.5.0", "postcss": "^8.5.12" },
  "devDependencies": { "@types/node": "^20.14.0", "@types/react": "^18.3.3", "@types/react-dom": "^18.3.0", "typescript": "5.9.2" }
}
```

From apps/dashboard/tailwind.config.ts — structure for marketing adaptation.

From apps/dashboard/lib/utils.ts — cn() helper:
```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";
export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
```

next-intl routing pattern from RESEARCH.md lines 251-336 (defineRouting, getRequestConfig, createMiddleware, LocaleLayout with RTL).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json, tsconfig.json, postcss.config.js</name>
  <files>apps/marketing/package.json, apps/marketing/tsconfig.json, apps/marketing/postcss.config.js</files>
  <read_first>
    apps/dashboard/package.json — Package structure pattern (scripts, dependency pattern)
    apps/dashboard/tsconfig.json — TS config reference (next.config.js reference)
    .planning/phases/07-public-presence/07-RESEARCH.md lines 121-124 (install commands)
    .planning/phases/07-public-presence/07-UI-SPEC.md line 861-868 (new packages: next-intl, velite, @cloudflare/turnstile, @repo/design, motion, @tailwindcss/typography)
  </read_first>
  <action>
    Create `apps/marketing/package.json`:
    - name: @repo/marketing, private: true
    - scripts: dev (next dev --port 3200), build (next build), start (next start --port 3200), lint (next lint), check-types (tsc --noEmit)
    - dependencies: next@^14.2.15, react@^18.3.1, react-dom@^18.3.1, next-intl@^4.13.2, velite@^0.4.0, satori@^0.28.0, sharp@^0.35.3, clsx@^2.1.1, tailwind-merge@^3.5.0, tailwindcss@3, tailwindcss-animate@^1.0.7, @tailwindcss/typography@^0.5, lucide-react@^1.11.0, motion@^12.42.2, @repo/design@workspace:*, autoprefixer@^10.5.0, postcss@^8.5.12
    - devDependencies: @types/node@^20.14.0, @types/react@^18.3.3, @types/react-dom@^18.3.0, typescript@5.9.2, turnstile-types@^1.2.3

    Create `apps/marketing/tsconfig.json` with a Next.js App Router config (target es2017, jsx preserve, moduleResolution bundler, paths @/* mapping to src/*). Base it on apps/dashboard's implicit tsconfig structure.

    Create `apps/marketing/postcss.config.js`:
    ```js
    module.exports = {
      plugins: {
        tailwindcss: {},
        autoprefixer: {},
      },
    };
    ```
  </action>
  <verify>
    <automated>test -f apps/marketing/package.json && test -f apps/marketing/tsconfig.json && test -f apps/marketing/postcss.config.js || echo "FAIL: scaffold files missing"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/marketing/package.json` contains name @repo/marketing, next, next-intl, velite, satori, sharp, @repo/design as dependencies
    - `apps/marketing/tsconfig.json` exists with proper Next.js config
    - `apps/marketing/postcss.config.js` loads tailwindcss and autoprefixer plugins
  </acceptance_criteria>
  <done>Marketing app package manifest and build configuration created.</done>
</task>

<task type="auto">
  <name>Task 2: Create next-intl i18n routing infrastructure</name>
  <files>apps/marketing/src/i18n/routing.ts, apps/marketing/src/i18n/request.ts, apps/marketing/src/i18n/navigation.ts, apps/marketing/proxy.ts</files>
  <read_first>
    .planning/phases/07-public-presence/07-RESEARCH.md lines 251-336 — Full next-intl v4 setup with defineRouting, getRequestConfig, createMiddleware
    .planning/phases/07-public-presence/07-CONTEXT.md lines 40-46 (D-14 through D-18: locale decisions)
  </read_first>
  <action>
    Create four files following RESEARCH.md Patterns 1 exactly:

    1. `src/i18n/routing.ts`: defineRouting({ locales: ['en', 'fr', 'es', 'de', 'ja', 'ar'], defaultLocale: 'en', localePrefix: 'always' })

    2. `src/i18n/request.ts`: getRequestConfig with hasLocale check, dynamic import of messages from ../../messages/{locale}.json. Falls back to defaultLocale.

    3. `src/i18n/navigation.ts`: createNavigation({ routing }) from next-intl/navigation, export { Link, redirect, usePathname, useRouter } as your shared navigation.

    4. `proxy.ts` (root of apps/marketing/): createMiddleware(routing) exported as default. Matcher: '/((?!api|trpc|_next|_vercel|.*\\..*).*)'.
  </action>
  <verify>
    <automated>grep -c 'defineRouting' apps/marketing/src/i18n/routing.ts || echo "FAIL: routing.ts missing"</automated>
  </verify>
  <acceptance_criteria>
    - `src/i18n/routing.ts` exports routing with 6 locales, defaultLocale 'en', localePrefix 'always'
    - `src/i18n/request.ts` exports getRequestConfig that dynamically imports locale messages
    - `src/i18n/navigation.ts` exports Link, redirect, usePathname, useRouter
    - `proxy.ts` exports createMiddleware(routing) middleware function
  </acceptance_criteria>
  <done>next-intl i18n routing infrastructure created with 6 locales and prefix routing.</done>
</task>

<task type="auto">
  <name>Task 3: Create globals.css, tailwind.config.ts, and cn() utility</name>
  <files>apps/marketing/src/lib/utils.ts, apps/marketing/tailwind.config.ts, apps/marketing/app/globals.css</files>
  <read_first>
    apps/dashboard/lib/utils.ts — cn() helper (exact copy)
    apps/dashboard/tailwind.config.ts — Tailwind structure reference
    apps/dashboard/app/globals.css — CSS variable pattern reference
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 30-68 — CSS variable architecture (marketing-specific vars)
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 106-124 — Typography: display 40px, heading 24px, body 16px, small 14px
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 406-427 — CSS animation keyframes (fade up, slide up)
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 121-125 — Tailwind config fontSize extension for display
  </read_first>
  <action>
    Create `src/lib/utils.ts` — exact copy of apps/dashboard/lib/utils.ts (cn() helper with clsx + tailwind-merge).

    Create `tailwind.config.ts`:
    - darkMode: false (marketing is light-first, no dark mode per UI-SPEC line 70)
    - content: ['./app/**/*.{ts,tsx}', './components/**/*.{ts,tsx}', './src/**/*.{ts,tsx}']
    - theme.extend.fontFamily: { sans: ['"IBM Plex Sans"', 'system-ui', '-apple-system', 'sans-serif'], mono: ['"JetBrains Mono"', 'monospace'] }
    - theme.extend.fontSize: { display: ['40px', { lineHeight: '1.1', fontWeight: '600' }] }
    - theme.extend.colors: Map CSS vars (--marketing-*) to Tailwind color names: background, foreground, muted, border, primary, primary-foreground, etc.
    - theme.extend.animation: fade-in, slide-up, fade-up (keyframe with translateY(24px→0))
    - theme.extend.keyframes: fadeIn, slideUp, fadeUp with UI-SPEC durations/easings
    - plugins: [require('tailwindcss-animate'), require('@tailwindcss/typography')]

    Create `app/globals.css`:
    - @tailwind base/components/utilities
    - @import Google Fonts for IBM Plex Sans (400, 600) + JetBrains Mono (400, 500, 600)
    - @layer base :root with CSS custom properties from UI-SPEC.md lines 44-66 (--marketing-bg through --marketing-container)
    - @layer base html { scroll-behavior: smooth }
    - @layer base body { font-family with Inter fallback, antialiasing }
    - @layer utilities: .bg-grid (animated grid background for hero), scrollbar-hide utility
    - @media (prefers-reduced-motion: reduce) disabling all animations (per UI-SPEC line 441)
  </action>
  <verify>
    <automated>grep -c '--marketing-primary' apps/marketing/app/globals.css || echo "FAIL: marketing CSS vars missing"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/utils.ts` exports `cn()` function with clsx + tailwind-merge
    - `tailwind.config.ts` has no darkMode, has display fontSize extension, has fade-in/slide-up/fade-up animations, uses @tailwindcss/typography plugin
    - `globals.css` has all --marketing-* CSS custom properties from UI-SPEC, Tailwind directives, Google Fonts import, and reduced motion media query
  </acceptance_criteria>
  <done>Tailwind configuration and global CSS created with marketing theme variables and animations.</done>
</task>

<task type="auto">
  <name>Task 4: Create layouts (root + locale) and skeleton pages</name>
  <files>apps/marketing/app/layout.tsx, apps/marketing/app/[locale]/layout.tsx, apps/marketing/app/[locale]/not-found.tsx, apps/marketing/app/robots.ts</files>
  <read_first>
    apps/dashboard/app/layout.tsx — Root layout pattern
    .planning/phases/07-public-presence/07-PATTERNS.md lines 202-277 — Layout patterns (root + locale layouts)
    .planning/phases/07-public-presence/07-RESEARCH.md lines 293-335 — next-intl locale layout with RTL
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 788-807 — RTL layout contract
    .planning/phases/07-public-presence/07-UI-SPEC.md lines 815-824 — JSON-LD structured data types
  </read_first>
  <action>
    Create `app/layout.tsx` — root shell:
    - Import globals.css
    - Export viewport: { themeColor: '#06b6d4', width: 'device-width', initialScale: 1 }
    - Export metadata: { title: { template, default }, description, icons }
    - Default export function RootLayout({ children }) — just wraps in {children} (the locale layout handles html/body)
    - Add OrganizationJsonLd as a Script component with JSON-LD schema (Organization type, name 'Oversight AI', url 'https://oversighthub.com')

    Create `app/[locale]/layout.tsx` — locale root layout:
    - Import NextIntlClientProvider, hasLocale from next-intl; getMessages, setRequestLocale from next-intl/server; notFound from next/navigation; routing from @/i18n/routing
    - rtlLocales constant: new Set(['ar'])
    - generateStaticParams returns routing.locales.map(l => ({ locale: l }))
    - export default async function LocaleLayout with params Promise<{ locale }> — await params, hasLocale check (else notFound), setRequestLocale, getMessages, compute direction
    - Render <html lang={locale} dir={direction}> with <body> containing <NextIntlClientProvider messages={messages}>{children}</NextIntlClientProvider>

    Create `app/[locale]/not-found.tsx`:
    - Simple page with heading "Page not found" and link back to home

    Create `app/robots.ts`:
    - Export default function robots() returning { rules: { userAgent: '*', allow: '/' }, sitemap: 'https://oversighthub.com/sitemap.xml' }
  </action>
  <verify>
    <automated>grep -c 'NextIntlClientProvider' apps/marketing/app/\[locale\]/layout.tsx || echo "FAIL: locale layout missing NextIntlClientProvider"</automated>
  </verify>
  <acceptance_criteria>
    - `app/layout.tsx` exports metadata, viewport, and renders children
    - `app/[locale]/layout.tsx` has generateStaticParams with 6 locales, RTL support for Arabic, NextIntlClientProvider wrapper
    - `not-found.tsx` renders a 404 page
    - `robots.ts` exports rules allowing all crawlers and links to sitemap
  </acceptance_criteria>
  <done>Root and locale layouts created with RTL support, JSON-LD, and basic 404 page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Marketing site | Publicly accessible marketing site — no auth, all content is public |
| next-intl middleware → routing | Locale detection from Accept-Language header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering | globals.css | mitigate | CSS custom properties only — no user-controlled values injected |
| T-07-11 | Information Disclosure | metadata/JSON-LD | accept | Public site — Organization schema intentionally exposes public company info |
| T-07-12 | Spoofing | proxy.ts locale routing | accept | Locale detection from browser headers is intentional UX improvement; users can always override |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | new packages: next-intl, velite, satori, sharp, @tailwindcss/typography, turnstile-types. Per RESEARCH.md all [ASSUMED] — see T-07-SC install checkpoint in execution plan. |
</threat_model>

<verification>
- [ ] Task 1: package.json, tsconfig.json, postcss.config.js created
- [ ] Task 2: 4 i18n files created with correct next-intl v4 API
- [ ] Task 3: globals.css has all --marketing-* vars, tailwind.config.ts correct
- [ ] Task 4: layouts render correctly, RTL works for Arabic, 404 page exists
</verification>

<success_criteria>
Marketing Next.js app scaffolded with full i18n support (6 locales, prefix routing, RTL for Arabic), Tailwind configured with marketing theme, CSS custom properties defined, and layouts rendering correctly.
</success_criteria>

<output>
Create `.planning/phases/07-public-presence/07-04-SUMMARY.md` when done
</output>
