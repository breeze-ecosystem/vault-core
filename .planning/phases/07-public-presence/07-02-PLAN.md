---
phase: 07-public-presence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/website.Dockerfile
  - docker-compose.yml
  - Caddyfile
autonomous: true
requirements: [WEB-08]

must_haves:
  truths:
    - "Marketing site can be built as a Docker image using a multi-stage build"
    - "Marketing site is deployable via docker-compose as the 'marketing' service"
    - "Caddy routes oversighthub.com traffic to the marketing service"
    - "Production deployment serves marketing site on port 3200"
  artifacts:
    - path: "docker/website.Dockerfile"
      provides: "Multi-stage Docker build for marketing site"
      min_lines: 40
    - path: "docker-compose.yml"
      provides: "Marketing service definition in compose stack"
      contains: "marketing:"
    - path: "Caddyfile"
      provides: "Host-based routing for oversighthub.com"
      contains: "oversighthub.com"
  key_links:
    - from: "Caddyfile"
      to: "docker-compose.yml"
      via: "service hostname 'marketing:3200'"
      pattern: "marketing:3200"
    - from: "docker-compose.yml"
      to: "docker/website.Dockerfile"
      via: "build context"
      pattern: "website.Dockerfile"

<objective>
Create the Docker build infrastructure for the marketing site

Purpose: Enable production deployment of the marketing Next.js app as a containerized service alongside the existing Dashboard, API, and supporting services.
Output: docker/website.Dockerfile, updated docker-compose.yml, updated Caddyfile
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-public-presence/07-RESEARCH.md
@.planning/phases/07-public-presence/07-PATTERNS.md
@.planning/phases/07-public-presence/07-CONTEXT.md

<interfaces>
From docker/dashboard.Dockerfile (analog for Dockerfile pattern):
- Multi-stage: deps → builder → runner
- Base image: node:20-alpine
- Corepack/pnpm: `corepack enable && corepack prepare pnpm@9.0.0 --activate`
- Standalone output: COPY .next/standalone, .next/static, public/
- Port: 3100 (marketing uses 3200)
- User: nextjs (uid 1001), group: nodejs (gid 1001)

From current Caddyfile:
```
:80 {
  handle /api/* { reverse_proxy api:4000 }
  handle /ws/* { reverse_proxy api:4000 }
  handle { reverse_proxy dashboard:3100 }
  log { output stdout }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketing site Dockerfile</name>
  <files>docker/website.Dockerfile</files>
  <read_first>
    docker/dashboard.Dockerfile — Reference pattern (exact analog)
    .planning/phases/07-public-presence/07-PATTERNS.md lines 376-431 (website.Dockerfile exact pattern)
    .planning/phases/07-public-presence/07-RESEARCH.md lines 745-801 (Dockerfile with explanations)
  </read_first>
  <action>
    Create `docker/website.Dockerfile` following the exact multi-stage pattern from PATTERNS.md lines 380-428:
    - Stage 1 (deps): node:20-alpine, corepack enable, pnpm@9.0.0, WORKDIR /app, COPY pnpm-lock.yaml + workspace config + package.json files for packages/shared, packages/design, packages/typescript-config, and apps/marketing. RUN pnpm install --frozen-lockfile.
    - Stage 2 (builder): node:20-alpine, corepack, WORKDIR /app, NODE_ENV=production, NEXT_TELEMETRY_DISABLED=1. COPY all source, COPY node_modules from deps. Build shared packages with `npx tsc -p packages/shared/tsconfig.json && npx tsc -p packages/design/tsconfig.json`. Build marketing with `pnpm --filter @repo/marketing build`.
    - Stage 3 (runner): node:20-alpine, addgroup/nodejs gid 1001, adduser/nextjs uid 1001. COPY .next/standalone, .next/static, public/. EXPOSE 3200, ENV PORT=3200 HOSTNAME="0.0.0.0", CMD ["node", "apps/marketing/server.js"].
    
    Note: Do NOT copy packages/ui or packages/eslint-config (marketing doesn't need them). The standalone output path is `apps/marketing/` not root.
  </action>
  <verify>
    <automated>grep -c 'EXPOSE 3200' docker/website.Dockerfile || echo "FAIL: port 3200 not exposed"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docker/website.Dockerfile`
    - Contains 3 stages: deps, builder, runner
    - Uses `node:20-alpine` base image
    - Exposes port `3200`
    - Copies `.next/standalone`, `.next/static`, and `public/` directories
    - CMD runs `apps/marketing/server.js`
  </acceptance_criteria>
  <done>Multi-stage Dockerfile for marketing site created with deps/builder/runner stages on port 3200.</done>
</task>

<task type="auto">
  <name>Task 2: Add marketing service to docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <read_first>
    docker-compose.yml — Current service definitions (lines 1-64 for dashboard pattern)
    .planning/phases/07-public-presence/07-PATTERNS.md lines 467-513 (docker-compose.yml marketing service pattern)
  </read_first>
  <action>
    Add a new `marketing:` service entry in docker-compose.yml after the `dashboard:` service block (after line 63). Follow the exact pattern from PATTERNS.md lines 473-513:
    - build: context: ., dockerfile: docker/website.Dockerfile
    - container_name: oversight-marketing
    - restart: unless-stopped
    - environment: PORT=3200, NODE_ENV=production, NEXT_PUBLIC_API_URL (reusing existing env var), NEXT_PUBLIC_TURNSTILE_SITE_KEY (with test key default), NEXT_PUBLIC_APP_NAME (reusing existing)
    - healthcheck: wget -qO- http://127.0.0.1:3200/ || exit 1
    - No networks or extra_hosts needed (uses default network which gives access to api service)
  </action>
  <verify>
    <automated>grep -c 'oversight-marketing' docker-compose.yml || echo "FAIL: marketing service missing"</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` contains `marketing:` service definition
    - Build references `docker/website.Dockerfile`
    - Container name is `oversight-marketing`
    - Healthcheck tests port 3200
    - Environment includes `NEXT_PUBLIC_TURNSTILE_SITE_KEY`
  </acceptance_criteria>
  <done>Marketing service added to docker-compose.yml with build config, env vars, and healthcheck.</done>
</task>

<task type="auto">
  <name>Task 3: Update Caddyfile for host-based routing</name>
  <files>Caddyfile</files>
  <read_first>
    Caddyfile — Current :80 catch-all routing (lines 1-20)
    .planning/phases/07-public-presence/07-PATTERNS.md lines 434-463 (Caddyfile host-based routing pattern)
    .planning/phases/07-public-presence/07-RESEARCH.md lines 803-834 (Caddy configuration with marketing host)
  </read_first>
  <action>
    Rewrite Caddyfile to use host-based site blocks instead of the current `:80` catch-all:
    - `oversighthub.com` block: reverse_proxy marketing:3200 for all traffic + log
    - `app.oversighthub.com` block: /api/* → api:4000, /ws/* → api:4000, default → dashboard:3100 + log

    This allows the same Caddy instance to serve both domains. Keep the log directive in both blocks. Do NOT keep the `:80` fallback since production uses explicit hostnames (per Pitfall 4 from RESEARCH.md).
  </action>
  <verify>
    <automated>grep -c 'oversighthub.com' Caddyfile || echo "FAIL: marketing host block missing"</automated>
  </verify>
  <acceptance_criteria>
    - `Caddyfile` has `oversighthub.com` block reverse_proxying to `marketing:3200`
    - `Caddyfile` has `app.oversighthub.com` block with /api/*, /ws/* routes
    - Both blocks have `log { output stdout }`
    - No `:80` catch-all remains
  </acceptance_criteria>
  <done>Caddyfile updated with host-based routing for oversighthub.com and app.oversighthub.com.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caddy → Marketing | Reverse proxy boundary — Caddy terminates TLS and forwards to marketing container |
| docker-compose → network | Container networking — marketing service must reach API for contact form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Spoofing | Caddyfile | mitigate | Host-based routing prevents DNS rebinding — explicit hostname matching |
| T-07-04 | Information Disclosure | docker-compose.yml | mitigate | No secrets in docker-compose; env vars reference external .env file |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan — infrastructure only |
</threat_model>

<verification>
- [ ] Task 1: `docker/website.Dockerfile` exists with multi-stage build
- [ ] Task 2: `docker-compose.yml` has `marketing:` service
- [ ] Task 3: `Caddyfile` routes `oversighthub.com` to marketing:3200
</verification>

<success_criteria>
Marketing site Docker image can be built, deployed via docker-compose as the `marketing` service on port 3200, and served via Caddy at `oversighthub.com`.
</success_criteria>

<output>
Create `.planning/phases/07-public-presence/07-02-SUMMARY.md` when done
</output>
