---
phase: 03-bastion-ai-access-control
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03-PLAN
files_modified:
  - apps/dashboard/lib/api.ts
  - apps/dashboard/app/(dashboard)/sites/page.tsx
  - apps/dashboard/app/(dashboard)/sites/nouveau/page.tsx
  - apps/dashboard/app/(dashboard)/sites/[id]/page.tsx
  - apps/dashboard/app/(dashboard)/sites/comparaison/page.tsx
  - apps/dashboard/app/(dashboard)/parametres/rbac/page.tsx
  - apps/dashboard/app/(dashboard)/parametres/sso/page.tsx
  - apps/dashboard/app/(dashboard)/parametres/synchronisation/page.tsx
  - apps/dashboard/components/multi-site-dashboard.tsx
  - apps/dashboard/components/site-card.tsx
  - apps/dashboard/components/site-switcher.tsx
  - apps/dashboard/components/cross-site-comparison.tsx
  - apps/dashboard/components/global-search-command.tsx
  - apps/dashboard/components/rbac-role-editor.tsx
  - apps/dashboard/components/sso-config-form.tsx
autonomous: true
requirements:
  - BAS-13
  - BAS-14
  - BAS-15
  - BAS-16
  - BAS-17
  - BAS-18
  - BAS-19
must_haves:
  truths:
    - "Parent org admin sees aggregate multi-site dashboard with KPIs (cameras, alerts, storage, uptime)"
    - "Admin can create and manage child sites from the dashboard"
    - "Admin can drill down from aggregate view to per-site dashboard"
    - "Cross-site comparison view shows side-by-side metrics"
    - "Admin manages custom roles with hierarchical RBAC editor"
    - "Admin configures SAML/OAuth2 SSO providers"
    - "Admin views inter-site sync status"
    - "Global search (Cmd+K) searches across all sites for events, people, credentials"
    - "All actions are audited and displayed in the immutable audit trail"
  artifacts:
    - path: "apps/dashboard/app/(dashboard)/sites/"
      provides: "Multi-site management pages"
      contains: "sites/page.tsx, nouveau/page.tsx, [id]/page.tsx, comparaison/page.tsx"
    - path: "apps/dashboard/app/(dashboard)/parametres/"
      provides: "BASTION settings pages"
      contains: "rbac/page.tsx, sso/page.tsx, synchronisation/page.tsx"
    - path: "apps/dashboard/components/global-search-command.tsx"
      provides: "Cross-site global search"
      contains: "Cmd+K command palette, search results with site tags"
  key_links:
    - from: "sites/[id]/page.tsx"
      to: "components/multi-site-dashboard.tsx"
      via: "Imports MultiSiteDashboard component for per-site view"
    - from: "parametres/rbac/page.tsx"
      to: "components/rbac-role-editor.tsx"
      via: "Imports RBACRoleEditor for role hierarchy management"
    - from: "lib/api.ts"
      to: "NestJS /api/sites/* and /api/parametres/* endpoints"
      via: "fetchWithAuth calls to BASTION API"
---

<objective>
Build the multi-site management dashboard, hierarchical RBAC editor, SSO configuration page, sync status page, and global search command palette.

Purpose: BASTION multi-site management requires a centralized dashboard for parent-org admins to oversee up to 5 child sites, manage custom roles, configure SSO, and search across all sites. These UI pages consume the backend APIs built in Plan 03.
Output: Multi-site dashboard pages, settings pages, and shared components.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bastion-ai-access-control/03-CONTEXT.md
@.planning/phases/03-bastion-ai-access-control/03-UI-SPEC.md
@.planning/phases/03-bastion-ai-access-control/03-PATTERNS.md
@.planning/phases/03-bastion-ai-access-control/03-03-SUMMARY.md

<interfaces>
From apps/dashboard/lib/api.ts (existing patterns):
- fetchWithAuth(url, options) — central HTTP client with 401 auto-refresh
- PaginatedResponse<T> — { data, total, page, limit }
- Organization, DashboardStats, CredentialDto interfaces
- getFaces(), addFace(), deleteFace() — VISION face API pattern
- fetchCredentials({ page, limit }) — returns PaginatedResponse<CredentialDto>

From apps/dashboard/app/(dashboard)/sites/page.tsx (existing — 294 lines):
- Site list with GlassCard, PageHeader, DataTable patterns
- 'use client', useState, useEffect, error/loading states

From UI-SPEC § Multi-site Dashboard Layout:
- Site Selector dropdown, StatsCard KPI grid (cameras, alerts, storage, uptime)
- Site List (DataTable), Cross-site Comparison (Sparkline/BarChart)

Existing shadcn components (DO NOT re-install):
- avatar, badge, button, card, dialog, dropdown-menu, input, label, progress, scroll-area, separator, sheet, skeleton, table, toast, tooltip

Components to install (shadcn):
- tabs, select, switch, slider, command, popover

Existing custom components (reuse):
- PageHeader, PageTransition, GlassCard, StatsCard, DataTable, ConfirmationDialog, Badge, Skeleton, Sparkline
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and build multi-site dashboard pages</name>
  <files>
    apps/dashboard/ (via shadcn CLI)
    apps/dashboard/components/multi-site-dashboard.tsx (NEW)
    apps/dashboard/components/site-card.tsx (NEW)
    apps/dashboard/components/site-switcher.tsx (NEW)
    apps/dashboard/components/cross-site-comparison.tsx (NEW)
    apps/dashboard/lib/api.ts (MODIFY)
    apps/dashboard/app/(dashboard)/sites/page.tsx (MODIFY)
    apps/dashboard/app/(dashboard)/sites/nouveau/page.tsx (MODIFY)
    apps/dashboard/app/(dashboard)/sites/[id]/page.tsx (NEW)
    apps/dashboard/app/(dashboard)/sites/comparaison/page.tsx (NEW)
  </files>
  <read_first>
    apps/dashboard/app/(dashboard)/sites/page.tsx
    apps/dashboard/lib/api.ts
    apps/dashboard/components/stats-card.tsx
    apps/dashboard/components/glass-card.tsx
    apps/dashboard/components/page-header.tsx
  </read_first>
  <action>
    **Install shadcn components** (per UI-SPEC § Additional Components to Install):
    ```bash
    cd apps/dashboard && npx shadcn@latest add tabs select switch slider command popover
    ```
    These components are from the official shadcn registry at `https://ui.shadcn.com/r/styles/default/{name}.json`. No third-party dependencies.

    **Extend `lib/api.ts` with BASTION multi-site API calls:**
    Follow existing api.ts pattern (fetchWithAuth, typed responses, French error messages):
    - `async getSites(params: { page?, limit? }): Promise<PaginatedResponse<Site>>` — GET /api/sites
    - `async getSite(id: string): Promise<Site>` — GET /api/sites/:id
    - `async createSite(data: { name, address?, city?, country? }): Promise<Site>` — POST /api/sites
    - `async updateSite(id: string, data: Partial<Site>): Promise<Site>` — PATCH /api/sites/:id
    - `async deleteSite(id: string): Promise<void>` — DELETE /api/sites/:id
    - `async getAggregateStats(): Promise<AggregateStats>` — GET /api/sites/aggregate
    - `async getSiteStats(id: string): Promise<SiteStats>` — GET /api/sites/:id/stats
    - `async getComparisonData(): Promise<ComparisonData>` — GET /api/sites/comparison
    - `async globalSearch(query: string, type?: string): Promise<SearchResults>` — GET /api/sites/search?q=query&type=type

    **Also add BASTION face, credential, group, schedule, and event API functions (consumed by Plan 05):**
    - `async getBastionFaces(params: { page?, limit?, search?, blacklisted? }): Promise<PaginatedResponse<BastionFaceEntry>>` — GET /api/bastion/faces
    - `async enrollBastionFace(data: { name, photoBase64, isBlacklisted?, riskThreshold? }): Promise<BastionFaceEntry>` — POST /api/bastion/faces
    - `async getBastionFace(id: string): Promise<BastionFaceDetail>` — GET /api/bastion/faces/:id
    - `async updateBastionFace(id: string, data): Promise<BastionFaceEntry>` — PATCH /api/bastion/faces/:id
    - `async deleteBastionFace(id: string): Promise<void>` — DELETE /api/bastion/faces/:id
    - `async toggleBlacklist(id: string): Promise<BastionFaceEntry>` — POST /api/bastion/faces/:id/toggle-blacklist
    - `async getFacePassages(id: string, params): Promise<PaginatedResponse<FacePassage>>` — GET /api/bastion/faces/:id/passages
    - `async getRecentPassages(params): Promise<PaginatedResponse<FacePassage>>` — GET /api/bastion/passages
    - `async createCredentialV2(data: CredentialCreateInput): Promise<CredentialDto>` — POST /api/access/credentials
    - `async getAccessGroups(params): Promise<PaginatedResponse<AccessGroup>>` — GET /api/access/groups
    - `async createAccessGroup(data): Promise<AccessGroup>` — POST /api/access/groups
    - `async updateAccessGroup(id, data): Promise<AccessGroup>` — PATCH /api/access/groups/:id
    - `async deleteAccessGroup(id): Promise<void>` — DELETE /api/access/groups/:id
    - `async addMemberToGroup(groupId, memberId): Promise<void>` — POST /api/access/groups/:id/members/:memberId
    - `async removeMemberFromGroup(groupId, memberId): Promise<void>` — DELETE /api/access/groups/:id/members/:memberId
    - `async createAccessSchedule(data): Promise<ScheduleDto>` — POST /api/access/schedules
    - `async getAccessSchedules(params): Promise<PaginatedResponse<ScheduleDto>>` — GET /api/access/schedules
    - `async getAccessEvents(params): Promise<PaginatedResponse<AccessEvent>>` — GET /api/access/events
    - `async getAccessEvent(id: string): Promise<AccessEventDetail>` — GET /api/access/events/:id

    - Type `Site`, `AggregateStats`, `SiteStats`, `ComparisonData`, `SearchResults`, `BastionFaceEntry`, `BastionFaceDetail`, `FacePassage`, `AccessGroup`, `AccessEvent`, `CredentialCreateInput` interfaces in same file

    **Create `components/multi-site-dashboard.tsx`:**
    - Props: `sites: Site[]`, `aggregateStats: AggregateStats`, `comparisonData?: ComparisonData`
    - Layout per UI-SPEC § Multi-site Dashboard Layout:
      - Site Selector dropdown (using Select component from shadcn) with period toggle [Today] [7d] [30d]
      - KPI grid: 4 StatsCard components (glass-premium): total cameras, active alerts, storage usage, uptime
      - Site List using DataTable: columns = site name, cameras, alerts, storage, status (online/offline badge), with row click → navigate to `/sites/[id]`
      - Cross-site Comparison section at bottom using Sparkline for alert volume, storage per site
    - Loading state: skeleton placeholders for each section
    - Error state: error message with retry button per existing pattern
    - Empty state (no sites): empty state copy per UI-SPEC § "Aucun site configuré"
    - Uses `motion/react` (framer-motion) from PageTransition pattern for animated entry

    **Create `components/site-card.tsx`:**
    - Reusable per-site KPI summary card with drill-down
    - GlassCard with: site name, camera count, alert count, online/offline badge, tiny sparkline
    - Click → navigates to `/sites/[id]`
    - Accent variant (glass-accent) for highlighted/selected site

    **Create `components/site-switcher.tsx`:**
    - Dropdown component using shadcn Select
    - Props: `sites: { id, name }[]`, `currentSiteId: string`, `onChange: (id: string) => void`
    - Shows current site name, icon, and count badge
    - "Tous les sites" option for aggregate view

    **Create `components/cross-site-comparison.tsx`:**
    - Side-by-side comparison view per UI-SPEC layout
    - Bar charts or comparison table showing: cameras, alerts, storage, uptime per site
    - Uses existing Sparkline component for trend data
    - Empty state: "Ajoutez au moins 2 sites pour activer la comparaison inter-sites"

    **Modify `app/(dashboard)/sites/page.tsx` (existing site list):**
    - If user has GLOBAL_ADMIN or SUPER_ADMIN role (from auth context):
      - Show aggregate stats at top via MultiSiteDashboard
      - Show child site list with create button
    - If user has regular role: show existing single-site view (no change)
    - Add "Ajouter un site" CTA (per UI-SPEC § Primary CTA Labels)

    **Create `app/(dashboard)/sites/nouveau/page.tsx`:**
    - Form: name, address, city, country fields
    - Validation using Zod schema (from shared package)
    - Submit → createSite() API → redirect to sites list
    - Error state per UI-SPEC § Error State Copy: "Échec de création du site"
    - Confirmation: toast "Site créé avec succès"

    **Create `app/(dashboard)/sites/[id]/page.tsx`:**
    - Per-site KPI dashboard per UI-SPEC
    - Fetches site detail + stats from API
    - Shows: site info header, KPI grid (cameras, alerts, storage, uptime per D-28), recent alerts list, member list
    - Loading state, error state, empty state per existing page patterns
    - Edit/delete actions in header (with ConfirmationDialog for delete)

    **Create `app/(dashboard)/sites/comparaison/page.tsx`:**
    - Full cross-site comparison page
    - Fetches comparison data from API
    - Uses CrossSiteComparison component
    - Period selector (Today, 7d, 30d) for time-range filtering
  </action>
  <acceptance_criteria>
    - shadcn tabs, select, switch, slider, command, popover components installed and available
    - MultiSiteDashboard component renders KPI grid with StatsCard
    - Site detail page (sites/[id]) renders per-site metrics
    - Site creation form (sites/nouveau) submits and redirects
    - Cross-site comparison page (sites/comparaison) renders side-by-side data
    - API client functions added to lib/api.ts with correct endpoint paths
    - French copy used throughout matching UI-SPEC exactly
    - Loading, error, empty states implemented for all pages
  </acceptance_criteria>
  <verify>
    <automated>ls apps/dashboard/components/ui/tabs.tsx apps/dashboard/components/ui/select.tsx apps/dashboard/components/ui/switch.tsx apps/dashboard/components/ui/slider.tsx apps/dashboard/components/ui/command.tsx apps/dashboard/components/ui/popover.tsx 2>/dev/null && echo "shadcn components installed"</automated>
  </verify>
  <done>
    shadcn components installed. Multi-site dashboard pages created: site list with aggregate KPI, child site creation form, per-site drill-down, cross-site comparison. French copy throughout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build RBAC editor, SSO config form, sync status, and global search</name>
  <files>
    apps/dashboard/components/rbac-role-editor.tsx (NEW)
    apps/dashboard/components/sso-config-form.tsx (NEW)
    apps/dashboard/components/global-search-command.tsx (NEW)
    apps/dashboard/lib/api.ts (MODIFY)
    apps/dashboard/app/(dashboard)/parametres/rbac/page.tsx (NEW)
    apps/dashboard/app/(dashboard)/parametres/sso/page.tsx (NEW)
    apps/dashboard/app/(dashboard)/parametres/synchronisation/page.tsx (NEW)
    apps/dashboard/app/(dashboard)/layout.tsx (MODIFY — add global search trigger)
  </files>
  <read_first>
    apps/dashboard/app/(dashboard)/layout.tsx
    apps/dashboard/lib/api.ts
    apps/dashboard/components/ui/command.tsx
    apps/dashboard/app/(dashboard)/parametres/layout.tsx (or check if exists)
  </read_first>
  <action>
    **Extend `lib/api.ts` with RBAC, SSO, sync API calls:**
    Follow existing patterns:
    - `async getRoles(): Promise<RoleDefinition[]>` — GET /api/parametres/rbac/roles
    - `async updateRole(roleName: string, permissions: string[]): Promise<void>` — PATCH /api/parametres/rbac/roles/:name
    - `async createCustomRole(data: { name, permissions, parentRole }): Promise<RoleDefinition>` — POST /api/parametres/rbac/roles
    - `async getSsoProviders(): Promise<SsoProvider[]>` — GET /api/parametres/sso
    - `async createSsoProvider(data: SsoProviderConfig): Promise<SsoProvider>` — POST /api/parametres/sso. Per UI-SPEC § SSO Configuration Form: type (SAML/OIDC), name, metadata URL, entity ID, certificate, entry point, issuer URL, client ID, client secret, attribute mappings, auto-provisioning toggle, SSO-enforced toggle
    - `async updateSsoProvider(id: string, data: Partial<SsoProviderConfig>): Promise<SsoProvider>` — PATCH /api/parametres/sso/:id
    - `async deleteSsoProvider(id: string): Promise<void>` — DELETE /api/parametres/sso/:id
    - `async testSsoConnection(id: string): Promise<{ success: boolean, message: string }>` — POST /api/parametres/sso/:id/test
    - `async getSyncStatus(): Promise<SyncStatus>` — GET /api/parametres/synchronisation
    - `async triggerSync(): Promise<void>` — POST /api/parametres/synchronisation/trigger
    - Define all TypeScript interfaces in the same file

    **Create `components/rbac-role-editor.tsx`:**
    - Hierarchical role editor per UI-SPEC
    - Visual hierarchy: GLOBAL_ADMIN → SUPER_ADMIN → SITE_ADMIN → ADMIN → SUPERVISOR → OPERATOR → VIEWER → AUDITOR
    - Each role shows: name, level badge (100, 90, 75...), member count, editable permissions matrix
    - Permissions matrix with toggle switches per module: advanced_facial_recognition, anti_spoofing, weapon_detection, abandoned_object_detection, crowd_counting, zone_intrusion, multi_site, rfid_integration, biometric_integration, sso, audit_view, site_management (per D-30)
    - `[+] Créer un rôle personnalisé` button for custom roles below standard hierarchy
    - Uses Card, Badge, Switch, Button components
    - Loading, error, empty states (UI-SPEC § Empty State Copy: "Aucun rôle personnalisé")

    **Create `components/sso-config-form.tsx`:**
    - SSO provider configuration form per UI-SPEC § SSO Configuration Form layout:
      - Type selector: SAML / OIDC tabs
      - Fields: Name, Issuer URL, ACS URL (SAML) / Redirect URL (OIDC), Certificate (SAML, textarea with monospace font per typography spec), Client ID / Client Secret (OIDC), Attribute mappings JSON editor
      - Toggle switches: "Activer le provisionnement automatique", "Forcer la connexion SSO (masquer login/pwd)"
      - Test Connection button → calls testSsoConnection API, shows success/error state
      - Save / Cancel buttons
    - Uses Input, Select, Button, Switch, Tabs components
    - Validation: required fields per protocol type
    - Error state per UI-SPEC § Error State Copy: "Échec de connexion au fournisseur SSO"

    **Create `components/global-search-command.tsx`:**
    - Cmd+K / Ctrl+K command palette using shadcn Command component
    - Props: `sites: Site[]` (for site filtering context)
    - Internal state: search query, loading, results
    - Debounced search (300ms) calling `globalSearch()` API
    - Search results grouped by type: Événements, Personnes, Justificatifs
    - Each result shows: title, site name badge (cyan per accent color rules), type icon, click → navigate
    - Empty state per UI-SPEC: "Aucun résultat trouvé pour "{query}""
    - Loading state: skeleton list items
    - Keyboard: Cmd+K opens, Escape closes, arrow keys navigate, Enter selects
    - Register keyboard shortcut in layout

    **Create `app/(dashboard)/parametres/rbac/page.tsx`:**
    - Page with PageHeader: "Rôles & permissions" with description "Gérez les rôles et permissions des utilisateurs"
    - Uses RBACRoleEditor component
    - Fetches role data from API on mount
    - Save changes with confirmation dialog

    **Create `app/(dashboard)/parametres/sso/page.tsx`:**
    - Page with PageHeader: "Authentification unique (SSO)" with description "Configurez un fournisseur SAML ou OAuth2"
    - List of configured SSO providers using DataTable or GlassCard list
    - "Ajouter un fournisseur" button → opens SSOConfigForm in dialog
    - Each provider card shows: name, type (SAML/OIDC), Active toggle, Edit/Delete/Test actions
    - Empty state per UI-SPEC: "Aucun fournisseur SSO configuré"

    **Create `app/(dashboard)/parametres/synchronisation/page.tsx`:**
    - Page with PageHeader: "Synchronisation inter-sites" with description "État de la synchronisation entre sites"
    - Sync status display: last sync timestamp, status (synced/pending/error), sites list with individual sync status
    - "Déclencher la synchronisation" button
    - Empty state per UI-SPEC: "Aucune synchronisation"

    **Modify `app/(dashboard)/layout.tsx`:**
    - Add GlobalSearchCommand component
    - Register Cmd+K keyboard shortcut at layout level (available from any page)
  </action>
  <acceptance_criteria>
    - RBACRoleEditor renders role hierarchy with permission matrix
    - SSOConfigForm renders with SAML/OIDC tabs, test connection button
    - GlobalSearchCommand opens with Cmd+K, searches across all sites
    - API client functions added to lib/api.ts for RBAC, SSO, sync endpoints
    - parametres/rbac, parametres/sso, parametres/synchronisation pages render with correct copy
    - French copy matches UI-SPEC § Empty State Copy and § Error State Copy exactly
    - All pages have loading, error, empty states
  </acceptance_criteria>
  <verify>
    <automated>ls apps/dashboard/app/\(dashboard\)/parametres/rbac/page.tsx apps/dashboard/app/\(dashboard\)/parametres/sso/page.tsx apps/dashboard/app/\(dashboard\)/parametres/synchronisation/page.tsx 2>/dev/null && echo "Settings pages created"</automated>
  </verify>
  <done>
    RBAC editor, SSO config, sync status, and global search components created. Settings pages render with full French copy. Loading/error/empty states implemented.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→Dashboard | Public web app, JWT-authenticated API calls |
| Dashboard→API | All BASTION management operations via fetchWithAuth |
| Command palette→API | Global search queries via authenticated API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Information Disclosure | Global search exposes cross-site data | mitigate | Search results filtered by role (GLOBAL_ADMIN sees all, SITE_ADMIN sees own). API-level tenant isolation enforced by TenantIsolationGuard. |
| T-03-22 | Elevation of Privilege | RBAC UI modification without authorization | mitigate | RBAC mutation APIs gated by GLOBAL_ADMIN role. UI hides edit controls for non-admin users. Role selectors disabled based on user's role level. |
| T-03-23 | Tampering | SSO certificate/XSS via certificate upload | mitigate | Certificate input uses textarea with monospace font. Stored encrypted in IdpConfig model. Zod validation on API rejects malformed certificates. |
| T-03-24 | Information Disclosure | SSO client secret exposure in UI | mitigate | Client secret masked as "••••••••" in form after save. API returns masked value. Full secret only visible during initial creation. |

</threat_model>

<verification>
- All new page routes render without 404
- Global search opens with Cmd+K, returns results grouped by type with site badges
- RBAC editor shows correct hierarchy with permission toggles
- SSO form accepts SAML and OIDC configurations
- All French copy matches UI-SPEC exactly
</verification>

<success_criteria>
1. Multi-site dashboard shows KPI grid (cameras, alerts, storage, uptime) per D-28
2. Site list with create/edit/delete flows work
3. Per-site drill-down shows individual site metrics
4. Cross-site comparison renders side-by-side metrics per BAS-15
5. RBAC editor shows hierarchy: GLOBAL_ADMIN(100) > SUPER_ADMIN(90) > SITE_ADMIN(75) > ADMIN(60)...
6. SSO form creates/edits/tests SAML and OIDC providers
7. Global search (Cmd+K) searches events, people, credentials with site tags per D-29
8. Sync status page shows last sync and per-site status
9. All loading, error, empty states working
</success_criteria>

<output>
Create `.planning/phases/03-bastion-ai-access-control/03-04-SUMMARY.md` when done
</output>
