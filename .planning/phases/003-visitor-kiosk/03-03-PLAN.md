---
phase: 003-visitor-kiosk
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/kiosk/app/page.tsx
  - apps/kiosk/components/welcome-screen.tsx
  - apps/kiosk/components/qr-scanner.tsx
  - apps/kiosk/components/search-screen.tsx
  - apps/kiosk/components/confirm-checkin.tsx
  - apps/kiosk/lib/kiosk-api.ts
  - apps/kiosk/lib/i18n.ts
autonomous: true
requirements: [KIO-01, KIO-03]
must_haves:
  truths:
    - "Welcome screen shows brand header, two CTAs (Scan QR, Search), and language toggle"
    - "QR scanner activates camera via WebRTC and decodes visit/locator QR codes"
    - "Name search debounces input and fetches matching scheduled visits from API"
    - "Confirmation screen displays visitor photo, name, host, company, date, and reason"
    - "FR/EN language toggle switches all visible text"
    - "API client sends X-API-Key header on every request"
    - "Idle timeout resets to welcome after 60s (welcome) or 30s (mid-flow)"
  artifacts:
    - path: "apps/kiosk/lib/kiosk-api.ts"
      provides: "Typed API client with API key auth"
      exports: ["searchVisits", "getVisit", "checkIn", "checkOut", "printBadge", "KioskApiError"]
    - path: "apps/kiosk/lib/i18n.ts"
      provides: "FR/EN flat dictionary with t() function"
      exports: ["t", "Locale"]
    - path: "apps/kiosk/components/welcome-screen.tsx"
      provides: "Welcome screen with brand + CTAs + language toggle"
    - path: "apps/kiosk/components/qr-scanner.tsx"
      provides: "instascan WebRTC QR scanner viewfinder"
    - path: "apps/kiosk/components/search-screen.tsx"
      provides: "Debounced name search with results list"
    - path: "apps/kiosk/components/confirm-checkin.tsx"
      provides: "Visitor details card + confirm/cancel actions"
    - path: "apps/kiosk/app/page.tsx"
      provides: "Root state machine (useReducer) with welcome/scanning/search/confirm phases"
  key_links:
    - from: "app/page.tsx"
      to: "components/*.tsx"
      via: "kioskReducer state machine dispatch"
      pattern: "case \"welcome\"|case \"scanning\"|case \"search\"|case \"confirm\""
    - from: "components/qr-scanner.tsx"
      to: "instascan.Scanner"
      via: "new Instascan.Scanner({ video })"
      pattern: "Instascan.Scanner"
    - from: "lib/kiosk-api.ts"
      to: "NestJS /api/visitors/ endpoints"
      via: "fetch with X-API-Key header"
      pattern: "X-API-Key"
    - from: "lib/i18n.ts"
      to: "All components"
      via: "t(key, locale, vars?) function"
      pattern: "t\("
---

<objective>
Build the core kiosk frontend: state machine (useReducer) in page.tsx, welcome screen, QR scanner (instascan + WebRTC), name search with debounced API calls, confirmation screen, kiosk API client, and FR/EN i18n dictionary.

Purpose: Implements the check-in flow UI (KIO-01) with QR scanning (KIO-03) and name search fallback. This is the primary user-facing part of the kiosk — all interaction screens except printing/success/checkout/error.

Output: Runnable kiosk SPA that transitions through welcome → scanning/select → confirm states with API key auth.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/003-visitor-kiosk/003-RESEARCH.md
@.planning/phases/003-visitor-kiosk/003-PATTERNS.md
@.planning/phases/003-visitor-kiosk/003-UI-SPEC.md
@.planning/phases/003-visitor-kiosk/003-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs -->

From apps/dashboard/lib/api.ts:
  - fetch wrapper pattern (kiosk uses API key instead of JWT)
  - Typed response handlers

From @repo/shared types:
  - VisitDto: { id, visitorId, hostUserId, hostName?, purpose?, validFrom, validUntil, status, visitor?: VisitorDto, checkedInAt?, checkedOutAt? }
  - VisitorDto: { id, firstName, lastName, email?, phone?, company?, photoUrl? }

From RESEARCH.md Pattern 2 (QR scanner):
  - Instascan.Scanner({ video: HTMLVideoElement, mirror: true, captureImage: false, backgroundScan: true, refractoryPeriod: 5000, scanPeriod: 1 })
  - scanner.addListener("scan", (content: string) => void)
  - Instascan.Camera.getCameras() → Camera[]
  - scanner.start(camera), scanner.stop()

From RESEARCH.md i18n pattern:
  - export type Locale = "fr" | "en"
  - const dict: Record<string, Record<Locale, string>>
  - export function t(key: string, locale: Locale, vars?: Record<string, string>): string

From UI-SPEC.md:
  - Color tokens: bg-white, bg-gray-50, bg-blue-50/600/700, text-gray-900/500, text-emerald-600, text-red-600
  - Spacing: touch (48px), touch-lg (56px) via tailwind config
  - Typography: text-3xl (28px display), text-lg (18px body), text-base (16px label), text-sm (14px caption)
  - Animations: transition-all duration-200, animate-pulse for scanning
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create i18n dictionary and kiosk API client</name>
  <files>
    apps/kiosk/lib/i18n.ts
    apps/kiosk/lib/kiosk-api.ts
  </files>
  <read_first>
    .planning/phases/003-visitor-kiosk/003-RESEARCH.md — i18n dictionary pattern (RESEARCH.md lines 826-863), API client pattern (lines 753-824)
    .planning/phases/003-visitor-kiosk/003-PATTERNS.md — kiosk-api.ts (lines 217-283), i18n.ts (lines 286-340) pattern assignments
    .planning/phases/003-visitor-kiosk/003-UI-SPEC.md — Copy contract table (all 50+ FR/EN pairs, lines 476-525), API client contract (lines 619-678)
    .planning/phases/003-visitor-kiosk/003-CONTEXT.md — D-21 (API key auth), D-27 (FR primary + EN toggle), D-06 (QR scan), D-07 (search fallback)
  </read_first>
  <action>
    Create two files:

    1. apps/kiosk/lib/i18n.ts:
       - export type Locale = "fr" | "en"
       - const dict: Record<string, Record<Locale, string>> with ALL 50+ keys from UI-SPEC copywriting table (lines 476-525):
         - welcome.* (heading, subtitle, scan, search)
         - language.* (fr, en)
         - common.* (cancel)
         - scanner.* (instruction, manualLink, noCamera, permissionDenied, unrecognized)
         - search.* (heading, placeholder, noResults, noResultsHint, error)
         - confirm.* (heading, hostLabel, companyLabel, dateLabel, purposeLabel, today, loading, button, cancel)
         - printing.* (status, connecting, generating, done, error, errorDetail, retry, cancelCheckin)
         - success.* (heading, subtitle, hostNotified, checkoutHint, home, countdown)
         - checkout.* (processing, success, alreadyDone)
         - error.* (heading, server, printerOffline, home)
         - footer.* (text)
       - Export function t(key: string, locale: Locale, vars?: Record<string, string>): string
         - Look up dict[key]?.[locale]
         - If vars provided, replace {name}, {seconds} etc. using Object.entries reduce pattern
         - Return key if not found (fail-safe)
       - DO NOT use React Context — it's a pure function

    2. apps/kiosk/lib/kiosk-api.ts:
       - Get API_URL and API_KEY from NEXT_PUBLIC_* env vars at module level:
         const API_URL = process.env.NEXT_PUBLIC_API_URL || "";
         const API_KEY = process.env.NEXT_PUBLIC_API_KEY || "";
         const KIOSK_ID = process.env.NEXT_PUBLIC_KIOSK_ID || "kiosk-01";
       - Export class KioskApiError extends Error with code field ("NETWORK" | "NOT_FOUND" | "PRINTER_ERROR" | "ALREADY_CHECKED_IN" | "ALREADY_CHECKED_OUT" | "VISIT_EXPIRED" | "UNAUTHORIZED" | "UNKNOWN")
       - Private async function kioskFetch(path: string, options?: RequestInit): Promise<any>
         - fetch with X-API-Key header, X-Kiosk-Id header, Content-Type: application/json
         - On !res.ok: parse status codes into typed KioskApiError
           - 401 → UNAUTHORIZED
           - 404 → NOT_FOUND
           - 409 → parse body for "already checked in" (ALREADY_CHECKED_IN), "already checked out" (ALREADY_CHECKED_OUT), "expired" (VISIT_EXPIRED)
           - else → UNKNOWN with HTTP status message
         - Return res.json()
       - Export async functions:
         - searchVisits(query: string): Promise&lt;VisitDto[]&gt; → GET /visitors/visits?search=...&status=scheduled
         - getVisit(visitId: string): Promise&lt;VisitDto&gt; → GET /visitors/visits/:id
         - checkIn(visitId: string): Promise&lt;void&gt; → POST /visitors/visits/:id/check-in
         - checkOut(visitId: string): Promise&lt;void&gt; → POST /visitors/visits/:id/check-out
         - printBadge(visitId: string): Promise&lt;void&gt; → POST /kiosk/print/:visitId

       Note: API calls go to /api prefix. The API_URL must include the /api suffix. Since the kiosk calls existing visitor endpoints (which have @Roles guards), the kiosk must go through the KioskController's @Public() + KioskAuthGuard-checked endpoints.
       
       ACTUALLY — the kiosk-api.ts calls the KioskController endpoints (NOT the @Roles-guarded visitor endpoints). So:
       - searchVisits: GET /kiosk/search?name=... (KioskController)
       - getVisit: GET /kiosk/visits/:id (KioskController)
       - checkIn: POST /kiosk/check-in/:visitId (KioskController)
       - checkOut: POST /kiosk/check-out/:visitId (KioskController)
       - printBadge: POST /kiosk/print/:visitId (KioskController)

       The API_URL should NOT include /api suffix — the kiosk controller routes are at /api/kiosk/*. So:
       const API_URL = process.env.NEXT_PUBLIC_API_URL || "/api";  (default relative to same origin)
       And kioskFetch calls: `${API_URL}/kiosk/search?...`
  </action>
  <verify>
    <automated>ls apps/kiosk/lib/i18n.ts apps/kiosk/lib/kiosk-api.ts && pnpm --filter @repo/kiosk build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - i18n.ts exports t() function and Locale type, has 50+ FR/EN key pairs from UI-SPEC
    - kiosk-api.ts exports searchVisits, getVisit, checkIn, checkOut, printBadge, KioskApiError
    - kioskFetch sends X-API-Key and X-Kiosk-Id headers
    - KioskApiError has typed error codes matching all kiosk error scenarios
    - Static build succeeds
  </acceptance_criteria>
  <done>pnpm --filter @repo/kiosk build succeeds. Library files compiled into static export.</done>
</task>

<task type="auto">
  <name>Task 2: Create welcome screen and QR scanner components</name>
  <files>
    apps/kiosk/components/welcome-screen.tsx
    apps/kiosk/components/qr-scanner.tsx
  </files>
  <read_first>
    .planning/phases/003-visitor-kiosk/003-UI-SPEC.md — Welcome screen layout (lines 177-213), QR scanner layout (lines 215-249), color palette + spacing scale
    .planning/phases/003-visitor-kiosk/003-RESEARCH.md — Pattern 2 (instascan QR scanner, lines 336-411)
    .planning/phases/003-visitor-kiosk/003-PATTERNS.md — QR scanner no-analog (build from RESEARCH.md)
  </read_first>
  <action>
    Create two components using lucide-react icons and Tailwind CSS classes. NO React Router, NO App Router navigation. All components receive locale as prop (no React Context).

    1. apps/kiosk/components/welcome-screen.tsx:
       Props: { onScanClick: () => void; onSearchClick: () => void; locale: Locale; onToggleLocale: () => void }
       - Full-screen centered layout (h-screen, flex flex-col items-center justify-center, bg-white)
       - Top-right language toggle (Locale = "fr" | "en"): two buttons "FR" / "EN", 44×44px touch target, active language has text-blue-600 font-bold underline, inactive has text-gray-500
       - Brand: "OVERSIGHT AI" text-4xl font-bold text-gray-900 centered
       - Welcome heading: t("welcome.heading", locale) in text-3xl font-bold
       - Subtitle: t("welcome.subtitle", locale) in text-lg text-gray-500
       - Primary CTA: large pill button (h-14 touch-lg, w-full max-w-sm, bg-blue-600 text-white, rounded-full, flex items-center justify-center gap-2) with Camera icon + t("welcome.scan", locale)
         onClick → onScanClick
       - Secondary CTA: outlined pill (h-14 touch-lg, w-full max-w-sm, bg-white border-2 border-gray-200 text-gray-900, rounded-full) with Search icon + t("welcome.search", locale)
         onClick → onSearchClick
       - Footer: "Visit.me by OVERSIGHT AI" text-sm text-gray-400 mt-auto pb-8
       - CSS: animate-fade-in on mount (transition-all duration-200)
       - Import { Camera, Search } from "lucide-react"
       - Import { t, Locale } from "@/lib/i18n"

    2. apps/kiosk/components/qr-scanner.tsx:
       Props: { onQRDecoded: (visitId: string) => void; onCancel: () => void; onManualEntry: () => void; locale: Locale }
       - Full-screen camera container with relative positioning
       - Top-left "Annuler"/"Cancel" button (h-12) onClick → onCancel, with ArrowLeft icon
       - Camera viewfinder: video element (ref), w-full max-w-lg h-72, rounded-xl, bg-black, centered
       - Dashed border overlay: absolute inset-0 border-2 border-dashed border-white/50 rounded-xl m-0 (or CSS pseudo-element)
       - Scanning indicator (animate-pulse border-blue-500) while camera active
       - Instruction text: t("scanner.instruction", locale) below viewfinder
       - Manual entry link: t("scanner.manualLink", locale) text-sm text-blue-600 underline cursor-pointer
       - useEffect for instascan initialization:
         - Create Instascan.Scanner with config: { video: videoRef.current, mirror: true, captureImage: false, backgroundScan: true, refractoryPeriod: 5000, scanPeriod: 1 }
         - scanner.addListener("scan", (content) => onQRDecoded(content))
         - Instascan.Camera.getCameras() → use back camera if available (find by name includes "back"), else cameras[0]
         - scanner.start(camera)
         - Cleanup: scanner.stop() on unmount
         - Error handling: if no cameras → set state "no-camera", if permission denied → set state "permission-denied"
         - Render error UI for "no-camera": CameraOff icon + t("scanner.noCamera", locale) + retry button
         - Render error UI for "permission-denied": t("scanner.permissionDenied", locale) + permission retry button
       - CSS: bg-gray-50 screen, animate-fade-in on mount
       - Import Instascan from "instascan" (default import)
       - Import { ArrowLeft, CameraOff } from "lucide-react"
  </action>
  <verify>
    <automated>ls apps/kiosk/components/welcome-screen.tsx apps/kiosk/components/qr-scanner.tsx && pnpm --filter @repo/kiosk build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Welcome screen renders: brand header, scanner/search CTAs with icons, language toggle, footer
    - QR scanner initializes instascan with WebRTC getCameras, shows viewfinder with dashed border
    - QR scanner handles no-camera and permission-denied error states with locale-aware messages
    - All text uses t() function with locale prop (no React Context)
    - Touch targets are 48px+ for language toggle, 56px for primary CTAs
    - Static build succeeds
  </acceptance_criteria>
  <done>Components render correctly. Static build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Create search screen and confirm check-in component</name>
  <files>
    apps/kiosk/components/search-screen.tsx
    apps/kiosk/components/confirm-checkin.tsx
  </files>
  <read_first>
    .planning/phases/003-visitor-kiosk/003-UI-SPEC.md — Search screen layout (lines 251-291), Confirmation screen layout (lines 293-338)
    .planning/phases/003-visitor-kiosk/003-RESEARCH.md — API client pattern for searchVisits call
    .planning/phases/003-visitor-kiosk/003-PATTERNS.md — confirm-checkin role-match analog from dashboard visitors page
    .planning/phases/003-visitor-kiosk/003-CONTEXT.md — D-07 (search resends QR), D-08 (confirm before print), D-26 (photo from pre-registration)
  </read_first>
  <action>
    Create two components:

    1. apps/kiosk/components/search-screen.tsx:
       Props: { onSelectVisit: (visitId: string) => void; onCancel: () => void; locale: Locale }
       State machine (useState): "empty" | "loading" | "results" | "no-results" | "error"
       - Header with ArrowLeft + t("search.heading", locale)
       - Search input: 56px tall (h-14), full-width, Search icon, auto-focused, debounced onChange (300ms)
         - On empty input: show "empty" state with t("search.placeholder", locale) hint
         - On debounced input: call searchVisits(text), transition through loading → results/no-results
       - Results list: scrollable list of visit cards
         - Each card: flex row with left border (border-l-4 border-gray-200, hover:border-blue-500)
         - Content: visitor name (text-lg font-bold), company (text-base text-gray-500), host label (text-sm text-gray-400, "Hôte: {name}")
         - onClick → onSelectVisit(visit.id)
         - active:bg-blue-50 touch feedback
       - No results: UserX icon + t("search.noResults", locale) + t("search.noResultsHint", locale)
       - Loading: 3 skeleton placeholder rows (animate-pulse bg-gray-100 rounded-lg h-16)
       - Error: inline error card with t("search.error", locale) + retry button
       - Cancel button: t("common.cancel", locale) secondary → onCancel
       - Import { Search, UserX, ArrowLeft } from "lucide-react"
       - Import { searchVisits } from "@/lib/kiosk-api"

    2. apps/kiosk/components/confirm-checkin.tsx:
       Props: { visit: VisitDto; onConfirm: () => void; onCancel: () => void; locale: Locale }
       State: "ready" | "confirming" | "error" with errorMessage
       - Screen heading: t("confirm.heading", locale) in text-3xl font-bold centered
       - Visitor photo: h-24 w-24 rounded-full, from visitor.photoUrl (img with object-cover) or fallback User icon in bg-gray-100 circle
       - Visitor name: text-3xl font-bold text-gray-900, from visit.visitor.firstName + lastName
       - Company: text-base text-gray-500, from visit.visitor.company or "—"
       - Details card: white bg-white, rounded-kiosk, border border-gray-200, p-6, space-y-3
         - Labeled rows: t("confirm.hostLabel", locale): {visit.hostName}, t("confirm.companyLabel", locale): {company}, t("confirm.dateLabel", locale): formatted today at HH:MM, t("confirm.purposeLabel", locale): {visit.purpose || "—"}
       - Confirm button: h-14 touch-lg w-full max-w-sm, bg-blue-600 text-white, rounded-full
         - State "ready": t("confirm.button", locale)
         - State "confirming": Loader2 spinner + t("confirm.loading", locale), disabled
         - onClick → onConfirm
       - Cancel button: secondary bg-white border-2 h-12, t("common.cancel", locale) → onCancel
       - Error state: inline red card below buttons with errorMessage + retry
       - Import { User, Loader2 } from "lucide-react"
       - Import { VisitDto } from "@repo/shared" (for type only)
  </action>
  <verify>
    <automated>ls apps/kiosk/components/search-screen.tsx apps/kiosk/components/confirm-checkin.tsx && pnpm --filter @repo/kiosk build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Search screen: debounced input, loading skeletons, results with tap-to-select, no-results hint, error with retry, cancel button
    - Confirmation screen: photo or initials avatar, visitor name + company, details card with labeled rows, confirm/cancel buttons, confirming spinner state, error inline card
    - Uses VisitDto type from @repo/shared for type safety
    - Static build succeeds
  </acceptance_criteria>
  <done>Components render correctly. Static build succeeds.</done>
</task>

<task type="auto">
  <name>Task 4: Create root state machine in page.tsx integrating welcome, scanner, search, confirm</name>
  <files>
    apps/kiosk/app/page.tsx
  </files>
  <read_first>
    .planning/phases/003-visitor-kiosk/003-UI-SPEC.md — State machine diagram (lines 128-175), all transition rules
    .planning/phases/003-visitor-kiosk/003-RESEARCH.md — Pattern 1 state machine (lines 257-333)
    .planning/phases/003-visitor-kiosk/003-PATTERNS.md — page.tsx partial analog (state machine root)
    .planning/phases/003-visitor-kiosk/003-CONTEXT.md — D-09 (welcome CTA), D-08 (confirm flow), D-25 (no host approval)
  </read_first>
  <action>
    Create apps/kiosk/app/page.tsx:

    "use client" directive (state machine is client-side)

    Type definitions:
    ```
    type KioskPhase = "welcome" | "scanning" | "search" | "confirm" | "printing" | "success" | "checkout-success" | "error";
    
    type KioskAction =
      | { type: "SCAN_QR" }
      | { type: "SEARCH_NAME" }
      | { type: "QR_DECODED"; visitId: string }
      | { type: "VISIT_SELECTED"; visitId: string }
      | { type: "CONFIRM" }
      | { type: "PRINT_COMPLETE" }
      | { type: "PRINT_ERROR"; error: string }
      | { type: "CANCEL" }
      | { type: "HOME" }
      | { type: "TIMEOUT" }
      | { type: "CHECKOUT_SUCCESS"; visitorName: string };
    ```

    kioskReducer function implementing the full state machine from UI-SPEC (lines 287-333 of RESEARCH.md). Include ALL transitions:
    - welcome → scanning (SCAN_QR)
    - welcome → search (SEARCH_NAME)
    - scanning → confirm (QR_DECODED), scanning → welcome (CANCEL/TIMEOUT)
    - search → confirm (VISIT_SELECTED), search → welcome (CANCEL/TIMEOUT)
    - confirm → printing (CONFIRM), confirm → welcome (CANCEL/TIMEOUT)
    - printing → success (PRINT_COMPLETE), printing → error (PRINT_ERROR), printing → welcome (CANCEL)
    - success → welcome (HOME/TIMEOUT)
    - checkout-success → welcome (HOME/TIMEOUT)
    - error → printing (CONFIRM/retry), error → welcome (HOME)

    Default export function Home() (Next.js page convention):
      - useReducer(kioskReducer, "welcome")
      - useState for locale: "fr" (D-27 - FR primary)
      - useState for selectedVisitId: string | null
      - useIdleTimer hook (separate useEffect):
        - welcome: 60s timer (reset on interaction)
        - scanning/search/confirm: 30s timer → dispatch TIMEOUT
        - printing: 30s timer from last event
        - success: 8s auto-advance via setTimeout → dispatch HOME (setTimeout, not TIMEOUT)
        - checkout-success: 5s auto-advance → dispatch HOME
        - error: NO timeout (user must tap)
        - Use useEffect cleanup to clear timers
      
      - Handler functions:
        - handleScanClick → dispatch SCAN_QR
        - handleSearchClick → dispatch SEARCH_NAME
        - handleQRDecoded(visitId) → dispatch QR_DECODED, set selectedVisitId
        - handleSelectVisit(visitId) → dispatch VISIT_SELECTED, set selectedVisitId
        - handleConfirm → calls checkIn(selectedVisitId) → on success dispatch CONFIRM → calls printBadge → on success dispatch PRINT_COMPLETE, on error dispatch PRINT_ERROR
        - handleCancel → dispatch CANCEL
        - handleHome → dispatch HOME
        - handlePrintRetry → dispatch CONFIRM (re-triggers print flow)
        - handleCheckout(visitId) → calls checkOut(visitId) → on success dispatch CHECKOUT_SUCCESS with visitor name

      - Render switch on phase:
        - welcome → &lt;WelcomeScreen onScanClick, onSearchClick, locale, onToggleLocale /&gt;
        - scanning → &lt;QRScanner onQRDecoded, onCancel={handleCancel}, locale /&gt;
        - search → &lt;SearchScreen onSelectVisit={handleSelectVisit}, onCancel={handleCancel}, locale /&gt;
        - confirm → &lt;ConfirmCheckin visit={selectedVisit}, onConfirm={handleConfirm}, onCancel={handleCancel}, locale /&gt;
        - printing → (placeholder text for now: "Printing screen coming soon")
        - success → (placeholder: "Success screen coming soon")
        - checkout-success → (placeholder: "Checkout success screen coming soon")
        - error → (placeholder: "Error screen coming soon")
      
      Note: The printing/success/checkout-success/error placeholders will be replaced by Plan 03-04's real components.
      
      - Idle timer implementation:
        ```
        useEffect(() => {
          if (phase === "success" || phase === "checkout-success" || phase === "error") return;
          
          const timeoutMs = phase === "welcome" ? 60000 : 30000;
          const timer = setTimeout(() => dispatch({ type: "TIMEOUT" }), timeoutMs);
          
          return () => clearTimeout(timer);
        }, [phase]);
        ```
        For success: setTimeout 8000ms → dispatch HOME
        For checkout-success: setTimeout 5000ms → dispatch HOME
      
      - Import all components from @/components/*
      - Import t from @/lib/i18n
      - Import { checkIn, checkOut, printBadge } from "@/lib/kiosk-api"
      - Import { useState, useReducer, useEffect, useCallback } from "react"
  </action>
  <verify>
    <automated>pnpm --filter @repo/kiosk build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx uses "use client" directive and useReducer for state machine
    - All 8 phases defined (welcome, scanning, search, confirm, printing, success, checkout-success, error)
    - kioskReducer implements all transitions from UI-SPEC state machine diagram
    - Idle timers: 60s welcome, 30s mid-flow, 8s success, 5s checkout-success
    - Welcome/search scanning and confirm use real components; printing/success/checkout-success/error use placeholder text for Plan 03-04
    - handleConfirm calls checkIn then printBadge sequentially
    - handleCheckout calls checkOut(visitId)
    - Static build succeeds
  </acceptance_criteria>
  <done>Static build succeeds. SPA loads with state machine driving screen transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (kiosk SPA) → NestJS API | API key in X-API-Key header crosses network. SPA on LAN, API on LAN. |
| instascan (browser) → WebRTC camera | Camera stream stays in browser. No data sent externally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-KIO-10 | Information Disclosure | kiosk-api.ts (API_KEY in JS bundle) | accept | API key visible in browser devtools. Kiosk is LAN-only, not internet-facing. Generate per-kiosk narrow-scope keys. |
| T-KIO-11 | Spoofing | QR code scanning | mitigate | QR contains visit UUID. Server validates visit status + time window. Client-side QR decode is just transport — server does auth. |
| T-KIO-12 | Tampering | Name search (search-screen.tsx) | mitigate | Debounced search input is user-controlled. Server-side search uses parameterized Prisma query (no injection risk). |
| T-KIO-13 | Denial of Service | Camera permission denial | accept | Kiosk on Chromium kiosk mode: camera permission pre-granted via chrome://settings/content/camera. Permission denied fallback goes to search screen. |
</threat_model>

<verification>
1. Build: pnpm --filter @repo/kiosk build
2. Verify state machine logic: code review of kioskReducer transitions
3. Verify all components: each .tsx file exports a default React function component
</verification>

<success_criteria>
- Welcome screen renders with brand, CTAs, language toggle (D-09)
- QR scanner activates camera and decodes QR codes via instascan (D-19, D-20)
- Search screen debounces input and calls API (D-07)
- Confirmation screen displays visitor details and confirm button (D-08)
- i18n dictionary contains 50+ keys with FR/EN pairs (D-27)
- API client sends X-API-Key + X-Kiosk-Id headers on every request (D-21, D-05)
- State machine transitions through all defined phases with idle timers (D-12)
- Static export builds successfully
</success_criteria>

<output>
Create `.planning/phases/003-visitor-kiosk/03-03-SUMMARY.md` when done.
Run `pnpm --filter @repo/kiosk build` as final verification.
</output>
