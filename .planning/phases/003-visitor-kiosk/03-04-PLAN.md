---
phase: 003-visitor-kiosk
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - apps/kiosk/components/printing-screen.tsx
  - apps/kiosk/components/success-screen.tsx
  - apps/kiosk/components/checkout-screen.tsx
  - apps/kiosk/components/error-screen.tsx
  - apps/kiosk/app/page.tsx
autonomous: true
requirements: [KIO-01, KIO-02, KIO-03]
must_haves:
  truths:
    - "Printing screen shows animated progress indicator and transitions through connecting/generating/printing states"
    - "Printer error shows retry button and cancel-check-in button"
    - "Success screen shows 'Bienvenue {name}!' with badge preview and 8s auto-reset"
    - "Check-out screen scans badge QR, auto-check-out, shows 'Au revoir {name}!' with 5s auto-reset"
    - "Error screen shows context-specific message with retry and home buttons"
    - "Idle timeouts reset to welcome: 30s mid-flow, 8s success, 5s checkout-success"
    - "All screens use FR/EN i18n dictionary"
  artifacts:
    - path: "apps/kiosk/components/printing-screen.tsx"
      provides: "Print progress states + printer error with retry/cancel"
    - path: "apps/kiosk/components/success-screen.tsx"
      provides: "Welcome message + badge preview + auto-reset countdown"
    - path: "apps/kiosk/components/checkout-screen.tsx"
      provides: "Badge QR → auto-check-out → success overlay"
    - path: "apps/kiosk/components/error-screen.tsx"
      provides: "Fatal error with icon + message + retry/home"
    - path: "apps/kiosk/app/page.tsx"
      provides: "Integrated state machine with all 8 phases + checkout QR path"
  key_links:
    - from: "printing-screen.tsx"
      to: "KioskService.printBadge via kiosk-api.ts"
      via: "onPrintComplete / onError callbacks"
      pattern: "printBadge"
    - from: "success-screen.tsx"
      to: "8s auto-reset timer"
      via: "useEffect setTimeout"
      pattern: "setTimeout.*8000"
    - from: "checkout-screen.tsx"
      to: "QR scanner → checkOut API"
      via: "badge QR decode → api.checkOut()"
      pattern: "checkOut"
    - from: "page.tsx"
      to: "All 8 components"
      via: "import + switch/case in render"
      pattern: "import.*from.*@/components/"
---

<objective>
Build the remaining kiosk screens: printing progress with error/retry, success with auto-reset, badge-QR check-out flow, and error screen. Integrate all screens into the page.tsx state machine and wire idle timeouts, checkout QR scanning, and print error recovery.

Purpose: Completes the full kiosk flow (KIO-01 check-in/out, KIO-02 printing, KIO-03 QR scanning). After this plan, the kiosk handles all states end-to-end.

Output: Complete, buildable kiosk SPA with all 8 screens and full state transitions.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/003-visitor-kiosk/003-RESEARCH.md
@.planning/phases/003-visitor-kiosk/003-PATTERNS.md
@.planning/phases/003-visitor-kiosk/003-UI-SPEC.md
@.planning/phases/003-visitor-kiosk/003-CONTEXT.md

<interfaces>
<!-- Contracts from prior plans and existing code -->

From apps/kiosk/lib/kiosk-api.ts (created in 03-03):
  - checkIn(visitId: string): Promise<void>
  - checkOut(visitId: string): Promise<void>
  - printBadge(visitId: string): Promise<void>
  - KioskApiError with codes: PRINTER_ERROR, NETWORK, etc.

From apps/kiosk/lib/i18n.ts (created in 03-03):
  - t(key: string, locale: Locale, vars?: Record<string, string>): string
  - type Locale = "fr" | "en"

From apps/kiosk/app/page.tsx (created in 03-03):
  - type KioskPhase = "welcome" | "scanning" | "search" | "confirm" | "printing" | "success" | "checkout-success" | "error"
  - type KioskAction = { type: "SCAN_QR" | "SEARCH_NAME" | "QR_DECODED" | "VISIT_SELECTED" | "CONFIRM" | "PRINT_COMPLETE" | "PRINT_ERROR" | "CANCEL" | "HOME" | "TIMEOUT" | "CHECKOUT_SUCCESS"; ... }
  - useReducer(kioskReducer, "welcome") → [phase, dispatch]
  - useState<Locale>("fr")
  - useState for selectedVisitId, selectedVisitorName
  - handleConfirm: calls checkIn() then printBadge(), dispatches CONFIRM/PRINT_COMPLETE/PRINT_ERROR
  - handleCheckout(visitId): calls checkOut(), dispatches CHECKOUT_SUCCESS or error
  - handleCheckoutSuccess(name): dispatches CHECKOUT_SUCCESS with visitorName

From UI-SPEC.md:
  - Printing screen states: connecting, generating, printing, complete, error (lines 340-354)
  - Success screen: 8s auto-reset, badge preview, host notified line (lines 355-398)
  - Check-out: badge QR scan → auto-check-out → 5s auto-reset (lines 400-410)
  - Error screen: icon + heading + message + retry/home (lines 411-422)
  - Colors: emerald-600 success, red-600 error, blue-600 primary, amber-600 warning
  - Animations: animate-pulse for printing, animate-scale-in for checkmark, transition-all duration-200
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create printing screen with progress states and printer error handling</name>
  <files>
    apps/kiosk/components/printing-screen.tsx
  </files>
  <read_first>
    .planning/phases/003-visitor-kiosk/003-UI-SPEC.md — Printing screen spec (lines 340-354), Copy table (printing.* keys)
    .planning/phases/003-visitor-kiosk/003-CONTEXT.md — D-11 (badge printing → welcome), D-18 (printer error retry, check-in waits), D-16 (server-side CUPS)
    apps/kiosk/lib/i18n.ts — t() function and locale keys
  </read_first>
  <action>
    Create apps/kiosk/components/printing-screen.tsx:

    Props: { onPrintComplete: () => void; onCancelCheckin: () => void; locale: Locale; error?: string | null; onRetry: () => void }

    Component state machine (useState): "connecting" | "generating" | "printing" | "complete" | "error"

    - Progress simulation (useEffect):
      - On mount: connecting → after 1.5s → generating → after 2s → printing → after 2.5s → complete
      - The actual print API call happens in page.tsx (handleConfirm calls printBadge). This component visualizes the progress.
      - If error prop is passed (non-null), transition to "error" state
      - On "complete": call onPrintComplete after 1s delay

    Visual layout:
      - Full-screen centered (h-screen, flex flex-col items-center justify-center, bg-white)
      - Animated printer icon: Printer icon from lucide-react, 16×16 (h-16 w-16), text-blue-600, animate-pulse
      - Status text: t("printing.status", locale) in text-lg font-medium text-gray-900
      - Sub-status messages based on state:
        - "connecting": t("printing.connecting", locale)
        - "generating": t("printing.generating", locale)
        - "printing": t("printing.status", locale)
        - "complete": t("printing.done", locale) in text-emerald-600
      - Progress bar: indeterminate animated bar (h-1 w-64 bg-gray-200 rounded-full overflow-hidden, inner bar bg-blue-600 animate-pulse)
        - "complete" state: full green bar (bg-emerald-500, w-full)

      - Error state (when error prop is set):
        - Red bg-red-50 border border-red-200 rounded-kiosk p-6 card
        - PrinterOff icon text-red-500
        - t("printing.error", locale) heading
        - error message text
        - t("printing.errorDetail", locale) hint text
        - Retry button: h-14 touch-lg, bg-blue-600 text-white rounded-full → onRetry
        - Cancel check-in: text button t("printing.cancelCheckin", locale) text-gray-500 → onCancelCheckin

    - Import { Printer, PrinterOff, CheckCircle } from "lucide-react"
  </action>
  <verify>
    <automated>ls apps/kiosk/components/printing-screen.tsx && pnpm --filter @repo/kiosk build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Progress simulation flows: connecting → generating → printing → complete (auto-advance)
    - Error state displays red error card with error message, retry button, and cancel-check-in link
    - Complete state shows green checkmark and calls onPrintComplete after 1s
    - All text uses t() function with locale prop
    - Static build succeeds
  </acceptance_criteria>
  <done>Printing screen compiles. Static build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create success screen and checkout screen</name>
  <files>
    apps/kiosk/components/success-screen.tsx
    apps/kiosk/components/checkout-screen.tsx
  </files>
  <read_first>
    .planning/phases/003-visitor-kiosk/003-UI-SPEC.md — Success screen spec (lines 355-398), Check-out screen spec (lines 400-410), Copy table
    .planning/phases/003-visitor-kiosk/003-CONTEXT.md — D-10 (check-out no confirm), D-15 (badge content), D-24 (no checkout notification)
    apps/kiosk/lib/i18n.ts — t() function and i18n keys
  </read_first>
  <action>
    Create two components:

    1. apps/kiosk/components/success-screen.tsx:
       Props: { visitorName: string; hostName: string; visitDate: string; onHome: () => void; locale: Locale }
       
       - Full-screen centered layout (h-screen, flex flex-col items-center justify-center, bg-white)
       - Success icon: CheckCircle from lucide-react, h-20 w-20, text-emerald-500, animate-scale-in on mount
       - Welcome heading: t("success.heading", locale, { name: visitorName }) in text-3xl font-bold text-gray-900
       - Subtitle: t("success.subtitle", locale) in text-lg text-gray-500
       
       - Badge preview card: white bg-gray-50 border-2 border-dashed border-gray-300 rounded-kiosk p-6, w-72 h-44
         - Simulated QR code placeholder (small gray square with qr icon)
         - Visitor name in bold, host name, date
         - Centered content matching badge layout from D-15
       
       - Host notification: Bell icon + t("success.hostNotified", locale) in text-sm text-gray-500
       - Check-out hint: LogOut icon + t("success.checkoutHint", locale) in text-sm text-gray-400
       
       - Auto-reset countdown: t("success.countdown", locale, { seconds: countdown.toString() }) in text-sm text-gray-400
         - useEffect: setInterval 1s, countdown from 8 to 0, dispatch HOME at 0
         - Show "Retour à l'accueil dans {seconds}s"
       
       - Home button: h-12, bg-white border-2 border-gray-200 rounded-full, t("success.home", locale) → onHome
       
       - Import { CheckCircle, Bell, LogOut, QrCode } from "lucide-react"

    2. apps/kiosk/components/checkout-screen.tsx:
       Props: { visitorName: string; onHome: () => void; locale: Locale }
       
       - Full-screen centered layout
       - Two states: "processing" (brief) → "success" (5s)
       
       - Processing state (0-2s):
         - LogOut icon text-blue-600 animate-pulse
         - t("checkout.processing", locale) in text-lg
       
       - Success state (after processing):
         - LogOut icon text-emerald-500 animate-scale-in
         - t("checkout.success", locale, { name: visitorName }) in text-3xl font-bold
         - Auto-reset: t("success.countdown", locale, { seconds: String(countdown) })
         - useEffect: 5s countdown → onHome
       
       - Import { LogOut, CheckCircle2 } from "lucide-react"
  </action>
  <verify>
    <automated>ls apps/kiosk/components/success-screen.tsx apps/kiosk/components/checkout-screen.tsx && pnpm --filter @repo/kiosk build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Success screen: checkmark icon, "Bienvenue {name}!", badge preview card with QR placeholder, host notification, check-out hint, 8s countdown
    - Checkout screen: processing → success transition, "Au revoir {name}!", 5s auto-reset
    - Both screens animate in with scale-in on mount
    - All text uses t() with locale prop
    - Static build succeeds
  </acceptance_criteria>
  <done>Both screens compile. Static build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Create error screen with context-specific messaging</name>
  <files>
    apps/kiosk/components/error-screen.tsx
  </files>
  <read_first>
    .planning/phases/003-visitor-kiosk/003-UI-SPEC.md — Error screen spec (lines 411-422), Error & Edge Case States table (lines 780-800)
    .planning/phases/003-visitor-kiosk/003-CONTEXT.md — D-18 (printer error retry, check-in waits)
    apps/kiosk/lib/i18n.ts — error.* keys
    apps/kiosk/lib/kiosk-api.ts — KioskApiError codes for error message mapping
  </read_first>
  <action>
    Create apps/kiosk/components/error-screen.tsx:

    Props: { errorCode?: string; errorMessage?: string; onRetry?: () => void; onHome: () => void; locale: Locale }

    - Full-screen centered layout (h-screen, flex flex-col items-center justify-center, bg-white)
    - Error icon: AlertTriangle or XCircle from lucide-react, h-16 w-16, text-red-500
    - Heading: t("error.heading", locale) in text-3xl font-bold text-gray-900
    
    - Context-specific error messages:
      - If errorCode === "NETWORK" or "UNKNOWN": t("error.server", locale)
      - If errorCode === "PRINTER_ERROR": t("printing.errorDetail", locale) OR t("error.printerOffline", locale)
      - If errorCode === "UNAUTHORIZED": "Erreur d'authentification. Contactez la réception." / "Authentication error. Contact reception."
      - If errorCode === "ALREADY_CHECKED_IN": t("error.alreadyCheckedIn", locale)
      - If errorCode === "ALREADY_CHECKED_OUT": t("checkout.alreadyDone", locale)
      - If errorCode === "VISIT_EXPIRED": "Cette visite a expiré. Contactez votre hôte." / "This visit has expired. Contact your host."
      - Default: errorMessage || t("error.heading", locale)
    
    - Retry button (only if onRetry provided): h-14 touch-lg, bg-blue-600 text-white rounded-full, t("printing.retry", locale) → onRetry
    - Home button: h-12, bg-white border-2 border-gray-200 rounded-full, t("error.home", locale) → onHome

    - Auto-reset for non-fatal errors:
      - ALREADY_CHECKED_IN: 5s auto-reset → onHome
      - ALREADY_CHECKED_OUT: 3s auto-reset → onHome
      - VISIT_EXPIRED: 8s auto-reset → onHome
      - NETWORK/PRINTER_ERROR/UNAUTHORIZED/UNKNOWN: no auto-reset (user must tap)

    - Import { AlertTriangle, XCircle } from "lucide-react"
  </action>
  <verify>
    <automated>ls apps/kiosk/components/error-screen.tsx && pnpm --filter @repo/kiosk build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Renders error icon, heading, and context-specific message based on errorCode
    - Shows retry button only when onRetry callback provided
    - Shows home button always
    - Auto-reset for non-fatal errors (ALREADY_CHECKED_IN: 5s, ALREADY_CHECKED_OUT: 3s, VISIT_EXPIRED: 8s)
    - All text uses t() with locale prop
    - Static build succeeds
  </acceptance_criteria>
  <done>Error screen compiles. Static build succeeds.</done>
</task>

<task type="auto">
  <name>Task 4: Integrate all 4 screens into page.tsx — full state machine + checkout QR path</name>
  <files>
    apps/kiosk/app/page.tsx
  </files>
  <read_first>
    apps/kiosk/app/page.tsx — current state machine with placeholders for printing/success/checkout-success/error
    .planning/phases/003-visitor-kiosk/003-UI-SPEC.md — Full state machine diagram (lines 128-175), checkout path (lines 400-410)
    .planning/phases/003-visitor-kiosk/003-CONTEXT.md — D-10 (checkout scan badge QR only), D-12 (idle timeouts), D-24 (no checkout notification)
    apps/kiosk/lib/kiosk-api.ts — printBadge, checkOut, checkIn functions
  </read_first>
  <action>
    Modify apps/kiosk/app/page.tsx — replace placeholder sections with real components:

    1. Add imports for the 4 new components:
       ```
       import { PrintingScreen } from "@/components/printing-screen"
       import { SuccessScreen } from "@/components/success-screen"  
       import { CheckoutScreen } from "@/components/checkout-screen"
       import { ErrorScreen } from "@/components/error-screen"
       ```

    2. Add useState for error state management:
       ```
       const [errorCode, setErrorCode] = useState<string | undefined>()
       const [errorMessage, setErrorMessage] = useState<string | undefined>()
       const [visitorName, setVisitorName] = useState<string>("")
       ```
       - Reset error state when entering printing or returning to welcome

    3. Replace placeholder cases in the main switch statement:

       case "printing":
         return (
           &lt;PrintingScreen
             onPrintComplete={handlePrintComplete}
             onCancelCheckin={handleCancelCheckin}
             locale={locale}
             error={errorMessage}
             onRetry={handleRetry}
           /&gt;
         )

       case "success":
         return (
           &lt;SuccessScreen
             visitorName={visitorName}
             hostName={visitHostName}  // from the checked-in visit data
             visitDate={todayFormatted}
             onHome={handleHome}
             locale={locale}
           /&gt;
         )

       case "checkout-success":
         return (
           &lt;CheckoutScreen
             visitorName={visitorName}
             onHome={handleHome}
             locale={locale}
           /&gt;
         )

       case "error":
         return (
           &lt;ErrorScreen
             errorCode={errorCode}
             errorMessage={errorMessage}
             onRetry={errorCode === "PRINTER_ERROR" ? handleRetry : undefined}
             onHome={handleHome}
             locale={locale}
           /&gt;
         )

    4. Update the kioskReducer to handle the newly wired transitions:
       - The reducer from 03-03 already includes all states (printing, success, checkout-success, error)
       - Ensure the reducer correctly handles these transitions:
         - printing → success (PRINT_COMPLETE)
         - printing → error (PRINT_ERROR)
         - printing → welcome (CANCEL — "Annuler l'arrivée")
         - success → welcome (HOME or TIMEOUT)
         - checkout-success → welcome (HOME or TIMEOUT)
         - error → printing (CONFIRM — retry)
         - error → welcome (HOME)

    5. Update handleConfirm to properly manage the check-in → print flow:
       - Set phase to "printing" (dispatch CONFIRM)
       - Call checkIn(selectedVisitId) from kiosk-api
       - On success: store visitor data (name, host, etc.)
       - Call printBadge(selectedVisitId)
       - On print success: dispatch PRINT_COMPLETE
       - On print error: dispatch PRINT_ERROR with error message, setErrorCode("PRINTER_ERROR")
       - On check-in error: dispatch PRINT_ERROR with appropriate error code

    6. Add checkout QR scanning path:
       - When QR scanner detects a badge QR (visit ID that's already active/checked-in):
         - Call getVisit(visitId) to check if status is "active"
         - If "active": call checkOut(visitId), on success dispatch CHECKOUT_SUCCESS with visitor name
         - If not "active": stay in scanning, show "QR non reconnu" flash

    7. Update idle timeout wiring:
       - welcome → 60s (no dispatch needed, already there)
       - scanning → 30s → dispatch TIMEOUT
       - search → 30s → dispatch TIMEOUT
       - confirm → 30s → dispatch TIMEOUT
       - printing → 30s from last event → dispatch TIMEOUT
       - success → 8s auto (use setTimeout dispatch HOME via separate useEffect)
       - checkout-success → 5s auto (use setTimeout dispatch HOME)
       - error → no timeout (user must tap)

    8. Add state for visit data:
       ```
       const [selectedVisit, setSelectedVisit] = useState<VisitDto | null>(null)
       ```
       - Set after QR decode or visit selection
       - Used by confirm check-in and success screens
       - Clear on CANCEL/TIMEOUT/HOME

    Verify all imports resolve correctly. Build must succeed.
  </action>
  <verify>
    <automated>pnpm --filter @repo/kiosk build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 8 phase components imported and rendered via switch/case
    - handleConfirm flow: checkIn API → printBadge API → dispatch PRINT_COMPLETE or PRINT_ERROR
    - Checkout QR path: decode badge QR → getVisit → checkOut → dispatch CHECKOUT_SUCCESS
    - Error state properly manages errorCode and errorMessage for context-specific display
    - Retry on printer error re-calls printBadge
    - Cancel during printer error cancels check-in via checkIn reversal (no cancel API — just dispatch HOME)
    - Idle timeouts: 60s welcome, 30s mid-flow, 8s success, 5s checkout-success, no timeout on error
    - Static build succeeds
  </acceptance_criteria>
  <done>Static build succeeds. Full kiosk state machine renders all 8 screens with correct transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (kiosk SPA) → NestJS API | Check-in/out/print API calls with X-API-Key |
| QR scanner → WebRTC camera → instascan | All client-side, no cross-boundary data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-KIO-14 | Spoofing | Checkout QR scan | mitigate | Server validates visit status before check-out. QR contains UUID — client can't forge. |
| T-KIO-15 | Repudiation | Cancel check-in (printer error) | accept | Cancel just dispatches HOME — no cancel API call. Visit stays in "active" state. Audit trail preserved by server check-in event. |
| T-KIO-16 | Information Disclosure | Error screen showing error details | mitigate | Error messages are user-facing (printer offline, server unreachable). No stack traces, no DB details exposed. |
</threat_model>

<verification>
1. Build: pnpm --filter @repo/kiosk build
2. Verify state machine: code review all 8 phase transitions in kioskReducer
3. Verify all components render in page.tsx switch/case
</verification>

<success_criteria>
- Start → Welcome → Scan QR → Camera → QR found → Confirm → Confirm → Print → Success (8s) → Welcome (D-08, D-11, D-12)
- Start → Welcome → Search by name → Type name → Select visit → Confirm → Print → Success (D-07)
- Start → Welcome → Scan QR (badge QR) → Check-out → "Au revoir!"  (5s) → Welcome (D-10)
- Printer error → Retry → Print success → Success (D-18)
- Printer error → Cancel → Welcome (D-18)
- Idle 60s on Welcome → stays (D-12)
- Idle 30s on mid-flow → Welcome (D-12)
- Auto-reset after 8s on success, 5s on checkout (D-12)
- Static export builds with `pnpm --filter @repo/kiosk build` — no warnings or errors
</success_criteria>

<output>
Create `.planning/phases/003-visitor-kiosk/03-04-SUMMARY.md` when done.
Run `pnpm --filter @repo/kiosk build` as final verification.
</output>
