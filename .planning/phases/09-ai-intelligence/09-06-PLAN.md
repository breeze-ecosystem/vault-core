---
phase: 09-ai-intelligence
plan: 06
type: execute
wave: 4
depends_on: ["09-01", "09-04", "09-05"]
files_modified:
  - apps/api/src/modules/patterns/patterns.service.ts
  - apps/api/src/modules/ai/ai.controller.ts
  - apps/api/src/modules/risk/risk.controller.ts
  - apps/api/src/modules/patterns/patterns.controller.ts
  - apps/api/src/app.module.ts
  - apps/dashboard/lib/api.ts
  - apps/dashboard/lib/nav-config.ts
  - apps/api/src/modules/ai-agent/qdrant/qdrant.service.ts
  - apps/api/src/modules/ai/ai.processor.ts
  - apps/api/src/modules/ai-agent/ai-agent.module.ts
autonomous: false
requirements:
  - FTR-08
  - FTR-09
  - FTR-10
  - FTR-11

must_haves:
  truths:
    - "3 new SQL patterns detect false positives, schedule mismatches, and impossible travel"
    - "Qdrant collections are created and populated with event embeddings"
    - "Legacy /api/chat endpoint proxies to orchestrator with deprecation notice"
    - "Dashboard API client has functions for agent chat, risk explanation, and pattern analysis"
    - "Navigation config has Command Center and Patterns routes"
    - "AiAgentModule is registered in AppModule"
  artifacts:
    - path: "apps/api/src/modules/patterns/patterns.service.ts"
      provides: "8 total pattern detection (5 existing + 3 new)"
      contains: "false_positive"
    - path: "apps/api/src/modules/ai-agent/qdrant/qdrant.service.ts"
      provides: "Qdrant vector DB integration for event search and agent memory"
      exports: ["QdrantService"]
    - path: "apps/api/src/modules/ai/ai.controller.ts"
      provides: "Legacy endpoint proxy to orchestrator"
      contains: "deprecated"
  key_links:
    - from: "qdrant.service.ts"
      to: "Qdrant Docker service on port 6333"
      via: "@qdrant/js-client-rest"
      pattern: "QdrantClient"
    - from: "risk.controller.ts"
      to: "RiskAnalysisAgent"
      via: "GET /api/risk/scores/:zoneId/explain"
      pattern: "explain"
    - from: "app.module.ts"
      to: "AiAgentModule"
      via: "imports array"
      pattern: "AiAgentModule"
    - from: "nav-config.ts"
      to: "/command-center and /patterns routes"
      via: "nav group definition"
      pattern: "command-center"
---

<objective>
Wire everything together: add 3 new SQL patterns to PatternsService (false-positive per camera, schedule mismatch, impossible travel per D-29), create QdrantService for vector search and agent memory in Qdrant, set up Qdrant collection creation and the parallel embedding pipeline, proxy legacy /api/chat endpoint to the new orchestrator, add risk explanation and pattern analysis API endpoints, register AiAgentModule in AppModule, extend the dashboard API client with agent-related functions, and update navigation config for Command Center and Patterns pages.

Purpose: This is the integration layer — connecting the new agent system to the existing codebase and preparing the API surface for dashboard and mobile UI.

Output: 8 patterns, Qdrant pipeline, legacy proxy, new API endpoints, AppModule registration, dashboard API client, navigation config.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-intelligence/09-RESEARCH.md (Pattern: Qdrant Collection Setup + Search lines 603-647, Pitfall 3: Qdrant Collection Empty lines 497-509)
@.planning/phases/09-ai-intelligence/09-PATTERNS.md (Group 9: Cron/Patterns lines 601-649, Group 14: Dashboard API lines 989-1041, Group 18: Nav Config lines 1163-1176, Group 19: AppModule lines 1182-1200)
@.planning/phases/09-ai-intelligence/09-CONTEXT.md (D-17 hybrid vector, D-18 hybrid search, D-27 legacy replacement, D-29 pattern expansion)
@apps/api/src/modules/patterns/patterns.service.ts (lines 122-180 for pattern loop pattern — already in PATTERNS.md)
@apps/dashboard/lib/api.ts (line format per PATTERNS.md Group 14)
@apps/dashboard/lib/nav-config.ts (structure per PATTERNS.md Group 18)

<interfaces>
<!-- PatternsService pattern from PATTERNS.md lines 601-649 -->
PATTERNS array: [{ id, name, severity, query, params }]; @Cron('*/15 * * * *') detectPatterns(); Redis dedup with key pattern:dedup:{id}:{deviceId}

<!-- Dashboard API client pattern from PATTERNS.md lines 989-1010 -->
export async function functionName(...): Promise<TypeDto> { const res = await fetchWithAuth(...); if (!res.ok) throw...; return res.json(); }

<!-- Nav config pattern from PATTERNS.md lines 1163-1177 -->
{ label, href, icon, minRole } in nav groups

<!-- AiController pattern from ai.controller.ts (already in context) -->
@Controller("ai") with @Post and @Get endpoints, ZodValidationPipe, Roles decorator
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 3 new SQL patterns to PatternsService</name>
  <files>apps/api/src/modules/patterns/patterns.service.ts</files>
  <read_first>apps/api/src/modules/patterns/patterns.service.ts (full file — all ~399 lines)</read_first>
  <action>
Edit apps/api/src/modules/patterns/patterns.service.ts. Add 3 new pattern definitions to the existing PATTERNS array (which has 5 patterns per PATTERNS.md). Each new pattern follows the exact same structure as existing ones: { id, name, severity, query (parameterized SQL with $1, $2), params }.

New pattern 6 — false-positive per camera: Detect cameras where alert count/day exceeds 2 standard deviations above the organization's camera average over the past 7 days. Query: SELECT ae.camera_id, COUNT(*) as occurrence_count FROM access_events ae (or alerts table — use the appropriate hypertable) WHERE ae.organization_id = $1::uuid AND ae.time > NOW() - INTERVAL '7 days' AND ae.event_type = 'false_positive' GROUP BY ae.camera_id HAVING COUNT(*) > (SELECT AVG(cnt) + 2*STDDEV(cnt) FROM (SELECT COUNT(*) as cnt FROM access_events WHERE organization_id = $1 AND time > NOW() - INTERVAL '7 days' AND event_type = 'false_positive' GROUP BY camera_id) sub). Params: [organizationId]. Severity: "Avertissement". id: "false-positive".

New pattern 7 — schedule mismatch: Detect events occurring outside zone schedules. Query: SELECT ae.zone_id, ae.event_type, COUNT(*) as occurrence_count FROM access_events ae LEFT JOIN zones z ON ae.zone_id = z.id LEFT JOIN schedules s ON z.id = s.zone_id WHERE ae.organization_id = $1::uuid AND ae.time > NOW() - INTERVAL '24 hours' AND s.id IS NOT NULL AND (EXTRACT(HOUR FROM ae.time) NOT BETWEEN s.start_hour AND s.end_hour) GROUP BY ae.zone_id, ae.event_type HAVING COUNT(*) > 3. Params: [organizationId]. Severity: "Avertissement". id: "schedule-mismatch". (Note: adjust column names to match actual schema — schedules.entries is JSON, so this query may need adaptation. Use $queryRawUnsafe with actual column names from the Prisma schema.)

New pattern 8 — impossible travel: Detect same credential used at doors > 500m apart within < 60 seconds. Query: SELECT ae1.credential_id, ae1.door_id as door1, ae2.door_id as door2, COUNT(*) as occurrence_count FROM access_events ae1 JOIN access_events ae2 ON ae1.credential_id = ae2.credential_id AND ae1.time < ae2.time AND ae2.time - ae1.time < INTERVAL '60 seconds' JOIN doors d1 ON ae1.door_id = d1.id JOIN doors d2 ON ae2.door_id = d2.id WHERE ae1.organization_id = $1::uuid AND ae2.organization_id = $2::uuid AND (ABS(d1.latitude - d2.latitude) > 0.005 OR ABS(d1.longitude - d2.longitude) > 0.005) AND ae1.time > NOW() - INTERVAL '24 hours' GROUP BY ae1.credential_id, ae1.door_id, ae2.door_id HAVING COUNT(*) > 2. Params: [organizationId, organizationId]. Severity: "Critique". id: "impossible-travel". (Note: adapt column references to actual TimescaleDB hypertable columns and Door model fields.)

All 3 patterns: use the existing Redis dedup pattern (dedupKey = `pattern:dedup:${pattern.id}:${deviceId}`), emit `pattern.detected` event via EventEmitter2, insert into detected_patterns hypertable. Follow the existing detectPatterns() loop pattern exactly.

Update pattern count: the cron loop already iterates `for (const pattern of PATTERNS)` so adding to the array is sufficient. No loop logic changes needed. Total patterns: 8 per D-22 + D-29.
  </action>
  <verify>
    <automated>grep -c '"false-positive"\|"schedule-mismatch"\|"impossible-travel"' apps/api/src/modules/patterns/patterns.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/modules/patterns/patterns.service.ts PATTERNS array contains 8 total patterns (5 existing + 3 new)
    - New patterns: false-positive (severity: Avertissement), schedule-mismatch (severity: Avertissement), impossible-travel (severity: Critique)
    - Each new pattern has: id, name, severity, query (parameterized SQL with $1/$2 bind parameters), params
    - Each pattern uses Redis dedup with key `pattern:dedup:{pattern.id}:{deviceId}`
    - Each pattern emits pattern.detected event via EventEmitter2
    - Queries use $queryRawUnsafe with parameter binding — NO string interpolation of organizationId
    - npm run check-types (from apps/api) exits 0
  </acceptance_criteria>
  <done>3 new SQL patterns added — 8 total patterns with false-positive, schedule-mismatch, and impossible-travel detection</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create QdrantService, extend embedding pipeline, wire AppModule, add legacy proxy, add risk/pattern API endpoints</name>
  <files>apps/api/src/modules/ai-agent/qdrant/qdrant.service.ts, apps/api/src/modules/ai/ai.processor.ts, apps/api/src/modules/ai/ai.controller.ts, apps/api/src/modules/risk/risk.controller.ts, apps/api/src/modules/patterns/patterns.controller.ts, apps/api/src/app.module.ts, apps/api/src/modules/ai-agent/ai-agent.module.ts</files>
  <read_first>apps/api/src/modules/ai/ai.service.ts (lines 311-330 for embedEvent pgvector pattern), apps/api/src/modules/ai/ai.controller.ts (full file), apps/api/src/modules/risk/risk.controller.ts (first 30 lines), apps/api/src/modules/patterns/patterns.controller.ts (first 30 lines), apps/api/src/app.module.ts (full file), apps/api/src/modules/ai-agent/ai-agent.module.ts</read_first>
  <action>
Create apps/api/src/modules/ai-agent/qdrant/qdrant.service.ts: @Injectable() service. Inject ConfigService (read qdrantUrl). Initialize QdrantClient from @qdrant/js-client-rest on construction. Methods:

- initializeCollections(): Called on module init. Creates 3 Qdrant collections if they don't exist: events (vectors: { size: 4096, distance: 'Cosine' }), knowledge (4096-dim Cosine for agent memory), incidents (4096-dim Cosine for incident summaries). Per D-17 (Qwen embeddings = 4096-dim).

- upsertEvents(points: Array<{ id, vector, payload: { organizationId, event_type, zone, time, severity } }>): Bulk upsert to events collection with wait: true.

- searchEvents(queryEmbedding, filters: { organizationId, eventType?, zone?, timeRange?, limit? }): Hybrid search. Build Qdrant filter with must: [ { key: 'organizationId', match: { value } }, ... ]. Call client.query('events', { query: queryEmbedding, filter, limit, with_payload: true }). Return scored results.

- upsertKnowledge / searchKnowledge: Same pattern for knowledge collection.

- healthCheck(): GET /healthz on Qdrant URL. Returns boolean.

Edit apps/api/src/modules/ai/ai.processor.ts: In the existing 'embed-event' job handler (or add a new handler), add a parallel write to Qdrant. After inserting into pgvector event_embeddings, call QdrantService.upsertEvents() with the same event data + Qwen embedding (use LlmProviderService.embed() with qwen-embedding model for 4096-dim). Per D-17 — pgvector continues with nomic-embed-text (768-dim), Qdrant gets Qwen embeddings (4096-dim). Wrap Qdrant write in try/catch — pgvector write must not be blocked by Qdrant failure (per D-12 fail-transparent). Per Pitfall 3 — this prevents the empty Qdrant collection problem.

Edit apps/api/src/modules/ai/ai.controller.ts: Add deprecation notice to the /api/ai/query, /api/ai/assistant, and /api/ai/summarize endpoints. Add new proxy endpoints:

- POST /api/ai/agent-chat: Proxies to OrchestratorService.executeQuery(). Body validates with agentChatSchema from shared package. This is a synchronous (non-SSE) fallback. Add @Roles('ADMIN','SUPERVISOR','OPERATOR').

- The existing /api/ai/* endpoints remain functional but log deprecation warnings. Per D-27 coexistence — nothing is removed, only deprecated.

Edit apps/api/src/modules/risk/risk.controller.ts: Add endpoint:
- GET /api/risk/scores/:zoneId/explain: @Roles('ADMIN','SUPERVISOR'). Calls RiskAnalysisAgent.explain(zoneId). Returns RiskExplanation. Add @Query('stream') option for SSE — if stream=true, delegates to SSE endpoint.

Edit apps/api/src/modules/patterns/patterns.controller.ts: Add endpoint (if controller exists) or add to existing query endpoint:
- GET /api/patterns/:patternId/analyze: @Roles('ADMIN','SUPERVISOR'). Calls PatternDetectionAgent.analyze(patternId). Returns trend analysis.

Edit apps/api/src/app.module.ts: Add `import { AiAgentModule } from './modules/ai-agent/ai-agent.module'` and add `AiAgentModule` to the imports array. Place after AiModule import.

Edit apps/api/src/modules/ai-agent/ai-agent.module.ts: Register QdrantService in providers. Register AiProcessor modification as needed (or keep it in AiModule for now).
  </action>
  <verify>
    <automated>grep -c "QdrantClient" apps/api/src/modules/ai-agent/qdrant/qdrant.service.ts && grep -c "AiAgentModule" apps/api/src/app.module.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/modules/ai-agent/qdrant/qdrant.service.ts exports QdrantService with initializeCollections(), upsertEvents(), searchEvents(), upsertKnowledge(), searchKnowledge()
    - Collections: events (4096-dim Cosine), knowledge (4096-dim Cosine), incidents (4096-dim Cosine) per D-17
    - apps/api/src/modules/ai/ai.processor.ts writes embeddings to both pgvector AND Qdrant in parallel per Pitfall 3 prevention
    - apps/api/src/modules/ai/ai.controller.ts has POST /api/ai/agent-chat proxy endpoint; existing endpoints preserved with deprecation logs per D-27
    - apps/api/src/modules/risk/risk.controller.ts has GET /api/risk/scores/:zoneId/explain
    - apps/api/src/modules/patterns/patterns.controller.ts has GET /api/patterns/:patternId/analyze
    - apps/api/src/app.module.ts imports AiAgentModule
    - Qdrant write in processor is wrapped in try/catch — pgvector write not blocked by Qdrant failure
    - npm run check-types (from apps/api) exits 0
  </acceptance_criteria>
  <done>QdrantService with 3 collections; parallel embedding pipeline; legacy proxy with deprecation; risk/pattern API endpoints; AppModule wired</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend dashboard API client and update navigation config</name>
  <files>apps/dashboard/lib/api.ts, apps/dashboard/lib/nav-config.ts</files>
  <read_first>apps/dashboard/lib/api.ts (focus on existing AI functions around lines 1481-1521 + fetchWithAuth pattern), apps/dashboard/lib/nav-config.ts (full file)</read_first>
  <action>
Edit apps/dashboard/lib/api.ts: Add new API functions following the existing pattern (fetchWithAuth + typed response + French error messages):

1. agentChat(message: string): Promise<{ sessionId: string, response: string, toolCalls?: any[] }> — POST /api/ai/agent-chat. Synchronous fallback for non-SSE chat.

2. createAgentChatStream(message: string, sessionId: string, callbacks: { onToken, onToolCall, onDone, onError }): () => void — SSE streaming helper per PATTERNS.md lines 1018-1039. Creates EventSource to /api/ai-agent/chat?message=...&sessionId=..., wires callbacks, returns cleanup function.

3. explainRisk(zoneId: string): Promise<RiskExplanation> — GET /api/risk/scores/{zoneId}/explain.

4. analyzePattern(patternId: string): Promise<any> — GET /api/patterns/{patternId}/analyze.

5. getAgentStatus(): Promise<{ ollamaAvailable, agentsCount, modelStatus }> — GET /api/ai-agent/status.

Add TypeScript interfaces inline for responses: AgentChatResponse, RiskExplanation (matching shared types), AgentStatusResponse. Follow the existing pattern: `export async function functionName(...): Promise<Type> { const res = await fetchWithAuth(...); if (!res.ok) { const errData = await res.json().catch(() => ({})); throw new Error(errData.message || 'French error message'); } return res.json(); }`.

Edit apps/dashboard/lib/nav-config.ts: Per UI-SPEC.md Navigation Contract lines 283-308:

1. Remove/rename: "Assistant IA" at /ia → replace with "Centre de commande" at /command-center. Change icon from MessageSquare to Zap.

2. Add new nav group "Intelligence" (icon: Zap, minRole: null) with items:
   - { label: "Centre de commande", href: "/command-center", icon: Zap, minRole: null }
   - { label: "Motifs", href: "/patterns", icon: Repeat, minRole: "SUPERVISOR" as Role }

3. Keep "Risques" at /risque in its current group (enhanced, not moved).

4. Move "Assistant IA" out of "Outils" group — replaced by Command Center.

Import Zap icon from lucide-react. Import Repeat icon from lucide-react (check if already imported). Follow existing nav group structure exactly.
  </action>
  <verify>
    <automated>grep -c "agentChat\|createAgentChatStream\|explainRisk\|analyzePattern\|getAgentStatus" apps/dashboard/lib/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/dashboard/lib/api.ts exports: agentChat(), createAgentChatStream(), explainRisk(), analyzePattern(), getAgentStatus()
    - createAgentChatStream() returns cleanup function for EventSource teardown
    - All API functions use fetchWithAuth() following existing pattern
    - All error messages are in French
    - apps/dashboard/lib/nav-config.ts has "Intelligence" group with Command Center and Motifs (Patterns)
    - "Assistant IA" at /ia replaced by "Centre de commande" at /command-center
    - Nav config uses Zap icon for Command Center, Repeat icon for Patterns
    - Patterns route restricted to minRole: "SUPERVISOR"
    - npm run check-types (from apps/dashboard) exits 0
  </acceptance_criteria>
  <done>Dashboard API client extended with agent functions + SSE helper; navigation updated with Command Center and Patterns routes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard → API (agent chat) | User chat input travels to agent system |
| Qdrant service → API | Embedding pipeline writes to Qdrant over Docker network |
| Legacy endpoints → Orchestrator | Deprecated endpoints proxied to new agent system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-21 | Information Disclosure | Qdrant cross-org search | mitigate | QdrantService.searchEvents() always includes must filter on organizationId — tenant isolation at vector search level per D-31 |
| T-09-22 | Tampering | SQL injection in new pattern queries | mitigate | All 3 new pattern queries use PostgreSQL $1/$2 parameter binding via $queryRawUnsafe — no string interpolation |
| T-09-23 | Denial of Service | Legacy endpoint unbounded proxy | mitigate | Legacy proxy endpoint still goes through rate-limit-agent.guard per D-32; max message length 2000 chars via Zod schema |
| T-09-24 | Information Disclosure | Qdrant collection without auth | mitigate | Qdrant service on internal Docker network only (no published ports); API key will be added if Qdrant supports it |
</threat_model>

<verification>
1. Run `grep -c "false-positive" apps/api/src/modules/patterns/patterns.service.ts` — must be ≥ 1
2. Run `grep -c "QdrantService" apps/api/src/modules/ai-agent/qdrant/qdrant.service.ts` — must be ≥ 1
3. Run `grep -c "AiAgentModule" apps/api/src/app.module.ts` — must be ≥ 1
4. Run `grep -c "deprecated\|deprecation\|DeprecationWarning" apps/api/src/modules/ai/ai.controller.ts` — must be ≥ 1
5. Run `grep -c "command-center" apps/dashboard/lib/nav-config.ts` — must be ≥ 1
6. Run `grep -c "agentChat" apps/dashboard/lib/api.ts` — must be ≥ 1
7. Run `npm run check-types` from both apps/api and apps/dashboard — must exit 0
</verification>

<success_criteria>
- 3 new SQL patterns (8 total) with Redis dedup and EventEmitter
- QdrantService with 3 collections and hybrid search
- Parallel embedding pipeline (pgvector + Qdrant)
- AiAgentModule registered in AppModule
- Legacy /api/ai endpoints proxied with deprecation
- Risk explanation and pattern analysis API endpoints
- Dashboard API client with agent functions + SSE helper
- Navigation config with Command Center and Patterns routes
</success_criteria>

<output>
Create `.planning/phases/09-ai-intelligence/09-06-SUMMARY.md` when done
</output>
