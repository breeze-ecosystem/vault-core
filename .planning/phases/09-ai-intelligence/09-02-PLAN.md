---
phase: 09-ai-intelligence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/ai-agent/ai-agent.module.ts
  - apps/api/src/modules/ai-agent/skills/skill.decorator.ts
  - apps/api/src/modules/ai-agent/skills/skill-registry.service.ts
  - apps/api/src/modules/ai-agent/types/agent.types.ts
  - packages/shared/src/schemas/agent.schema.ts
  - packages/shared/src/types/agent.types.ts
  - packages/shared/src/index.ts
  - apps/api/src/modules/ai-agent/prompts/orchestrator.prompt.md
  - apps/api/src/modules/ai-agent/prompts/event-search.prompt.md
  - apps/api/src/modules/ai-agent/prompts/risk-analysis.prompt.md
  - apps/api/src/modules/ai-agent/prompts/pattern-detection.prompt.md
  - apps/api/src/modules/ai-agent/prompts/incident.prompt.md
  - apps/api/src/modules/ai-agent/prompts/door-control.prompt.md
autonomous: true
requirements:
  - FTR-08
  - FTR-09
  - FTR-10
  - FTR-11

must_haves:
  truths:
    - "@Skill() decorator can be applied to any class and metadata is extracted by SkillRegistry"
    - "Shared Zod schemas validate agent chat input and SSE events"
    - "System prompts are version-controlled .md files, separate from agent code"
    - "AiAgentModule exports SkillRegistry and OrchestratorService for downstream plans"
  artifacts:
    - path: "apps/api/src/modules/ai-agent/skills/skill.decorator.ts"
      provides: "@Skill() class decorator"
      exports: ["Skill", "SKILL_METADATA", "SkillDefinition"]
    - path: "apps/api/src/modules/ai-agent/skills/skill-registry.service.ts"
      provides: "Auto-discovery of @Skill() decorated providers"
      exports: ["SkillRegistry"]
    - path: "packages/shared/src/schemas/agent.schema.ts"
      provides: "Zod schemas for agent I/O"
      contains: "agentChatSchema"
  key_links:
    - from: "skill-registry.service.ts"
      to: "skill.decorator.ts"
      via: "Reflect.getMetadata(SKILL_METADATA, metatype)"
      pattern: "SKILL_METADATA"
    - from: "ai-agent.module.ts"
      to: "SkillRegistry"
      via: "providers array"
      pattern: "SkillRegistry"
---

<objective>
Create the AiAgentModule foundation: TypeScript type interfaces for the agent system (AgentContext, AgentResult, SSEEvent, SkillDefinition), shared Zod schemas for agent chat input and SSE events, the @Skill() decorator with SkillRegistry using NestJS DiscoveryService for auto-registration, the AiAgentModule that registers the SkillRegistry and exports it, and version-controlled system prompt .md files for all 6 agents (orchestrator, event-search, risk-analysis, pattern-detection, incident, door-control).

Purpose: Establish the contracts (types, schemas, decorators) that Plans 04 through 06 will implement against. Without this shared foundation, agent implementations would have inconsistent interfaces.

Output: AiAgentModule skeleton, SkillRegistry with auto-discovery, shared types and schemas, prompt files.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-intelligence/09-RESEARCH.md (Pattern 2: @Skill() Decorator Registry lines 303-345)
@.planning/phases/09-ai-intelligence/09-PATTERNS.md (Group 1: NestJS Module lines 110-156, Group 3: Decorator lines 315-341, Group 13: Shared Zod Schemas lines 960-986)
@.planning/phases/09-ai-intelligence/09-CONTEXT.md (D-03 Skill Registry, D-11 System prompts, D-37 Multilingual)

<interfaces>
<!-- NestJS module pattern from apps/api/src/modules/ai/ai.module.ts (already in context) -->
@Module({ imports, controllers, providers, exports }) with BullModule.registerQueue

<!-- Decorator pattern from apps/api/src/common/decorators/roles.decorator.ts (already in context) -->
import { SetMetadata } from "@nestjs/common"; export const ROLES_KEY = "roles";

<!-- Shared schema pattern from packages/shared/src/schemas/ai.schema.ts (already in context) -->
import { z } from "zod"; export const schemaName = z.object({...}); export type TypeName = z.infer<typeof schemaName>;

<!-- Barrel export pattern from packages/shared/src/index.ts (already in context) -->
Sectioned exports with comments like "// Schemas - AI"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create shared types, schemas, and prompt files</name>
  <files>packages/shared/src/schemas/agent.schema.ts, packages/shared/src/types/agent.types.ts, packages/shared/src/index.ts, apps/api/src/modules/ai-agent/types/agent.types.ts, apps/api/src/modules/ai-agent/prompts/*.md</files>
  <read_first>packages/shared/src/schemas/ai.schema.ts, packages/shared/src/types/ai.types.ts, packages/shared/src/index.ts, apps/api/src/modules/ai/ai.service.ts (lines 1-35 for type imports)</read_first>
  <action>
Create packages/shared/src/schemas/agent.schema.ts with Zod schemas: agentChatSchema (message: z.string().min(1).max(2000)), agentSseEventSchema (type: z.enum(['thinking','token','tool_call','tool_result','agent_switch','error','done']), content: z.string().optional(), agent: z.string().optional(), tool: z.string().optional()), riskExplainSchema (zoneId: z.string().uuid()). Export inferred types.

Create packages/shared/src/types/agent.types.ts with AgentChatInput, AgentChatMessage (role, content, timestamp), SSEEvent, AgentStatus (agentName, status: 'idle'|'thinking'|'responding'|'error'), RiskExplanation, PatternDetail, AgentTraceEntry types.

Add barrel exports to packages/shared/src/index.ts under "// Types - Agent" and "// Schemas - Agent" sections following existing pattern.

Create apps/api/src/modules/ai-agent/types/agent.types.ts with AgentContext interface (userId: string, organizationId: string, role: string, language?: string), AgentResult (content, toolCalls, metadata), ToolCallDefinition (name, description, inputSchema as ZodTypeAny), SkillDefinition (name, description, inputSchema as ZodTypeAny).

Create 6 prompt files under apps/api/src/modules/ai-agent/prompts/:
- orchestrator.prompt.md: French-first system prompt instructing the orchestrator to plan→delegate→aggregate. Include "Réponds en français sauf si l'utilisateur parle une autre langue."
- event-search.prompt.md: Convert NL to structured event search queries. Include tool definitions for search_events.
- risk-analysis.prompt.md: Analyze zone risk factors from event data. Include "Explique ce qui cause le score de risque."
- pattern-detection.prompt.md: Detect and explain recurring security patterns.
- incident.prompt.md: Generate structured incident summaries with timeline, zones, persons, video evidence, recommended actions. Include required JSON output format.
- door-control.prompt.md: Vision analysis instructions for camera assessment and door control actions with guardrail awareness.

All prompts reference D-37 (multilingual — French primary) and D-30 (action guardrails — no auto-execution without confirmation).
  </action>
  <verify>
    <automated>grep -c "agentChatSchema" packages/shared/src/schemas/agent.schema.ts && grep -c "agentChatSchema" packages/shared/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/schemas/agent.schema.ts exports agentChatSchema, agentSseEventSchema, riskExplainSchema
    - packages/shared/src/types/agent.types.ts exports AgentChatInput, AgentChatMessage, SSEEvent, AgentStatus, RiskExplanation, AgentTraceEntry
    - packages/shared/src/index.ts has barrel exports for all new agent schemas and types
    - apps/api/src/modules/ai-agent/types/agent.types.ts exports AgentContext, AgentResult, ToolCallDefinition, SkillDefinition
    - apps/api/src/modules/ai-agent/prompts/ directory contains 6 .md files, each with substantive French-first system prompts
    - npm run check-types (from root) exits 0
  </acceptance_criteria>
  <done>Shared types and schemas defined; prompt files created; all exports wired through barrel</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create @Skill() decorator, SkillRegistry, and AiAgentModule</name>
  <files>apps/api/src/modules/ai-agent/skills/skill.decorator.ts, apps/api/src/modules/ai-agent/skills/skill-registry.service.ts, apps/api/src/modules/ai-agent/ai-agent.module.ts</files>
  <read_first>apps/api/src/common/decorators/roles.decorator.ts, apps/api/src/modules/ai/ai.module.ts, apps/api/src/modules/ai-agent/types/agent.types.ts</read_first>
  <action>
Create apps/api/src/modules/ai-agent/skills/skill.decorator.ts: Export SkillDefinition interface from agent.types (name, description, inputSchema), SKILL_METADATA constant, and Skill() function that calls SetMetadata(SKILL_METADATA, def). Follow the exact pattern from roles.decorator.ts — a simple SetMetadata wrapper. The SkillDefinition uses ZodTypeAny for inputSchema (so skills define their own Zod schemas as input contracts).

Create apps/api/src/modules/ai-agent/skills/skill-registry.service.ts: Injectable service implementing OnModuleInit. Inject DiscoveryService from @nestjs/core. In onModuleInit(): call this.discovery.getProviders(), iterate wrappers, check Reflect.getMetadata(SKILL_METADATA, metatype) on each, if found register in a Map<string, {definition: SkillDefinition, instance: any}>. Provide methods: getSkill(name): returns skill definition + execute method, listSkills(): returns all registered skill definitions, hasSkill(name): boolean. Export the SkillRegistry class.

Create apps/api/src/modules/ai-agent/ai-agent.module.ts: NestJS module importing EventEmitterModule (already global, no need to re-import), BullModule.registerQueue({ name: "ai-agent" }) for future agent task dispatch. Providers: SkillRegistry, DiscoveryService. Exports: SkillRegistry (so downstream plans can inject it). No controllers yet (added in Plan 04). No agents registered yet (agents are created in Plan 04-05). Follow the exact structure from ai.module.ts — same import style, same @Module decorator pattern.

The module must NOT import AiModule (coexistence per D-09) — it runs alongside it, not within it.
  </action>
  <verify>
    <automated>grep -c "SKILL_METADATA" apps/api/src/modules/ai-agent/skills/skill.decorator.ts && grep -c "getProviders" apps/api/src/modules/ai-agent/skills/skill-registry.service.ts && grep -c "SkillRegistry" apps/api/src/modules/ai-agent/ai-agent.module.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/modules/ai-agent/skills/skill.decorator.ts exports Skill (function), SKILL_METADATA (string), SkillDefinition (interface)
    - apps/api/src/modules/ai-agent/skills/skill-registry.service.ts exports SkillRegistry class with OnModuleInit, getSkill(), listSkills(), hasSkill() methods
    - apps/api/src/modules/ai-agent/ai-agent.module.ts exports AiAgentModule with SkillRegistry in providers and exports
    - Decorator uses SetMetadata pattern consistent with existing apps/api/src/common/decorators/roles.decorator.ts
    - npm run check-types (from apps/api) exits 0
    - Module does NOT import AiModule (grep -c "AiModule" ai-agent.module.ts is 0)
  </acceptance_criteria>
  <done>Skill decorator created; SkillRegistry auto-discovers decorated providers on init; AiAgentModule skeleton ready for agent registration</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → System prompts | User messages are wrapped in XML tags to prevent prompt injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Spoofing | System prompts (.md files) | mitigate | Prompt injection prevention via XML boundary tags; user input wrapped in `<user_query>` tags in prompt templates; system prompts instruct model to treat user input as data, not instructions |
| T-09-04 | Information Disclosure | Agent context (organizationId, userId) | mitigate | AgentContext types carry organizationId for tenant scoping; SkillRegistry is read-only after module init — no runtime mutation of skill definitions |
</threat_model>

<verification>
1. Run `grep -c "agentChatSchema" packages/shared/src/schemas/agent.schema.ts` — must be 1
2. Run `grep -c "SkillRegistry" apps/api/src/modules/ai-agent/ai-agent.module.ts` — must be ≥ 1
3. Run `grep -c "SetMetadata" apps/api/src/modules/ai-agent/skills/skill.decorator.ts` — must be 1
4. Run `ls apps/api/src/modules/ai-agent/prompts/*.md | wc -l` — must be 6
5. Run `npm run check-types` from apps/api — must exit 0
</verification>

<success_criteria>
- Shared schemas and types defined for agent I/O
- @Skill() decorator follows NestJS SetMetadata pattern
- SkillRegistry auto-discovers skills via DiscoveryService
- AiAgentModule skeleton exports SkillRegistry
- 6 version-controlled prompt files created
- TypeScript compilation passes
</success_criteria>

<output>
Create `.planning/phases/09-ai-intelligence/09-02-SUMMARY.md` when done
</output>
