---
phase: 09-ai-intelligence
plan: 08
type: execute
wave: 5
depends_on: ["09-06"]
files_modified:
  - apps/mobile/app/(tabs)/more/chat.tsx
  - apps/mobile/app/(tabs)/more.tsx
  - apps/mobile/lib/api.ts
autonomous: true
requirements:
  - FTR-08
  - FTR-09

must_haves:
  truths:
    - "Guard opens chat from More tab and sends text messages to the AI assistant"
    - "Voice input triggers Faster-Whisper transcription and auto-sends transcribed text"
    - "Quick action chips allow one-tap common queries"
    - "Offline state shows 'Hors ligne. Le chat nécessite une connexion.' banner"
  artifacts:
    - path: "apps/mobile/app/(tabs)/more/chat.tsx"
      provides: "Full-screen chat with SSE streaming and voice input"
      contains: "chat"
    - path: "apps/mobile/app/(tabs)/more.tsx"
      provides: "Updated More tab with chat route"
      contains: "/(tabs)/more/chat"
  key_links:
    - from: "chat.tsx"
      to: "SSE endpoint /api/ai-agent/chat"
      via: "fetchWithAuth + EventSource polyfill"
      pattern: "ai-agent/chat"
    - from: "more.tsx"
      to: "chat.tsx"
      via: "router.push('/(tabs)/more/chat')"
      pattern: "more/chat"
---

<objective>
Add AI chat to the mobile app: a full-screen chat interface in the More tab with SSE streaming, voice input via Faster-Whisper transcription, contextual quick actions, and offline detection. Update the More tab menu to route to the chat screen, and extend the mobile API client with agent chat functions.

Purpose: Per D-23, the mobile app needs guard-first chat with voice input and contextual quick actions for field operators.

Output: Mobile chat screen, updated More tab, extended mobile API client.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-intelligence/09-UI-SPEC.md (Mobile Chat Copywriting lines 189-200, Mobile Chat Interaction lines 376-385)
@.planning/phases/09-ai-intelligence/09-PATTERNS.md (Group 12: Mobile Screen lines 866-956)
@.planning/phases/09-ai-intelligence/09-CONTEXT.md (D-23 mobile chat, D-19 voice transcription)
@apps/mobile/app/(tabs)/more.tsx (full file ~126 lines — menu structure + StyleSheet pattern)
@apps/mobile/lib/api.ts (existing pattern for API calls)

<interfaces>
<!-- Mobile screen pattern from more.tsx (PATTERNS.md lines 866-955) -->
import { View, Text, StyleSheet, ScrollView, Pressable } from "react-native"; colors.dark.* tokens; StyleSheet.create() at bottom

<!-- Mobile API call pattern from PATTERNS.md lines 949-953 -->
import { fetchWithAuth } from "@/lib/auth-client"; import { API_URL as API_BASE } from "@/lib/config";

<!-- More tab menu item pattern (PATTERNS.md lines 940-942) -->
{ id: "chat", label: "Chat IA", icon: <MessageSquareText size={22} color={colors.dark.text} />, route: "/(tabs)/more/chat" }

<!-- Auth context import (PATTERNS.md line 945) -->
import { useAuth } from "@/lib/auth-context";

<!-- Phase 6 design tokens: colors.dark.bg, colors.dark.surface, colors.dark.elevated, colors.dark.text, colors.dark.textMuted, colors.dark.border, colors.dark.primary -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend mobile API client with agent chat functions</name>
  <files>apps/mobile/lib/api.ts</files>
  <read_first>apps/mobile/lib/api.ts (full file for existing pattern), apps/mobile/lib/auth-client.ts (for fetchWithAuth), apps/mobile/lib/config.ts (for API_URL)</read_first>
  <action>
Edit apps/mobile/lib/api.ts: Add new API functions following the existing pattern (fetchWithAuth, typed responses, French error messages):

1. agentChat(message: string): Promise<{ response: string, sessionId: string }> — POST /api/ai/agent-chat. Body: { message }. Returns agent response. Synchronous (non-SSE) for mobile — SSE on React Native requires a polyfill, so use the synchronous endpoint as primary and add SSE as enhancement.

2. createAgentChatSSE(message: string, sessionId: string, onToken: (token: string) => void, onDone: () => void, onError: (err: Error) => void): AbortController — Basic SSE implementation for React Native. Since React Native doesn't have native EventSource, use fetch() with streaming response: `fetch(url, { headers: { Accept: 'text/event-stream' } })` and read the response body as a ReadableStream. Parse SSE event format (data: {...}\n\n). Call onToken for each token event, onDone for done event, onError for errors. Return AbortController for cleanup.

3. getAgentStatus(): Promise<{ ollamaAvailable: boolean, agentsCount: number }> — GET /api/ai-agent/status.

Add TypeScript types inline: MobileAgentChatResponse, MobileAgentStatus.

Error messages in French: "Échec de la communication avec l'assistant IA", "Service IA indisponible", etc. Follow the existing api.ts pattern with try/catch + throw Error(message).
  </action>
  <verify>
    <automated>grep -c "agentChat\|createAgentChatSSE\|getAgentStatus" apps/mobile/lib/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/lib/api.ts exports agentChat(), createAgentChatSSE(), getAgentStatus()
    - agentChat() uses fetchWithAuth POST /api/ai/agent-chat
    - createAgentChatSSE() uses fetch() with streaming response and SSE parsing
    - createAgentChatSSE() returns AbortController for connection teardown
    - Error messages in French
    - TypeScript compilation for apps/mobile passes
  </acceptance_criteria>
  <done>Mobile API client extended with agent chat (synchronous + SSE) and status functions</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create mobile chat screen and update More tab</name>
  <files>apps/mobile/app/(tabs)/more/chat.tsx, apps/mobile/app/(tabs)/more.tsx</files>
  <read_first>apps/mobile/app/(tabs)/more.tsx (full file ~126 lines), apps/mobile/lib/theme.ts (colors.dark tokens), apps/mobile/lib/auth-context.tsx (useAuth), apps/mobile/lib/api.ts (agentChat, createAgentChatSSE functions)</read_first>
  <action>
Create apps/mobile/app/(tabs)/more/chat.tsx: React Native screen following the more.tsx StyleSheet pattern and using colors.dark.* tokens from Phase 6.

Layout (per UI-SPEC Mobile Chat Interaction lines 376-385):
- Header bar: "Chat IA" title (Heading style: 20px, 600 weight, colors.dark.text). SSE status indicator dot (green=connected, amber=reconnecting, red=disconnected).
- Message list (flex: 1, ScrollView): ChatMessage bubbles. User messages: right-aligned, colors.dark.primary bg, white text. Agent messages: left-aligned, colors.dark.surface bg, colors.dark.text. Agent thinking indicator during SSE streaming (animated dots). Auto-scroll to bottom on new messages.
- Quick actions bar (middle): Horizontal ScrollView of action chips per UI-SPEC Copywriting: "Voir les alertes", "Caméras actives", "État des portes", "Signalement". Chip style: colors.dark.surface bg, 14px text, borderRadius 20, paddingHorizontal 16, paddingVertical 8. Tap chip → auto-fills and sends the query.
- Input area (bottom, fixed): TextInput (flex: 1) + send button (48x48 px touch target per UI-SPEC Exceptions, icon-only with aria-label="Envoyer le message") + voice mic button (56x56 px per UI-SPEC, icon-only with aria-label="Activer le microphone"). Voice button uses Mic/Voice icon from expo-vector-icons or lucide-react-native.

Voice input flow: Tap mic → recording state (animated waveform indicator, "Parlez maintenant..." text per UI-SPEC) → stop recording → transcribe via /api/v1/audio/transcribe (AI Preprocessor) → auto-fill input with transcribed text → user reviews and sends.

State management: useAuth() for user. useState for: messages array, inputText, isStreaming, isRecording, connectionStatus. useRef for ScrollView auto-scroll.

SSE connection: On mount, creates SSE connection via createAgentChatSSE() if online. On token: append to last agent message. On done: finalize. On error: show error state.

Offline detection: Use NetInfo or similar to detect connectivity. When offline: show "Hors ligne. Le chat nécessite une connexion." banner per UI-SPEC Copywriting. Grey out input. Queue actions cannot be processed.

Empty state per UI-SPEC: "Assistant de sécurité. Posez une question ou utilisez une action rapide."

Reconnection: When coming back online, show "Reconnecté. Rattrapage des messages..." banner, reconnect SSE.

Edit apps/mobile/app/(tabs)/more.tsx: Update the chat menu item route. Find the existing chat entry (or add if absent) and change route from `/chat` to `/(tabs)/more/chat`. Menu item: { id: "chat", label: "Chat IA", icon: <MessageSquareText size={22} color={colors.dark.text} />, route: "/(tabs)/more/chat" }. Follow the existing menu item pattern exactly.
  </action>
  <verify>
    <automated>grep -c "more/chat" apps/mobile/app/\(tabs\)/more.tsx && grep -c "Chat IA\|Assistant de sécurité" apps/mobile/app/\(tabs\)/more/chat.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/(tabs)/more/chat.tsx exists as React Native screen
    - Chat screen has: header with SSE status indicator, ScrollView message list, quick actions bar, input area with send and mic buttons
    - Send button: 48x48 px touch target, icon-only with aria-label
    - Mic button: 56x56 px touch target, icon-only with aria-label
    - Quick action chips: "Voir les alertes", "Caméras actives", "État des portes", "Signalement"
    - Voice input flow: mic → recording state → transcription → auto-fill → send
    - Offline state shows French banner per UI-SPEC
    - Empty state shows French greeting with quick action suggestion
    - apps/mobile/app/(tabs)/more.tsx routes chat to "/(tabs)/more/chat"
    - Uses colors.dark.* tokens and StyleSheet.create() pattern from Phase 6
    - All labels and messages in French
    - npm run check-types (from apps/mobile) exits 0
  </acceptance_criteria>
  <done>Mobile chat screen with SSE streaming, voice input, quick actions, and offline detection; More tab updated with chat route</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile app → SSE endpoint | Persistent streaming connection for agent responses |
| Mobile mic → Faster-Whisper | Audio recorded on device sent to AI Preprocessor for transcription |
| Mobile app → Offline queue | Queued actions pending network restoration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-29 | Information Disclosure | Voice transcription data in transit | mitigate | Audio sent to AI Preprocessor over HTTPS; audio data not persisted — processed in-memory only; transcription endpoint requires JWT auth |
| T-09-30 | Information Disclosure | Mobile chat data at rest | mitigate | expo-secure-store used for tokens; conversation history not persisted on device — fetched from server Redis per session |
| T-09-31 | Denial of Service | SSE reconnect storm | mitigate | Exponential backoff on reconnect (1s, 2s, 4s, 8s, max 30s); max 1 concurrent SSE connection per device |
</threat_model>

<verification>
1. Run `grep -c "more/chat" apps/mobile/app/\(tabs\)/more.tsx` — must be ≥ 1
2. Run `grep -c "Chat IA" apps/mobile/app/\(tabs\)/more/chat.tsx` — must be ≥ 1
3. Run `grep -c "agentChat\|createAgentChatSSE" apps/mobile/lib/api.ts` — must be ≥ 2
4. Run `grep -c "aria-label" apps/mobile/app/\(tabs\)/more/chat.tsx` — must be ≥ 2 (send + mic buttons)
5. Run `npm run check-types` from apps/mobile — must exit 0
</verification>

<success_criteria>
- Mobile chat screen with guard-first UX (large touch targets, dark mode, French labels)
- SSE streaming with reconnection and exponential backoff
- Voice input with Faster-Whisper transcription
- Quick action chips for one-tap common queries
- Offline detection with French error banner
- More tab updated with chat route
- Mobile API client extended with agent functions
- TypeScript compilation passes
</success_criteria>

<output>
Create `.planning/phases/09-ai-intelligence/09-08-SUMMARY.md` when done
</output>
