---
phase: 09-ai-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/src/config/configuration.ts
  - apps/api/src/config/validation.ts
  - .env.example
  - docker-compose.yml
  - docker-compose.prod.yml
  - services/ai-preprocessor/requirements.txt
  - apps/api/package.json
autonomous: false
requirements:
  - FTR-08
  - FTR-09
  - FTR-10
  - FTR-11

must_haves:
  truths:
    - "Agent traces are persisted to PostgreSQL with organization-scoped queries"
    - "Qdrant Docker service runs and responds to health checks"
    - "New AI config keys (QWEN_VL_MODEL, VLLM_URL, YOLO_MODEL, WHISPER_MODEL) are validated on startup"
    - "npm and pip packages install without errors"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "AgentTrace model"
      contains: "model AgentTrace"
    - path: "apps/api/src/config/configuration.ts"
      provides: "New AI config keys"
      contains: "qwenVlModel"
    - path: "docker-compose.yml"
      provides: "Qdrant Docker service"
      contains: "qdrant/qdrant"
  key_links:
    - from: "docker-compose.yml"
      to: "Qdrant container on port 6333"
      via: "expose directive"
      pattern: "qdrant:.*image: qdrant/qdrant"
    - from: "Prisma schema"
      to: "PostgreSQL database"
      via: "prisma db push"
      pattern: "model AgentTrace"
---

<objective>
[BLOCKING — Schema Push Required] Set up all infrastructure prerequisites for Phase 9: add AgentTrace model to Prisma schema, add new AI configuration keys (Qwen VL, vLLM, YOLO, Whisper, Qdrant), add Qdrant Docker service to both compose files, install npm packages (ollama, @modelcontextprotocol/sdk, @qdrant/js-client-rest), install Python packages (ultralytics, supervision, faster-whisper, tensorflow-hub, opencv-python, av, qdrant-client), and push the database schema.

Purpose: Every other plan depends on the schema, config, Docker service, and packages being in place.
Output: Migrated database with agent_traces table, Qdrant service running, config keys validated, packages installed.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-intelligence/09-RESEARCH.md (Standard Stack lines 36-109, Package Legitimacy Audit lines 94-109, Environment Availability lines 742-759)
@.planning/phases/09-ai-intelligence/09-PATTERNS.md (Group 15: Configuration Files lines 1047-1089, Group 16: Prisma Schema Extension lines 1093-1114, Group 17: Docker lines 1119-1158)

<interfaces>
<!-- Existing config keys from apps/api/src/config/configuration.ts (already in context) -->
Existing ai config: ollamaModel, embeddingModel, summaryModel at config key `ai`
Existing qdrantUrl already present
Existing Joi validation for AI_PREPROCESSOR_URL, OLLAMA_BASE_URL

<!-- Existing docker-compose.yml service pattern (already in context) -->
ai-preprocessor service uses: build, container_name, restart, extra_hosts, environment, healthcheck

<!-- Prisma model pattern from schema.prisma (already in context) -->
Existing models use @id @default(uuid()) @db.Uuid, @db.Timestamptz(), organizationId String
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add AgentTrace model + config keys + env vars</name>
  <files>apps/api/prisma/schema.prisma, apps/api/src/config/configuration.ts, apps/api/src/config/validation.ts, .env.example</files>
  <read_first>apps/api/prisma/schema.prisma, apps/api/src/config/configuration.ts, apps/api/src/config/validation.ts, .env.example</read_first>
  <action>
Add the AgentTrace model to apps/api/prisma/schema.prisma after the last model (before the closing of the schema) with fields: id (String @id @default(uuid()) @db.Uuid), time (DateTime @default(now()) @db.Timestamptz()), organizationId (String @db.Uuid), userId (String @db.Uuid), agentName (String), toolName (String), input (Json), output (Json?), durationMs (Int), success (Boolean), errorMessage (String?). Add @@index([organizationId, agentName]) and @@index([time]).

Add to configuration.ts under the `ai:` block: qwenVlModel (default 'qwen-vl'), qwenEmbeddingModel (default 'qwen-embedding'), llamaModel (default 'llama3.1'). Add top-level keys: vllmUrl (default 'http://localhost:8000'), qdrantUrl (already exists — verify key path is top-level, not nested). Ensure yoloModel and whisperModel keys exist — add if absent.

Add to validation.ts: VLLM_URL (Joi.string().default('http://localhost:8000')), QWEN_VL_MODEL (Joi.string().default('qwen-vl')), QWEN_EMBEDDING_MODEL (Joi.string().default('qwen-embedding')), LLAMA_MODEL (Joi.string().default('llama3.1')), YOLO_MODEL (Joi.string().default('yolov12n')), WHISPER_MODEL (Joi.string().default('medium')).

Add to .env.example after the QDRANT_URL block: # ── Serveur vLLM (prod) section with VLLM_URL=http://localhost:8000, # ── Modèles IA with QWEN_VL_MODEL=qwen-vl, QWEN_EMBEDDING_MODEL=qwen-embedding, LLAMA_MODEL=llama3.1, YOLO_MODEL=yolov12n, WHISPER_MODEL=medium.
  </action>
  <verify>
    <automated>grep -c "model AgentTrace" apps/api/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/prisma/schema.prisma contains "model AgentTrace" with fields: id, time, organizationId, userId, agentName, toolName, input, output, durationMs, success, errorMessage
    - apps/api/src/config/configuration.ts contains keys: qwenVlModel, qwenEmbeddingModel, llamaModel, vllmUrl, yoloModel, whisperModel
    - apps/api/src/config/validation.ts contains Joi validations for: VLLM_URL, QWEN_VL_MODEL, QWEN_EMBEDDING_MODEL, LLAMA_MODEL, YOLO_MODEL, WHISPER_MODEL
    - .env.example contains QWEN_VL_MODEL, QWEN_EMBEDDING_MODEL, LLAMA_MODEL, VLLM_URL, YOLO_MODEL, WHISPER_MODEL entries
    - npm run check-types (from apps/api) exits 0
  </acceptance_criteria>
  <done>AgentTrace model exists in Prisma schema; config keys validated by Joi; env vars documented in .env.example</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install npm + pip packages and add Qdrant Docker service</name>
  <files>apps/api/package.json, services/ai-preprocessor/requirements.txt, docker-compose.yml, docker-compose.prod.yml</files>
  <read_first>apps/api/package.json, services/ai-preprocessor/requirements.txt, docker-compose.yml, docker-compose.prod.yml</read_first>
  <action>
Run: pnpm add ollama @modelcontextprotocol/sdk @qdrant/js-client-rest --filter=api (in project root). This adds ollama (0.6.3+), @modelcontextprotocol/sdk (1.29.0+), and @qdrant/js-client-rest (1.18.0+) to apps/api/package.json.

Add to services/ai-preprocessor/requirements.txt: ultralytics>=8.4, supervision>=0.25, faster-whisper>=1.2, tensorflow-hub>=0.16, opencv-python>=5.0, av>=18.0, qdrant-client>=1.18. Run: pip install -r services/ai-preprocessor/requirements.txt (inside the ai-preprocessor directory or Docker build context).

Add Qdrant service to docker-compose.yml after the mosquitto service block, following the existing ai-preprocessor service pattern: image: qdrant/qdrant:latest, container_name: oversight-qdrant, restart: unless-stopped, expose ports 6333 and 6334, volume qdrant_data:/qdrant/storage, healthcheck with wget http://127.0.0.1:6333/healthz. Add qdrant_data to the volumes section. Place on the same networks as ai-preprocessor.

Add the same Qdrant service to docker-compose.prod.yml after the ai-preprocessor service block, with the same configuration plus network membership in both backend and frontend networks. Add qdrant_data to prod volumes.
  </action>
  <verify>
    <automated>grep -c "ollama" apps/api/package.json && grep -c "@modelcontextprotocol/sdk" apps/api/package.json && grep -c "@qdrant/js-client-rest" apps/api/package.json</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/package.json contains ollama, @modelcontextprotocol/sdk, @qdrant/js-client-rest in dependencies
    - services/ai-preprocessor/requirements.txt contains ultralytics, supervision, faster-whisper, tensorflow-hub, opencv-python, av, qdrant-client
    - docker-compose.yml contains qdrant service with image qdrant/qdrant:latest, ports 6333 and 6334, healthcheck
    - docker-compose.prod.yml contains qdrant service matching the same pattern
    - docker compose config (from project root) validates without errors
  </acceptance_criteria>
  <done>npm packages installed for API; Python deps listed for AI Preprocessor; Qdrant service defined in both compose files</done>
</task>

<task type="auto" tdd="false">
  <name>[BLOCKING] Task 3: Run Prisma DB Push to create agent_traces table</name>
  <files>apps/api/prisma/schema.prisma (depends on Task 1 modification)</files>
  <read_first>apps/api/prisma/schema.prisma</read_first>
  <action>
Run: npx prisma db push from apps/api directory. This creates the agent_traces table in the connected PostgreSQL database WITHOUT generating a migration file (db push is appropriate for development/review — a formal migration can be generated via prisma migrate dev later if needed). Use --accept-data-loss only if required. Then run: npx prisma generate to regenerate the Prisma client with the AgentTrace model included.
  </action>
  <verify>
    <automated>npx prisma db push --print 2>&1 | grep -c "agent_traces"</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` exits 0 with no errors
    - `npx prisma generate` exits 0
    - PostgreSQL database contains agent_traces table (verify with: `npx prisma db execute --stdin <<< "SELECT count(*) FROM information_schema.tables WHERE table_name = 'agent_traces';"` — but skip if interactive fails; fallback to checking prisma db push output)
    - `grep -c "AgentTrace" node_modules/.prisma/client/index.d.ts` > 0 (Prisma client regenerated)
  </acceptance_criteria>
  <done>agent_traces table exists in PostgreSQL; Prisma client regenerated with AgentTrace model; DB schema synced</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qdrant Docker service → host network | Qdrant exposes REST API on port 6333 and gRPC on 6334 |
| npm registry → developer machine | npm packages downloaded during install |
| PyPI → developer machine/Docker build | Python packages downloaded during pip install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | Qdrant Docker service | mitigate | Qdrant exposed only on Docker internal networks (backend/frontend), not published to host ports — no external access |
| T-09-02 | Information Disclosure | Qdrant unauthenticated API | mitigate | Qdrant runs on internal Docker network only; no public port mapping; API key will be configured later if Qdrant supports it |
| T-09-SC | Tampering | npm install (ollama, @modelcontextprotocol/sdk, @qdrant/js-client-rest) | mitigate | Packages verified via slopcheck in RESEARCH.md Package Legitimacy Audit — all three approved [OK]; no postinstall scripts |
| T-09-SC-2 | Tampering | pip install (ultralytics, supervision, faster-whisper, etc.) | mitigate | Packages verified via PyPI registry check in RESEARCH.md — all approved; byteTrack flagged and replaced with supervision |
</threat_model>

<verification>
1. Run `npx prisma db push` from apps/api — must succeed
2. Run `grep -c "model AgentTrace" apps/api/prisma/schema.prisma` — must be 1
3. Run `grep -c "qwenVlModel" apps/api/src/config/configuration.ts` — must be 1
4. Run `grep -c "VLLM_URL" apps/api/src/config/validation.ts` — must be 1
5. Run `grep -c "qdrant:" docker-compose.yml` — must be ≥ 1
6. Run `grep -c "ollama" apps/api/package.json` — must be ≥ 1
</verification>

<success_criteria>
- Prisma schema extended with AgentTrace model
- Database pushed with agent_traces table
- Config keys added and validated
- Qdrant Docker service defined
- npm and pip packages installed/declared
- TypeScript compilation passes
</success_criteria>

<output>
Create `.planning/phases/09-ai-intelligence/09-01-SUMMARY.md` when done
</output>
