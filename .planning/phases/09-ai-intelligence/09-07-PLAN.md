---
phase: 09-ai-intelligence
plan: 07
type: execute
wave: 4
depends_on: ["09-06"]
files_modified:
  - apps/dashboard/app/(dashboard)/command-center/page.tsx
  - apps/dashboard/app/(dashboard)/patterns/page.tsx
  - apps/dashboard/app/(dashboard)/risque/page.tsx
  - apps/dashboard/components/chat-panel.tsx
  - apps/dashboard/components/chat-message.tsx
  - apps/dashboard/components/streaming-text.tsx
  - apps/dashboard/components/risk-gauge.tsx
  - apps/dashboard/components/agent-status-bar.tsx
  - apps/dashboard/components/camera-grid.tsx
  - apps/dashboard/components/pattern-card.tsx
  - apps/dashboard/components/pattern-trend-detail.tsx
  - apps/dashboard/components/risk-explanation-panel.tsx
  - apps/dashboard/components/quick-actions.tsx
  - apps/dashboard/components/confirmation-dialog.tsx
  - apps/dashboard/components/agent-thinking-indicator.tsx
  - apps/dashboard/components/sse-status-banner.tsx
  - apps/dashboard/components/proactive-notification.tsx
autonomous: true
requirements:
  - FTR-08
  - FTR-10
  - FTR-11

must_haves:
  truths:
    - "Operator opens Command Center at /command-center and sees a 3-panel layout with chat, camera grid, and agent status"
    - "Agent responses stream token-by-token via SSE with blinking cursor"
    - "Risk page shows 'Expliquer ce score' button that opens AI explanation panel"
    - "Patterns page shows 8 pattern cards with sparkline trends and severity badges"
    - "Destructive actions require explicit confirmation via ConfirmationDialog"
    - "Proactive notifications appear as agent messages and toast alerts"
  artifacts:
    - path: "apps/dashboard/app/(dashboard)/command-center/page.tsx"
      provides: "Command Center page with 3-panel layout"
      contains: "command-center"
    - path: "apps/dashboard/components/chat-panel.tsx"
      provides: "SSE-streaming chat interface"
      contains: "EventSource"
    - path: "apps/dashboard/components/risk-gauge.tsx"
      provides: "4-color semi-circular risk gauge with needle"
      contains: "RiskGauge"
  key_links:
    - from: "chat-panel.tsx"
      to: "createAgentChatStream() from lib/api.ts"
      via: "EventSource SSE connection"
      pattern: "createAgentChatStream"
    - from: "risque/page.tsx"
      to: "explainRisk() from lib/api.ts"
      via: "'Expliquer ce score' button"
      pattern: "explainRisk"
    - from: "command-center/page.tsx"
      to: "glass-card.tsx, metric-hero.tsx, sparkline.tsx"
      via: "Phase 6 design system components"
      pattern: "GlassCard"
---

<objective>
Build the entire dashboard frontend for Phase 9 AI Intelligence: the Command Center page (/command-center) with 3-panel chat+camera+risk layout and SSE streaming, the Patterns page (/patterns) with 8 pattern cards and trend visualization, the Risk page enhancement (/risque) with "Expliquer ce score" AI explanation panel, and 15 new React components following Phase 6 design system patterns (GlassCard, MetricHero, Sparkline, DonutChart) and UI-SPEC.md visual contract.

Purpose: Per D-20 (Command Center), D-21 (Risk enhancement), D-22 (Patterns page), D-24 (proactive assistant), D-30 (action confirmation UI). The dashboard is the primary interface for the agentic AI system.

Output: 3 new/modified pages, 15 new components, all using Phase 6 design system and UI-SPEC visual contract.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-intelligence/09-UI-SPEC.md (Design System lines 19-30, Spacing lines 36-57, Typography lines 60-73, Color lines 77-103, Visual Hierarchy lines 110-139, Copywriting lines 143-209, Navigation lines 284-307, Interaction Patterns lines 322-385, State Inventory lines 389-403, Component Inventory lines 223-280)
@.planning/phases/09-ai-intelligence/09-PATTERNS.md (Group 11: Dashboard Page lines 749-862, Group 14: API Client lines 1018-1039 for SSE pattern)
@.planning/phases/09-ai-intelligence/09-CONTEXT.md (D-20 Command Center, D-21 Risk, D-22 Patterns, D-24 proactive, D-30 action guardrails, D-33 fallback messages)
@apps/dashboard/app/(dashboard)/ia/page.tsx (existing AI page — reference for page structure pattern)
@apps/dashboard/app/(dashboard)/risque/page.tsx (existing risk page — reference for charts)
@apps/dashboard/lib/api.ts (updated in Plan 06 — agent functions available)

<interfaces>
<!-- Existing page pattern from ia/page.tsx and risque/page.tsx (PATTERNS.md lines 749-862) -->
"use client"; import pattern; export default function PageName(); useState/useEffect; useTranslation(); useAuth()

<!-- SSE pattern from PATTERNS.md lines 845-857 -->
const eventSource = new EventSource(url); eventSource.onmessage = (event) => {...}; eventSource.onerror

<!-- Phase 6 component imports: GlassCard, MetricHero, Sparkline, DonutChart, QuickActionBar, PageHeader, PageTransition -->

<!-- shadcn/ui primitives: Button, Card, Input, Badge, Skeleton, ScrollArea, Dialog, Toast, Tooltip, Progress, Separator, Avatar -->

<!-- Design tokens from UI-SPEC: --shadcn-primary (190 90% 50%), --shadcn-card (228 24% 6%), .glass/.glass-premium/.glass-accent classes -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create all 15 new UI components</name>
  <files>apps/dashboard/components/chat-panel.tsx, apps/dashboard/components/chat-message.tsx, apps/dashboard/components/streaming-text.tsx, apps/dashboard/components/risk-gauge.tsx, apps/dashboard/components/agent-status-bar.tsx, apps/dashboard/components/camera-grid.tsx, apps/dashboard/components/pattern-card.tsx, apps/dashboard/components/pattern-trend-detail.tsx, apps/dashboard/components/risk-explanation-panel.tsx, apps/dashboard/components/quick-actions.tsx, apps/dashboard/components/confirmation-dialog.tsx, apps/dashboard/components/agent-thinking-indicator.tsx, apps/dashboard/components/sse-status-banner.tsx, apps/dashboard/components/proactive-notification.tsx</files>
  <read_first>apps/dashboard/components/glass-card.tsx, apps/dashboard/components/metric-hero.tsx, apps/dashboard/components/sparkline.tsx, apps/dashboard/components/ui/button.tsx, apps/dashboard/components/ui/card.tsx, apps/dashboard/components/ui/dialog.tsx, apps/dashboard/components/ui/progress.tsx, apps/dashboard/components/ui/scroll-area.tsx</read_first>
  <action>
Create 15 new components under apps/dashboard/components/, each following UI-SPEC.md visual contract and Phase 6 patterns. Use "use client" directive for interactive components.

**Chat components:**
- ChatPanel.tsx: SSE-streaming chat interface. Props: { sessionId, onActionConfirm }. Uses createAgentChatStream() from lib/api.ts. Renders: ScrollArea with ChatMessage list, AgentThinkingIndicator during streaming, input area with Send button (icon-only, aria-label="Envoyer le message"), suggested query chips on empty state per UI-SPEC Copywriting Contract. SSE events mapped to message states. Uses .glass-accent for agent messages.

- ChatMessage.tsx: Single message bubble. Props: { role, content, toolCalls?, timestamp, sources? }. User messages: right-aligned, primary bg. Agent messages: left-aligned, .glass-accent bg with primary/4% + primary/12% border. Tool call results: collapsible card within agent message. Tool call args: monospace (JetBrains Mono), truncated at 500 chars.

- StreamingText.tsx: Token-by-token text renderer. Props: { tokens: string[], isStreaming: boolean }. Accumulates tokens with 50ms stagger per UI-SPEC. Blinking cursor animation when streaming (primary color, pulse animation). When done, cursor disappears.

- AgentThinkingIndicator.tsx: Animated dots. Props: { agentName, status: 'thinking'|'responding' }. Shows agent name + animated ellipsis. Warning color (amber) for thinking, primary (cyan) for responding per UI-SPEC color allocation.

- SSEStatusBanner.tsx: Connection status banner. Props: { status: 'connected'|'reconnecting'|'disconnected' }. Shows "Connexion perdue. Reconnexion en cours..." during reconnection per UI-SPEC Copywriting. Amber banner. Retry button when disconnected.

- ProactiveNotification.tsx: Auto-posted agent message. Props: { eventType, zone, timestamp, severity }. Renders as a special agent message with amber/red badge. Uses Badge component. Content per UI-SPEC: "{event_type} détecté sur {zone}".

**Command Center components:**
- AgentStatusBar.tsx: Left panel — per-agent status. Props: { agents: AgentStatus[] }. Renders agent list with status indicators: idle (muted circle), thinking (amber pulse), responding (cyan pulse), error (red). Shows agent count. Uses GlassCard for container.

- CameraGrid.tsx: Right panel — responsive camera thumbnail grid. Props: { cameras: CameraThumbnail[], onCameraClick }. 2-column grid of camera thumbnails with status overlays (green dot = online, red = offline). Click opens full camera view in dialog/modal. Uses GlassCard for container. Placeholder thumbnails if no RTSP stream available (gray gradient with camera icon).

- QuickActions.tsx: Contextual action chips. Props: { actions: Array<{ label, icon?, onClick }> }. Renders tappable chip buttons below chat input. Per UI-SPEC Copywriting: "Voir les alertes", "Caméras actives", "État des portes", "Signalement".

**Risk & Patterns components:**
- RiskGauge.tsx: Semi-circular gauge with 4-color scale. Props: { score: number (0-100), zoneName, previousScore? }. Renders SVG semi-circle (270° arc) with: 0-33 green (--shadcn-success), 34-66 amber (--shadcn-warning), 67-84 cyan (--shadcn-primary), 85-100 red (--shadcn-destructive) per UI-SPEC. Needle at current score. Score label in JetBrains Mono font-semibold 600 weight. Trend arrow (↑/↓) if previousScore provided.

- RiskExplanationPanel.tsx: Slide-out AI explanation. Props: { zoneId, isOpen, onClose }. SSE-streaming risk explanation from RiskAnalysisAgent. Same streaming pattern as chat. Shows: color-coded gauge (RiskGauge), streaming explanation text, list of contributing events. Slide-in animation from bottom.

- PatternCard.tsx: Individual pattern display. Props: { pattern: PatternDetail } where PatternDetail has name, severity, sparklineData, occurrenceCount, trendPercent. Renders GlassCard with: pattern name (Heading, 20px, 600 weight), Sparkline component (7-day trend), MetricHero for occurrence count, Badge for severity (Info/Avertissement/Critique per UI-SPEC), trend arrow with "↑/↓ {n}% cette semaine", "Voir les détails" button.

- PatternTrendDetail.tsx: Expanded pattern view. Props: { pattern: PatternDetail, isOpen, onClose }. Renders Dialog or slide-out with: full Sparkline (30-day), occurrence table (using Table component), severity badge, AI analysis section (calls analyzePattern from lib/api.ts), "Expliquer" integration.

**Security components:**
- ConfirmationDialog.tsx: Destructive action confirmation. Props: { isOpen, title, description, confirmLabel, onConfirm, onCancel, requiredRole }. Renders Dialog with: AlertTriangle icon (amber), title (e.g., "Verrouiller la zone Zone B ?"), description per UI-SPEC Copywriting Contract (D-30), Confirm button (destructive variant), Cancel button (outline variant). If user role < requiredRole: show "Action non autorisée pour votre rôle." instead of confirm button per UI-SPEC Interaction Patterns.

All components: use motion for animations (AnimatePresence for mount/unmount), Tailwind classes with cn() utility, dark-first theme (dark class), French labels per UI-SPEC Copywriting Contract. Icons from lucide-react. Typography: IBM Plex Sans body, JetBrains Mono numeric per UI-SPEC.
  </action>
  <verify>
    <automated>ls apps/dashboard/components/chat-panel.tsx apps/dashboard/components/risk-gauge.tsx apps/dashboard/components/agent-status-bar.tsx apps/dashboard/components/pattern-card.tsx apps/dashboard/components/confirmation-dialog.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 15 new component files exist in apps/dashboard/components/ — all listed in the files section must exist
    - ChatPanel uses createAgentChatStream() from lib/api.ts with SSE EventSource pattern
    - StreamingText has blinking cursor animation with primary color
    - RiskGauge renders SVG semi-circle with 4-color scale per UI-SPEC
    - AgentStatusBar shows per-agent status with colored pulse indicators
    - PatternCard uses GlassCard + Sparkline + MetricHero components
    - ConfirmationDialog has destructive variant confirm + cancel buttons
    - All component labels and messages in French per UI-SPEC Copywriting Contract
    - All icon-only buttons have aria-label per UI-SPEC Icon-Only Accessibility table
    - GlassCard, MetricHero, Sparkline are imported from Phase 6 components
    - Typography: IBM Plex Sans for body, JetBrains Mono for numeric values
    - npm run build (from apps/dashboard) exits 0 (or npm run check-types at minimum)
  </acceptance_criteria>
  <done>15 new components created following UI-SPEC visual contract and Phase 6 design system patterns</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Command Center page at /command-center</name>
  <files>apps/dashboard/app/(dashboard)/command-center/page.tsx</files>
  <read_first>apps/dashboard/app/(dashboard)/ia/page.tsx (full file ~591 lines for page structure), apps/dashboard/lib/api.ts (agentChat, createAgentChatStream, getAgentStatus functions), apps/dashboard/lib/nav-config.ts (for route validation), apps/dashboard/components/chat-panel.tsx, apps/dashboard/components/agent-status-bar.tsx, apps/dashboard/components/camera-grid.tsx, apps/dashboard/components/risk-gauge.tsx, apps/dashboard/components/proactive-notification.tsx</read_first>
  <action>
Create apps/dashboard/app/(dashboard)/command-center/page.tsx: "use client" Next.js page following ia/page.tsx structure.

Layout: 3-panel responsive layout per UI-SPEC and D-20:
- Left panel (20% width): AgentStatusBar component showing 6 agents. RiskGauge widget (overall organization risk score from RiskService via API). Model status indicator ("Modèles connectés" / "Modèles dégradés" per UI-SPEC Copywriting).
- Center panel (50%): ChatPanel component as the primary focal point per UI-SPEC Visual Hierarchy. SSE streaming chat with the orchestrator. Empty state per UI-SPEC: "Agent de sécurité activé. Posez une question..." with suggested query chips.
- Right panel (30%): CameraGrid component. DonutChart (Phase 6) for alert severity distribution. ActivityTimeline (Phase 6) for recent agent actions.

State management: useAuth() for user context. useState for sessionId (created on mount), agentStatuses, modelHealth. useEffect on mount: call getAgentStatus() to populate agent statuses. ProactiveNotification messages from SSE tool_call events with severity detection.

SSE connection: Uses createAgentChatStream() from lib/api.ts. On token: update chat messages. On tool_call: show collapsible card. On done: finalize message. On error: show error state per UI-SPEC Copywriting. Reconnection with exponential backoff per UI-SPEC Interaction Patterns.

Destructive actions: When agent proposes destructive action (lockdown, door control), render ConfirmationDialog inside the chat. On confirm: execute action, show result, create audit log entry. On cancel: show "Action annulée".

Styling: Use GlassCard for panels. PageHeader for title "Centre de commande". PageTransition for route animation. Use existing dashboard layout wrappers. Dark-first. Responsive: on mobile (< 768px), stack panels vertically with chat on top.

Copyright footer: "L'IA peut produire des informations incorrectes. Vérifiez les décisions critiques." per UI-SPEC Shared UI Elements.
  </action>
  <verify>
    <automated>grep -c "command-center\|CommandCenter\|Centre de commande" apps/dashboard/app/\(dashboard\)/command-center/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/dashboard/app/(dashboard)/command-center/page.tsx exists as "use client" page component
    - 3-panel layout: AgentStatusBar (left), ChatPanel (center), CameraGrid (right)
    - Uses createAgentChatStream() SSE connection from lib/api.ts
    - Empty state shows French greeting with suggested query chips per UI-SPEC Copywriting
    - Destructive actions render ConfirmationDialog per D-30
    - Uses Phase 6 components: GlassCard, MetricHero, Sparkline, DonutChart, PageHeader, PageTransition
    - Responsive: stacks vertically on mobile
    - Disclaimer footer shown: "L'IA peut produire des informations incorrectes..."
    - All labels in French
    - npm run build (from apps/dashboard) exits 0
  </acceptance_criteria>
  <done>Command Center page at /command-center with 3-panel chat+camera+risk layout and SSE streaming</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create Patterns page and enhance Risk page</name>
  <files>apps/dashboard/app/(dashboard)/patterns/page.tsx, apps/dashboard/app/(dashboard)/risque/page.tsx</files>
  <read_first>apps/dashboard/app/(dashboard)/risque/page.tsx (full file ~496 lines), apps/dashboard/app/(dashboard)/maintenance/page.tsx (lines 1-50 for table pattern), apps/dashboard/lib/api.ts (explainRisk, analyzePattern functions), apps/dashboard/components/pattern-card.tsx, apps/dashboard/components/pattern-trend-detail.tsx, apps/dashboard/components/risk-explanation-panel.tsx, apps/dashboard/components/risk-gauge.tsx</read_first>
  <action>
Create apps/dashboard/app/(dashboard)/patterns/page.tsx: "use client" Next.js page per D-22. Fetch patterns from API (GET /api/patterns — use existing patterns functions from api.ts, or the analyzePattern for detail).

Layout: PageHeader "Motifs". Responsive grid (2 columns desktop, 1 mobile) of 8 PatternCard components. Each card shows: pattern name (French per UI-SPEC Copywriting — "Porte forcée", "Porte maintenue ouverte", "Panne de lecteur", "Chute FPS caméra", "Accès refusé répété", "Fausses alertes caméra", "Anomalie d'horaire", "Déplacement impossible"), Sparkline (7-day trend), occurrence count via MetricHero, severity Badge, "Voir les détails" button.

Empty state per UI-SPEC: "Aucun motif récurrent détecté. Le système analyse vos événements en continu. Les motifs apparaîtront ici dès qu'ils seront identifiés."

Click "Voir les détails" → expands PatternTrendDetail inline or opens Dialog. Detail view: full Sparkline (30-day), occurrence table (paginated via Table component), "Expliquer" integration (calls analyzePattern API).

Loading state: Skeleton cards (use existing LoadingSkeleton pattern from risque/page.tsx lines 57-82). Error state: "Les motifs récurrents sont temporairement indisponibles. Réessayer." with retry button per UI-SPEC Copywriting.

Restrict to SUPERVISOR+ per D-22 (nav config handles routing, but page should also check role).

Edit apps/dashboard/app/(dashboard)/risque/page.tsx: Enhance existing risk page per D-21. Add "Expliquer ce score" button (accent/primary variant) next to each zone's risk score. On click: opens RiskExplanationPanel (slide-out below or inline). Panel shows: streaming AI explanation via explainRisk(zoneId), color-coded RiskGauge component re-rendering, list of contributing events.

Preserve existing risk page features: bar chart, score list, auto-refresh (every 60s), zone detail drill-down. The "Expliquer" button is additive — does not replace existing functionality.

Loading state for explanation: "Analyse du score de la zone {zone}..." per UI-SPEC. Error: "Impossible d'expliquer ce score pour le moment. Réessayer." Empty: "Aucun événement récent n'explique ce score. Il reflète l'état de base de la zone."

Both pages: use Phase 6 design system components. Dark-first theme. French labels. Responsive layouts.
  </action>
  <verify>
    <automated>grep -c "patterns\|Motifs\|PatternCard" apps/dashboard/app/\(dashboard\)/patterns/page.tsx && grep -c "Expliquer\|explainRisk\|RiskExplanationPanel" apps/dashboard/app/\(dashboard\)/risque/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/dashboard/app/(dashboard)/patterns/page.tsx exists with 8 PatternCard grid layout
    - Patterns page has empty state per UI-SPEC Copywriting
    - Pattern names match UI-SPEC: "Porte forcée", "Porte maintenue ouverte", "Panne de lecteur", "Chute FPS caméra", "Accès refusé répété", "Fausses alertes caméra", "Anomalie d'horaire", "Déplacement impossible"
    - Pattern detail uses PatternTrendDetail with Sparkline + occurrence table
    - apps/dashboard/app/(dashboard)/risque/page.tsx has "Expliquer ce score" buttons on each zone
    - RiskExplanationPanel slides open with streaming AI explanation
    - Risk page existing functionality preserved (charts, auto-refresh, drill-down)
    - Both pages use GlassCard, MetricHero, Sparkline, PageHeader from Phase 6
    - All labels and messages in French
    - npm run build (from apps/dashboard) exits 0
  </acceptance_criteria>
  <done>Patterns page with 8 pattern cards and trend visualization; Risk page enhanced with AI "Expliquer" button and explanation panel</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard → SSE endpoint | Persistent connection for streaming agent responses |
| Dashboard → API (destructive actions) | Confirmation-gated actions requiring user approval |
| User browser → Dashboard | Client-side rendering of sensitive security data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-25 | Information Disclosure | SSE connection data in transit | mitigate | SSE endpoint over HTTPS (Caddy reverse proxy with TLS); JWT auth token validated on connection |
| T-09-26 | Spoofing | Client-side confirmation bypass | mitigate | ConfirmationDialog sends token to server for validation — ActionConfirmationGuard (Plan 05) validates server-side; client-side UI is convenience layer, not security boundary |
| T-09-27 | Elevation of Privilege | Patterns page role bypass | mitigate | Nav config restricts to SUPERVISOR+; page component also checks user role via useAuth(); API endpoint also RBAC-guarded |
| T-09-28 | Information Disclosure | Sensitive risk data in browser | mitigate | Page data already tenant-scoped via API JWT; client renders only authorized org data; no data leakage possible without API bypass |
</threat_model>

<verification>
1. Run `ls apps/dashboard/components/{chat-panel,risk-gauge,agent-status-bar,pattern-card,confirmation-dialog}.tsx | wc -l` — must be 5
2. Run `grep -c "command-center" apps/dashboard/app/\(dashboard\)/command-center/page.tsx` — must be ≥ 1
3. Run `grep -c "PatternCard\|patterns" apps/dashboard/app/\(dashboard\)/patterns/page.tsx` — must be ≥ 1
4. Run `grep -c "Expliquer\|explainRisk" apps/dashboard/app/\(dashboard\)/risque/page.tsx` — must be ≥ 1
5. Run `grep -c "createAgentChatStream" apps/dashboard/app/\(dashboard\)/command-center/page.tsx` — must be ≥ 1
6. Run `npm run build` from apps/dashboard — must exit 0
</verification>

<success_criteria>
- Command Center page with 3-panel SSE-streaming chat layout
- 15 new components following UI-SPEC visual contract
- Patterns page with 8 pattern cards and trend visualization
- Risk page with "Expliquer" AI explanation panel
- All components use Phase 6 design system (GlassCard, MetricHero, Sparkline)
- All UI text in French per Copywriting Contract
- Dark-first theme, responsive layouts
- Destructive action confirmation UI per D-30
- Build passes with no errors
</success_criteria>

<output>
Create `.planning/phases/09-ai-intelligence/09-07-SUMMARY.md` when done
</output>
