---
phase: 09-ai-intelligence
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - services/ai-preprocessor/app/routes/detection.py
  - services/ai-preprocessor/app/routes/audio.py
  - services/ai-preprocessor/app/models/detector.py
  - services/ai-preprocessor/app/models/tracker.py
  - services/ai-preprocessor/app/models/audio_classifier.py
  - services/ai-preprocessor/app/models/transcriber.py
  - services/ai-preprocessor/app/main.py
  - services/ai-preprocessor/app/config.py
  - services/ai-preprocessor/app/routes/inference.py
autonomous: true
requirements:
  - FTR-08
  - FTR-11

must_haves:
  truths:
    - "YOLOv12 detection fires on RTSP streams and sends detection events to NestJS API"
    - "ByteTrack maintains cross-frame object identity across video frames"
    - "YAMNet detects glass breaks, shouts, and alarms from camera audio streams"
    - "Faster-Whisper transcribes guard voice messages to text"
  artifacts:
    - path: "services/ai-preprocessor/app/routes/detection.py"
      provides: "POST /api/v1/detect endpoint for YOLOv12 object detection"
      exports: ["detection_router"]
    - path: "services/ai-preprocessor/app/models/detector.py"
      provides: "Lazy-loaded YOLOv12 model with ultralytics"
      contains: "YOLO("
    - path: "services/ai-preprocessor/app/routes/audio.py"
      provides: "POST /api/v1/audio/classify and POST /api/v1/audio/transcribe"
      exports: ["audio_router"]
  key_links:
    - from: "detection.py router"
      to: "NestJS API event bus"
      via: "httpx POST to API endpoint"
      pattern: "httpx.AsyncClient.*api/v1"
    - from: "YOLOv12 detector"
      to: "ByteTrack tracker"
      via: "supervision.Detections.from_ultralytics + tracker.update_with_detections"
      pattern: "from_ultralytics"
---

<objective>
Extend the Python AI Preprocessor with real-time computer vision (YOLOv12 object detection + ByteTrack cross-frame tracking) and audio analysis (YAMNet event detection + Faster-Whisper transcription). Add new FastAPI routes for detection and audio processing, create lazy-loaded model classes, and register routes in main.py. Replace the existing VLM inference pipeline (routes/inference.py) with a two-tier approach: YOLOv12 fast detection first, then Qwen VL deep analysis only when detection fires.

Purpose: Per D-14 and D-19, the AI Preprocessor must support real-time object detection and audio analysis. This is independent of the NestJS agent work and can run in parallel.

Output: 2 new FastAPI routes (detection, audio), 4 model classes, updated main.py and config, modified inference pipeline.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-intelligence/09-RESEARCH.md (Pattern: YOLOv12 + ByteTrack Detection lines 650-685, Standard Stack Python lines 55-64, Don't Hand-Roll lines 443-455, Pitfall 2: ByteTrack Package Confusion lines 487-496, Code Examples lines 650-685)
@.planning/phases/09-ai-intelligence/09-PATTERNS.md (Group 10: FastAPI Python Route lines 655-743)
@.planning/phases/09-ai-intelligence/09-CONTEXT.md (D-13 to D-19, D-15 Qwen VL replacement)

<interfaces>
<!-- Existing FastAPI route pattern from services/ai-preprocessor/app/routes/anpr.py (referenced in PATTERNS.md lines 657-713) -->
Pattern: APIRouter(), Pydantic BaseModel for request/response, async def handler, lazy-load global model

<!-- Existing lazy model loading pattern from anpr.py (PATTERNS.md lines 702-713) -->
_ocr = None; def get_ocr(): global _ocr; if _ocr is None: ... return _ocr

<!-- Existing main.py router registration pattern (PATTERNS.md lines 731-735) -->
app.include_router(health.router); app.include_router(inference.router, prefix=settings.API_V1_PREFIX)

<!-- Existing config.py pattern (PATTERNS.md line 100) -->
Pydantic BaseSettings with OLLAMA_BASE_URL
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create YOLOv12 detector and ByteTrack tracker models</name>
  <files>services/ai-preprocessor/app/models/detector.py, services/ai-preprocessor/app/models/tracker.py</files>
  <read_first>services/ai-preprocessor/app/routes/anpr.py (lines 1-24 for lazy-load pattern), services/ai-preprocessor/app/config.py</read_first>
  <action>
Create services/ai-preprocessor/app/models/detector.py: Implement lazy-loaded YOLOv12 model using the get_model pattern from anpr.py's get_ocr(). Use `from ultralytics import YOLO`. Lazy init: `_yolo_model = None; def get_detector(model_name: str = "yolov12n"): global _yolo_model; if _yolo_model is None: logger.info(f"Loading YOLO model: {model_name}"); _yolo_model = YOLO(model_name); return _yolo_model`. Export a `detect(frame, confidence=0.45)` function that calls model(frame, conf=confidence)[0] and returns supervision.Detections.from_ultralytics(results). Default confidence 0.45 per D-(agent discretion). The model name is configurable via config.DETECTION_MODEL (default yolov12n per RESEARCH.md).

Create services/ai-preprocessor/app/models/tracker.py: Implement ByteTrack via `supervision` library (NOT the `bytetrack` PyPI package — per Pitfall 2 in RESEARCH.md). Use `from supervision import ByteTrack`. Lazy init: `_tracker = None; def get_tracker(): global _tracker; if _tracker is None: _tracker = ByteTrack(); return _tracker`. Export `track(detections: sv.Detections)` that calls `tracker.update_with_detections(detections)` and returns tracked detections with tracker_id. Use supervision defaults for ByteTrack parameters (max_age=30, min_hits=3 per agent discretion per D-(agent discretion)). 
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'services/ai-preprocessor/app'); from models.detector import get_detector; print('detector imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - services/ai-preprocessor/app/models/detector.py exists with get_detector() and detect() functions using ultralytics.YOLO
    - services/ai-preprocessor/app/models/tracker.py exists with get_tracker() and track() functions using supervision.ByteTrack
    - Both use lazy-load pattern matching get_ocr() from app/routes/anpr.py (global + None check + init + return)
    - `from ultralytics import YOLO` imports successfully
    - `from supervision import ByteTrack` imports successfully (NOT from bytetrack)
    - No imports from `bytetrack` PyPI package anywhere in the codebase
  </acceptance_criteria>
  <done>YOLOv12 detector and ByteTrack tracker models created with lazy-load pattern; using ultralytics and supervision packages</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create YAMNet audio classifier and Faster-Whisper transcriber models</name>
  <files>services/ai-preprocessor/app/models/audio_classifier.py, services/ai-preprocessor/app/models/transcriber.py</files>
  <read_first>services/ai-preprocessor/app/routes/anpr.py (lines 14-24 for lazy-load pattern), services/ai-preprocessor/app/config.py</read_first>
  <action>
Create services/ai-preprocessor/app/models/audio_classifier.py: Implement YAMNet via tensorflow-hub. Lazy init: `_yamnet_model = None; def get_yamnet(): global _yamnet_model; if _yamnet_model is None: import tensorflow_hub as hub; _yamnet_model = hub.load('https://tfhub.dev/google/yamnet/1'); return _yamnet_model`. Export `classify_audio(audio_samples: np.ndarray, sample_rate: int = 16000) -> list[dict]` that: (1) runs model(audio_samples), (2) maps YAMNet class IDs to Oversight Hub alert types using a WHITELIST constant (list of (yamnet_class_id, class_name, alert_severity) tuples — include: Glass breaking→HIGH, Shout→MEDIUM, Alarm→CRITICAL, Gunshot/explosion→CRITICAL, Siren→HIGH, Dog bark→INFO, plus 10 other relevant classes to reach ~15 classes per RESEARCH.md Open Question 3), (3) returns only whitelisted detections above confidence 0.3.

Create services/ai-preprocessor/app/models/transcriber.py: Implement Faster-Whisper. Lazy init: `_whisper_model = None; def get_whisper(model_size="medium"): global _whisper_model; if _whisper_model is None: from faster_whisper import WhisperModel; _whisper_model = WhisperModel(model_size, device="cpu", compute_type="int8"); return _whisper_model`. Export `transcribe(audio_path_or_bytes) -> str` that: (1) runs model.transcribe(audio_path_or_bytes, language="fr") with French as default per D-37, (2) returns the transcribed text, (3) catches errors and returns empty string on failure. Model size configurable via config.WHISPER_MODEL (default "medium").
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'services/ai-preprocessor/app'); from models.audio_classifier import WHITELIST; print(f'YAMNet whitelist: {len(WHITELIST)} classes')"</automated>
  </verify>
  <acceptance_criteria>
    - services/ai-preprocessor/app/models/audio_classifier.py exists with get_yamnet() and classify_audio() functions
    - WHITELIST constant defined with ~15 (yamnet_class_id, class_name, alert_severity) tuples covering: Glass breaking→HIGH, Shout→MEDIUM, Alarm→CRITICAL, Gunshot→CRITICAL, Siren→HIGH
    - services/ai-preprocessor/app/models/transcriber.py exists with get_whisper() and transcribe() functions
    - Both use lazy-load pattern matching get_ocr() from anpr.py
    - transcribe() defaults to language="fr" per D-37
    - classify_audio() filters to whitelist only, confidence threshold 0.3
  </acceptance_criteria>
  <done>YAMNet audio classifier with alert severity mapping; Faster-Whisper transcriber with French default; both lazy-loaded</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create detection and audio FastAPI routes, update main.py and config, modify inference pipeline</name>
  <files>services/ai-preprocessor/app/routes/detection.py, services/ai-preprocessor/app/routes/audio.py, services/ai-preprocessor/app/main.py, services/ai-preprocessor/app/config.py, services/ai-preprocessor/app/routes/inference.py</files>
  <read_first>services/ai-preprocessor/app/routes/anpr.py (full file for route pattern), services/ai-preprocessor/app/routes/inference.py, services/ai-preprocessor/app/main.py, services/ai-preprocessor/app/config.py</read_first>
  <action>
Create services/ai-preprocessor/app/routes/detection.py: FastAPI APIRouter following anpr.py pattern. Define Pydantic models: DetectionRequest (camera_id: str, image_base64: str, timestamp: str | None, confidence: float = 0.45, organization_id: str), DetectionResponse (detections: list[DetectionResult] with class_name, confidence, bbox, tracker_id). POST /detect handler: decode base64 image → PIL → numpy → call detect() from detector.py → call track() from tracker.py → filter to person/vehicle classes → POST results to NestJS API at {API_URL}/api/internal/detection-fire (or return response for sync mode). Handle errors with HTTPException.

Create services/ai-preprocessor/app/routes/audio.py: Two endpoints. POST /audio/classify: AudioClassifyRequest (camera_id, audio_base64 or audio_url, organization_id), AudioClassifyResponse (events: list of (class_name, confidence, alert_severity, timestamp)). Handler: decode audio → resample to 16000Hz → classify_audio() → filter whitelist → return. POST /audio/transcribe: AudioTranscribeRequest (audio_base64, language="fr"), AudioTranscribeResponse (text: str). Handler: decode → transcribe() → return text.

Add to services/ai-preprocessor/app/config.py: DETECTION_MODEL (str, default "yolov12n"), DETECTION_CONFIDENCE (float, default 0.45), WHISPER_MODEL (str, default "medium"), WHISPER_DEVICE (str, default "cpu"), NESTJS_API_URL (str, default "http://api:4000"), YOLO_PERSON_CLASSES (list[int], default [0] for COCO person class including hard-hat/vest variants).

Update services/ai-preprocessor/app/main.py: Add `from app.routes import detection, audio` and `app.include_router(detection.router, prefix=settings.API_V1_PREFIX)` and `app.include_router(audio.router, prefix=settings.API_V1_PREFIX)`. Follow exact pattern of existing router registrations.

Modify services/ai-preprocessor/app/routes/inference.py: Add deprecation notice in module docstring. Keep existing endpoints functioning but add `DeprecationWarning` log. The VLM pipeline is being replaced but must continue working until the NestJS DoorControlAgent is fully operational (per D-27 coexistence). DO NOT remove existing code — add a log.warning() on each endpoint invocation.
  </action>
  <verify>
    <automated>grep -c "include_router.*detection" services/ai-preprocessor/app/main.py && grep -c "include_router.*audio" services/ai-preprocessor/app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - services/ai-preprocessor/app/routes/detection.py exists with POST /api/v1/detect endpoint accepting DetectionRequest and returning DetectionResponse
    - services/ai-preprocessor/app/routes/audio.py exists with POST /api/v1/audio/classify and POST /api/v1/audio/transcribe endpoints
    - Both route files follow APIRouter + Pydantic BaseModel pattern from anpr.py
    - services/ai-preprocessor/app/main.py includes detection.router and audio.router
    - services/ai-preprocessor/app/config.py has DETECTION_MODEL, DETECTION_CONFIDENCE, WHISPER_MODEL, NESTJS_API_URL settings
    - services/ai-preprocessor/app/routes/inference.py is NOT deleted — existing endpoints preserved with deprecation log
    - `python -c "from app.main import app; print(len(app.routes))"` shows increased route count (from services/ai-preprocessor dir)
  </acceptance_criteria>
  <done>Detection and audio routes registered; config extended with new settings; inference pipeline preserved with deprecation notice</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RTSP camera stream → AI Preprocessor | Untrusted video stream enters detection pipeline |
| Camera audio stream → YAMNet | Untrusted audio input for event detection |
| Mobile voice input → Faster-Whisper | User-generated audio for transcription |
| AI Preprocessor → NestJS API | Detection events sent via HTTP to internal API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | RTSP stream input | mitigate | OpenCV cv2.VideoCapture handles decoding failures gracefully; corrupted frames skipped, not crashed |
| T-09-06 | Denial of Service | YOLOv12 inference | mitigate | Confidence threshold (0.45) filters low-confidence detections; model processes one frame at a time — no unbounded queue |
| T-09-07 | Information Disclosure | Audio transcription data | mitigate | Transcribed text sent only to internal NestJS API; audio data not persisted to disk; in-memory processing only |
| T-09-08 | Elevation of Privilege | Detection fire → NestJS | mitigate | Internal API endpoint for detection events must validate organization_id against JWT; use internal service token (EDGE_AGENT_SECRET pattern) for AI Preprocessor → API auth |
</threat_model>

<verification>
1. Run `python -c "from app.routes.detection import router; print('detection route OK')"` from services/ai-preprocessor/app
2. Run `grep -c "include_router.*detection" services/ai-preprocessor/app/main.py` — must be 1
3. Run `grep -c "DeprecationWarning\|deprecation\|log.warning" services/ai-preprocessor/app/routes/inference.py` — must be ≥ 1 (preserved, not deleted)
4. Run `grep -c "bytetrack" services/ai-preprocessor/ -r --include="*.py"` — must be 0 (using supervision instead)
</verification>

<success_criteria>
- YOLOv12 + ByteTrack detection pipeline operational
- YAMNet audio classification with alert severity mapping
- Faster-Whisper transcription with French default
- All routes registered in main.py
- Config extended with new model settings
- Existing inference pipeline preserved (not deleted)
- No `bytetrack` PyPI package imported (supervision used instead)
</success_criteria>

<output>
Create `.planning/phases/09-ai-intelligence/09-03-SUMMARY.md` when done
</output>
