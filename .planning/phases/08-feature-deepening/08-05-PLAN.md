---
phase: 08-feature-deepening
plan: 05
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - apps/api/src/modules/access/access.service.ts
  - apps/api/src/modules/access/access.controller.ts
  - apps/dashboard/components/credential-lifecycle-form.tsx
  - apps/dashboard/components/credential-status-badge.tsx
  - apps/dashboard/app/(dashboard)/acces/page.tsx
autonomous: true
requirements: [FTR-01]

must_haves:
  truths:
    - "Admin can issue new credentials (badge/PIN/mobile/QR) with expiry dates, zone restrictions, and temporary pass settings"
    - "Admin can revoke credentials with a required reason — revocation invalidates Redis cache immediately"
    - "Admin can reissue credentials (revoke old + create new with same settings and new expiry)"
    - "Dashboard credentials page shows lifecycle status badges (active/expiring/expired/revoked) with expiry countdown"
    - "Dashboard credentials page supports create/edit/revoke/reissue actions via dialog forms"
  artifacts:
    - path: apps/api/src/modules/access/access.service.ts
      provides: Full credential lifecycle (issue/expire/revoke/reissue)
      contains: "revokeCredential"
    - path: apps/dashboard/components/credential-lifecycle-form.tsx
      provides: Credential create/edit form with lifecycle fields
      contains: "Nouvel identifiant"
    - path: apps/dashboard/components/credential-status-badge.tsx
      provides: Status badge with expiry countdown
      contains: "Expire"
  key_links:
    - from: access.service.ts revokeCredential
      to: Redis
      via: credential:revoked cache invalidation
      pattern: "redis.del"
    - from: access.service.ts reissueCredential
      to: revokeCredential + createCredential
      via: two-step operation
      pattern: "reissueCredential"
---

<objective>
Deliver full credential lifecycle management — issuance with zone restrictions, expiration tracking, revocation with reason, and reissuance — with a complete dashboard UI.

Purpose: Deliver FTR-01 — the access control (credential) module is deepened from basic activate/deactivate to full lifecycle with audit trail, expiration notifications, and Redis-backed revocation enforcement.
Output: Enhanced AccessService with revoke/reissue/expiration logic, dashboard credential lifecycle forms and status badges, enhanced acces page.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-feature-deepening/08-CONTEXT.md
@.planning/phases/08-feature-deepening/08-RESEARCH.md
@.planning/phases/08-feature-deepening/08-PATTERNS.md
@.planning/phases/08-feature-deepening/08-UI-SPEC.md

<interfaces>
From 08-01-Foundation (already merged):
- Credential model: revokedAt DateTime?, revokedReason String?, revokedById String?, lastAccessedAt DateTime?, zoneRestrictions Json?
- revokeCredentialSchema (reason: z.string().min(1)), reissueCredentialSchema (newValidUntil: z.string().datetime())
- dashboard api.ts: revokeCredential(credentialId, reason), reissueCredential(credentialId, newValidUntil)

Existing patterns (from PATTERNS.md lines 469-499):
- AccessService: getCredential(), createCredential(), evaluateAccess(), anti-passback support, Redis cache
- AccessController: existing CRUD routes at /api/access
- Credential model: has isActive, validFrom, validUntil, maxUses, useCount — enhanced with lifecycle fields
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add credential lifecycle API methods (revoke, reissue, expiration)</name>
  <files>
    apps/api/src/modules/access/access.service.ts
    apps/api/src/modules/access/access.controller.ts
  </files>
  <read_first>
    apps/api/src/modules/access/access.service.ts (full file — understand getCredential, createCredential, evaluateAccess, Redis cache patterns)
    apps/api/src/modules/access/access.controller.ts (all routes)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 469-499 for credential lifecycle pattern)
    .planning/phases/08-feature-deepening/08-RESEARCH.md (lines 411-415 Pitfall 4: credential race conditions)
  </read_first>
  <action>
    **access.service.ts** — Add these methods (DO NOT remove existing createCredential, evaluateAccess, etc.):

    1. **revokeCredential(id: string, reason: string, revokedBy: string)** — D-16:
    - Find credential, throw NotFoundException if not found
    - Check credential is not already revoked (revokedAt must be null)
    - Update: isActive = false, revokedAt = new Date(), revokedReason = reason, revokedById = revokedBy
    - Invalidate Redis cache: `await this.redis.del(\`credential:revoked:${id}\`)`
    - Emit event `credential.revoked` for audit trail (follow existing event emitter pattern)
    - Return updated credential

    2. **reissueCredential(id: string, newValidUntil: string, reissuedBy: string)** — D-16:
    - Call revokeCredential(id, 'Reissued - replaced by new credential', reissuedBy)
    - Get old credential data for reference
    - Call createCredential with same userId, type, badgeNumber (if applicable), zoneRestrictions, and new validUntil
    - Return new credential

    3. **getExpiringCredentials(orgId: string, withinDays: number = 7)** — D-16:
    - Query credentials where validUntil is within withinDays from now and isActive = true
    - Returns list of credentials nearing expiry (for dashboard display and notification)
    
    4. **updateLastAccessed(id: string)** — Called by evaluateAccess() to track last access:
    - When access is evaluated, update lastAccessedAt on the credential
    - Fire-and-forget (do not block access evaluation)

    5. **Modify evaluateAccess()** — Add revocation cache check (Pitfall 4 mitigation):
    - Before granting access, check `credential:revoked:{id}` in Redis
    - If key exists, deny access with reason 'Credential revoked'
    - This prevents the race condition where DB update hasn't propagated to cache yet

    **access.controller.ts** — Add new endpoints:
    - `POST /api/access/:id/revoke` → revokeCredential(id, body.reason, user.id), @Roles("ADMIN"), @Audited(), @Body revokeCredentialSchema
    - `POST /api/access/:id/reissue` → reissueCredential(id, body.newValidUntil, user.id), @Roles("ADMIN"), @Audited(), @Body reissueCredentialSchema
    - `GET /api/access/expiring` → getExpiringCredentials(orgId, days), @Roles("ADMIN","SUPERVISOR")
    
    All new endpoints follow existing guard pattern with @Roles and @Audited decorators.
  </action>
  <acceptance_criteria>
    - revokeCredential sets isActive=false, revokedAt, revokedReason, revokedById, invalidates Redis cache
    - reissueCredential revokes old credential and creates new one with same base settings
    - getExpiringCredentials returns credentials validUntil within N days
    - evaluateAccess checks Redis revocation cache before granting access
    - Controller has POST /access/:id/revoke, POST /access/:id/reissue, GET /access/expiring
    - All new endpoints have @Roles("ADMIN") and @Audited() on mutation endpoints
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Credential lifecycle API complete — revoke, reissue, expiration tracking, Redis cache enforcement</done>
</task>

<task type="auto">
  <name>Task 2: Build credential lifecycle dashboard UI</name>
  <files>
    apps/dashboard/components/credential-lifecycle-form.tsx
    apps/dashboard/components/credential-status-badge.tsx
    apps/dashboard/app/(dashboard)/acces/page.tsx
  </files>
  <read_first>
    apps/dashboard/app/(dashboard)/acces/page.tsx (current credentials page structure)
    .planning/phases/08-feature-deepening/08-UI-SPEC.md (lines 361-396 for credential lifecycle spec, lines 727-769 for copywriting)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 860-877 for Phase 6 component pattern)
  </read_first>
  <action>
    **CredentialStatusBadge** (`apps/dashboard/components/credential-status-badge.tsx`):
    - "use client" component (per Phase 6 pattern)
    - Props: `credential: { isActive: boolean; validUntil?: string; revokedAt?: string }`
    - Computes status from credential state:
      - revokedAt !== null → "⚫ Révoqué" (gray with strikethrough, shows reason on hover/tooltip)
      - !isActive && !revokedAt → "⚫ Inactif" (gray)
      - validUntil < now → "🔴 Expiré" (red badge)
      - validUntil < 7 days from now → "🟡 Expire dans {N} jour(s)" (amber badge with countdown)
      - Otherwise → "🟢 Actif" (green badge)
    - Uses shadcn/ui Badge component and Tooltip for revocation reason
    
    **CredentialLifecycleForm** (`apps/dashboard/components/credential-lifecycle-form.tsx`):
    - "use client" component
    - Dialog form for creating/editing credentials
    - Fields per UI-SPEC:
      - Type: radio group (Badge/PIN/Mobile/QR) with fieldset/legend for accessibility
      - Utilisateur: user selection from org members
      - Identifiant: text input (badge number) + "Générer automatiquement" toggle
      - "Pass temporaire" toggle → shows start/end DateTimePickers
      - Restrictions de zone: multi-select zones
      - Notes: textarea
    - Create: POST /api/access (existing endpoint via new lib/api.ts function)
    - Edit: Pre-populated form with existing credential data
    - Revoke button (on existing credentials): shows confirmation dialog per UI-SPEC destructive actions table
    - Reissue button: prompts for new expiry date, calls reissueCredential API
    - All copy in French per UI-SPEC table (lines 727-769)
    
    **Acces Page** enhancement (`apps/dashboard/app/(dashboard)/acces/page.tsx`):
    - Enhance credential table per UI-SPEC layout (lines 366-379):
      - Columns: Utilisateur, Type, Identifiant, Statut (CredentialStatusBadge), Expire le, Actions
      - Each row: status badge rendered via CredentialStatusBadge
      - Actions: Edit / Revoke / Reissue buttons via DropdownMenu
    - Replace existing basic deactivate action with full lifecycle actions
    - Add "➕ Nouvel identifiant" button that opens CredentialLifecycleForm dialog
    - Loading state: table skeleton; Empty state: "Aucun identifiant" with create CTA
    - Error state: toast "Échec de l'opération"
    - New credential creation shows animated slide-in per UI-SPEC animation contract
  </action>
  <acceptance_criteria>
    - CredentialStatusBadge renders correct badge (active/expiring/expired/revoked) based on credential state
    - CredentialLifecycleForm shows type selector, user picker, identifier input, temporary pass toggle, zone restrictions
    - Revoke action shows confirmation dialog with required reason field
    - Acces page table shows status badges, lifecycle action buttons, and new credential button
    - All French copy matches UI-SPEC exactly
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Dashboard build not yet runnable. Human verification recommended.</automated>
  </verify>
  <done>Credential lifecycle dashboard UI complete — create, edit, revoke, reissue with status badges</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard→API | Credential create/revoke/reissue requests — all require ADMIN role |
| API→Redis | Revocation cache keys created on revoke — checked on every access evaluation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Elevation of Privilege | Credential revocation bypass | mitigate | Redis cache invalidation on revoke (immediate). evaluateAccess() checks Redis cache before DB |
| T-08-05-02 | Repudiation | Credential lifecycle without audit trail | mitigate | @Audited decorator on all lifecycle mutation endpoints per existing pattern |
| T-08-05-03 | Elevation of Privilege | Unauthorized credential creation | mitigate | @Roles("ADMIN") on create/revoke/reissue endpoints |
| T-08-05-04 | Information Disclosure | Expired/revoked credential data exposure | mitigate | GET /api/access returns isActive=false for revoked/expired; UI shows appropriate status |
| T-08-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages required |
</threat_model>

<verification>
1. AccessService has revokeCredential, reissueCredential, getExpiringCredentials methods
2. Redis cache invalidation on revoke: `redis.del(\`credential:revoked:${id}\`)`
3. evaluateAccess() checks Redis revocation cache before granting
4. Controller has 3 new endpoints with @Roles("ADMIN") on mutations
5. CredentialStatusBadge renders correct variant for active/expiring/expired/revoked
6. CredentialLifecycleForm renders type radio, user select, date picker, zone multi-select
</verification>

<success_criteria>
- [ ] revokeCredential sets revokedAt + invalidates Redis cache
- [ ] reissueCredential revokes old + creates new in one operation
- [ ] evaluateAccess() checks Redis revocation cache
- [ ] Dashboard credential table shows lifecycle status badges
- [ ] CredentialLifecycleForm supports create, edit, revoke, reissue actions
- [ ] All mutations audited via @Audited decorator
</success_criteria>

<output>
Create `.planning/phases/08-feature-deepening/08-05-SUMMARY.md` when done
</output>
