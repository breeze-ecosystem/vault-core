---
phase: 08-feature-deepening
plan: 06
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - apps/api/src/modules/analytics/analytics.service.ts
  - apps/api/src/modules/analytics/analytics.controller.ts
  - apps/dashboard/components/zone-metrics-grid.tsx
  - apps/dashboard/components/trend-chart.tsx
  - apps/dashboard/components/heatmap-grid.tsx
  - apps/dashboard/app/(dashboard)/analytique/page.tsx
autonomous: true
requirements: [FTR-06]

must_haves:
  truths:
    - "Analytics dashboard shows per-zone metrics (event counts, denied/granted, anomalies) from TimescaleDB zone_analytics_hourly"
    - "Trend chart displays daily/weekly event patterns with 7d/30d/90d date range selector"
    - "Heatmap grid shows activity intensity by hour of day and day of week using CSS grid with opacity-based coloring"
    - "Analytics page uses tabbed layout (Overview / Zones / Trends / Heatmap) with Phase 6 premium components"
    - "All charts are custom SVG/CSS (no recharts dependency) per D-12"
  artifacts:
    - path: apps/api/src/modules/analytics/analytics.service.ts
      provides: Zone metrics, trend data, heatmap data queries
      contains: "getZoneMetricsSummary"
    - path: apps/dashboard/components/trend-chart.tsx
      provides: Custom SVG line chart with date range selector
      contains: "motion.path"
    - path: apps/dashboard/components/heatmap-grid.tsx
      provides: CSS Grid 24h×7d heatmap
      contains: "grid-cols-24"
    - path: apps/dashboard/app/(dashboard)/analytique/page.tsx
      provides: Tabbed analytics dashboard page
      contains: "Tendances"
  key_links:
    - from: analytics.service.ts
      to: zone_analytics_hourly hypertable
      via: $queryRawUnsafe with org-scoped params
      pattern: "zone_analytics_hourly"
    - from: trend-chart.tsx
      to: analytics.service.ts trend endpoint
      via: fetchWithAuth
      pattern: "/api/analytics/trends"
---

<objective>
Build a new security analytics dashboard with per-zone metrics, trend charts, and activity heatmaps — using custom SVG/CSS charts (no new charting library per D-12) and Phase 6 premium components.

Purpose: Deliver FTR-06 — operators can view per-zone event metrics, daily/weekly trends, and activity heatmaps from a single tabbed dashboard. All visualizations are custom SVG/CSS using existing Phase 6 design system components.
Output: Enhanced AnalyticsService with zone metrics / trends / heatmap endpoints, three new chart components (TrendChart, HeatmapGrid, ZoneMetricsGrid), completely rewritten analytique page with tabbed layout.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-feature-deepening/08-CONTEXT.md
@.planning/phases/08-feature-deepening/08-RESEARCH.md
@.planning/phases/08-feature-deepening/08-PATTERNS.md
@.planning/phases/08-feature-deepening/08-UI-SPEC.md

<interfaces>
From 08-01-Foundation (already merged):
- zoneMetricsQuerySchema, trendQuerySchema, heatmapQuerySchema in @repo/shared
- dashboard api.ts: fetchZoneMetrics(orgId, from?, to?), fetchTrendData(orgId, period), fetchHeatmapData(orgId, from?, to?)

Existing patterns (from PATTERNS.md lines 361-398):
- AnalyticsService: getZoneAnalytics() using $queryRawUnsafe, parameterized queries, try/catch with logger.warn
- Existing analytics page: Uses recharts library — D-12 forbids new charting libs. Replace only sections being reworked.
- Phase 6 components: GlassCard, MetricHero, Sparkline, DonutChart (resuable for analytics)
- Sparkline component pattern (lines 652-660): SVG with PolyLine, gradient fill, preserveAspectRatio
- Custom SVG charts follow UI-SPEC contract (lines 399-470)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add analytics query endpoints for zone metrics, trends, and heatmap data</name>
  <files>
    apps/api/src/modules/analytics/analytics.service.ts
    apps/api/src/modules/analytics/analytics.controller.ts
  </files>
  <read_first>
    apps/api/src/modules/analytics/analytics.service.ts (full file — understand existing getZoneAnalytics, getSiteAnalytics patterns with $queryRawUnsafe)
    apps/api/src/modules/analytics/analytics.controller.ts (all routes)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 361-398 for analytics patterns)
    .planning/phases/08-feature-deepening/08-RESEARCH.md (lines 389-391 for anti-pattern: sequential hypertable queries)
  </read_first>
  <action>
    **analytics.service.ts** — Add these methods (never remove existing getZoneAnalytics, etc.):

    1. **getZoneMetricsSummary(orgId: string, from?: string, to?: string)** — Zone overview:
    - Query zone_analytics_hourly with JOIN on zones table
    - Group by zone_id, zone name
    - Return: zoneId, zoneName, totalEvents (granted + denied), denied, granted, anomalies, trend direction (+%/flat/-%)
    - Follow existing $queryRawUnsafe parameterized pattern from analytics.service.ts lines 37-78
    - Use Promise.all for parallel queries if splitting by granularity
    - Wrap in try/catch, return [] on error with logger.warn

    2. **getTrendData(orgId: string, period: '7d' | '30d' | '90d')** — Trend chart data:
    - Compute date range from period: 7/30/90 days back from today
    - Query zone_analytics_hourly or site_analytics_daily aggregated by day
    - Return: [{ date: string, value: number }] for SVG chart consumption
    - Use $queryRawUnsafe with org-scoped parameters

    3. **getHeatmapData(orgId: string, from?: string, to?: string)** — Heatmap grid data:
    - Query access_events or zone_analytics_hourly to get event counts by hour of day and day of week
    - Return 24×7 grid: { hour: number, dayOfWeek: number, count: number }[]
    - Default to last 7 days if no range specified
    - Use $queryRawUnsafe with EXTRACT(HOUR FROM time) and EXTRACT(DOW FROM time)

    **analytics.controller.ts** — Add new endpoints:
    - `GET /api/analytics/zones` → getZoneMetricsSummary(orgId, from?, to?), @Roles("ADMIN","SUPERVISOR","OPERATOR","VIEWER")
    - `GET /api/analytics/trends` → getTrendData(orgId, period), @Roles("ADMIN","SUPERVISOR","OPERATOR","VIEWER")
    - `GET /api/analytics/heatmap` → getHeatmapData(orgId, from?, to?), @Roles("ADMIN","SUPERVISOR","OPERATOR","VIEWER")
    
    All endpoints extract orgId from JWT via @CurrentOrg() or (req as any).user.orgId.
  </action>
  <acceptance_criteria>
    - getZoneMetricsSummary queries zone_analytics_hourly with org scope, returns zone metrics with trend direction
    - getTrendData returns date/value pairs aggregated by day for 7/30/90 day periods
    - getHeatmapData returns 24×7 grid of hour/dayOfWeek event counts
    - All 3 endpoints accessible with proper @Roles guards
    - TypeScript compilation passes
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Analytics data endpoints complete — zone metrics, trends, heatmap data from TimescaleDB</done>
</task>

<task type="auto">
  <name>Task 2: Build custom chart components and analytics dashboard page</name>
  <files>
    apps/dashboard/components/zone-metrics-grid.tsx
    apps/dashboard/components/trend-chart.tsx
    apps/dashboard/components/heatmap-grid.tsx
    apps/dashboard/app/(dashboard)/analytique/page.tsx
  </files>
  <read_first>
    apps/dashboard/app/(dashboard)/analytique/page.tsx (current page — note recharts usage to replace)
    apps/dashboard/components/sparkline.tsx (Phase 6 pattern for TrendChart)
    apps/dashboard/components/donut-chart.tsx (Phase 6 pattern for chart architecture)
    apps/dashboard/components/glass-card.tsx (container pattern)
    apps/dashboard/components/metric-hero.tsx (hero metric pattern)
    .planning/phases/08-feature-deepening/08-UI-SPEC.md (lines 399-470 for analytics layout, lines 464-470 for chart implementation notes)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 630-717 for TrendChart and HeatmapGrid patterns)
    .planning/phases/08-feature-deepening/08-RESEARCH.md (lines 516-573 for SVG TrendChart code example, lines 697-717 for HeatmapGrid CSS pattern)
  </read_first>
  <action>
    **TrendChart** (`apps/dashboard/components/trend-chart.tsx`):
    - "use client" component (per Phase 6 pattern)
    - Props: `data: { date: string; value: number }[], width?: number, height?: number, dateRange?: string, onRangeChange?: (range: string) => void`
    - Custom SVG line chart following Sparkline pattern (PATTERNS.md lines 652-660):
      - SVG with polyline/path for data series
      - Gradient fill underneath (linear gradient, stops at 0% and 100%)
      - Grid lines (horizontal reference lines at 0%, 25%, 50%, 75%, 100%)
      - X-axis labels (dates), Y-axis labels (values using <text> elements)
      - Date range selector: pill buttons "7j" / "30j" / "90j"
    - Animation: motion.path with initial/animate pathLength (500ms easeOut)
    - Accepts `cn()` class for seamless GlassCard integration
    - Reuses existing Sparkline patterns for SVG architecture (no canvas)
    - Handle max 90 data points per PERFORMANCE requirements
    
    **HeatmapGrid** (`apps/dashboard/components/heatmap-grid.tsx`):
    - "use client" component
    - Props: `data: { hour: number; dayOfWeek: number; count: number }[], maxValue?: number`
    - Pure CSS Grid per PATTERNS.md lines 697-717:
      - Grid: `grid-cols-24` (24 columns for hours) with 7 rows (days)
      - Cell opacity based on count/maxValue: `style={{ backgroundColor: \`hsl(var(--primary) / ${opacity})\` }}`
      - Each cell has aria-label: "{hour}:00 — {dayLabel} — {count} événements"
      - Tooltip on hover (shadcn/ui Tooltip)
      - Summary table available screen-reader only (sr-only)
    - No canvas, no SVG — pure Tailwind CSS grid
    - Animation: cells fade in staggered by row (70ms stagger, 300ms per row)
    - Accessibility: keyboard navigation between cells, role="grid"
    
    **ZoneMetricsGrid** (`apps/dashboard/components/zone-metrics-grid.tsx`):
    - "use client" component
    - Props: `zones: { zoneId: string; zoneName: string; totalEvents: number; denied: number; granted: number; anomalies: number; trend: string }[]`
    - Card grid showing per-zone metrics in small GlassCards
    - Each card: zone name, total events (large), denied count, trend direction (📈/📉/➡️)
    - Reuses MetricHero component for individual zone stat display
    
    **Analytique Page** (`apps/dashboard/app/(dashboard)/analytique/page.tsx`):
    - Complete rewrite with tabbed layout per UI-SPEC (lines 399-462):
      - PageHeader: "Analytique" / "Résumé de l'activité et tendances de sécurité"
      - Tab bar: Aperçu | Zones | Tendances | Carte thermique
      
    **Overview tab (default):**
    - 3 MetricHero cards in a row: Événements (total), Alertes actives (count), Zones à risque (count with trend)
    - Two-column layout below: TrendChart (daily activity, 7j default) + DonutChart (alert severity distribution)
    - Data fetched from new analytics API endpoints on page load
    
    **Zones tab:**
    - ZoneMetricsGrid showing all zones with per-zone stats
    - Filter: date range picker optional
    
    **Trends tab:**
    - Full-width TrendChart with date range selector (7j/30j/90j)
    - Below: mini sparklines per zone
    
    **Heatmap tab:**
    - Full-width HeatmapGrid
    - Date range selector
    
    PER D-12: Replace recharts imports used in sections being reworked. If entire page is replaced, remove recharts import. Existing recharts imports for untouched sections stay.
    
    Loading state: 3 MetricHero skeletons, chart outlines with pulse animation
    Empty state: MetricHero shows 0, charts show "Pas assez de données" centered
    Error state: Full-page error card inside GlassCard with retry button
    All French copy per UI-SPEC copywriting contract (lines 771-796)
    Data fetching via fetchWithAuth from api.ts methods
  </action>
  <acceptance_criteria>
    - TrendChart renders SVG line chart with gradient fill, grid lines, axis labels, and date range selector
    - HeatmapGrid renders CSS grid 24×7 with opacity-based coloring and aria-labels
    - ZoneMetricsGrid renders per-zone metric cards with trend indicators
    - Analytics page renders tabbed layout (Overview/Zones/Trends/Heatmap)
    - All French copy matches UI-SPEC
    - Loading, empty, and error states render correctly per UI-SPEC matrix
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Dashboard build not yet runnable. Human verification recommended.</automated>
  </verify>
  <done>Analytics dashboard page with custom SVG/CSS charts complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard→API | Analytics data queries — read-only, all roles permitted |
| API→TimescaleDB | Hypertable queries with org scoping — no mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Information Disclosure | Cross-org analytics data leak | mitigate | All queries scoped to orgId from JWT (existing tenant isolation). Parameters passed via $queryRawUnsafe with org ID param |
| T-08-06-02 | Denial of Service | Large hypertable queries | mitigate | $queryRawUnsafe with LIMIT, date range bounds enforced, try/catch with [] return |
| T-08-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages required |
</threat_model>

<verification>
1. AnalyticsService has getZoneMetricsSummary, getTrendData, getHeatmapData methods
2. All 3 methods use $queryRawUnsafe with org-scoped params and try/catch
3. TrendChart renders SVG with path, gradient, axis labels, range selector
4. HeatmapGrid renders CSS grid with proper aria-labels
5. Analytics page has 4 tabs with proper data fetching
6. Loading/empty/error states functional
</verification>

<success_criteria>
- [ ] Zone metrics, trend, and heatmap API endpoints serve data from TimescaleDB
- [ ] TrendChart renders SVG line chart with 7d/30d/90d selector
- [ ] HeatmapGrid renders 24×7 CSS grid with aria-labels on each cell
- [ ] Analytics page tabbed layout functional in French
- [ ] All loading, empty, and error states render correctly
</success_criteria>

<output>
Create `.planning/phases/08-feature-deepening/08-06-SUMMARY.md` when done
</output>
