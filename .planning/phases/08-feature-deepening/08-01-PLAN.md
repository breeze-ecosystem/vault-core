---
phase: 08-feature-deepening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - packages/shared/src/schemas/door.schema.ts
  - packages/shared/src/schemas/incident.schema.ts
  - packages/shared/src/schemas/visitor.schema.ts
  - packages/shared/src/schemas/organization.schema.ts
  - packages/shared/src/schemas/credential.schema.ts
  - packages/shared/src/schemas/analytics.schema.ts
  - packages/shared/src/schemas/equipment.schema.ts
  - packages/shared/src/schemas/vehicle.schema.ts
  - packages/shared/src/constants/incident-constants.ts
  - packages/shared/src/constants/vehicle-constants.ts
  - packages/shared/src/index.ts
  - apps/api/src/modules/organization/organization.service.ts
  - apps/api/src/modules/organization/organization.controller.ts
  - apps/dashboard/lib/api.ts
autonomous: true
requirements: [FTR-01, FTR-02, FTR-03, FTR-04, FTR-05, FTR-06, FTR-07]

must_haves:
  truths:
    - "Organization model stores per-org SLA profiles, ANPR confidence threshold, and health thresholds as JSON config"
    - "Door model has dedicated nullable heldOpenThresholdMs and settlingTimeoutMs columns"
    - "Visit model has approval status fields (approvalStatus, approvedById, approvedAt, timeWindowStart, timeWindowEnd)"
    - "Credential model has lifecycle fields (revokedAt, revokedReason, revokedById, lastAccessedAt)"
    - "All new Zod schemas and types are exported from the shared package barrel"
    - "OrganizationService exposes config read/write methods for SLA profiles, ANPR threshold, and health thresholds"
    - "Dashboard api.ts exports typed fetch functions for all new Phase 8 endpoints"
  artifacts:
    - path: apps/api/prisma/schema.prisma
      provides: Extended models with all Phase 8 fields
      contains: "heldOpenThresholdMs"
    - path: packages/shared/src/index.ts
      provides: Barrel exports for all new schemas and types
      contains: "doorThresholdConfigSchema"
    - path: apps/api/src/modules/organization/organization.service.ts
      provides: Config management methods
      contains: "updateSlaProfiles"
  key_links:
    - from: packages/shared/src/schemas/door.schema.ts
      to: packages/shared/src/index.ts
      via: barrel export
      pattern: "doorThresholdConfigSchema"
    - from: apps/api/src/modules/organization/organization.controller.ts
      to: organization.service.ts
      via: dependency injection
      pattern: "updateSlaProfiles"
---

<objective>
Establish all database schema extensions, shared validation schemas, org-level configuration service methods, and dashboard API client functions needed by every other Phase 8 plan.

Purpose: Create the single shared foundation (schema + shared package + org config + API client) so all 7 module-hardening plans (Plans 02-08) can run in parallel Wave 2 without file conflicts.
Output: Updated Prisma schema with new columns on Door, Organization, Visit, and Credential models; new Zod schemas in packages/shared/; new org config endpoints for SLA, ANPR, and health thresholds; typed dashboard API client functions for all Phase 8 endpoints.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-feature-deepening/08-CONTEXT.md
@.planning/phases/08-feature-deepening/08-RESEARCH.md
@.planning/phases/08-feature-deepening/08-PATTERNS.md
@.planning/phases/08-feature-deepening/08-UI-SPEC.md

<interfaces>
Existing Prisma models (current state):
- model Door: has `alertConfig Json @default("{}")` — no dedicated threshold columns
- model Organization: has billing/plan fields — no slaProfiles, anprConfidenceThreshold, healthThresholds
- model Visit: has hostUserId, credentialId, checkIn/Out — no approval fields or timeWindow fields
- model Credential: has isActive, validFrom, validUntil — no revokedAt, revokedReason, revokedById, lastAccessedAt

OrganizationService current pattern:
- `apps/api/src/modules/organization/organization.service.ts` — CRUD methods, no config-specific methods

Dashboard api.ts pattern:
- `apps/dashboard/lib/api.ts` — export async functions using `fetchWithAuth`, typed responses, French error messages
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Prisma schema with Phase 8 columns</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>apps/api/prisma/schema.prisma (lines 87-133 for Door, 384-415 for Organization, 321-344 for Visit, 87-112 for Credential)</read_first>
  <action>
    Add the following columns to existing models in schema.prisma per D-02, D-04, D-07, D-10, D-14, D-16:

    **Door model** (after `alertConfig` field, D-02):
    - `heldOpenThresholdMs Int?` — nullable, fallback to DEFAULT_ALERT_CONFIG
    - `settlingTimeoutMs Int?` — nullable, fallback to DEFAULT_ALERT_CONFIG

    **Organization model** (after `trialEndDate` field):
    - `slaProfiles Json @default("{}")` — D-04: per-severity SLA config with escalation chains
    - `anprConfidenceThreshold Int @default(70)` — D-10: ANPR confidence threshold %, default 70
    - `healthThresholds Json @default("{}")` — D-14: health monitoring thresholds (frameDropRate, responseTimeMs, heartbeatTimeout)

    **Visit model** (after `status` field, D-07):
    - `approvalStatus String @default("pending")` — values: "pending"/"approved"/"denied"/"cancelled"
    - `approvedById String?`
    - `approvedAt DateTime?`
    - `timeWindowStart DateTime?` — D-08: timed pass start
    - `timeWindowEnd DateTime?` — D-08: timed pass end

    Add relation for Visit.approvedById:
    - `approvedBy User? @relation("VisitApprover", fields: [approvedById], references: [id])`

    **Credential model** (after `updatedAt` field, D-16):
    - `revokedAt DateTime?`
    - `revokedReason String?`
    - `revokedById String?`
    - `lastAccessedAt DateTime?`
    - `zoneRestrictions Json?` — optional zone ID array for restriction

    Add relation for Credential.revokedById:
    - `revokedBy User? @relation(fields: [revokedById], references: [id])`

    Do NOT remove or modify any existing fields. Only additive changes.
  </action>
  <acceptance_criteria>
    - schema.prisma contains `heldOpenThresholdMs Int?` on Door model
    - schema.prisma contains `slaProfiles Json @default("{}")` on Organization model
    - schema.prisma contains `approvalStatus String @default("pending")` on Visit model
    - schema.prisma contains `revokedAt DateTime?` on Credential model
    - schema.prisma contains `zoneRestrictions Json?` on Credential model
    - Prisma validates: `npx prisma validate` exits with code 0 (in the api directory)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx prisma validate</automated>
  </verify>
  <done>Prisma schema validates with all new columns, no syntax errors</done>
</task>

<task type="auto">
  <name>Task 2: Create new shared Zod schemas + constants + barrel exports</name>
  <files>
    packages/shared/src/schemas/door.schema.ts
    packages/shared/src/schemas/incident.schema.ts
    packages/shared/src/schemas/visitor.schema.ts
    packages/shared/src/schemas/organization.schema.ts
    packages/shared/src/schemas/credential.schema.ts
    packages/shared/src/schemas/analytics.schema.ts
    packages/shared/src/schemas/equipment.schema.ts
    packages/shared/src/schemas/vehicle.schema.ts
    packages/shared/src/constants/incident-constants.ts
    packages/shared/src/constants/vehicle-constants.ts
    packages/shared/src/index.ts
  </files>
  <read_first>
    packages/shared/src/schemas/door.schema.ts (current export — read to extend, not replace)
    packages/shared/src/schemas/incident.schema.ts
    packages/shared/src/schemas/visitor.schema.ts
    packages/shared/src/schemas/organization.schema.ts
    packages/shared/src/schemas/access.schema.ts (for credential schema pattern)
    packages/shared/src/index.ts (for barrel export pattern)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 547-608 for exact schema definitions)
    .planning/phases/08-feature-deepening/08-RESEARCH.md (lines 489-513 for SLA profile schema)
  </read_first>
  <action>
    Add the following new schemas and types to existing files — NEVER remove existing exports, only append:

    **door.schema.ts** — ADD after existing exports (D-02):
    - `doorThresholdConfigSchema`: { heldOpenThresholdMs: z.number().int().min(100).max(30000).optional(), settlingTimeoutMs: z.number().int().min(100).max(60000).optional() }
    - Export type `DoorThresholdConfigInput`

    **incident.schema.ts** — ADD after existing exports (D-04, D-05):
    - `slaProfileSchema`: { severity: z.enum(["CRITICAL","HIGH","MEDIUM","LOW"]), slaMinutes: z.number().int().positive(), escalationUserIds: z.array(z.string().uuid()), notifyOnBreach: z.boolean().default(true) }
    - `slaProfilesConfigSchema`: object with CRITICAL/HIGH/MEDIUM/LOW keys, each being slaProfileSchema shape (without severity field)
    - `evidenceBundleSchema`: { incidentId: z.string().uuid() } — used for triggering auto-bundle on closure
    - Export types `SlaProfileInput`, `SlaProfilesConfigInput`, `EvidenceBundleInput`

    **visitor.schema.ts** — ADD after existing exports (D-07, D-08):
    - `hostApprovalSchema`: { visitId: z.string().uuid(), approved: z.boolean(), reason: z.string().optional() }
    - `timedPassSchema`: { visitId: z.string().uuid(), timeWindowStart: z.string().datetime(), timeWindowEnd: z.string().datetime() }
    - Export types `HostApprovalInput`, `TimedPassInput`

    **organization.schema.ts** — ADD after existing exports:
    - `slaProfilesConfigSchema` (re-export from incident.schema or define inline)
    - `anprThresholdSchema`: { confidenceThreshold: z.number().int().min(50).max(100) }
    - `healthThresholdSchema`: { frameDropRate: z.number().int().min(1).max(100).optional(), responseTimeMs: z.number().int().positive().optional(), heartbeatTimeout: z.number().int().positive().optional() }
    - Export types `AnprThresholdInput`, `HealthThresholdInput`

    **New file: packages/shared/src/schemas/credential.schema.ts** (D-16):
    - `createCredentialSchema` extending the base from access.schema.ts but adding: zoneRestrictions (z.array(z.string().uuid()).optional()), isTemporary (z.boolean().optional())
    - `revokeCredentialSchema`: { reason: z.string().min(1, "Reason is required") }
    - `reissueCredentialSchema`: { newValidUntil: z.string().datetime() }
    - Export types `RevokeCredentialInput`, `ReissueCredentialInput`
    - Note: the existing createCredentialSchema in access.schema.ts is kept as-is; this new file adds lifecycle-specific schemas

    **analytics.schema.ts** — ADD after existing exports (D-12, FTR-06):
    - `zoneMetricsQuerySchema`: { zoneId: z.string().uuid().optional(), from: z.string().datetime().optional(), to: z.string().datetime().optional() }
    - `heatmapQuerySchema`: { from: z.string().datetime().optional(), to: z.string().datetime().optional() }
    - `trendQuerySchema`: { period: z.enum(["7d","30d","90d"]).default("7d"), zoneId: z.string().uuid().optional() }
    - Export types `ZoneMetricsQueryInput`, `HeatmapQueryInput`, `TrendQueryInput`

    **equipment.schema.ts** — ADD after existing exports (D-14, D-15, FTR-07):
    - `healthScoreQuerySchema`: { siteId: z.string().uuid().optional() }
    - `deviceHealthQuerySchema`: { deviceType: z.enum(["CAMERA","READER"]).optional(), siteId: z.string().uuid().optional() }
    - Export types `HealthScoreQueryInput`, `DeviceHealthQueryInput`

    **vehicle.schema.ts** — ADD after existing exports (D-10, D-11):
    - `vehicleEventCorrelationSchema`: { plate: z.string(), from: z.string().datetime(), to: z.string().datetime() }
    - Export type `VehicleEventCorrelationInput`

    **NEW constants file: packages/shared/src/constants/incident-constants.ts** (if not existing):
    - `SLA_SEVERITY_DEFAULTS`: { CRITICAL: 15, HIGH: 30, MEDIUM: 120, LOW: 480 } (in minutes per D-04)
    - `INCIDENT_EVIDENCE_TYPES`: ["access_event", "alert", "video_clip"]

    **NEW constants file: packages/shared/src/constants/vehicle-constants.ts** (if not existing, ADD to existing):
    - `VEHICLE_CONFIDENCE_DEFAULT`: 70 (per D-10)

    **index.ts** — ADD new barrel exports following existing grouped section pattern:
    - "Schemas - Door" section: export doorThresholdConfigSchema and its type
    - "Schemas - Incident" section: export slaProfileSchema, slaProfilesConfigSchema, evidenceBundleSchema and their types
    - "Schemas - Visitor" section: export hostApprovalSchema, timedPassSchema and their types
    - "Schemas - Organization" section: export anprThresholdSchema, healthThresholdSchema and their types
    - "Schemas - Credential" section: export revokeCredentialSchema, reissueCredentialSchema and their types
    - "Schemas - Analytics" section: export zoneMetricsQuerySchema, heatmapQuerySchema, trendQuerySchema and their types
    - "Schemas - Equipment" section: export healthScoreQuerySchema, deviceHealthQuerySchema and their types
    - "Schemas - Vehicle" section: export vehicleEventCorrelationSchema and its type
    - "Constants - Incident" section: export SLA_SEVERITY_DEFAULTS, INCIDENT_EVIDENCE_TYPES
    - "Constants - Vehicle" section: export VEHICLE_CONFIDENCE_DEFAULT (add to existing section)

    Per D-12 convention and existing patterns: all Zod schemas use double quotes, French descriptions in z.object where helpful. Always export both the schema and its inferred type.
  </action>
  <acceptance_criteria>
    - door.schema.ts exports `doorThresholdConfigSchema` and `DoorThresholdConfigInput`
    - incident.schema.ts exports `slaProfilesConfigSchema`, `evidenceBundleSchema`, and their types
    - visitor.schema.ts exports `hostApprovalSchema` and `timedPassSchema`
    - credential.schema.ts exports `revokeCredentialSchema` with `reason: z.string().min(1)`
    - analytics.schema.ts exports `trendQuerySchema` with period enum
    - equipment.schema.ts exports `healthScoreQuerySchema`
    - index.ts contains all new barrel exports with section headers
    - `npx tsc --noEmit` from package root passes (types compile)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All new schemas added to existing files, barrel exports in index.ts, TypeScript compilation passes</done>
</task>

<task type="auto">
  <name>Task 3: Add org-level config endpoints + dashboard API client functions</name>
  <files>
    apps/api/src/modules/organization/organization.service.ts
    apps/api/src/modules/organization/organization.controller.ts
    apps/dashboard/lib/api.ts
  </files>
  <read_first>
    apps/api/src/modules/organization/organization.service.ts (current CRUD pattern)
    apps/api/src/modules/organization/organization.controller.ts (current routes)
    apps/dashboard/lib/api.ts (current fetchWithAuth pattern, last 50 lines for append)
    .planning/phases/08-feature-deepening/08-RESEARCH.md (lines 301-313 for per-org config pattern, lines 489-513 for SLA config)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 768-798 for controller auth pattern, lines 924-933 for API client pattern)
  </read_first>
  <action>
    **OrganizationService** — ADD these methods (never remove existing):
    - `getSlaProfiles(orgId: string): Promise<SlaProfilesConfig>` — reads slaProfiles JSON from org, returns parsed config with defaults from SLA_SEVERITY_DEFAULTS
    - `updateSlaProfiles(orgId: string, profiles: SlaProfilesConfigInput): Promise<Org>` — validates via slaProfilesConfigSchema, stores as JSON, returns updated org
    - `getAnprThreshold(orgId: string): Promise<number>` — reads anprConfidenceThreshold, returns value (default 70)
    - `updateAnprThreshold(orgId: string, threshold: number): Promise<Org>` — validates 50-100 range, stores on org
    - `getHealthThresholds(orgId: string): Promise<HealthThresholds>` — reads healthThresholds JSON, returns with defaults
    - `updateHealthThresholds(orgId: string, thresholds: HealthThresholdInput): Promise<Org>` — validates via healthThresholdSchema, stores as JSON

    All methods use: @Audited() for mutation operations (follow existing decorator pattern), @Roles("ADMIN") guard enforcement, ZodValidationPipe for input validation.

    **OrganizationController** — ADD new endpoints:
    - `GET /api/organizations/:id/sla` → `getSlaProfiles(id)`, @Roles("ADMIN","SUPERVISOR")
    - `PUT /api/organizations/:id/sla` → `updateSlaProfiles(id, body)`, @Roles("ADMIN"), @Audited()
    - `GET /api/organizations/:id/anpr-threshold` → `getAnprThreshold(id)`, @Roles("ADMIN","SUPERVISOR")
    - `PUT /api/organizations/:id/anpr-threshold` → `updateAnprThreshold(id, body)`, @Roles("ADMIN"), @Audited()
    - `GET /api/organizations/:id/health-thresholds` → `getHealthThresholds(id)`, @Roles("ADMIN","SUPERVISOR")
    - `PUT /api/organizations/:id/health-thresholds` → `updateHealthThresholds(id, body)`, @Roles("ADMIN"), @Audited()

    All endpoints use `@Body(new ZodValidationPipe(schema))` pattern.

    **Dashboard api.ts** — APPEND these typed fetch functions at the end of the file (per existing pattern):

    // ─── Phase 8: SLA Config ───
    - `async function fetchSlaProfiles(orgId: string)`: GET /api/organizations/${orgId}/sla
    - `async function updateSlaProfiles(orgId: string, profiles: SlaProfilesConfigInput)`: PUT /api/organizations/${orgId}/sla

    // ─── Phase 8: ANPR Config ───
    - `async function fetchAnprThreshold(orgId: string)`: GET /api/organizations/${orgId}/anpr-threshold
    - `async function updateAnprThreshold(orgId: string, threshold: number)`: PUT /api/organizations/${orgId}/anpr-threshold

    // ─── Phase 8: Health Thresholds ───
    - `async function fetchHealthThresholds(orgId: string)`: GET /api/organizations/${orgId}/health-thresholds
    - `async function updateHealthThresholds(orgId: string, thresholds: HealthThresholdInput)`: PUT /api/organizations/${orgId}/health-thresholds

    // ─── Phase 8: Door Config ───
    - `async function fetchDoorThresholds(orgId: string)`: GET /api/doors/thresholds?orgId=${orgId}
    - `async function updateDoorThresholds(doorId string, config: DoorThresholdConfigInput)`: PATCH /api/doors/${doorId}/thresholds

    // ─── Phase 8: Incident SLA ───
    - `async function fetchSlaTimer(incidentId: string)`: GET /api/incidents/${incidentId}/sla
    - `async function triggerEvidenceBundle(incidentId: string)`: POST /api/incidents/${incidentId}/auto-bundle

    // ─── Phase 8: ANPR ───
    - `async function fetchAnprConfidenceThreshold(orgId: string)`: GET /api/anpr/threshold?orgId=${orgId}
    - `async function updateAnprConfidenceThreshold(threshold: number)`: PUT /api/anpr/threshold

    // ─── Phase 8: Visitor ───
    - `async function sendHostApproval(visitId: string)`: POST /api/visitors/${visitId}/request-approval
    - `async function approveVisit(visitId: string, approved: boolean, reason?: string)`: POST /api/visitors/${visitId}/approve
    - `async function setTimedPass(visitId: string, start: string, end: string)`: PATCH /api/visitors/${visitId}/timed-pass

    // ─── Phase 8: Credential Lifecycle ───
    - `async function revokeCredential(credentialId: string, reason: string)`
    - `async function reissueCredential(credentialId: string, newValidUntil: string)`

    // ─── Phase 8: Analytics ───
    - `async function fetchZoneMetrics(orgId: string, from?: string, to?: string)`: GET /api/analytics/zones
    - `async function fetchTrendData(orgId: string, period: string)`: GET /api/analytics/trends
    - `async function fetchHeatmapData(orgId: string, from?: string, to?: string)`: GET /api/analytics/heatmap

    // ─── Phase 8: Equipment Health ───
    - `async function fetchSiteHealth(orgId: string)`: GET /api/equipment/health
    - `async function fetchDeviceHealth(orgId: string, deviceType?: string)`: GET /api/equipment/devices

    Each function follows the existing pattern: fetchWithAuth, !res.ok throws French error, return res.json(). Import types from @repo/shared.
  </action>
  <acceptance_criteria>
    - organization.service.ts has getSlaProfiles, updateSlaProfiles, getAnprThreshold, updateAnprThreshold, getHealthThresholds, updateHealthThresholds methods
    - organization.controller.ts has 6 new endpoints (3 GET, 3 PUT) with proper @Roles and @Audited decorators
    - All new api.ts functions use fetchWithAuth, typed imports from @repo/shared, French error messages
    - `npx tsc --noEmit` from apps/api directory passes
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>Org config endpoints operational, dashboard API client exports all new typed functions, TypeScript compiles</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>[BLOCKING] Task 4: Run Prisma db push to apply schema changes</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>apps/api/prisma/schema.prisma (verify all new fields are as intended before running push)</read_first>
  <action>
    Run Prisma db push to apply the new schema columns to the database:
    ```bash
    cd apps/api && npx prisma db push --accept-data-loss
    ```
    This is required because npx prisma validate (Task 1) and TypeScript compilation (Tasks 2-3) both pass
    without a live database — creating a false-positive verification state if this push is skipped.

    The `--accept-data-loss` flag is safe here because we're only ADDING nullable columns and new JSON fields — no destructive changes.
  </action>
  <acceptance_criteria>
    - npx prisma db push completes without errors
    - New columns (heldOpenThresholdMs, settlingTimeoutMs, slaProfiles, anprConfidenceThreshold, healthThresholds, approvalStatus, approvedById, approvedAt, timeWindowStart, timeWindowEnd, revokedAt, revokedReason, revokedById, lastAccessedAt, zoneRestrictions) exist in the live database
    - npx prisma generate succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx prisma db push --accept-data-loss 2>&1</automated>
  </verify>
  <done>Database schema updated with all Phase 8 columns, Prisma client regenerated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | All new org config endpoints accept untrusted input via Zod-validated request bodies |
| API→DB | Org config changes written to Organization model via Prisma (tenant-scoped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Organization config PUT endpoints | mitigate | @Roles("ADMIN") on all mutation endpoints per existing guard pattern |
| T-08-02 | Information Disclosure | Organization config GET endpoints | mitigate | @Roles("ADMIN","SUPERVISOR") — SLA profiles contain escalation chain user IDs |
| T-08-03 | Tampering | schema.prisma migration | accept | Additive-only changes (nullable columns, new JSON fields) — no data migration risk |
| T-08-04 | Spoofing | Config values from client | mitigate | All mutation inputs validated via ZodValidationPipe before reaching service layer |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages required — all dependencies already in codebase |
</threat_model>

<verification>
1. `cd apps/api && npx prisma validate` passes (schema is valid)
2. `npx tsc --noEmit` passes from project root (types compile)
3. `cd apps/api && npx tsc --noEmit` passes (API types compile with new methods)
4. `cd apps/api && npx prisma db push --accept-data-loss` applies schema without errors
5. All new barrel exports are importable: `grep -c "export.*ThresholdConfig" packages/shared/src/index.ts` > 0
6. Organization controller has 6 new endpoints: `grep -c "@Roles" apps/api/src/modules/organization/organization.controller.ts` >= 6 (existing + new)
</verification>

<success_criteria>
- [ ] Prisma schema has all Phase 8 columns on Door, Organization, Visit, and Credential models
- [ ] All 6 new Zod schemas exist and are barrel-exported from index.ts
- [ ] OrganizationService has 6 new config management methods
- [ ] OrganizationController has 6 new config endpoints with proper guards
- [ ] Dashboard api.ts has typed fetch functions for all Phase 8 modules
- [ ] Prisma db push applied schema to database
- [ ] TypeScript compilation passes for both API and shared packages
</success_criteria>

<output>
Create `.planning/phases/08-feature-deepening/08-01-SUMMARY.md` when done
</output>
