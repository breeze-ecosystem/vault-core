---
phase: 08-feature-deepening
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - apps/api/src/modules/incident/incident-state-machine.ts
  - apps/api/src/modules/incident/incident.service.ts
  - apps/api/src/modules/incident/incident.controller.ts
  - apps/api/src/modules/incident/incident.processor.ts
  - apps/dashboard/components/sla-profile-grid.tsx
  - apps/dashboard/components/sla-status-badge.tsx
  - apps/dashboard/components/evidence-bundle-list.tsx
  - apps/dashboard/components/escalation-chain-editor.tsx
  - apps/dashboard/app/(dashboard)/incidents/page.tsx
  - apps/dashboard/app/(dashboard)/incidents/[id]/page.tsx
autonomous: true
requirements: [FTR-04]

must_haves:
  truths:
    - "Incident creation reads per-severity SLA profiles from Organization settings (Critical=15min, High=30min, Medium=2hr, Low=8hr)"
    - "SLA timers use Redis-backed deduplication to prevent duplicate jobs on restart recovery"
    - "Incident closure triggers evidence auto-bundling from a ±5min time window across access events, alerts, and video clips"
    - "Closure PDF report includes SLA breach tracking and auto-bundled evidence summary"
    - "Dashboard incidents page shows SLA status badges with countdown and evidence bundle display"
    - "Org settings include SLA Profile Grid for configuring per-severity SLA targets and escalation chains"
  artifacts:
    - path: apps/api/src/modules/incident/incident.service.ts
      provides: SLA profile lookup, evidence auto-bundling, Redis dedup
      contains: "getSlaConfig"
    - path: apps/dashboard/components/sla-profile-grid.tsx
      provides: Editable SLA profile table
      contains: "CRITICAL"
    - path: apps/dashboard/components/evidence-bundle-list.tsx
      provides: Evidence display with remove
      contains: "access_event"
  key_links:
    - from: incident.service.ts
      to: organization.service.ts (via getSlaConfig)
      via: Prisma Organization query on slaProfiles JSON
      pattern: "slaProfiles"
    - from: incident.service.ts autoBundleEvidence
      to: access_events, alerts, video_clips hypertables
      via: $queryRaw with ±5min window
      pattern: "LIMIT 50"
---

<objective>
Rebuild incident management with per-severity SLA profiles at organization level, Redis-backed job deduplication, evidence auto-bundling on closure, and enhanced PDF closure reports with SLA breach tracking.

Purpose: Deliver FTR-04 — production-grade incident workflow with configurable SLA timers, automatic evidence collection from time-window joins, and resilient job scheduling.
Output: Enhanced incident module (state machine, service, controller, processor), SLA configuration UI components, evidence auto-bundle display, dashboard page enhancements.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-feature-deepening/08-CONTEXT.md
@.planning/phases/08-feature-deepening/08-RESEARCH.md
@.planning/phases/08-feature-deepening/08-PATTERNS.md
@.planning/phases/08-feature-deepening/08-UI-SPEC.md

<interfaces>
From 08-01-Foundation (already merged):
- Organization model: slaProfiles JSON column
- slaProfilesConfigSchema, evidenceBundleSchema in @repo/shared
- SLA_SEVERITY_DEFAULTS constant: { CRITICAL: 15, HIGH: 30, MEDIUM: 120, LOW: 480 }
- OrganizationService.getSlaProfiles(), updateSlaProfiles() — returns configuration with defaults
- Dashboard api.ts: fetchSlaTimer(incidentId), triggerEvidenceBundle(incidentId), fetchSlaProfiles(orgId), updateSlaProfiles(orgId, profiles)

Existing incident module patterns (from PATTERNS.md lines 196-297):
- IncidentService: 728 lines, create() hardcodes `slaMinutes: 30`, has addEvidence(), generateClosureReport(), handleAlertCreated() with Redis dedup
- IncidentStateMachine: status transitions OPEN→TRIAGE→INVESTIGATING→RESOLVED→CLOSED
- IncidentProcessor: SLA escalation + auto-triage workers, WorkerHost
- onModuleInit() recovery: lines 28-68, schedules SLA for all active incidents — needs dedup
- generateClosureReport(): PDFKit + Handlebars, French locale, A4 format — enhance with SLA breach + evidence summary
- handleAlertCreated(): existing Redis dedup pattern at lines 680-689 — pattern to extend for SLA timers
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SLA profiles, evidence auto-bundling, and Redis dedup to incident API</name>
  <files>
    apps/api/src/modules/incident/incident-state-machine.ts
    apps/api/src/modules/incident/incident.service.ts
    apps/api/src/modules/incident/incident.controller.ts
    apps/api/src/modules/incident/incident.processor.ts
  </files>
  <read_first>
    apps/api/src/modules/incident/incident.service.ts (full 728 lines — understand create(), addEvidence(), handleAlertCreated(), onModuleInit(), generateClosureReport())
    apps/api/src/modules/incident/incident.controller.ts (all routes)
    apps/api/src/modules/incident/incident.processor.ts (all workers)
    apps/api/src/modules/incident/incident-state-machine.ts (status transitions)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 196-297 for exact patterns)
    .planning/phases/08-feature-deepening/08-RESEARCH.md (lines 393-397 Pitfall 1: SLA timer loss, lines 431-487 code examples)
  </read_first>
  <action>
    **incident-state-machine.ts** — D-04: No structural changes needed. The state machine transitions remain correct. SLA severity mapping is handled in IncidentService via org config lookup, not in the state machine.

    **incident.service.ts** — Make these specific additive changes (DO NOT remove existing logic):

    1. **getSlaConfig(orgId: string, severity: string)** — New method that reads slaProfiles JSON from Organization model. Returns targetMinutes and escalationUserIds with SLA_SEVERITY_DEFAULTS fallback. Follows pattern from RESEARCH.md lines 505-512.

    2. **Modify create()** — Replace the hardcoded `slaMinutes: 30` with:
    ```typescript
    const slaConfig = await this.getSlaConfig(organizationId, severity);
    // Then use slaConfig.targetMinutes for slaMinutes field
    ```
    Also store escalation user IDs from the config for reference during escalation.

    3. **autoBundleEvidence(incidentId: string)** — New method (D-05). On closure:
    - Read incident's createdAt and organizationId
    - Compute ±5 minute window
    - Run 3 parallel queries via Promise.all: access_events hypertable, alerts model, video_clips hypertable
    - Create IncidentEvidence records for each match with source type
    - Return count of attached evidence items
    - Follow exact pattern from PATTERNS.md lines 221-258 and RESEARCH.md lines 436-487
    - Limit 50 per type for performance (Pitfall 3 mitigation)

    4. **Redis dedup for SLA timer scheduling** (D-06) — In both assignIncident() and onModuleInit() recovery:
    ```typescript
    const dedupKey = `incident:sla:dedup:${incidentId}:${level}`;
    const alreadyScheduled = await this.redis.get(dedupKey);
    if (alreadyScheduled) {
      this.logger.debug(`SLA timer already scheduled for incident ${incidentId}`);
      return;
    }
    await this.redis.setex(dedupKey, 3600, '1');
    ```
    This follows the existing pattern from handleAlertCreated() (lines 680-689 in current codebase).

    5. **Enhance generateClosureReport()** — Add SLA breach tracking:
    - Check if incident.slaMinutes was breached (compare resolvedAt or closedAt to createdAt + slaMinutes)
    - Include SLA breach status (met/breached) in PDF report
    - Add auto-bundled evidence summary section: "Preuves automatiques" with counts and types
    - Append to existing Handlebars template (do not rewrite — extend)
    - Follow existing French locale + A4 format

    6. **Call autoBundleEvidence() inside the status transition to CLOSED** — In the close/resolve flow, after status update succeeds:
    ```typescript
    if (newStatus === 'closed' || newStatus === 'resolved') {
      // Fire and forget — don't block the status transition response
      this.autoBundleEvidence(incidentId).catch(err => 
        this.logger.error(`Evidence auto-bundle failed for ${incidentId}: ${err.message}`)
      );
    }
    ```

    **incident.controller.ts** — Add new endpoints:
    - `GET /api/incidents/:id/sla` → returns current SLA state for badge rendering: { targetMinutes, elapsedMinutes, remainingMinutes, breached }
    - `POST /api/incidents/:id/auto-bundle` → manually trigger evidence auto-bundling, @Roles("ADMIN","SUPERVISOR","OPERATOR")

    **incident.processor.ts** — Enhance SLA escalation workers:
    - In handleSlaEscalation(), add Redis dedup check before executing to prevent duplicate escalations:
    ```
    const dedupKey = `incident:sla:executed:${incidentId}:${level}`;
    if (await this.redis.get(dedupKey)) return { skipped: true };
    await this.redis.setex(dedupKey, 86400, '1');
    ```
    - Log failed escalations to incident timeline (not DLQ — per D-06, operator notification is sufficient)
  </action>
  <acceptance_criteria>
    - getSlaConfig reads slaProfiles from Organization with SLA_SEVERITY_DEFAULTS fallback
    - create() uses getSlaConfig instead of hardcoded 30 minutes
    - autoBundleEvidence runs 3 parallel time-window queries and creates IncidentEvidence records
    - generateClosureReport() includes SLA breach status and evidence summary
    - Redis dedup keys exist for both SLA scheduling (`incident:sla:dedup:*`) and execution (`incident:sla:executed:*`)
    - IncidentController has GET /incidents/:id/sla and POST /incidents/:id/auto-bundle
    - TypeScript compilation passes
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Incident API hardening complete — SLA profiles, evidence auto-bundle, Redis dedup, enhanced PDF</done>
</task>

<task type="auto">
  <name>Task 2: Build SLA/evidence dashboard UI components and enhance incident pages</name>
  <files>
    apps/dashboard/components/sla-profile-grid.tsx
    apps/dashboard/components/sla-status-badge.tsx
    apps/dashboard/components/evidence-bundle-list.tsx
    apps/dashboard/components/escalation-chain-editor.tsx
    apps/dashboard/app/(dashboard)/incidents/page.tsx
    apps/dashboard/app/(dashboard)/incidents/[id]/page.tsx
  </files>
  <read_first>
    apps/dashboard/app/(dashboard)/incidents/page.tsx (current incident list page structure)
    apps/dashboard/app/(dashboard)/incidents/[id]/page.tsx (current detail page with evidence section)
    .planning/phases/08-feature-deepening/08-UI-SPEC.md (lines 249-289 for SLA/evidence UI spec)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 860-877 for Phase 6 component pattern)
  </read_first>
  <action>
    **SLAStatusBadge** (`apps/dashboard/components/sla-status-badge.tsx`):
    - "use client" component per Phase 6 pattern
    - Props: `incidentId: string, targetMinutes: number, createdAt: string`
    - Computes elapsed minutes from createdAt vs now
    - Renders Badge variant per UI-SPEC:
      - Green (ok): "Dans les temps ({elapsed}/{target})" when <50% elapsed
      - Amber (warning): "Bientôt dépassé ({elapsed}/{target})" when >=50% elapsed
      - Red (breached): "⚠️ SLA dépassé ({elapsed}/{target})" when >=100% elapsed
    - Fetches SLA state from `fetchSlaTimer(incidentId)` on mount with 30s refresh interval
    - Loading: show skeleton for 300ms
    - Breach transition: color transition amber→red with subtle pulse animation (motion)
    
    **SLAProfileGrid** (`apps/dashboard/components/sla-profile-grid.tsx`):
    - "use client" component
    - Renders a 4-row editable table inside a GlassCard:
      - Columns: Sévérité | Délai (input, minutes) | Chaîne d'escalade (multi-select users)
      - 4 fixed rows: CRITIQUE, HAUTE, MOYENNE, BASSE
    - Fetches current SLA profiles from `fetchSlaProfiles(orgId)` on mount
    - "💾 Appliquer" button saves via `updateSlaProfiles(orgId, profiles)`
    - Follow UI-SPEC layout exactly (lines 268-283)
    - Intended for org settings page (deployed in parametres/)
    
    **EscalationChainEditor** (`apps/dashboard/components/escalation-chain-editor.tsx`):
    - "use client" component, used within SLAProfileGrid
    - Multi-select user picker: shows org members with checkboxes
    - Add/remove users, reorder by priority via drag or up/down buttons
    - Timeout before escalation input (optional, default matches SLA target)
    - "notify on breach" checkbox per severity
    
    **EvidenceBundleList** (`apps/dashboard/components/evidence-bundle-list.tsx`):
    - "use client" component
    - Props: `incidentId: string`
    - Fetches evidence list from incident detail endpoint
    - Groups items by type: "🚪 Événement d'accès", "🚨 Alerte", "🎥 Clip vidéo"
    - Each item: type icon, description, timestamp, [× Retirer] button
    - Empty state: "Aucune preuve automatique trouvée dans cette fenêtre"
    - Total count badge: "{n} preuve(s) liée(s)"
    
    **Incidents List Page** enhancement (`apps/dashboard/app/(dashboard)/incidents/page.tsx`):
    - Add `SLAStatusBadge` to each incident row, positioned next to severity badge
    - Load SLA data for each visible incident (fetched as part of incident data or inline)
    - Apply loading/empty/error state patterns per UI-SPEC matrix
    
    **Incidents Detail Page** enhancement (`apps/dashboard/app/(dashboard)/incidents/[id]/page.tsx`):
    - Add SLA timer display at top of incident detail: target time, elapsed, breach status
    - Integrate EvidenceBundleList component in the evidence section
    - Add "Déclencher la recherche de preuves" button that calls POST /incidents/:id/auto-bundle
    - Show toast on bundle completion with count of attached evidence
  </action>
  <acceptance_criteria>
    - SLAStatusBadge renders green/amber/red badge with time display, refreshes every 30s
    - SLAProfileGrid renders 4-row editable table with severity/delay/escalation columns
    - EscalationChainEditor renders multi-select user picker with notify-on-breach checkbox
    - EvidenceBundleList groups evidence by type, shows remove button, handles empty state
    - Incidents list page shows SLAStatusBadge per incident row
    - Incidents detail page shows SLA timer, EvidenceBundleList, and manual trigger button
    - All French copy matches UI-SPEC copywriting contract
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Dashboard build not yet runnable. Human verification recommended.</automated>
  </verify>
  <done>Incident dashboard UI renders SLA status badges, profile grid, and evidence bundle display</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | SLA config write, evidence auto-bundle trigger, SLA state read |
| API→Redis | SLA dedup keys created/read — no PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | SLA profile config PUT | mitigate | @Roles("ADMIN") on organization SLA write endpoints per D-04 |
| T-08-03-02 | Denial of Service | Evidence auto-bundle hypertable queries | mitigate | LIMIT 50 per type, Promise.all parallel execution, catch errors per query |
| T-08-03-03 | Repudiation | Evidence removal | mitigate | @Audited decorator on evidence mutation endpoints per RESEARCH.md Security Domain |
| T-08-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages required |
</threat_model>

<verification>
1. IncidentService has getSlaConfig method reading org.slaProfiles
2. create() uses dynamic SLA instead of hardcoded 30min
3. autoBundleEvidence() runs 3 parallel time-window queries
4. Redis dedup keys created for SLA scheduling
5. generateClosureReport() includes SLA breach + evidence summary
6. SLAStatusBadge renders appropriate color per SLA state
7. EvidenceBundleList groups by type and shows remove buttons
</verification>

<success_criteria>
- [ ] Per-severity SLA profiles readable/writable via Organization endpoints
- [ ] SLA timers use Redis dedup keys (scheduling + execution)
- [ ] Evidence auto-bundling runs on closure, creates IncidentEvidence records
- [ ] PDF closure reports include SLA breach status and evidence summary
- [ ] Dashboard SLAStatusBadge renders with correct color per time elapsed
- [ ] Dashboard SLAProfileGrid editable with save
- [ ] Dashboard EvidenceBundleList displayed on incident detail
</success_criteria>

<output>
Create `.planning/phases/08-feature-deepening/08-03-SUMMARY.md` when done
</output>
