---
phase: 08-feature-deepening
plan: 08
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - apps/mobile/app/(tabs)/incidents.tsx
  - apps/mobile/app/incident/[id].tsx
  - apps/mobile/components/mobile-incident-card.tsx
  - apps/mobile/lib/api.ts
autonomous: true
requirements: [FTR-04]
# Note: FTR-02 mobile door control is delivered by 08-02

must_haves:
  truths:
    - "Mobile incident list shows assigned incidents with severity badge, status badge, and SLA countdown"
    - "Mobile incident detail shows full incident info with status transition buttons (Acknowledge → Investigate → Resolve → Close)"
    - "Status transitions call API, update optimistically, and show toast on success/failure"
    - "Mobile uses guard-first design patterns: large touch targets (min 44px), simplified layout, full-width buttons"
  artifacts:
    - path: apps/mobile/app/(tabs)/incidents.tsx
      provides: Incident list with cards
      contains: "Incident"
    - path: apps/mobile/app/incident/[id].tsx
      provides: Incident detail with status transition buttons
      contains: "Résoudre"
    - path: apps/mobile/components/mobile-incident-card.tsx
      provides: Incident card component
      contains: "severity"
  key_links:
    - from: incidents.tsx
      to: incident API endpoints
      via: fetchWithAuth from mobile/lib/api.ts
      pattern: "/api/incidents"
    - from: mobile/lib/api.ts
      to: NestJS IncidentController
      via: PATCH /api/incidents/:id/status
      pattern: "updateIncidentStatus"
---

<objective>
Deliver guard-first mobile incident response — list view of assigned incidents with SLA badges and a detail screen with status transition buttons.

Purpose: Deliver FTR-02 and FTR-04 mobile components per D-19 — guards view and respond to incidents from their mobile device with large touch targets, simplified layout, and real-time status updates.
Output: Full mobile incident response: list screen replacing placeholder, detail screen with status transitions, reusable incident card component, mobile API client additions.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-feature-deepening/08-CONTEXT.md
@.planning/phases/08-feature-deepening/08-RESEARCH.md
@.planning/phases/08-feature-deepening/08-PATTERNS.md
@.planning/phases/08-feature-deepening/08-UI-SPEC.md

<interfaces>
Existing mobile patterns (from PATTERNS.md lines 721-761, 879-898):
- Mobile screen pattern: StyleSheet.create() at bottom, colors/typography from @repo/design
- Phase 6 guard-first: 4-tab navigation, quick-action home, large touch targets (44px min)
- Current incidents.tsx is a placeholder with "coming soon" text

Mobile API client pattern:
- apps/mobile/lib/api.ts: fetchWithAuth wrapper, typed functions, French error messages
- Uses expo-secure-store for JWT token

Existing mobile components:
- apps/mobile/components/quick-action-button.tsx (Phase 6) — pattern for action buttons
- apps/mobile/components/error-boundary.tsx — error handling pattern

Door control mobile was built in 08-02 Plan door-hardening — this plan focuses on incident response only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build mobile incident list and detail screens with API client</name>
  <files>
    apps/mobile/lib/api.ts
    apps/mobile/components/mobile-incident-card.tsx
    apps/mobile/app/(tabs)/incidents.tsx
    apps/mobile/app/incident/[id].tsx
  </files>
  <read_first>
    apps/mobile/app/(tabs)/incidents.tsx (current placeholder — full file)
    apps/mobile/lib/api.ts (full file — understand existing fetchWithAuth and typed API function pattern)
    apps/mobile/app/(tabs)/alerts.tsx or similar existing screen (for mobile screen pattern reference)
    .planning/phases/08-feature-deepening/08-UI-SPEC.md (lines 537-583 for mobile incident spec, lines 832-852 for copywriting)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 721-761 for mobile incident patterns, lines 879-898 for mobile screen pattern)
  </read_first>
  <action>
    **Mobile API client** (`apps/mobile/lib/api.ts`) — ADD these functions (never remove existing):

    // ─── Phase 8: Incident Response ───
    - `async function fetchMyIncidents(): Promise<IncidentDto[]>` — GET /api/incidents?assignedToMe=true
    - `async function fetchIncident(id: string): Promise<IncidentDto>` — GET /api/incidents/${id}
    - `async function updateIncidentStatus(id: string, status: string, reason?: string): Promise<void>` — PATCH /api/incidents/${id}/status

    Import types from @repo/shared (IncidentDto, etc.)

    **MobileIncidentCard** (`apps/mobile/components/mobile-incident-card.tsx`):
    - React Native component following StyleSheet.create() pattern
    - Props: `incident: { id, title, severity, status, zoneName, createdAt, slaMinutes?, slaElapsed? }, onPress: (id: string) => void`
    - Card layout per UI-SPEC (lines 544-556):
      - Severity badge (colored left border, matching severity level)
      - Title text (bold, white)
      - Zone name + relative time "Il y a {time}"
      - SLA countdown if available: "⏱️ SLA: {elapsed}/{target}"
      - Status badge (colored dot + status text)
    - Large touch target (min 44px) per Phase 6 guard-first pattern
    - Uses colors/typography from @repo/design tokens
    
    **Incidents List** (`apps/mobile/app/(tabs)/incidents.tsx`):
    - Complete rewrite replacing placeholder content
    - Screen title: "Mes incidents"
    - FlatList rendering MobileIncidentCard for each item
    - Pull-to-refresh (RefreshControl)
    - Data fetching from fetchMyIncidents() on screen focus / mount
    - Loading state: FlatList with 3 skeleton cards (gray placeholder cards with pulse animation)
    - Empty state: centered shield-check icon, "Aucun incident assigné", subtitle "Les nouveaux incidents vous seront automatiquement attribués"
    - Error state: full-screen error "Impossible de charger les incidents" with retry button
    - Tap card → router.push(\`/incident/${incident.id}\`)
    
    **Incident Detail** (`apps/mobile/app/incident/[id].tsx`):
    - New Expo Router screen
    - Route: /incident/[id].tsx (dynamic route per existing pattern)
    - Fetch incident from fetchIncident(id) on mount
    - Display per UI-SPEC (lines 558-575):
      - Header: severity badge, title
      - Detail rows: ID incident, Zone, Porte, Créé le, Statut
      - SLA countdown: "⏱️ SLA: {elapsed}/{target}" with color (green/amber/red)
    - Status transition buttons per current state:
      - OPEN → "✓ Accuser réception" → status becomes 'triage'
      - TRIAGE → "🔍 Investiguer" → status becomes 'investigating'
      - INVESTIGATING → "✅ Résoudre" → status becomes 'resolved'
      - RESOLVED → "📁 Fermer" → status becomes 'closed'
    - Only show buttons for valid transitions (follow IncidentStateMachine)
    - Optimistic UI: update button state immediately on press, revert on error with toast
    - Button shows spinner during API call
    - Toast: "Statut mis à jour" (success) / "Échec de la mise à jour" (error)
    - Large full-width buttons (min 44px height) per Phase 6 guard-first pattern
    - `accessibilityLabel` on each status button: "Passer au statut {statusName}"
    
    Both screens use: View, Text, StyleSheet from react-native; colors/typography from @repo/design; fetch from mobile/lib/api.ts
  </action>
  <acceptance_criteria>
    - fetchMyIncidents, fetchIncident, updateIncidentStatus functions exist in mobile lib/api.ts
    - MobileIncidentCard renders severity badge, title, zone, time ago, SLA countdown, status badge
    - Incidents list renders FlatList with MobileIncidentCard items, pull-to-refresh, loading/empty/error states
    - Incident detail renders full incident info with status transition buttons
    - Status buttons only shown for valid state transitions
    - Optimistic UI updates with revert on error + toast
    - All French copy matches UI-SPEC copywriting contract (lines 832-852)
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Mobile build not yet runnable. Human verification required.</automated>
  </verify>
  <done>Mobile incident response screens complete — list, detail, status transitions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile→API | Incident status updates sent via REST PATCH /api/incidents/:id/status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Elevation of Privilege | Unauthorized incident status change | mitigate | Server-side IncidentStateMachine validates all transitions per RESEARCH.md Security Domain: "Mobile sends commands, API validates" |
| T-08-08-02 | Spoofing | Stale JWT after role change | mitigate | Existing server-side role resolution from DB (not JWT claims) per Phase 4 guard pattern |
| T-08-08-03 | Tampering | Offline command queuing | accept | Mobile has no offline queue for incident commands (future feature). Commands require active API connection |
| T-08-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages required |
</threat_model>

<verification>
1. Mobile lib/api.ts has fetchMyIncidents, fetchIncident, updateIncidentStatus functions
2. MobileIncidentCard renders with required props
3. Incidents.tsx renders FlatList with data from fetchMyIncidents
4. Incident/[id].tsx renders detail with status transition buttons
5. Status buttons respect IncidentStateMachine valid transitions
6. All French copy matches UI-SPEC
</verification>

<success_criteria>
- [ ] Mobile incident list shows assigned incidents with severity/status/SLA badges
- [ ] Mobile incident detail shows full info with status transition buttons
- [ ] Status transitions call API and update optimistically
- [ ] Loading, empty, and error states render correctly for both screens
- [ ] Guard-first design: large touch targets (min 44px), simplified layout
- [ ] All French copy matches UI-SPEC contract
</success_criteria>

<output>
Create `.planning/phases/08-feature-deepening/08-08-SUMMARY.md` when done
</output>
