---
phase: 08-feature-deepening
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - apps/api/src/modules/door/door-state-machine.ts
  - apps/api/src/modules/door/door.service.ts
  - apps/api/src/modules/door/door.controller.ts
  - apps/api/src/modules/door/door.processor.ts
  - apps/dashboard/components/door-threshold-config.tsx
  - apps/dashboard/app/(dashboard)/portes/page.tsx
  - apps/mobile/app/portes.tsx
  - apps/mobile/components/mobile-door-card.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/lib/api.ts
autonomous: true
requirements: [FTR-02]

must_haves:
  truths:
    - "DoorService reads per-door threshold columns (heldOpenThresholdMs, settlingTimeoutMs) with fallback to DEFAULT_ALERT_CONFIG constants"
    - "Door event deduplication persists last sequence number to Redis (survives restarts)"
    - "DoorController exposes PATCH /doors/:id/thresholds endpoint for per-door config"
    - "Dashboard Portes page shows per-door threshold configuration panel with slider for heldOpenThresholdMs"
    - "Mobile app has a door control screen with open/close/lock buttons and status badges"
  artifacts:
    - path: apps/api/src/modules/door/door.service.ts
      provides: Per-door threshold config with Redis-persisted sequence dedup
      contains: "redis.setex.*door:seq"
    - path: apps/dashboard/components/door-threshold-config.tsx
      provides: Threshold slider UI component
      contains: "heldOpenThresholdMs"
    - path: apps/mobile/app/portes.tsx
      provides: Mobile door control screen
      contains: "Porte"
  key_links:
    - from: door.service.ts
      to: door-state-machine.ts
      via: getOrCreateMachine() config lookup
      pattern: "new DoorStateMachine"
    - from: apps/dashboard/components/door-threshold-config.tsx
      to: apps/dashboard/app/(dashboard)/portes/page.tsx
      via: component import
      pattern: "DoorThresholdConfig"
---

<objective>
Harden the door state machine with per-door configurable thresholds, Redis-persisted sequence deduplication, a threshold configuration UI on the dashboard, and a mobile door control screen.

Purpose: Deliver FTR-02 — door state machine with sequence dedup survives restarts (Redis-persisted), operators configure thresholds per door via dashboard sliders, guards control doors from mobile.
Output: Enhanced door module (state machine, service, controller, processor), DoorThresholdConfig React component, mobile portes.tsx screen with control buttons.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-feature-deepening/08-CONTEXT.md
@.planning/phases/08-feature-deepening/08-RESEARCH.md
@.planning/phases/08-feature-deepening/08-PATTERNS.md
@.planning/phases/08-feature-deepening/08-UI-SPEC.md

<interfaces>
From 08-01-Foundation (in codebase, already merged):
- Door model: new `heldOpenThresholdMs Int?` and `settlingTimeoutMs Int?` columns
- `doorThresholdConfigSchema` in packages/shared (100-30000ms range per D-02, not old 30000-300000)
- `DoorThresholdConfigInput` type exported from @repo/shared
- dashboard api.ts has `fetchDoorThresholds` and `updateDoorThresholds` functions

Existing door module patterns (from PATTERNS.md lines 79-194):
- `DoorStateMachine` (door-state-machine.ts): formal state graph, `shouldGenerateAlert()`, `DEFAULT_ALERT_CONFIG` with `heldOpenThresholdMs: 30000`, `settlingTimeoutMs: 10000`
- `lastSequence = new Map<string, number>()` — in-memory only, must persist to Redis
- `handleDoorStateEvent()`: validates sequence at lines 120-127, calls `shouldGenerateAlert()` for alert decision
- `updateAlertConfig()`: writes to JSON `alertConfig` column — now must also write to dedicated columns
- `getOrCreateMachine()`: creates machines with DEFAULT_ALERT_CONFIG — must now read per-door columns
- DoorProcessor: Redis cooldown at lines 49-59 with `withTenantContext`

Existing Door model: `alertConfig Json @default("{}")` — write to both JSON and new columns for backward compat
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden door state machine with per-door thresholds + Redis-persisted sequence dedup</name>
  <files>
    apps/api/src/modules/door/door-state-machine.ts
    apps/api/src/modules/door/door.service.ts
    apps/api/src/modules/door/door.controller.ts
    apps/api/src/modules/door/door.processor.ts
  </files>
  <read_first>
    apps/api/src/modules/door/door-state-machine.ts (full file — understand DEFAULT_ALERT_CONFIG, DoorAlertConfig, shouldGenerateAlert)
    apps/api/src/modules/door/door.service.ts (full file — understand getOrCreateMachine, handleDoorStateEvent, updateAlertConfig, lastSequence)
    apps/api/src/modules/door/door.controller.ts (full file — understand existing routes, guards, @Roles pattern)
    apps/api/src/modules/door/door.processor.ts (full file — understand Redis cooldown pattern)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 79-194 for exact code patterns)
    .planning/phases/08-feature-deepening/08-RESEARCH.md (lines 396-406 Pitfall 2: alert config migration, lines 424-428 Pitfall 6: sequence dedup across restarts)
  </read_first>
  <action>
    **door-state-machine.ts** — Per D-02, the existing state machine transitions and `DoorAlertConfig` interface are correct (D-02: "Existing state machine transitions remain unchanged — they are correct"). The key change is how config is resolved:
    - Ensure `DoorAlertConfig` interface has heldOpenThresholdMs and settlingTimeoutMs (it likely already does)
    - No changes needed to the state machine class itself — config resolution moves to DoorService

    **door.service.ts** — Make these specific changes:
    
    1. **getOrCreateMachine()** — Modify to read per-door threshold columns. When creating a machine, read `door.heldOpenThresholdMs` and `door.settlingTimeoutMs`. If not null, override DEFAULT_ALERT_CONFIG; otherwise use defaults:
    ```typescript
    const config: DoorAlertConfig = {
      ...DEFAULT_ALERT_CONFIG,
      ...(door.heldOpenThresholdMs != null && { heldOpenThresholdMs: door.heldOpenThresholdMs }),
      ...(door.settlingTimeoutMs != null && { settlingTimeoutMs: door.settlingTimeoutMs }),
    };
    ```
    
    2. **Sequence dedup** (D-03) — Extend `handleDoorStateEvent()` to persist last sequence number to Redis. Add a call after the existing sequence check passes:
    ```typescript
    // In handleDoorStateEvent(), after the sequence dedup check passes (around line 127):
    const seqKey = `door:seq:${doorId}`;
    if (message.sequence) {
      await this.redis.setex(seqKey, 86400, String(message.sequence));
    }
    ```
    
    Add a new method `restoreSequences()` that on startup reads all `door:seq:*` keys from Redis and populates `this.lastSequence`. Call it in `onModuleInit()` or from the module's lifecycle hook.
    
    3. **updateThresholdConfig()** — New method following the existing `updateAlertConfig()` pattern:
    ```typescript
    async updateThresholdConfig(doorId: string, config: { heldOpenThresholdMs?: number; settlingTimeoutMs?: number }) {
      // Validate door exists, throw NotFoundException if not
      // Validate ranges: heldOpenThresholdMs 100-30000, settlingTimeoutMs 100-60000
      // Update BOTH new columns AND existing alertConfig JSON for backward compat
      // Update in-memory machine config if it exists
    }
    ```

    4. **Inject `@InjectRedis()` or use `this.redis`** — DoorService already has Redis access (used in handleDoorStateEvent for cooldown). Verify the import and injection pattern.

    **door.controller.ts** — Add new endpoint:
    - `PATCH /api/doors/:id/thresholds` → `updateThresholdConfig(id, body)`
    - @Roles("ADMIN") — only admins can change door thresholds
    - @Body(new ZodValidationPipe(doorThresholdConfigSchema)) body

    **door.processor.ts** — Per D-03: no changes needed to the alert evaluation logic. The existing Redis cooldown pattern (60s per door+state) is correct and stays. The sequence dedup is handled in door.service.ts, not the processor.
  </action>
  <acceptance_criteria>
    - getOrCreateMachine reads heldOpenThresholdMs/settlingTimeoutMs from door object with fallback to DEFAULT_ALERT_CONFIG
    - handleDoorStateEvent persists last sequence to Redis via `redis.setex("door:seq:{doorId}", 86400, seq)`
    - restoreSequences method reads `door:seq:*` keys from Redis and populates lastSequence map
    - updateThresholdConfig writes to both new columns AND alertConfig JSON for backward compat
    - DoorController has PATCH /doors/:id/thresholds with @Roles("ADMIN") guard
    - TypeScript compilation passes: `npx tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Door API hardening complete — per-door thresholds, Redis sequence dedup, threshold endpoint</done>
</task>

<task type="auto">
  <name>Task 2: Build Dashboard door threshold config UI + mobile door control screen</name>
  <files>
    apps/dashboard/components/door-threshold-config.tsx
    apps/dashboard/app/(dashboard)/portes/page.tsx
    apps/mobile/app/portes.tsx
    apps/mobile/components/mobile-door-card.tsx
    apps/mobile/app/(tabs)/_layout.tsx
    apps/mobile/lib/api.ts
  </files>
  <read_first>
    apps/dashboard/app/(dashboard)/portes/page.tsx (full file — understand existing DoorCard, alert config modal pattern at lines 588-631)
    apps/dashboard/components/glass-card.tsx (Phase 6 pattern)
    apps/mobile/app/(tabs)/_layout.tsx (current tab structure)
    apps/mobile/lib/api.ts (current mobile API client pattern)
    .planning/phases/08-feature-deepening/08-UI-SPEC.md (lines 224-247 for door threshold UI spec, lines 589-631 for mobile door control spec)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 612-626 for dashboard pattern, lines 879-898 for mobile screen pattern)
  </read_first>
  <action>
    **DoorThresholdConfig component** (`apps/dashboard/components/door-threshold-config.tsx`):
    - "use client" component per Phase 6 pattern
    - Props: `doorId: string, initialConfig: DoorThresholdConfigInput, onSaved: () => void`
    - Render an inline config panel inside a GlassCard (per UI-SPEC layout):
      - Section heading: "⚙️ Seuils de la porte"
      - Slider for heldOpenThresholdMs: range 100-30000ms, displays current value
      - Input field for settlingTimeoutMs: range 100-60000ms
      - "Valeur par défaut" fallback badge if value is null
      - [💾 Enregistrer] button calls `updateDoorThresholds(doorId, config)` from api.ts
      - [↺ Réinitialiser] button clears to null (restores org default)
      - Toast on success/failure per UI-SPEC copy
    - Use Radix Slider or native range input, label in French per UI-SPEC copywriting
    - Loading state: skeleton while fetching, disabled on save. Error state: toast revert.
    - Import `fetchDoorThresholds`, `updateDoorThresholds` from `@/lib/api`
    
    **Portes page** (`apps/dashboard/app/(dashboard)/portes/page.tsx`):
    - Integrate DoorThresholdConfig component into the door detail area
    - Add a "Seuils" button/icon on each DoorCard that expands the threshold config panel
    - Follow existing pattern: open inline or in a Dialog overlay
    - The config panel replaces the simple Alert Config modal (lines 588-631 in current page)
    
    **Mobile door control screen** (`apps/mobile/app/portes.tsx`):
    - New Expo Router screen showing a FlatList of doors for the current organization
    - Each item renders a MobileDoorCard with: door name, zone, status badge (color + icon per UI-SPEC), action buttons
    - Action buttons: only show relevant actions per current door state (UNLOCKED shows lock, LOCKED shows unlock, FORCED shows lock)
    - Confirmation dialog before any action: "Confirmer l'action" / "Déverrouiller {doorName} ?" / [Annuler] [Confirmer]
    - API calls via `fetchWithAuth` from mobile/lib/api.ts
    - Add new API functions to mobile/lib/api.ts: `lockDoor(doorId)`, `unlockDoor(doorId)`
    - Socket.IO subscription for real-time status updates (follow existing pattern)
    
    **MobileDoorCard** (`apps/mobile/components/mobile-door-card.tsx`):
    - Props: `door: { id, name, zoneName, status }, onLock, onUnlock`
    - Card layout per UI-SPEC: name, zone, status badge + color, action buttons
    - Follow Phase 6 guard-first patterns: large touch targets (min 44px), full-width buttons
    - French copy per UI-SPEC contract (lines 856-877)
    - `accessibilityLabel` on control buttons per accessibility requirements
    
    **Tab layout** (`apps/mobile/app/(tabs)/_layout.tsx`):
    - No structural tab changes needed — door control is accessed via a quick-action button from the Accueil tab
    - Add a navigation link to `portes.tsx` from the Accueil screen or as a quick-action
  </action>
  <acceptance_criteria>
    - DoorThresholdConfig renders slider for heldOpenThresholdMs (100-30000) and input for settlingTimeoutMs (100-60000)
    - DoorThresholdConfig calls updateDoorThresholds API on save, shows toast on success/error
    - Portes page integrates DoorThresholdConfig in door detail area
    - Mobile portes.tsx renders door list with MobileDoorCard components
    - MobileDoorCard shows status badge, name, zone, and action buttons appropriate to state
    - Mobile door confirmation dialog appears before any destructive action
    - French copy matches UI-SPEC exactly for all elements
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Wave 0 cannot run dashboard/mobile build yet. Human verification required.</automated>
  </verify>
  <done>Dashboard door threshold UI renders correctly, mobile door control screen functional</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile→API | Mobile door control commands sent via REST POST /api/doors/:id/command |
| dashboard→API | Threshold config changes via PATCH /api/doors/:id/thresholds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Elevation of Privilege | Mobile door control commands | mitigate | Server-side state machine validates all transitions (D-19: "Mobile sends commands, API validates"). @Roles guards on POST endpoint |
| T-08-02-02 | Tampering | Door threshold config | mitigate | @Roles("ADMIN") on PATCH endpoint. Zod validation of range (100-30000ms, 100-60000ms) |
| T-08-02-03 | Repudiation | Sequence dedup bypass | mitigate | Sequence number dedup prevents replay. Event logged to TimescaleDB with original sequence |
| T-08-02-04 | Spoofing | Sequence number manipulation | accept | Sequence counters are per-door in Redis (not per-session). Old sequences are simply ignored |
| T-08-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages required |
</threat_model>

<verification>
1. DoorService compiles with updated getOrCreateMachine, updateThresholdConfig, restoreSequences
2. DoorController has PATCH /doors/:id/thresholds endpoint with @Roles("ADMIN")
3. DoorThresholdConfig component accept the expected props and renders the slider
4. Mobile portes.tsx renders a FlatList and calls lock/unlock API endpoints
5. French copy matches UI-SPEC copywriting table
</verification>

<success_criteria>
- [ ] Door state machine reads per-door threshold columns with DEFAULT_ALERT_CONFIG fallback
- [ ] Redis persists last sequence per door; restoreSequences recovers on startup
- [ ] PATCH /doors/:id/thresholds endpoint exists with Zod validation
- [ ] DoorThresholdConfig component shows slider + input + save/reset buttons
- [ ] Mobile portes.tsx has door list with correct status badges and action buttons
- [ ] Confirmation dialog appears before door unlock/lock
</success_criteria>

<output>
Create `.planning/phases/08-feature-deepening/08-02-SUMMARY.md` when done
</output>
