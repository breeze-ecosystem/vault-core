---
phase: 08-feature-deepening
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - apps/api/src/modules/visitor/visitor.service.ts
  - apps/api/src/modules/visitor/visitor.controller.ts
  - apps/api/src/modules/visitor/visitor.module.ts
  - apps/api/src/modules/anpr/anpr.service.ts
  - apps/api/src/modules/anpr/anpr.controller.ts
  - apps/api/src/modules/anpr/anpr.processor.ts
  - apps/dashboard/components/visitor-approval-panel.tsx
  - apps/dashboard/components/anpr-confidence-slider.tsx
  - apps/dashboard/components/allowlist-blocklist-table.tsx
  - apps/dashboard/app/(dashboard)/visiteurs/page.tsx
  - apps/dashboard/app/(dashboard)/vehicules/page.tsx
autonomous: true
requirements: [FTR-03, FTR-05]

must_haves:
  truths:
    - "Visitor pre-registration sends host approval email via Resend with JWT-signed approval link"
    - "Host can approve or deny visitor via dashboard panel; approved visitors receive timed-pass credentials"
    - "ANPR frame processing filters plates below per-org confidence threshold (default 70%) — logs but does not alert"
    - "Recognized plates are correlated with access events and video clips within configurable time windows"
    - "Dashboard ANPR page shows confidence threshold slider and enhanced allowlist/blocklist table with confidence column"
  artifacts:
    - path: apps/api/src/modules/visitor/visitor.service.ts
      provides: Host approval workflow, timed pass support
      contains: "requestHostApproval"
    - path: apps/api/src/modules/anpr/anpr.service.ts
      provides: Per-org confidence threshold filtering, vehicle-event correlation
      contains: "getConfidenceThreshold"
    - path: apps/dashboard/components/visitor-approval-panel.tsx
      provides: Host approval UI with timed pass config
      contains: "Approbation"
  key_links:
    - from: anpr.service.ts processFrame()
      to: Organization.anprConfidenceThreshold
      via: getConfidenceThreshold()
      pattern: "anprConfidenceThreshold"
    - from: visitor.service.ts requestHostApproval()
      to: Resend SDK
      via: sendApprovalEmail()
      pattern: "this.resend.emails.send"
---

<objective>
Deepen visitor management with host approval workflow and expand ANPR/LPR with configurable confidence thresholds and vehicle-event correlation.

Purpose: Deliver FTR-03 (host approval, timed passes) and FTR-05 (confidence scoring, allowlist/blocklist, vehicle-event correlation) — two modules deepened with configurable policies and missing workflow pieces.
Output: Enhanced Visitor module (host approval email via Resend, timed pass QR), enhanced ANPR module (confidence threshold filtering, vehicle-event correlation), dashboard UI components for both.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-feature-deepening/08-CONTEXT.md
@.planning/phases/08-feature-deepening/08-RESEARCH.md
@.planning/phases/08-feature-deepening/08-PATTERNS.md
@.planning/phases/08-feature-deepening/08-UI-SPEC.md

<interfaces>
From 08-01-Foundation (already merged):
- Visit model: approvalStatus, approvedById, approvedAt, timeWindowStart, timeWindowEnd columns
- Organization model: anprConfidenceThreshold Int @default(70)
- hostApprovalSchema, timedPassSchema, anprConfidenceSchema in @repo/shared
- dashboard api.ts: sendHostApproval(visitId), approveVisit(visitId, approved, reason), setTimedPass(visitId, start, end), fetchAnprConfidenceThreshold(orgId), updateAnprConfidenceThreshold(threshold)

Existing patterns (from PATTERNS.md lines 299-369):
- VisitorService: preregister() at lines 67-212, check-in/check-out/cancel flows, follows NestJS module pattern
- AnprService: processFrame() at lines 176-198, evaluatePlate(), recordEvent(), allowlist/blocklist with Redis cache
- AnprProcessor: frame processing worker

Email pattern (from PATTERNS.md lines 901-921):
- InviteService.sendInviteEmail(): Resend SDK, JWT-signed links, French templates
- Follow exactly for visitor approval emails
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add host approval workflow and timed passes to visitor API</name>
  <files>
    apps/api/src/modules/visitor/visitor.service.ts
    apps/api/src/modules/visitor/visitor.controller.ts
    apps/api/src/modules/visitor/visitor.module.ts
  </files>
  <read_first>
    apps/api/src/modules/visitor/visitor.service.ts (full file — understand preregister, checkIn, checkOut, cancel)
    apps/api/src/modules/visitor/visitor.controller.ts (all routes)
    apps/api/src/modules/visitor/visitor.module.ts (imports and providers)
    apps/api/src/modules/organization/invite/invite.service.ts (Resend email pattern for approval emails, lines 302-341)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 299-323 for host approval pattern, lines 901-921 for email pattern)
  </read_first>
  <action>
    **visitor.service.ts** — Add these methods (never remove existing):

    1. **requestHostApproval(visitId: string)** — D-07:
    - Find visit by ID with visitor and host relations included
    - Check approvalStatus is 'pending' (throw BadRequestException if not)
    - Generate JWT approval token (follow invite.ts JWT pattern, 7-day expiry)
    - Create approval URL: `{DASHBOARD_URL}/visiteurs/approve?token={token}`
    - Send email via Resend to host.email with French template (per UI-SPEC copy):
      Subject: "Nouvelle demande de visite — {visitorName}"
      Body: "{hostName}, {visitorName} souhaite vous rendre visite le {date}. Approuvez ou refusez cette demande."
    - If Resend not configured: log warning, proceed without email (same pattern as invite service lines 905-907)
    - Return { sent: true, visitId }

    2. **approveVisit(visitId: string, approved: boolean, userId: string, reason?: string)** — D-07:
    - Find visit, check status is 'pending'
    - Set approvalStatus to 'approved' or 'denied', approvedById = userId, approvedAt = now()
    - If denied and reason provided, log as note
    - If approved: create or activate credential with timeWindowStart/timeWindowEnd (timed pass per D-08)
    
    3. **setTimedPass(visitId: string, timeWindowStart: string, timeWindowEnd: string)** — D-08:
    - Validates timeWindowEnd > timeWindowStart
    - Updates both timeWindow fields on Visit
    - If a credential exists, update its validFrom/validUntil to match
    - Return updated visit

    4. **Modify preregister()** to set approvalStatus = 'pending' on new visits by default (the Visit model default already handles this via @default("pending"))

    **visitor.controller.ts** — Add new endpoints:
    - `POST /api/visitors/:id/request-approval` → requestHostApproval(id), @Roles("ADMIN","SUPERVISOR","OPERATOR")
    - `POST /api/visitors/:id/approve` → approveVisit(id, body), @Roles("ADMIN","SUPERVISOR"), @Body hostApprovalSchema
    - `PATCH /api/visitors/:id/timed-pass` → setTimedPass(id, body), @Roles("ADMIN","SUPERVISOR"), @Body timedPassSchema

    **visitor.module.ts**:
    - Inject notifications service or Resend SDK if not already wired
    - Add any providers needed for email sending (follow invite.module.ts pattern)
  </action>
  <acceptance_criteria>
    - requestHostApproval validates visit status, generates JWT approval URL, sends Resend email
    - approveVisit sets approvalStatus to 'approved'/'denied' with user and timestamp
    - setTimedPass updates timeWindowStart/timeWindowEnd on Visit model
    - preregister creates visits with default approvalStatus 'pending'
    - All new endpoints have @Roles guards per existing pattern
    - French email copy matches UI-SPEC exactly
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Host approval workflow and timed pass API complete</done>
</task>

<task type="auto">
  <name>Task 2: Add per-org confidence threshold and vehicle-event correlation to ANPR API</name>
  <files>
    apps/api/src/modules/anpr/anpr.service.ts
    apps/api/src/modules/anpr/anpr.controller.ts
    apps/api/src/modules/anpr/anpr.processor.ts
  </files>
  <read_first>
    apps/api/src/modules/anpr/anpr.service.ts (full file — understand processFrame, evaluatePlate, recordEvent, allowlist/blocklist)
    apps/api/src/modules/anpr/anpr.controller.ts (all routes)
    apps/api/src/modules/anpr/anpr.processor.ts (all workers)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 326-358 for ANPR pattern)
    .planning/phases/08-feature-deepening/08-RESEARCH.md (lines 588 for confidence threshold state change)
  </read_first>
  <action>
    **anpr.service.ts** — Add/modify these:

    1. **getConfidenceThreshold(orgId: string): Promise<number>** — New method (D-10):
    - Reads `anprConfidenceThreshold` from Organization model
    - Returns value or default 70 (VEHICLE_CONFIDENCE_DEFAULT from constants)
    - Cache result in Redis for 5 minutes to reduce org queries

    2. **Modify processFrame()** — Add confidence threshold filter (D-10):
    - At the start of plate evaluation loop, call `getConfidenceThreshold(organizationId)`
    - For each plateResult: if plateResult.confidence < threshold, call recordEvent with decision='MONITOR' and reason='low-confidence' — log but don't trigger allowlist/blocklist evaluation
    - Only plates >= threshold proceed to full allowlist/blocklist evaluation via evaluatePlate()
    - This is server-side enforcement per RESEARCH.md security domain: "Threshold applied server-side in AnprService.processFrame(), not client-side"

    3. **getVehicleEventCorrelation(plate: string, from: string, to: string)** — New method (D-11):
    - Query TimescaleDB for matching access events within time range where plate correlates
    - Query video_clips hypertable for clips within time range
    - Return correlated events and clips with confidence score
    - Use $queryRaw for hypertable queries (follow analytics.service.ts pattern)

    **anpr.controller.ts** — Add new endpoints:
    - `PUT /api/anpr/threshold` → updateAnprConfidenceThreshold(body), @Roles("ADMIN"), @Body anprConfidenceSchema
    - `GET /api/anpr/threshold` → getConfidenceThreshold(orgId from JWT), @Roles("ADMIN","SUPERVISOR","OPERATOR")
    - `GET /api/anpr/correlate` → getVehicleEventCorrelation(plate, from, to), @Roles("ADMIN","SUPERVISOR")

    **anpr.processor.ts** — D-10: The processor calls processFrame() which already has threshold filtering. No changes needed to the processor itself, but verify processFrame() is the entry point used by the processor.
  </action>
  <acceptance_criteria>
    - getConfidenceThreshold reads Organization.anprConfidenceThreshold with 70% default
    - processFrame() filters plates below threshold but still logs them as 'MONITOR' with 'low-confidence' reason
    - getVehicleEventCorrelation queries access events and video clips within time range
    - PUT /api/anpr/threshold updates per-org threshold with @Roles("ADMIN")
    - GET /api/anpr/correlate exists with @Roles("ADMIN","SUPERVISOR")
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ANPR confidence threshold filtering and vehicle-event correlation API complete</done>
</task>

<task type="auto">
  <name>Task 3: Build visitor approval and ANPR config dashboard UI components</name>
  <files>
    apps/dashboard/components/visitor-approval-panel.tsx
    apps/dashboard/components/anpr-confidence-slider.tsx
    apps/dashboard/components/allowlist-blocklist-table.tsx
    apps/dashboard/app/(dashboard)/visiteurs/page.tsx
    apps/dashboard/app/(dashboard)/vehicules/page.tsx
  </files>
  <read_first>
    apps/dashboard/app/(dashboard)/visiteurs/page.tsx (current visitor page tabs and table structure)
    apps/dashboard/app/(dashboard)/vehicules/page.tsx (current ANPR page with events/lists tabs)
    .planning/phases/08-feature-deepening/08-UI-SPEC.md (lines 291-359 for visitor/ANPR spec)
    .planning/phases/08-feature-deepening/08-PATTERNS.md (lines 860-877 for Phase 6 component pattern)
  </read_first>
  <action>
    **VisitorApprovalPanel** (`apps/dashboard/components/visitor-approval-panel.tsx`):
    - "use client" component
    - Props: `visitId: string, hostUserId?: string, approvalStatus: string, onUpdate: () => void`
    - Shows host selection dropdown (when creating/editing visit) populated from org members
    - When approvalStatus is 'pending': shows "⏳ En attente d'approbation" with [✅ Approuver] [❌ Refuser] action buttons
    - When approved: shows "✅ Approuvé" badge with approvedAt timestamp
    - When denied: shows "❌ Refusé" badge with reason tooltip
    - Timed pass section: start/end DateTimePickers, visible only when approved
    - [➕ Envoyer la demande d'approbation] button → calls sendHostApproval(visitId)
    - Approve/Deny buttons → calls approveVisit(visitId, true/false, reason)
    - Follow UI-SPEC layout exactly (lines 307-319)
    
    **ANPRConfidenceSlider** (`apps/dashboard/components/anpr-confidence-slider.tsx`):
    - "use client" component
    - Renders slider in range 50-100%, default 70%
    - Current value displayed as percentage
    - Help text: "Les plaques sous ce seuil sont journalisées mais ne déclenchent pas d'alerte."
    - Fetches current threshold from `fetchAnprConfidenceThreshold(orgId)` on mount
    - [💾 Enregistrer] button calls `updateAnprConfidenceThreshold(value)`
    - Per UI-SPEC layout (lines 329-341)
    
    **AllowlistBlocklistTable** (`apps/dashboard/components/allowlist-blocklist-table.tsx`):
    - "use client" component
    - Two tabs: "📋 Liste autorisée" and "❌ Liste bloquée"
    - Table columns: Plaque, Type (auto-determined by list), Ajouté le, Confiance min. (for allowlist)
    - Add plate: Dialog with plate input, optional confidence minimum
    - Import CSV: File upload dialog, parses CSV with plate column
    - Remove: confirmation dialog with undo toast
    - Follow UI-SPEC layout (lines 344-358)
    
    **Visiteurs Page** enhancement:
    - Add approval status column ("Approbation") to visits table per UI-SPEC layout (lines 297-304)
    - Status badges: "✅ Approuvé" (green), "⏳ En attente" (amber), "❌ Refusé" (red)
    - Integrate VisitorApprovalPanel in visit create/edit form and visit detail view
    - Add approval action buttons on pending rows
    
    **Véhicules Page** enhancement:
    - Add "Configuration" section below tabs with ANPRConfidenceSlider (per UI-SPEC lines 329-341)
    - Replace existing basic lists with AllowlistBlocklistTable component
    - Add confidence column to vehicle events table showing score vs threshold
    - Color code: green if >= threshold, amber if below threshold
  </action>
  <acceptance_criteria>
    - VisitorApprovalPanel shows host dropdown, approval status badge, approve/deny buttons, timed pass section
    - ANPRConfidenceSlider renders range 50-100% with save button, fetches current value on mount
    - AllowlistBlocklistTable has tabbed UI with add/import/remove actions
    - Visiteurs page shows approval status column and integrates approval panel
    - Véhicules page shows ANPRConfidenceSlider and enhanced allowlist/blocklist
    - All French copy matches UI-SPEC copywriting contract
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Dashboard build not yet runnable. Human verification recommended.</automated>
  </verify>
  <done>Visitor approval and ANPR config UI components complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor→API | Approval JWT token received via email, validated server-side |
| dashboard→API | Host approval/deny, ANPR threshold config, allowlist/blocklist mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Information Disclosure | Approval email token theft | mitigate | JWT-signed approval tokens with 7-day expiry, same pattern as invite tokens (D-07) |
| T-08-04-02 | Spoofing | ANPR confidence threshold bypass | mitigate | Threshold applied server-side in processFrame(), never client-side (per RESEARCH.md Security Domain) |
| T-08-04-03 | Elevation of Privilege | Host approval action without host role | mitigate | approveVisit checks JWT userId matches visit.hostUserId; server-side validation |
| T-08-04-04 | Tampering | Allowlist/blocklist manipulation | mitigate | @Roles("ADMIN") on list mutation endpoints. All changes audited |
| T-08-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages required |
</threat_model>

<verification>
1. VisitorService has requestHostApproval, approveVisit, setTimedPass methods
2. AnprService.processFrame() filters by threshold before full evaluation
3. AnprService.getVehicleEventCorrelation queries across hypertables
4. All new endpoints have @Roles guards
5. VisitorApprovalPanel renders approve/deny buttons + timed pass config
6. AllowlistBlocklistTable renders with add/import/remove actions
</verification>

<success_criteria>
- [ ] Host approval email sent via Resend with JWT link
- [ ] Approval/deny updates Visit.approvalStatus with audit trail
- [ ] Timed passes configured via timeWindowStart/timeWindowEnd
- [ ] ANPR plates below threshold logged as MONITOR (no alert)
- [ ] Vehicle-event correlation query joins across access events and video clips
- [ ] Dashboard UI for visitor approval, ANPR threshold, and allowlist/blocklist complete
</success_criteria>

<output>
Create `.planning/phases/08-feature-deepening/08-04-SUMMARY.md` when done
</output>
