---
phase: 02-hardware-integration
plan: 05
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - apps/dashboard/components/cameras/ptz-controls.tsx
  - apps/dashboard/components/video-player.tsx
autonomous: false
requirements: [HWR-03]

must_haves:
  truths:
    - "PTZ controls (directional pad + zoom slider) appear as overlay on video player for Supervisor+ roles"
    - "PTZ overlay auto-hides after 4 seconds of inactivity"
    - "Directional pad has 44x44px touch targets for pan/tilt with stop center button"
    - "Zoom slider on right edge controls zoom in/out"
    - "Preset thumbnails bar below video shows saved positions with recall capability"
    - "PTZ role-gated: only ADMIN, SUPER_ADMIN, SUPERVISOR can see controls"
    - "No-PTZ cameras show no overlay (hasPtz === false)"
  artifacts:
    - path: "apps/dashboard/components/cameras/ptz-controls.tsx"
      provides: "PTZ directional pad, zoom slider, preset bar, role gate"
      min_lines: 150
    - path: "apps/dashboard/components/video-player.tsx"
      provides: "PTZ overlay integrated into video player with auto-hide"
      min_lines: 10 added
  key_links:
    - from: "apps/dashboard/components/cameras/ptz-controls.tsx"
      to: "apps/dashboard/lib/api.ts"
      via: "ptzContinuousMove, ptzStop, ptzGotoPreset, ptzSavePreset"
      pattern: "ptzContinuousMove|ptzGotoPreset"
    - from: "apps/dashboard/components/video-player.tsx"
      to: "apps/dashboard/components/cameras/ptz-controls.tsx"
      via: "import and use PTZControls component"
      pattern: "PTZControls"
---

<objective>
**Dashboard PTZ Controls Overlay — Directional Pad, Zoom, Presets**

Purpose: Add PTZ camera control capabilities to the Dashboard camera viewer. Create a reusable PTZControls component (directional pad, zoom slider, preset thumbnail bar) and integrate it into the existing VideoPlayer component as an auto-hiding overlay. Role-gated for Supervisor+ per D-16.

Dependencies: Requires Plan 02-03 for PTZ API endpoints (POST /api/cameras/:id/ptz/*) and client functions (ptzContinuousMove, ptzStop, ptzGotoPreset, ptzSavePreset). Does NOT depend on Plan 02-04 (separate UI domain, no file conflicts).
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hardware-integration/02-UI-SPEC.md (sections: Camera Viewer PTZ Overlay [156-188], PTZ controls component [234-258], Motion/Animation patterns [374-383], Accessibility requirements [387-396])
@.planning/phases/02-hardware-integration/02-PATTERNS.md (sections: ptz-controls.tsx [735-765], video-player.tsx [959-998])
@.planning/phases/02-hardware-integration/02-RESEARCH.md (sections: D-06 PTZ overlay, D-07 thumbnail presets, D-16 PTZ access control, Pitfall 4 — PTZ command overwhelm)
@.planning/phases/02-hardware-integration/02-CONTEXT.md (D-06 overlay, D-07 thumbnails, D-16 roles)
</context>

<interfaces>
From apps/dashboard/lib/api.ts — available functions (from Plan 02-03):
```typescript
export async function ptzContinuousMove(cameraId: string, pan: number, tilt: number, zoom: number): Promise<void>
export async function ptzStop(cameraId: string): Promise<void>
export async function ptzGotoPreset(cameraId: string, presetToken: string): Promise<void>
export async function ptzSavePreset(cameraId: string, name: string): Promise<any>
```

From packages/shared/src/types/camera.types.ts (from Plan 02-02):
```typescript
export interface PTZPreset { token: string; name: string; snapshotUrl: string | null; }
export interface PTZCapabilities { hasPtz: boolean; absoluteMove: boolean; continuousMove: boolean; relativeMove: boolean; presets: boolean; }
```

From apps/dashboard/components/video-player.tsx — existing component:
```typescript
interface VideoPlayerProps { cameraId: string; cameraName: string; streamUrl?: string; }
export default function VideoPlayer({ cameraId, cameraName, streamUrl }: VideoPlayerProps) { ... }
```
VideoPlayer currently returns a `<div>` with video element, loading overlay, offline overlay, top bar (camera name + quality), and bottom bar (fullscreen button).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create PTZControls Component — Directional Pad, Zoom Slider, Preset Bar</name>
  <files>
    apps/dashboard/components/cameras/ptz-controls.tsx (NEW)
  </files>
  <read_first>
    apps/dashboard/components/video-player.tsx — existing component pattern (lines 1-248), useCallback usage, lucide-react imports
    .planning/phases/02-hardware-integration/02-UI-SPEC.md lines 156-188 (PTZ Overlay Layout) and 234-258 (PTZ Controls Props)
    .planning/phases/02-hardware-integration/02-PATTERNS.md lines 735-765 (ptz-controls.tsx pattern)
  </read_first>
  <action>
    Create `apps/dashboard/components/cameras/ptz-controls.tsx`:
    ```typescript
    'use client';

    import { useState, useRef, useCallback, useEffect } from 'react';
    import {
      ArrowUp, ArrowDown, ArrowLeft, ArrowRight,
      ZoomIn, ZoomOut, StopCircle, Plus, ChevronDown, ChevronUp,
    } from 'lucide-react';
    import { Button } from '@/components/ui/button';
    import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from '@/components/ui/tooltip';
    import { ptzContinuousMove, ptzStop, ptzGotoPreset, ptzSavePreset } from '@/lib/api';
    import type { PTZPreset } from '@repo/shared'; // or define inline
    ```

    - Props interface:
    ```typescript
    interface PTZControlsProps {
      cameraId: string;
      hasPtz: boolean;
      presets?: PTZPreset[];
      userRole: string;
      onMove?: (pan: number, tilt: number, zoom: number) => void;
      onStop?: () => void;
      onGotoPreset?: (presetToken: string) => void;
      onSavePreset?: (name: string) => void;
      disabled?: boolean;
    }
    ```
    - Role gate: if userRole not in ['ADMIN', 'SUPER_ADMIN', 'SUPERVISOR'] and hasPtz is false, return null
    - State: `presetsExpanded` (boolean), `savingPreset` (boolean), `presetName` (string)

    - Directional pad layout (reusing lucide-react Arrow icons):
    ```html
    <div className="absolute bottom-20 left-4 flex flex-col items-center gap-1">
      {/* Top: ArrowUp */}
      <button className="w-11 h-11 flex items-center justify-center rounded-lg bg-background/80 hover:bg-primary/20 backdrop-blur-sm transition-colors"
        onMouseDown={() => handleMove(0, -1, 0)} onMouseUp={handleStop} onMouseLeave={handleStop}
        aria-label="Pan up">
        <ArrowUp className="h-5 w-5" />
      </button>
      {/* Middle row: Left + Stop + Right */}
      <div className="flex items-center gap-1">
        <button ... onMouseDown={() => handleMove(-1, 0, 0)} ...><ArrowLeft /></button>
        <button ... onMouseDown={handleStop} ...><StopCircle className="h-5 w-5 text-destructive" /></button>
        <button ... onMouseDown={() => handleMove(1, 0, 0)} ...><ArrowRight /></button>
      </div>
      {/* Bottom: ArrowDown */}
      <button ... onMouseDown={() => handleMove(0, 1, 0)} ...><ArrowDown /></button>
    </div>
    ```
    - All direction buttons: 44x44px minimum per WCAG 2.5.8 (w-11 h-11 = 44px)
    - On `onMouseDown`: call `onMove(pan, tilt, zoom)`
    - On `onMouseUp`/`onMouseLeave`: call `onStop()`
    - Client-side rate limiting: use `useRef` for last command time, throttle to max 5 commands/second (200ms minimum gap)
    - The PTZControls should call api.ts functions directly via the callback props

    - Zoom slider (vertical, on right edge):
    ```html
    <div className="absolute right-4 top-1/2 -translate-y-1/2 flex flex-col items-center gap-2">
      <button className="w-11 h-11 flex items-center justify-center rounded-lg bg-background/80 hover:bg-primary/20 backdrop-blur-sm"
        onMouseDown={() => handleMove(0, 0, 1)} onMouseUp={handleStop} aria-label="Zoom in">
        <ZoomIn className="h-5 w-5" />
      </button>
      <div className="w-1 h-24 rounded-full bg-background/40">
        <div className="w-full bg-primary/60 rounded-full" style={{ height: '50%' }} />
      </div>
      <button className="w-11 h-11 ..." onMouseDown={() => handleMove(0, 0, -1)} ...>
        <ZoomOut className="h-5 w-5" />
      </button>
    </div>
    ```

    - Preset thumbnail bar (collapsible, below video):
    ```html
    <div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/80 to-transparent px-3 py-2">
      <button onClick={() => setPresetsExpanded(!presetsExpanded)} className="flex items-center gap-1 text-xs text-white/70 mb-1">
        {presetsExpanded ? <ChevronDown className="h-3 w-3" /> : <ChevronUp className="h-3 w-3" />}
        {presets?.length || 0} présents
      </button>
      {presetsExpanded && (
        <div className="flex gap-2 overflow-x-auto pb-1">
          {presets?.map(p => (
            <button key={p.token} onClick={() => onGotoPreset?.(p.token)}
              className="flex-shrink-0 w-16 h-12 rounded border border-white/10 bg-black/40 hover:border-primary/50 transition-colors overflow-hidden">
              {p.snapshotUrl ? (
                <img src={p.snapshotUrl} alt={p.name} className="w-full h-full object-cover" />
              ) : (
                <div className="w-full h-full flex items-center justify-center text-[10px] text-white/60">{p.name}</div>
              )}
            </button>
          ))}
          <button onClick={() => {
            const name = prompt("Nom du preset");
            if (name) onSavePreset?.(name);
          }} className="flex-shrink-0 w-16 h-12 rounded border border-dashed border-white/20 flex items-center justify-center hover:border-primary/50 transition-colors">
            <Plus className="h-4 w-4 text-white/60" />
          </button>
        </div>
      )}
    </div>
    ```
  </action>
  <verify>
    <automated>cd /home/devuser/projects/oversight-hub/apps/dashboard && npx next build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - ptz-controls.tsx exists with PTZControls component
    - Component returns null if hasPtz === false or userRole < SUPERVISOR (D-16)
    - Directional pad has 4 direction buttons + center stop button, all 44x44px (w-11 h-11)
    - Zoom buttons (+/-) on right side of component
    - Preset bar is collapsible with thumbnail buttons and save (+) button
    - Client-side rate limiting at 5 commands/second (200ms gap)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Integrate PTZControls into VideoPlayer with Auto-Hide Overlay</name>
  <files>
    apps/dashboard/components/video-player.tsx (MODIFY)
  </files>
  <read_first>
    apps/dashboard/components/video-player.tsx — entire file (248 lines), current render structure
    .planning/phases/02-hardware-integration/02-PATTERNS.md lines 959-998 (video-player.tsx extension pattern)
    .planning/phases/02-hardware-integration/02-UI-SPEC.md lines 156-188 (PTZ overlay specs)
  </read_first>
  <action>
    Modify `apps/dashboard/components/video-player.tsx`:
    - Add import at top:
    ```typescript
    import { PTZControls } from './cameras/ptz-controls';
    ```

    - Extend VideoPlayerProps:
    ```typescript
    interface VideoPlayerProps {
      cameraId: string;
      cameraName: string;
      streamUrl?: string;
      // Phase 2 additions
      hasPtz?: boolean;
      ptzPresets?: PTZPreset[];
      userRole?: string;
    }
    ```
    - Where PTZPreset type is used, define it inline or import. For simplicity, define interface inline since the shared package type may not be importable from dashboard:
    ```typescript
    interface PTZPreset { token: string; name: string; snapshotUrl: string | null; }
    ```

    - Add new state variables after existing state:
    ```typescript
    const [showPtz, setShowPtz] = useState(false);
    const hidePtzTimer = useRef<ReturnType<typeof setTimeout> | null>(null);
    const lastPtzCommand = useRef(0);
    ```

    - Add PTZ handler functions (after `toggleFullscreen` around line 158):
    ```typescript
    const handlePtzMove = useCallback(async (pan: number, tilt: number, zoom: number) => {
      // Rate limit: max 5 commands/second
      const now = Date.now();
      if (now - lastPtzCommand.current < 200) return;
      lastPtzCommand.current = now;
      try {
        await ptzContinuousMove(cameraId, pan, tilt, zoom);
      } catch {
        // Silently handle
      }
    }, [cameraId]);

    const handlePtzStop = useCallback(async () => {
      try {
        await ptzStop(cameraId);
      } catch {
        // Silently handle
      }
    }, [cameraId]);

    const handlePtzGotoPreset = useCallback(async (presetToken: string) => {
      try {
        await ptzGotoPreset(cameraId, presetToken);
      } catch {
        // Silently handle
      }
    }, [cameraId]);

    const handlePtzSavePreset = useCallback(async (name: string) => {
      try {
        const result = await ptzSavePreset(cameraId, name);
        if (result) {
          // PTZ presets updated
        }
      } catch {
        // Silently handle
      }
    }, [cameraId]);
    ```

    - Add auto-hide logic (after the fullscreen effect around line 164):
    ```typescript
    const showPtzOverlay = useCallback(() => {
      setShowPtz(true);
      if (hidePtzTimer.current) clearTimeout(hidePtzTimer.current);
    }, []);

    const startHideTimer = useCallback(() => {
      if (hidePtzTimer.current) clearTimeout(hidePtzTimer.current);
      hidePtzTimer.current = setTimeout(() => setShowPtz(false), 4000);
    }, []);
    ```

    - Add PTZ overlay inside the main container div (after the bottom fullscreen button around line 245, before the closing `</div>`):
    ```html
    {/* PTZ Controls Overlay (Phase 2) — role-gated per D-16 */}
    {hasPtz && userRole && ['ADMIN', 'SUPER_ADMIN', 'SUPERVISOR'].includes(userRole) && (
      <div
        className={`absolute inset-0 transition-opacity duration-300 ${
          showPtz ? 'opacity-100' : 'opacity-0 pointer-events-none'
        }`}
        onMouseEnter={showPtzOverlay}
        onMouseMove={showPtzOverlay}
        onMouseLeave={startHideTimer}
        onTouchStart={showPtzOverlay}
      >
        <PTZControls
          cameraId={cameraId}
          hasPtz={hasPtz}
          presets={ptzPresets}
          userRole={userRole}
          onMove={handlePtzMove}
          onStop={handlePtzStop}
          onGotoPreset={handlePtzGotoPreset}
          onSavePreset={handlePtzSavePreset}
          disabled={connectionState !== 'connected'}
        />
      </div>
    )}
    ```

    - Import PTZ library functions:
    ```typescript
    import { ptzContinuousMove, ptzStop, ptzGotoPreset, ptzSavePreset } from '@/lib/api';
    ```

    - Cleanup: clear the hide timer on unmount (add inside the existing `cleanup` callback or in a new effect):
    ```typescript
    useEffect(() => {
      return () => {
        if (hidePtzTimer.current) clearTimeout(hidePtzTimer.current);
      };
    }, []);
    ```

    - Note: The `hasPtz`, `ptzPresets`, and `userRole` props are optional (default to undefined). When not provided, no PTZ overlay renders — maintaining backward compatibility with existing camera page usage.
  </action>
  <verify>
    <automated>cd /home/devuser/projects/oversight-hub/apps/dashboard && npx next build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - VideoPlayer accepts optional hasPtz, ptzPresets, userRole props
    - PTZ overlay renders only when hasPtz=true AND userRole is ADMIN/SUPER_ADMIN/SUPERVISOR
    - PTZ overlay is hidden by default, shows on mouse enter/move, auto-hides after 4s
    - PTZ commands are rate-limited client-side to max 5/sec (200ms gap)
    - PTZControls component is imported and rendered inside the PTZ overlay div
    - Overlay uses transition-opacity duration-300 for smooth show/hide
    - Existing VideoPlayer behavior unchanged when new props not provided
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Visual Verification — PTZ Overlay on Camera View</name>
  <what-built>PTZ controls overlay integrated into the VideoPlayer component with directional pad, zoom slider, preset bar, and role-based visibility</what-built>
  <how-to-verify>
    1. Navigate to Dashboard Cameras page
    2. Click on a camera that has PTZ capabilities (hasPtz=true)
    3. Verify PTZ directional pad + zoom slider appear as overlay on the video
    4. Hover over the video — controls should appear
    5. Move mouse away — controls should auto-hide after 4 seconds
    6. Click a direction button — verify PTZ command is sent
    7. Click STOP center button — verify PTZ stop command is sent
    8. Expand preset bar — verify presets display as thumbnails
    9. Login as OPERATOR role — verify PTZ controls are hidden (D-16)
    10. Open a camera without PTZ capabilities — verify no overlay appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard ↔ User | UI interaction with PTZ controls |
| Dashboard ↔ API | PTZ command HTTP requests to backend (JWT-authenticated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Denial of Service | Rapid PTZ button clicks overwhelm camera | mitigate | Client-side rate limiting at 200ms minimum interval (5 cmd/s) per Pitfall 4 |
| T-02-19 | Elevation of Privilege | Non-supervisor accesses PTZ via UI manipulation | mitigate | Dual gate: UI hides controls (this plan) AND API @Roles prevents direct endpoint calls (Plan 02-03, D-16) |
| T-02-20 | Information Disclosure | PTZ preset thumbnails expose camera positions | mitigate | Preset thumbnails show only camera's current view; no position data exposed in UI |

</threat_model>

<verification>
- PTZControls component renders only for authorized roles and PTZ-capable cameras
- Directional pad buttons are 44x44px minimum (w-11 h-11)
- Client-side rate limit enforces 200ms minimum between commands
- Auto-hide activates after 4 seconds of inactivity
- VideoPlayer backward compatible when new optional props not provided
- `npx next build` compiles without errors
</verification>

<success_criteria>
- PTZControls component with directional pad, zoom, preset bar — complete and functional
- Integrated into VideoPlayer as auto-hiding overlay
- Role-gated (Supervisor+) with client-side check
- Rate-limited at 5 commands/second
- No regression to existing VideoPlayer behavior
</success_criteria>

<output>
Create `.planning/phases/02-hardware-integration/02-05-SUMMARY.md` when done
</output>
