---
phase: 02-vision-pack
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/src/modules/detection/detection.module.ts
  - apps/api/src/modules/detection/detection.controller.ts
  - apps/api/src/modules/detection/detection.service.ts
  - apps/api/src/modules/face-recognition/face-recognition.module.ts
  - apps/api/src/modules/face-recognition/face-recognition.controller.ts
  - apps/api/src/modules/face-recognition/face-recognition.service.ts
  - apps/api/src/modules/camera/camera.controller.ts
  - apps/api/src/modules/camera/camera.service.ts
  - apps/api/src/common/decorators/feature-gate.decorator.ts
  - apps/api/src/modules/ingestion/ingestion.service.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements:
  - VIS-01
  - VIS-05
  - VIS-07
  - VIS-10
  - VIS-11
must_haves:
  truths:
    - "Admin can create, update, and delete detection zones per camera"
    - "Admin can set per-camera detection sensitivity threshold"
    - "Admin can upload face photos and manage whitelist (max 50 faces for VISION)"
    - "Camera sensitivity and zones are passed to AI Preprocessor during detection"
    - "ONVIF discovery endpoint returns available cameras on the network"
    - "Cameras support substream toggle (HD/SD) stored as field"
  artifacts:
    - path: "apps/api/src/modules/detection/"
      provides: "Detection zones and sensitivity CRUD"
    - path: "apps/api/src/modules/face-recognition/"
      provides: "Face whitelist CRUD with embedding passthrough"
    - path: "apps/api/src/modules/camera/camera.service.ts"
      provides: "Extended camera CRUD with sensitivity, zones, substream"
  key_links:
    - from: "detection.controller.ts"
      to: "camera.service.ts"
      via: "CameraService dependency for camera validation"
      pattern: "CameraService"
    - from: "face-recognition.service.ts"
      to: "@repo/shared"
      via: "Zod schemas for validation"
      pattern: "vision"
---

<objective>
Create the NestJS API backend modules for detection zones, face whitelist management, camera sensitivity/substream settings, and ONVIF discovery.

**Purpose:** Deliver VIS-01 (ONVIF discovery endpoint), VIS-05 (substream settings), VIS-07 (face whitelist CRUD), VIS-10 (per-camera sensitivity), and VIS-11 (detection zone CRUD). These APIs are consumed by the dashboard and mobile UIs in Wave 3, and by the AI Preprocessor during detection.

**Output:** DetectionModule, FaceRecognitionModule, extended CameraService/Controller, and new internal endpoints for AI Preprocessor communication.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/devuser/projects/vault-os/.planning/phases/02-vision-pack/02-CONTEXT.md
@/home/devuser/projects/vault-os/.planning/phases/02-vision-pack/02-RESEARCH.md

<interfaces>
From apps/api/src/modules/camera/camera.service.ts (existing):
- findAll(filters) → { data, total, page, limit }
- findById(id) → camera with relations
- create(data) → camera (checks license limits)
- update(id, data) → camera
- remove(id) → deleted camera

From apps/api/src/modules/camera/camera.controller.ts (existing):
- GET /cameras — list cameras
- GET /cameras/:id — get camera
- POST /cameras — create camera (LicenseExpiryGuard, ADMIN/SUPER_ADMIN/SUPERVISOR)
- PATCH /cameras/:id — update camera
- DELETE /cameras/:id — delete camera

From packages/shared/src/schemas/vision.ts (from Plan 01):
- detectionZoneSchema, faceWhitelistSchema

From apps/api/src/common/decorators/feature-gate.decorator.ts:
- @RequiresFeature(key: string) — guards routes by feature flag

From apps/api/src/common/guards/:
- JwtAuthGuard, TenantIsolationGuard, RolesGuard, FeatureGateGuard, LicenseExpiryGuard

From apps/api/src/modules/prisma/prisma.service.ts:
- Global PrismaService singleton
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DetectionModule (zones CRUD + sensitivity config)</name>
  <files>
    apps/api/src/modules/detection/detection.module.ts
    apps/api/src/modules/detection/detection.service.ts
    apps/api/src/modules/detection/detection.controller.ts
    apps/api/src/app.module.ts
  </files>
  <read_first>
    apps/api/src/modules/camera/camera.service.ts (pattern for service with Prisma)
    apps/api/src/modules/camera/camera.controller.ts (pattern for controller with ZodValidationPipe, Roles, audit)
    apps/api/src/modules/camera/camera.module.ts (pattern for module registration)
    apps/api/src/app.module.ts (where to add import)
  </read_first>
  <action>
    Create the DetectionModule with full CRUD for detection zones and camera sensitivity:

    1. Create `apps/api/src/modules/detection/detection.module.ts`:
       - @Module({ controllers: [DetectionController], providers: [DetectionService], exports: [DetectionService] })
       - Import PrismaModule (global — may not need explicit import)

    2. Create `apps/api/src/modules/detection/detection.service.ts`:
       - `findZonesByCamera(cameraId)` → DetectionZone[]
       - `createZone(cameraId, data: { name, polygon, isActive?, sensitivity? })` → DetectionZone — validates camera exists
       - `updateZone(zoneId, data)` → DetectionZone
       - `deleteZone(zoneId)` → void
       - `getDetectionConfig(cameraId)` → returns { confidence, zones[], faceRecognitionEnabled } — used by ingestion service to pass to AI Preprocessor
       - Standard NotFoundException + try/catch Prisma error pattern

    3. Create `apps/api/src/modules/detection/detection.controller.ts`:
       - `GET /cameras/:cameraId/zones` — list zones for camera
       - `POST /cameras/:cameraId/zones` — create zone (ADMIN/SUPER_ADMIN, @RequiresFeature('vision_detection_zones'), ZodValidationPipe(detectionZoneSchema))
       - `PATCH /zones/:id` — update zone
       - `DELETE /zones/:id` — delete zone (ADMIN/SUPER_ADMIN)
       - `GET /cameras/:cameraId/detection-config` — returns sensitivity + zones for AI Preprocessor
       - Use audit logging (AuditService) for create/update/delete operations
       - Follow existing controller pattern: ZodValidationPipe, Roles decorator, audit logging

    4. Update `apps/api/src/app.module.ts` — add DetectionModule to imports array

    Follow existing codebase conventions: single-line guard clauses, French error messages (per D-05), camelCase function names, @Roles decorator for RBAC.
  </action>
  <acceptance_criteria>
    - detection.module.ts, detection.service.ts, detection.controller.ts created
    - All CRUD endpoints for detection zones working
    - GET /cameras/:cameraId/detection-config returns sensitivity + zones
    - Endpoints protected by @Roles and @RequiresFeature decorators
    - NestJS build succeeds with detection module
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx nest build 2>&1 | grep -E "(ERROR|success|Found)" || true && ls src/modules/detection/ | wc -l</automated>
  </verify>
  <done>DetectionModule created with zone CRUD and sensitivity endpoints, NestJS builds successfully</done>
</task>

<task type="auto">
  <name>Task 2: Create FaceRecognitionModule (whitelist CRUD + embedding endpoint)</name>
  <files>
    apps/api/src/modules/face-recognition/face-recognition.module.ts
    apps/api/src/modules/face-recognition/face-recognition.service.ts
    apps/api/src/modules/face-recognition/face-recognition.controller.ts
    apps/api/src/app.module.ts
  </files>
  <read_first>
    apps/api/src/modules/camera/camera.service.ts (pattern for CRUD + license check)
    apps/api/src/modules/camera/camera.controller.ts (pattern for controller)
    packages/shared/src/schemas/vision.ts (faceWhitelistSchema)
    apps/api/src/common/decorators/feature-gate.decorator.ts (@RequiresFeature)
  </read_first>
  <action>
    Create the FaceRecognitionModule for managing face whitelist entries:

    1. Create `apps/api/src/modules/face-recognition/face-recognition.module.ts`:
       - Standard @Module with controller + service
       - Import HttpModule (for AI Preprocessor communication) or use plain fetch

    2. Create `apps/api/src/modules/face-recognition/face-recognition.service.ts`:
       - `findAll(orgId)` → FaceWhitelist[]
       - `create(data: { name, imageBase64 }, orgId)` → FaceWhitelist — checks VISION limit (max 50) via licenseStatus.maxFaces before creating. If limit reached, throw BadRequestException with "Limite de 50 visages atteinte. Passez à BASTION pour débloquer la reconnaissance illimitée."
       - `update(id, data)` → FaceWhitelist
       - `remove(id)` → void
       - `getEmbedding(imageBase64)` — calls AI Preprocessor POST /face/register to extract embedding, stores embeddingBase64 in FaceWhitelist record
       - `getWhitelistForOrg(orgId)` → list of {id, name, embeddingBase64} — this is the internal endpoint AI Preprocessor calls for cache refresh
       - CRITICAL: Enforce VISION max 50 faces via `@RequiresFeature('vision_face_whitelist')` at controller level and double-check in service

    3. Create `apps/api/src/modules/face-recognition/face-recognition.controller.ts`:
       - `GET /visages` — list faces for current org (from JWT tenant)
       - `POST /visages` — create face (uploads image, sends to AI Preprocessor for embedding, stores result)
       - `PATCH /visages/:id` — update face name
       - `DELETE /visages/:id` — delete face
       - `GET /visages/:id` — get single face details
       - `GET /internal/face-whitelist?organizationId=X` — internal endpoint (no auth or internal-auth) used by AI Preprocessor to refresh cache — returns list of {id, name, embedding_base64}
       - All external endpoints: @RequiresFeature('vision_face_whitelist'), ADMIN/SUPER_ADMIN roles
       - Use ZodValidationPipe with faceWhitelistSchema
       - French error messages

    4. Update `apps/api/src/app.module.ts` — add FaceRecognitionModule to imports

    Follow existing conventions: UUID primary keys, audit logging on mutations, @Roles for RBAC.
  </action>
  <acceptance_criteria>
    - face-recognition.module.ts, service.ts, controller.ts created
    - CRUD endpoints for face whitelist
    - Face creation enforces VISION max 50 limit
    - /internal/face-whitelist endpoint returns embeddings for AI Preprocessor
    - NestJS build succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx nest build 2>&1 | grep -E "(ERROR|success|Found)" || true && ls src/modules/face-recognition/ | wc -l</automated>
  </verify>
  <done>FaceRecognitionModule created with whitelist CRUD and internal API endpoint, NestJS builds</done>
</task>

<task type="auto">
  <name>Task 3: Extend CameraService/Controller for ONVIF discovery, sensitivity, substreams</name>
  <files>
    apps/api/src/modules/camera/camera.service.ts
    apps/api/src/modules/camera/camera.controller.ts
  </files>
  <read_first>
    apps/api/src/modules/camera/camera.service.ts (full file)
    apps/api/src/modules/camera/camera.controller.ts (full file)
  </read_first>
  <action>
    Extend the existing CameraService and CameraController:

    1. In `CameraController` — add new endpoints:
       - `POST /cameras/onvif-scan` — starts ONVIF device discovery (ADMIN/SUPER_ADMIN). Body: `{ subnet?: string }`. Returns immediately with a job ID. The scan runs async and results are pushed via WebSocket.
       - `GET /cameras/onvif-results?scanId=X` — polls ONVIF scan results.
       - `PATCH /cameras/:id/substream` — updates camera substreamUrl field (ADMIN/SUPER_ADMIN). Body: `{ substreamUrl: string }`.
       - `GET /cameras/:id/detection-config` — returns { confidence: number, zones: Zone[], faceRecognitionEnabled: boolean } for ingestion service.

    2. In `CameraService` — add methods:
       - `startOnvifScan(subnet?: string)` — triggers async ONVIF probe. For VISION simplicity, use the `onvif` NPM package to send WS-Discovery probe on the subnet. Collect responses (device IP, model, ONVIF version) and store in memory (Map<string, ScanResult[]>). Emit WebSocket event `onvif-scan-progress` for each device found. Return { scanId }.
       - `getOnvifResults(scanId)` → returns current scan results for that scanId.
       - `updateSubstream(cameraId, substreamUrl)` — updates the camera's substreamUrl field.
       - Extend `findById` to include detection zones for the response.

    3. In `CameraService` — extend existing `update()` to handle new fields:
       - The existing `update()` method already accepts Prisma.CameraUpdateInput which should include detectionConfidence and substreamUrl since they're now in the schema.

    For ONVIF, implement a simple async discovery using Node's `dgram` socket for WS-Discovery multicast. If the `onvif` npm package is not available, implement a minimal WS-Discovery probe. The scan runs via a setTimeout-based async loop that collects responses for 15 seconds.

    Follow existing patterns: async methods, PrismaService for DB, audit logging, French error messages, @Roles decorator.
  </action>
  <acceptance_criteria>
    - CameraController has POST /cameras/onvif-scan, GET /cameras/onvif-results, PATCH /cameras/:id/substream, GET /cameras/:id/detection-config
    - CameraService has startOnvifScan, getOnvifResults, updateSubstream methods
    - ONVIF scan runs asynchronously and returns scanId immediately
    - Detection-config endpoint aggregates sensitivity + zones for a camera
    - NestJS build succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx nest build 2>&1 | grep -E "(ERROR|success|Found)" || true && npx ts-node -e "
import { CameraController } from './src/modules/camera/camera.controller';
// Controller class found = endpoints registered
console.log('CameraController:', CameraController.name);
" 2>&1 | head -5