---
phase: 02-vision-pack
plan: 06
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - apps/dashboard/components/live-camera-grid.tsx
  - apps/dashboard/components/substream-toggle.tsx
  - apps/dashboard/components/onvif-scan-panel.tsx
  - apps/dashboard/components/detection-zone-canvas.tsx
  - apps/dashboard/components/sensitivity-slider.tsx
  - apps/dashboard/components/face-upload-dropzone.tsx
  - apps/dashboard/components/face-recognition-badge.tsx
  - apps/dashboard/components/camera-card-premium.tsx
  - apps/dashboard/components/video-player.tsx
  - apps/dashboard/app/(dashboard)/cameras/page.tsx
  - apps/dashboard/app/(dashboard)/cameras/[id]/page.tsx
  - apps/dashboard/app/(dashboard)/cameras/decouverte/page.tsx
  - apps/dashboard/app/(dashboard)/cameras/[id]/zones/page.tsx
  - apps/dashboard/app/(dashboard)/visages/page.tsx
  - apps/dashboard/lib/api.ts
  - apps/dashboard/components/sidebar.tsx
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-05
  - VIS-07
  - VIS-10
  - VIS-11
must_haves:
  truths:
    - "Operator can view live camera feeds in a responsive grid with stream quality toggle"
    - "Operator can scan network for ONVIF cameras and add them"
    - "Operator can draw detection zones on camera snapshot"
    - "Operator can adjust per-camera detection sensitivity"
    - "Admin can upload face photos and manage whitelist (max 50, drag-drop)"
    - "Camera detail page shows live stream, zones, sensitivity, and recent alerts"
  artifacts:
    - path: "apps/dashboard/components/live-camera-grid.tsx"
      provides: "Multi-camera live view grid with status overlays"
    - path: "apps/dashboard/components/detection-zone-canvas.tsx"
      provides: "Interactive polygon drawing canvas"
    - path: "apps/dashboard/components/face-upload-dropzone.tsx"
      provides: "Drag-drop face image upload with preview"
  key_links:
    - from: "live-camera-grid.tsx"
      to: "apps/dashboard/lib/api.ts"
      via: "fetchCameras() and stream URLs"
      pattern: "fetchWithAuth"
    - from: "detection-zone-canvas.tsx"
      to: "POST /cameras/:id/zones"
      via: "API client"
      pattern: "createZone"
---

<objective>
Build the dashboard UI for camera live view, ONVIF discovery, detection zones, sensitivity controls, and face management.

**Purpose:** Deliver VIS-01 (ONVIF scan UI), VIS-02 (live streaming grid), VIS-03 (multi-device), VIS-05 (substream toggle), VIS-07 (face management), VIS-10 (sensitivity slider), VIS-11 (zone drawing). All new components follow the established dark/glass design system per UI-SPEC.

**Output:** 6 new components, 4 new/augmented pages, updated API client.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/devuser/projects/vault-os/.planning/phases/02-vision-pack/02-UI-SPEC.md
@/home/devuser/projects/vault-os/.planning/phases/02-vision-pack/02-CONTEXT.md

<interfaces>
From apps/dashboard/components/video-player.tsx (existing):
- VideoPlayer component with WebRTC/HLS, connection states, fullscreen, PTZ controls
- Props: streamUrl, cameraName, status, onFullscreen

From apps/dashboard/components/camera-card-premium.tsx (existing):
- CameraCardPremium with name, status, thumbnail, actions

From apps/dashboard/components/camera-grid.tsx (existing):
- Responsive camera grid wrapping CameraCardPremium

From apps/dashboard/components/page-header.tsx (existing):
- PageHeader with title, subtitle, actions

From apps/dashboard/app/(dashboard)/cameras/page.tsx (existing):
- Camera list page, currently shows camera cards

From apps/dashboard/lib/api.ts (existing):
- fetchWithAuth() wrapper for all API calls
- getCameras(), getCamera(), createCamera(), updateCamera()

From UI-SPEC:
- PAGE 1: Camera Live Grid — LiveCameraGrid, SubstreamToggle
- PAGE 2: Camera Detail — VideoPlayer, DetectionZoneCanvas, SensitivitySlider
- PAGE 3: ONVIF Scan — ONVIFScanPanel
- PAGE 6: Detection Zones — DetectionZoneCanvas
- PAGE 7: Face Management — FaceUploadDropzone, FaceRecognitionBadge

Design system: shadcn/ui components (Button, Card, Dialog, Badge, Progress, Skeleton, Toast, Avatar), glass morphism (glass, glass-premium CSS classes), motion/react animations, lucide-react icons, French copy per UI-SPEC copywriting contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LiveCameraGrid + SubstreamToggle + augment camera pages</name>
  <files>
    apps/dashboard/components/live-camera-grid.tsx
    apps/dashboard/components/substream-toggle.tsx
    apps/dashboard/components/camera-card-premium.tsx
    apps/dashboard/components/video-player.tsx
    apps/dashboard/app/(dashboard)/cameras/page.tsx
    apps/dashboard/app/(dashboard)/cameras/[id]/page.tsx
    apps/dashboard/lib/api.ts
  </files>
  <read_first>
    apps/dashboard/components/camera-grid.tsx (pattern for grid layout)
    apps/dashboard/components/video-player.tsx (existing component)
    apps/dashboard/components/camera-card-premium.tsx (existing card)
    apps/dashboard/app/(dashboard)/cameras/page.tsx (existing page)
    UI-SPEC PAGE 1 and PAGE 2 specs
  </read_first>
  <action>
    Create and extend dashboard UI for camera live view:

    1. Create `components/live-camera-grid.tsx`:
       - Props: { cameras: Camera[], onCameraClick: (id) => void }
       - Responsive grid: 2-col mobile, 3-col tablet, 4-col desktop using Tailwind grid (`grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-2`)
       - Each tile: aspect-video container, VideoPlayer (or thumbnail if offline), camera name overlay, status dot (green ONLINE, red OFFLINE), recording indicator (red pulsing dot), SubstreamToggle badge
       - Loading state: 8 Skeleton blocks with aspect-video, animate-pulse
       - Empty state: centered glass card with VideoOff icon + "Aucune caméra configurée" + CTA "Ajoutez votre première caméra"
       - Hover effect: slight scale-up (1.02), "Voir le flux" overlay
       - Use motion/react for staggered tile appear animation

    2. Create `components/substream-toggle.tsx`:
       - Props: { current: 'hd' | 'sd', onToggle: () => void }
       - Small clickable badge: "HD" (cyan) or "SD" (muted)
       - On toggle: brief "HD"/"SD" overlay with fade animation (1s display)
       - Uses Badge component from shadcn/ui

    3. Extend `components/video-player.tsx`:
       - Add optional props: substreamUrl, onSubstreamToggle, showRecordingIndicator, recordingActive
       - Add recording indicator overlay (red pulsing dot + "Enregistrement" text)
       - Proper fullscreen handling via browser Fullscreen API

    4. Augment `cameras/page.tsx`:
       - Replace or wrap CameraGrid with LiveCameraGrid
       - PageHeader with title "Caméras", camera count badge, "+ Ajouter" button opening ONVIFScanPanel dialog
       - Toggle between Grid/List view
       - Camera limit banner: if max cameras reached, show warning "Limite de 10 caméras atteinte. Passez au pack BASTION."

    5. Augment `cameras/[id]/page.tsx`:
       - Full-width VideoPlayer with all controls
       - Right panel: camera info, detection settings (sensitivity slider), recording status
       - Bottom section: zones thumbnail link, recent alerts from this camera

    6. Update `lib/api.ts`:
       - Add `getCamerasWithStreams()`, `getCameraDetail(id)`, `updateSubstream(id, substreamUrl)`, `toggleRecording(id)`

    Follow UI-SPEC exactly for copy, states, and component breakdown. All copy in French per D-05.
  </action>
  <acceptance_criteria>
    - LiveCameraGrid renders responsive grid with VideoPlayer tiles
    - SubstreamToggle shows HD/SD badge, calls onToggle
    - Camera page shows live grid with all states (loading, empty, online, offline, limit reached)
    - Camera detail page shows full stream + detection settings
    - No TypeScript errors, dashboard builds
  </acceptance_criteria>
  <verify>
    <automated>cd apps/dashboard && npx next build 2>&1 | grep -E "(Error|error|✓|✔)" || true</automated>
  </verify>
  <done>LiveCameraGrid, SubstreamToggle, augmented camera pages created, dashboard builds</done>
</task>

<task type="auto">
  <name>Task 2: Create ONVIFScanPanel, DetectionZoneCanvas, SensitivitySlider</name>
  <files>
    apps/dashboard/components/onvif-scan-panel.tsx
    apps/dashboard/components/detection-zone-canvas.tsx
    apps/dashboard/components/sensitivity-slider.tsx
    apps/dashboard/app/(dashboard)/cameras/decouverte/page.tsx
    apps/dashboard/app/(dashboard)/cameras/[id]/zones/page.tsx
    apps/dashboard/lib/api.ts
  </files>
  <read_first>
    UI-SPEC PAGE 3 (ONVIF Scan) and PAGE 6 (Detection Zones) specs
    apps/dashboard/components/ui/ (Dialog, Table, Progress, Button, Badge — existing shadcn)
    apps/dashboard/app/(dashboard)/cameras/page.tsx (pattern for page layout)
  </read_first>
  <action>
    Create ONVIF scan, detection zone canvas, and sensitivity components:

    1. Create `components/onvif-scan-panel.tsx`:
       - Props: { open: boolean, onClose, onCameraAdded }
       - Pre-scan: glass card with subnet input (pre-filled from current network), "Lancer le scan" button
       - Scanning: ProgressBar indeterminate + "Scan en cours... {count} caméra(s) trouvée(s)" + animated radar/pulse icon
       - Results: Table showing discovered devices (model, IP, ONVIF version, status badge Compatible/Non compatible), each row has "Ajouter" button
       - Adding device: row shows spinner, on success toast + refresh grid
       - Empty results: "Aucun appareil ONVIF détecté" + troubleshooting tips
       - Error state: "Erreur de scan" + retry button
       - Uses Dialog wrapper for panel display

    2. Create `components/detection-zone-canvas.tsx`:
       - Props: { cameraId, snapshotUrl, zones: Zone[], onSave }
       - Full-width canvas with camera snapshot as background (locked aspect ratio)
       - Drawing toolbar: Rectangle | Polygon | Clear All (as compact icon buttons)
       - Existing zones shown as semi-transparent colored polygons (cyan active, amber paused)
       - Drawing interaction per UI-SPEC: click-drag for rectangle, click-to-place-vertices for polygon, double-click to close
       - Click existing zone to select (highlight border), shows zone name toggle
       - Zone list sidebar in a ScrollArea: zone names with toggle on/off, delete button per zone
       - Sensitivity slider below canvas (1-100 scale)
       - Loading state: skeleton canvas placeholder
       - No zones state: snapshot with "+ Ajouter une zone" overlay

    3. Create `components/sensitivity-slider.tsx`:
       - Props: { value: number, onChange: (v) => void, min?: number, max?: number }
       - Custom range input styled with glass theme
       - Labels: "Faible" left, "Élevée" right
       - Icon indicators: low sensitivity (shield icon), high sensitivity (alert icon)

    4. Create `cameras/decouverte/page.tsx`:
       - Full page with PageHeader "Découverte de caméras"
       - ONVIFScanPanel as main content area
       - Uses PageTransition wrapper

    5. Create `cameras/[id]/zones/page.tsx`:
       - PageHeader "Zones de détection — {camera_name}"
       - DetectionZoneCanvas full-width
       - SensitivitySlider below

    6. Update `lib/api.ts`: add `startOnvifScan(subnet)`, `getOnvifResults(scanId)`, `getZones(cameraId)`, `createZone(cameraId, data)`, `updateZone(id, data)`, `deleteZone(id)`, `updateSensitivity(cameraId, confidence)`, `getDetectionConfig(cameraId)`

    Follow UI-SPEC exactly for all states (loading, empty, drawing, saved, error). Use existing shadcn components for shells. All copy in French.
  </action>
  <acceptance_criteria>
    - ONVIFScanPanel with scan trigger, progress, results table
    - DetectionZoneCanvas with polygon drawing, existing zones overlay, toolbar
    - SensitivitySlider with glass-themed styling
    - Pages created at decouverte/ and zones/ routes
    - Dashboard builds successfully
  </acceptance_criteria>
  <verify>
    <automated>cd apps/dashboard && npx next build 2>&1 | grep -E "(Error|error|✓|✔)" || true</automated>
  </verify>
  <done>ONVIFScanPanel, DetectionZoneCanvas, SensitivitySlider created, dashboard builds</done>
</task>

<task type="auto">
  <name>Task 3: Create FaceUploadDropzone + FaceRecognitionBadge + visages page</name>
  <files>
    apps/dashboard/components/face-upload-dropzone.tsx
    apps/dashboard/components/face-recognition-badge.tsx
    apps/dashboard/app/(dashboard)/visages/page.tsx
    apps/dashboard/lib/api.ts
    apps/dashboard/components/sidebar.tsx
  </files>
  <read_first>
    UI-SPEC PAGE 7 (Face Management) specs
    apps/dashboard/app/(dashboard)/cameras/page.tsx (page pattern)
    apps/dashboard/components/sidebar.tsx (add nav items)
  </read_first>
  <action>
    Create face management UI components and page:

    1. Create `components/face-upload-dropzone.tsx`:
       - Props: { onFileSelected: (file) => void, disabled?: boolean, currentCount: number, maxCount: number }
       - Drag-and-drop zone with dashed border
       - Click to open file picker (accept: .jpg,.jpeg,.png, max 5MB)
       - Shows file preview after selection with crop area (1:1 aspect ratio box)
       - Name input field below preview
       - Upload progress bar
       - States: idle (Déposer une photo ou cliquer pour télécharger), dragging (highlighted), file-selected (preview), uploading (progress), error
       - If limit reached (currentCount >= maxCount): show disabled state with lock icon + "Limite de {maxCount} visages atteinte. Passez à BASTION."

    2. Create `components/face-recognition-badge.tsx`:
       - Props: { status: 'recognized' | 'unknown' | 'pending' | 'error', name?: string, confidence?: number }
       - Small badge overlay: green "Reconnu — {name}" for recognized, yellow "Inconnu" for unknown, red "Erreur" for error
       - Uses Badge component from shadcn/ui with variant mapping

    3. Create `visages/page.tsx`:
       - PageHeader: "Reconnaissance faciale" + counter "{count}/50 visages"
       - Face grid: responsive grid of face cards (Avatar thumbnail + name + status badge + last seen)
       - "+ Ajouter un visage" button opens FaceUploadDropzone in Dialog
       - Search bar: filter by name
       - Empty state: "Aucun visage enregistré" + "Ajoutez des visages à la liste blanche pour activer la reconnaissance"
       - 50 faces reached: disable add button + upgrade BASTION message
       - Delete face: confirmation dialog "Supprimer {name} de la liste blanche ?"
       - Uses FlashList-like grid with Tailwind grid

    4. Update `lib/api.ts`: add `getFaces()`, `addFace(data)`, `deleteFace(id)`, `updateFace(id, data)`

    5. Update `components/sidebar.tsx`: add navigation items:
       - "Visages" link under "Sécurité" nav group
       - "Découverte" link under "Caméras" nav group
       - "Zones" accessible from camera detail

    All copy in French per D-05 and UI-SPEC copywriting contract.
  </action>
  <acceptance_criteria>
    - FaceUploadDropzone with drag-drop, preview, name input, progress
    - FaceRecognitionBadge with 4 states
    - /visages page with grid, search, limit enforcement
    - Sidebar updated with VISION nav items
    - Dashboard builds successfully
  </acceptance_criteria>
  <verify>
    <automated>cd apps/dashboard && npx next build 2>&1 | grep -E "(Error|error|✓|✔)" || true</automated>
  </verify>
  <done>Face management UI created with drag-drop upload, face grid page, sidebar nav, dashboard builds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → Dashboard API | User interactions sent via fetchWithAuth — authenticated by JWT |
| Canvas drawing → API | Zone polygon coordinates sent via API to backend |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | Detection zone polygon input | mitigate | Canvas coordinates validated by Zod schema on the backend (min 3 vertices, numeric values) |
| T-02-16 | Info Disclosure | Face photo upload | accept | Photos stored as base64 in DB; access controlled by existing TenantIsolationGuard and JWT auth |
</threat_model>

<verification>
- [ ] LiveCameraGrid renders responsive grid with VideoPlayer tiles
- [ ] SubstreamToggle shows HD/SD badge
- [ ] ONVIFScanPanel with scan progress and results table
- [ ] DetectionZoneCanvas with polygon drawing and zone list
- [ ] SensitivitySlider with glass styling
- [ ] FaceUploadDropzone with drag-drop, preview, limit enforcement
- [ ] FaceRecognitionBadge with recognized/unknown/pending/error states
- [ ] All pages created at correct routes
- [ ] Sidebar updated with VISION nav items
- [ ] Dashboard builds successfully
</verification>

<success_criteria>
1. Camera grid shows live streams with status indicators and quality toggle
2. ONVIF scan discovers cameras and allows adding them
3. Detection zones can be drawn as polygons on camera snapshot
4. Sensitivity slider adjusts per-camera detection threshold
5. Face whitelist supports drag-drop upload with 50-face limit enforcement
6. All pages in French, dark/glass theme, responsive layout
</success_criteria>

<output>
Create `.planning/phases/02-vision-pack/02-06-SUMMARY.md` when done
</output>
