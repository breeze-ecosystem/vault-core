---
phase: 02-vision-pack
plan: 07
type: execute
wave: 3
depends_on:
  - 02-04
  - 02-05
files_modified:
  - apps/dashboard/components/timeline-filter-bar.tsx
  - apps/dashboard/components/clip-export-button.tsx
  - apps/dashboard/components/stream-share-sheet.tsx
  - apps/dashboard/components/geofencing-status-bar.tsx
  - apps/dashboard/components/geofencing-settings.tsx
  - apps/dashboard/components/dnd-schedule-editor.tsx
  - apps/dashboard/components/recording-settings-form.tsx
  - apps/dashboard/components/alert-channel-config.tsx
  - apps/dashboard/app/(dashboard)/chronologie/page.tsx
  - apps/dashboard/app/(dashboard)/partage/page.tsx
  - apps/dashboard/app/(dashboard)/parametres/enregistrement/page.tsx
  - apps/dashboard/app/(dashboard)/parametres/absence/page.tsx
  - apps/dashboard/app/(dashboard)/parametres/notification-silencieuse/page.tsx
  - apps/dashboard/app/(dashboard)/parametres/alertes/page.tsx
  - apps/dashboard/app/(dashboard)/parametres/acces-distant/page.tsx
  - apps/dashboard/app/(dashboard)/utilisateurs/page.tsx
  - apps/dashboard/lib/api.ts
  - apps/dashboard/components/sidebar.tsx
autonomous: true
requirements:
  - VIS-08
  - VIS-09
  - VIS-12
  - VIS-13
  - VIS-14
  - VIS-15
  - VIS-16
  - VIS-17
  - VIS-19
  - VIS-20
  - VIS-21
  - VIS-22
  - VIS-23
must_haves:
  truths:
    - "Operator can view and filter event timeline by date, camera, event type"
    - "Operator can export 30s video clip around any event"
    - "Admin can configure recording retention (7/15/30), H.264/H.265 toggle"
    - "Admin can configure alert channels (Push/SMS/WhatsApp) with QR code for WhatsApp"
    - "Admin can configure geofencing WiFi networks, arm delay, timeout"
    - "Admin can configure DND schedule with day-of-week time ranges"
    - "Admin can generate and manage stream share links"
    - "Admin can manage secondary user accounts with role assignment"
    - "DDNS configuration guide page available"
  artifacts:
    - path: "apps/dashboard/components/timeline-filter-bar.tsx"
      provides: "Date range + event type + camera select filters"
    - path: "apps/dashboard/components/stream-share-sheet.tsx"
      provides: "Share link generator with duration/camera select"
    - path: "apps/dashboard/components/geofencing-status-bar.tsx"
      provides: "Global arm/disarm status indicator"
  key_links:
    - from: "chronologie/page.tsx"
      to: "apps/dashboard/lib/api.ts"
      via: "searchEvents(params)"
      pattern: "searchEvents"
    - from: "stream-share-sheet.tsx"
      to: "POST /shares"
      via: "API client"
      pattern: "createShare"
---

<objective>
Build the dashboard UI for event timeline, recording settings, alert channels, geofencing, DND, stream sharing, multi-user, and remote access configuration.

**Purpose:** Deliver VIS-08/09 (alert channel config UI), VIS-12/13/17 (recording settings), VIS-14/15/16 (timeline + clip export), VIS-19 (share UI), VIS-20 (geofencing UI), VIS-21 (multi-user), VIS-22 (DND editor), VIS-23 (DDNS guide). Creates 8 new pages and 8 new components following UI-SPEC.

**Output:** 8 new components, 8 new/augmented pages, updated API client, sidebar navigation.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/devuser/projects/vault-os/.planning/phases/02-vision-pack/02-UI-SPEC.md
@/home/devuser/projects/vault-os/.planning/phases/02-vision-pack/02-CONTEXT.md

<interfaces>
From UI-SPEC:
- PAGE 4: Event Timeline — TimelineFilterBar, ClipExportButton, ActivityTimeline (existing)
- PAGE 5: Recording Settings — RecordingSettingsForm
- PAGE 8: Multi-User — extend existing utilisateurs page
- PAGE 9: Stream Sharing — StreamShareSheet
- PAGE 10: Geofencing — GeofencingStatusBar, GeofencingSettings
- PAGE 11: DND — DNDScheduleEditor
- PAGE 12: Alert Channels — AlertChannelConfig
- PAGE 13: DDNS/Remote Access — guide page

From apps/dashboard/lib/api.ts (extend with):
- searchEvents(params), exportClip(eventId), getRecordingConfig(), updateRecordingConfig(data)
- getShares(), createShare(data), revokeShare(id)
- getGeofencingStatus(), getGeofencingConfig(), updateGeofencingConfig(data), forceArm(), forceDisarm()
- getDndSchedule(), updateDndSchedule(data)
- getAlertChannelConfig(), updateAlertChannelConfig(data)
- getHermesStatus(), getModemStatus(), sendTestWhatsApp(), sendTestSms(), getWhatsAppQrCode()

All copy in French per D-05. Design: existing shadcn/ui components, glass morphism, motion/react animations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TimelineFilterBar, ClipExportButton, augment chronologie page</name>
  <files>
    apps/dashboard/components/timeline-filter-bar.tsx
    apps/dashboard/components/clip-export-button.tsx
    apps/dashboard/app/(dashboard)/chronologie/page.tsx
    apps/dashboard/lib/api.ts
  </files>
  <read_first>
    UI-SPEC PAGE 4 spec (timeline layout, states, copy)
    apps/dashboard/components/activity-timeline.tsx (existing component)
    apps/dashboard/app/(dashboard)/chronologie/page.tsx (existing page)
  </read_first>
  <action>
    Create timeline filtering and clip export components:

    1. Create `components/timeline-filter-bar.tsx`:
       - Props: { onFilterChange: (filters) => void, cameras: Camera[] }
       - Date range picker: "Aujourd'hui" / "Hier" / "Personnalisé" buttons, custom opens date input
       - Event type filter chips: "Tous", "Alertes", "Mouvement", "Visage", "Système" (multi-select)
       - Camera selector dropdown (multi-select, with "Toutes les caméras" default)
       - Search input with glass styling
       - Uses shadcn Button, Badge, Select components

    2. Create `components/clip-export-button.tsx`:
       - Props: { eventId: string, disabled?: boolean }
       - Button with Download icon + "Exporter le clip (30s)"
       - States: idle (button), loading (spinner + "Préparation de l'export..."), ready (link + télécharger), error (retry)
       - On click: POST to /events/{eventId}/export, wait for response, trigger download
       - Uses shadcn Button with variant

    3. Augment `chronologie/page.tsx`:
       - PageHeader: "Chronologie"
       - TimelineFilterBar at top
       - ActivityTimeline with infinite scroll, date group headers ("Aujourd'hui", "Hier", {date})
       - Each event card: snapshot thumbnail, event type icon, camera name, timestamp, severity badge
       - Click event → Sheet slide-in: larger snapshot, event details, ClipExportButton
       - States per UI-SPEC: loading (skeleton), empty, filter-no-results, clip-loading, clip-ready, clip-error

    4. Update `lib/api.ts`: add `searchEvents(params)`, `exportClip(eventId)`, `getEventDetail(eventId)`

    Follow UI-SPEC exactly for copy (French), states, and component breakdown.
  </action>
  <acceptance_criteria>
    - TimelineFilterBar with date range, event type chips, camera selector
    - ClipExportButton with idle/loading/ready/error states
    - /chronologie page with filter bar, timeline, event detail sheet
    - Dashboard builds successfully
  </acceptance_criteria>
  <verify>
    <automated>cd apps/dashboard && npx next build 2>&1 | grep -E "(Error|error|✓|✔)" || true</automated>
  </verify>
  <done>Timeline filter bar, clip export, chronologie page created, dashboard builds</done>
</task>

<task type="auto">
  <name>Task 2: Create recording settings, stream sharing, and multi-user pages</name>
  <files>
    apps/dashboard/components/recording-settings-form.tsx
    apps/dashboard/components/stream-share-sheet.tsx
    apps/dashboard/app/(dashboard)/parametres/enregistrement/page.tsx
    apps/dashboard/app/(dashboard)/partage/page.tsx
    apps/dashboard/app/(dashboard)/utilisateurs/page.tsx
    apps/dashboard/lib/api.ts
  </files>
  <read_first>
    UI-SPEC PAGE 5 (Recording), PAGE 9 (Sharing), PAGE 8 (Multi-user) specs
    apps/dashboard/app/(dashboard)/utilisateurs/page.tsx (existing extend)
    apps/dashboard/lib/api.ts (extend for new APIs)
  </read_first>
  <action>
    Create recording settings, stream sharing, and extend multi-user pages:

    1. Create `components/recording-settings-form.tsx`:
       - Props: { config: RecordingConfig, onSave: (config) => void }
       - Section 1 — Rétention: Radio/button group 7j/15j/30j with estimated space display
       - Section 2 — Compression: Switch toggle H.264 (Standard) / H.265/HEVC (Économique) with savings description
       - Section 3 — Emplacement: read-only storage path display
       - Storage usage ProgressBar with warning/critical banners
       - States: loading (skeleton), loaded, saving (spinner), saved (toast), error

    2. Create `components/stream-share-sheet.tsx`:
       - Props: { cameras: Camera[], onShareGenerated: (link) => void }
       - Camera selector (checkboxes or multi-select)
       - Duration: 1h/6h/24h buttons + custom time input
       - "Générer le lien" CTA
       - Result: monospace link display + "Copier" button (navigator.clipboard)
       - Active shares list: link preview, camera names, expires at, status badge, revoke button
       - States: no-shares (empty), link-generated, copied, revoking, revoked
       - Warning text: "Le tiers pourra accéder au flux sans authentification pendant la durée définie."

    3. Create `parametres/enregistrement/page.tsx`:
       - PageHeader "Enregistrement"
       - RecordingSettingsForm as main content
       - Uses PageTransition wrapper

    4. Create `partage/page.tsx`:
       - PageHeader "Partage de flux"
       - StreamShareSheet with camera list from API
       - Uses PageTransition wrapper

    5. Extend `utilisateurs/page.tsx`:
       - Add VISION limit banner: "{current}/{max} utilisateurs secondaires"
       - If limit reached: disable invite button + "Limite de 3 utilisateurs secondaires atteinte. Passez à BASTION pour plus d'utilisateurs."
       - Add invite options: email (opens dialog with email input), SMS (opens dialog with phone input), manual (opens creation form)
       - Role selector at invitation: Admin or Viewer only
       - States per UI-SPEC: table skeleton, invite-pending badge, user-suspended badge

    6. Update `lib/api.ts`: add recording, share, and user invite API functions

    Follow UI-SPEC exactly for copy (French), all states, and component breakdown.
  </action>
  <acceptance_criteria>
    - RecordingSettingsForm with retention, codec, storage sections
    - StreamShareSheet with duration selector, link generation, copy, revoke
    - /parametres/enregistrement page created
    - /partage page created
    - /utilisateurs page extended with VISION limits, invite options
    - Dashboard builds successfully
  </acceptance_criteria>
  <verify>
    <automated>cd apps/dashboard && npx next build 2>&1 | grep -E "(Error|error|✓|✔)" || true</automated>
  </verify>
  <done>Recording settings, stream sharing, multi-user pages created, dashboard builds</done>
</task>

<task type="auto">
  <name>Task 3: Create geofencing, DND, alert channels, and remote access settings pages</name>
  <files>
    apps/dashboard/components/geofencing-status-bar.tsx
    apps/dashboard/components/geofencing-settings.tsx
    apps/dashboard/components/dnd-schedule-editor.tsx
    apps/dashboard/components/alert-channel-config.tsx
    apps/dashboard/app/(dashboard)/parametres/absence/page.tsx
    apps/dashboard/app/(dashboard)/parametres/notification-silencieuse/page.tsx
    apps/dashboard/app/(dashboard)/parametres/alertes/page.tsx
    apps/dashboard/app/(dashboard)/parametres/acces-distant/page.tsx
    apps/dashboard/lib/api.ts
    apps/dashboard/components/sidebar.tsx
  </files>
  <read_first>
    UI-SPEC PAGE 10 (Geofencing), PAGE 11 (DND), PAGE 12 (Alert Channels), PAGE 13 (DDNS) specs
    apps/dashboard/components/sidebar.tsx (add settings nav items)
  </read_first>
  <action>
    Create geofencing, DND, alert channels, and DDNS pages:

    1. Create `components/geofencing-status-bar.tsx`:
       - Props: { armed: boolean, connectedPhones: number, armDelayMinutes: number }
       - Compact banner: "Armé" (green/destructive) or "Désarmé" (muted)
       - Shows connected phone count, arming countdown if in progress
       - Uses animated transition on status change (0.5s)

    2. Create `components/geofencing-settings.tsx`:
       - Section "État actuel": large status indicator, phone list, last change time
       - Section "Réseaux WiFi": SSID list with connect/disconnect status, "Ajouter le réseau actuel" button, manual add
       - Section "Paramètres": arm delay slider (1-30 min), timeout slider (5-30 min), reinforced sensitivity toggle
       - Section "Programmation horaire": day checkboxes + time inputs, force-arm/disarm buttons
       - States per UI-SPEC (loading, active, no phones configured, manual override)

    3. Create `components/dnd-schedule-editor.tsx`:
       - Global enable/disable toggle
       - Schedule editor: day-of-week matrix, time range per day (or "Mêmes horaires pour tous les jours" toggle)
       - Critical alerts override toggle (ON by default)
       - Status indicator: "Mode silencieux actif" / "Mode silencieux inactif"
       - Warning if geofencing overrides: "Le mode absence est prioritaire"

    4. Create `components/alert-channel-config.tsx`:
       - Per-channel glass cards: Push, SMS, WhatsApp
       - Push: toggle (ON default), description
       - SMS: toggle, modem status badge (connecté/détecté), phone number display, test button, cloud fallback toggle
       - WhatsApp: toggle, connection status (connecté / QR code), QR code image display, test button
       - Channel-independent: alert types checkboxes (motion, face, all)
       - States per UI-SPEC: loading, modem-detected, modem-not-detected, whatsapp-connected, whatsapp-disconnected, test-sending, test-sent

    5. Create all 4 settings pages:
       - `parametres/absence/page.tsx` — PageHeader "Mode Absence", GeofencingSettings
       - `parametres/notification-silencieuse/page.tsx` — PageHeader "Notifications silencieuses", DNDScheduleEditor
       - `parametres/alertes/page.tsx` — PageHeader "Canaux d'alerte", AlertChannelConfig
       - `parametres/acces-distant/page.tsx` — PageHeader "Accès distant", status card + DDNS config + step-by-step guide + VPN section

    6. Update `lib/api.ts`: add geofencing, DND, alert channel, modem, hermes API functions

    7. Update `components/sidebar.tsx`: add settings sub-navigation items:
       - "Enregistrement" under Paramètres
       - "Mode Absence" under Paramètres
       - "Notifications silencieuses" under Paramètres
       - "Canaux d'alerte" under Paramètres
       - "Accès distant" under Paramètres

    Follow UI-SPEC exactly for copy (French), all states (loading, empty, error, edge cases), and component breakdown. Use existing shadcn Card, Switch, Button, Badge, Progress, Skeleton, Slider components.
  </action>
  <acceptance_criteria>
    - GeofencingStatusBar and GeofencingSettings with all sections
    - DNDScheduleEditor with day/time matrix, critical override
    - AlertChannelConfig with Push/SMS/WhatsApp channel cards
    - DDNS remote access page with guide
    - 4 new settings pages at correct routes
    - Sidebar updated with all settings nav items
    - Dashboard builds successfully
  </acceptance_criteria>
  <verify>
    <automated>cd apps/dashboard && npx next build 2>&1 | grep -E "(Error|error|✓|✔)" || true</automated>
  </verify>
  <done>All settings pages (geofencing, DND, alert channels, DDNS) created, dashboard builds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Dashboard UI → API | All settings changes flow through authenticated fetchWithAuth calls |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Spoofing | WhatsApp QR code display | accept | QR code retrieved from authenticated API endpoint; only ADMIN roles can view. QR code is for WhatsApp Web pairing, not auth |
| T-02-18 | Tampering | Geofencing SSID config | mitigate | SSIDs validated as non-empty strings; passed to API via Zod-validated PATCH |
| T-02-19 | Info Disclosure | Share link in browser | accept | Share link is intentionally public (no auth). Revocable by owner. HTTPS required per D-20 |
</threat_model>

<verification>
- [ ] TimelineFilterBar with date range, event type chips, camera selector
- [ ] ClipExportButton with idle/loading/ready/error states
- [ ] RecordingSettingsForm with retention, codec, storage sections
- [ ] StreamShareSheet with generation, copy, revoke
- [ ] GeofencingSettings with WiFi config, delay sliders, schedule
- [ ] DNDScheduleEditor with day/time matrix
- [ ] AlertChannelConfig with Push/SMS/WhatsApp cards
- [ ] DDNS remote access with guide
- [ ] Multi-user page with VISION limits and invite options
- [ ] All 8 pages created at correct routes
- [ ] Sidebar updated with all nav items
- [ ] Dashboard builds successfully
</verification>

<success_criteria>
1. Event timeline supports date filtering, event type chips, camera selector, and clip export
2. Recording settings page configures retention period and H.264/H.265 codec
3. Alert channels page shows Push/SMS/WhatsApp with modem status and WhatsApp QR code
4. Geofencing configures trusted WiFi SSIDs, arm delay, and timeout
5. DND schedule editor supports day-of-week time ranges and critical override
6. Stream sharing generates revocable links with configurable duration
7. Multi-user page enforces VISION limit of 3 secondary accounts with upgrade prompt
8. DDNS page provides configuration guide
</success_criteria>

<output>
Create `.planning/phases/02-vision-pack/02-07-SUMMARY.md` when done
</output>
