---
phase: 02-vision-pack
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - services/ai-preprocessor/requirements.txt
  - services/ai-preprocessor/Dockerfile
  - services/ai-preprocessor/app/config.py
  - services/ai-preprocessor/app/main.py
  - services/ai-preprocessor/app/models/face_recogniser.py
  - services/ai-preprocessor/app/routes/face_recognition.py
  - services/ai-preprocessor/app/routes/enhance.py
  - services/ai-preprocessor/app/routes/detection.py
  - services/ai-preprocessor/app/schemas/face_recognition.py
autonomous: false
requirements:
  - VIS-04
  - VIS-06
  - VIS-07
  - VIS-10
  - VIS-11
must_haves:
  truths:
    - "AI Preprocessor can recognise faces against whitelist (max 50 faces, cosine similarity)"
    - "AI Preprocessor enhances night-time camera frames for improved visibility"
    - "Detection pipeline filters humans by class 0 only, applies zone polygons, and does temporal smoothing"
    - "Per-camera confidence threshold is passed from API to AI Preprocessor"
  artifacts:
    - path: "services/ai-preprocessor/app/models/face_recogniser.py"
      provides: "insightface singleton + whitelist cache"
      min_lines: 80
    - path: "services/ai-preprocessor/app/routes/face_recognition.py"
      provides: "POST /face/recognise, POST /face/register, POST /face/refresh-whitelist"
      exports: ["router"]
    - path: "services/ai-preprocessor/app/routes/enhance.py"
      provides: "POST /enhance for night vision"
      exports: ["router"]
    - path: "services/ai-preprocessor/app/schemas/face_recognition.py"
      provides: "Pydantic models for face rec requests/responses"
  key_links:
    - from: "app/routes/detection.py"
      to: "app/models/face_recogniser.py"
      via: "calls get_face_recogniser() on person crops"
      pattern: "get_face_recogniser"
    - from: "app/models/face_recogniser.py"
      to: "app/config.py"
      via: "reads FACE_RECOGNITION_ENABLED, FACE_MATCH_THRESHOLD, NESTJS_API_URL"
      pattern: "settings\\.FACE"
---

<objective>
Extend the AI Preprocessor (Python FastAPI microservice) with face recognition via insightface, night vision enhancement, and improved human-only detection pipeline with zone filtering and temporal smoothing.

**Purpose:** Deliver VIS-04 (night vision), VIS-06 (human-only detection), VIS-07 (face recognition), VIS-10 (sensitivity), and VIS-11 (detection zones) by extending the existing YOLOv12+supervision pipeline with insightface CPU-only face recognition, histogram-based night enhancement, and supervision PolygonZone filtering with temporal smoothing.

**Output:** Face recognition module, night vision enhancement endpoint, extended detection pipeline, Pydantic schemas, updated Dockerfile with model pre-download.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/devuser/projects/vault-os/.planning/phases/02-vision-pack/02-AI-SPEC.md
@/home/devuser/projects/vault-os/.planning/phases/02-vision-pack/02-RESEARCH.md
@/home/devuser/projects/vault-os/.planning/phases/02-vision-pack/02-CONTEXT.md

<interfaces>
From services/ai-preprocessor/app/config.py (existing):
- settings.DETECTION_MODEL = "yolov12n"
- settings.DETECTION_CONFIDENCE = 0.45
- settings.NESTJS_API_URL = "http://api:4000"
- settings.API_V1_PREFIX = "/api/v1"

From services/ai-preprocessor/app/models/detector.py (existing):
- get_detector() → YOLO model singleton
- detect(frame, confidence) → sv.Detections

From services/ai-preprocessor/app/routes/detection.py (existing):
- POST /detect — takes DetectionRequest { camera_id, image_base64, timestamp, confidence, organization_id }
- _notify_nestjs(detections, camera_id, org_id, timestamp) — POST to NestJS /api/internal/detection-fire

From services/ai-preprocessor/app/main.py (existing):
- FastAPI app factory with CORSMiddleware
- app.include_router(detection.router, prefix=settings.API_V1_PREFIX)

Key insightface config from AI-SPEC.md §3:
- FaceAnalysis(name="buffalo_l", providers=["CPUExecutionProvider"]), ctx_id=-1
- match_threshold = 0.48 (configurable)
- embeddings are np.ndarray shape (512,) float32, L2-normalized
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add insightface dependencies, config, and face recogniser singleton</name>
  <files>
    services/ai-preprocessor/requirements.txt
    services/ai-preprocessor/Dockerfile
    services/ai-preprocessor/app/config.py
    services/ai-preprocessor/app/models/face_recogniser.py
    services/ai-preprocessor/app/schemas/face_recognition.py
  </files>
  <read_first>
    services/ai-preprocessor/requirements.txt (append new deps)
    services/ai-preprocessor/Dockerfile (add model pre-download step)
    services/ai-preprocessor/app/config.py (add face rec config)
    services/ai-preprocessor/app/models/detector.py (copy lazy singleton pattern)
  </read_first>
  <action>
    Four file modifications for insightface integration:

    1. `services/ai-preprocessor/requirements.txt` — append:
       ```
       # ── Phase 2: VISION Pack — Face Recognition ──
       insightface>=0.7.3
       onnxruntime>=1.17.0
       ```

    2. `services/ai-preprocessor/Dockerfile` — add AFTER existing pip install:
       ```
       # Pre-download insightface model to avoid cold-start latency
       RUN python -c "from insightface.app import FaceAnalysis; FaceAnalysis(name='buffalo_l', providers=['CPUExecutionProvider'])"
       ```
       Also add a VOLUME line: `VOLUME /root/.insightface` to persist model across restarts.

    3. `services/ai-preprocessor/app/config.py` — add after `WHISPER_DEVICE`:
       ```python
       # ── Face Recognition (insightface) ────────────────────────────
       FACE_RECOGNITION_ENABLED: bool = True
       FACE_MATCH_THRESHOLD: float = 0.48
       FACE_WHITELIST_REFRESH_INTERVAL: int = 60  # seconds
       MIN_FACE_SIZE: int = 80  # minimum face crop dimension in px
       ```

    4. Create `services/ai-preprocessor/app/models/face_recogniser.py` — insightface singleton + whitelist cache:
       - Lazy-loaded `_face_app: FaceAnalysis | None = None` — explicitly CPU-only: `FaceAnalysis(name="buffalo_l", providers=["CPUExecutionProvider"])`, `ctx_id=-1`
       - `_whitelist_cache: list[dict] = []` — list of `{id, name, embedding: np.ndarray}`
       - `get_face_recogniser()` → FaceAnalysis singleton (same lazy pattern as `get_detector()`)
       - `refresh_whitelist_cache()` — async function fetching from `{settings.NESTJS_API_URL}/api/internal/face-whitelist`, returning list of `{id, name, embedding_base64}`, decoding base64 to np.float32 array
       - `match_whitelist(embedding: np.ndarray) -> tuple[float, str | None]` — linear cosine similarity scan over _whitelist_cache, returns (best_sim, best_id) or (-1, None)
       - `prune_stale_buffers()` — periodic cleanup of tracker buffers not seen in 30+ seconds
       - Use `import cv2` for BGR conversion, `import numpy as np` for embedding ops
       - Per AI-SPEC pitfalls: set providers=["CPUExecutionProvider"], SINGLETON pattern (not per-request), pre-download in Dockerfile

    5. Create `services/ai-preprocessor/app/schemas/face_recognition.py` — Pydantic models per AI-SPEC §4b:
       - `FaceWhitelistEntry` — id, name, embedding_base64, created_at, updated_at
       - `FaceRecognitionRequest` — camera_id, image_base64, timestamp?, organization_id, min_face_size=80, match_threshold=0.48
       - `FaceMatch` — bbox, confidence, identity?, identity_id?, similarity?
       - `FaceRecognitionResponse` — faces: list[FaceMatch], camera_id, processing_time_ms, total_whitelist_size
       - `FaceRegistrationRequest` — name (1-100 chars), image_base64, organization_id
  </action>
  <acceptance_criteria>
    - requirements.txt has insightface and onnxruntime added
    - Dockerfile pre-downloads buffalo_l model
    - config.py has FACE_RECOGNITION_ENABLED, FACE_MATCH_THRESHOLD, etc.
    - face_recogniser.py implements the singleton + whitelist cache pattern per AI-SPEC
    - face_recognition.py Pydantic schemas match AI-SPEC §4b
    - No syntax errors in any Python file
  </acceptance_criteria>
  <verify>
    <automated>cd services/ai-preprocessor && python -c "from app.config import settings; print('FACE_MATCH_THRESHOLD:', settings.FACE_MATCH_THRESHOLD)" 2>&1 | grep -q "FACE_MATCH_THRESHOLD" && echo "CONFIG_OK"; python -c "from app.schemas.face_recognition import FaceRecognitionRequest; print('SCHEMAS_OK:', FaceRecognitionRequest.__name__)" 2>&1</automated>
  </verify>
  <done>insightface dependencies, config, singleton, and Pydantic schemas created and importable</done>
</task>

<task type="auto">
  <name>Task 2: Create face recognition and night vision enhancement endpoints</name>
  <files>
    services/ai-preprocessor/app/routes/face_recognition.py
    services/ai-preprocessor/app/routes/enhance.py
    services/ai-preprocessor/app/main.py
  </files>
  <read_first>
    services/ai-preprocessor/app/routes/detection.py (copy the router + Pydantic + _notify_nestjs pattern)
    services/ai-preprocessor/app/main.py (extend include_router)
    services/ai-preprocessor/app/models/face_recogniser.py (just created)
  </read_first>
  <action>
    Create two new route files and update main.py:

    1. Create `services/ai-preprocessor/app/routes/face_recognition.py`:
       - APIRouter with prefix (no prefix — main.py adds via API_V1_PREFIX)
       - `POST /face/recognise` endpoint:
         - Accepts FaceRecognitionRequest
         - Decode base64 → PIL → numpy RGB frame
         - Run YOLO detection to find persons (reuse detect() from app.models.detector)
         - For each person bbox (max 1 per frame per CPU budget — take highest confidence), crop and convert to BGR
         - Run `get_face_recogniser().get(crop_bgr)` — skip if crop < min_face_size
         - Compare each face embedding against whitelist via `match_whitelist()`
         - Return FaceRecognitionResponse
         - Per AI-SPEC pitfalls: use `run_in_executor` for CPU-bound ops, skip face rec on dark frames (mean pixel < 20), cooldown per tracker_id (5s)
       - `POST /face/register` endpoint — accepts FaceRegistrationRequest, extracts embedding via insightface, returns embedding_base64
       - `POST /face/refresh-whitelist` — triggers whitelist cache refresh from NestJS API
       - All endpoints use `loop.run_in_executor(None, ...)` for CPU-bound insightface calls
       
    2. Create `services/ai-preprocessor/app/routes/enhance.py`:
       - APIRouter
       - `POST /enhance` endpoint:
         - Accepts `{ image_base64: str, mode: str = "night" }`
         - Decode base64 → numpy RGB
         - Apply histogram equalization (cv2.equalizeHist on Y channel of YUV) + mild CLAHE
         - Return enhanced image as base64 JSON
         - Processing time <100ms target on CPU
       - For now, a lightweight enhancement (frame differencing + histogram equalization), NOT a deep learning model

    3. Update `services/ai-preprocessor/app/main.py` — add:
       ```python
       from app.routes import face_recognition, enhance
       app.include_router(face_recognition.router, prefix=settings.API_V1_PREFIX)
       app.include_router(enhance.router, prefix=settings.API_V1_PREFIX)
       ```

    Follow existing patterns: async def endpoints, base64 decode using PIL/Image, httpx for NestJS calls, logger for debug/info.
  </action>
  <acceptance_criteria>
    - face_recognition.py has /face/recognise, /face/register, /face/refresh-whitelist endpoints
    - enhance.py has /enhance endpoint with histogram equalization
    - main.py includes both new routers
    - All endpoints use run_in_executor for CPU-bound ops
    - Syntax valid: `python -c "from app.routes.face_recognition import router"` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd services/ai-preprocessor && python -c "
from app.routes.face_recognition import router as fr_router
from app.routes.enhance import router as enh_router
routes = [r.path for r in fr_router.routes] + [r.path for r in enh_router.routes]
assert '/face/recognise' in routes, 'Missing /face/recognise'
assert '/face/register' in routes, 'Missing /face/register'
assert '/enhance' in routes, 'Missing /enhance'
print('ALL_ROUTES_OK:', sorted(routes))
" 2>&1</automated>
  </verify>
  <done>Face recognition and night vision enhancement endpoints created and importable</done>
</task>

<task type="auto">
  <name>Task 3: Extend existing detection pipeline with zone filtering and temporal smoothing</name>
  <files>
    services/ai-preprocessor/app/routes/detection.py
  </files>
  <read_first>
    services/ai-preprocessor/app/routes/detection.py (full file — extend the /detect endpoint)
    services/ai-preprocessor/app/config.py (DETECTION_CONFIDENCE, MIN_FACE_SIZE)
    services/ai-preprocessor/app/models/face_recogniser.py (get_face_recogniser, match_whitelist)
  </read_first>
  <action>
    Extend `services/ai-preprocessor/app/routes/detection.py`:

    1. Add new request fields to `DetectionRequest`:
       - `detection_zones: list[list[list[float]]] = []` — list of polygons, each polygon is list of [x,y] vertices
       - `enable_face_recognition: bool = False`
       - `enable_temporal_smoothing: bool = True`

    2. Add new response fields to `DetectionResponse`:
       - `face_matches: list[dict] = []` — per-detection face match results
       - `zone_hits: list[bool] = []` — per-detection whether it was inside a zone
       - `enhancement_applied: bool = False`

    3. Import `sv.PolygonZone` from supervision and `get_face_recogniser`, `match_whitelist` from face_recogniser.

    4. Extend `detect_objects()` POST /detect logic (AFTER tracking, BEFORE building results):
       - **Zone filtering:** If `detection_zones` is non-empty, convert each polygon to `sv.PolygonZone` with `frame_resolution_wh=(frame.shape[1], frame.shape[0])`. Create a boolean mask where any detection in any zone passes. Drop detections outside all zones.
       - **Temporal smoothing:** If `enable_temporal_smoothing`, use an in-memory dict `_frame_buffer: dict[tuple[str, int], list[bool]]` keyed by (camera_id, tracker_id). Maintain a rolling window of 5 detections. Only include detections with >=3 true in last 5 frames (stable). Update buffer per detection. Prune stale entries (>30s).
       - **Face recognition:** If `enable_face_recognition` and face recogniser available, for each person detection (class_id=0), crop the bbox region (expand top by 20% for face), convert to BGR, run `get_face_recogniser().get(crop_bgr)`, and for each face compare against whitelist via `match_whitelist()`. Apply cooldown per tracker_id (5s since last face match). Process at most 1 person crop per frame (highest confidence).
       - **Dark frame skip:** If frame mean pixel value < 20, skip face recognition entirely.
       
    5. Extend `_notify_nestjs()` payload to include `face_matches` and `zone_hits`.

    Use `run_in_executor` for CPU-bound YOLO+insightface operations. Follow AI-SPEC §4 Core Pattern for the temporal smoothing buffer implementation. Per AI-SPEC pitfalls: always run YOLO/insightface via executor to avoid blocking FastAPI event loop.
  </action>
  <acceptance_criteria>
    - /detect endpoint accepts detection_zones, enable_face_recognition, enable_temporal_smoothing params
    - /detect response includes face_matches, zone_hits, enhancement_applied
    - Zone filtering uses sv.PolygonZone to filter detections
    - Temporal smoothing maintains per-tracker rolling buffer, minimum 3/5 positive frames
    - Face recognition runs on person crops when enabled, max 1 crop per frame
    - Dark frame (mean pixel < 20) skips face recognition
  </acceptance_criteria>
  <verify>
    <automated>cd services/ai-preprocessor && python -c "
from app.routes.detection import DetectionRequest, DetectionResponse
req = DetectionRequest(camera_id='test', image_base64='', organization_id='org1')
print('REQUEST_FIELDS:', list(req.model_dump().keys()))
# Check zone filtering works with mock
import numpy as np
import supervision as sv
polygon = np.array([[0,0], [100,0], [100,100], [0,100]])
zone = sv.PolygonZone(polygon=polygon, frame_resolution_wh=(640, 480))
print('POLYGON_ZONE_OK:', zone)
" 2>&1</automated>
  </verify>
  <done>Detection pipeline extended with zone filtering, temporal smoothing, and face recognition</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Camera frame → AI Preprocessor | Base64-encoded image data crosses HTTP boundary |
| AI Preprocessor → NestJS API | Detection results sent via internal HTTP webhook |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | DoS | YOLO/insightface CPU-bound inference | mitigate | run_in_executor prevents event loop blocking; max 1 face crop per frame; temporal cooldown per tracker_id; dark frame skip |
| T-02-05 | Tampering | Base64 image decode | mitigate | try/except around base64 decode and PIL open; HTTPException(400) on invalid image |
| T-02-06 | Info Disclosure | Face embeddings in memory | accept | Embeddings stored in-memory for inference; never logged; encrypted in PostgreSQL via NestJS API |
| T-02-SC | Tampering | pip package installs | mitigate | HUMAN VERIFY REQUIRED — checkpoint before install: insightface (ASSUMED) and onnxruntime (ASSUMED) from PyPI |
</threat_model>

<verification>
- [ ] requirements.txt has insightface>=0.7.3 and onnxruntime>=1.17.0
- [ ] Dockerfile pre-downloads buffalo_l model
- [ ] config.py has FACE_RECOGNITION_ENABLED, FACE_MATCH_THRESHOLD, etc.
- [ ] face_recogniser.py singleton + whitelist cache imports correctly
- [ ] schemas/face_recognition.py Pydantic models defined
- [ ] face_recognition.py route file with /face/recognise, /face/register, /face/refresh-whitelist
- [ ] enhance.py route file with /enhance endpoint
- [ ] main.py includes both new routers
- [ ] detection.py extended with zone filtering, temporal smoothing, face rec
- [ ] All Python files import without errors
</verification>

<success_criteria>
1. AI Preprocessor can recognise faces against whitelist with CPU-only insightface
2. Per-frame processing stays <500ms for single-person-with-face scenario
3. Zone filtering correctly excludes detections outside configured polygons
4. Temporal smoothing prevents flickering alerts (<3/5 frames = no alert)
5. Night vision enhancement endpoint returns improved frame
6. Human verifies insightface and onnxruntime package legitimacy before install
</success_criteria>

<output>
Create `.planning/phases/02-vision-pack/02-02-SUMMARY.md` when done
</output>
