---
phase: 04-commercial-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-03
  - 04-04
files_modified:
  - apps/api/src/modules/camera/camera.service.ts
  - apps/api/src/modules/camera/camera.controller.ts
  - apps/api/src/modules/camera/camera.module.ts
  - apps/api/src/modules/door/door.service.ts
  - apps/api/src/modules/door/door.controller.ts
  - apps/api/src/modules/door/door.module.ts
  - apps/api/src/modules/door/door.gateway.ts
  - apps/api/src/modules/door/door.processor.ts
  - apps/api/src/modules/zone/zone.service.ts
  - apps/api/src/modules/zone/zone.controller.ts
  - apps/api/src/modules/incident/incident.service.ts
  - apps/api/src/modules/incident/incident.controller.ts
  - apps/api/src/modules/incident/incident.module.ts
  - apps/api/src/modules/incident/incident.gateway.ts
  - apps/api/src/modules/alert/alert.service.ts
  - apps/api/src/modules/alert/alert.controller.ts
  - apps/api/src/modules/alert/alert.module.ts
  - apps/api/src/modules/alert/alert.processor.ts
  - apps/api/src/modules/user/user.service.ts
  - apps/api/src/modules/user/user.controller.ts
  - apps/api/src/modules/user/user.module.ts
  - apps/api/src/modules/access/access.service.ts
  - apps/api/src/modules/access/access.controller.ts
  - apps/api/src/modules/access/access.module.ts
  - apps/api/src/modules/access/access.gateway.ts
  - apps/api/src/modules/access/access.processor.ts
  - apps/api/src/modules/visitor/visitor.service.ts
  - apps/api/src/modules/visitor/visitor.controller.ts
  - apps/api/src/modules/visitor/visitor.module.ts
  - apps/api/src/modules/visitor/visitor.gateway.ts
  - apps/api/src/modules/anpr/anpr.service.ts
  - apps/api/src/modules/anpr/anpr.controller.ts
  - apps/api/src/modules/anpr/anpr.processor.ts
  - apps/api/src/modules/vehicle/vehicle.service.ts
  - apps/api/src/modules/vehicle/vehicle.controller.ts
  - apps/api/src/modules/correlation/correlation.service.ts
  - apps/api/src/modules/correlation/correlation.processor.ts
  - apps/api/src/modules/ai/ai.service.ts
  - apps/api/src/modules/ai/ai.processor.ts
  - apps/api/src/modules/analytics/analytics.service.ts
  - apps/api/src/modules/analytics/analytics.module.ts
  - apps/api/src/modules/analytics/analytics.gateway.ts
  - apps/api/src/modules/risk/risk.service.ts
  - apps/api/src/modules/risk/risk.module.ts
  - apps/api/src/modules/risk/risk.gateway.ts
  - apps/api/src/modules/patterns/patterns.service.ts
  - apps/api/src/modules/patterns/patterns.processor.ts
  - apps/api/src/modules/equipment/equipment.service.ts
  - apps/api/src/modules/governance/governance.service.ts
  - apps/api/src/modules/governance/governance.processor.ts
  - apps/api/src/modules/maintenance/maintenance.service.ts
  - apps/api/src/modules/ingestion/ingestion.service.ts
  - apps/api/src/modules/inference/inference.processor.ts
  - apps/api/src/modules/supervision/supervision.service.ts
  - apps/api/src/modules/notification/notification.service.ts
  - apps/api/src/modules/notifications/notifications.processor.ts
  - apps/api/src/modules/dashboard/dashboard.service.ts
  - apps/api/src/modules/chat/chat.service.ts
  - apps/api/src/modules/audit/audit.service.ts
  - apps/api/src/modules/audit/audit.processor.ts
  - apps/api/src/modules/tailgating/tailgating.processor.ts
  - apps/api/src/app.module.ts
  - apps/api/src/common/filters/all-exceptions.filter.ts
  - apps/api/src/common/dto/index.ts
  - apps/dashboard/lib/api.ts
  - apps/dashboard/lib/auth-client.ts
  - apps/dashboard/lib/auth-context.tsx
  - apps/mobile/lib/api.ts
  - apps/mobile/lib/auth-client.ts
  - apps/mobile/lib/auth-context.tsx
autonomous: true
requirements:
  - FND-01
  - FND-02
user_setup: []

must_haves:
  truths:
    - "No TypeScript file references `siteId`, `site_id`, or `site` as a Prisma relation in any service or controller"
    - "No import references `Site` as a Prisma model type"
    - "All Prisma queries use `organizationId` not `siteId`"
    - "All Prisma relation navigations use `.organization` not `.site`"
    - "BullMQ job data types reference `orgId` not `siteId`"
    - "Socket.IO room names use `org:${orgId}` not `site:${siteId}`"
    - "TypeScript compilation passes across all apps/packages"
  artifacts:
    - path: "apps/api/src/modules/**/*.ts"
      provides: "All module files with siteId→organizationId rename complete"
    - path: "apps/api/src/app.module.ts"
      provides: "SiteModule replaced with OrganizationModule in imports"
  key_links:
    - from: "Every service file"
      to: "Prisma extension"
      via: "organizationId auto-injection"
      pattern: "Prisma extension transparently scopes queries — no manual WHERE needed"
---

<objective>
Perform the mechanical rename of `siteId` → `organizationId`, `site` → `organization` (Prisma relations), `Site` → `Organization` (type imports), and `site_id` → `organization_id` (raw SQL/TimescaleDB references) across all ~55 remaining API source files, dashboard/mobile API clients, and app.module.ts. Replace SiteModule with OrganizationModule in AppModule imports. This implements the clean break from D-03 — no backward compatibility layer.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-commercial-foundation/04-CONTEXT.md (D-01, D-03, D-04 — clean break rename)
@.planning/phases/04-commercial-foundation/04-RESEARCH.md (Pitfall 3 lines 956-964 — rename misses indirect references; Migration Strategy lines 1146-1181)
@.planning/phases/04-commercial-foundation/04-PATTERNS.md (lines 1041-1118 — BullMQ processor and Socket.IO gateway patterns)
@apps/api/src/app.module.ts (current state — Plan 03 already updated middleware, Plan 04 created OrganizationModule)

<interfaces>
Key rename targets:
- `siteId` → `organizationId` in all TypeScript files
- `site_id` → `organization_id` in raw SQL strings within code
- `site\.` → `organization.` for Prisma relation navigation (e.g., `camera.site` → `camera.organization`, `door.site` → `door.organization`)
- `import.*Site` → `import.*Organization` for Prisma type imports
- `Prisma.SiteWhereInput` → `Prisma.OrganizationWhereInput`
- `Prisma.SiteCreateInput` → `Prisma.OrganizationCreateInput`
- `Prisma.SiteUpdateInput` → `Prisma.OrganizationUpdateInput`
- `site: { connect: { id } }` → `organization: { connect: { id } }` in Prisma mutation args
- Room names: `site:${siteId}` → `org:${orgId}` in gateway files
- Job data: `siteId` → `orgId` in processor files
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate Prisma Client and replace SiteModule with OrganizationModule in AppModule</name>
  <files>apps/api/src/app.module.ts</files>
  <read_first>apps/api/src/app.module.ts</read_first>
  <action>
    A. Regenerate Prisma Client:
       Run in apps/api: `npx prisma generate`
       This is needed because Plan 01 changed the schema and Plan 03/04 may have been written before regeneration.

    B. Update apps/api/src/app.module.ts:
       1. REPLACE SiteModule import (line 12): `import { SiteModule } from './modules/site/site.module';` → `import { OrganizationModule } from './modules/organization/organization.module';`
       2. REPLACE SiteModule in imports array (line 65): `SiteModule,` → `OrganizationModule,`
       3. ADD InviteModule import and to imports:
          `import { InviteModule } from './modules/organization/invite/invite.module';`
          Add `InviteModule,` to imports array

    C. Verify no SiteModule reference remains:
       Run: `grep -n "SiteModule" apps/api/src/app.module.ts` — should return empty
  </action>
  <verify>
    <automated>grep -c "SiteModule" apps/api/src/app.module.ts | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - app.module.ts imports OrganizationModule (not SiteModule)
    - app.module.ts imports InviteModule
    - OrganizationModule and InviteModule are in the imports array
    - `grep "SiteModule" apps/api/src/app.module.ts` returns empty
    - Prisma Client regenerated successfully
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mechanical rename — siteId→organizationId across ALL remaining API source files (~60 files)</name>
  <files>apps/api/src/modules/*/**.ts</files>
  <read_first>apps/api/src/modules/camera/camera.service.ts</read_first>
  <read_first>apps/api/src/modules/door/door.service.ts</read_first>
  <action>
    Execute systematic rename across all files in apps/api/src/ that reference `siteId`, `site_id`, Prisma `Site` types, or `.site` relations. This is a mechanical operation — use grep to find all occurrences, then edit each file.

    STEP-BY-STEP PROCESS:

    1. DISCOVERY: List all files with siteId references:
       `grep -rl "siteId\|site_id\|\.site\b\|import.*Site\|Prisma\.Site" apps/api/src/modules/ apps/api/src/common/ --include="*.ts" | sort`
       Read each file, note the specific occurrences.

    2. RENAME PATTERNS (apply to each file):
       A. `siteId` → `organizationId` (in TypeScript identifiers, string literals, object keys)
       B. `site_id` → `organization_id` (in SQL strings inside $queryRaw/$executeRaw)
       C. `.site` → `.organization` (Prisma relation navigation — e.g., `camera.site` → `camera.organization`)
       D. `site: { connect:` → `organization: { connect:` (Prisma create/update args)
       E. `import { Site }` → `import { Organization }` (type imports)
       F. `Prisma.SiteWhereInput` → `Prisma.OrganizationWhereInput`
       G. `Prisma.SiteCreateInput` → `Prisma.OrganizationCreateInput`
       H. `Prisma.SiteUpdateInput` → `Prisma.OrganizationUpdateInput`
       I. `include: { site:` → `include: { organization:`
       J. `select: { site:` → `select: { organization:`

    3. KEY FILES TO HANDLE (read each before editing):
       - camera.service.ts: camera queries reference siteId in WHERE, site in include
       - door.service.ts/door.gateway.ts/door.processor.ts: door queries, room names, job data
       - incident.service.ts/incident.controller.ts/incident.gateway.ts
       - alert.service.ts/alert.processor.ts
       - user.service.ts: user queries reference siteId (now removed from User model — remove siteId from User queries entirely)
       - zone.service.ts
       - vehicle/vehicleList queries
       - access.service.ts/access.gateway.ts/access.processor.ts
       - visitor.service.ts/visitor.gateway.ts
       - anpr.service.ts/anpr.processor.ts
       - correlation.service.ts/correlation.processor.ts
       - ai.service.ts/ai.processor.ts
       - analytics.service.ts/analytics.gateway.ts
       - risk.service.ts/risk.gateway.ts
       - patterns.service.ts/patterns.processor.ts
       - equipment.service.ts
       - governance.service.ts/governance.processor.ts
       - maintenance.service.ts
       - ingestion.service.ts
       - inference.processor.ts
       - supervision.service.ts
       - notification.service.ts
       - notifications.processor.ts
       - dashboard.service.ts
       - chat.service.ts
       - audit.service.ts/audit.processor.ts
       - tailgating.processor.ts
       - NESTED SUBFILES: look for .gateway.ts, .processor.ts files in each module directory

    4. VERIFY after all edits:
       `grep -rn "siteId\|\.site\b" apps/api/src/modules/ apps/api/src/common/ --include="*.ts" | grep -v "node_modules" | grep -v "organizationId"`
       Should return zero results (except possibly in comments).

    DO NOT modify:
    - apps/api/src/modules/auth/* (already updated in Plan 04)
    - apps/api/src/modules/organization/* (new files from Plan 04)
    - apps/api/src/modules/prisma/* (already updated in Plan 03)
    - apps/api/src/common/middleware/* (already updated in Plan 03)
    - apps/api/src/common/guards/* (TenantIsolationGuard from Plan 03)
    - apps/api/src/common/decorators/* (CurrentOrg from Plan 03)
  </action>
  <verify>
    <automated>grep -rn "siteId\b" apps/api/src/modules/camera/ apps/api/src/modules/door/ apps/api/src/modules/incident/ apps/api/src/modules/alert/ apps/api/src/modules/user/ --include="*.ts" | grep -v "organizationId" | grep -v '^#' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - All module service files reference `organizationId` (not `siteId`) in Prisma queries
    - All Prisma relation navigation uses `.organization` (not `.site`)
    - All `import { Site }` replaced with `import { Organization }` or removed
    - Prisma type references use `Organization*Input` (not `Site*Input`)
    - `grep -rn "siteId\|\.site\b" apps/api/src/modules/ --include="*.ts" | grep -v "node_modules" | grep -v "//.*site"` returns zero results for files NOT in auth/organization/prisma/middleware/guards/decorators directories
    - User service queries no longer reference `siteId` (field removed from User model)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update BullMQ processors — add orgId propagation and Socket.IO gateways — org-scoped rooms</name>
  <files>apps/api/src/modules/*/*.processor.ts, apps/api/src/modules/*/*.gateway.ts</files>
  <read_first>apps/api/src/modules/door/door.processor.ts</read_first>
  <read_first>apps/api/src/modules/door/door.gateway.ts</read_first>
  <read_first>apps/api/src/common/helpers/tenant-worker.ts</read_first>
  <action>
    A. BULLMQ PROCESSORS (12 files in apps/api/src/modules/**/*.processor.ts):
       For EACH processor (door, inference, audit, anpr, governance, patterns, ai, tailgating, correlation, notifications, access, incident):

       1. Import withTenantContext from "../../common/helpers/tenant-worker" (adjust relative path per file depth)
       2. At the TOP of each processing method (before any DB query), add:
          ```
          if (job.data.orgId) {
            await withTenantContext(this.prisma, job.data.orgId, async () => {
              // existing processing logic
            });
          } else {
            this.logger.warn(`Job ${job.id} missing orgId — skipping`);
            return;
          }
          ```
       3. Update job data TypeScript interfaces to include `orgId: string` if they have type definitions
       4. Already renamed siteId→orgId in job data references from Task 2

    B. SOCKET.IO GATEWAYS (6 files in apps/api/src/modules/**/*.gateway.ts):
       For EACH gateway (door, access, incident, visitor, analytics, risk):

       1. In handleConnection(client):
          - Extract orgId from JWT handshake auth: `const orgId = client.handshake.auth?.orgId;`
          - Store on client data: `client.data.orgId = orgId;`
          - If no orgId, optionally disconnect: `if (!orgId) client.disconnect();`

       2. In SubscribeMessage handlers and @OnEvent emitters:
          - Replace room names: `site:${siteId}` → `org:${orgId}` (using `client.data.orgId`)
          - On gateway emit: `this.server.to(`org:${orgId}`).emit(...)` instead of `site:${siteId}`

       3. The @WebSocketGateway decorator namespace stays unchanged (e.g., "/ws/doors")

       Follow PATTERNS.md lines 1085-1117 for exact patterns.
  </action>
  <verify>
    <automated>grep -c "withTenantContext" apps/api/src/modules/door/door.processor.ts | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - All 12 processor files use withTenantContext() before DB queries
    - All 6 gateway handleConnection() extracts orgId from JWT handshake
    - Room names use `org:${orgId}` format (not `site:${siteId}`)
    - No processor silently processes jobs without orgId — either wraps in withTenantContext or logs warning and returns
    - Gateway emit events scoped to org room
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update Dashboard and Mobile API clients and auth contexts for orgId</name>
  <files>apps/dashboard/lib/api.ts, apps/dashboard/lib/auth-client.ts, apps/dashboard/lib/auth-context.tsx, apps/mobile/lib/api.ts, apps/mobile/lib/auth-client.ts, apps/mobile/lib/auth-context.tsx</files>
  <read_first>apps/dashboard/lib/api.ts</read_first>
  <read_first>apps/dashboard/lib/auth-client.ts</read_first>
  <read_first>apps/dashboard/lib/auth-context.tsx</read_first>
  <read_first>apps/mobile/lib/api.ts</read_first>
  <read_first>apps/mobile/lib/auth-client.ts</read_first>
  <read_first>apps/mobile/lib/auth-context.tsx</read_first>
  <action>
    Update Dashboard and Mobile client libraries to support organization context. These are preparatory changes — full org switcher UI comes in Plans 08/09.

    A. apps/dashboard/lib/auth-client.ts:
       1. ADD switchOrganization function:
          ```typescript
          export async function switchOrganization(orgId: string): Promise<AuthResult> {
            const token = getAccessToken();
            const res = await fetch(`${API_URL}/api/auth/switch-org`, {
              method: "POST",
              headers: { "Content-Type": "application/json", ...(token ? { Authorization: `Bearer ${token}` } : {}) },
              credentials: "include",
              body: JSON.stringify({ organizationId: orgId }),
            });
            const data = await res.json();
            if (!res.ok) return { error: data.message || "Échec du changement d'organisation" };
            if (typeof window !== "undefined") {
              sessionStorage.setItem("accessToken", data.accessToken);
              sessionStorage.setItem("user", JSON.stringify(data.user));
              sessionStorage.setItem("organization", JSON.stringify(data.organization));
            }
            return { accessToken: data.accessToken, user: data.user };
          }
          ```

       2. UPDATE login() to also store organization:
          Add after setting accessToken: `sessionStorage.setItem("organization", JSON.stringify(data.organization));`

       3. ADD getOrganization() helper:
          ```typescript
          export function getOrganization() {
            if (typeof window === "undefined") return null;
            const raw = sessionStorage.getItem("organization");
            return raw ? JSON.parse(raw) : null;
          }
          ```

    B. apps/dashboard/lib/auth-context.tsx:
       1. ADD to AuthContextType interface:
          ```typescript
          organization: { id: string; name: string } | null;
          organizations: Array<{ id: string; name: string; role: string }>;
          switchOrganization: (orgId: string) => Promise<void>;
          ```
       2. ADD state: `const [organization, setOrganization] = useState(...)`
       3. ADD state: `const [organizations, setOrganizations] = useState<Array<...>>([])`
       4. In init (useEffect): after getting user, also read organization from sessionStorage
       5. ADD switchOrganization method that calls auth-client's switchOrganization, updates context state, redirects to /dashboard
       6. UPDATE logout to also clear organization from sessionStorage and context

    C. apps/dashboard/lib/api.ts:
       1. UPDATE Site interface → Organization interface (add billing fields)
       2. ADD fetchOrganizations(), createOrganization() functions
       3. UPDATE any fetchSites() → fetchOrganizations()
       4. UPDATE API URL paths: `/api/sites` → `/api/organizations`
       5. ADD fetchInvites(orgId), createInvite(orgId, data) functions

    D. apps/mobile/lib/api.ts, auth-client.ts, auth-context.tsx:
       Same changes as dashboard but using `expo-secure-store` instead of sessionStorage.
       Follow existing mobile patterns (import from @/lib/auth-storage for token persistence).
  </action>
  <verify>
    <automated>grep -c "switchOrganization" apps/dashboard/lib/auth-client.ts | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard auth-client has switchOrganization(), getOrganization(), stores org on login
    - Dashboard auth-context has organization state, organizations array, switchOrganization method
    - Dashboard api.ts has Organization interface and fetchOrganizations() function
    - Mobile auth-client/sdk has equivalent switchOrganization and org storage
    - Mobile auth-context has equivalent organization state
    - All API URL references to /api/sites changed to /api/organizations
    - No `siteId` references remain in dashboard/mobile lib files
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renamed code → Prisma extension | After rename, all queries use organizationId; extension auto-injects WHERE clause |
| BullMQ job data → Worker | orgId propagated from HTTP producer to queue consumer via job.data |
| WebSocket handshake → Gateway | orgId extracted from JWT during connection; room scoping enforces isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-22 | Information Disclosure | Incomplete rename — missed siteId reference | mitigate | Mechanical grep verification catches remaining references. TypeScript compilation catches type-level mismatches (Site vs Organization types). |
| T-04-23 | Denial of Service | BullMQ jobs without orgId | mitigate | Processors check `if (job.data.orgId)` before processing; warn and return if missing. RLS blocks any DB access without session var anyway. |
| T-04-24 | Information Disclosure | Socket.IO events to wrong org room | mitigate | Room names use `org:${orgId}` extracted from JWT at connection. Client cannot join arbitrary rooms — gateway controls room membership. |
| T-04-25 | Information Disclosure | Dashboard API client — stale org context | mitigate | switchOrganization() updates sessionStorage tokens immediately. Old tokens become invalid when user switches org (new tokens issued). |
</threat_model>

<verification>
1. `grep -rn "siteId" apps/api/src/modules/ --include="*.ts" | grep -v "organizationId" | grep -v node_modules | grep -v "// " | wc -l` — should be 0
2. `grep -rn "\.site\b" apps/api/src/modules/ --include="*.ts" | grep -v "\.organization" | grep -v node_modules | wc -l` — should be 0
3. `grep -rn "import.*Site\b" apps/api/src/modules/ --include="*.ts" | grep -v node_modules | wc -l` — should be 0
4. `npx tsc --noEmit --project apps/api/tsconfig.json` — compiles without errors (workdir=apps/api)
5. `grep -c "withTenantContext" apps/api/src/modules/door/door.processor.ts` — processor has worker helper
6. `grep -c "org:\${" apps/api/src/modules/door/door.gateway.ts` — gateway uses org rooms
</verification>

<success_criteria>
All 55+ API source files use organizationId (not siteId). All Prisma relation navigation uses .organization. All type imports use Organization. BullMQ processors wrap DB queries in withTenantContext(). Socket.IO gateways scope rooms to org:{orgId}. Dashboard and Mobile API clients reference /api/organizations. TypeScript compilation passes.
</success_criteria>

<output>
Create `.planning/phases/04-commercial-foundation/04-05-SUMMARY.md` when done
</output>
