---
phase: 10-enterprise-grade
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/api/src/modules/compliance/compliance.module.ts
  - apps/api/src/modules/compliance/compliance.controller.ts
  - apps/api/src/modules/compliance/compliance.service.ts
  - apps/api/src/modules/compliance/templates/soc2-report.hbs
  - apps/api/src/modules/compliance/templates/iso27001-report.hbs
  - apps/api/src/modules/compliance/templates/access-review.hbs
  - apps/api/src/modules/governance/governance.service.ts
  - apps/api/src/modules/governance/governance.controller.ts
  - apps/api/src/modules/organization/organization.service.ts
  - apps/api/src/modules/license/license.service.ts
autonomous: true
requirements:
  - ENT-02
  - ENT-03
  - ENT-07
must_haves:
  truths:
    - "Admin generates a SOC 2 compliance report as a downloadable PDF"
    - "Admin generates an ISO 27001 compliance summary as a downloadable PDF"
    - "Admin generates a periodic access review report (who has access to what, role assignments, last activity)"
    - "Retention policies support data classification labels (PII, security, audit, operational)"
    - "Retention policies support pre-purge export (PDF/CSV archive before deletion)"
    - "License generation supports multi-currency selection (USD, EUR, XOF, GBP, JPY)"
    - "Organization supports white-label fields (logoUrl, primaryColor, displayName)"
  artifacts:
    - path: "apps/api/src/modules/compliance/compliance.service.ts"
      provides: "PDF report generation using PDFKit + Handlebars"
      contains: "generateReport"
      contains: "PDFDocument"
      contains: "Handlebars.compile"
    - path: "apps/api/src/modules/compliance/templates/"
      provides: "Handlebars templates for SOC 2, ISO 27001, and Access Review reports"
    - path: "apps/api/src/modules/governance/governance.service.ts"
      provides: "Retention policy classification + pre-purge export logic"
      contains: "classification"
      contains: "exportBeforePurge"
    - path: "apps/api/src/modules/license/license.service.ts"
      provides: "Multi-currency license generation"
      contains: "currency"
  key_links:
    - from: "apps/api/src/modules/compliance/compliance.service.ts"
      to: "apps/api/src/modules/incident/incident.service.ts"
      via: "PDFKit + Handlebars PDF generation pattern"
      pattern: "new PDFDocument"
    - from: "apps/api/src/modules/governance/governance.service.ts"
      to: "apps/api/prisma/schema.prisma — RetentionPolicy model"
      via: "classification + exportBeforePurge fields"
      pattern: "classification"
---

<objective>
Build compliance reporting (SOC 2, ISO 27001, Access Review) using PDFKit + Handlebars following the existing incident closure report pattern. Extend retention policies with data classification labels and pre-purge export configuration. Add multi-currency support to license generation and white-label fields to organization settings.

Purpose: Enterprise customers can generate compliance evidence for audits, classify data for retention, price licenses in their local currency, and customize platform branding.
Output: ComplianceModule with 3 PDF report types, GovernanceService extension with classification + export, LicenseService multi-currency, OrganizationService white-label fields.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-enterprise-grade/10-CONTEXT.md
@.planning/phases/10-enterprise-grade/10-RESEARCH.md
@.planning/phases/10-enterprise-grade/10-PATTERNS.md

<interfaces>
From apps/api/src/modules/incident/incident.service.ts (lines 675-789) — PDFKit + Handlebars pattern:
```typescript
import PDFDocument from "pdfkit";
import Handlebars from "handlebars";

// Compile Handlebars template
const template = Handlebars.compile(templateSource);
const html = template({ ...reportContext, generatedAt: new Date().toLocaleString("fr-FR") });

// Generate PDF
return new Promise<Buffer>((resolve, reject) => {
  const doc = new PDFDocument({ margin: 50, size: "A4" });
  const buffers: Buffer[] = [];
  doc.on("data", (chunk: Buffer) => buffers.push(chunk));
  doc.on("end", () => resolve(Buffer.concat(buffers)));
  doc.on("error", reject);
  doc.fontSize(20).font("Helvetica-Bold").text("Title", { align: "center" });
  doc.moveDown();
  doc.fontSize(10).font("Helvetica").text("Content...");
  doc.end();
});
```

From apps/api/src/modules/governance/governance.service.ts — RetentionPolicy CRUD (lines 145-176):
```typescript
async createPolicy(dto: { eventType: string; tableType: string; retentionDays: number; enabled?: boolean }) {
  return this.prisma.retentionPolicy.create({ data: { ... } });
}
async listPolicies() {
  return this.prisma.retentionPolicy.findMany({ orderBy: { createdAt: "desc" } });
}
async updatePolicy(id: string, dto: { retentionDays?: number; enabled?: boolean }) {
  return this.prisma.retentionPolicy.update({ where: { id }, data: dto });
}
```

From apps/api/src/modules/organization/organization.service.ts — Organization update:
```typescript
async update(id: string, data: Prisma.OrganizationUpdateInput) {
  await this.findById(id);
  return this.prisma.organization.update({ where: { id }, data });
}
```

From apps/api/src/modules/license/license.service.ts — License generation (lines 30-50):
```typescript
async generateLicense(dto: { organizationId: string; maxCameras: number; maxDoors: number; expiresAt: string; gracePeriodDays?: number; licenseVersion?: number }) {
  const claims: LicenseClaims = { organizationId, issuedAt, expiresAt, maxCameras, maxDoors, gracePeriodDays, licenseVersion };
  const licenseJwt = jwt.sign(claims, privateKey, { algorithm: "RS256", issuer: "oversight-hub" });
}
```

CONTEXT.md decisions:
- D-18: Reuse PDFKit + Handlebars from incident closure reports — no new dependencies
- D-19: Three report types: SOC 2 evidence package, ISO 27001 compliance summary, Access Review
- D-20: Retention policies global (org-scoped). Add classification labels (PII, security, audit, operational). Extend existing GovernanceService.
- D-21: Pre-purge export option + auto-delete. Export archive as PDF/CSV, stored as downloadable file. Audit event for every purge.
- D-22: White labeling — logoUrl, primaryColor, displayName on Organization. Gated by `custom_branding` feature flag.
- D-16: License currency set at generation (USD, EUR, XOF, GBP, JPY). Stored in License JWT claims.
- D-17: Admin-side only — no exchange rate API. Currency field in License model.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ComplianceModule — PDF report generation for SOC 2, ISO 27001, Access Review</name>
  <files>
    apps/api/src/modules/compliance/compliance.module.ts,
    apps/api/src/modules/compliance/compliance.controller.ts,
    apps/api/src/modules/compliance/compliance.service.ts,
    apps/api/src/modules/compliance/templates/soc2-report.hbs,
    apps/api/src/modules/compliance/templates/iso27001-report.hbs,
    apps/api/src/modules/compliance/templates/access-review.hbs
  </files>
  <read_first>apps/api/src/modules/incident/incident.service.ts</read_first>
  <read_first>apps/api/src/modules/governance/governance.module.ts</read_first>
  <read_first>apps/api/src/modules/audit/audit.service.ts</read_first>
  <read_first>apps/api/prisma/schema.prisma</read_first>
  <action>
Create ComplianceModule at `apps/api/src/modules/compliance/`:

**`compliance.module.ts`** — Follow governance.module.ts pattern. No BullMQ needed. Controllers: [ComplianceController]. Providers: [ComplianceService]. Exports: [ComplianceService].

**`compliance.service.ts`** — Follow the EXACT PDFKit + Handlebars pattern from incident.service.ts (lines 675-789). Inject PrismaService for data queries. Methods:

- `generateReport(orgId: string, reportType: "soc2" | "iso27001" | "access-review", dateRange?: { from: Date; to: Date })`: 
  1. Query relevant data based on reportType:
     - SOC 2: Audit logs (queryAuditLog from AuditService pattern), incident summaries, user access events, security control evidence
     - ISO 27001: ISMS controls status, risk assessment data, policy compliance records, incident response metrics
     - Access Review: OrganizationMember list with roles, last activity timestamps, permission matrix per user
  2. Read the corresponding Handlebars template file (fs.readFileSync from templates/ directory)
  3. Compile template with Handlebars.compile(templateSource)
  4. Render HTML with report context (org name, generatedAt, data arrays)
  5. Generate PDF via PDFKit: new PDFDocument({ margin: 50, size: "A4" }) → buffer aggregation pattern → return Buffer
  6. Return PDF buffer as `{ buffer, filename: "soc2-report-2026.pdf" }`

- `getReportData(orgId: string, reportType: string)`: Shared data aggregation — queries AuditService, PrismaService (organizationMember, incident, user), and compiles report context object.

**Handlebars templates** — Create three `.hbs` files in `apps/api/src/modules/compliance/templates/`. Each template is a professional report layout with:
- Header: Oversight Hub logo placeholder, report title, org name, generation date, report ID
- Summary section: Key metrics (total events, incidents, users, compliance status)
- Detail sections: Tables with audit entries, access reviews, or control evidence
- Footer: Page numbers, confidentiality notice, generation timestamp

Follow the inline template pattern from incident.service.ts but store as `.hbs` files for maintainability (D-18). Use professional fonts (Helvetica, Helvetica-Bold) matching existing incident reports. French-first language for report labels per existing pattern.

**`compliance.controller.ts`** — Prefix: `@Controller("compliance")`. Requires JWT + ADMIN role. Routes:
- `@Post("reports/generate")`: Accept `@Body(new ZodValidationPipe(generateComplianceReportSchema))`, call `complianceService.generateReport()`, set response header `Content-Type: application/pdf` and `Content-Disposition: attachment; filename="...pdf"`, return PDF buffer as stream.
- `@Get("reports")`: List previously generated reports (store metadata in a simple JSON file or in-memory — or create a ComplianceReport model if time permits. For MVP, return empty list with a note that reports are generated on-demand and downloaded immediately).
</action>
  <acceptance_criteria>
    <criteria id="A1">compliance.module.ts exists with ComplianceController, ComplianceService</criteria>
    <criteria id="A2">compliance.service.ts uses PDFKit + Handlebars pattern from incident.service.ts (new PDFDocument, buffer aggregation, doc.end())</criteria>
    <criteria id="A3">compliance.service.ts has generateReport() accepting reportType ("soc2" | "iso27001" | "access-review") and optional dateRange</criteria>
    <criteria id="A4">Three .hbs template files exist in templates/ directory (soc2-report.hbs, iso27001-report.hbs, access-review.hbs)</criteria>
    <criteria id="A5">Templates compile via Handlebars.compile() and produce valid HTML (no runtime errors)</criteria>
    <criteria id="A6">compliance.controller.ts has POST /api/compliance/reports/generate endpoint with @Roles("ADMIN"), returning application/pdf</criteria>
    <criteria id="A7">PDF generation queries data from AuditService (queryAuditLog) and PrismaService (organizationMember, incident)</criteria>
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/api/src/modules/compliance/compliance.service.ts</automated>
    <automated>test -f apps/api/src/modules/compliance/templates/soc2-report.hbs</automated>
    <automated>test -f apps/api/src/modules/compliance/templates/iso27001-report.hbs</automated>
    <automated>test -f apps/api/src/modules/compliance/templates/access-review.hbs</automated>
    <automated>grep "PDFDocument" apps/api/src/modules/compliance/compliance.service.ts | grep -c "pdfkit" | xargs test 1 -eq</automated>
    <automated>grep "Handlebars.compile" apps/api/src/modules/compliance/compliance.service.ts | grep -c "compile" | xargs test 1 -eq</automated>
    <automated>grep "application/pdf" apps/api/src/modules/compliance/compliance.controller.ts | grep -c "Content-Type" | xargs test 1 -eq</automated>
  </verify>
  <done>Compliance module generates SOC 2, ISO 27001, and Access Review reports as downloadable PDFs using the existing PDFKit + Handlebars stack. No new dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Extend GovernanceService — data classification + pre-purge export</name>
  <files>
    apps/api/src/modules/governance/governance.service.ts,
    apps/api/src/modules/governance/governance.controller.ts
  </files>
  <read_first>apps/api/src/modules/governance/governance.service.ts</read_first>
  <read_first>apps/api/src/modules/governance/governance.controller.ts</read_first>
  <read_first>apps/api/prisma/schema.prisma</read_first>
  <action>
Extend the existing GovernanceService to support data classification labels and pre-purge export (D-20, D-21).

**`governance.service.ts` additions:**

1. **Update `createPolicy()`** — Add `classification?: string` and `exportBeforePurge?: boolean` and `exportFormat?: string` to the DTO parameter. The Prisma create call already handles these since the schema fields exist.

2. **Update `listPolicies()`** — No changes needed (Prisma returns new fields automatically).

3. **Update `updatePolicy()`** — Add `classification?: string`, `exportBeforePurge?: boolean`, `exportFormat?: string` to the DTO parameter.

4. **New method: `exportBeforePurge(eventType: string, tableType: string, retentionDays: number, format: string)`**: Before pruning, export data as PDF or CSV.
   - Query the data that would be deleted: for timescaledb tables, use `$queryRawUnsafe` to SELECT rows older than the cutoff. For prisma tables, use findMany with createdAt lt cutoff.
   - If format is "PDF": Use PDFKit to generate a simple table-based report of the exported data. Return as Buffer.
   - If format is "CSV": Convert query results to CSV string (simple comma-separated values with header row). Return as string.
   - Store the export as a downloadable file — create an `ExportedArchive` record or store path in a simple file listing. For MVP, return the export data directly (caller handles storage).
   - Write an audit log entry: `auditService.log({ action: "retention.export", entityType: "RetentionPolicy", entityId: eventType, details: { format, recordCount } })`.

5. **Update `pruneExpiredData()` cron** (line 64): Before pruning, check if any policy has `exportBeforePurge: true`. If so, call `exportBeforePurge()` first, then prune.

6. **New validation:** `validateClassification(classification: string)` — Accept only "PII", "security", "audit", "operational". Throw BadRequestException for invalid values.

**`governance.controller.ts` additions:**

1. **Update `createPolicy` endpoint** — Accept new DTO fields via Zod schema from shared package (the `createRetentionPolicySchema` in shared should already be updated or use the extended model).

2. **Add endpoint: `@Post("retention-policies/:id/export")`** — Triggers immediate pre-purge export for a policy. Returns the exported file as a download.

3. **Add endpoint: `@Get("retention-policies/classifications")`** — Returns the list of valid classification labels: `["PII", "security", "audit", "operational"]`. Used by dashboard dropdown.

Since the shared package schema `createRetentionPolicySchema` is in `packages/shared/src/schemas/governance.schema.ts`, extend it with optional `classification`, `exportBeforePurge`, `exportFormat` fields. Or create a separate `updateRetentionPolicySchema` if it doesn't exist. Check the existing schema.

**Schema extension:** If `packages/shared/src/schemas/governance.schema.ts` has `createRetentionPolicySchema`, extend it:
```typescript
export const createRetentionPolicySchema = z.object({
  eventType: z.string().min(1),
  tableType: z.string().min(1),
  retentionDays: z.number().int().min(1),
  enabled: z.boolean().optional(),
  classification: z.enum(["PII", "security", "audit", "operational"]).optional(),
  exportBeforePurge: z.boolean().optional(),
  exportFormat: z.enum(["PDF", "CSV"]).optional(),
});
```
</action>
  <acceptance_criteria>
    <criteria id="B1">createPolicy() accepts classification, exportBeforePurge, exportFormat fields</criteria>
    <criteria id="B2">exportBeforePurge() method exists — queries data before cutoff, exports as PDF or CSV</criteria>
    <criteria id="B3">pruneExpiredData() cron checks exportBeforePurge flag before pruning, calls exportBeforePurge() if true</criteria>
    <criteria id="B4">validateClassification() validates against ["PII", "security", "audit", "operational"]</criteria>
    <criteria id="B5">POST /api/governance/retention-policies/:id/export endpoint triggers export</criteria>
    <criteria id="B6">Governance controller and service compile without TypeScript errors</criteria>
  </acceptance_criteria>
  <verify>
    <automated>grep "classification" apps/api/src/modules/governance/governance.service.ts | grep -c "string" | xargs test 3 -ge</automated>
    <automated>grep "exportBeforePurge" apps/api/src/modules/governance/governance.service.ts | grep -c "export" | xargs test 2 -ge</automated>
    <automated>grep "export" apps/api/src/modules/governance/governance.controller.ts | grep -c "Post" | xargs test 1 -ge</automated>
  </verify>
  <done>Retention policies support data classification (PII/security/audit/operational) and pre-purge export (PDF/CSV archive before deletion). Audit events logged for every purge.</done>
</task>

<task type="auto">
  <name>Task 3: Multi-currency license + white-label organization fields</name>
  <files>
    apps/api/src/modules/license/license.service.ts,
    apps/api/src/modules/organization/organization.service.ts
  </files>
  <read_first>apps/api/src/modules/license/license.service.ts</read_first>
  <read_first>apps/api/src/modules/organization/organization.service.ts</read_first>
  <read_first>apps/api/prisma/schema.prisma</read_first>
  <action>
**Multi-currency in LicenseService** (D-16, D-17):

In `apps/api/src/modules/license/license.service.ts`:

1. **Extend the `generateLicense` DTO** — Add optional `currency?: string` parameter. Default to "USD" if not provided.

2. **Validate currency** — Accept only "USD", "EUR", "XOF", "GBP", "JPY". Throw BadRequestException for invalid values.

3. **Add currency to LicenseClaims** — Include `currency: dto.currency ?? "USD"` in the JWT claims object. The License JWT will carry the currency field.

4. **Store currency in License model** — Pass `currency` to the Prisma create call when storing the license record. The field already exists in the schema (Plan 01 added `currency String @default("USD")`).

5. **Update `activateLicense`** — When verifying a license JWT, extract and validate the `currency` claim if present. Store it in the License record.

No exchange rate API — admin sets currency at generation time. Customer's instance displays what the license JWT says (D-17).

**White-labeling in OrganizationService** (D-22):

In `apps/api/src/modules/organization/organization.service.ts`:

1. **Add `updateBranding(orgId: string, data: { logoUrl?: string; primaryColor?: string; displayName?: string })`** — Update organization with white-label fields. Validate primaryColor as hex color (regex: `/^#[0-9A-Fa-f]{6}$/`). Validate logoUrl as a valid URL if provided. Use `this.prisma.organization.update({ where: { id: orgId }, data: { logoUrl: data.logoUrl, primaryColor: data.primaryColor, displayName: data.displayName } })`.

2. **Add `getBranding(orgId: string)`** — Return `{ logoUrl, primaryColor, displayName, name }` for the org. This is used by the dashboard to apply branding.

3. **The existing `update()` method automatically supports the new fields** since Prisma's OrganizationUpdateInput will include them after schema push. No code changes needed — just verify the type includes the new fields.

4. **Mark the branding endpoints with @RequiresFeature("custom_branding")** in the controller (the decorator goes on controller methods). Since OrganizationController is in a separate file, add the decorator when extending. For this task, we're only modifying the service — controller updates happen in Plan 07 (Dashboard wiring).

**OrganizationController extension** (if needed now):
In `apps/api/src/modules/organization/organization.controller.ts` (or when extending in Plan 06):
- `@Patch("branding")` — Update white-label settings with @RequiresFeature("custom_branding")
- `@Get("branding")` — Get current branding (public — used by dashboard layout)
</action>
  <acceptance_criteria>
    <criteria id="C1">generateLicense() accepts optional currency parameter, validated against ["USD", "EUR", "XOF", "GBP", "JPY"]</criteria>
    <criteria id="C2">LicenseClaims includes currency field</criteria>
    <criteria id="C3">activateLicense() extracts and stores currency from license JWT</criteria>
    <criteria id="C4">OrganizationService has updateBranding() method accepting logoUrl, primaryColor, displayName</criteria>
    <criteria id="C5">primaryColor validated as hex color (#RRGGBB format)</criteria>
    <criteria id="C6">OrganizationService compiles without TypeScript errors</criteria>
  </acceptance_criteria>
  <verify>
    <automated>grep "currency" apps/api/src/modules/license/license.service.ts | grep -c "USD" | xargs test 1 -ge</automated>
    <automated>grep "updateBranding" apps/api/src/modules/organization/organization.service.ts | grep -c "async" | xargs test 1 -eq</automated>
    <automated>grep "primaryColor" apps/api/src/modules/organization/organization.service.ts | grep -c "#" | xargs test 1 -ge</automated>
    <automated>grep "displayName" apps/api/src/modules/organization/organization.service.ts | grep -c "String" | xargs test 1 -ge</automated>
  </verify>
  <done>License generation supports multi-currency (USD/EUR/XOF/GBP/JPY). Organization supports white-label fields (logoUrl, primaryColor, displayName) with hex color validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compliance report data | Audit logs and access data aggregated from DB — must respect tenant isolation (org-scoped queries only) |
| Logo upload | User-supplied logo URL must not allow XSS or path traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-20 | Information Disclosure | ComplianceService.generateReport | mitigate | All data queries are org-scoped via existing Prisma Client Extension; reports only contain data for the requesting org |
| T-10-21 | Tampering | Organization.logoUrl | mitigate | Validate logoUrl as valid HTTPS URL; reject javascript: or data: URIs; use Content-Security-Policy headers in dashboard |
| T-10-22 | Tampering | Organization.primaryColor | mitigate | Validate as hex color (#RRGGBB) via regex; reject arbitrary CSS values that could inject styles |
| T-10-23 | Information Disclosure | Pre-purge export | mitigate | Export files only accessible to admin users; export path stored in DB, not publicly accessible |
| T-10-24 | Repudiation | Pre-purge + delete | mitigate | Audit log entry written for every purge operation (existing AuditInterceptor + explicit audit log call) |
| T-10-25 | Elevation of Privilege | Multi-currency | accept | Currency is a display field — no financial calculations affected; exchange rates not computed server-side |
| T-10-SC | Tampering | npm installs (none new in this plan) | accept | No new npm packages — PDFKit and Handlebars already installed and verified |
</threat_model>

<verification>
- [ ] Compliance service generates PDF reports using PDFKit + Handlebars
- [ ] Three Handlebars templates exist for SOC 2, ISO 27001, Access Review
- [ ] Governance service supports classification labels and pre-purge export
- [ ] License generation supports multi-currency with validation
- [ ] Organization service supports white-label field updates with hex color validation
</verification>

<success_criteria>
Admin can generate SOC 2, ISO 27001, and Access Review compliance reports as downloadable PDFs. Retention policies support data classification labeling and pre-purge export (PDF or CSV) with audit trail. Licenses support multi-currency pricing. Organization settings support white-label branding (logo, color, display name).
</success_criteria>

<output>
Create `.planning/phases/10-enterprise-grade/10-04-SUMMARY.md` when done
</output>
