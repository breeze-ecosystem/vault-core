---
phase: 10-enterprise-grade
plan: 05
type: execute
wave: 3
depends_on: ["10-02", "10-03", "10-04"]
files_modified:
  - apps/api/src/app.module.ts
  - apps/dashboard/app/(dashboard)/parametres/page.tsx
  - apps/dashboard/app/(dashboard)/conformite/page.tsx
  - apps/dashboard/app/(dashboard)/api-keys/page.tsx
  - apps/dashboard/app/(dashboard)/webhooks/page.tsx
  - apps/dashboard/app/(auth)/login/page.tsx
  - apps/dashboard/components/sso/SsoConfigForm.tsx
  - apps/dashboard/components/api-keys/ApiKeyTable.tsx
  - apps/dashboard/components/api-keys/ApiKeyCreateForm.tsx
  - apps/dashboard/components/webhooks/WebhookSubscriptionForm.tsx
  - apps/dashboard/components/webhooks/WebhookDeliveryTimeline.tsx
  - apps/dashboard/components/compliance/ComplianceReportSelector.tsx
  - apps/dashboard/components/branding/BrandingPreviewCard.tsx
  - apps/dashboard/components/branding/LogoUploader.tsx
  - apps/dashboard/components/branding/ColorPicker.tsx
  - apps/dashboard/lib/api.ts
  - apps/dashboard/lib/nav-config.ts
  - apps/dashboard/lib/i18n/fr.ts
  - apps/dashboard/lib/i18n/en.ts
autonomous: true
requirements:
  - ENT-01
  - ENT-02
  - ENT-03
  - ENT-04
  - ENT-05
  - ENT-06
  - ENT-07
must_haves:
  truths:
    - "Admin sees SSO configuration settings on the parametres page"
    - "Admin creates, lists, and revokes API keys from the dashboard"
    - "Admin creates webhook subscriptions and views delivery timeline"
    - "Admin generates compliance reports from the conformite page"
    - "Login page shows SSO button when IdP is configured"
    - "Navigation menu includes new enterprise sections"
  artifacts:
    - path: "apps/api/src/app.module.ts"
      provides: "All new modules registered in NestJS application"
      contains: "SsoModule"
      contains: "ApiKeyModule"
      contains: "WebhookModule"
      contains: "ComplianceModule"
    - path: "apps/dashboard/app/(dashboard)/parametres/page.tsx"
      provides: "Extended settings page with SSO, API Keys, Webhooks, Branding tabs"
    - path: "apps/dashboard/app/(dashboard)/conformite/page.tsx"
      provides: "Compliance report generation page"
    - path: "apps/dashboard/app/(auth)/login/page.tsx"
      provides: "SSO login button when IdP configured"
  key_links:
    - from: "apps/dashboard/app/(dashboard)/parametres/page.tsx"
      to: "apps/api/src/modules/sso/sso.controller.ts"
      via: "fetchWithAuth POST/GET /api/auth/sso/config"
      pattern: "fetchWithAuth.*sso"
    - from: "apps/dashboard/app/(dashboard)/api-keys/page.tsx"
      to: "apps/api/src/modules/api-key/api-key.controller.ts"
      via: "fetchWithAuth GET/POST/DELETE /api/api-keys"
      pattern: "fetchWithAuth.*api-keys"
---

<objective>
Wire all new NestJS modules into the application and build the enterprise dashboard pages. Register SsoModule, ApiKeyModule, WebhookModule, and ComplianceModule in AppModule. Create dashboard pages for SSO configuration, API key management, webhook subscriptions, compliance reports, and white-label branding. Extend the settings page with tabbed enterprise sections. Add SSO button to the login page. Update navigation and i18n dictionaries.

Purpose: Enterprise administrators can configure all enterprise features through the dashboard UI — no env vars or command-line configuration required.
Output: AppModule with 4 new module imports, 4 new dashboard pages, 10+ reusable dashboard components, extended settings page, SSO login button, updated navigation and i18n.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-enterprise-grade/10-UI-SPEC.md
@.planning/phases/10-enterprise-grade/10-PATTERNS.md
@.planning/phases/10-enterprise-grade/10-CONTEXT.md

<interfaces>
From apps/api/src/app.module.ts (lines 52-111) — Module registration pattern:
Import new modules at the top, add to @Module({ imports: [...] }) array. Add new modules in alphabetical order: ApiKeyModule, ComplianceModule, SsoModule, WebhookModule.

From apps/dashboard/app/(dashboard)/parametres/page.tsx — Dashboard page pattern:
```typescript
"use client";
import { useState, useEffect } from "react";
import { useAuth } from "@/lib/auth-context";
import { PageHeader } from "@/components/page-header";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { toast } from "@/components/ui/toast";

export default function ParametresPage() {
  const { user } = useAuth();
  async function handleAction(e: React.FormEvent) { e.preventDefault(); /* try/catch with toast */ }
  return (<div><PageHeader title="..." description="..." />{/* Card-based layout */}</div>);
}
```

From apps/dashboard/lib/nav-config.ts — Navigation group pattern:
```typescript
const groups: NavGroup[] = [
  { label: "Gouvernance", icon: ShieldCheck, minRole: "ADMIN" as Role,
    items: [{ label: "Audit", href: "/audit", icon: Shield, minRole: "ADMIN" }, ...] },
];
```

From apps/dashboard/lib/api.ts — API client pattern:
```typescript
export async function fetchSomething(): Promise<Type> {
  return fetchWithAuth("/api/endpoint").then(r => r.json());
}
```

UI-SPEC component contracts:
- SsoConfigForm: SAML + OIDC tabs with metadata URL fetch, cert upload, test connection button
- ApiKeyTable: Masked key list with last-used timestamp, usage sparkline, revoke button
- ApiKeyCreateForm: Name input + scope checkboxes + rate limit slider + expiry date picker
- WebhookSubscriptionForm: Event type select + target URL input + enable/disable toggle
- WebhookDeliveryTimeline: Color-coded delivery log (green=200, yellow=retry, red=failed)
- ComplianceReportSelector: Report type select (SOC2/ISO/Access Review) + date range + generate button
- BrandingPreviewCard: Visual preview showing header/login mockups with uploaded logo + color
- LogoUploader: File upload with preview + size validation
- ColorPicker: Hex color input with live preview

UI-SPEC copywriting contract (use these exact strings in French):
- Login SSO: "Se connecter avec SSO" / "Utiliser vos identifiants d'entreprise"
- Empty states: see UI-SPEC lines 140-163
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppModule wiring — register all new modules</name>
  <files>apps/api/src/app.module.ts</files>
  <read_first>apps/api/src/app.module.ts</read_first>
  <read_first>apps/api/src/modules/sso/sso.module.ts</read_first>
  <read_first>apps/api/src/modules/api-key/api-key.module.ts</read_first>
  <read_first>apps/api/src/modules/webhook/webhook.module.ts</read_first>
  <read_first>apps/api/src/modules/compliance/compliance.module.ts</read_first>
  <action>
Register all four new NestJS modules in AppModule. This is the central wiring task that makes all Phase 10 features available.

**`apps/api/src/app.module.ts`:**

1. **Add import statements** at the top (after existing module imports, around line 41):
```typescript
import { SsoModule } from './modules/sso/sso.module';
import { ApiKeyModule } from './modules/api-key/api-key.module';
import { WebhookModule } from './modules/webhook/webhook.module';
import { ComplianceModule } from './modules/compliance/compliance.module';
```

2. **Add to @Module imports array** — Insert new modules alphabetically in the imports array:
- After AiAgentModule, before AnalyticsModule (or in correct alphabetical position):
```typescript
ApiKeyModule,
```
- After `ApiKeyModule`:
```typescript
ComplianceModule,
```
- After `LicenseModule` (or wherever alphabetically correct):
```typescript
SsoModule,
```
- After `SsoModule`:
```typescript
WebhookModule,
```

The full module order (alphabetical): AccessModule → AiAgentModule → AiModule → AlertModule → AnalyticsModule → AnprModule → **ApiKeyModule** → AuditModule → AuthModule → CameraModule → ChatModule → **ComplianceModule** → ContactModule → CorrelationModule → DashboardModule → DoorModule → EquipmentModule → FeatureGateModule → GovernanceModule → IncidentModule → InferenceModule → IngestionModule → InviteModule → LicenseModule → MaintenanceModule → MqttModule → NotificationModule → NotificationsModule → OrganizationModule → PatternsModule → PrismaModule → RiskModule → **SsoModule** → SupervisionModule → UserModule → VisitorModule → **WebhookModule**

3. **Verify guard order**: The existing guard chain is JwtAuthGuard → TenantIsolationGuard → RolesGuard → FeatureGateGuard. The TenantApiKeyGuard is NOT registered as a global APP_GUARD — it's applied per-controller via @UseGuards() on the v1 controllers. This avoids conflict with JWT-based routes.

4. **Rate limiter conflict resolution** (Pitfall 5): The global @fastify/rate-limit protects `/api/*` routes. The v1 endpoints `/api/v1/*` use per-key rate limiting in TenantApiKeyGuard instead. Add a comment documenting that if the global rate limiter causes double-counting, it should be configured to exclude `/api/v1/*` paths. For now, the per-key guard runs inside the NestJS request lifecycle and the global Fastify rate limiter runs at the HTTP layer — they may both trigger. This is acceptable for initial delivery; the exclusion hook can be added in a follow-up.

5. **No middleware changes needed** — TenantContextMiddleware already applies to all routes (`forRoutes('*')`). SSO callback routes are @Public() so they bypass JwtAuthGuard but still go through TenantContextMiddleware (which needs an orgId — for SSO callbacks, the orgId is passed in the relay state or derived from the IdP config lookup).

**IMPORTANT: Handle SSO callback org context.** For SSO callback routes (POST /api/auth/sso/saml/callback, GET /api/auth/sso/oidc/callback), the request has no JWT and thus no orgId. The TenantContextMiddleware currently destroys connections without orgId (from tenant-context.middleware.ts: the `handleConnection` pattern). SSO callbacks must be exempted — the SsoService resolves orgId from the IdP config during SAML assertion validation. For now, ensure SSO callback routes work by having SsoService set the org context in AsyncLocalStorage manually during validate().

Add a try/catch around the SSO callback's org context setup: in SsoController's callback methods, after Passport validation, call `orgContext.run({ orgId: user.orgId }, () => { /* token exchange */ })`.
</action>
  <acceptance_criteria>
    <criteria id="A1">app.module.ts imports SsoModule, ApiKeyModule, WebhookModule, ComplianceModule</criteria>
    <criteria id="A2">All four modules listed in @Module imports array</criteria>
    <criteria id="A3">Existing guard order (JwtAuthGuard → TenantIsolationGuard → RolesGuard → FeatureGateGuard) preserved</criteria>
    <criteria id="A4">TenantApiKeyGuard NOT registered as global APP_GUARD</criteria>
    <criteria id="A5">SSO callback org context handling documented or implemented</criteria>
    <criteria id="A6">No TypeScript compilation errors in app.module.ts</criteria>
  </acceptance_criteria>
  <verify>
    <automated>grep "SsoModule" apps/api/src/app.module.ts | grep -c "from" | xargs test 1 -eq</automated>
    <automated>grep "ApiKeyModule" apps/api/src/app.module.ts | grep -c "from" | xargs test 1 -eq</automated>
    <automated>grep "WebhookModule" apps/api/src/app.module.ts | grep -c "from" | xargs test 1 -eq</automated>
    <automated>grep "ComplianceModule" apps/api/src/app.module.ts | grep -c "from" | xargs test 1 -eq</automated>
    <automated>grep "APP_GUARD" apps/api/src/app.module.ts | grep -c "useClass" | xargs test 4 -eq</automated>
  </verify>
  <done>All four new NestJS modules registered in AppModule. Guard order preserved. Application boots with all Phase 10 features available.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard enterprise pages — SSO config, API keys, webhooks, compliance, branding</name>
  <files>
    apps/dashboard/app/(dashboard)/parametres/page.tsx,
    apps/dashboard/app/(dashboard)/conformite/page.tsx,
    apps/dashboard/app/(dashboard)/api-keys/page.tsx,
    apps/dashboard/app/(dashboard)/webhooks/page.tsx,
    apps/dashboard/components/sso/SsoConfigForm.tsx,
    apps/dashboard/components/api-keys/ApiKeyTable.tsx,
    apps/dashboard/components/api-keys/ApiKeyCreateForm.tsx,
    apps/dashboard/components/webhooks/WebhookSubscriptionForm.tsx,
    apps/dashboard/components/webhooks/WebhookDeliveryTimeline.tsx,
    apps/dashboard/components/compliance/ComplianceReportSelector.tsx,
    apps/dashboard/components/branding/BrandingPreviewCard.tsx,
    apps/dashboard/components/branding/LogoUploader.tsx,
    apps/dashboard/components/branding/ColorPicker.tsx
  </files>
  <read_first>apps/dashboard/app/(dashboard)/parametres/page.tsx</read_first>
  <read_first>apps/dashboard/app/(dashboard)/gouvernance/page.tsx</read_first>
  <read_first>apps/dashboard/components/ui/card.tsx</read_first>
  <read_first>apps/dashboard/components/ui/button.tsx</read_first>
  <read_first>apps/dashboard/components/ui/dialog.tsx</read_first>
  <read_first>apps/dashboard/lib/api.ts</read_first>
  <read_first>apps/dashboard/lib/i18n/fr.ts</read_first>
  <action>
Create enterprise dashboard pages and components. Follow the EXACT page pattern from parametres/page.tsx: "use client", useAuth(), useState/useEffect, PageHeader, Card-based layout, toast notifications, French-language UI per UI-SPEC copy contract.

**1. Extend `apps/dashboard/app/(dashboard)/parametres/page.tsx`:**
Add tabbed sections for enterprise settings. Use simple tab implementation (state-based content switching — no new shadcn tabs component needed if not installed; use button group + conditional rendering). Tabs:
- "SSO" tab — renders SsoConfigForm component
- "API Keys" tab — renders inline ApiKeyTable + ApiKeyCreateForm
- "Webhooks" tab — renders inline WebhookSubscriptionForm
- "Branding" tab — renders BrandingPreviewCard + LogoUploader + ColorPicker
- "Général" tab — existing settings content

The page already has Card-based sections. Convert existing sections into a tabbed layout. State: `const [activeTab, setActiveTab] = useState("general")`. Render tab buttons then conditionally render tab content.

**2. New page: `apps/dashboard/app/(dashboard)/conformite/page.tsx`:**
Compliance report generation page. Layout: PageHeader with title "Conformité" and description from UI-SPEC. Card containing ComplianceReportSelector component. Card for report history (list of previously generated reports with download links — for MVP, show empty state). Follow parametres page pattern exactly.

**3. New page: `apps/dashboard/app/(dashboard)/api-keys/page.tsx`:**
Dedicated API key management page. Layout: PageHeader + ApiKeyTable (list all keys) + "Créer une clé API" button that opens ApiKeyCreateForm in a Dialog. On key creation, show the raw key once with copy-to-clipboard button and warning message per UI-SPEC: "Copiez votre clé API maintenant. Vous ne pourrez plus la voir."

**4. New page: `apps/dashboard/app/(dashboard)/webhooks/page.tsx`:**
Dedicated webhook management page. Layout: PageHeader + WebhookSubscriptionForm (create new) + list of existing subscriptions each with their WebhookDeliveryTimeline. Each subscription row shows: event type, target URL (truncated), status toggle, latest delivery status, delete button. Clicking a subscription expands its WebhookDeliveryTimeline.

**5. Components to create in `apps/dashboard/components/`:**

**`sso/SsoConfigForm.tsx`** — Two sub-tabs: SAML and OIDC. SAML tab: metadataUrl input + "Tester la connexion" button, entityId input, certificate textarea, attribute mappings JSON editor (or simple key-value inputs). OIDC tab: clientId, clientSecret, issuerUrl inputs, "Tester la connexion" button. Both show test result (success/failure) via toast. Save button: POST /api/auth/sso/config. Load existing config: GET /api/auth/sso/config.

**`api-keys/ApiKeyTable.tsx`** — Table component showing: name, key prefix (osk_abc...XXX), scopes (badge list), rate limit, last used, status (active/revoked), revoke button. Uses existing shadcn Table component. Props: `keys: ApiKey[]`, `onRevoke: (id: string) => void`, `loading: boolean`. Empty state per UI-SPEC: "Aucune clé API".

**`api-keys/ApiKeyCreateForm.tsx`** — Dialog form: name input, scope checkboxes (Cameras, Doors, Alerts, Incidents, Events, Audit), rate limit number input (default 300), optional expiry date. Create button calls POST /api/api-keys. On success: show the raw API key in a readonly input with copy button, and warning text "Copiez votre clé API maintenant. Vous ne pourrez plus la voir."

**`webhooks/WebhookSubscriptionForm.tsx`** — Form: event type select (alert.created, incident.escalated, door.forced, etc.), target URL input (HTTPS validated), active toggle. Create button calls POST /api/webhooks/subscriptions.

**`webhooks/WebhookDeliveryTimeline.tsx`** — Timeline/list of delivery attempts for a subscription. Each entry: timestamp, HTTP status code (color-coded: green 2xx, yellow 4xx, red 5xx/error), response time, attempt number, retry button for failed deliveries. Calls GET /api/webhooks/subscriptions/:id/deliveries.

**`compliance/ComplianceReportSelector.tsx`** — Form: report type select (SOC 2 / ISO 27001 / Access Review), optional date range (from/to). "Générer le rapport" button with loading spinner ("Génération en cours..."). On completion, trigger PDF download. Calls POST /api/compliance/reports/generate and handles blob response.

**`branding/BrandingPreviewCard.tsx`** — Visual preview card showing a mini mockup of the dashboard header with the org's logo and primary color. Props: `logoUrl?: string`, `primaryColor?: string`, `displayName?: string`.

**`branding/LogoUploader.tsx`** — File input accepting image files. Shows preview of uploaded logo. Validates file size < 2MB. On upload: convert to base64 or upload to a storage endpoint. For MVP, store the logo URL (user provides a hosted URL). Input: text field for logo URL + preview image.

**`branding/ColorPicker.tsx`** — Input field for hex color (#RRGGBB) with a color preview swatch. Validates hex format on blur. Props: `value: string`, `onChange: (color: string) => void`.

**6. API client functions** in `apps/dashboard/lib/api.ts`:
Add fetch functions for all new endpoints. Follow the existing pattern (export async function, fetchWithAuth, typed response). Functions to add:
- `fetchIdpConfig()` → GET /api/auth/sso/config
- `saveIdpConfig(data)` → POST /api/auth/sso/config
- `fetchApiKeys()` → GET /api/api-keys
- `createApiKey(data)` → POST /api/api-keys
- `revokeApiKey(id)` → DELETE /api/api-keys/:id
- `fetchWebhookSubscriptions()` → GET /api/webhooks/subscriptions
- `createWebhookSubscription(data)` → POST /api/webhooks/subscriptions
- `updateWebhookSubscription(id, data)` → PATCH /api/webhooks/subscriptions/:id
- `deleteWebhookSubscription(id)` → DELETE /api/webhooks/subscriptions/:id
- `fetchWebhookDeliveries(id, filters?)` → GET /api/webhooks/subscriptions/:id/deliveries
- `generateComplianceReport(data)` → POST /api/compliance/reports/generate (returns blob)
- `fetchOrganizationBranding()` → GET /api/organizations/me (or dedicated branding endpoint)
- `updateOrganizationBranding(data)` → PATCH /api/organizations/branding
</action>
  <acceptance_criteria>
    <criteria id="B1">parametres/page.tsx has tabbed layout with SSO, API Keys, Webhooks, Branding tabs</criteria>
    <criteria id="B2">conformite/page.tsx exists with ComplianceReportSelector and report generation flow</criteria>
    <criteria id="B3">api-keys/page.tsx exists with ApiKeyTable and ApiKeyCreateForm in Dialog</criteria>
    <criteria id="B4">webhooks/page.tsx exists with WebhookSubscriptionForm and WebhookDeliveryTimeline</criteria>
    <criteria id="B5">All components follow "use client" + useAuth() + Card-based layout pattern</criteria>
    <criteria id="B6">API client functions added to lib/api.ts for all new endpoints</criteria>
    <criteria id="B7">French-language UI text matches UI-SPEC copy contract</criteria>
    <criteria id="B8">Empty states for API keys, webhooks, compliance use exact copy from UI-SPEC</criteria>
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/dashboard/app/(dashboard)/conformite/page.tsx</automated>
    <automated>test -f apps/dashboard/app/(dashboard)/api-keys/page.tsx</automated>
    <automated>test -f apps/dashboard/app/(dashboard)/webhooks/page.tsx</automated>
    <automated>test -f apps/dashboard/components/sso/SsoConfigForm.tsx</automated>
    <automated>test -f apps/dashboard/components/api-keys/ApiKeyTable.tsx</automated>
    <automated>test -f apps/dashboard/components/compliance/ComplianceReportSelector.tsx</automated>
    <automated>test -f apps/dashboard/components/branding/BrandingPreviewCard.tsx</automated>
    <automated>grep "fetchApiKeys" apps/dashboard/lib/api.ts | grep -c "export" | xargs test 1 -eq</automated>
    <automated>grep "Aucune clé API" apps/dashboard/components/api-keys/ApiKeyTable.tsx | grep -c "Aucune" | xargs test 1 -eq</automated>
  </verify>
  <done>Enterprise dashboard pages with SSO configuration, API key management, webhook subscription management, compliance report generation, and white-label branding. All pages follow existing parametres page pattern with "use client", Card-based layout, and French UI text.</done>
</task>

<task type="auto">
  <name>Task 3: Navigation + Login SSO button + i18n dictionaries</name>
  <files>
    apps/dashboard/lib/nav-config.ts,
    apps/dashboard/app/(auth)/login/page.tsx,
    apps/dashboard/lib/i18n/fr.ts,
    apps/dashboard/lib/i18n/en.ts
  </files>
  <read_first>apps/dashboard/lib/nav-config.ts</read_first>
  <read_first>apps/dashboard/app/(auth)/login/page.tsx</read_first>
  <read_first>apps/dashboard/lib/i18n/fr.ts</read_first>
  <read_first>apps/dashboard/lib/i18n/en.ts</read_first>
  <action>
**Navigation update** in `apps/dashboard/lib/nav-config.ts`:

Add new navigation entries under the existing "Gouvernance" group (which already contains Audit and Gouvernance items). Add these items:
```typescript
{ label: "Conformité", href: "/conformite", icon: FileCheck, minRole: "ADMIN" as Role },
{ label: "Clés API", href: "/api-keys", icon: Key, minRole: "ADMIN" as Role },
{ label: "Webhooks", href: "/webhooks", icon: Webhook, minRole: "ADMIN" as Role },
```

Import new icons at the top: `FileCheck` and `Webhook` from lucide-react. These may not exist in the exact name — use the closest available: `FileCheck` → `FileCheck` (exists?), `Webhook` → `Webhook` (exists in lucide-react). If not available, use alternatives: `ClipboardCheck` for compliance, `Link2` or `Radio` for webhooks.

**Login page SSO button** in `apps/dashboard/app/(auth)/login/page.tsx`:

Add an SSO section after the email/password form. Pattern:
1. Check if IdP is configured: fetch `GET /api/auth/sso/config` (or just attempt to detect — show button always if the `sso` feature flag is enabled for the org, or check a public endpoint)
2. Simpler approach for initial delivery: Show the SSO button unconditionally (it will fail gracefully if not configured) or add a check: if the `sso` feature flag exists in the license, show the button.
3. Most pragmatic: Add a link/button "Se connecter avec SSO" that navigates to `/api/auth/sso/saml/login` (SAML) or shows a protocol selector. For MVP, show a single SSO button that defaults to SAML (most common enterprise protocol).
4. Button placement: Below the login form, separated by a divider with "ou" text. Styling: secondary/outline variant, full width.
5. Supporting text below: "Utiliser vos identifiants d'entreprise"

Per D-04: "SSO-first login page with local email/password fallback for break-glass admin access." — The SSO button is prominent but the email/password form remains the primary form. SSO button is below as an alternative.

**i18n dictionaries** in `apps/dashboard/lib/i18n/fr.ts` and `en.ts`:

Add new translation keys to the French dictionary (primary). English as fallback.
```typescript
// French additions
"sso": {
  "title": "Single Sign-On",
  "configured": "SSO configuré",
  "notConfigured": "SSO non configuré",
  "signIn": "Se connecter avec SSO",
  "supportingText": "Utiliser vos identifiants d'entreprise",
  "saml": "SAML 2.0",
  "oidc": "OpenID Connect",
  "testConnection": "Tester la connexion",
  "testSuccess": "Connexion réussie",
  "testFailed": "Échec de la connexion",
},
"apiKeys": {
  "title": "Clés API",
  "create": "Créer une clé API",
  "copyWarning": "Copiez votre clé API maintenant. Vous ne pourrez plus la voir.",
  "revoke": "Révoquer",
  "revokeConfirm": "Êtes-vous sûr de vouloir révoquer cette clé ? Cette action est irréversible.",
  "empty": "Aucune clé API",
  "emptyBody": "Créez votre première clé API pour intégrer des services externes via l'API REST publique.",
},
"webhooks": {
  "title": "Webhooks",
  "create": "Créer un webhook",
  "empty": "Aucun webhook",
  "emptyBody": "Créez un abonnement webhook pour recevoir des notifications en temps réel.",
},
"compliance": {
  "title": "Conformité",
  "generate": "Générer le rapport",
  "generating": "Génération en cours...",
  "empty": "Aucun rapport généré",
  "emptyBody": "Générez votre premier rapport de conformité.",
},
"branding": {
  "title": "Image de marque",
  "logo": "Logo",
  "color": "Couleur principale",
  "displayName": "Nom d'affichage",
  "preview": "Aperçu",
}
```

Add corresponding English keys to `en.ts` with English text.

Note: The i18n dictionaries use a flat key structure with dot notation. Adjust to match the existing dictionary format in fr.ts (check if it uses nested or flat keys).
</action>
  <acceptance_criteria>
    <criteria id="C1">nav-config.ts has Conformité, Clés API, Webhooks entries under Gouvernance group with ADMIN minRole</criteria>
    <criteria id="C2">login page has "Se connecter avec SSO" button below the email/password form</criteria>
    <criteria id="C3">login page has divider ("ou") between form and SSO button</criteria>
    <criteria id="C4">i18n fr.ts has new keys for sso, apiKeys, webhooks, compliance, branding sections</criteria>
    <criteria id="C5">i18n en.ts has corresponding English translations</criteria>
  </acceptance_criteria>
  <verify>
    <automated>grep "Conformité" apps/dashboard/lib/nav-config.ts | grep -c "href" | xargs test 1 -eq</automated>
    <automated>grep "Clés API" apps/dashboard/lib/nav-config.ts | grep -c "href" | xargs test 1 -eq</automated>
    <automated>grep "Se connecter avec SSO" apps/dashboard/app/\(auth\)/login/page.tsx | grep -c "SSO" | xargs test 1 -ge</automated>
    <automated>grep "sso" apps/dashboard/lib/i18n/fr.ts | grep -c "title" | xargs test 1 -ge</automated>
    <automated>grep "apiKeys" apps/dashboard/lib/i18n/fr.ts | grep -c "title" | xargs test 1 -ge</automated>
  </verify>
  <done>Navigation includes enterprise sections (Conformité, Clés API, Webhooks). Login page shows SSO button. i18n dictionaries have enterprise translations in French and English.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard ↔ API | All fetchWithAuth calls use JWT Bearer token — same auth boundary as existing dashboard |
| Login page ↔ SSO redirect | Browser redirects to external IdP — must validate RelayState/state parameter on return |
| Logo URL input | User-supplied URL rendered in dashboard — must prevent XSS via Content-Security-Policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-26 | Information Disclosure | API key display in dashboard | mitigate | Raw key shown ONLY on creation in a controlled Dialog; closed dialog cannot be reopened; keyHash never sent to client |
| T-10-27 | Information Disclosure | Webhook signing secret in dashboard | mitigate | Signing secret NEVER displayed in UI after creation; only masked version shown (first 8 + last 4 chars) |
| T-10-28 | Cross-Site Scripting | Organization.logoUrl rendering | mitigate | Render logo as `<img src={logoUrl}>` with Content-Security-Policy img-src directive; sanitize URL before rendering |
| T-10-29 | Cross-Site Scripting | Organization.displayName | mitigate | React auto-escapes JSX content; displayName rendered as textContent, not dangerouslySetInnerHTML |
| T-10-30 | Elevation of Privilege | Dashboard admin pages | mitigate | All enterprise pages gated by `minRole: "ADMIN"` in nav-config; server-side @Roles("ADMIN") on all controllers |
| T-10-SC | Tampering | npm installs (no new packages in dashboard) | accept | No new npm packages — all components use existing shadcn/ui, lucide-react, and @repo/design |
</threat_model>

<verification>
- [ ] AppModule imports all 4 new modules
- [ ] Dashboard parametres page has tabbed SSO/API Keys/Webhooks/Branding sections
- [ ] Conformite, api-keys, webhooks dashboard pages exist
- [ ] Login page has SSO button
- [ ] Navigation config has enterprise entries
- [ ] i18n dictionaries have enterprise translations
</verification>

<success_criteria>
Enterprise administrators can configure all Phase 10 features through the dashboard UI. Settings page has tabbed sections for SSO configuration, API key management, webhook subscriptions, and white-label branding. Dedicated pages exist for compliance reports, API keys, and webhooks. Login page shows SSO button. Navigation menu includes enterprise sections. All UI text follows the UI-SPEC copy contract in French (primary) and English.
</success_criteria>

<output>
Create `.planning/phases/10-enterprise-grade/10-05-SUMMARY.md` when done
</output>
