---
phase: 05-bug-fixing-cross-platform-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-bug-fixing-cross-platform-polish/parity-matrix.md
  - apps/mobile/package.json
  - apps/mobile/app/_layout.tsx
  - apps/mobile/components/error-boundary.tsx
  - apps/mobile/.env.example
autonomous: true
requirements:
  - POL-02
  - POL-03
user_setup:
  - service: Sentry
    why: Crash reporting for Mobile (D-07)
    env_vars:
      - name: EXPO_PUBLIC_SENTRY_DSN
        source: "Sentry.io Dashboard -> Settings -> Projects -> [Your Project] -> Client Keys (DSN)"
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry.io Dashboard -> Settings -> Auth Tokens -> Create New Token (scopes: project:releases, project:releases, org:read)"

must_haves:
  truths:
    - "Parity matrix exists mapping every Dashboard route to its Mobile counterpart"
    - "Sentry crash reporting initialized in Mobile app root layout"
    - "Error boundary captures exceptions via Sentry"
    - "@sentry/react-native and @shopify/flash-list are installed and importable"
  artifacts:
    - path: ".planning/phases/05-bug-fixing-cross-platform-polish/parity-matrix.md"
      provides: "Dashboard-to-Mobile route mapping with status flags"
      contains: "28 Dashboard routes mapped to Mobile screens"
    - path: "apps/mobile/app/_layout.tsx"
      provides: "Sentry initialization before navigation"
      contains: "Sentry.init() and Sentry.wrap()"
    - path: "apps/mobile/components/error-boundary.tsx"
      provides: "Sentry exception capture in componentDidCatch"
      contains: "Sentry.captureException"
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "@sentry/react-native"
      via: "Sentry.init() call"
      pattern: "Sentry\\.init"
    - from: "apps/mobile/components/error-boundary.tsx"
      to: "@sentry/react-native"
      via: "Sentry.captureException in componentDidCatch"
      pattern: "Sentry\\.captureException"
---

<objective>
**Set up foundational infrastructure** for the entire Phase 5 polish effort: the parity matrix (D-05), Mobile crash reporting via Sentry (D-07), and all new Mobile dependencies (Sentry, FlashList, i18n packages).

**Purpose:** Establish the parity reference document that guides all subsequent work, and install/configure the Mobile performance and i18n dependencies so downstream plans can use them immediately.

**Output:** Parity matrix document, Sentry integration in Mobile, all new packages installed.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-bug-fixing-cross-platform-polish/05-CONTEXT.md
@.planning/phases/05-bug-fixing-cross-platform-polish/05-RESEARCH.md
@.planning/phases/05-bug-fixing-cross-platform-polish/05-UI-SPEC.md
@apps/mobile/app/_layout.tsx
@apps/mobile/components/error-boundary.tsx
@apps/mobile/package.json
@apps/dashboard/app/(dashboard) (README: directory listing)
@apps/mobile/app (README: directory listing)

<interfaces>
From apps/mobile/app/_layout.tsx:
```typescript
// Current root layout - uses Stack navigator with AuthProvider + ErrorBoundary
export default function RootLayout() { ... }
```

From apps/mobile/components/error-boundary.tsx:
```typescript
// Class component with getDerivedStateFromError + componentDidCatch
export class ErrorBoundary extends Component<Props, State> { ... }
```

From apps/mobile/package.json:
```json
"dependencies": {
  "expo": "~54.0.34",
  "@repo/design": "workspace:*",
  "expo-router": "~6.0.23",
  ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Full Parity Matrix (D-05)</name>
  <files>.planning/phases/05-bug-fixing-cross-platform-polish/parity-matrix.md</files>
  <read_first>
    @apps/dashboard/app/(dashboard) (listing of all 27 route directories + page.tsx)
    @apps/mobile/app (listing of all mobile screens)
    @apps/mobile/app/(tabs) (listing of tab screens)
    @.planning/phases/05-bug-fixing-cross-platform-polish/05-UI-SPEC.md (Section 7 for baseline mapping)
  </read_first>
  <action>
    Create a parity matrix document at `.planning/phases/05-bug-fixing-cross-platform-polish/parity-matrix.md`.

    Format: markdown table with columns:
    1. **#** — Sequential number
    2. **Dashboard Route** — Path under `apps/dashboard/app/(dashboard)/`, e.g. `acces/`
    3. **Dashboard Lines** — Line count of page.tsx (from `wc -l`)
    4. **Mobile Counterpart** — Path under `apps/mobile/app/`, e.g. `(tabs)/alerts.tsx`
    5. **Parity Status** — ✅ Exists / ⚠️ Missing / 🔲 Partial / 🚫 Admin-only
    6. **Key Data Fields** — What data the screen displays (derived from reading page.tsx)
    7. **Key Actions** — What user actions are available
    8. **Build Priority** — High / Medium / Low (for this phase)

    Use the UI-SPEC Section 7 baseline mapping as a starting point. Read each Dashboard page.tsx (limit 50 lines) to identify the data fields and actions. Map every Dashboard route (27 + dashboard home = 28 total).

    For the 20 currently-missing Mobile screens, assign a build priority:
    - **High**: Core operator workflows (acces, command-center, notifications, utilisateurs)
    - **Medium**: Important management screens (analytique, chronologie, audit, equipement, licences, vehicules)
    - **Low**: Admin/utility screens (api-keys, conformite, gouvernance, ia, maintenance, patterns, risque, schemas, webhooks)

    Open question flag: For any route where the page.tsx appears to be a redirect or simple wrapper, note it.
  </action>
  <verify>
    <automated>
      File exists and has expected headers:
      grep -c "Dashboard Route" .planning/phases/05-bug-fixing-cross-platform-polish/parity-matrix.md
      Should be &gt;= 1 (header row present)
    </automated>
  </verify>
  <done>
    Parity matrix created with all 28 Dashboard routes mapped. Each entry has parity status, data fields, actions, and priority. Document saved to parity-matrix.md.
  </done>
  <acceptance_criteria>
    - File `.planning/phases/05-bug-fixing-cross-platform-polish/parity-matrix.md` exists
    - File contains a table with columns: Dashboard Route, Mobile Counterpart, Parity Status, Data Fields, Actions, Priority
    - All 28 Dashboard routes are listed (27 directories + 1 dashboard home page.tsx)
    - Every route has a parity status (✅ / ⚠️ / 🔲 / 🚫)
    - Every ⚠️ Missing route has a build priority (High / Medium / Low)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install Mobile Dependencies</name>
  <files>apps/mobile/package.json, pnpm-lock.yaml (auto-updated)</files>
  <read_first>
    @apps/mobile/package.json (existing deps to verify versions)
  </read_first>
  <action>
    Install the following packages using pnpm:

    ```bash
    pnpm --filter @repo/mobile add @sentry/react-native@latest
    pnpm --filter @repo/mobile add @shopify/flash-list@latest
    pnpm --filter @repo/mobile add expo-localization
    pnpm --filter @repo/mobile add i18n-js
    ```

    Note: `@sentry/react-native` latest stable version must be compatible with Expo SDK 54 — check `npm view @sentry/react-native version` first.

    If pnpm install encounters peer dependency conflicts, use `--config.conflictResolution=manual` and document resolution in the task log.

    Verify installation by importing each package in a test script:
    ```bash
    node -e "require('@sentry/react-native'); console.log('sentry OK');" 2>&1 || true
    ```
    (This may fail in Node context since it's React Native — that's OK. The key verification is that pnpm install succeeds without errors.)
  </action>
  <verify>
    <automated>
      # Verify packages added to package.json
      grep -c "@sentry/react-native" apps/mobile/package.json
      grep -c "@shopify/flash-list" apps/mobile/package.json
      grep -c "expo-localization" apps/mobile/package.json
      grep -c "i18n-js" apps/mobile/package.json
      # All must return 1
    </automated>
  </verify>
  <done>
    All four packages appear in apps/mobile/package.json dependencies. pnpm install completed without errors. Lockfile updated.
  </done>
  <acceptance_criteria>
    - `apps/mobile/package.json` has `@sentry/react-native` in dependencies
    - `apps/mobile/package.json` has `@shopify/flash-list` in dependencies
    - `apps/mobile/package.json` has `expo-localization` in dependencies
    - `apps/mobile/package.json` has `i18n-js` in dependencies
    - `pnpm-lock.yaml` is updated
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Configure Sentry Integration in Mobile (D-07)</name>
  <files>apps/mobile/app/_layout.tsx, apps/mobile/components/error-boundary.tsx, apps/mobile/.env.example</files>
  <read_first>
    @apps/mobile/app/_layout.tsx (full file — current root layout structure)
    @apps/mobile/components/error-boundary.tsx (full file — current error boundary pattern)
    @apps/dashboard/app/layout.tsx (for env variable pattern reference, if exists)
    @.planning/phases/05-bug-fixing-cross-platform-polish/05-RESEARCH.md (Sentry integration pattern section)
  </read_first>
  <action>
    **Step A — Update `apps/mobile/_layout.tsx`:**
    Add Sentry initialization at the top of the file, wrapping the root component with `Sentry.wrap()`.

    ```typescript
    // Add at top of file (after existing imports)
    import * as Sentry from "@sentry/react-native";

    // Add before the RootLayout function
    Sentry.init({
      dsn: process.env.EXPO_PUBLIC_SENTRY_DSN,
      tracesSampleRate: 1.0,
      environment: __DEV__ ? "development" : "production",
      enableNative: true,
    });

    // Wrap the default export:
    // Change from: export default function RootLayout() {
    // To: export default Sentry.wrap(function RootLayout() {
    ```

    **Step B — Update `apps/mobile/components/error-boundary.tsx`:**
    Add Sentry exception capture in `componentDidCatch`:
    ```typescript
    import * as Sentry from "@sentry/react-native";
    // In componentDidCatch:
    componentDidCatch(error: Error, errorInfo: ErrorInfo) {
      Sentry.captureException(error, {
        extra: { componentStack: errorInfo.componentStack },
      });
    }
    ```

    **Step C — Add env var example to `apps/mobile/.env.example`:**
    Add lines:
    ```
    # Sentry (crash reporting)
    EXPO_PUBLIC_SENTRY_DSN=
    ```

    Do NOT add the actual DSN value — the user must create a Sentry project and provide it (recorded in user_setup frontmatter).

    Follow the existing code patterns from the codebase:
    - Import type syntax: `import * as Sentry from "@sentry/react-native"` (no type import for the namespace)
    - Use the same error boundary pattern (class component stays as-is, just add Sentry call)
    - Match the export pattern: `export default Sentry.wrap(function RootLayout() {`
  </action>
  <verify>
    <automated>
      # Verify Sentry.init exists in _layout.tsx
      grep -c "Sentry.init" apps/mobile/app/_layout.tsx
      # Verify Sentry.wrap wraps the export
      grep -c "Sentry.wrap" apps/mobile/app/_layout.tsx
      # Verify Sentry.captureException in error-boundary
      grep -c "Sentry.captureException" apps/mobile/components/error-boundary.tsx
      # Verify env var in .env.example
      grep -c "EXPO_PUBLIC_SENTRY_DSN" apps/mobile/.env.example
      # All must return 1
    </automated>
  </verify>
  <done>
    Sentry.init called in _layout.tsx before navigation, root export wrapped with Sentry.wrap(), error-boundary.tsx captures exceptions via Sentry.captureException(), and EXPO_PUBLIC_SENTRY_DSN documented in .env.example.
  </done>
  <acceptance_criteria>
    - `apps/mobile/app/_layout.tsx` contains `Sentry.init({ dsn: process.env.EXPO_PUBLIC_SENTRY_DSN, ... })`
    - `apps/mobile/app/_layout.tsx` default export is wrapped with `Sentry.wrap()`
    - `apps/mobile/components/error-boundary.tsx` imports Sentry and calls `Sentry.captureException()` in componentDidCatch
    - `apps/mobile/.env.example` has `EXPO_PUBLIC_SENTRY_DSN=` entry
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile app → Sentry | Exception data sent from the app to Sentry's servers via @sentry/react-native SDK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Sentry DSN in env var | accept | DSN is a public key by design — it identifies the project but does not grant write access to Sentry org; the sensitive auth token (SENTRY_AUTH_TOKEN) is NOT included in the app bundle, only in CI/EAS build secrets |
| T-05-02 | Tampering | npm package installs | mitigate | @sentry/react-native [APPROVED] per RESEARCH.md Package Legitimacy Audit; @shopify/flash-list [APPROVED]; expo-localization [APPROVED]; i18n-js [APPROVED] |
| T-05-SC | Tampering | Package installs via pnpm | mitigate | All four packages verified as [APPROVED] in 05-RESEARCH.md Package Legitimacy Audit — no checkpoint needed |
</threat_model>

<verification>
1. Parity matrix document exists with all 28 Dashboard routes mapped
2. All four packages installed in apps/mobile/package.json
3. Sentry.init() present in _layout.tsx
4. error-boundary.tsx captures exceptions via Sentry
5. EXPO_PUBLIC_SENTRY_DSN documented in .env.example
</verification>

<success_criteria>
1. Parity matrix complete and accurate — all 28 routes mapped with status and priority
2. `pnpm install` completes without errors
3. Sentry integration compiles (TypeScript check passes: `pnpm --filter @repo/mobile check-types`)
4. error-boundary.tsx updated with Sentry capture
</success_criteria>

<output>
Create `.planning/phases/05-bug-fixing-cross-platform-polish/05-01-SUMMARY.md` when done
</output>
