---
phase: 05-bug-fixing-cross-platform-polish
plan: 07
type: execute
wave: 6
depends_on: [05-06]
files_modified:
  - apps/mobile/lib/api.ts
  - apps/mobile/lib/i18n/locales/fr.ts
  - apps/mobile/lib/i18n/locales/en.ts
  - apps/mobile/app/(tabs)/acces/index.tsx
  - apps/mobile/app/(tabs)/analytique/index.tsx
  - apps/mobile/app/(tabs)/chronologie/index.tsx
  - apps/mobile/app/(tabs)/command-center/index.tsx
  - apps/mobile/app/(tabs)/gouvernance/index.tsx
  - apps/mobile/app/(tabs)/ia/index.tsx
  - apps/mobile/app/(tabs)/licences/index.tsx
  - apps/mobile/app/(tabs)/risque/index.tsx
  - apps/mobile/app/(tabs)/utilisateurs/index.tsx
  - apps/mobile/app/(tabs)/vehicules/index.tsx
  - apps/mobile/app/(tabs)/notifications/index.tsx (enhancement of existing)
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/more/_layout.tsx
autonomous: true
requirements:
  - POL-02

must_haves:
  truths:
    - "All 28 Dashboard routes have parity Mobile screens"
    - "Complex screens (command-center, analytique, ia, licences, risque) match their Dashboard counterparts in data fields and actions"
    - "Navigation structure accommodates all new screens"
    - "All new screens use i18n system and @repo/design tokens"
  artifacts:
    - path: "apps/mobile/app/(tabs)/acces/index.tsx"
      provides: "Access control viewer (credentials, zones, schedules)"
    - path: "apps/mobile/app/(tabs)/analytique/index.tsx"
      provides: "Analytics dashboard (stats, charts, trends)"
    - path: "apps/mobile/app/(tabs)/command-center/index.tsx"
      provides: "Command center (live view, quick actions)"
    - path: "apps/mobile/app/(tabs)/ia/index.tsx"
      provides: "AI configuration (model settings, inference status)"
    - path: "apps/mobile/app/(tabs)/utilisateurs/index.tsx"
      provides: "User management (list, invite, manage roles)"
  key_links:
    - from: "apps/mobile/app/(tabs)/_layout.tsx"
      to: "apps/mobile/app/(tabs)/acces/index.tsx"
      via: "Tabs.Screen registration"
---

<objective>
**Build the remaining 11 parity Mobile screens** for the more complex Dashboard routes: acces, analytique, chronologie, command-center, gouvernance, ia, licences, risque, utilisateurs, vehicules, and full notifications. Register all new screens in navigation.

**Purpose:** Complete the parity gap (POL-02). After this plan, every Dashboard route has a Mobile counterpart showing the same data fields and actions.

**Output:** 11 new Mobile screens + all navigation changes complete.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-bug-fixing-cross-platform-polish/parity-matrix.md (reference for remaining screens)
@.planning/phases/05-bug-fixing-cross-platform-polish/05-UI-SPEC.md (Section 7)
@apps/mobile/lib/api.ts (verify function exists for each new screen)
@apps/dashboard/app/(dashboard)/acces/page.tsx (Dashboard reference)
@apps/dashboard/app/(dashboard)/analytique/page.tsx
@apps/dashboard/app/(dashboard)/chronologie/page.tsx
@apps/dashboard/app/(dashboard)/command-center/page.tsx
@apps/dashboard/app/(dashboard)/gouvernance/page.tsx
@apps/dashboard/app/(dashboard)/ia/page.tsx
@apps/dashboard/app/(dashboard)/licences/page.tsx
@apps/dashboard/app/(dashboard)/risque/page.tsx
@apps/dashboard/app/(dashboard)/utilisateurs/page.tsx
@apps/dashboard/app/(dashboard)/vehicules/page.tsx
@apps/mobile/app/(tabs)/_layout.tsx (tab navigator — needs update for new screens)
@apps/mobile/app/(tabs)/more.tsx (existing "more" tab — entry point for secondary screens)
@apps/mobile/app/(tabs)/more/_layout.tsx (existing "more" stack navigator)
@apps/mobile/app/(tabs)/guard/_layout.tsx (example of nested navigation pattern)

<interfaces>
From apps/mobile/app/(tabs)/_layout.tsx — current tab structure:
```typescript
<Tabs>
  <Tabs.Screen name="index" options={{ title: "Accueil" }} />
  <Tabs.Screen name="cameras" options={{ title: "Caméras" }} />
  <Tabs.Screen name="incidents" options={{ title: "Incidents" }} />
  <Tabs.Screen name="guard" options={{ title: "Garde" }} />
  <Tabs.Screen name="more" options={{ title: "Plus" }} />
</Tabs>
```

Navigation strategy for new screens:
- Primary workflow screens (command-center, acces) → new tabs or nested under existing tabs
- Admin/utility screens (ia, licences, gouvernance, risque, schemas from previous plan) → nested in "more" tab stack
- User-facing management screens (utilisateurs, vehicules) → nested in "more" tab
- Analytics/chronologie → nested under existing tabs or "more"

The "more" tab currently has a Stack navigator with chat.tsx. Extend it to include ALL secondary screens.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Remaining API Client Functions for Complex Screens</name>
  <files>apps/mobile/lib/api.ts</files>
  <read_first>
    @apps/mobile/lib/api.ts (check which functions already exist from Plan 06)
    @apps/dashboard/lib/api.ts (find functions for acces, analytique, chronologie, command-center, gouvernance, ia, licences, risque, users, vehicules)
  </read_first>
  <action>
    Add remaining API client functions that weren't covered in Plan 06. Check which exist and add missing ones.

    **Access control:**
    - `AccessCredential` interface (id, type, status, userId, userName, siteId, issuedAt, expiresAt)
    - `AccessZone` interface (id, name, siteId, level, scheduleId)
    - `AccessSchedule` interface (id, name, timezone, entries: ScheduleEntry[])
    - `fetchAccessCredentials(params?): Promise<PaginatedResponse<AccessCredential>>`
    - `fetchAccessZones(params?): Promise<PaginatedResponse<AccessZone>>`

    **Analytics:**
    - `AnalyticsData` interface (events over time, top cameras, alert distribution, etc.)
    - `fetchAnalytics(params?): Promise<AnalyticsData>`

    **Timeline/Chronologie:**
    - `TimelineEvent` interface (id, type, timestamp, description, cameraId, userId, metadata)
    - `fetchTimeline(params?): Promise<PaginatedResponse<TimelineEvent>>`

    **Command Center:**
    - `CommandCenterStatus` interface (activeAlerts, onlineCameras, doorStates, recentEvents, etc.)
    - `fetchCommandCenterStatus(): Promise<CommandCenterStatus>`

    **Governance:**
    - `GovernancePolicy` interface (id, name, type, retentionDays, status, updatedAt)
    - `fetchGovernancePolicies(): Promise<GovernancePolicy[]>`

    **AI/IA:**
    - `AIConfig` interface (id, name, model, status, capabilities, lastUpdated)
    - `fetchAIConfig(): Promise<AIConfig>`
    - `updateAIConfig(data): Promise<AIConfig>`

    **Licenses:**
    - `LicenseInfo` interface (id, key, type, status, seats, used, expiresAt, features)
    - `fetchLicenses(): Promise<LicenseInfo[]>`
    - `activateLicense(key): Promise<LicenseInfo>`

    **Risk:**
    - `RiskScore` interface (overall score, category scores, trends, recommendations)
    - `fetchRiskScores(params?): Promise<RiskScore>`

    **Users (admin):**
    - `fetchUsers(params?): Promise<PaginatedResponse<UserProfile>>` (if not already present — there may be a fetchUsers-like function)
    - `createUser(data): Promise<UserProfile>`
    - `updateUser(id, data): Promise<UserProfile>`
    - `deleteUser(id): Promise<void>`

    **Vehicles:**
    - `VehicleRecord` interface (id, plate, make, model, color, siteId, lastSeen, status)
    - `fetchVehicles(params?): Promise<PaginatedResponse<VehicleRecord>>`

    All functions follow the existing fetchWithAuth pattern. Verify API endpoints exist by cross-referencing with the API module structure.
  </action>
  <verify>
    <automated>
      grep -c "export async function fetchAccessCredentials\|export async function fetchAnalytics\|export async function fetchTimeline\|export async function fetchCommandCenterStatus\|export async function fetchGovernancePolicies\|export async function fetchAIConfig\|export async function fetchLicenses\|export async function fetchRiskScores\|export async function fetchVehicles" apps/mobile/lib/api.ts || echo "Some or all functions already added in Plan 06 — verify individually"
    </automated>
  </verify>
  <done>
    All remaining API client functions added to mobile api.ts. All API types defined. Total API function count reflects parity with Dashboard's feature areas.
  </done>
  <acceptance_criteria>
    - Access control API functions added
    - Analytics, timeline, command center API functions added
    - Governance, AI, licenses, risk API functions added  
    - User management and vehicles API functions added
    - All use fetchWithAuth pattern
    - All have proper TypeScript interfaces
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build 6 Complex Parity Screens — Access, Analytics, Timeline, Command Center, Governance, AI</name>
  <files>
    apps/mobile/app/(tabs)/acces/index.tsx,
    apps/mobile/app/(tabs)/analytique/index.tsx,
    apps/mobile/app/(tabs)/chronologie/index.tsx,
    apps/mobile/app/(tabs)/command-center/index.tsx,
    apps/mobile/app/(tabs)/gouvernance/index.tsx,
    apps/mobile/app/(tabs)/ia/index.tsx,
    apps/mobile/lib/i18n/locales/fr.ts,
    apps/mobile/lib/i18n/locales/en.ts
  </files>
  <read_first>
    @apps/dashboard/app/(dashboard)/acces/page.tsx (complex — credentials, zones, schedules)
    @apps/dashboard/app/(dashboard)/analytique/page.tsx (597 lines — analytics dashboard with metrics/charts)
    @apps/dashboard/app/(dashboard)/chronologie/page.tsx (535 lines — timeline event log)
    @apps/dashboard/app/(dashboard)/command-center/page.tsx (371 lines — live ops dashboard)
    @apps/dashboard/app/(dashboard)/gouvernance/page.tsx (439 lines — governance policies)
    @apps/dashboard/app/(dashboard)/ia/page.tsx (591 lines — AI configuration)
    @apps/mobile/app/(tabs)/index.tsx (mobile screen pattern — has stats cards, good reference for analytique)
  </read_first>
  <action>
    Create 6 Mobile screens. Each demonstrates the same data fields and actions as its Dashboard counterpart, adapted for mobile layout.

    **acces/index.tsx** — Access Control
    - Tab or segmented view switching between: Credentials, Zones, Schedules
    - Credentials list: card showing badge type, user name, status, validity dates
    - Zones list: zone name, access level, schedule info
    - Simple segmented control at top using TouchableOpacity buttons
    - Each section has FlashList, pull-to-refresh, loading/error/empty states
    - i18n keys: acces.title, acces.credentials, acces.zones, acces.schedules, acces.status.active, acces.status.expired, etc.

    **analytique/index.tsx** — Analytics Dashboard
    - Summary cards at top (like home page StatsCard): total events, active alerts, camera uptime
    - ScrollView with FlashList for event type breakdown
    - Use StatsCard component (already exists, already memoized from Plan 04)
    - Simplified chart representation using color bars/indicators (no recharts dependency on mobile)
    - Pull-to-refresh on ScrollView
    - i18n keys: analytique.title, analytique.totalEvents, analytique.activeAlerts, analytique.cameraUptime, analytique.eventBreakdown, analytique.trends

    **chronologie/index.tsx** — Timeline
    - FlashList of TimelineEvent items
    - Each item: timestamp (formatted), event type icon, description, camera/source name
    - Group by date with section headers (use FlashList's sticky header or manual grouping)
    - Pull-to-refresh, paginated loading (infinite scroll)
    - Color-coded by event type (access=cyan, alert=red, system=gray, etc.)
    - i18n keys: chronologie.title, chronologie.timestamp, chronologie.event, chronologie.source, chronologie.empty, chronologie.groupToday, chronologie.groupYesterday

    **command-center/index.tsx** — Command Center
    - Top section: Quick stats row (active alerts count, online cameras, door states)
    - Middle section: Recent events list (FlashList, last 20 events)
    - Bottom: Quick action buttons (like guard/index.tsx)
    - Pull-to-refresh overall
    - This is the mobile ops dashboard — prioritize real-time information
    - i18n keys: commandCenter.title, commandCenter.activeAlerts, commandCenter.onlineCameras, commandCenter.doorStates, commandCenter.recentEvents, commandCenter.empty

    **gouvernance/index.tsx** — Governance Policies
    - FlashList of GovernancePolicy items
    - Each item: policy name, type badge, retention period, status toggle, last updated
    - Pull-to-refresh
    - i18n keys: gouvernance.title, gouvernance.name, gouvernance.type, gouvernance.retention, gouvernance.status, gouvernance.updatedAt, gouvernance.empty

    **ia/index.tsx** — AI Configuration
    - Card-based layout showing: model name, status (online/offline), capabilities list
    - Toggle switches for enabling/disabling AI capabilities
    - Status section with last inference time, frames processed
    - ScrollView (not FlashList — this is a config screen, not a list)
    - i18n keys: ia.title, ia.model, ia.status, ia.capabilities, ia.lastInference, ia.framesProcessed, ia.updateConfig

    For each screen, add all new i18n keys to both fr.ts and en.ts.
  </action>
  <verify>
    <automated>
      for screen in acces analytique chronologie command-center gouvernance ia; do
        test -f "apps/mobile/app/(tabs)/$screen/index.tsx" && echo "$screen OK" || echo "$screen MISSING"
        grep -c "useTranslation" "apps/mobile/app/(tabs)/$screen/index.tsx" 2>/dev/null || echo "  no useTranslation"
      done
    </automated>
  </verify>
  <done>
    6 complex Mobile screens created: acces, analytique, chronologie, command-center, gouvernance, ia. Each matches its Dashboard counterpart in data fields and actions.
  </done>
  <acceptance_criteria>
    - All 6 screen files exist with index.tsx
    - Each uses appropriate data fetching from api.ts
    - Each has loading, error, empty states
    - Each uses FlashList (for list screens) or ScrollView (for config screens)
    - Each uses useTranslation() for all UI text
    - Each uses colors/typography from @repo/design
    - All i18n keys added to both fr.ts and en.ts
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build Remaining 5 Parity Screens + Navigation Registration</name>
  <files>
    apps/mobile/app/(tabs)/licences/index.tsx,
    apps/mobile/app/(tabs)/risque/index.tsx,
    apps/mobile/app/(tabs)/utilisateurs/index.tsx,
    apps/mobile/app/(tabs)/vehicules/index.tsx,
    apps/mobile/app/(tabs)/notifications/index.tsx (enhance existing),
    apps/mobile/app/(tabs)/_layout.tsx,
    apps/mobile/app/(tabs)/more/_layout.tsx,
    apps/mobile/app/_layout.tsx,
    apps/mobile/lib/i18n/locales/fr.ts,
    apps/mobile/lib/i18n/locales/en.ts
  </files>
  <read_first>
    @apps/dashboard/app/(dashboard)/licences/page.tsx (533 lines)
    @apps/dashboard/app/(dashboard)/risque/page.tsx (537 lines)
    @apps/dashboard/app/(dashboard)/utilisateurs/page.tsx (305 lines)
    @apps/dashboard/app/(dashboard)/vehicules/page.tsx (426 lines)
    @apps/mobile/app/notifications.tsx (existing standalone screen)
    @apps/mobile/app/(tabs)/_layout.tsx (current tab config)
    @apps/mobile/app/(tabs)/more/_layout.tsx (current more stack config)
  </read_first>
  <action>
    **Step A — Create 5 more parity screens:**

    **licences/index.tsx** — License management
    - List of LicenseInfo with FlashList
    - Each item: license key (masked), type badge, status (active/expired), seats used/total, expiration date
    - "Activer une licence" button → Alert.prompt for license key → activateLicense()
    - Pull-to-refresh
    - i18n keys: licences.title, licences.key, licences.type, licences.status, licences.seats, licences.expiresAt, licences.activate, licences.empty

    **risque/index.tsx** — Risk scoring
    - Top: Overall risk score displayed prominently (large number with color: green/yellow/red)
    - Category breakdown cards: each category with score and trend direction
    - FlashList of risk recommendations
    - Pull-to-refresh
    - i18n keys: risque.title, risque.overallScore, risque.category, risque.score, risque.trend, risque.recommendation, risque.empty

    **utilisateurs/index.tsx** — User management
    - FlashList of user profiles
    - Each item: avatar (initials), name, email, role badge, status indicator
    - Search/filter at top
    - "Inviter" button → createUser() with Alert.prompt name/email/role
    - Long-press → options: suspend, delete, edit role
    - Pull-to-refresh
    - i18n keys: utilisateurs.title, utilisateurs.name, utilisateurs.email, utilisateurs.role, utilisateurs.status, utilisateurs.invite, utilisateurs.suspend, utilisateurs.delete, utilisateurs.empty, utilisateurs.search

    **vehicules/index.tsx** — Vehicle/ANPR records
    - FlashList of VehicleRecord
    - Each item: license plate (large, monospace), make/model/color, last seen location + timestamp, status badge
    - Search by plate number at top
    - Pull-to-refresh
    - i18n keys: vehicules.title, vehicules.plate, vehicules.make, vehicules.model, vehicules.color, vehicules.lastSeen, vehicules.site, vehicules.status, vehicules.search, vehicules.empty

    **notifications/index.tsx** — Enhance existing standalone screen
    - Read `apps/mobile/app/notifications.tsx` first
    - Add: FlashList, i18n migration (if not already done), full loading/error/empty states
    - Ensure shows: notification title, message, timestamp, read/unread indicator
    - Pull-to-refresh
    - Add tabs or filters for "Toutes" vs "Non lues"

    **Step B — Navigation Registration:**
    Update `apps/mobile/app/(tabs)/_layout.tsx` to register all new screens.

    Strategy (choose the most practical given Mobile's tab-based navigation):
    - Primary screens (acces, command-center) → could be new tabs or nested under existing tabs
    - Most admin/utility screens → nested as stack screens in the "more" tab
    - Update `apps/mobile/app/(tabs)/more/_layout.tsx` to add Stack.Screen entries for each new screen
    
    Example for more/_layout.tsx:
    ```typescript
    <Stack.Screen name="audit/index" options={{ title: "Audit" }} />
    <Stack.Screen name="api-keys/index" options={{ title: "Clés API" }} />
    // ... etc
    ```

    Also update `apps/mobile/app/_layout.tsx` if needed for any standalone screens.

    NOTE: Not all 20 new screens need top-level tab entries. Some can be reachable through the "more" tab which serves as a menu for secondary screens. The key is that they exist and are navigable.

    **Step C — Verify:**
    After creating all screens, run `pnpm --filter @repo/mobile check-types` (or best effort) to catch any import/type errors.
  </action>
  <verify>
    <automated>
      for screen in licences risque utilisateurs vehicules; do
        test -f "apps/mobile/app/(tabs)/$screen/index.tsx" && echo "$screen OK" || echo "$screen MISSING"
      done
      # Verify tab layout updated
      grep -c "utilisateurs\|licences\|vehicules" apps/mobile/app/(tabs)/_layout.tsx apps/mobile/app/(tabs)/more/_layout.tsx
      # Verify i18n keys added
      grep -c "licences" apps/mobile/lib/i18n/locales/fr.ts
      grep -c "risque" apps/mobile/lib/i18n/locales/fr.ts
      grep -c "utilisateurs" apps/mobile/lib/i18n/locales/fr.ts
      grep -c "vehicules" apps/mobile/lib/i18n/locales/fr.ts
    </automated>
  </verify>
  <done>
    All 11 remaining parity screens created. Navigation structure updated to include all screens. All screens use i18n and @repo/design.
  </done>
  <acceptance_criteria>
    - 5 new screen files created (licences, risque, utilisateurs, vehicules, notifications enhancement)
    - All screens have loading/error/empty states
    - All screens use useTranslation() and @repo/design
    - Tab navigator or more-stack updated for all new screens
    - i18n dictionaries have all new keys in both fr.ts and en.ts
    - TypeScript compiles (check-types best effort)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile app → API | New screens call existing API endpoints with JWT auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Elevation of Privilege | User management screen (utilisateurs) | mitigate | Only available to users with existing admin/manager role — relies on existing JWT auth and backend role guards; mobile client does NOT bypass server-side authorization |
| T-05-13 | Tampering | License activation | accept | activateLicense() calls existing backend endpoint that validates license keys server-side |
</threat_model>

<verification>
1. All 11 remaining parity screens created (acces, analytique, chronologie, command-center, gouvernance, ia, licences, risque, utilisateurs, vehicules, notifications enhancement)
2. All API functions exist for these screens
3. Navigation registration complete
4. All screens use i18n and @repo/design
5. Total: 28 Dashboard routes → 28 Mobile screens parity achieved
</verification>

<success_criteria>
1. Every Dashboard route has a Mobile counterpart screen (per parity matrix)
2. Each Mobile screen shows the same data fields and offers the same actions as its Dashboard counterpart
3. Navigation is functional (all screens reachable through tabs or stack navigation)
4. All UI text uses the i18n system
5. All design tokens come from @repo/design
</success_criteria>

<output>
Create `.planning/phases/05-bug-fixing-cross-platform-polish/05-07-SUMMARY.md` when done
</output>
