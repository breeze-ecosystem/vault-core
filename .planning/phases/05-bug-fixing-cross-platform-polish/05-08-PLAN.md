---
phase: 05-bug-fixing-cross-platform-polish
plan: 08
type: execute
wave: 7
depends_on: [05-07]
files_modified: []
autonomous: false
requirements:
  - POL-01
  - POL-02
  - POL-03
  - POL-04

must_haves:
  truths:
    - "All 28 Mobile screens have been verified against their fixed Dashboard counterparts"
    - "All 6 standard operator workflows complete without crashes or performance issues"
    - "No translation gaps remain — French and English have identical coverage across both platforms"
    - "The phase goal is achieved: zero known production bugs, full cross-platform consistency, crash-free Mobile, complete translations"
  artifacts:
    - path: ".planning/phases/05-bug-fixing-cross-platform-polish/05-UAT.md"
      provides: "User Acceptance Test log documenting verification of all success criteria"
  key_links:
    - from: "05-UAT.md"
      to: "parity-matrix.md"
      via: "Cross-reference all 28 parity items"
---

<objective>
**Final verification pass** — walk through all 28 Mobile screens against the fixed Dashboard, run through all standard operator workflows, and confirm the phase's four success criteria are met (POL-01 through POL-04).

**Purpose:** This is the quality gate before phase sign-off. Every bug is fixed, every screen has parity, Mobile is performant, and translations are complete.

**Output:** UAT verification log, phase sign-off.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-bug-fixing-cross-platform-polish/parity-matrix.md (reference — verify every entry)
@.planning/phases/05-bug-fixing-cross-platform-polish/05-RESEARCH.md (Mobile bug audit checklist)
@.planning/phases/05-bug-fixing-cross-platform-polish/05-UI-SPEC.md (Section 8 — Standard Operator Workflows, expected behaviors)
@.planning/phases/05-bug-fixing-cross-platform-polish/05-CONTEXT.md (D-01 through D-10 — verify all decisions implemented)
@apps/mobile/app/(tabs)/alerts.tsx (representative mobile screen for spot-check)
@apps/dashboard/app/(dashboard)/alertes/page.tsx (representative dashboard route for comparison)

<interfaces>
From UI-SPEC Section 8 — Standard Operator Workflows (must ALL complete without crashes):
1. View cameras → browse camera grid → tap camera → view stream → back to grid
2. Respond to alerts → view alert list → tap alert → view details → acknowledge → back to list
3. Check door status → open door control → view door states → toggle lock → confirm
4. Manage visitors → view visitor queue → tap visitor → check in / check out → confirm
5. View incidents → view incident list → tap incident → view details → update status
6. AI Chat → open chat → send message → view response → send follow-up

Mobile Bug Audit Checklist (from RESEARCH.md):
1. Data display — same fields as Dashboard?
2. Actions parity — same actions as Dashboard?
3. Empty state — follows UI-SPEC copy (French)?
4. Error state — retry mechanism present?
5. Loading state — ActivityIndicator or skeleton?
6. Navigation — tab/stack navigation works, no jank?
7. Crash-free — no unhandled exceptions during the workflow?
8. Design token usage — uses @repo/design consistently?
9. i18n — all strings through t() function?
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final Cross-Platform Verification — Code Audit</name>
  <files>
    (Code review of all parity screens — no file modifications)
  </files>
  <read_first>
    @.planning/phases/05-bug-fixing-cross-platform-polish/parity-matrix.md (full matrix — use as checklist)
    @apps/mobile/app/(tabs)/alerts.tsx (sample screen to verify checklist items)
    @apps/mobile/app/(tabs)/cameras.tsx (sample screen)
    @apps/mobile/app/(tabs)/acces/index.tsx (new complex screen)
    @apps/mobile/app/(tabs)/command-center/index.tsx (new complex screen)
  </read_first>
  <action>
    Perform a final code-level audit across ALL Mobile screens to verify consistency and correctness.

    Use the parity matrix as a checklist. For EACH of the 28 parity entries, read both the Dashboard route's page.tsx and the Mobile screen's index.tsx (or equivalent) and verify:

    **Data Fields:**
    - Mobile screen fetches and displays the same fields as Dashboard
    - No data fields are missing on Mobile that exist on Dashboard
    - Field types match (string, number, status enum, timestamp format)
    
    **Actions:**
    - All actions available on Dashboard (acknowledge, resolve, create, delete, toggle, etc.) have Mobile equivalents
    - Actions use the same API endpoints (via the api.ts functions)
    - Loading state shown during action execution
    - Success/error feedback shown after action completes
    
    **States:**
    - Loading: Mobile screen shows ActivityIndicator or skeleton pattern
    - Error: Mobile screen shows error message with retry button (French)
    - Empty: Mobile screen shows meaningful empty state message (French, from UI-SPEC Section 4)
    - Refetch: Pull-to-refresh on list screens

    **i18n:**
    - All user-facing strings go through t() calls
    - Hardcoded strings in new screens all migrated
    - Both fr.ts and en.ts have keys for all t() calls made

    **Design Tokens:**
    - Colors imported from @repo/design (not from @/lib/theme directly, not hardcoded hex values)
    - Typography imported from @repo/design or theme.ts re-exports
    - No hardcoded color values (except for status-specific colors like severity indicators)

    **Performance:**
    - List screens use FlashList (not ScrollView)
    - Card components used in lists are wrapped with React.memo
    - No console.log in production code paths

    Flag any issues found in the audit log. Fix any issues immediately (this is an audit+FIX task).
    Create `.planning/phases/05-bug-fixing-cross-platform-polish/05-UAT.md` documenting the audit results.
  </action>
  <verify>
    <automated>
      # Verify all parity screens exist
      echo "=== Parity Screen Existence Check ==="
      for screen in audit api-keys conformite webhooks schemas equipement maintenance patterns acces analytique chronologie command-center gouvernance ia licences risque utilisateurs vehicules; do
        if [ -f "apps/mobile/app/(tabs)/$screen/index.tsx" ]; then
          echo "✅ $screen/index.tsx"
        else
          echo "❌ $screen/index.tsx MISSING"
        fi
      done
      # Count total parity screens (existing + new)
      echo "=== Summary ==="
      echo "Existing screens: alerts cameras incidents sites chat settings guard more index notifications"
      echo "New screens added: audit api-keys conformite webhooks schemas equipement maintenance patterns acces analytique chronologie command-center gouvernance ia licences risque utilisateurs vehicules"
      echo "Total new: 19"
      echo "Grand total (est.): ~28"
    </automated>
  </verify>
  <done>
    Full code-level audit complete. Parity matrix verified. All screens have correct data fields, actions, states, i18n, design tokens, and performance patterns. UAT log created with findings.
  </done>
  <acceptance_criteria>
    - Parity matrix is fully verified: all 28 Dashboard routes have Mobile counterparts
    - Each Mobile screen has: loading/error/empty states, i18n, @repo/design tokens, FlashList (lists), React.memo (cards)
    - UAT.md created documenting verification results
    - Any issues found were fixed immediately
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-Verify — Full Phase Validation</name>
  <files>
    (Human verification — no code changes)
  </files>
  <read_first>
    @.planning/phases/05-bug-fixing-cross-platform-polish/05-CONTEXT.md (decisions D-01 through D-10)
    @.planning/phases/05-bug-fixing-cross-platform-polish/05-UAT.md (if created in Task 1)
  </read_first>
  <action>
    No automated actions. This is a blocking human-verify checkpoint.

    The human needs to verify the phase's four success criteria from ROADMAP.md:

    1. **POL-01: Bugs fixed** — Walk through a sampling of Dashboard routes and Mobile screens. Verify that previously known issues are resolved. Confirm no new regressions introduced.

    2. **POL-02: Cross-platform consistency** — Spot-check 5-6 key screens (e.g., Alerts, Cameras, Dashboard Home, Door Control, Incidents) on both Dashboard and Mobile. Verify same data fields and same actions appear on both platforms. Visual differences are acceptable per D-04.

    3. **POL-03: Mobile stability** — Run through the 6 standard operator workflows:
       - Browse cameras → tap camera → view → back
       - View alerts → tap → acknowledge → back
       - Check door status → toggle lock → confirm
       - Manage visitors → check-in/out → confirm
       - View incidents → update status
       - AI Chat → send message → view response
       
       Verify: no crashes, smooth navigation (60fps), no visible jank.

    4. **POL-04: Translation audit** — Spot-check 3-4 screens on both platforms. Verify:
       - French text is consistent between Dashboard and Mobile
       - No untranslated English strings visible
       - No hardcoded French strings that bypass the i18n system (on Dashboard)

    **If all pass** → Type "approve" to complete the phase.
    **If issues found** → Describe the specific issues. The agent will fix them and return to this checkpoint.
  </action>
  <what-built>
    - Dashboard: All 28 routes audited and bugs fixed
    - Dashboard: i18n audit complete — all strings in dictionaries, en.ts matches fr.ts
    - Mobile: Sentry crash reporting integrated
    - Mobile: Design tokens reconciled with @repo/design
    - Mobile: Full i18n framework (expo-localization + i18n-js) with locale files
    - Mobile: 19 new parity screens built for previously-missing Dashboard routes
    - Mobile: List screens converted to FlashList for 60fps scrolling
    - Mobile: Card components optimized with React.memo
    - Mobile: Console.log removed from production code
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    - Dashboard running locally or on staging
    - Mobile app running on device or simulator
    - Ensure API is accessible from both

    **Step 1: Dashboard Spot-Check** (5 min)
    - Navigate to 5 different Dashboard routes (e.g., Cameras, Alerts, Settings, Audit, Command Center)
    - Verify pages load data, errors show error UI, empty lists show empty state
    
    **Step 2: Mobile Parity Check** (10 min)
    - Compare 5-6 key screens between Dashboard and Mobile:
      - Dashboard: alertes → Mobile: alerts tab
      - Dashboard: cameras → Mobile: cameras tab
      - Dashboard: portes → Mobile: guard → door control
      - Dashboard: visiteurs → Mobile: guard → QR check-in
      - Dashboard: home → Mobile: home tab
    - Verify same data fields appear on both

    **Step 3: Mobile Workflows** (5 min)
    - Run through camera browsing: open app → cameras tab → tap camera → view detail → back
    - Run through alert response: alerts tab → tap alert → view → acknowledge
    - Verify no crashes, smooth navigation

    **Step 4: Translation Check** (3 min)
    - Check a Dashboard page — all text in French?
    - Check same Mobile screen — all text in French?
    - No English strings in unexpected places?

    **Expected result:** All checks pass. Any issues should be specific and reproducible.
  </how-to-verify>
  <resume-signal>
    Type "approve" to complete Phase 5.
    Or describe specific issues found for the agent to fix.
  </resume-signal>
  <done>
    Human verification complete. Phase 5 signed off. All success criteria met.
  </done>
  <acceptance_criteria>
    - Human confirms all four POL success criteria are met
    - No blocking bugs remain
    - Parity is visually confirmed on sampled screens
    - Mobile workflows complete without crashes
    - Translations are complete and consistent
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Verification/audit phase — no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Tampering | UAT verification log | accept | Log documents findings but contains no executable code |
</threat_model>

<verification>
1. Parity matrix cross-referenced and verified
2. All 28 Mobile screens have correct data/actions
3. No crashes in standard workflows
4. Translations consistent across platforms
5. Human approval received
</verification>

<success_criteria>
1. All parity matrix items verified and correct
2. UAT document created
3. Human confirmation that all four POL success criteria are met
4. Phase ready for sign-off
</success_criteria>

<output>
Create `.planning/phases/05-bug-fixing-cross-platform-polish/05-08-SUMMARY.md` when done
</output>
