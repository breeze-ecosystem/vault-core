---
phase: 01-infrastructure-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - edge/agent/tests/__init__.py
  - edge/agent/tests/test_config.py
  - edge/agent/tests/test_models.py
  - edge/agent/tests/test_mqtt_task.py
  - edge/agent/tests/test_http_task.py
  - edge/agent/tests/test_serial_task.py
  - edge/agent/tests/test_metrics.py
  - edge/agent/tests/integration/test_mqtt_integration.py
  - edge/agent/tests/integration/__init__.py
  - edge/agent/tests/integration/docker-compose.test.yml
  - edge/agent/pytest.ini
  - edge/agent/Makefile
autonomous: false
requirements: [INF-01, INF-02, INF-03]

must_haves:
  truths:
    - "Unit tests verify config loading, model serialization, MQTT reconnect logic, HTTP error handling"
    - "Integration test proves Edge Agent can connect to Mosquitto with TLS + password auth, publish a message, and receive it"
    - "Build validation: Docker image builds successfully from edge/agent/Dockerfile"
    - "pytest runs in CI with mocked I/O for unit tests, no hardware required"
    - "Makefile targets for test, build, lint"
  artifacts:
    - path: "edge/agent/tests/test_config.py"
      provides: "Config validation tests"
      contains: "test_defaults, test_env_override, test_serial_ports_parsing"
    - path: "edge/agent/tests/test_mqtt_task.py"
      provides: "MQTT task unit tests with mocked aiomqtt"
      contains: "test_reconnect_loop, test_buffer_overflow, test_message_parsing"
    - path: "edge/agent/tests/integration/test_mqtt_integration.py"
      provides: "Integration test with real Mosquitto container"
      contains: "test_publish_subscribe_roundtrip, test_tls_connection"
    - path: "edge/agent/tests/integration/docker-compose.test.yml"
      provides: "Test infrastructure with Mosquitto"
      contains: "mosquitto service with TLS config"
    - path: "edge/agent/pytest.ini"
      provides: "pytest configuration"
      contains: "testpaths = tests"
    - path: "edge/agent/Makefile"
      provides: "Build and test targets"
      contains: "test, build, lint"
  key_links:
    - from: "edge/agent/tests/test_mqtt_task.py"
      to: "edge/agent/tasks/mqtt_task.py"
      via: "patch aiomqtt.Client to test reconnect logic without real broker"
      pattern: "unittest.mock.patch.*aiomqtt"
    - from: "edge/agent/tests/integration/docker-compose.test.yml"
      to: "docker-compose.yml mosquitto service"
      via: "same Mosquitto TLS config pattern but standalone for test isolation"
      pattern: "eclipse-mosquitto:2.0"
---

<objective>
Create unit and integration test suites for the async Edge Agent rewrite, Mosquitto TLS configuration, and Docker networking. Verify that existing functionality (heartbeat, health monitoring, event forwarding) works correctly after the async rewrite.

Purpose: Ensure the rewrite preserves all existing capabilities and the new async architecture handles concurrent protocols correctly. These tests prove INF-01, INF-02, and INF-03 are complete.

Output: Test suite under `edge/agent/tests/` with unit tests (mocked I/O), integration tests (Mosquitto in Docker Compose), build validation, and Makefile targets.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@edge/agent/agent.py
@edge/agent/main.py
@edge/agent/config.py
@edge/agent/models.py
@edge/agent/tasks/mqtt_task.py
@edge/agent/tasks/serial_task.py
@edge/agent/tasks/http_task.py
@edge/agent/services/docker.py
@edge/agent/services/metrics.py
@edge/agent/requirements.txt
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests with mocked I/O for all modules</name>

  <files>
    - edge/agent/tests/__init__.py (new)
    - edge/agent/tests/test_config.py (new)
    - edge/agent/tests/test_models.py (new)
    - edge/agent/tests/test_mqtt_task.py (new)
    - edge/agent/tests/test_http_task.py (new)
    - edge/agent/tests/test_serial_task.py (new)
    - edge/agent/tests/test_metrics.py (new)
    - edge/agent/pytest.ini (new)
  </files>

  <read_first>
    @/home/devuser/projects/oversight-hub/edge/agent/config.py (read full file to understand Settings class and fields)
    @/home/devuser/projects/oversight-hub/edge/agent/models.py (read full file to understand all pydantic model classes)
    @/home/devuser/projects/oversight-hub/edge/agent/tasks/mqtt_task.py (read full file to understand mqtt_handler, buffer logic, publish_state)
    @/home/devuser/projects/oversight-hub/edge/agent/tasks/serial_task.py (read full file to understand serial_reader function)
    @/home/devuser/projects/oversight-hub/edge/agent/tasks/http_task.py (read full file to understand send_heartbeat, send_health_report, check_for_updates)
    @/home/devuser/projects/oversight-hub/edge/agent/services/metrics.py (read full file to understand system_metrics, service_status)
    @/home/devuser/projects/oversight-hub/.planning/phases/01-infrastructure-foundation/01-RESEARCH.md (lines 41-46 — D-05 testing approach: unit tests with mocked I/O)
  </read_first>

  <action>

  **1. `edge/agent/tests/__init__.py`** — empty file

  **2. `edge/agent/pytest.ini`**
  ```
  [pytest]
  testpaths = tests
  python_files = test_*.py
  asyncio_mode = auto
  markers =
      integration: marks tests that require Docker (Mosquitto) — skipped in CI
      slow: marks slow tests
  ```
  - `asyncio_mode = auto` enables async test functions via `pytest-asyncio` (add to test dependencies)

  **3. `edge/agent/tests/test_config.py`**
  - `test_defaults`: verify Settings() creates defaults for all fields (`MQTT_BROKER_HOST == "localhost"`, `MQTT_BROKER_PORT == 8883`, etc.)
  - `test_env_override`: use `monkeypatch.setenv()` to override each field and verify Settings reads them
  - `test_serial_ports_list`: set `SERIAL_PORTS="/dev/ttyUSB0,/dev/ttyAMA0"` and verify `settings.SERIAL_PORTS == ["/dev/ttyUSB0", "/dev/ttyAMA0"]`

  **4. `edge/agent/tests/test_models.py`**
  - `test_door_state_payload`: create with all fields, verify JSON serialization includes sequence
  - `test_badge_read_payload`: create with required fields, verify optional fields default to None
  - `test_health_payload`: create with required fields, verify serialization roundtrip
  - `test_heartbeat_payload`: verify nested dict structure
  - `test_sequence_included`: verify every model that should have `sequence` field actually includes it

  **5. `edge/agent/tests/test_mqtt_task.py`**
  - Uses `pytest.mark.asyncio` (auto via conf) and `unittest.mock.patch`
  - `test_mqtt_handler_reconnect`: patch `aiomqtt.Client.__aenter__` to raise `aiomqtt.MqttError` on first call, succeed on second; verify handler retries
  - `test_publish_message`: mock the client, call publish helper, verify `client.publish` was called with correct topic `site/{site_id}/door/{doorId}/state`
  - `test_subscribe_topic`: verify subscribe is called with `site/{site_id}/door/+/cmd`
  - `test_buffer_overflow`: fill the `asyncio.Queue` to maxsize+1, verify oldest message is dropped (FIFO eviction)
  - `test_replay_on_reconnect`: buffer a message after disconnect, then mock reconnect and verify publish is called for buffered message
  - `test_tls_params`: verify `aiomqtt.TLSParameters` is constructed with `ca_certs`, `cert_reqs=ssl.CERT_REQUIRED`, `certfile=None`
  - `test_message_parsing`: mock an incoming MQTT message, verify it's parsed as JSON and the topic path triggers the right handler

  **6. `edge/agent/tests/test_http_task.py`**
  - `test_heartbeat_payload_structure`: mock `aiohttp.ClientSession`, call `send_heartbeat` once, verify the POST payload contains `clientId`, `tier`, `timestamp`, `uptime`, `system`, `services`
  - `test_heartbeat_http_error`: mock session.post to raise `aiohttp.ClientError`, verify handler logs error and continues (doesn't crash)
  - `test_heartbeat_timeout`: mock session.post to raise `asyncio.TimeoutError`, verify handler continues
  - `test_health_report_calls_docker`: verify `send_health_report` calls the docker service status

  **7. `edge/agent/tests/test_serial_task.py`**
  - `test_serial_reader_timeout`: mock `serial_asyncio.open_serial_connection`, make `reader.readexactly` raise `asyncio.TimeoutError`, verify loop continues
  - `test_serial_shutdown`: set shutdown event, verify writer.close() and writer.wait_closed() are called in finally block
  - `test_serial_connection_error`: mock `open_serial_connection` to raise exception, verify error is logged and function exits

  **8. `edge/agent/tests/test_metrics.py`**
  - `test_system_metrics_structure`: call `system_metrics()` (runs in CI where /proc is available), verify return dict has keys `cpu`, `ram`, `disk`, `ramTotal`, `ramUsed`
  - `test_service_status_empty`: when Docker is unavailable, verify `service_status` returns False for all services (graceful degradation per existing agent behavior)
  </action>

  <acceptance_criteria>
    - All test files exist and pass `python3 -c "import ast; ast.parse(...)"`
    - `pytest edge/agent/tests/ -x --asyncio-mode=auto` runs without import errors (even if tests fail — the framework loads correctly)
    - `test_config.py` covers defaults, env override, and serial ports parsing
    - `test_mqtt_task.py` covers reconnect, publish, subscribe, buffer, replay, TLS params
    - `test_http_task.py` covers heartbeat structure, error handling, and timeout resilience
  </acceptance_criteria>

  <verify>
    <automated>test -f edge/agent/tests/__init__.py && test -f edge/agent/pytest.ini</automated>
    <automated>for f in test_config.py test_models.py test_mqtt_task.py test_http_task.py test_serial_task.py test_metrics.py; do test -f "edge/agent/tests/$f" && python3 -c "import ast; ast.parse(open('edge/agent/tests/$f').read())"; done</automated>
    <automated>grep -q "asyncio_mode" edge/agent/pytest.ini</automated>
  </verify>

  <done>Unit test suite created with mocked I/O for all modules: config, models, MQTT task, HTTP task, serial task, metrics.</done>
</task>

<task type="auto">
  <name>Task 2: Create integration tests + test infrastructure</name>

  <files>
    - edge/agent/tests/integration/__init__.py (new)
    - edge/agent/tests/integration/test_mqtt_integration.py (new)
    - edge/agent/tests/integration/docker-compose.test.yml (new)
    - edge/agent/Makefile (new)
  </files>

  <read_first>
    @/home/devuser/projects/oversight-hub/mosquitto/config/mosquitto.conf (read full file to use same TLS config in test compose)
    @/home/devuser/projects/oversight-hub/mosquitto/config/acl (read full file to use in test ACL)
    @/home/devuser/projects/oversight-hub/docker-compose.yml (read mosquitto-init and mosquitto service definitions for test inspiration)
    @/home/devuser/projects/oversight-hub/edge/agent/requirements.txt (check test dependency additions)
    @/home/devuser/projects/oversight-hub/.planning/phases/01-infrastructure-foundation/01-RESEARCH.md (lines 217-251 — aiomqtt reconnect pattern for integration test design)
  </read_first>

  <action>

  **1. `edge/agent/tests/integration/__init__.py`** — empty file

  **2. `edge/agent/tests/integration/docker-compose.test.yml`**
  - Standalone Mosquitto for tests using `eclipse-mosquitto:2.0`, container name `oversight-mqtt-test`
  - Ports: `1883:1883` and `8883:8883`
  - Volumes: mount `./mosquitto/config:/mosquitto/config:ro` for the mosquitto.conf and acl, plus `mosquitto_test_data` and `mosquitto_test_log` named volumes
  - Named volumes section with `mosquitto_test_data` and `mosquitto_test_log`
  - The test assumes Mosquitto is started externally (`make test-integration` starts it, or it uses `pytest-docker`).
  - Expose port 1883 for localhost test connections without TLS, and 8883 for TLS tests.
  - Reference the same `mosquitto.conf` that was created in Plan 01 — but allow override via `MOSQUITTO_CONF_DIR` env var pointing to `./mosquitto/config/` relative to the project root.

  **3. `edge/agent/tests/integration/test_mqtt_integration.py`**
  - Mark all tests with `@pytest.mark.integration`
  - Use `pytest.skip` if `MOSQUITTO_HOST` env var is not set (skip integration tests in CI without broker)
  - `test_connect_with_tls`:
    - Connect to `{MOSQUITTO_HOST}:8883` with TLS, `username`, `password` from env
    - Verify connection succeeds and client can subscribe to a test topic
  - `test_publish_subscribe_roundtrip`:
    - Two aiomqtt clients: subscriber and publisher
    - Subscriber subscribes to `site/test/door/+/state`
    - Publisher publishes to `site/test/door/1/state` with `{"state": "locked", "sequence": 1}`
    - Verify subscriber receives the message with correct payload
  - `test_tls_rejects_anonymous`:
    - Attempt to connect without TLS on port 8883 without username/password
    - Verify connection is rejected (aiomqtt.MqttError)
  - `test_tls_rejects_wrong_credentials`:
    - Connect with TLS on 8883 with wrong username/password
    - Verify connection is rejected
  - `test_buffer_replay`:
    - Use the MQTT task's `publish_state` function (import from task module)
    - Simulate a disconnect, buffer messages, reconnect, verify they're published

  **4. `edge/agent/Makefile`**
  - `test`: run unit tests — `cd edge/agent && python -m pytest tests/ -x -v --asyncio-mode=auto`
  - `test-integration`: `cd edge/agent && python -m pytest tests/integration/ -x -v --asyncio-mode=auto`
  - `test-all`: both unit + integration
  - `build`: `docker build -t oversight-edge-agent:test -f edge/agent/Dockerfile edge/agent/`
  - `lint`: `python -m py_compile main.py config.py models.py && python -m py_compile tasks/*.py services/*.py`
  - `test-ci`: runs unit tests only (integration tests need Docker)
  - Use Makefile conventions: `.PHONY` targets, `@` prefix for silent commands
  </action>

  <acceptance_criteria>
    - `docker-compose.test.yml` exists with Mosquitto service and config volumes
    - Integration tests verify TLS connection, publish/subscribe roundtrip, anonymous rejection, bad credentials rejection
    - `Makefile` has `test`, `test-integration`, `test-all`, `build`, `lint`, `test-ci` targets
    - Integration tests use `@pytest.mark.integration` marker
    - Tests skip gracefully when `MOSQUITTO_HOST` env var is not set
  </acceptance_criteria>

  <verify>
    <automated>test -f edge/agent/tests/integration/__init__.py && test -f edge/agent/tests/integration/test_mqtt_integration.py && test -f edge/agent/tests/integration/docker-compose.test.yml && test -f edge/agent/Makefile</automated>
    <automated>grep -q "integration" edge/agent/tests/integration/test_mqtt_integration.py && grep -q "pytest.mark.integration" edge/agent/tests/integration/test_mqtt_integration.py</automated>
    <automated>grep -q "publish_subscribe_roundtrip\|test_connect_with_tls\|test_tls_rejects_anonymous\|test_tls_rejects_wrong_credentials" edge/agent/tests/integration/test_mqtt_integration.py</automated>
    <automated>grep -q "^test:\|^test-integration:\|^build:" edge/agent/Makefile</automated>
  </verify>

  <done>Integration tests created with Docker Compose test infrastructure for Mosquitto. Tests cover TLS connect, pub/sub roundtrip, anonymous rejection, and credentials validation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Package legitimacy and build validation</name>

  <what-built>
    The following files have been created/modified:
    - Unit tests under `edge/agent/tests/`
    - Integration tests under `edge/agent/tests/integration/`
    - Test Docker Compose at `edge/agent/tests/integration/docker-compose.test.yml`
    - Makefile at `edge/agent/Makefile`
    - Requirements file at `edge/agent/requirements.txt` with new async packages

    All tests have been run and verified passing.
  </what-built>

  <how-to-verify>
    1. **Build the Docker image:**
       ```bash
       cd edge/agent && docker build -t oversight-edge-agent:test .
       ```
       Expected output: Build succeeds, output includes "Successfully tagged oversight-edge-agent:test"

    2. **Verify package legitimacy (per RESEARCH.md Package Legitimacy Audit):**
       Visit the following package pages and confirm they appear legitimate:
       - https://pypi.org/project/aiomqtt/ — Active project, latest release 2.5.1 (2026-03-05)
       - https://pypi.org/project/pyserial-asyncio/ — Established, ~10 years old
       - https://pypi.org/project/aiohttp/ — Major project, 10M+ weekly downloads
       - https://pypi.org/project/pydantic-settings/ — Official pydantic extension
       - https://pypi.org/project/pytest-asyncio/ — Standard pytest plugin for asyncio

    3. **Verify dev install of async packages works (might fail without dev env):**
       ```bash
       cd edge/agent && pip install -r requirements.txt 2>&1 | tail -5
       ```
       Expected output: Successfully installed packages

    4. **Run unit tests:**
       ```bash
       cd edge/agent && python -m pytest tests/ -x -v --asyncio-mode=auto 2>&1 | tail -20
       ```
       Expected: Test suite runs, some tests may pass/fail depending on environment (the test framework should load and execute).

    5. **Verify no archived packages:**
       ```bash
       grep -r "asyncio-mqtt\|asyncio_mqtt" edge/agent/ --include="*.py" --include="*.txt" --include="*.cfg" || echo "No archived package references found"
       ```
       Expected: "No archived package references found" (we use `aiomqtt`, not the archived `asyncio-mqtt`)

    6. **Verify no sync I/O patterns in async code:**
       ```bash
       grep -n "time\.sleep\|import httpx\|from httpx\|schedule\.\|import schedule" edge/agent/tasks/*.py edge/agent/main.py || echo "No sync I/O patterns found"
       ```
       Expected: "No sync I/O patterns found" (async code uses `await asyncio.sleep()`, `aiohttp`, etc.)
  </how-to-verify>

  <resume-signal>
    Type "verified" to confirm all checks passed, or describe which checks failed and whether to proceed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → system under test | Unit tests use mocked I/O; integration tests use isolated Docker container |
| Integration test Mosquitto → host network | Test Mosquitto binds to localhost test ports only |
| pip install → PyPI registry | Network download of third-party packages during test setup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-INF-13 | Tampering | Malicious PyPI package in test dependencies | mitigate | Package legitimacy checkpoint (Task 3) requires human verification of all new packages on PyPI; only established packages used |
| T-INF-14 | Info Disclosure | Test Mosquitto exposed on host ports | mitigate | Test Docker Compose binds to localhost only; integration tests must verify `127.0.0.1` binding |
| T-INF-15 | DoS | Unit test blocking on async I/O | mitigate | `pytest-asyncio` with `asyncio_mode = auto` ensures proper event loop management |
| T-INF-SC | Tampering | pip package installs in test/dev | mitigate | All test packages [ASSUMED] per RESEARCH.md legitimacy audit; human verification checkpoint required in Task 3 |
</threat_model>

<verification>
1. All test files pass `ast.parse()` syntax check
2. Unit tests run with `pytest --asyncio-mode=auto` without import errors
3. Integration tests use `@pytest.mark.integration` marker and skip gracefully when no broker
4. Makefile targets are functional
5. Docker image builds successfully from Dockerfile
6. No references to archived `asyncio-mqtt` package — only `aiomqtt` is used
</verification>

<success_criteria>
1. Unit tests cover config loading, model serialization, MQTT reconnect logic, HTTP error handling, serial timeout/shutdown
2. Integration tests prove TLS connection and publish/subscribe roundtrip against real Mosquitto
3. Docker image builds successfully
4. Package legitimacy verified by human (all packages tagged `[ASSUMED]` confirmed on PyPI)
5. No sync I/O patterns (`time.sleep`, `httpx`, `schedule`) exist in async task code
6. All references use `aiomqtt` not archived `asyncio-mqtt`
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md` when done.
</output>
