---
phase: 06-premium-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/app/globals.css
  - apps/dashboard/tailwind.config.ts
  - apps/dashboard/app/layout.tsx
  - apps/dashboard/lib/theme-provider.tsx
  - apps/dashboard/components/theme-toggle.tsx
  - apps/dashboard/components/sidebar.tsx
autonomous: true
requirements:
  - UIX-01
  - UIX-05

must_haves:
  truths:
    - "CSS vars are namespace-migrated to --shadcn-* prefix with zero visual regression in dark mode"
    - "Radix Themes CSS loads before Tailwind base to prevent button reset conflicts"
    - "Light mode overrides exist under [data-theme='light'] selector with complementary HSL values"
    - "User can toggle between dark, light, and system-preferred modes via a sidebar toggle"
    - "Theme preference persists across page refreshes (localStorage via next-themes)"
    - "Radix Themes components auto-switch appearance based on html class"
  artifacts:
    - path: "apps/dashboard/app/globals.css"
      provides: "Namespace-migrated CSS vars with --shadcn- prefix, Radix @import, light mode overrides, glass-premium/accent utilities"
      min_lines: 170
    - path: "apps/dashboard/tailwind.config.ts"
      provides: "--shadcn- CSS var mappings in colors extend, new animation keyframes"
      min_lines: 95
    - path: "apps/dashboard/lib/theme-provider.tsx"
      provides: "Client component wrapping next-themes ThemeProvider + Radix Themes Theme"
      contains: "next-themes"
    - path: "apps/dashboard/app/layout.tsx"
      provides: "suppressHydrationWarning on html, ThemeProvider wrapping children"
      contains: "suppressHydrationWarning"
    - path: "apps/dashboard/components/theme-toggle.tsx"
      provides: "3-state dropdown (dark/light/system) with Lucide icons and SR labels"
      contains: "useTheme"
  key_links:
    - from: "globals.css"
      to: "tailwind.config.ts"
      via: "Tailwind colors reference hsl(var(--shadcn-*))"
    - from: "globals.css"
      to: "@radix-ui/themes/styles.css"
      via: "@import at top of globals.css before Tailwind directives"
    - from: "layout.tsx"
      to: "theme-provider.tsx"
      via: "ThemeProvider import and usage in RootLayout"
    - from: "sidebar.tsx"
      to: "theme-toggle.tsx"
      via: "ThemeToggle component added to sidebar bottom section"

---

<objective>
Rewrite the CSS variable system to namespace-migrate all existing shadcn/ui vars with `--shadcn-` prefix, integrate Radix Themes CSS import, add light mode overrides, install `@radix-ui/themes` and `next-themes`, create the ThemeProvider wrapper, build the ThemeToggle component, update the root layout for SSR-safe theme switching, and add the theme toggle to the sidebar.

Purpose: Foundational layer that everything else in Phase 6 builds on — CSS var namespacing prevents Radix Themes collision, light mode enables the toggle, and the provider/toggle infrastructure enables user-controlled dark/light switching.

Output: Rewritten globals.css, updated tailwind.config.ts, ThemeProvider client component, ThemeToggle dropdown component, updated root layout, updated sidebar.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-premium-experience/06-CONTEXT.md
@.planning/phases/06-premium-experience/06-RESEARCH.md
@.planning/phases/06-premium-experience/06-UI-SPEC.md
@apps/dashboard/app/globals.css
@apps/dashboard/tailwind.config.ts
@apps/dashboard/app/layout.tsx
@apps/dashboard/components/sidebar.tsx
@apps/dashboard/components/ui/dropdown-menu.tsx
@apps/dashboard/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Namespace-migrate CSS vars in globals.css + add Radix import + light mode + premium utilities</name>

  <read_first>
    @apps/dashboard/app/globals.css — Current state with un-prefixed CSS vars, dark theme only, existing .glass utility
    @.planning/phases/06-premium-experience/06-RESEARCH.md — CSS variable namespace migration pattern lines 237-276, Pitfall 1 (CSS import order) lines 387-391, Pitfall 2 (Tailwind base conflict) lines 393-396
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — Glassmorphism pattern lines 104-125, Color tables lines 185-235
  </read_first>

  <files>apps/dashboard/app/globals.css</files>

  <action>
    Rewrite globals.css with these sequential changes:

    1. **Radix Themes CSS import**: Add `@import "@radix-ui/themes/styles.css";` BEFORE the `@tailwind` directives. This prevents the Next.js CSS import order bug (Pitfall 1).

    2. **Namespace migration**: Prefix all 17 existing CSS custom properties in `:root` with `--shadcn-`:
       - `--background` → `--shadcn-background`
       - `--foreground` → `--shadcn-foreground`
       - `--card` → `--shadcn-card`
       - `--card-foreground` → `--shadcn-card-foreground`
       - `--popover` → `--shadcn-popover`
       - `--popover-foreground` → `--shadcn-popover-foreground`
       - `--primary` → `--shadcn-primary`
       - `--primary-foreground` → `--shadcn-primary-foreground`
       - `--secondary` → `--shadcn-secondary`
       - `--secondary-foreground` → `--shadcn-secondary-foreground`
       - `--muted` → `--shadcn-muted`
       - `--muted-foreground` → `--shadcn-muted-foreground`
       - `--accent` → `--shadcn-accent`
       - `--accent-foreground` → `--shadcn-accent-foreground`
       - `--destructive` → `--shadcn-destructive`
       - `--destructive-foreground` → `--shadcn-destructive-foreground`
       - `--warning` → `--shadcn-warning`
       - `--warning-foreground` → `--shadcn-warning-foreground`
       - `--success` → `--shadcn-success`
       - `--success-foreground` → `--shadcn-success-foreground`
       - `--border` → `--shadcn-border`
       - `--input` → `--shadcn-input`
       - `--ring` → `--shadcn-ring`
       - `--radius` → `--shadcn-radius`
       All 17 HSL values stay the same (dark theme defaults).

       Update `::selection`, `.glass`, `.bg-grid`, `.bg-scan`, `.glow-cyan`, `.glow-red`, `.glow-amber` to reference `--shadcn-*` vars instead of `--primary`, `--card`, `--border`, etc.

    3. **Light mode overrides**: Add a `[data-theme="light"]` block inside `@layer base` with ALL 17 vars overridden to light HSL values per the color contract in UI-SPEC.md lines 219-234:
       - `--shadcn-background: 0 0% 100%` (white)
       - `--shadcn-foreground: 228 20% 4%` (nearly black)
       - `--shadcn-card: 0 0% 100%` (white)
       - `--shadcn-card-foreground: 228 20% 4%`
       - `--shadcn-popover: 0 0% 100%`
       - `--shadcn-popover-foreground: 228 20% 4%`
       - `--shadcn-primary: 190 90% 40%` (deeper cyan per UI-SPEC light mode)
       - `--shadcn-primary-foreground: 0 0% 100%`
       - `--shadcn-secondary: 220 20% 97%`
       - `--shadcn-secondary-foreground: 228 20% 4%`
       - `--shadcn-muted: 220 20% 97%`
       - `--shadcn-muted-foreground: 228 10% 40%`
       - `--shadcn-accent: 190 90% 40%`
       - `--shadcn-accent-foreground: 0 0% 100%`
       - `--shadcn-destructive: 0 84% 55%`
       - `--shadcn-destructive-foreground: 0 0% 100%`
       - `--shadcn-warning: 35 92% 50%`
       - `--shadcn-warning-foreground: 228 20% 4%`
       - `--shadcn-success: 160 84% 39%`
       - `--shadcn-success-foreground: 228 20% 4%`
       - `--shadcn-border: 220 20% 88%`
       - `--shadcn-input: 220 20% 88%`
       - `--shadcn-ring: 190 90% 40%`
       - `--shadcn-radius: 0.625rem`

    4. **Enhanced glass utilities** (per UI-SPEC lines 107-125):
       - Keep existing `.glass` for backward compat.
       - Add `.glass-premium`: `background: hsl(var(--shadcn-card) / 0.5)`, `backdrop-filter: blur(16px) saturate(1.2)`, `border: 1px solid hsl(var(--shadcn-border) / 0.4)`, `box-shadow: 0 8px 32px hsl(var(--shadcn-primary) / 0.04)`
       - Add `.glass-accent`: `background: hsl(var(--shadcn-primary) / 0.04)`, `backdrop-filter: blur(16px) saturate(1.2)`, `border: 1px solid hsl(var(--shadcn-primary) / 0.12)`, `box-shadow: 0 8px 32px hsl(var(--shadcn-primary) / 0.06)`

    5. Keep all existing utility classes unchanged: `.tabular-nums`, `.stat-value`, `.section-title`, scrollbar styles, keyframes, animations.
  </action>

  <verify>
    <automated>grep -c -- '--shadcn-background' apps/dashboard/app/globals.css | xargs test 1 -eq && grep -c -- '@import "@radix-ui/themes/styles.css"' apps/dashboard/app/globals.css | xargs test 1 -eq && grep -c -- '\--shadcn-' apps/dashboard/app/globals.css | xargs test $(grep -oP -- '--shadcn-\w+' apps/dashboard/app/globals.css | sort -u | wc -l) -ge 17 && grep -c -- '\[data-theme="light"\]' apps/dashboard/app/globals.css | xargs test 1 -eq && grep -c -- 'glass-premium' apps/dashboard/app/globals.css | xargs test 1 -eq</automated>
  </verify>

  <acceptance_criteria>
    - globals.css contains `@import "@radix-ui/themes/styles.css"` before `@tailwind` directives
    - globals.css contains `--shadcn-background`, `--shadcn-primary`, `--shadcn-card`, `--shadcn-border`, `--shadcn-ring` — all 17+ vars prefixed
    - globals.css contains `[data-theme="light"]` selector block
    - Light mode block has overridden values for all 17 vars (white background, darker foreground, deeper cyan accent)
    - globals.css contains `.glass-premium` and `.glass-accent` utility classes
    - `.glass`, `.bg-grid`, `.bg-scan`, `.glow-*` utilities reference `--shadcn-*` not the old unprefixed names
    - Backward compat: `@tailwind base` is preserved or properly layered
    - No runtime regression: existing components continue rendering in dark theme
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update tailwind.config.ts for --shadcn- references + create ThemeProvider component</name>

  <read_first>
    @apps/dashboard/tailwind.config.ts — Current config with hsl(var(--background)) etc.
    @apps/dashboard/app/globals.css — Already rewritten with --shadcn- vars (from Task 1)
    @.planning/phases/06-premium-experience/06-RESEARCH.md — Pattern 2 (Theme Provider) lines 279-302, tailwind config example lines 266-276
  </read_first>

  <files>
    apps/dashboard/tailwind.config.ts
    apps/dashboard/lib/theme-provider.tsx
  </files>

  <action>
    Update two files:

    **tailwind.config.ts**: Replace all occurrences of `hsl(var(--XXX))` with `hsl(var(--shadcn-XXX))`:
    - `--background` → `--shadcn-background`
    - `--foreground` → `--shadcn-foreground`
    - `--primary` → `--shadcn-primary`
    - `--primary-foreground` → `--shadcn-primary-foreground`
    - `--card` → `--shadcn-card`
    - `--card-foreground` → `--shadcn-card-foreground`
    - `--popover` → `--shadcn-popover`
    - `--popover-foreground` → `--shadcn-popover-foreground`
    - `--secondary` → `--shadcn-secondary`
    - `--secondary-foreground` → `--shadcn-secondary-foreground`
    - `--muted` → `--shadcn-muted`
    - `--muted-foreground` → `--shadcn-muted-foreground`
    - `--accent` → `--shadcn-accent`
    - `--accent-foreground` → `--shadcn-accent-foreground`
    - `--destructive` → `--shadcn-destructive`
    - `--destructive-foreground` → `--shadcn-destructive-foreground`
    - `--warning` → `--shadcn-warning`
    - `--warning-foreground` → `--shadcn-warning-foreground`
    - `--success` → `--shadcn-success`
    - `--success-foreground` → `--shadcn-success-foreground`
    - `--border` → `--shadcn-border`
    - `--input` → `--shadcn-input`
    - `--ring` → `--shadcn-ring`
    - `--radius` → `--shadcn-radius`
    
    No other config changes needed (animations, fonts, keyframes stay the same).

    **theme-provider.tsx** (new file at `apps/dashboard/lib/theme-provider.tsx`):
    Create a `"use client"` component following Pattern 2 from RESEARCH.md lines 283-298:
    - Import `Theme` from `"@radix-ui/themes"`
    - Import `ThemeProvider as NextThemesProvider` from `"next-themes"`
    - Import `"@radix-ui/themes/styles.css"` (for client-side CSS loading, complements globals.css import)
    - Export `ThemeProvider` component that wraps children in:
      ```tsx
      <NextThemesProvider attribute="class" defaultTheme="system" enableSystem>
        <Theme accentColor="cyan" grayColor="slate" radius="medium">
          {children}
        </Theme>
      </NextThemesProvider>
      ```
    - Props: `{ children: React.ReactNode }` only — no extra config needed.
  </action>

  <verify>
    <automated>grep -c -- 'shadcn-background' apps/dashboard/tailwind.config.ts | xargs test 1 -eq && grep -c -- 'shadcn-primary' apps/dashboard/tailwind.config.ts | xargs test 1 -eq && grep -c -- 'next-themes' apps/dashboard/lib/theme-provider.tsx | xargs test 2 -ge && grep -c -- 'accentColor="cyan"' apps/dashboard/lib/theme-provider.tsx | xargs test 1 -eq</automated>
  </verify>

  <acceptance_criteria>
    - tailwind.config.ts: all 17 `hsl(var(--shadcn-*))` references present, zero old `hsl(var(--background))` references remain
    - tailwind.config.ts: `darkMode: "class"` still set (next-themes manages this)
    - theme-provider.tsx: imports `Theme` from `@radix-ui/themes` and `ThemeProvider` from `next-themes`
    - theme-provider.tsx: uses `accentColor="cyan"`, `grayColor="slate"`, `radius="medium"` on Radix Theme
    - theme-provider.tsx: has `"use client"` directive
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update root layout, create ThemeToggle, add toggle to sidebar</name>

  <read_first>
    @apps/dashboard/app/layout.tsx — Current root layout with I18nProvider
    @apps/dashboard/lib/theme-provider.tsx — Created in Task 2
    @apps/dashboard/components/sidebar.tsx — Current sidebar with bottom collapse button
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — Theme provider location lines 437-451, toggle visual contract lines 448-452
    @.planning/phases/06-premium-experience/06-RESEARCH.md — Theme Toggle component code lines 493-555
  </read_first>

  <files>
    apps/dashboard/app/layout.tsx
    apps/dashboard/components/theme-toggle.tsx
    apps/dashboard/components/sidebar.tsx
  </files>

  <action>
    Modify/create three files:

    **layout.tsx**:
    - Keep `suppressHydrationWarning` on `<html>` (already present)
    - Remove `className="dark"` from `<html>` (next-themes manages this)
    - Import `ThemeProvider` from `@/lib/theme-provider`
    - Wrap the `<I18nProvider>` children with `<ThemeProvider>` so order is: `<body> → <ThemeProvider> → <I18nProvider> → {children}`
    - Keep all existing metadata, manifest, service worker script unchanged
    - Keep the `I18nProvider` import

    **theme-toggle.tsx** (new at `apps/dashboard/components/theme-toggle.tsx`):
    Create a `"use client"` component per UI-SPEC toggle visual contract + RESEARCH.md code example lines 493-555:
    - Import `useTheme` from `"next-themes"`
    - Import `Sun`, `Moon`, `Monitor` from `"lucide-react"`
    - Import `Button` from `@/components/ui/button`
    - Import `DropdownMenu`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuTrigger` from `@/components/ui/dropdown-menu`
    - Mounted state guard: `const [mounted, setMounted] = useState(false)` + `useEffect(() => setMounted(true), [])` — when not mounted, render disabled Button with Sun icon
    - Dropdown with 3 items: "Clair" (Sun, calls `setTheme("light")`), "Sombre" (Moon, calls `setTheme("dark")`), "Automatique" (Monitor, calls `setTheme("system")`)
    - Current theme shown as icon on trigger button
    - SR label: `"Changer le thème"` on trigger, each item has inline label
    - The `DropdownMenuContent` has `align="end"`
    - Export as named `ThemeToggle` function

    **sidebar.tsx**:
    - Import `ThemeToggle` from `@/components/theme-toggle`
    - In the bottom section (lines 158-169, currently only has collapse button), add `<ThemeToggle />` ABOVE the collapse button
    - Style: put it in a `px-2 py-1` container so it fills the width
  </action>

  <verify>
    <automated>grep -c -- 'className="dark"' apps/dashboard/app/layout.tsx | xargs test 0 -eq && grep -c -- 'ThemeProvider' apps/dashboard/app/layout.tsx | xargs test 1 -eq && grep -c -- 'useTheme' apps/dashboard/components/theme-toggle.tsx | xargs test 1 -eq && grep -c -- 'ThemeToggle' apps/dashboard/components/sidebar.tsx | xargs test 1 -eq</automated>
  </verify>

  <acceptance_criteria>
    - layout.tsx: `<html>` tag has `suppressHydrationWarning` but NO `className="dark"`
    - layout.tsx: `ThemeProvider` wraps content between `<body>` and `<I18nProvider>`
    - theme-toggle.tsx: mounted-state guard prevents hydration mismatch
    - theme-toggle.tsx: 3 dropdown items with `setTheme("light")`, `setTheme("dark")`, `setTheme("system")`
    - theme-toggle.tsx: uses existing shadcn/ui DropdownMenu and Button components
    - sidebar.tsx: `ThemeToggle` imported and rendered at bottom section
    - `pnpm --filter @repo/dashboard run check-types` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage | next-themes stores theme preference via `class` attribute on `<html>` — no sensitive data stored |
| CSS injection | ThemeProvider controls class toggling — no dynamic CSS injection from user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | next-themes inline `<script>` | accept | Inline script sets class before render — no user-controlled input in this context |
| T-06-02 | Tampering | CSS var namespace | mitigate | `--shadcn-*` prefix prevents collision with Radix Themes vars — verified by grep in acceptance criteria |
| T-06-SC | Tampering | npm installs (@radix-ui/themes, next-themes) | mitigate | Research.md Package Legitimacy Audit confirms [OK] for all three packages — approved disposition |
</threat_model>

<verification>
1. `pnpm --filter @repo/dashboard run check-types` passes
2. `pnpm --filter @repo/dashboard run lint` passes
3. Manual: Dashboard loads without hydration errors in dark mode
4. Manual: Theme toggle cycles dark → light → system correctly
5. Manual: Light mode renders all components with correct backgrounds
</verification>

<success_criteria>
- globals.css rewritten with --shadcn-* namespace, Radix CSS import, light mode overrides, glass-premium/accent utilities
- tailwind.config.ts references --shadcn-* vars
- ThemeProvider wraps root layout with next-themes + Radix Theme
- ThemeToggle in sidebar with 3-state dropdown
- TypeScript compilation passes with zero errors for all modified files
- All existing dashboard pages render without visual regression in dark mode
</success_criteria>

<output>
Create `.planning/phases/06-premium-experience/06-01-SUMMARY.md` when done
</output>
