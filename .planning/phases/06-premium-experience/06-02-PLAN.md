---
phase: 06-premium-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/design/package.json
  - packages/design/tsconfig.json
  - packages/design/src/index.ts
  - packages/design/src/colors.ts
  - packages/design/src/typography.ts
  - packages/design/src/spacing.ts
  - packages/design/src/shadows.ts
  - apps/dashboard/package.json
  - apps/dashboard/components/page-transition.tsx
  - apps/dashboard/components/dashboard-layout.tsx
  - pnpm-workspace.yaml
autonomous: true
requirements:
  - UIX-01
  - UIX-03

must_haves:
  truths:
    - "@repo/design package exists with colors, typography, spacing, shadows as JS constants"
    - "Mobile app can import colors and typography from @repo/design"
    - "motion is installed as a dependency of @repo/dashboard"
    - "Page transitions animate with fade-up enter + fade-down exit (AnimatePresence)"
    - "Dashboard-layout wraps content in PageTransition for route-level animations"
    - "Staggered entrance container/item helper variants are available for component use"
  artifacts:
    - path: "packages/design/src/colors.ts"
      provides: "Dark + light color tokens as JS constants matching UI-SPEC HSL values"
      exports: ["colors"]
    - path: "packages/design/src/typography.ts"
      provides: "Font size/weight/line-height tokens matching UI-SPEC typography contract"
      exports: ["typography"]
    - path: "packages/design/package.json"
      provides: "@repo/design workspace package with build config"
      contains: "@repo/design"
    - path: "apps/dashboard/components/page-transition.tsx"
      provides: "AnimatePresence wrapper with motion.div (opacity + y transitions)"
      contains: "AnimatePresence"
    - path: "apps/dashboard/components/dashboard-layout.tsx"
      provides: "PageTransition wrapper around main content area"
      contains: "PageTransition"
  key_links:
    - from: "packages/design/src/colors.ts"
      to: ".planning/phases/06-premium-experience/06-UI-SPEC.md"
      via: "HSL values sourced from UI-SPEC color contract lines 185-234"
    - from: "mobile/app/(tabs)/_layout.tsx"
      to: "@repo/design"
      via: "Import colors, typography from @repo/design (future task in Plan 06-04)"
    - from: "page-transition.tsx"
      to: "motion"
      via: "import { motion, AnimatePresence } from 'motion/react'"

---

<objective>
Create the `@repo/design` shared workspace package exporting JS constants for mobile consumption, install `motion` as a dashboard dependency, and build the `PageTransition` component with `AnimatePresence` wired into the dashboard layout. Also export staggered animation helpers.

Purpose: Two parallel foundations — the shared token package ensures cross-platform visual parity (Dashboard uses CSS vars, Mobile imports JS constants), and the animation layer enables fluid page transitions and micro-interactions used by all page redesigns.

Output: `packages/design/` workspace package with src/index.ts barrel export, motion dependency installed, PageTransition component created, dashboard-layout.tsx updated.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-premium-experience/06-CONTEXT.md
@.planning/phases/06-premium-experience/06-RESEARCH.md
@.planning/phases/06-premium-experience/06-UI-SPEC.md
@packages/shared/package.json
@packages/shared/tsconfig.json
@apps/dashboard/package.json
@apps/dashboard/components/dashboard-layout.tsx
@apps/dashboard/lib/utils.ts
@apps/mobile/lib/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create @repo/design workspace package with design token JS constants</name>

  <read_first>
    @packages/shared/package.json — Reference for workspace package structure (name, build scripts, tsconfig pattern)
    @packages/shared/tsconfig.json — Reference tsconfig (extends @repo/typescript-config/base.json)
    @apps/mobile/lib/theme.ts — Current mobile theme values to migrate into shared tokens
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — Colors lines 185-234, Typography lines 155-180, Spacing lines 131-151, Shadows (glass-premium box-shadow)
    @.planning/phases/06-premium-experience/06-RESEARCH.md — Package code examples lines 558-632
    @pnpm-workspace.yaml — To register the new package
  </read_first>

  <files>
    packages/design/package.json
    packages/design/tsconfig.json
    packages/design/src/index.ts
    packages/design/src/colors.ts
    packages/design/src/typography.ts
    packages/design/src/spacing.ts
    packages/design/src/shadows.ts
    pnpm-workspace.yaml
  </files>

  <action>
    Create the `packages/design/` directory structure with 5 source files, package.json, and tsconfig.json. Then register in workspace.

    **packages/design/package.json**:
    - `name: "@repo/design"`, `private: true`, `version: "0.0.0"`
    - `main: "./dist/index.js"`, `types: "./dist/index.d.ts"`
    - `exports` map with `"."` → `{ "types": "./dist/index.d.ts", "default": "./dist/index.js" }`
    - `scripts: { "build": "tsc", "dev": "tsc --watch" }`
    - `devDependencies: { "@repo/typescript-config": "workspace:*", "typescript": "5.9.2" }`

    **packages/design/tsconfig.json**:
    - Extend `@repo/typescript-config/base.json`
    - `outDir: "./dist"`, `rootDir: "./src"`
    - `declaration: true`, `declarationMap: true`
    - `module: "commonjs"`, `target: "ES2021"`, `moduleResolution: "node"`
    - Include `["src"]`

    **packages/design/src/colors.ts**:
    Export `colors` const with `dark`, `light`, and `shared` objects per UI-SPEC color tables:
    - `dark.bg`: `"#070912"` (hsl(228 20% 4%)), `dark.surface`: `"#0c1020"`, `dark.elevated`: `"#1a2332"`, `dark.border`: `"#1e293b"`, `dark.borderLight`: `"#334155"`, `dark.text`: `"#f1f5f9"`, `dark.textSecondary`: `"#94a3b8"`, `dark.textMuted`: `"#64748b"`, `dark.glassBg`: `"rgba(12,16,32,0.7)"`
    - `light.bg`: `"#ffffff"`, `light.surface`: `"#f5f7fa"`, `light.elevated`: `"#ffffff"`, `light.border`: `"#e2e8f0"`, `light.borderLight`: `"#cbd5e1"`, `light.text`: `"#070912"`, `light.textSecondary`: `"#5c6573"`, `light.textMuted`: `"#94a3b8"`, `light.glassBg`: `"rgba(245,247,250,0.7)"`
    - `shared.primary`: `"#06b6d4"`, `shared.primaryDark`: `"#0891b2"`, `shared.success`: `"#10b981"`, `shared.warning`: `"#f59e0b"`, `shared.destructive`: `"#ef4444"`, `shared.info`: `"#06b6d4"`
    - All values as `as const`
    - Export `type ColorScheme = keyof typeof colors`
    - Export `type ColorPalette = typeof colors`

    **packages/design/src/typography.ts**:
    Export `typography` const per UI-SPEC typography contract lines 156-166:
    - `display`: `{ fontSize: 28, fontWeight: 600, lineHeight: 1.2 }`
    - `heading`: `{ fontSize: 20, fontWeight: 600, lineHeight: 1.2 }`
    - `body`: `{ fontSize: 14, fontWeight: 400, lineHeight: 1.5 }`
    - `label`: `{ fontSize: 12, fontWeight: 600, lineHeight: 1.2 }`
    - `mono`: `{ fontSize: 24, fontWeight: 600, fontVariant: ["tabular-nums"] }`
    - `monoSmall`: `{ fontSize: 14, fontWeight: 600, fontVariant: ["tabular-nums"] }`
    - All as `as const`
    - Export `type TypographyScale = typeof typography`

    **packages/design/src/spacing.ts**:
    Export `spacing` const matching UI-SPEC spacing scale lines 131-143:
    - `xs: 4`, `sm: 8`, `md: 12`, `base: 16`, `lg: 24`, `xl: 32`, `xxl: 48`, `xxxl: 64`
    - All as `as const`
    - Export `type SpacingScale = typeof spacing`

    **packages/design/src/shadows.ts**:
    Export `shadows` const:
    - `sm`: `{ shadowColor: "#000", shadowOffset: { width: 0, height: 1 }, shadowOpacity: 0.2, shadowRadius: 3, elevation: 2 }`
    - `md`: `{ shadowColor: "#000", shadowOffset: { width: 0, height: 2 }, shadowOpacity: 0.3, shadowRadius: 6, elevation: 4 }`
    - `glow`: `{ shadowColor: "#06b6d4", shadowOffset: { width: 0, height: 0 }, shadowOpacity: 0.15, shadowRadius: 20, elevation: 8 }`
    - All as `as const`

    **packages/design/src/index.ts**:
    Barrel export: `export * from "./colors"`, `export * from "./typography"`, `export * from "./spacing"`, `export * from "./shadows"`

    **pnpm-workspace.yaml** — READ first to understand format. Add `packages/design` to the workspace packages list if it uses glob patterns (should auto-include via `packages/*`).
  </action>

  <verify>
    <automated>test -f packages/design/package.json && test -f packages/design/src/index.ts && test -f packages/design/src/colors.ts && test -f packages/design/src/typography.ts && test -f packages/design/src/spacing.ts && test -f packages/design/src/shadows.ts && grep -c -- '@repo/design' packages/design/package.json | xargs test 1 -eq && grep -c -- 'dark:' packages/design/src/colors.ts | xargs test 1 -eq && grep -c -- 'display:' packages/design/src/typography.ts | xargs test 1 -eq</automated>
  </verify>

  <acceptance_criteria>
    - packages/design/package.json exists with `name: "@repo/design"` and `main: "./dist/index.js"`
    - packages/design/tsconfig.json extends base config with outDir dist, declaration true
    - packages/design/src/colors.ts exports `colors` const with dark, light, shared objects — dark.bg is "#070912", light.bg is "#ffffff", shared.primary is "#06b6d4"
    - packages/design/src/typography.ts exports `typography` const with display (28px/600/1.2), heading (20px/600/1.2), body (14px/400/1.5)
    - packages/design/src/spacing.ts exports `spacing` const with xs=4, base=16, xl=32 values
    - packages/design/src/shadows.ts exports `shadows` const with sm, md, glow entries
    - packages/design/src/index.ts barrel-exports all four modules
    - pnpm-workspace.yaml includes the packages/design directory
    - `pnpm --filter @repo/design run build` compiles without errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install motion dependency + create PageTransition component with AnimatePresence</name>

  <read_first>
    @apps/dashboard/package.json — Current dependencies to add motion to
    @.planning/phases/06-premium-experience/06-RESEARCH.md — motion installation line 107, Pattern 3 lines 304-331 (PageTransition code example)
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — Animation contract lines 398-403, reduced motion lines 426-429
  </read_first>

  <files>
    apps/dashboard/package.json
    apps/dashboard/components/page-transition.tsx
  </files>

  <action>
    Two changes:

    **apps/dashboard/package.json**: Add `"motion": "^12.42.2"` to the `dependencies` object. Insert alphabetically after `"lucide-react"`.

    **apps/dashboard/components/page-transition.tsx** (new file):
    Create a `"use client"` component per RESEARCH.md Pattern 3:
    ```tsx
    import { motion, AnimatePresence } from "motion/react";
    import { usePathname } from "next/navigation";

    // Container variants object for staggered children animations
    // Used by components that import this file for staggered list reveals
    export const containerVariants = {
      hidden: {},
      visible: {
        transition: {
          staggerChildren: 0.08,
          delayChildren: 0.1,
        },
      },
    };

    // Individual item variant for staggered entrances
    export const itemVariants = {
      hidden: { opacity: 0, y: 20 },
      visible: {
        opacity: 1,
        y: 0,
        transition: { duration: 0.4, ease: "easeOut" },
      },
    };

    // Page transition wrapper with fade-up enter + fade-down exit
    export function PageTransition({ children }: { children: React.ReactNode }) {
      const pathname = usePathname();

      return (
        <AnimatePresence mode="wait">
          <motion.div
            key={pathname}
            initial={{ opacity: 0, y: 12 }}
            animate={{ opacity: 1, y: 0 }}
            exit={{ opacity: 0, y: -8 }}
            transition={{ duration: 0.3, ease: [0.16, 1, 0.3, 1] }}
          >
            {children}
          </motion.div>
        </AnimatePresence>
      );
    }
    ```
    
    Export both `PageTransition` as named export, and `containerVariants`/`itemVariants` as named exports for reuse by component plans.

    After writing, run: `pnpm install --filter @repo/dashboard` to install motion.
  </action>

  <verify>
    <automated>test -f apps/dashboard/components/page-transition.tsx && grep -c -- 'AnimatePresence' apps/dashboard/components/page-transition.tsx | xargs test 1 -eq && grep -c -- '"motion/react"' apps/dashboard/components/page-transition.tsx | xargs test 1 -eq && grep -c -- 'containerVariants' apps/dashboard/components/page-transition.tsx | xargs test 1 -eq && grep -c -- 'itemVariants' apps/dashboard/components/page-transition.tsx | xargs test 1 -eq && grep -c -- 'motion' apps/dashboard/package.json | xargs test 1 -eq</automated>
  </verify>

  <acceptance_criteria>
    - apps/dashboard/package.json has `"motion": "^12.42.2"` in dependencies
    - page-transition.tsx imports `motion` and `AnimatePresence` from `"motion/react"`
    - page-transition.tsx exports `PageTransition` function with AnimatePresence → motion.div keyed on `usePathname()`
    - page-transition.tsx exports `containerVariants` and `itemVariants` constants
    - `pnpm install` runs without errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire PageTransition into dashboard-layout.tsx</name>

  <read_first>
    @apps/dashboard/components/dashboard-layout.tsx — Current layout with SidebarProvider → DashboardShell
    @apps/dashboard/components/page-transition.tsx — Created in Task 2
  </read_first>

  <files>apps/dashboard/components/dashboard-layout.tsx</files>

  <action>
    In `dashboard-layout.tsx`, add the PageTransition wrapper around the `<main>` content area:

    1. Import `PageTransition` from `@/components/page-transition`
    2. In `DashboardShell` component, wrap the `{children}` inside `<main>` with `<PageTransition>`:

    Current:
    ```tsx
    <main className="flex-1 p-6 pt-4">{children}</main>
    ```

    After:
    ```tsx
    <main className="flex-1 p-6 pt-4">
      <PageTransition>{children}</PageTransition>
    </main>
    ```
    
    No other changes to the layout. The sidebar and header remain static during page transitions.
  </action>

  <verify>
    <automated>grep -c -- 'PageTransition' apps/dashboard/components/dashboard-layout.tsx | xargs test 1 -eq && pnpm --filter @repo/dashboard run check-types</automated>
  </verify>

  <acceptance_criteria>
    - dashboard-layout.tsx imports `PageTransition` from `@/components/page-transition`
    - `<main>` children are wrapped in `<PageTransition>`
    - Sidebar and header remain outside PageTransition (only content area animates)
    - TypeScript compilation passes
    - Route navigation triggers fade-up animation on new page content
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| motion animation runtime | Client-side only — no server/data interaction |
| @repo/design package | Pure JS constants, no runtime evaluation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | motion | accept | motion is animation-only, no data access; packaged by same team as framer-motion |
| T-06-SC | Tampering | npm installs (motion) | mitigate | RESEARCH.md Package Legitimacy Audit confirms [OK] — approved |
</threat_model>

<verification>
1. `pnpm --filter @repo/dashboard run check-types` passes
2. `pnpm --filter @repo/dashboard run lint` passes
3. Manual: Navigating between pages shows fade-up animation (enter) and fade-down (exit)
4. Manual: page-transition.tsx exported containerVariants/itemVariants available for import by page components
</verification>

<success_criteria>
- @repo/design package compiles and exports colors, typography, spacing, shadows
- motion ^12.42.2 installed in dashboard dependencies
- PageTransition component created with AnimatePresence and motion.div
- dashboard-layout.tsx wraps main content in PageTransition
- containerVariants and itemVariants exported for component-level reuse
- TypeScript compilation passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/06-premium-experience/06-02-SUMMARY.md` when done
</output>
