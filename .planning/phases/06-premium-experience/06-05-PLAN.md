---
phase: 06-premium-experience
plan: 05
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - apps/dashboard/app/(dashboard)/page.tsx
  - apps/dashboard/app/(dashboard)/cameras/page.tsx
  - apps/dashboard/components/camera-card-premium.tsx
  - apps/dashboard/components/camera-grid.tsx
autonomous: true
requirements:
  - UIX-07

must_haves:
  truths:
    - "Overview page has premium layout: 4 MetricHero cards in grid with sparklines, DonutChart showing severity distribution, ActivityTimeline for recent events, CameraGrid preview, QuickActionBar"
    - "Overview MetricHero cards animate in with staggered entrance (80ms stagger)"
    - "Overview shows shimmer skeletons during loading"
    - "Overview shows empty state for camera grid section when no cameras exist"
    - "Overview shows error state with retry button on API failure"
    - "Cameras page has premium responsive grid with status filter pills and search bar"
    - "Cameras page has grid/map view toggle with animated switch"
    - "CameraCardPremium shows status badge (with pulse animation for ONLINE), hover overlay with 'Voir le flux' button, camera name, site name"
    - "Cameras page shows skeleton cards during loading, empty state when no cameras, and inline 'no results' when filter yields 0"
  artifacts:
    - path: "apps/dashboard/app/(dashboard)/page.tsx"
      provides: "Full premium Overview page layout with all premium components"
      contains: "MetricHero"
    - path: "apps/dashboard/app/(dashboard)/cameras/page.tsx"
      provides: "Full premium Cameras page with grid/map toggle, filters, premium cards"
      contains: "CameraGrid"
    - path: "apps/dashboard/components/camera-card-premium.tsx"
      provides: "Enhanced camera card with 16:9 aspect, status badge, hover overlay, video thumbnail"
      exports: ["CameraCardPremium"]
    - path: "apps/dashboard/components/camera-grid.tsx"
      provides: "Responsive camera grid with staggered entrance, filter integration"
      exports: ["CameraGrid"]
  key_links:
    - from: "page.tsx (Overview)"
      to: "MetricHero"
      via: "4 MetricHero components in responsive grid, data from fetchDashboardStats"
    - from: "page.tsx (Overview)"
      to: "QuickActionBar"
      via: "Quick actions row between metrics and timeline section"
    - from: "page.tsx (Cameras)"
      to: "CameraGrid"
      via: "CameraGrid component with status filter and search state"

---

<objective>
Build the full premium redesign for the Overview and Cameras pages — the two highest-traffic dashboard pages. Overview gets a complete layout rearchitecture with MetricHero grid, DonutChart, ActivityTimeline, QuickActionBar, and camera preview grid. Cameras gets responsive grid with status filters, search, grid/map toggle, and premium camera cards.

Purpose: Deliver UIX-07 for the two most-visited pages — transforming the operator's primary dashboard experience and camera management interface.

Output: Rewritten Overview and Cameras pages, new CameraCardPremium and CameraGrid components.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-premium-experience/06-CONTEXT.md
@.planning/phases/06-premium-experience/06-UI-SPEC.md
@apps/dashboard/app/(dashboard)/page.tsx
@apps/dashboard/app/(dashboard)/cameras/page.tsx
@apps/dashboard/components/glass-card.tsx
@apps/dashboard/components/metric-hero.tsx
@apps/dashboard/components/sparkline.tsx
@apps/dashboard/components/donut-chart.tsx
@apps/dashboard/components/quick-action-bar.tsx
@apps/dashboard/components/activity-timeline.tsx
@apps/dashboard/components/page-transition.tsx
@apps/dashboard/lib/api.ts
@apps/dashboard/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Overview page with full premium layout</name>

  <read_first>
    @apps/dashboard/app/(dashboard)/page.tsx — Current Overview page with StatsCard grid + camera section + recent activity
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — Overview layout architecture lines 459-492, states table lines 494-504, data contract lines 505-515, copywriting lines 243-268
    @apps/dashboard/components/metric-hero.tsx — Created in Plan 03
    @apps/dashboard/components/donut-chart.tsx — Created in Plan 03
    @apps/dashboard/components/quick-action-bar.tsx — Created in Plan 03
    @apps/dashboard/components/activity-timeline.tsx — Created in Plan 03
    @apps/dashboard/components/page-transition.tsx — containerVariants, itemVariants
    @apps/dashboard/components/camera-card-premium.tsx — Will be created in Task 2 (reference only — Overview uses grid)
    @apps/dashboard/lib/api.ts — fetchDashboardStats, fetchCameras, DashboardStats type
  </read_first>

  <files>apps/dashboard/app/(dashboard)/page.tsx</files>

  <action>
    Rewrite the Overview page following UI-SPEC layout architecture:

    - Keep `"use client"` directive
    - Keep `fetchDashboardStats` and `fetchCameras` imports from `@/lib/api`
    - Import new premium components: `GlassCard` from `@/components/glass-card`, `MetricHero` from `@/components/metric-hero`, `DonutChart` from `@/components/donut-chart`, `QuickActionBar` from `@/components/quick-action-bar`, `ActivityTimeline` from `@/components/activity-timeline`, `Sparkline` from `@/components/sparkline`
    - Import `containerVariants`, `itemVariants` from `@/components/page-transition`
    - Import `motion` from `"motion/react"`
    - Remove imports of `StatsCard`, old `RecentActivity`, old `Card`/`CardContent`/`CardHeader`/`CardTitle`
    - Keep `Camera` type from api, keep `CameraCard` (or replace with CameraGrid — see below)

    **Layout structure** (top to bottom):

    1. **Page header**: Same as current — "Vue d'ensemble" heading, "Résumé de l'activité du système de surveillance" subtitle, real-time indicator. But use UI-SPEC copy lines 243-268 for all labels.

    2. **Metric Hero grid** (4 columns): Wrap in `<motion.div variants={containerVariants} initial="hidden" animate="visible">`:
       - Caméras en ligne: value from stats, trend from stats.cameras.trend, sparkline from stats.cameraTrend, icon: Video
       - Alertes actives: value stats.alerts.open, description with critical count, icon: AlertTriangle
       - Sites actifs: value stats.sites.active, description total, icon: MapPin
       - Utilisateurs: value stats.users.total, icon: Users

    3. **Quick-Action Bar**: Horizontal row with GlassCard wrapper:
       - "Ajouter une caméra" (Plus icon) → links to /cameras
       - "Voir les alertes" (AlertTriangle icon) → links to /alertes
       - "Générer un rapport" (FileText icon) → placeholder toast "Fonctionnalité à venir"
       - "Voir le flux" (Camera icon) → links to /cameras

    4. **Two-column section** (left 2/3 + right 1/3):
       - Left: **ActivityTimeline** with recent alerts data from stats.recentAlerts
       - Right: **DonutChart** showing alert severity distribution (stats.alerts.bySeverity or compute from recentAlerts)

    5. **Camera preview grid**: 4-column grid showing first 8 cameras using CameraCardPremium or inline CameraCard (reuse existing pattern). If no cameras, show empty state with "Ajouter une caméra" CTA.

    6. **Recent Activity** (full width): Use ActivityTimeline (same component but with all recent alerts)

    **States** (per UI-SPEC lines 494-504):
    - Loading: Shimmer skeletons in exact same grid shape as loaded state (4 metric skeleton cards + 8 camera card skeletons + timeline skeleton)
    - Loaded: Full content with staggered mount animation
    - Empty (no cameras): Camera section shows empty state CTA, rest of page renders normally
    - Empty (no activity): Timeline shows "Aucune activité récente"
    - Error: Full-page centered error card with retry button (same pattern as current)
    - Partial failure: If one API call fails but another succeeds, show partial data with warning

    **Data fetching**:
    - Keep `useEffect` with `Promise.all([fetchDashboardStats(), fetchCameras({ limit: 8 })])`
    - Keep loading/error state management pattern
  </action>

  <verify>
    <automated>grep -c -- 'MetricHero' apps/dashboard/app/(dashboard)/page.tsx | xargs test 1 -eq && grep -c -- 'QuickActionBar' apps/dashboard/app/(dashboard)/page.tsx | xargs test 1 -eq && grep -c -- 'ActivityTimeline' apps/dashboard/app/(dashboard)/page.tsx | xargs test 1 -eq && grep -c -- 'containerVariants' apps/dashboard/app/(dashboard)/page.tsx | xargs test 1 -eq && pnpm --filter @repo/dashboard run check-types</automated>
  </verify>

  <acceptance_criteria>
    - Overview page uses MetricHero (4 items), QuickActionBar, DonutChart, ActivityTimeline, camera preview grid
    - Staggered entrance animation via containerVariants/itemVariants from page-transition
    - Loading state shows skeleton placeholders matching loaded grid layout
    - Error state shows centered error card with retry button
    - Empty state for cameras section shows CTA
    - All French copy matches UI-SPEC (Vue d'ensemble, Caméras en ligne, Alertes actives, etc.)
    - Data fetched from existing fetchDashboardStats API
    - `pnpm --filter @repo/dashboard run check-types` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create CameraCardPremium and CameraGrid components</name>

  <read_first>
    @apps/dashboard/app/(dashboard)/page.tsx — Current CameraCard inline component (lines 42-84) for pattern reference
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — Camera card premium specs lines 571-579, Cameras page layout lines 517-569, states table lines 555-565
    @apps/dashboard/components/glass-card.tsx — GlassCard wrapper
    @apps/dashboard/components/page-transition.tsx — containerVariants, itemVariants
  </read_first>

  <files>
    apps/dashboard/components/camera-card-premium.tsx
    apps/dashboard/components/camera-grid.tsx
  </files>

  <action>
    Create two components:

    **camera-card-premium.tsx**:
    - `"use client"` component
    - Interface `CameraCardPremiumProps`:
      - `camera: { id: string; name: string; status: string; lastSnapshotUrl?: string | null; site?: { name: string } | null }`
      - `onClick?: (id: string) => void`
      - `className?: string`
    - Import `motion` from `"motion/react"`, `cn` from `@/lib/utils`, `Image` from `next/image`, `Link` from `next/link`, `Video` from `"lucide-react"`
    - Render a `motion.div` with `itemVariants` for stagger support + `whileHover={{ scale: 1.02 }}` transition
    - Layout:
      - Relative container with `rounded-xl overflow-hidden border bg-card hover:border-primary/30 transition-all duration-200`
      - **Thumbnail area** (aspect-video / 16:9): If `lastSnapshotUrl`, render `<Image>` with fill, object-cover, unoptimized, loading="lazy". If no thumbnail, show centered `Video` icon in `text-muted-foreground/20`. Show `loading="lazy"` for below-fold thumbnails.
      - **Status badge** (top-right): Badge with status color and label. ONLINE badge uses `status-pulse` dot animation. Per UI-SPEC: ONLINE=success/En ligne, OFFLINE=destructive/Hors ligne, MAINTENANCE=warning/Maintenance, DEGRADED=warning/Dégradé
      - **Hover overlay**: Semi-transparent dark overlay with "Voir le flux" button (links to /cameras/{id}) — `opacity-0 group-hover:opacity-100` via CSS
      - **Details** (below image): Camera name (14px semibold, truncate), site name (12px muted-foreground)
    - The whole card is a group for hover effects: `className="group"`
    - Export `CameraCardPremium` as named export

    **camera-grid.tsx**:
    - `"use client"` component
    - Interface `CameraGridProps`:
      - `cameras: CameraCardPremiumProps["camera"][]`
      - `loading?: boolean`
      - `emptyMessage?: string`
      - `filter?: (camera: any) => boolean`
      - `className?: string`
    - Import `CameraCardPremium` from `@/components/camera-card-premium`
    - Import `motion`, `containerVariants`, `itemVariants` from `@/components/page-transition`
    - Import `Skeleton` from `@/components/ui/skeleton`
    - Render:
      - `<motion.div variants={containerVariants} initial="hidden" animate="visible" className="grid gap-4 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">`
      - If `loading`: 8 skeleton cards (Skeleton with aspect-video + text skeleton lines)
      - If not loading and cameras.length === 0: Empty state with icon + message + "Ajouter une caméra" button
      - If cameras exist: map over filtered cameras, each as `<motion.div variants={itemVariants}><CameraCardPremium .../></motion.div>`
    - Export `CameraGrid` as named export
  </action>

  <verify>
    <automated>test -f apps/dashboard/components/camera-card-premium.tsx && test -f apps/dashboard/components/camera-grid.tsx && grep -c -- 'CameraCardPremium' apps/dashboard/components/camera-card-premium.tsx | xargs test 1 -eq && grep -c -- 'CameraGrid' apps/dashboard/components/camera-grid.tsx | xargs test 1 -eq && grep -c -- 'group-hover' apps/dashboard/components/camera-card-premium.tsx | xargs test 1 -ge && pnpm --filter @repo/dashboard run check-types</automated>
  </verify>

  <acceptance_criteria>
    - camera-card-premium.tsx: 16:9 thumbnail with Image unoptimized or placeholder Video icon
    - camera-card-premium.tsx: status badge top-right with color and label — ONLINE uses status-pulse dot
    - camera-card-premium.tsx: hover overlay with "Voir le flux" button, scale 1.02 on hover
    - camera-card-premium.tsx: camera name (14px semibold truncate), site name (12px muted)
    - camera-grid.tsx: responsive grid (1→2→3→4 columns), staggered entrance via containerVariants
    - camera-grid.tsx: loading state with 8 skeleton cards, empty state with CTA
    - `pnpm --filter @repo/dashboard run check-types` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rewrite Cameras page with premium layout, filters, grid/map toggle</name>

  <read_first>
    @apps/dashboard/app/(dashboard)/cameras/page.tsx — Current cameras page with DataTable, create/delete/edit flows
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — Cameras page layout lines 517-553, states lines 555-565, search/filter lines 566-569, copywriting lines 271-295
    @apps/dashboard/components/camera-grid.tsx — Created in Task 2
    @apps/dashboard/components/camera-card-premium.tsx — Created in Task 2
  </read_first>

  <files>apps/dashboard/app/(dashboard)/cameras/page.tsx</files>

  <action>
    Rewrite the Cameras page with premium layout. Keep ALL existing data operations (fetchCameras, createCamera, updateCamera, deleteCamera) but present them in the new premium layout.

    **Rewrite page.tsx** with this structure:

    1. **Page header**: "Caméras" heading, subtitle "{count} caméra{plural} connectée{plural}", using CameraGrid data

    2. **Search + Filter bar**: 
       - Search input (debounced 300ms) filtering by camera name and site name
       - Status filter pills: Toutes, En ligne, Hors ligne, Dégradé, Maintenance (horizontal pill group)
       - "Ajouter une caméra" button (opens existing form dialog/sheet)

    3. **View toggle**: Grid | Plan toggle switch (two buttons: Grille icon + Plan/Map icon). When "Grille" selected, show CameraGrid. When "Plan" selected, show a placeholder map area with "Carte des caméras — fonctionnalité à venir" message (map integration deferred per Phase 6 scope — map is visual placeholder only).
       - The toggle uses `AnimatePresence mode="wait"` for the transition

    4. **CameraGrid**: Pass filtered cameras (by status + search). Handle all states per UI-SPEC:
       - Loading: 8 skeleton cards (via CameraGrid's loading prop)
       - Loaded with cameras: Grid with staggered entrance
       - Empty (no cameras at all): "Aucune caméra" full-page empty state with "Ajouter une caméra" button
       - Empty (filter yields 0 results): "Aucune caméra ne correspond aux filtres" with "Réinitialiser les filtres" ghost button
       - Error: Full-page centered error card with retry

    5. **Keep existing modals/dialogs**: The create/edit/delete dialogs should still work. Import existing dialog components that were used before. Keep the existing form states (showForm, editingId, etc).

    6. **Data fetching**: Keep all existing API functions. The page already uses `fetchCameras()` with status filter and search — adapt the premium UI to drive these same data flows.

    **Important**: Do NOT remove any existing API integration or CRUD functionality. The redesign is visual only — operations (create, edit, delete, stream start/stop) must remain functional. Preserve the existing dialog/sheet/table for camera operations, or integrate with new premium dialogs if time permits.

    Note: If the existing cameras page is >400 lines with complex CRUD logic, consider creating the new premium layout as the primary view while keeping the existing CRUD dialogs accessible via the "Ajouter" button.
  </action>

  <verify>
    <automated>grep -c -- 'CameraGrid' apps/dashboard/app/(dashboard)/cameras/page.tsx | xargs test 1 -eq && grep -c -- 'fetchCameras' apps/dashboard/app/(dashboard)/cameras/page.tsx | xargs test 1 -eq && grep -c -- 'createCamera\|deleteCamera' apps/dashboard/app/(dashboard)/cameras/page.tsx | xargs test 1 -ge && grep -c -- 'search\|Search\|debounce' apps/dashboard/app/(dashboard)/cameras/page.tsx | xargs test 1 -ge && pnpm --filter @repo/dashboard run check-types</automated>
  </verify>

  <acceptance_criteria>
    - Cameras page uses CameraGrid premium grid (not old table/DataTable)
    - Search bar with debounced input filtering by camera name and site name
    - Status filter pills (Toutes, En ligne, Hors ligne, Dégradé, Maintenance)
    - View toggle between Grid and Plan/Map views (map shows placeholder)
    - Loading state with skeleton cards, empty state with CTA, filter-zero-results state with reset button
    - Error state with retry button
    - All existing CRUD operations preserved (create/edit/delete via dialogs)
    - French copy matches UI-SPEC (Caméras, Rechercher une caméra, Toutes, En ligne, etc.)
    - `pnpm --filter @repo/dashboard run check-types` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API data rendering | Page components render existing API data — no new data access added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Spoofing | Image rendering (camera thumbnails) | accept | Camera snapshot URLs come from existing authenticated API — same URLs already rendered in current pages |
| T-06-09 | Information Disclosure | Search debounce | accept | Search filters local data only — no new API queries introduced |
</threat_model>

<verification>
1. `pnpm --filter @repo/dashboard run check-types` passes
2. `pnpm --filter @repo/dashboard run lint` passes
3. Manual: Overview page renders with premium layout — MetricHero grid, donut chart, timeline, quick actions, camera grid
4. Manual: Overview staggered entrance animation plays on load
5. Manual: Cameras page renders with premium grid, filters work, search works
6. Manual: Camera card hover shows overlay with "Voir le flux"
7. Manual: Camera CRUD operations still work via existing dialogs
8. Manual: All loading, empty, and error states display correctly
9. Manual: French copy per UI-SPEC contract
</verification>

<success_criteria>
- Overview page fully rewritten with premium layout (MetricHero, DonutChart, QuickActionBar, ActivityTimeline, CameraGrid)
- Overview page handles all states: loading (skeletons), loaded (animated), empty (CTA), error (retry), partial failure
- Cameras page rewritten with premium grid layout, search bar, status filter pills, grid/map toggle
- CameraCardPremium component with status badge, hover overlay, scaled animation
- CameraGrid component with responsive columns, staggered entrance, loading/empty states
- All existing camera CRUD operations remain functional
- TypeScript compilation passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/06-premium-experience/06-05-SUMMARY.md` when done
</output>
