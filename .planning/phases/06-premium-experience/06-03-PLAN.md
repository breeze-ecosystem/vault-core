---
phase: 06-premium-experience
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - apps/dashboard/components/glass-card.tsx
  - apps/dashboard/components/sparkline.tsx
  - apps/dashboard/components/metric-hero.tsx
  - apps/dashboard/components/donut-chart.tsx
  - apps/dashboard/components/quick-action-bar.tsx
  - apps/dashboard/components/activity-timeline.tsx
  - apps/dashboard/components/stats-card.tsx
  - apps/dashboard/components/page-header.tsx
  - apps/dashboard/components/header.tsx
  - apps/dashboard/components/sidebar.tsx
autonomous: true
requirements:
  - UIX-02
  - UIX-06

must_haves:
  truths:
    - "Premium shared UI components (GlassCard, Sparkline, MetricHero, DonutChart, QuickActionBar, ActivityTimeline) exist and are importable by page redesigns"
    - "GlassCard supports default and accent variants with backdrop-blur glassmorphism"
    - "Sparkline renders inline SVG mini-chart from data points array"
    - "DonutChart renders SVG donut chart for severity distribution data"
    - "Existing StatsCard is enhanced with GlassCard styling and optional sparkline prop"
    - "Existing PageHeader uses new typography scale (20px heading, 14px subtitle)"
    - "Header has glass effect background"
    - "Sidebar nav items use accent color for active indicator per UI-SPEC reservation list"
  artifacts:
    - path: "apps/dashboard/components/glass-card.tsx"
      provides: "Shared glassmorphism card with default/accent variants"
      exports: ["GlassCard"]
    - path: "apps/dashboard/components/metric-hero.tsx"
      provides: "Large stat display with sparkline, icon, trend indicator"
      exports: ["MetricHero"]
    - path: "apps/dashboard/components/sparkline.tsx"
      provides: "Inline SVG mini-chart for metric trends"
      exports: ["Sparkline"]
    - path: "apps/dashboard/components/donut-chart.tsx"
      provides: "SVG donut chart for severity distribution"
      exports: ["DonutChart"]
    - path: "apps/dashboard/components/quick-action-bar.tsx"
      provides: "Floating toolbar with primary page actions"
      exports: ["QuickActionBar"]
    - path: "apps/dashboard/components/activity-timeline.tsx"
      provides: "Real-time scrollable timeline with severity bars"
      exports: ["ActivityTimeline"]
  key_links:
    - from: "glass-card.tsx"
      to: "globals.css"
      via: "Uses glass-premium / glass-accent CSS classes + cn() utility"
    - from: "sparkline.tsx"
      to: "metric-hero.tsx"
      via: "Imported and rendered inside MetricHero"
    - from: "donut-chart.tsx"
      to: "recharts"
      via: "Uses PieChart, Pie, Cell from recharts"
    - from: "stats-card.tsx"
      to: "glass-card.tsx"
      via: "Replaced Card with GlassCard styling"

---

<objective>
Create all premium shared UI components for the dashboard (GlassCard, Sparkline, MetricHero, DonutChart, QuickActionBar, ActivityTimeline) and apply the global design system uplift to existing components (StatsCard, PageHeader, Header, Sidebar) without altering their logic.

Purpose: Build the reusable component library that the three page redesigns (Overview, Cameras, Alerts) will consume, while simultaneously upgrading all existing dashboard pages to use the new design tokens.

Output: 6 new components in apps/dashboard/components/, 4 existing components enhanced with new styling.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-premium-experience/06-CONTEXT.md
@.planning/phases/06-premium-experience/06-RESEARCH.md
@.planning/phases/06-premium-experience/06-UI-SPEC.md
@apps/dashboard/app/globals.css
@apps/dashboard/components/stats-card.tsx
@apps/dashboard/components/page-header.tsx
@apps/dashboard/components/header.tsx
@apps/dashboard/components/sidebar.tsx
@apps/dashboard/lib/utils.ts
@apps/dashboard/components/page-transition.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GlassCard, Sparkline, MetricHero, DonutChart components</name>

  <read_first>
    @apps/dashboard/app/globals.css — .glass-premium, .glass-accent CSS classes created in Plan 01, stats classes (.stat-value, .section-title)
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — GlassCard specs lines 105-125, MetricHero component spec (Overview layout lines 459-492), Sparkline inline SVG, DonutChart (severity distribution)
    @.planning/phases/06-premium-experience/06-RESEARCH.md — GlassCard code example lines 634-667, staggered animation variants (page-transition.tsx created in Plan 02)
    @apps/dashboard/lib/utils.ts — cn() utility
    @apps/dashboard/components/stats-card.tsx — Existing pattern for stat display (reference for MetricHero)
  </read_first>

  <files>
    apps/dashboard/components/glass-card.tsx
    apps/dashboard/components/sparkline.tsx
    apps/dashboard/components/metric-hero.tsx
    apps/dashboard/components/donut-chart.tsx
  </files>

  <action>
    Create four new components:

    **glass-card.tsx** (per RESEARCH.md lines 634-667 + UI-SPEC glassmorphism pattern):
    - Import `cn` from `@/lib/utils`
    - Import `motion` from `"motion/react"`
    - Import `containerVariants` from `@/components/page-transition` for staggered entrance
    - Interface `GlassCardProps extends HTMLAttributes<HTMLDivElement>` with optional `variant: "default" | "accent"` (defaults to "default")
    - Render a `motion.div` (not a regular div — enables stagger animation) with:
      - `default` variant: `rounded-xl border bg-card/60 backdrop-blur-sm border-border/40 shadow-[0_8px_32px_hsl(var(--shadcn-primary)/0.04)]`
      - `accent` variant: `bg-primary/[0.04] border-primary/[0.12] shadow-[0_8px_32px_hsl(var(--shadcn-primary)/0.06)]`
      - Both: `transition-all duration-200`
    - Use `cn()` to compose classes
    - Include `variants` prop wiring with itemVariants for staggered entrance support
    - Forward remaining div props via spread
    - Export `GlassCard` as named export

    **sparkline.tsx**:
    - Interface `SparklineProps`: `{ data: number[]; color?: string; height?: number; className?: string }`
    - Render an inline SVG element:
      - SVG viewBox: `0 0 ${data.length * 10} ${height}` (default height 32)
      - Calculate polyline points: map data values to x,y coordinates
      - Draw `<polyline>` with `fill="none"`, `stroke={color || "hsl(var(--shadcn-primary))"}`, `strokeWidth="2"`
      - Add subtle gradient fill below the line using `<linearGradient>` + `<path>`
      - Smooth bezier curves using SVG path commands
    - Export `Sparkline` as named export

    **metric-hero.tsx**:
    - Interface `MetricHeroProps`:
      - `title: string`, `value: string | number`, `icon: LucideIcon`
      - Optional `trend?: { value: number; positive: boolean }`
      - Optional `description?: string`
      - Optional `sparklineData?: number[]`
      - Optional `className?: string`
    - Use GlassCard as wrapper (variant="default")
    - Layout: icon top-right in `bg-primary/10` rounded-lg container, value in `font-mono tabular-nums text-2xl font-semibold tracking-tight`, title in `text-xs font-medium uppercase tracking-wider text-muted-foreground`
    - If `sparklineData` provided, render `<Sparkline data={sparklineData} />` inline below value
    - If `trend` provided, render trend indicator: `↑` (green/positive) or `↓` (red/negative)
    - Use `motion.div` with the itemVariants from page-transition for staggered entrance
    - Export `MetricHero` as named export

    **donut-chart.tsx**:
    - Import `recharts` PieChart, Pie, Cell, ResponsiveContainer
    - Interface `DonutChartProps`: `{ data: Array<{ name: string; value: number; color: string }>; size?: number; className?: string }`
    - Render `ResponsiveContainer` → `PieChart` → `Pie` with:
      - `innerRadius={60}`, `outerRadius={80}`, `paddingAngle={2}`, `dataKey="value"`
      - `startAngle={90}`, `endAngle={-270}` (top-starting donut)
      - Custom label showing total in center (render custom label component)
    - Use `Cell` for each entry with `fill={entry.color}`
    - Colors per UI-SPEC accent reservation: critical=destructive (#ef4444), high=warning (#f59e0b), medium=primary (#06b6d4), low=muted-foreground
    - Wrap in GlassCard variant="default"
    - Export `DonutChart` as named export
  </action>

  <verify>
    <automated>test -f apps/dashboard/components/glass-card.tsx && test -f apps/dashboard/components/sparkline.tsx && test -f apps/dashboard/components/metric-hero.tsx && test -f apps/dashboard/components/donut-chart.tsx && grep -c -- 'GlassCard' apps/dashboard/components/glass-card.tsx | xargs test 1 -eq && grep -c -- 'Sparkline' apps/dashboard/components/sparkline.tsx | xargs test 1 -eq && grep -c -- 'MetricHero' apps/dashboard/components/metric-hero.tsx | xargs test 1 -eq && grep -c -- 'DonutChart' apps/dashboard/components/donut-chart.tsx | xargs test 1 -eq && grep -c -- 'recharts' apps/dashboard/components/donut-chart.tsx | xargs test 1 -eq</automated>
  </verify>

  <acceptance_criteria>
    - glass-card.tsx: exports `GlassCard` with variant "default" and "accent"; uses backdrop-blur, border, shadow per UI-SPEC glass spec
    - sparkline.tsx: exports `Sparkline` component rendering inline SVG with data-driven polyline + gradient fill
    - metric-hero.tsx: exports `MetricHero` with optional trend, sparklineData, description; uses GlassCard wrapper; icon in bg-primary/10 container
    - donut-chart.tsx: exports `DonutChart` using recharts PieChart with innerRadius=60 outerRadius=80, severity color mapping
    - All components import `cn()` from `@/lib/utils`
    - All components are "use client" where needed (motion.div, recharts)
    - `pnpm --filter @repo/dashboard run check-types` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create QuickActionBar and ActivityTimeline components</name>

  <read_first>
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — QuickActionBar (Overview layout lines 471-473, tooltip specs), ActivityTimeline (Overview layout lines 476-484)
    @apps/dashboard/components/recent-activity.tsx — Existing timeline pattern (severity config, time formatting)
    @apps/dashboard/components/ui/button.tsx — Button component variant patterns
  </read_first>

  <files>
    apps/dashboard/components/quick-action-bar.tsx
    apps/dashboard/components/activity-timeline.tsx
  </files>

  <action>
    Create two components:

    **quick-action-bar.tsx**:
    - Interface `QuickActionBarProps`:
      - `actions: Array<{ id: string; label: string; icon: LucideIcon; onClick: () => void; variant?: "default" | "outline" }>`
      - `className?: string`
    - Render a horizontal flex container with `gap-2 p-1` inside a GlassCard (variant="default") with no padding override
    - Each action rendered as a `Button` using shadcn/ui Button with:
      - Default variant="outline" (can be overridden via action.variant)
      - `size="sm"` with `gap-2`
      - Icon + label
    - Use `motion.div` layout container with staggered children entrance
    - Export `QuickActionBar` as named export

    **activity-timeline.tsx**:
    - Interface `ActivityTimelineProps`:
      - `alerts: Array<{ id: string; title: string; severity: string; createdAt: string; camera?: { name: string } | null }>`
      - `className?: string`
      - `maxItems?: number` (default 10)
    - Severity color config matching UI-SPEC: CRITICAL=red (#ef4444), HIGH=amber (#f59e0b), MEDIUM=primary (#06b6d4), LOW=muted-foreground
    - Render inside GlassCard variant="default"
    - Each row: 3px left border in severity color, title (14px semibold truncate), location line (12px muted-foreground with camera name + relative time), relative time formatted per existing formatTime pattern
    - Empty state: centered placeholder with AlertTriangle icon, "Aucune activité récente"
    - Use `motion.div` with staggered item variants for row entrance animation
    - Use `AnimatePresence` for alert appearance/disappearance transitions
    - Export `ActivityTimeline` as named export
  </action>

  <verify>
    <automated>test -f apps/dashboard/components/quick-action-bar.tsx && test -f apps/dashboard/components/activity-timeline.tsx && grep -c -- 'QuickActionBar' apps/dashboard/components/quick-action-bar.tsx | xargs test 1 -eq && grep -c -- 'ActivityTimeline' apps/dashboard/components/activity-timeline.tsx | xargs test 1 -eq && grep -c -- 'staggerChildren\|stagger' apps/dashboard/components/activity-timeline.tsx | xargs test 1 -ge</automated>
  </verify>

  <acceptance_criteria>
    - quick-action-bar.tsx: renders action buttons in horizontal flex row with GlassCard wrapper
    - quick-action-bar.tsx: each action has icon + label, uses shadcn/ui Button
    - activity-timeline.tsx: renders rows with 3px left severity color border
    - activity-timeline.tsx: severity colors mapped: CRITICAL→destructive, HIGH→warning, MEDIUM→primary, LOW→muted
    - activity-timeline.tsx: empty state renders when no alerts provided
    - activity-timeline.tsx: uses staggered motion entrance
    - Both are "use client"
    - `pnpm --filter @repo/dashboard run check-types` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Apply global design system uplift to existing components (StatsCard, PageHeader, Header, Sidebar)</name>

  <read_first>
    @apps/dashboard/components/stats-card.tsx — Current Card-based implementation
    @apps/dashboard/components/page-header.tsx — Current h1 + description
    @apps/dashboard/components/header.tsx — Current header with bg-background/80 backdrop-blur-xl
    @apps/dashboard/components/sidebar.tsx — Current sidebar with bg-background/80
    @apps/dashboard/components/glass-card.tsx — Created in Task 1
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — Global uplift table lines 641-655
  </read_first>

  <files>
    apps/dashboard/components/stats-card.tsx
    apps/dashboard/components/page-header.tsx
    apps/dashboard/components/header.tsx
    apps/dashboard/components/sidebar.tsx
  </files>

  <action>
    Enhance four existing components WITHOUT rewriting their logic:

    **stats-card.tsx**:
    - Replace the outer `<Card>` wrapper with `<GlassCard variant="default">` (import from `@/components/glass-card`)
    - Remove the absolute rounded div (the glowing circle) — replaced by glass card natural styling
    - Keep the exact same props interface and data flow
    - Wrap in `motion.div` with `itemVariants` from page-transition for stagger entrance support
    - Keep icon, title, value, trend props and rendering exactly as is
    - Keep the `group` hover effects

    **page-header.tsx**:
    - Update `h1` to use `text-xl font-semibold tracking-tight` (per UI-SPEC typography: 20px heading)
    - Update description paragraph to use `text-sm text-muted-foreground` (14px body)
    - Keep the same Props interface and action button rendering
    - Keep the flex layout structure

    **header.tsx**:
    - Change the header's `bg-background/80 backdrop-blur-xl` to `glass-premium` class (adds the enhanced glass effect)
    - Keep `sticky top-0 z-20` and all layout/structure
    - Keep all existing content (language switcher, notification bell, user dropdown, org switcher)
    - No structural changes

    **sidebar.tsx**:
    - The sidebar already has `bg-background/80 backdrop-blur-xl` — change to use same enhanced glass, or keep existing (the glass-premium class doesn't apply well to full-height sidebars). Instead, ensure the active nav item uses the accent color properly: the active item already uses `bg-primary/10 text-primary` — this matches the accent reservation list. No additional changes needed beyond what Plan 01 already did (adding ThemeToggle).
    - Confirm: sidebar already has correct accent color for active items. No changes needed here.
    - Just ensure the `ThemeToggle` added in Plan 01 is visible and renders correctly in the new token context.
  </action>

  <verify>
    <automated>grep -c -- 'GlassCard' apps/dashboard/components/stats-card.tsx | xargs test 1 -eq && grep -c -- 'text-xl font-semibold' apps/dashboard/components/page-header.tsx | xargs test 1 -eq && grep -c -- 'glass-premium' apps/dashboard/components/header.tsx | xargs test 1 -eq && pnpm --filter @repo/dashboard run check-types</automated>
  </verify>

  <acceptance_criteria>
    - stats-card.tsx: imports and uses `GlassCard` instead of `Card`; wrapped in motion.div with itemVariants
    - page-header.tsx: h1 uses `text-xl font-semibold tracking-tight` (20px); description uses `text-sm text-muted-foreground`
    - header.tsx: header element uses `glass-premium` class for enhanced glass effect
    - `ThemeToggle` in sidebar renders correctly within new design token context
    - `pnpm --filter @repo/dashboard run check-types` passes
    - No changes to component logic, props interfaces, or import consumers
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side components | All new components are presentational — no data fetching, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | recharts (donut-chart) | accept | recharts is already installed, charts render data from existing API — no new data exposure |
| T-06-05 | Tampering | CSS class injection | mitigate | All className composed via `cn()` utility — no direct string interpolation from user input |
</threat_model>

<verification>
1. `pnpm --filter @repo/dashboard run check-types` passes
2. `pnpm --filter @repo/dashboard run lint` passes
3. Manual: Dashboard renders with enhanced glass effects on header and stats cards
4. Manual: Page header typography updated (20px heading, 14px subtitle)
5. Manual: All existing pages render without layout breakage
</verification>

<success_criteria>
- 6 new premium components created and type-checked: GlassCard, Sparkline, MetricHero, DonutChart, QuickActionBar, ActivityTimeline
- StatsCard enhanced with GlassCard wrapper and motion staggered entrance
- PageHeader uses new typography scale (20px heading, 14px subtitle)
- Header has glass-premium background effect
- All existing page logic remains unchanged — CSS variable substitution and component enhancement only
- TypeScript compilation passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/06-premium-experience/06-03-SUMMARY.md` when done
</output>
