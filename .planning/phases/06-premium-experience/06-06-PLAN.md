---
phase: 06-premium-experience
plan: 06
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - apps/dashboard/app/(dashboard)/alertes/page.tsx
  - apps/dashboard/components/alert-feed.tsx
  - apps/dashboard/components/alert-row.tsx
  - apps/dashboard/components/alert-side-panel.tsx
  - apps/dashboard/components/bulk-action-bar.tsx
autonomous: true
requirements:
  - UIX-07

must_haves:
  truths:
    - "Alerts page has real-time scrolling feed with severity color bars (3px left border)"
    - "Each alert row shows severity color bar, title, location + relative time, inline acknowledge button, video thumbnail button"
    - "Alert rows animate in with sequential slide-in from right (40ms stagger)"
    - "Side panel slides in from right when alert is selected, shows details + actions"
    - "Side panel slides out when alert is deselected"
    - "Bulk action bar appears when ≥1 alert is selected (acknowledge/resolve)"
    - "Filter pills filter by severity (Toutes, Critiques, Haute, Moyenne, Basse)"
    - "Empty state when no alerts: 'Aucune alerte active' with shield-check icon"
    - "Loading state shows 5 skeleton rows"
    - "Real-time WebSocket connection feeds new alerts into the feed"
    - "Socket.IO disconnect banner shows when connection is lost"
  artifacts:
    - path: "apps/dashboard/components/alert-row.tsx"
      provides: "Individual alert item with severity bar, inline actions, hover effect"
      exports: ["AlertRow"]
    - path: "apps/dashboard/components/alert-feed.tsx"
      provides: "Real-time scrolling alert list with staggered entrance"
      exports: ["AlertFeed"]
    - path: "apps/dashboard/components/alert-side-panel.tsx"
      provides: "Detail/action side panel for selected alert"
      exports: ["AlertSidePanel"]
    - path: "apps/dashboard/components/bulk-action-bar.tsx"
      provides: "Floating toolbar for batch acknowledge/resolve"
      exports: ["BulkActionBar"]
    - path: "apps/dashboard/app/(dashboard)/alertes/page.tsx"
      provides: "Full premium Alerts page with feed, side panel, bulk actions, real-time WS"
      contains: "AlertFeed"
  key_links:
    - from: "alert-row.tsx"
      to: "alert-feed.tsx"
      via: "Imported and rendered by AlertFeed"
    - from: "alert-side-panel.tsx"
      to: "alertes/page.tsx"
      via: "Controlled by selected alert state in page"
    - from: "alertes/page.tsx"
      to: "socket.io-client"
      via: "WebSocket connection for real-time alert feed"

---

<objective>
Build the full premium redesign for the Alerts page — a real-time scrolling alert feed with severity color bars, inline actions, detail side panel with slide animation, and bulk action toolbar. Preserve existing WebSocket real-time connectivity.

Purpose: Deliver UIX-07 for the Alerts page — security operators need a premium triage interface that shows alert priority at a glance with fast acknowledge/resolve workflows.

Output: Rewritten Alerts page, new AlertRow, AlertFeed, AlertSidePanel, BulkActionBar premium components.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-premium-experience/06-CONTEXT.md
@.planning/phases/06-premium-experience/06-UI-SPEC.md
@apps/dashboard/app/(dashboard)/alertes/page.tsx
@apps/dashboard/components/glass-card.tsx
@apps/dashboard/components/page-transition.tsx
@apps/dashboard/app/globals.css
@apps/dashboard/lib/api.ts
@apps/dashboard/components/ui/button.tsx
@apps/dashboard/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AlertRow and AlertFeed components</name>

  <read_first>
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — Alert Row spec lines 628-636, Alert Feed layout lines 586-611, animation table lines 410-412, states table lines 614-627
    @apps/dashboard/app/(dashboard)/alertes/page.tsx — Current severity/status label configs (severityVariant, severityLabels, statusLabels) for reference
    @apps/dashboard/components/page-transition.tsx — containerVariants, itemVariants
    @apps/dashboard/components/glass-card.tsx — GlassCard wrapper
    @apps/dashboard/lib/api.ts — Alert type
  </read_first>

  <files>
    apps/dashboard/components/alert-row.tsx
    apps/dashboard/components/alert-feed.tsx
  </files>

  <action>
    Create two components:

    **alert-row.tsx**:
    - `"use client"` component
    - Interface `AlertRowProps`:
      - `alert: { id: string; title: string; severity: string; status: string; createdAt: string; camera?: { id: string; name: string } | null; snapshotUrl?: string | null }`
      - `selected?: boolean`
      - `onSelect?: (id: string) => void`
      - `onAcknowledge?: (id: string) => void`
      - `onViewCamera?: (id: string) => void`
      - `className?: string`
    - Import `motion` from `"motion/react"`, `itemVariants` from `@/components/page-transition`, `cn` from `@/lib/utils`
    - Import `Button` from `@/components/ui/button`, `Badge` from `@/components/ui/badge`
    - Import `CheckCircle`, `Eye`, `Camera` from `"lucide-react"`

    - Severity color config (3px left border color):
      - CRITICAL: `border-l-destructive` (red #ef4444)
      - HIGH: `border-l-warning` (amber #f59e0b)
      - MEDIUM: `border-l-primary` (cyan #06b6d4)
      - LOW: `border-l-muted-foreground` (gray)
      - INFO: `border-l-muted` (light gray)

    - Render a `motion.div` with `itemVariants` for stagger support:
      - Layout: horizontal flex row with:
        - **Severity bar**: 3px left border in severity color (via Tailwind `border-l-{color}` classes)
        - **Content** (flex-1): Title (14px semibold, 1-line truncation), Location + relative time (12px muted-foreground)
        - **Actions** (right, flex shrink-0): 
          - If camera-linked, show small icon button (Eye→Camera icon) for viewing camera
          - If status === OPEN, show small acknowledge button (CheckCircle icon, ghost variant, text-xs)
          - Relative timestamp badge
      - Hover: `hover:bg-muted/50`
      - Selected state: `bg-muted/30 border-l-[3px]` with the severity color
      - Clicking the row calls `onSelect(alert.id)`
      - Cursor: `cursor-pointer` for entire row
    - Export `AlertRow` as named export

    **alert-feed.tsx**:
    - `"use client"` component
    - Interface `AlertFeedProps`:
      - `alerts: AlertRowProps["alert"][]`
      - `loading?: boolean`
      - `selectedId?: string | null`
      - `onSelectAlert?: (id: string) => void`
      - `onAcknowledge?: (id: string) => void`
      - `onViewCamera?: (id: string) => void`
      - `emptyMessage?: string`
      - `className?: string`
    - Import `motion`, `AnimatePresence`, `containerVariants` from `@/components/page-transition`
    - Import `AlertRow` from `@/components/alert-row`
    - Import `Skeleton` from `@/components/ui/skeleton`

    - Render:
      - If `loading`: 5 skeleton rows (Skeleton h-16 w-full with rounded-lg, separated by 1px)
      - If not loading and alerts.length === 0: Centered empty state with ShieldCheck icon + "Aucune alerte active" heading + body text per UI-SPEC copy
      - If alerts exist: `<motion.div variants={containerVariants} initial="hidden" animate="visible">` with `<AnimatePresence mode="popLayout">` wrapping the mapped AlertRow components
      - Each AlertRow wrapped in `<motion.div variants={itemVariants} layout>` for staggered + layout animation
      - Filtered alerts should animate out with exit animation
    - Wrap the entire component in a scrollable container with max-h (calculate based on viewport)
    - Export `AlertFeed` as named export
  </action>

  <verify>
    <automated>test -f apps/dashboard/components/alert-row.tsx && test -f apps/dashboard/components/alert-feed.tsx && grep -c -- 'AlertRow' apps/dashboard/components/alert-row.tsx | xargs test 1 -eq && grep -c -- 'AlertFeed' apps/dashboard/components/alert-feed.tsx | xargs test 1 -eq && grep -c -- 'severity' apps/dashboard/components/alert-row.tsx | xargs test 1 -ge && pnpm --filter @repo/dashboard run check-types</automated>
  </verify>

  <acceptance_criteria>
    - alert-row.tsx: 3px left border in severity color (destructive/warning/primary/muted-foreground)
    - alert-row.tsx: title (14px semibold truncate), location + relative time (12px muted)
    - alert-row.tsx: inline acknowledge button (if OPEN), camera thumbnail button (if camera-linked)
    - alert-row.tsx: selected state with bg-muted/30, hover with bg-muted/50
    - alert-row.tsx: uses itemVariants for stagger support
    - alert-feed.tsx: renders AlertRow list with containerVariants stagger + AnimatePresence popLayout
    - alert-feed.tsx: loading state with 5 skeleton rows
    - alert-feed.tsx: empty state with "Aucune alerte active" + shield-check icon
    - `pnpm --filter @repo/dashboard run check-types` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create AlertSidePanel and BulkActionBar components</name>

  <read_first>
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — Side panel spec lines 598-612, bulk action bar spec lines 591-592, animation specs side panel lines 412-414, bulk action bar line 419
    @apps/dashboard/components/glass-card.tsx — GlassCard wrapper
    @apps/dashboard/components/ui/button.tsx — Button variants
    @apps/dashboard/lib/api.ts — Alert type
  </read_first>

  <files>
    apps/dashboard/components/alert-side-panel.tsx
    apps/dashboard/components/bulk-action-bar.tsx
  </files>

  <action>
    Create two components:

    **alert-side-panel.tsx**:
    - `"use client"` component
    - Interface `AlertSidePanelProps`:
      - `alert: { id: string; title: string; description?: string | null; severity: string; status: string; createdAt: string; camera?: { id: string; name: string } | null; snapshotUrl?: string | null; site?: string | null } | null`
      - `open: boolean`
      - `onClose: () => void`
      - `onAcknowledge?: (id: string) => void`
      - `onResolve?: (id: string) => void`
      - `onViewCamera?: (id: string) => void`
    - Import `motion`, `AnimatePresence` from `"motion/react"`
    - Import `cn` from `@/lib/utils`, `Button` from `@/components/ui/button`
    - Import `X`, `CheckCircle`, `CheckSquare`, `Eye`, `Camera`, `Clock`, `MapPin` from `"lucide-react"`

    - Render inside `<AnimatePresence>`:
      - If `!open || !alert`, render nothing (or render placeholder text "Sélectionnez une alerte")
      - If open + alert:
        - `motion.aside` or `motion.div` that:
          - Slides in from right: `initial={{ x: 320 }}` → `animate={{ x: 0 }}` → `exit={{ x: 320 }}`
          - Duration: enter 250ms easeOut, exit 200ms easeIn (per UI-SPEC animation table)
          - Width: `w-80` (320px)
          - Full height, fixed position on right side of the alert feed area
          - GlassCard style background with backdrop-blur
          - `border-l` on the left edge
      
      - Content inside panel (top to bottom):
        1. **Close button** (X icon) top-right
        2. **Alert title** (18px semibold) + severity badge
        3. **Divider**
        4. **Détails section**: 
           - "ID alerte: {alert.id}" (12px mono muted-foreground)
           - "Caméra: {camera.name}" with Camera icon (if camera-linked)
           - "Date/heure: {formatted timestamp}"
           - "Site: {site}" with MapPin icon
        5. **Divider**
        6. **Actions section**:
           - "Accuser réception" button (CheckCircle icon, outline variant) — disabled if already acknowledged
           - "Résoudre" button (CheckSquare icon, default variant) — disabled if already resolved
           - "Voir la caméra" button (Eye icon, ghost variant) — if camera-linked
        7. If alert has description: expandable description section
      
      - Scrollable if content exceeds height
    - Export `AlertSidePanel` as named export

    **bulk-action-bar.tsx**:
    - `"use client"` component
    - Interface `BulkActionBarProps`:
      - `selectedCount: number`
      - `onAcknowledgeSelected: () => void`
      - `onResolveSelected: () => void`
      - `processing?: boolean`
      - `className?: string`
    - Import `motion`, `AnimatePresence` from `"motion/react"`
    - Import `Button` from `@/components/ui/button`
    - Import `CheckSquare`, `CheckCircle`, `X` from `"lucide-react"`

    - Render inside `<AnimatePresence>`:
      - If `selectedCount === 0`, render nothing
      - If `selectedCount > 0`, render:
        - `motion.div` that slides up from bottom: `initial={{ y: 60, opacity: 0 }}` → `animate={{ y: 0, opacity: 1 }}` → `exit={{ y: 60, opacity: 0 }}`
        - Fixed at bottom of the alert area, centered
        - GlassCard-style background, `shadow-lg`, `rounded-lg`
        - Content: "{count} sélectionnée(s)" label, "Accuser réception" button, "Résoudre" button
        - If processing, show spinner on buttons and disable
    - Export `BulkActionBar` as named export
  </action>

  <verify>
    <automated>test -f apps/dashboard/components/alert-side-panel.tsx && test -f apps/dashboard/components/bulk-action-bar.tsx && grep -c -- 'AlertSidePanel' apps/dashboard/components/alert-side-panel.tsx | xargs test 1 -eq && grep -c -- 'BulkActionBar' apps/dashboard/components/bulk-action-bar.tsx | xargs test 1 -eq && grep -c -- 'initial={{ x: 320 }}' apps/dashboard/components/alert-side-panel.tsx | xargs test 1 -ge && pnpm --filter @repo/dashboard run check-types</automated>
  </verify>

  <acceptance_criteria>
    - alert-side-panel.tsx: slides from right (x 320→0), GlassCard-style background, fixed position
    - alert-side-panel.tsx: shows alert ID (mono), camera name, timestamp, site, severity badge
    - alert-side-panel.tsx: action buttons for acknowledge, resolve, view camera — disabled based on status
    - alert-side-panel.tsx: close button with X icon
    - bulk-action-bar.tsx: slides up from bottom when selectedCount > 0, disappears when 0
    - bulk-action-bar.tsx: shows count label + "Accuser réception" + "Résoudre" buttons
    - bulk-action-bar.tsx: processing state disables buttons with spinner
    - `pnpm --filter @repo/dashboard run check-types` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rewrite Alerts page with premium feed, side panel, bulk actions, real-time WS</name>

  <read_first>
    @apps/dashboard/app/(dashboard)/alertes/page.tsx — Current alerts page with DataTable, WebSocket connection
    @apps/dashboard/components/alert-feed.tsx — Created in Task 1
    @apps/dashboard/components/alert-side-panel.tsx — Created in Task 2
    @apps/dashboard/components/bulk-action-bar.tsx — Created in Task 2
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — Alerts page layout lines 582-610, states table lines 614-627, copywriting lines 297-339
  </read_first>

  <files>apps/dashboard/app/(dashboard)/alertes/page.tsx</files>

  <action>
    Rewrite the Alerts page with premium layout. Keep ALL existing data operations and the WebSocket real-time connection.

    **Rewrite page.tsx** with this structure:

    1. **Page header**: "Alertes" heading, subtitle "{count} alerte(s) active(s)" badge

    2. **Filter pills**: Horizontal pill group for severity: Toutes, Critiques, Haute, Moyenne, Basse. Selecting a severity filters the feed immediately with staggered exit/enter animation.

    3. **Two-column layout** (responsive):
       - Left: **AlertFeed** with all filtered alerts
       - Right: **AlertSidePanel** slides in when an alert is selected (on desktop, overlay panel on mobile)

    4. **BulkActionBar**: Appears at bottom when >=1 alert is selected (via checkbox mode or multi-select) — with acknowledge and resolve actions

    5. **Real-time WebSocket**: Keep the existing Socket.IO connection (socketRef.current). New alerts arriving via WebSocket should:
       - Prepending to the feed with slide-in animation + severity glow pulse (600ms)
       - Update the alert count in the header
       - If side panel is open for a specific alert, update its data

    6. **State management**:
       - `selectedAlert: string | null` — controls side panel
       - `selectedAlerts: Set<string>` — controls bulk action bar
       - `severityFilter: string` — controls feed filtering
       - Keep existing `fetchAlerts`, `acknowledgeAlert`, `resolveAlert` API integrations
       - Keep existing WebSocket event listeners (new_alert, alert_updated)

    7. **States** (per UI-SPEC lines 614-627):
       - Loading: 5 skeleton rows via AlertFeed loading prop. Filter pills disabled.
       - Loaded (with alerts): Premium feed with staggered mount
       - Loaded (no alerts): Empty state — "Aucune alerte active" centered display
       - Empty (filter yields 0): "Aucune alerte ne correspond aux filtres" with "Réinitialiser" link
       - Error: Full-page centered error card with retry
       - WebSocket disconnect: Show "Connexion perdue — reconnexion..." banner at top
       - Acknowledge/resolve: Optimistic UI update, confirm from server

    8. **Important**: Keep ALL existing functionality:
       - WebSocket connection (socketRef, io, getAccessToken, wsConnected state)
       - Alert CRUD operations
       - Toast notifications for success/failure
       - The existing acknowledgment and resolve logic

    The page should be restructured for the feed+panel layout while preserving all event handlers and data flows.
  </action>

  <verify>
    <automated>grep -c -- 'AlertFeed' apps/dashboard/app/(dashboard)/alertes/page.tsx | xargs test 1 -eq && grep -c -- 'AlertSidePanel' apps/dashboard/app/(dashboard)/alertes/page.tsx | xargs test 1 -eq && grep -c -- 'BulkActionBar' apps/dashboard/app/(dashboard)/alertes/page.tsx | xargs test 1 -eq && grep -c -- 'socketRef\|io(' apps/dashboard/app/(dashboard)/alertes/page.tsx | xargs test 1 -eq && grep -c -- 'fetchAlerts' apps/dashboard/app/(dashboard)/alertes/page.tsx | xargs test 1 -eq && pnpm --filter @repo/dashboard run check-types</automated>
  </verify>

  <acceptance_criteria>
    - Alerts page uses AlertFeed premium feed (not old DataTable)
    - Severity filter pills (Toutes, Critiques, Haute, Moyenne, Basse)
    - Side panel slides in from right when alert selected, slides out on close/deselect
    - Bulk action bar shows when >=1 alert selected with acknowledge/resolve
    - WebSocket connection preserved for real-time alert updates
    - All existing API operations (fetchAlerts, acknowledgeAlert, resolveAlert) remain functional
    - Loading state with skeleton rows, empty state, filter-empty state, error state
    - WebSocket disconnect banner visible
    - French copy per UI-SPEC (Alertes, Critiques, Haute, Ouvertes, etc.)
    - `pnpm --filter @repo/dashboard run check-types` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket connection | Existing socket.io-client connection — no new connection logic added |
| Alert acknowledge/resolve | Existing API calls — no new data mutations introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Spoofing | WebSocket event handlers | accept | Same socket.io connection as current implementation — event handlers reused, no new event types added |
| T-06-11 | Repudiation | Bulk acknowledge/resolve | mitigate | Optimistic UI with server confirmation — API calls use existing auth and audit trail |
</threat_model>

<verification>
1. `pnpm --filter @repo/dashboard run check-types` passes
2. `pnpm --filter @repo/dashboard run lint` passes
3. Manual: Alerts page renders with premium feed layout
4. Manual: Clicking alert row opens side panel with slide animation
5. Manual: Side panel shows alert details and action buttons
6. Manual: Bulk action bar appears when multiple alerts selected
7. Manual: Severity filters work with staggered animation
8. Manual: Empty state shows "Aucune alerte active" when no alerts
9. Manual: Real-time WebSocket alerts appear in feed (if server running)
10. Manual: All acknowledge/resolve operations work correctly
</verification>

<success_criteria>
- 4 new premium alert components created and type-checked: AlertRow, AlertFeed, AlertSidePanel, BulkActionBar
- AlertRow has 3px severity color border, inline actions, stagger animation
- AlertSidePanel slides from right with alert details and actions
- BulkActionBar slides up from bottom on selection
- Alerts page rewritten with premium feed+panel layout
- WebSocket real-time connectivity preserved
- All states: loading, empty, loaded, error, filter-empty, WS-disconnect
- TypeScript compilation passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/06-premium-experience/06-06-SUMMARY.md` when done
</output>