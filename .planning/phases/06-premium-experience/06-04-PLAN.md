---
phase: 06-premium-experience
plan: 04
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/incidents.tsx
  - apps/mobile/app/(tabs)/more.tsx
  - apps/mobile/components/quick-action-button.tsx
  - apps/mobile/lib/offline-storage.ts
  - .planning/phases/06-premium-experience/component-spec-mobile-vs-dashboard.md
autonomous: true
requirements:
  - UIX-04

must_haves:
  truths:
    - "Mobile app shows 4-tab navigation: Accueil, Caméras, Incidents, Plus"
    - "Accueil tab shows guard-first dashboard with greeting, check-in timer, quick-action tiles, and stats summary"
    - "Quick-action tiles are 64×64px with icon + label, primary row (Check In, Report Incident, Control Door) and secondary row (View Alerts, Find Camera)"
    - "Incidents tab is a placeholder screen with heading and offline capture info"
    - "Plus tab shows overflow menu for Settings, Chat IA, Sites, Audit, Licences"
    - "Check-in works offline using expo-secure-store for state and AsyncStorage for queued sync"
    - "Camera list cached in AsyncStorage for offline viewing"
    - "Incident capture stores photo to expo-file-system and notes to AsyncStorage, syncs on reconnect"
    - "Mobile imports colors and typography from @repo/design instead of local theme.ts"
  artifacts:
    - path: "apps/mobile/app/(tabs)/_layout.tsx"
      provides: "4-tab navigator with guard-first tab bar styling (64px height, large icons)"
      contains: "Tabs"
    - path: "apps/mobile/app/(tabs)/index.tsx"
      provides: "Guard-first dashboard with quick-action grid, check-in timer, site stats"
      contains: "QuickActionButton"
    - path: "apps/mobile/components/quick-action-button.tsx"
      provides: "64×64px touch target with icon and label"
      exports: ["QuickActionButton"]
    - path: "apps/mobile/lib/offline-storage.ts"
      provides: "Offline queue utilities for check-in, camera cache, incident queue"
      exports: ["OfflineStorage"]
  key_links:
    - from: "mobile/app/(tabs)/_layout.tsx"
      to: "@repo/design"
      via: "import { colors, typography } from '@repo/design'"
    - from: "mobile/lib/offline-storage.ts"
      to: "expo-secure-store"
      via: "check-in state stored via SecureStore"
    - from: "mobile/lib/offline-storage.ts"
      to: "AsyncStorage"
      via: "camera list and incident queues via AsyncStorage"

---

<objective>
Redesign the Expo mobile app with guard-first 4-tab navigation, quick-action home screen with check-in/incident/door control, offline-capable queue infrastructure, and a consolidated overflow menu.

Purpose: Mobile operators (security guards) need large touch targets, simplified navigation, and offline resilience — this plan delivers the Phase 6 mobile transformation per D-10, D-11, D-12.

Output: Rewritten tab layout (4 tabs), new guard-first home screen, incident/more tab placeholders, QuickActionButton component, offline storage module, expo-file-system dependency installed.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-premium-experience/06-CONTEXT.md
@.planning/phases/06-premium-experience/06-RESEARCH.md
@.planning/phases/06-premium-experience/06-UI-SPEC.md
@apps/mobile/app/(tabs)/_layout.tsx
@apps/mobile/app/(tabs)/index.tsx
@apps/mobile/lib/theme.ts
@apps/mobile/lib/api.ts
@packages/design/src/colors.ts
@packages/design/src/typography.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install expo-file-system + rewrite tab layout to 4-tab navigation</name>

  <read_first>
    @apps/mobile/package.json — Current dependencies
    @apps/mobile/app/(tabs)/_layout.tsx — Current 6-tab layout
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — 4-tab design lines 658-702, tab bar visual contract lines 670-677
    @.planning/phases/06-premium-experience/06-RESEARCH.md — Mobile tab layout code example lines 669-733
    @packages/design/src/colors.ts — Shared color tokens (created in 06-02)
    @packages/design/src/typography.ts — Shared typography tokens
  </read_first>

  <files>
    apps/mobile/package.json
    apps/mobile/app/(tabs)/_layout.tsx
  </files>

  <action>
    Two changes:

    **apps/mobile/package.json**: Add `"expo-file-system": "~18.0.14"` to `dependencies`. Insert alphabetically, after `"expo-constants"`. Then run `pnpm install --filter @repo/mobile`.

    **apps/mobile/app/(tabs)/_layout.tsx**: Rewrite from 6-tab to 4-tab navigation:

    - Import `Tabs` from `"expo-router"`
    - Import icons from `"lucide-react-native"`: `LayoutDashboard`, `Camera`, `AlertTriangle`, `MoreHorizontal` (remove unused: Bell, MessageSquareText, MapPin, Settings)
    - Import `{ colors, typography }` from `"@repo/design"` (replaces `@/lib/theme` import)
    - Keep `useAuth` and auth guard (loading state + redirect if not authenticated)
    - Remove the `TabIcon` helper component — use inline `tabBarIcon` rendering
    - Rewrite `TabLayout` component:

    Screen options:
    - `tabBarStyle`: `backgroundColor: colors.dark.surface`, `borderTopColor: colors.dark.border`, `borderTopWidth: 1`, `height: 64`, `paddingBottom: 10`, `paddingTop: 10`
    - `tabBarActiveTintColor: colors.shared.primary`
    - `tabBarInactiveTintColor: colors.dark.textMuted`
    - `tabBarLabelStyle: { fontSize: 10, fontWeight: "600" }`
    - `headerShown: false` for all tabs

    Tab screens (order matters):
    1. `name="index"` → title: `"Accueil"`, icon: `LayoutDashboard`
    2. `name="cameras"` → title: `"Caméras"`, icon: `Camera`
    3. `name="incidents"` → title: `"Incidents"`, icon: `AlertTriangle`
    4. `name="more"` → title: `"Plus"`, icon: `MoreHorizontal`

    Remove all references to `alerts`, `chat`, `sites`, `settings` tab screens.
    Keep the `isLoading` and `!isAuthenticated` guard logic exactly as-is.
  </action>

  <verify>
    <automated>grep -c -- '@repo/design' apps/mobile/app/(tabs)/_layout.tsx | xargs test 1 -eq && grep -c -- '@/lib/theme' apps/mobile/app/(tabs)/_layout.tsx | xargs test 0 -eq && grep -c -- 'Alerts' apps/mobile/app/(tabs)/_layout.tsx | xargs test 0 -eq && grep -c -- 'incidents' apps/mobile/app/(tabs)/_layout.tsx | xargs test 1 -eq && grep -c -- 'more' apps/mobile/app/(tabs)/_layout.tsx | xargs test 1 -eq && grep -c -- 'expo-file-system' apps/mobile/package.json | xargs test 1 -eq</automated>
  </verify>

  <acceptance_criteria>
    - Tab layout has exactly 4 Tabs.Screen entries: index (Accueil), cameras (Caméras), incidents (Incidents), more (Plus)
    - All 5 old tab screens removed (alerts, chat, sites, settings)
    - Tab bar: height=64, bg=colors.dark.surface, active tint=colors.shared.primary
    - Icons from lucide-react-native: LayoutDashboard, Camera, AlertTriangle, MoreHorizontal
    - Colors imported from @repo/design (not @/lib/theme)
    - expo-file-system installed as dependency
    - Auth guard (loading + !authenticated redirect) preserved exactly
    - `pnpm --filter @repo/mobile run check-types` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create guard-first home screen with quick-action shortcuts</name>

  <read_first>
    @apps/mobile/app/(tabs)/index.tsx — Current home screen with stats grid + recent alerts
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — Mobile dashboard visual contract lines 678-711, quick-action specs lines 713-720, copywriting lines 341-363
    @.planning/phases/06-premium-experience/06-RESEARCH.md — QuickActionButton pattern (64×64px)
    @packages/design/src/colors.ts — Shared colors
    @packages/design/src/typography.ts — Shared typography
  </read_first>

  <files>
    apps/mobile/components/quick-action-button.tsx
    apps/mobile/app/(tabs)/index.tsx
  </files>

  <action>
    Create one component and rewrite one screen:

    **quick-action-button.tsx** (new at `apps/mobile/components/quick-action-button.tsx`):
    - Import `Pressable`, `Text`, `View`, `StyleSheet` from `"react-native"`
    - Import colors and typography from `@repo/design`
    - Interface `QuickActionButtonProps`:
      - `icon: React.ReactNode` (Lucide icon component rendered by parent)
      - `label: string`
      - `onPress: () => void`
      - `color?: string` (optional accent color, defaults to `colors.shared.primary`)
    - Render:
      - `Pressable` with `style={({ pressed }) => [...]}` — scale 0.95 on press
      - Inside: `View` with size 64×64, `backgroundColor: colors.dark.elevated`, `borderRadius: 14`, `borderWidth: 1`, `borderColor: colors.dark.border`, centered icon
      - Below: `Text` with 10px semibold centered label text
    - Export `QuickActionButton` as named export
    - Use `React.memo` for performance

    **index.tsx** (rewrite at `apps/mobile/app/(tabs)/index.tsx`):
    Rewrite the home screen per UI-SPEC visual contract:
    - Import `colors`, `typography` from `"@repo/design"` (replaces `@/lib/theme`)
    - Import `QuickActionButton` from `@/components/quick-action-button`
    - Keep `fetchDashboardStats` from `@/lib/api`, keep loading/error states
    - Layout (top to bottom):
      1. **Header bar**: "Bonjour, {firstName}" greeting, role subtitle, shield icon. Same pattern as current but with `colors.*` from `@repo/design`.
      2. **Check-in Card**: If no active check-in, show "Pointer" button (large, full-width, primary color). If check-in active, show timer display with "00:32:15" mono text and "Pointage en cours" label. Timer logic: store start time in SecureStore via offline-storage module, calculate elapsed on mount, update every second.
      3. **Quick-action grid**: 
         - Primary row (3 items, horizontal): Report Incident (camera icon), View Alerts (bell icon), Control Door (door icon)
         - Secondary row (2 items, horizontal): Find Camera (camera+search icon), View Alerts (alert icon)
         - Each as `QuickActionButton` — onPress handlers show Alert.alert placeholder for now (full implementation in Phase 8)
      4. **Site Stats Summary**: Glass-like card showing camera count (online/total) and active alerts. Data from `DashboardStats` if loaded.
      5. **Loading state**: Show skeleton placeholders for each section
      6. **Error state**: Show error banner with retry (same pattern as current)
    - Keep `ScrollView` with `RefreshControl`
    - Use `StyleSheet.create()` at bottom (same pattern as current)
    - Export default `HomeScreen`
  </action>

  <verify>
    <automated>test -f apps/mobile/components/quick-action-button.tsx && test -f apps/mobile/app/(tabs)/index.tsx && grep -c -- 'QuickActionButton' apps/mobile/app/(tabs)/index.tsx | xargs test 1 -eq && grep -c -- '@repo/design' apps/mobile/app/(tabs)/index.tsx | xargs test 1 -eq && grep -c -- '64' apps/mobile/components/quick-action-button.tsx | xargs test 1 -ge</automated>
  </verify>

  <acceptance_criteria>
    - quick-action-button.tsx: exports `QuickActionButton` with 64×64px Pressable, icon + label, scale 0.95 on press
    - index.tsx: greeting header with firstName from auth context
    - index.tsx: check-in card with start/end timer display
    - index.tsx: quick-action grid with 5 buttons (primary row: Check In, Report Incident, Control Door; secondary: View Alerts, Find Camera)
    - index.tsx: site stats summary card
    - index.tsx: imports from `@repo/design` for colors and typography
    - index.tsx: preserved data fetching from `@/lib/api`, loading and error states
    - `StyleSheet.create()` pattern maintained
    - `pnpm --filter @repo/mobile run check-types` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create offline storage module + incident/more tab placeholders + component spec doc</name>

  <read_first>
    @.planning/phases/06-premium-experience/06-RESEARCH.md — Offline mode lines 406-409 (Pitfall 4: SecureStore 5KB limit), mobile directory structure lines 214-233
    @.planning/phases/06-premium-experience/06-UI-SPEC.md — Offline mode specs lines 721-726, Incident and Plus tab descriptions lines 663-668
    @apps/mobile/lib/api.ts — Current API client structure
  </read_first>

  <files>
    apps/mobile/lib/offline-storage.ts
    apps/mobile/app/(tabs)/incidents.tsx
    apps/mobile/app/(tabs)/more.tsx
    .planning/phases/06-premium-experience/component-spec-mobile-vs-dashboard.md
  </files>

  <action>
    Create four files:

    **offline-storage.ts** (new at `apps/mobile/lib/offline-storage.ts`):
    A utility module for offline queuing:
    - Import `* as SecureStore` from `"expo-secure-store"`
    - Import `AsyncStorage` from `"@react-native-async-storage/async-storage"` (check if already installed; if not, add to mobile package.json dependencies as `"@react-native-async-storage/async-storage": "^2.1.0"`)
    - Export object `OfflineStorage` with methods:
      - `saveCheckInTime(timestamp: number): Promise<void>` — stores via SecureStore key `"check_in_time"`
      - `getCheckInTime(): Promise<number | null>` — reads via SecureStore
      - `clearCheckInTime(): Promise<void>` — deletes via SecureStore
      - `cacheCameraList(cameras: any[]): Promise<void>` — JSON.stringify via AsyncStorage key `"cached_cameras"`
      - `getCachedCameraList(): Promise<any[] | null>` — reads via AsyncStorage
      - `getLastRefreshTime(): Promise<string | null>` — reads AsyncStorage key `"last_refresh"`
      - `setLastRefreshTime(): Promise<void>` — writes current ISO string
      - `queueIncident(incident: { photoUri?: string; notes?: string; timestamp: number }): Promise<void>` — appends to AsyncStorage array key `"incident_queue"`
      - `getPendingIncidents()` — reads the array
      - `clearPendingIncidents()` — clears the array
    - Each method wrapped in try/catch with console.warn fallback
    - Export types: `PendingIncident`

    **incidents.tsx** (new at `apps/mobile/app/(tabs)/incidents.tsx`):
    Placeholder screen per UI-SPEC:
    - Imports View, Text, StyleSheet from react-native
    - Imports colors, typography from @repo/design
    - Shows: AlertTriangle icon, "Incidents" heading, body text: "Les incidents capturés hors ligne apparaîtront ici. Fonctionnalité complète prévue dans une mise à jour ultérieure."
    - Uses exact same StyleSheet.create pattern

    **more.tsx** (new at `apps/mobile/app/(tabs)/more.tsx`):
    Overflow menu screen per UI-SPEC:
    - Imports View, Text, StyleSheet, ScrollView, Pressable from react-native
    - Imports colors, typography from @repo/design
    - Shows list of menu items: Chat IA, Sites, Paramètres, Audit, Licences
    - Each as a Pressable row with icon + label + chevron, navigates to respective route via `router.push()`
    - Uses same StyleSheet pattern

    **component-spec-mobile-vs-dashboard.md** (new doc):
    Create a lightweight component parity spec per D-04:
    - Document that Dashboard uses CSS vars for tokens, Mobile uses @repo/design JS constants
    - List the 5 shared tokens and their mobile-vs-dashboard equivalents:
      - Primary color: Dashboard uses `hsl(var(--shadcn-primary))` / Mobile uses `colors.shared.primary`
      - Background: Dashboard uses `bg-background` / Mobile uses `colors.dark.bg`
      - Typography display: Dashboard uses `text-2xl font-semibold` / Mobile uses `typography.display`
      - Typography heading: Dashboard uses `text-xl font-semibold` / Mobile uses `typography.heading`
      - Typography body: Dashboard uses `text-sm` / Mobile uses `typography.body`
    - Document that the 4-tab design must mirror the dashboard's sidebar navigation structure
  </action>

  <verify>
    <automated>test -f apps/mobile/lib/offline-storage.ts && test -f apps/mobile/app/(tabs)/incidents.tsx && test -f apps/mobile/app/(tabs)/more.tsx && grep -c -- 'SecureStore' apps/mobile/lib/offline-storage.ts | xargs test 1 -ge && grep -c -- 'AsyncStorage' apps/mobile/lib/offline-storage.ts | xargs test 1 -ge && grep -c -- 'OfflineStorage' apps/mobile/lib/offline-storage.ts | xargs test 1 -eq</automated>
  </verify>

  <acceptance_criteria>
    - offline-storage.ts: exports `OfflineStorage` object with check-in (SecureStore), camera cache (AsyncStorage), incident queue (AsyncStorage) methods
    - offline-storage.ts: `saveCheckInTime`/`getCheckInTime`/`clearCheckInTime` use SecureStore
    - offline-storage.ts: `cacheCameraList`/`getCachedCameraList` use AsyncStorage
    - offline-storage.ts: `queueIncident`/`getPendingIncidents` use AsyncStorage with array append
    - incidents.tsx: displays heading + descriptive text placeholder
    - more.tsx: list of menu items (Chat IA, Sites, Paramètres, Audit, Licences)
    - component-spec-mobile-vs-dashboard.md: documents token mapping between CSS vars and JS constants
    - `pnpm --filter @repo/mobile run check-types` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expo-secure-store | Secure local key-value storage — already used for auth tokens |
| AsyncStorage | Non-encrypted local storage — used for cached lists only, no PII |
| expo-file-system | Local file storage for incident photos — no network transmission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | offline-storage (AsyncStorage) | accept | Camera list and cached alerts contain no PII — only camera names, IDs, alert titles |
| T-06-07 | Information Disclosure | SecureStore (check-in) | accept | Check-in time is non-sensitive; SecureStore already protects auth tokens |
| T-06-SC | Tampering | npm/expo installs | mitigate | RESEARCH.md Package Legitimacy Audit confirms [OK] for expo-file-system (Expo SDK) |
</threat_model>

<verification>
1. `pnpm --filter @repo/mobile run check-types` passes
2. `pnpm --filter @repo/mobile run lint` passes
3. Manual: Mobile app shows 4-tab navigation
4. Manual: Home screen shows greeting, check-in button, quick-action tiles, site stats
5. Manual: Incidents tab shows placeholder
6. Manual: More tab shows menu items
</verification>

<success_criteria>
- Mobile tab navigation reduced from 6 to 4 with correct guard-first labels (Accueil, Caméras, Incidents, Plus)
- Home screen with guard-first quick-action grid (64×64px buttons), check-in card, site stats summary
- Offline storage module with SecureStore check-in + AsyncStorage camera cache + incident queue
- Incident tab placeholder and More tab overflow menu created
- Colors/typography imported from @repo/design (not local theme.ts)
- Component parity spec document created
- TypeScript compilation passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/06-premium-experience/06-04-SUMMARY.md` when done
</output>
