---
phase: 04-bastion-enterprise
plan: 05
type: execute
wave: 3
depends_on:
  - 04-02-PLAN
  - 04-03-PLAN
files_modified:
  - apps/dashboard/app/(dashboard)/analytique/enhanced/page.tsx
  - apps/dashboard/app/(dashboard)/conformite/hapdp/page.tsx
  - apps/dashboard/app/(dashboard)/conformite/hapdp/portail/page.tsx
  - apps/dashboard/app/(dashboard)/conformite/hapdp/registre/page.tsx
  - apps/dashboard/components/analytics-kpi-grid.tsx
  - apps/dashboard/components/trend-chart-card.tsx
  - apps/dashboard/components/report-schedule-config.tsx
  - apps/dashboard/components/report-preview.tsx
  - apps/dashboard/components/hapdp-wizard.tsx
  - apps/dashboard/components/hapdp-declaration-form.tsx
  - apps/dashboard/components/processing-register-table.tsx
  - apps/dashboard/components/consent-signage-generator.tsx
  - apps/dashboard/components/subject-access-portal.tsx
  - apps/dashboard/components/access-traceability-log.tsx
autonomous: true
requirements:
  - BAS-20
  - BAS-21
  - BAS-22
  - BAS-23
  - BAS-24
  - BAS-30
  - BAS-31
  - BAS-32
  - BAS-34
  - BAS-35
must_haves:
  truths:
    - "Analytics dashboard shows 5 KPI cards with animated values"
    - "Trend charts display 7d/30d line charts with granularity toggle"
    - "Advanced search filters by date range, site, event type, person name"
    - "CSV and PDF export buttons trigger downloads"
    - "Report schedule configuration modal with weekly/monthly options"
    - "HAPDP wizard has 6 steps with progress indicator and validation"
    - "HAPDP declaration form auto-fills from org profile"
    - "Consent signage generator creates PDF for any camera"
    - "Processing register table shows entries with search and export"
    - "Subject access portal has 3-step OTP verification flow"
    - "Access traceability log shows who viewed what data"
    - "All components handle loading, empty, error states per UI-SPEC"
  artifacts:
    - path: "apps/dashboard/app/(dashboard)/analytique/enhanced/page.tsx"
      provides: "Enhanced analytics dashboard with KPI grid, trends, export"
    - path: "apps/dashboard/app/(dashboard)/conformite/hapdp/page.tsx"
      provides: "HAPDP compliance wizard container (6 steps)"
    - path: "apps/dashboard/app/(dashboard)/conformite/hapdp/portail/page.tsx"
      provides: "Public subject access portal (no sidebar)"
    - path: "apps/dashboard/app/(dashboard)/conformite/hapdp/registre/page.tsx"
      provides: "Processing register list with search and export"
    - path: "apps/dashboard/components/"
      provides: "14 new components for analytics and HAPDP"
      contains: "analytics-kpi-grid, trend-chart-card, hapdp-wizard, subject-access-portal, etc."
  key_links:
    - from: "analytique/enhanced/page.tsx"
      to: "analytics-kpi-grid.tsx, trend-chart-card.tsx"
      via: "Recharts (LineChart) + fetchBastionKpis()/fetchBastionTrends()"
    - from: "conformite/hapdp/page.tsx"
      to: "hapdp-wizard.tsx with 6 steps"
      via: "localStorage persistence, step state management"
    - from: "conformite/hapdp/portail/page.tsx"
      to: "subject-access-portal.tsx with 3-step OTP flow"
      via: "public API endpoints (requestOtp, verifyOtp, submitRequest)"
---

<objective>
Build all analytics and HAPDP compliance dashboard pages and components — enhanced analytics dashboard with KPI grid, trend charts, and export; HAPDP compliance 6-step wizard; subject access public portal; processing register; and access traceability log.

Purpose: BAS-20 to BAS-24 and BAS-30 to BAS-35 require rich dashboard UIs. These 4 pages (analytics, HAPDP wizard, subject access portal, processing register) and 10 components make the BASTION Enterprise features accessible to operators and compliance managers.
Output: Enhanced analytics page, HAPDP wizard page, subject access portal page, processing register page, 10 new components.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-bastion-enterprise/04-CONTEXT.md
@.planning/phases/04-bastion-enterprise/04-UI-SPEC.md
@.planning/phases/04-bastion-enterprise/04-PATTERNS.md
@.planning/phases/03-bastion-ai-access-control/03-04-SUMMARY.md
@.planning/phases/03-bastion-ai-access-control/03-05-SUMMARY.md

<interfaces>
Dashboard page pattern (PATTERNS.md lines 1400-1492):
```typescript
"use client";
import { useState, useEffect, useCallback } from "react";
import { PageHeader } from "@/components/page-header";
import { PageTransition } from "@/components/page-transition";
import { GlassCard } from "@/components/glass-card";
import { Skeleton } from "@/components/ui/skeleton";
// Data: loading → error → empty → render
```

Existing API client functions (Plan 02 api.ts additions):
- fetchBastionKpis(), fetchBastionTrends(), advancedSearch(), exportData()
- generateReport(), getReportsList()

Existing public API functions (Plan 03 api.ts additions):
- requestSubjectAccessOtp(), verifySubjectAccessOtp(), submitSubjectAccessRequest()

From existing components/:
- donut-chart.tsx — Recharts PieChart pattern
- cross-site-comparison.tsx — Recharts chart pattern
- sparkline.tsx — mini trend preview component
- stats-card.tsx — MetricHero with icon, label, value, trend
- multi-site-dashboard.tsx — KPI grid with skeleton loading
- glass-card.tsx — GlassCard wrapper
- page-header.tsx — PageHeader with title, description
- page-transition.tsx — motion.div animation wrapper

From UI-SPEC:
- 5 component states: loading → skeleton, error → icon+message+retry, empty → icon+heading+body+CTA, no results → "Aucun résultat", data → normal render
- French text for all labels, errors, empty states
- KPI colors: normal (<80%) = primary, warning (80-90%) = warning, critical (>90%) = destructive
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enhanced analytics dashboard page with KPI grid, trend charts, and export</name>
  <files>
    apps/dashboard/app/(dashboard)/analytique/enhanced/page.tsx (NEW)
    apps/dashboard/components/analytics-kpi-grid.tsx (NEW)
    apps/dashboard/components/trend-chart-card.tsx (NEW)
    apps/dashboard/components/report-schedule-config.tsx (NEW)
    apps/dashboard/components/report-preview.tsx (NEW)
  </files>
  <read_first>
    apps/dashboard/app/(dashboard)/analytique/page.tsx
    apps/dashboard/components/multi-site-dashboard.tsx
    apps/dashboard/components/donut-chart.tsx
    apps/dashboard/components/cross-site-comparison.tsx
    apps/dashboard/components/stats-card.tsx
    apps/dashboard/components/page-header.tsx
  </read_first>
  <action>
    Create the enhanced analytics dashboard page and its components per UI-SPEC §Interaction Pattern 3 (Analytics Dashboard layout).

    1. Create `components/analytics-kpi-grid.tsx`:
       - Props: { kpis: BastionKpisDto | null; loading?: boolean; error?: string | null; onRetry?: () => void }
       - Displays 5 KPI cards in a responsive 5-column grid (collapses to 2-col on mobile):
         - Incidents aujourd'hui — AlertTriangle icon, trend direction indicator
         - Alertes actives — Bell icon
         - Caméras en ligne — Camera icon, show X/Y format
         - Stockage utilisé — HardDrive icon, format bytes to human-readable (GB/TB)
         - Entrées aujourd'hui — LogIn icon
       - Loading state: 5 skeleton cards via Array.from({length:5}).map → <Skeleton className="h-28 rounded-xl" />
       - Error state: AlertTriangle icon + error message + "Réessayer" button
       - Data state: uses <StatsCard> or inline implementation with motion animation
       - Each card should use `motion.div` with stagger animation (containerVariants from existing pattern)
       - Colored status for storage: green (<80%), amber (80-90%), red (>90%) per UI-SPEC

    2. Create `components/trend-chart-card.tsx`:
       - Props: { metric: string; data: AnalyticsTrendPoint[]; granularity: 7 | 30; onGranularityChange: (days: 7 | 30) => void }
       - Uses Recharts LineChart with:
         - X-axis: time buckets (formatted as dates)
         - Y-axis: count values
         - Cyan accent line color (--shadcn-primary)
         - Tooltip with date + value
         - Responsive container
       - Loading state: skeleton chart (rounded rectangle matching chart dimensions)
       - Empty state: "Aucune donnée" centered message
       - Error state: retry option
       - Granularity toggle buttons: "7 jours" | "30 jours" (active state with cyan accent)
       - Use existing `motion` (framer-motion) for chart entrance animation

    3. Create `components/report-schedule-config.tsx`:
       - Props: { schedule: ReportSchedule | null; onSave: (config: ReportSchedule) => Promise<void> }
       - Form fields:
         - Type de rapport: select (Hebdomadaire / Mensuel)
         - Format: select (PDF)
         - Destinataires: email input (comma-separated) with validation
         - Activer la planification: Switch toggle
       - Loading state: skeleton form
       - Save: "Enregistrer" button → calls onSave → toast "Planification des rapports enregistrée" per UI-SPEC
       - All labels in French per UI-SPEC copywriting

    4. Create `components/report-preview.tsx`:
       - Props: { reportId: string; onDownload: () => void; onClose: () => void }
       - Shows preview: loading → spinner, ready → download button
       - "Télécharger le rapport" CTA button
       - PDF icon and metadata (type, date, size)

    5. Create `apps/dashboard/app/(dashboard)/analytique/enhanced/page.tsx`:
       - "use client" page component
       - Layout per UI-SPEC §Interaction Pattern 3:
         ```
         PageHeader: "Analytique" with description
         Filter bar: Site select, Période select, Type d'événement select
         KPI Grid (analytics-kpi-grid)
         Tab: Tendances 7j | Tendances 30j | Export
           → trend-chart-card (on Tendances tab)
           → Export buttons CSV/PDF + report-schedule-config (on Export tab)
         ```
       - Data fetching: useEffect → fetchBastionKpis() + fetchBastionTrends()
       - Site filter: dropdown populated from existing sites API
       - Date range: preset buttons "Aujourd'hui", "7 jours", "30 jours", "Personnalisé"
       - "Exporter CSV" button → calls exportData("csv") → blob download
       - "Exporter PDF" button → calls exportData("pdf") → blob download
       - "Planifier rapport" button → opens report-schedule-config modal/dialog
       - Tab switch preserves filter state (useState)
       - All 5 states: loading → error → empty → filtered-empty → data
       - French text for all labels and messages per UI-SPEC

    Use existing patterns: 'use client', useState/useEffect/useCallback, fetchWithAuth, skeleton loading, error + retry, empty state with CTA.
  </action>
  <acceptance_criteria>
    - analytique/enhanced/page.tsx exists with full layout per UI-SPEC §Interaction Pattern 3
    - analytics-kpi-grid.tsx renders 5 KPI cards with motion animation
    - trend-chart-card.tsx renders Recharts LineChart with 7d/30d toggle
    - report-schedule-config.tsx renders form with type, format, recipients, toggle
    - All components handle loading (skeleton), error (retry), empty (CTA) states
    - French text used for all labels per UI-SPEC
    - Filter bar has site, period, event type dropdowns
    - CSV/PDF export triggers download via blob
    - `pnpm build` passes (or `npx next build` from dashboard)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/dashboard && npx next build 2>&1 | grep -E "Error|✓|✗|Failed" | head -20</automated>
  </verify>
  <done>
    Enhanced analytics dashboard page created with KPI grid, trend charts, search, and export. All states handled.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create HAPDP compliance wizard, declaration form, and consent signage generator</name>
  <files>
    apps/dashboard/app/(dashboard)/conformite/hapdp/page.tsx (NEW)
    apps/dashboard/components/hapdp-wizard.tsx (NEW)
    apps/dashboard/components/hapdp-declaration-form.tsx (NEW)
    apps/dashboard/components/consent-signage-generator.tsx (NEW)
  </files>
  <read_first>
    apps/dashboard/components/ui/tabs.tsx
    apps/dashboard/components/glass-card.tsx
    apps/dashboard/components/face-upload-dropzone.tsx
  </read_first>
  <action>
    Create the HAPDP compliance wizard with 6-step flow per UI-SPEC §Interaction Pattern 1 and D-04.

    1. Create `components/hapdp-wizard.tsx`:
       - The most complex new interaction per RESEARCH.md risk assessment
       - Props: { onComplete: () => void; onClose: () => void }
       - 6-step wizard state management using React Context or useState:
         - Step 1: "Informations client" — auto-fill from org profile
         - Step 2: "Traitements déclarés" — select from system activity types
         - Step 3: "Signalétique consentement" — generate camera signage PDF
         - Step 4: "Pseudonymisation" — toggle on/off + preview toggle effect
         - Step 5: "Portail accès sujet" — configure verification method
         - Step 6: "Récapitulatif + Export" — review all + generate declaration PDF
       - Fixed sidebar showing step indicators (number, name, status):
         - Not started: ○ circle with step number
         - Active: ● filled circle with cyan accent + pulsing glow
         - Complete: ✓ checkmark with success green
         - Error: ⚠ warning with red + inline message
         - Skipped: - dashed circle (for optional steps like consent signage)
       - "Suivant" / "Précédent" navigation buttons (French labels)
       - Each step validates before advancing (inline field validation)
       - Final step: "Tout est conforme — Générer la déclaration" primary CTA
       - Persist progress in localStorage (auto-save after each step)
       - On completion: success animation (checkmark + details), PDF download trigger
       - Loading states: skeleton for data-dependent steps
       - Error states: step validation errors shown inline

    2. Create `components/hapdp-declaration-form.tsx`:
       - Props: { clientInfo: any; siteInfo: any; onGenerate: (data: any) => Promise<void> }
       - Auto-filled form with fields:
         - Organisation name (read-only, from org profile)
         - Address (pre-filled, editable)
         - SIRET number (editable)
         - Representative name (editable)
         - Processing types list (checkboxes from system activity categories)
         - Declaration date (auto-filled today, editable)
       - Loading state: skeleton form fields
       - "Générer la déclaration HAPDP" CTA → calls generateHapdpDeclaration() API
       - On success: toast "Déclaration HAPDP générée — Téléchargement en cours"

    3. Create `components/consent-signage-generator.tsx`:
       - Props: { cameras: { id: string; name: string; siteName: string }[] }
       - Camera selector (Select component)
       - Site name display (auto-filled from camera selection)
       - "Générer la signalétique" CTA button
       - Loading state: spinner while generating
       - Success: PDF preview + "Télécharger" button
       - Per UI-SPEC: toast "Signalétique générée — Prête à imprimer"
       - Empty state: "Aucune caméra trouvée"

    4. Create `apps/dashboard/app/(dashboard)/conformite/hapdp/page.tsx`:
       - "use client" page component
       - Layout: PageHeader "Conformité HAPDP" with description
       - Shows hapdp-wizard component as main content
       - If HAPDP already configured: show status summary
       - Empty state: "Module HAPDP non configuré" → "Démarrer le wizard" CTA per UI-SPEC
       - Error state: retry on data load failure

    All text in French per UI-SPEC copywriting contract. Follow existing dashboard patterns.
  </action>
  <acceptance_criteria>
    - hapdp-wizard.tsx implements 6-step flow with sidebar progress indicator per UI-SPEC §Interaction Pattern 1
    - Each step validates before advancing; localStorage persistence after each step
    - hapdp-declaration-form.tsx auto-fills org info, has processing type checkboxes
    - consent-signage-generator.tsx has camera selector and PDF generation
    - conformite/hapdp/page.tsx renders wizard with empty state before configuration
    - All labels and buttons in French per UI-SPEC copywriting table
    - Final step: "Tout est conforme — Générer la déclaration" CTA triggers PDF download
    - `pnpm build` passes (or `npx next build` from dashboard)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/dashboard && npx next build 2>&1 | grep -E "Error|✓|✗|Failed" | head -20</automated>
  </verify>
  <done>
    HAPDP wizard created with 6-step flow, declaration form, consent signage generator. All states and validation handled.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create processing register, subject access portal, and access traceability components</name>
  <files>
    apps/dashboard/app/(dashboard)/conformite/hapdp/portail/page.tsx (NEW)
    apps/dashboard/app/(dashboard)/conformite/hapdp/registre/page.tsx (NEW)
    apps/dashboard/components/processing-register-table.tsx (NEW)
    apps/dashboard/components/subject-access-portal.tsx (NEW)
    apps/dashboard/components/access-traceability-log.tsx (NEW)
  </files>
  <read_first>
    apps/dashboard/components/access-event-timeline.tsx
    apps/dashboard/components/ui/table.tsx
    apps/dashboard/components/ui/dialog.tsx
    apps/dashboard/components/ui/input.tsx
  </read_first>
  <action>
    Create the processing register, public subject access portal, and access traceability log.

    1. Create `components/processing-register-table.tsx`:
       - Props: { entries: ProcessingRecord[]; loading?: boolean; error?: string | null; onRetry?: () => void; filters: any; onFilterChange: (f: any) => void; onExport: (format: "csv" | "pdf") => Promise<void> }
       - Searchable table with columns: Type d'événement, Entité, Action, Effectué par, Date
       - Filter bar: date range, event type, entity type
       - Export buttons: "Exporter en CSV" / "Exporter en PDF"
       - Loading state: skeleton rows (5 rows)
       - Empty state: "Aucune activité enregistrée" with body text per UI-SPEC
       - Error state: retry
       - Uses shadcn/ui Table component
       - Pagination: page navigation at bottom

    2. Create `components/subject-access-portal.tsx`:
       - Public-facing component (no auth required, minimal styling)
       - Props: none (self-contained with internal state)
       - 3-step flow per UI-SPEC §Interaction Pattern 2:
         - Step 1: "Vérification d'identité"
           - Email input field with validation
           - "Vérifier mon identité" CTA → calls requestSubjectAccessOtp()
           - Success: "Code envoyé à {email}" message
           - Error: inline error message
         - Step 2: "Consultation des données"
           - Code OTP input (6 digits, individual input boxes)
           - "Vérifier le code" CTA → calls verifySubjectAccessOtp()
           - On success: display personal data cards (name, role, sites, related records)
           - "Signaler une erreur" and "Demander la suppression" CTAs
         - Step 3: "Action"
           - Rectification: textarea form → "Envoyer la demande" CTA
           - Deletion: confirmation dialog "Taper SUPPRIMER pour confirmer" → submit request
           - Success: confirmation message with reference number
       - Loading states: spinner during API calls
       - Error states: "Code incorrect" → "Renvoyer le code" link
       - Empty state: "Aucune donnée trouvée" if email has no records
       - All French text per UI-SPEC copywriting

    3. Create `apps/dashboard/app/(dashboard)/conformite/hapdp/portail/page.tsx`:
       - Public page — NO sidebar, minimal layout
       - Renders subject-access-portal component
       - Full-page centered layout with subtle branding
       - PageHeader: "Portail d'accès aux données personnelles"
       - No auth requirement

    4. Create `components/access-traceability-log.tsx`:
       - Props: { entries: AuditEntry[]; loading?: boolean; error?: string | null; onRetry?: () => void; filters: { userId?: string; dateFrom?: string; dateTo?: string; entityType?: string }; onFilterChange: (f: any) => void }
       - Expandable table rows showing: Qui (user name/email), Quoi (entity type), Quand (date), Détails (expandable)
       - Filter bar: user selector, date range, entity type
       - Loading state: skeleton rows
       - Empty state: "Aucun accès enregistré" per UI-SPEC
       - Error state: retry
       - Expandable detail panel: IP address, request path, timestamp, changes made

    5. Create `apps/dashboard/app/(dashboard)/conformite/hapdp/registre/page.tsx`:
       - "use client" page component
       - PageHeader: "Registre des traitements"
       - Fetches data from GET /api/compliance/registre/entries
       - Renders processing-register-table with filter bar and export buttons
       - All states: loading → error → empty → filtered → data

    All French text per UI-SPEC copywriting. Follow existing dashboard patterns.
  </action>
  <acceptance_criteria>
    - processing-register-table.tsx renders searchable table with event type, entity, action, user, date columns
    - subject-access-portal.tsx implements 3-step OTP flow: email input → OTP verification → data view → rectify/delete
    - subject-access-portal.tsx handles all states: loading (spinner), error ("Code incorrect"), empty ("Aucune donnée")
    - portail/page.tsx is public (no sidebar, minimal layout)
    - access-traceability-log.tsx renders expandable audit table with filters
    - registre/page.tsx fetches and displays processing register
    - All text in French per UI-SPEC
    - `pnpm build` passes (or `npx next build` from dashboard)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/dashboard && npx next build 2>&1 | grep -E "Error|✓|✗|Failed" | head -20</automated>
  </verify>
  <done>
    Processing register, subject access portal, and access traceability components created. All states handled with full French localization per UI-SPEC.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard→API | All analytics and compliance dashboard pages communicate via fetchWithAuth() |
| Public→SubjectAccess | Subject access portal is public (no auth) — communicates via plain fetch() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-23 | Information Disclosure | Analytics data — cross-org exposure | mitigate | All fetch calls use fetchWithAuth which includes JWT. TenantIsolationGuard enforces org scoping server-side. |
| T-04-24 | Spoofing | Subject access portal — fake email entry | mitigate | OTP sent to provided email. No personal data returned without valid OTP. Rate-limited per email. |
| T-04-25 | Tampering | Wizard localStorage — data corruption | mitigate | localStorage used for UX persistence only. Server-side validation on final submission. Wizard state validated before PDF generation. |

</threat_model>

<verification>
- `npx next build` from dashboard directory passes (or `pnpm build` from root)
- All components implement loading/error/empty states
- French text used for all labels
- Subject access portal is accessible without authentication
</verification>

<success_criteria>
1. Analytics dashboard shows 5 KPI cards (incidents, alerts, cameras, storage, entries) with animated values
2. Trend charts toggle between 7d and 30d with Recharts LineChart
3. Advanced search filters by date, site, event type, person with paginated results
4. CSV and PDF export trigger browser download
5. Report schedule modal saves configuration with toast confirmation
6. HAPDP wizard progresses through 6 steps with sidebar indicators and per-step validation
7. localStorage persists wizard state across page refreshes
8. Declaration form auto-fills org info and generates PDF on submit
9. Consent signage generator selects camera and generates printable PDF
10. Subject access portal: email → OTP → view data → request rectify/delete
11. Processing register: searchable table with CSV/PDF export
12. Access traceability: expandable audit log with filters
</success_criteria>

<output>
Create `.planning/phases/04-bastion-enterprise/04-05-SUMMARY.md` when done
</output>
