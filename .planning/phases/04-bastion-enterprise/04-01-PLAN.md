---
phase: 04-bastion-enterprise
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - packages/shared/src/schemas/compliance.schema.ts
  - packages/shared/src/schemas/storage.schema.ts
  - packages/shared/src/schemas/integration.schema.ts
  - packages/shared/src/types/compliance.types.ts
  - packages/shared/src/types/storage.types.ts
  - packages/shared/src/types/integration.types.ts
  - packages/shared/src/constants/bastion-event-types.ts
  - packages/shared/src/constants/feature-keys.ts
  - packages/shared/src/index.ts
  - apps/api/src/modules/pseudonymization/pseudonymization.service.ts
  - apps/api/src/modules/pseudonymization/pseudonymization.interceptor.ts
  - apps/api/src/modules/pseudonymization/pseudonymization.module.ts
  - apps/api/src/common/decorators/feature-gate.decorator.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements:
  - BAS-25
  - BAS-26
  - BAS-27
  - BAS-28
  - BAS-29
  - BAS-30
  - BAS-31
  - BAS-32
  - BAS-33
  - BAS-34
  - BAS-35
  - BAS-41
  - BAS-42
  - BAS-43
  - BAS-44
must_haves:
  truths:
    - "Prisma schema has new models: ProcessingRecord, SubjectAccessRequest, ForensicEvidence, BackupConfig, BackupJob, IntegrationEndpoint, ConsentSignage"
    - "Organization model has pseudonymizationEnabled toggle and nasConfig JSON"
    - "RetentionPolicy model has siteId and eventType fields"
    - "Zod schemas exist for all Phase 4 endpoints"
    - "BASTION_EVENT_TYPES constant is exported from shared package"
    - "Sharp library is installed in the API package"
    - "Pseudonymization interceptor blurs snapshot images for non-privileged roles"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "All Phase 4 data models"
      contains: "ProcessingRecord, SubjectAccessRequest, ForensicEvidence, BackupConfig, BackupJob, IntegrationEndpoint, ConsentSignage models + RetentionPolicy/Organization extensions"
    - path: "packages/shared/src/"
      provides: "Shared schemas, types, constants for Phase 4"
      contains: "compliance.schema.ts, storage.schema.ts, integration.schema.ts, bastion-event-types.ts"
    - path: "apps/api/src/modules/pseudonymization/"
      provides: "On-the-fly face blurring via sharp"
      contains: "pseudonymization.service.ts, pseudonymization.interceptor.ts, pseudonymization.module.ts"
  key_links:
    - from: "pseudonymization.interceptor.ts"
      to: "@fastify/static snapshot serving"
      via: "NestInterceptor wrapping response to blur images"
    - from: "pseudonymization.service.ts"
      to: "sharp npm package"
      via: "sharp(imageBuffer).blur(15).toBuffer()"
    - from: "packages/shared/src/schemas/compliance.schema.ts"
      to: "apps/api/src/modules/compliance/compliance.controller.ts"
      via: "ZodValidationPipe in HAPDP endpoints"
---

<objective>
Create all foundational data models, shared types/schemas/constants, and the pseudonymization face-blurring pipeline for Phase 4.

Purpose: Every downstream Phase 4 plan depends on these models and schemas. The pseudonymization feature (BAS-33 — faces blurred by default) requires the sharp library and an interceptor at the NestJS API layer. This plan ensures all downstream plans have the contracts and infrastructure they need.
Output: Updated Prisma schema with 7 new models + 2 extensions, new shared package schemas/types/constants, sharp installation, pseudonymization module with service + interceptor.
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-bastion-enterprise/04-CONTEXT.md
@.planning/phases/04-bastion-enterprise/04-RESEARCH.md
@.planning/phases/04-bastion-enterprise/04-PATTERNS.md
@.planning/phases/03-bastion-ai-access-control/03-02-SUMMARY.md

<interfaces>
From existing schema.prisma (verified in PATTERNS.md):
- Organization model: id, name, displayName, isActive, parentOrganizationId, parent, children, maxSites, planTier (to remove), createdAt, updatedAt
- RetentionPolicy model: id, organizationId, eventType, tableType, retentionDays, enabled, exportBeforePurge, exportFormat, createdAt, updatedAt
- Recordings model: id, organizationId, cameraId, startedAt, endedAt, filePath, sizeBytes, codec, status

From existing feature-gate.decorator.ts:
- @RequiresPack(pack: "VISION" | "BASTION")
- @RequiresModule(moduleKey: string) — module keys pre-seeded at org creation
- FeatureGateGuard uses FeatureGateService to check pack/module

From existing packages/shared/src/constants/credential-types.ts:
- Pattern: export const CREDENTIAL_TYPES = { ... } as const; export type CredentialType = (typeof CREDENTIAL_TYPES)[keyof typeof CREDENTIAL_TYPES];

From existing packages/shared/src/schemas/access.schema.ts:
- Pattern: import { z } from "zod"; export const createCameraSchema = z.object({ ... }); export type CreateCameraInput = z.infer<typeof createCameraSchema>;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Prisma schema with Phase 4 models</name>
  <files>
    apps/api/prisma/schema.prisma (MODIFY)
  </files>
  <read_first>
    apps/api/prisma/schema.prisma
  </read_first>
  <action>
    Extend `apps/api/prisma/schema.prisma` with 7 new models and 2 existing model extensions:

    1. Add `ProcessingRecord` model for HAPDP processing register (BAS-31):
       - id: String @id @default(uuid())
       - organizationId: String
       - eventType: String (e.g., "ACCESS_GRANTED", "ALERT_CREATED")
       - entityType: String (e.g., "incident", "alert", "access_event")
       - entityId: String
       - action: String (e.g., "CREATE", "VIEW", "UPDATE")
       - performedById: String?
       - performedAt: DateTime @default(now())
       - metadata: Json?
       - createdAt: DateTime @default(now())
       - Relations: organization Organization @relation(fields: [organizationId], references: [id])
       - Indexes: @@index([organizationId]), @@index([eventType])
       - Pattern: follows existing model conventions with @id @default(uuid()) and relation fields

    2. Add `SubjectAccessRequest` model for HAPDP subject access (BAS-34):
       - id, organizationId, email, requestType ("VIEW" | "RECTIFY" | "DELETE"), status ("PENDING" | "APPROVED" | "REJECTED" | "COMPLETED"), details (String?), otpCode (String?), otpExpiresAt (DateTime?), adminNote (String?), createdById (String?), approvedById (String?), approvedAt (DateTime?), createdAt, updatedAt
       - Relations: organization Organization
       - Indexes: @@index([email]), @@index([organizationId])

    3. Add `ForensicEvidence` model for certified evidence (BAS-27):
       - id, eventId, organizationId, mediaType ("zip" | "clip"), zipPath, tsaCertPath, hash, sizeBytes (Int?), createdAt
       - Relations: organization Organization
       - Indexes: @@index([organizationId])

    4. Add `BackupConfig` model for NAS auto-backup config (BAS-29):
       - id, organizationId (@unique), targetPath, username (String?), password (String?), schedule (String, default "daily"), enabled (Boolean, default false), lastBackupAt (DateTime?), lastStatus (String?), createdAt, updatedAt
       - Relations: organization Organization

    5. Add `BackupJob` model for backup execution logs (BAS-29):
       - id, backupConfigId, organizationId, status ("RUNNING" | "SUCCESS" | "FAILED"), sizeBytes (BigInt?), error (String?), startedAt, completedAt (DateTime?)
       - Relations: backupConfig BackupConfig, organization Organization
       - Indexes: @@index([backupConfigId])

    6. Add `IntegrationEndpoint` model for fire alarm/BMS config (BAS-43, BAS-44):
       - id, organizationId, type ("fire_alarm" | "bms"), name, isActive (Boolean, default true), config (Json), lastEventAt (DateTime?), createdAt, updatedAt
       - Relations: organization Organization
       - @@unique([organizationId, type])

    7. Add `ConsentSignage` model for camera consent signage (BAS-32):
       - id, organizationId, cameraId, siteName, pdfPath, generatedAt (DateTime @default(now())), generatedById
       - Relations: organization Organization
       - Indexes: @@index([cameraId])

    8. Extend `RetentionPolicy` model (BAS-26):
       - Add: siteId String? (nullable — null = global policy, non-null = per-site)
       - Add: @@index([siteId])

    9. Extend `Organization` model (BAS-33):
       - Add: pseudonymizationEnabled Boolean @default(false)
       - Add: nasConfig Json? (backup NAS target configuration)

    Run `npx prisma validate` after modifications to verify schema is valid. Do NOT run `prisma migrate` — migration is handled separately.
  </action>
  <acceptance_criteria>
    - All 7 new models exist in schema.prisma with correct fields, relations, and indexes
    - RetentionPolicy has siteId String? field
    - Organization has pseudonymizationEnabled Boolean and nasConfig Json? fields
    - `npx prisma validate` passes in apps/api directory
    - Every model has proper organization relation for multi-tenancy
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx prisma validate 2>&1 | grep -E "Your Prisma schema is valid|Error"</automated>
  </verify>
  <done>
    Prisma schema extended with all Phase 4 models and validated successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create shared package schemas, types, constants, and event types</name>
  <files>
    packages/shared/src/schemas/compliance.schema.ts (NEW)
    packages/shared/src/schemas/storage.schema.ts (NEW)
    packages/shared/src/schemas/integration.schema.ts (NEW)
    packages/shared/src/types/compliance.types.ts (NEW)
    packages/shared/src/types/storage.types.ts (NEW)
    packages/shared/src/types/integration.types.ts (NEW)
    packages/shared/src/constants/bastion-event-types.ts (NEW)
    packages/shared/src/constants/feature-keys.ts (MODIFY)
    packages/shared/src/index.ts (MODIFY)
  </files>
  <read_first>
    packages/shared/src/schemas/access.schema.ts
    packages/shared/src/types/access.types.ts
    packages/shared/src/constants/credential-types.ts
    packages/shared/src/constants/feature-keys.ts
    packages/shared/src/index.ts
  </read_first>
  <action>
    Create shared package files for Phase 4. Follow existing patterns exactly — Zod schemas use `z.object({})` with `.optional()` and `.describe()` for fields. Types use `interface` for object shapes. Constants use `as const` with `UPPER_SNAKE_CASE`.

    1. Create `packages/shared/src/schemas/compliance.schema.ts`:
       - Export Zod schemas for HAPDP endpoints:
         - `hapdpDeclarationSchema`: z.object with fields for processing types (array of strings), organization details (name, address, siret, representative), declaration date, signature. All fields validated with min length.
         - `consentSignageSchema`: z.object with cameraId (z.string().uuid()), siteName (z.string().min(1))
         - `subjectAccessOtpSchema`: z.object({ email: z.string().email() })
         - `subjectAccessVerifySchema`: z.object({ email: z.string().email(), code: z.string().length(6) })
         - `subjectAccessRequestSchema`: z.object({ email: z.string().email(), type: z.enum(["rectify", "delete"]), details: z.string().optional() })
       - Export infer types: `type HapdpDeclarationInput = z.infer<typeof hapdpDeclarationSchema>` etc.
       - Follow same pattern as `access.schema.ts`

    2. Create `packages/shared/src/schemas/storage.schema.ts`:
       - `retentionPolicySchema`: z.object with siteId (z.string().uuid().optional()), eventType, tableType, retentionDays (z.number().int().min(30).max(730)), exportBeforePurge (z.boolean().optional()), exportFormat (z.enum(["CSV","PDF"]).optional())
       - `forensicEvidenceSchema`: z.object with eventId (z.string().uuid()), mediaType (z.enum(["zip", "clip"])), metadata (z.record(z.any()).optional())
       - `backupConfigSchema`: z.object with targetPath (z.string().min(3)), username (z.string().optional()), password (z.string().optional()), schedule (z.enum(["daily", "weekly"]))

    3. Create `packages/shared/src/schemas/integration.schema.ts`:
       - `fireAlarmSchema`: z.object with siteId (z.string()), zone (z.string()), sensorId (z.string()), severity (z.enum(["INFO","WARNING","CRITICAL"])), message (z.string().optional()), timestamp (z.string().datetime().optional())
       - `bmsEventSchema`: z.object with siteId (z.string()), zone (z.string()), eventType (z.enum(["hvac_temperature","hvac_humidity","emergency_lighting","fire_door_release"])), value (z.number().optional()), unit (z.string().optional()), message (z.string().optional()), timestamp (z.string().datetime().optional())

    4. Create `packages/shared/src/types/compliance.types.ts`:
       - Interface `HapdpDeclarationDto`: extends HapdpDeclarationInput with generatedAt, referenceNumber
       - Interface `SubjectAccessRequestDto`: id, email, requestType, status, details, adminNote, createdAt, updatedAt
       - Interface `SubjectDataDto`: email, name, role, sites (string[]), lastAccess, relatedAlerts (number), relatedFaces (number), dataItems (array of { type, description, date })

    5. Create `packages/shared/src/types/storage.types.ts`:
       - Interface `RetentionPolicyDto`: id, siteId, eventType, tableType, retentionDays, enabled, exportBeforePurge, exportFormat
       - Interface `ForensicEvidenceDto`: id, eventId, mediaType, zipPath, tsaCertPath, hash, sizeBytes, createdAt
       - Interface `BackupConfigDto`: id, targetPath, schedule, enabled, lastBackupAt, lastStatus
       - Interface `BackupJobDto`: id, status, sizeBytes, error, startedAt, completedAt
       - Interface `BastionKpisDto` (D-02): incidentsToday (number), activeAlerts (number), camerasOnline (number), storageUsedBytes (number), entriesToday (number)

    6. Create `packages/shared/src/constants/bastion-event-types.ts`:
       - Export `BASTION_EVENT_TYPES` as const object with keys:
         - ALERT_CREATED: "bastion.alert_created", ALERT_RESOLVED: "bastion.alert_resolved"
         - ACCESS_GRANTED: "bastion.access_granted", ACCESS_DENIED: "bastion.access_denied", DOOR_FORCED: "bastion.door_forced"
         - AI_DETECTION: "bastion.ai_detection", FACE_MATCH: "bastion.face_match", WEAPON_DETECTED: "bastion.weapon_detected"
         - COMPLIANCE_EVENT: "bastion.compliance_event", SUBJECT_ACCESS_REQUEST: "bastion.subject_access_request"
         - FIRE_ALARM: "bastion.fire_alarm", BMS_EVENT: "bastion.bms_event"
         - REPORT_READY: "bastion.report_ready"
         - BACKUP_COMPLETED: "bastion.backup_completed", BACKUP_FAILED: "bastion.backup_failed"
       - Export type `BastionEventType = (typeof BASTION_EVENT_TYPES)[keyof typeof BASTION_EVENT_TYPES]`

    7. Extend `packages/shared/src/constants/feature-keys.ts`:
       - Add BASTION sub-feature module keys: HAPDP, BASTION_REPORTS, BASTION_STORAGE, BASTION_API, BASTION_INTEGRATIONS
       - Follow existing pattern: `export const BASTION_MODULE_KEYS = { HAPDP: "hapdp", ... } as const`

    8. Extend `packages/shared/src/index.ts`:
       - Add export lines: `export * from "./schemas/compliance.schema"`, `export * from "./schemas/storage.schema"`, `export * from "./schemas/integration.schema"`, `export * from "./types/compliance.types"`, `export * from "./types/storage.types"`, `export * from "./types/integration.types"`, `export * from "./constants/bastion-event-types"`
       - Follow existing barrel export pattern with section headers

    Run `pnpm tsc --noEmit` from repo root to verify type compilation.
  </action>
  <acceptance_criteria>
    - All 3 new schema files exist with correct Zod schemas per CONTEXT.md D-02, D-12
    - All 3 new type files exist with interfaces matching the DTO contracts
    - BASTION_EVENT_TYPES constant exported with all 16 event type strings
    - feature-keys.ts has BASTION sub-feature module keys
    - index.ts exports all new modules
    - `pnpm tsc --noEmit` passes for shared package
  </acceptance_criteria>
  <verify>
    <automated>cd packages/shared && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    Shared package extended with Phase 4 schemas, types, constants, and event types. Type compilation passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Install sharp and create pseudonymization module for face blurring</name>
  <files>
    apps/api/package.json (MODIFY — add sharp dependency)
    apps/api/src/modules/pseudonymization/pseudonymization.service.ts (NEW)
    apps/api/src/modules/pseudonymization/pseudonymization.interceptor.ts (NEW)
    apps/api/src/modules/pseudonymization/pseudonymization.module.ts (NEW)
    apps/api/src/common/decorators/feature-gate.decorator.ts (MODIFY)
    apps/api/src/app.module.ts (MODIFY)
  </files>
  <read_first>
    apps/api/package.json
    apps/api/src/modules/audit/audit.interceptor.ts
    apps/api/src/common/decorators/feature-gate.decorator.ts
    apps/api/src/app.module.ts
  </read_first>
  <action>
    Install sharp and create pseudonymization infrastructure per D-06 (face blurring at data layer):

    1. Install sharp: `cd apps/api && pnpm add sharp` (sharp 0.35.3 is the version on npm)
       - Per RESEARCH.md Package Legitimacy Audit: sharp is Approved (40M weekly downloads, 12+ years)

    2. Create `pseudonymization/pseudonymization.module.ts`:
       - Simple @Module with imports: [PrismaModule] (global), providers: [PseudonymizationService], exports: [PseudonymizationService]
       - If APP_INTERCEPTOR needed, register PseudonymizationInterceptor globally

    3. Create `pseudonymization/pseudonymization.service.ts`:
       - Inject PrismaService in constructor
       - `async blurSnapshot(filename: string, snapshotDir: string): Promise<Buffer>`:
         - Read file from `path.join(snapshotDir, filename)`
         - Apply sharp(imageBuffer).blur(15).toBuffer() — strong Gaussian blur per RESEARCH.md Pattern 4
         - Cache blurred result in `/tmp/blur-cache/{filename}` with 5-minute TTL (check mtime on cache)
         - Return blurred buffer
       - `async isPseudonymizationEnabled(orgId: string): Promise<boolean>`:
         - Query Organization.pseudonymizationEnabled for the org
         - Return false if org not found

    4. Create `pseudonymization/pseudonymization.interceptor.ts`:
       - Implement NestInterceptor interface
       - In intercept() method:
         - Extract request user and orgId from context.switchToHttp().getRequest()
         - Skip blur if user role is ADMIN, SUPERVISOR, SUPER_ADMIN, or GLOBAL_ADMIN (D-06: full resolution with explicit role)
         - Call pseudonymizationService.isPseudonymizationEnabled(orgId)
         - If enabled and role not privileged: wrap response using rxjs pipe with switchMap
         - For image responses: modify the response to apply blur via sharp before sending
         - Log access via AuditService: `action: "VIEW_SNAPSHOT_PSEUDONYMIZED"` per BAS-35
       - Import: Injectable, NestInterceptor, ExecutionContext, CallHandler from @nestjs/common; Observable, from, switchMap from rxjs

    5. Extend `apps/api/src/common/decorators/feature-gate.decorator.ts`:
       - Add BASTION feature module keys for Phase 4: `HAPDP`, `BASTION_REPORTS`, `BASTION_STORAGE`, `BASTION_API`, `BASTION_INTEGRATIONS`
       - Follow existing pattern for BASTION_MODULE_KEYS export

    6. Register `PseudonymizationModule` in `apps/api/src/app.module.ts`:
       - Add PseudonymizationModule to the imports array

    Verify compilation: `cd apps/api && npx nest build` passes.
  </action>
  <acceptance_criteria>
    - sharp installed in apps/api/package.json (verify with `pnpm ls sharp` or check package.json)
    - `pseudonymization.service.ts` exists with blurSnapshot() and isPseudonymizationEnabled() methods
    - `pseudonymization.interceptor.ts` exists and implements NestInterceptor
    - Blur is applied for non-ADMIN/SUPERVISOR roles when pseudonymizationEnabled=true per D-06
    - Full resolution accessible for ADMIN, SUPERVISOR, SUPER_ADMIN, GLOBAL_ADMIN roles
    - Access to full-resolution images is logged via AuditService
    - feature-gate.decorator.ts has Phase 4 BASTION module keys
    - PseudonymizationModule registered in AppModule
    - `npx nest build` passes
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx nest build 2>&1 | grep -E "Error|Nest successfully|ERROR" | head -10</automated>
  </verify>
  <done>
    Sharp installed. Pseudonymization module created with on-the-fly face blurring interceptor. Blur applied per D-06 for non-privileged roles. Compilation passes.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | All Phase 4 endpoints accessed via JWT-authenticated HTTP (except public subject access routes and incoming webhooks) |
| API→Sharp | Local in-process image processing for face blurring |
| API→Prisma | Database operations for all Phase 4 data models |
| API→Filesystem | Snapshot serving via @fastify/static, pseudonymization cache in /tmp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Prisma schema — model integrity | mitigate | All models have organizationId relation for tenant isolation. Cascade deletes protected by Prisma referential actions. |
| T-04-02 | Information Disclosure | Pseudonymization bypass | mitigate | PseudonymizationInterceptor checks user role server-side. Only ADMIN/SUPERVISOR/SUPER_ADMIN/GLOBAL_ADMIN bypass blur per D-06. |
| T-04-03 | Repudiation | Face blur bypass access | mitigate | All full-resolution snapshot accesses logged via AuditService with VIEW_SNAPSHOT_FULL action per BAS-35. |
| T-04-04 | Tampering | Sharp arbitrary file read | mitigate | Snapshots served from restricted directory only. filename validated to prevent path traversal. Sharp receives only validated paths. |
| T-04-05 | Spoofing | Subject access portal — OTP brute force | mitigate | 6-digit OTP with 15-min TTL. Rate limit: max 3 attempts per email per hour. (Enforced in Plan 03 SubjectAccessService.) |
| T-04-SC | Tampering | npm install — sharp package | mitigate | sharp is [APPROVED] per Package Legitimacy Audit (40M weekly downloads, 12+ years, MIT license, maintained by Lovell). No checkpoint needed. |

</threat_model>

<verification>
- `npx prisma validate` passes
- `pnpm tsc --noEmit` passes for packages/shared
- `npx nest build` passes for apps/api
- PseudonymizationInterceptor correctly blurs for non-privileged roles
</verification>

<success_criteria>
1. Prisma schema has all 7 new models + RetentionPolicy siteId + Organization pseudonymizationEnabled/nasConfig
2. Shared package exports all Phase 4 Zod schemas, types, and BASTION_EVENT_TYPES
3. Sharp installed, PseudonymizationModule registered, interceptor blurs snapshots per D-06
4. All compilations pass
</success_criteria>

<output>
Create `.planning/phases/04-bastion-enterprise/04-01-SUMMARY.md` when done
</output>
