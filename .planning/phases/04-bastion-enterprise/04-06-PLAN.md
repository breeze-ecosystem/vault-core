---
phase: 04-bastion-enterprise
plan: 06
type: execute
wave: 3
depends_on:
  - 04-04-PLAN
files_modified:
  - apps/dashboard/app/(dashboard)/parametres/retention/page.tsx
  - apps/dashboard/app/(dashboard)/parametres/sauvegarde/page.tsx
  - apps/dashboard/components/retention-config-form.tsx
  - apps/dashboard/components/forensic-evidence-export.tsx
  - apps/dashboard/components/backup-config-form.tsx
  - apps/dashboard/components/backup-status-card.tsx
autonomous: true
requirements:
  - BAS-26
  - BAS-27
  - BAS-29
must_haves:
  truths:
    - "Retention configuration page shows per-site and per-event-type sliders"
    - "Retention settings can be saved with toast confirmation"
    - "Forensic evidence export modal has event selection, format choice, and certification trigger"
    - "Certification progress shown with animated indicator"
    - "Backup configuration form has target path, schedule, and credentials fields"
    - "Test connection button verifies NAS accessibility"
    - "Manual backup trigger starts backup and shows progress"
    - "Backup status card shows last backup, next backup, and health status"
    - "All components handle loading, empty, error states per UI-SPEC"
  artifacts:
    - path: "apps/dashboard/app/(dashboard)/parametres/retention/page.tsx"
      provides: "Per-site/per-event retention configuration interface"
    - path: "apps/dashboard/app/(dashboard)/parametres/sauvegarde/page.tsx"
      provides: "Auto-backup configuration with test connection and manual trigger"
    - path: "apps/dashboard/components/retention-config-form.tsx"
      provides: "Retention policy editor with site/event scope selectors"
    - path: "apps/dashboard/components/forensic-evidence-export.tsx"
      provides: "Certified evidence export modal with progress tracking"
    - path: "apps/dashboard/components/backup-config-form.tsx"
      provides: "NAS backup configuration form"
    - path: "apps/dashboard/components/backup-status-card.tsx"
      provides: "Backup health dashboard card"
  key_links:
    - from: "parametres/retention/page.tsx"
      to: "retention-config-form.tsx + fetchRetentionPolicies()"
      via: "CRUD API client functions for per-site retention policies"
    - from: "parametres/sauvegarde/page.tsx"
      to: "backup-config-form.tsx + backup-status-card.tsx"
      via: "backup API client functions (getConfig, saveConfig, test, run)"
    - from: "forensic-evidence-export.tsx"
      to: "certifyEvidence() API call"
      via: "BullMQ async job with progress polling or Socket.IO event"
---

<objective>
Build advanced storage and archiving dashboard pages — retention configuration, forensic evidence export, and auto-backup management.

Purpose: BAS-26, BAS-27, and BAS-29 require dashboard UIs for configuring per-site/per-event retention, exporting certified forensic evidence, and managing NAS auto-backup. These pages and components make the backend capabilities (Plan 04) accessible to operators.
Output: Retention config page, backup config page, 4 new components (retention-config-form, forensic-evidence-export, backup-config-form, backup-status-card).
</objective>

<execution_context>
@/home/devuser/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/devuser/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-bastion-enterprise/04-CONTEXT.md
@.planning/phases/04-bastion-enterprise/04-UI-SPEC.md
@.planning/phases/04-bastion-enterprise/04-PATTERNS.md
@.planning/phases/03-bastion-ai-access-control/03-05-SUMMARY.md

<interfaces>
Dashboard page pattern (PATTERNS.md lines 1400-1492):
- "use client" → useState/useEffect → fetchWithAuth → skeleton loading → error state → empty state → data render
- All pages follow this exact pattern with French French text

Existing analog components (PATTERNS.md tables):
- retention-config-form.tsx → analog: recording-settings-form.tsx (100% match)
- backup-config-form.tsx → analog: recording-settings-form.tsx (100% match)
- backup-status-card.tsx → analog: stats-card.tsx (100% match)
- forensic-evidence-export.tsx → analog: face-upload-dropzone.tsx (partial match)

Existing UI patterns:
- GlassCard for config panels
- Switch for toggle settings
- Select for dropdown options
- Input for text fields
- Button for CTAs
- Progress for progress bars
- ConfirmationDialog for destructive actions
- Toast for success/error feedback

From UI-SPEC:
- Colors: storage usage normal (<80%) = primary, warning (80-90%) = warning, critical (>90%) = destructive
- Backup: healthy = success green, failed = destructive red + glow
- Load states: skeleton, shimmer, progress bar with status text
- Empty states with specific French heading + body + CTA per UI-SPEC table
- Error states with retry per UI-SPEC table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create retention configuration page and forensic evidence export component</name>
  <files>
    apps/dashboard/app/(dashboard)/parametres/retention/page.tsx (NEW)
    apps/dashboard/components/retention-config-form.tsx (NEW)
    apps/dashboard/components/forensic-evidence-export.tsx (NEW)
  </files>
  <read_first>
    apps/dashboard/components/recording-settings-form.tsx
    apps/dashboard/app/(dashboard)/parametres/enregistrement/page.tsx
    apps/dashboard/components/stats-card.tsx
    apps/dashboard/components/ui/select.tsx
  </read_first>
  <action>
    Create the advanced retention configuration page and forensic evidence export component per UI-SPEC.

    1. Create `components/retention-config-form.tsx`:
       - Props: { data: RetentionPolicyDto[]; sites: { id: string; name: string }[]; eventTypes: string[]; onSave: (policies: RetentionPolicyDto[]) => Promise<void> }
       - Form with per-site sections:
         - Site selector (Select component) — "Tous les sites" option for global
         - For selected site: list of event types with retention sliders:
           - Retention period slider/select: values from 30d to 730d (1yr+)
           - Export avant purge: Switch toggle
           - Format d'export: Select (CSV/PDF)
         - Add rule button: "Ajouter une règle" (new row for additional event type)
       - Loading state: skeleton form matching layout (Skeleton for each row)
       - Empty state (no sites): "Aucune configuration de rétention" with "Configurer la rétention" CTA per UI-SPEC
       - Error state: retry button
       - Save: "Enregistrer" button → onSave → toast "Configuration de rétention enregistrée"
       - All labels and options in French per UI-SPEC

    2. Create `components/forensic-evidence-export.tsx`:
       - Props: { eventId: string; eventType: string; onComplete: (evidenceId: string) => void }
       - Modal/dialog component with steps:
         - Step 1: Select format (ZIP / Clip vidéo) via radio buttons
         - Step 2: Metadata form (optional notes, case reference)
         - Step 3: "Exporter comme preuve judiciaire" CTA
         - During certification: animated progress bar with pulsing cert icon
         - On complete: success message + "Télécharger la preuve certifiée" button + TSA certificate info
         - On error: "Échec de la certification" with "Réessayer" option per UI-SPEC
       - Loading state: form skeleton
       - Error state: "Échec de la certification" with retry
       - Empty state: "Aucun événement sélectionné" if no eventId provided
       - French text per UI-SPEC copywriting

    3. Create `apps/dashboard/app/(dashboard)/parametres/retention/page.tsx`:
       - "use client" page component
       - PageHeader: "Rétention avancée" with description
       - Data fetching: fetch sites + fetch retention policies from APIs
       - Renders retention-config-form with loaded data
       - Loading state: skeleton page
       - Error state: "Échec du chargement des paramètres de rétention" with retry per UI-SPEC
       - Empty state (no sites configured): "Aucune configuration de rétention" with CTA

    Follow existing dashboard page patterns exactly. All French per UI-SPEC.
  </action>
  <acceptance_criteria>
    - retention-config-form.tsx renders per-site sections with event type rows and retention sliders (30d-730d)
    - Form supports "Tous les sites" global selector and individual site selectors
    - Save triggers onSave callback with toast confirmation per UI-SPEC
    - forensic-evidence-export.tsx renders as modal with format selection, metadata, certification progress, and download
    - paramatres/retention/page.tsx renders with loading/empty/error states
    - All text in French per UI-SPEC
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>cd apps/dashboard && npx next build 2>&1 | grep -E "Error|✓|✗|Failed" | head -20</automated>
  </verify>
  <done>
    Advanced retention configuration page and forensic evidence export component created. All states handled.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create backup configuration page with form, status card, and test connection</name>
  <files>
    apps/dashboard/app/(dashboard)/parametres/sauvegarde/page.tsx (NEW)
    apps/dashboard/components/backup-config-form.tsx (NEW)
    apps/dashboard/components/backup-status-card.tsx (NEW)
  </files>
  <read_first>
    apps/dashboard/components/recording-settings-form.tsx
    apps/dashboard/components/stats-card.tsx
    apps/dashboard/components/glass-card.tsx
    apps/dashboard/components/ui/progress.tsx
  </read_first>
  <action>
    Create the auto-backup configuration page per UI-SPEC §Interaction Pattern 4 (Save + Test pattern).

    1. Create `components/backup-config-form.tsx`:
       - Props: { config: BackupConfigDto | null; onSave: (config: BackupConfigInput) => Promise<void>; onTest: (config: BackupConfigInput) => Promise<{ success: boolean; message: string }>; onRunBackup: () => Promise<void> }
       - Form fields:
         - Chemin NAS: Input for targetPath (e.g., "//192.168.1.100/backup")
         - Nom d'utilisateur: Input for NAS username (optional)
         - Mot de passe: Password input for NAS password (optional)
         - Planification: Select (Quotidienne / Hebdomadaire)
         - Activer la sauvegarde: Switch toggle
       - Buttons:
         - "Tester la connexion" → calls onTest with current form values → shows result (toast success "NAS accessible" / error "Connexion impossible")
         - "Enregistrer" → calls onSave → toast "Configuration de sauvegarde enregistrée"
         - "Lancer une sauvegarde" → calls onRunBackup → shows progress bar (Progress component) + "Sauvegarde en cours — {percent}%"
       - Loading state: skeleton form
       - Empty state (no config): form empty with default values
       - Error state: inline validation errors per field
       - All French labels per UI-SPEC copywriting

    2. Create `components/backup-status-card.tsx`:
       - Props: { status: BackupJobDto | null; loading?: boolean; error?: string | null; onRetry?: () => void }
       - Displays:
         - Status icon: green checkmark (healthy) / red X (failed) / amber warning per UI-SPEC
         - Last backup date/time (formatted in French)
         - Next scheduled backup date/time
         - Data size of last backup
         - Health indicator: "Sauvegarde {status}" label
       - Loading state: skeleton card
       - Error state: retry option
       - Empty state: "Sauvegarde non configurée" with "Configurer la sauvegarde" CTA per UI-SPEC

    3. Create `apps/dashboard/app/(dashboard)/parametres/sauvegarde/page.tsx`:
       - "use client" page component
       - PageHeader: "Sauvegarde" with description
       - Layout: backup-status-card at top, backup-config-form below
       - Data fetching: fetch backup config + fetch backup jobs
       - "Lancer une sauvegarde" button also available from page level
       - Loading state: skeleton page
       - Error state: "Échec du chargement" with retry per UI-SPEC
       - Empty state: "Sauvegarde non configurée" with CTA per UI-SPEC

    Follow existing dashboard patterns. All French per UI-SPEC copywriting.
  </action>
  <acceptance_criteria>
    - backup-config-form.tsx renders all form fields (path, username, password, schedule, toggle)
    - Test connection button calls onTest and shows toast result per UI-SPEC save+test pattern
    - Save button calls onSave with toast confirmation
    - Manual backup shows progress bar with percentage
    - backup-status-card.tsx shows last backup, next backup, status icon, data size
    - Status colors follow UI-SPEC (green=healthy, red=failed+glow, amber=warning)
    - paramatres/sauvegarde/page.tsx renders with loading/empty/error states
    - All text in French per UI-SPEC
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>cd apps/dashboard && npx next build 2>&1 | grep -E "Error|✓|✗|Failed" | head -20</automated>
  </verify>
  <done>
    Backup configuration page created with form, test connection, manual trigger, and status card. All states handled.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard→API | Storage configuration pages communicate via fetchWithAuth() (JWT-authenticated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-26 | Information Disclosure | Backup password in frontend | mitigate | Password field uses type="password". Password masked in UI. Never displayed in response (GET /config doesn't return password). |
| T-04-27 | Tampering | Retention config — accidental mass deletion | mitigate | All retention changes saved via POST with server-side validation. Confirmation dialog for destructive retention changes. |
| T-04-28 | Denial of Service | Forensic evidence — rapid repeated exports | mitigate | Frontend rate-limits export triggers. BullMQ queue handles concurrency server-side. Progress UI prevents double-click. |

</threat_model>

<verification>
- `npx next build` from dashboard passes
- Retention form loads sites and policies, saves with toast confirmation
- Backup form tests connection and shows result
- Forensic export modal shows progress and completion with download
- All components handle loading/error/empty states
- French text for all labels
</verification>

<success_criteria>
1. Retention page shows per-site section with event type rows, each with 30d-730d retention slider
2. Retention save triggers toast "Configuration de rétention enregistrée"
3. Forensic evidence modal: format selection → metadata → certification progress → download
4. Certification progress uses animated pulsing icon per UI-SPEC
5. Backup form saves configuration with toast
6. Test connection returns success/failure toast per UI-SPEC pattern
7. Manual backup shows progress bar with percentage
8. Backup status card shows last/next backup with color-coded health indicator
9. Loading states use skeleton placeholders matching final layout
10. Empty states show per-UI-SPEC French copy with CTA buttons
</success_criteria>

<output>
Create `.planning/phases/04-bastion-enterprise/04-06-SUMMARY.md` when done
</output>
